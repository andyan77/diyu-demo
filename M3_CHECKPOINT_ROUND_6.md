# M3 Checkpoint 第 6 轮 · 载体 v1.2（修 A/B/D/E + 链式投影 + 盲评方法）

> `task_id` = `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`（不变，未建新根任务）
> 权威事件：Founder 2026-08-26 `CONTINUE_TASK`（七条）
> 判据与绑定冻结于 `6bbad98`，**全部正式取证运行发生在它之后**；提交顺序即时序自证。

---

## 1. 一句话结论

**你让我修的四条缺陷都修好了，而且在真实运行上被验证；但修复过程本身又造出四条新缺陷，
其中三条现在还没修。任务不能 `DONE`。**

与上一轮的差别是：这一轮的坏消息**不是我自己发现并汇报的**，
有三条是**独立判定者在我看不到的地方查出来的**——这说明取证链这次真的在工作。

**独立收口审查也做完了**（`R-1`～`R-7`），它又查出两条我自己没看见的：
一条是**我把一份判据文件在结果出来之后原地改了**，另一条是**验收判据文件里的状态停在第 4 轮、和我自己的结论打架**。
两条都已在本轮闭合，代价是 **`AC-01③` 从 `PASS` 降为探索级**——见 §9。

---

## 2. Founder 七条逐条对账

| # | 你要求的 | 做到没有 | 证据 |
|---|---|---|---|
| 1 | 保持原 task_id / 分支 / worktree / Dify 候选，不建新根任务 | ✅ | 全部沿用；App `b7fb5b1a…`；相对入场基线 **543** 个文件全部为新增、0 修改 0 删除（独立 Reviewer 复核） |
| 2 | A/B/D/E 五项技术要求 | ✅ 已实现并实测 | §3 |
| 3 | 缺陷 C 属执行侧取证 HOW；先去标签泄漏，再逐场景单臂独立随机盲评；不降原增益门 | ✅ **盲评这次真的盲了** | §6 |
| 4 | E07 投影缺陷作为 `AC-17` 阻断在本任务修，四项要求 | ⚠️ **第 1 项成立，第 3 项不成立** | §5 |
| 5 | 先夹具与定向复验，全过再冻结、再整轮重跑 | ✅ | EP-11 预检包全绿，冻结于 `6bbad98`，之后才跑 |
| 6 | 最终候选稳定后再做收口审查、回滚演练、远端哈希、`AC-20` | ✅ 四项全做完 | 收口审查见 §9，两条阻断项已闭合 |
| 7 | Founder 产品验收必须给可亲自运行的 Dify 实测包 | ✅ | `M3_FOUNDER_DIFY_TEST_PACK_v1.0.md` + 八个场景完整输入 |

---

## 3. 四条缺陷的修法与实测

| 缺陷 | 修法 | 实测结果 |
|---|---|---|
| **A** 闸门给零交付盖章 | 最低实质产出硬门；命中即**不进补齐节点** | 本轮真实碰到 **2 次零产出**（纵向 `E09` 模型只吐一个反引号、行为 `B15-CTA` 只有空 `<think>` 壳），**两次全部被拦**，都没盖章、都没让补齐编内容 |
| **B** 闸门建在模型自报上 | 冲突反馈/暂定锚点由代码从投影算；算不出的用结构证据；新增输入槽位事实回抄核对 | 82 次运行零复发；`E07` 的自报与输入计算这次一致 |
| **D** 内部字段泄漏 | 必填项移出正文改审计块；外观机器痕迹由代码渲染 | **A/B 四臂 12 份输出机械扫描，内部字段命中 0** |
| **E** 裸标签逃逸 | 锚定行必须逐字指回正文互不重叠的实质文字 | 28 条夹具全绿；正式重跑未复现 |
| **F** 链式投影丢内容 | `cycle_state_carry` + 三分支投影 | **第 1 项成立**（`E09` 实测：硬门触发→不修复→`KEPT_PREVIOUS`，判定者独立核到 `E10` 基线与 `E09` 逐字节相同）；**第 3 项不成立**，见 §5 |

---

## 4. 本轮新造出来的四条缺陷

**这四条都是 v1.2 自己造成的，没有一条是上一轮遗留。**

> G-4 是纵向重跑轮的独立判定者发现的，我三套机制一个都没覆盖到那个方向。

### G-1 · 闸门把一句真话改成了假话（**已修，已在真实运行上验证**）

纵向 `E07` 的输入里三条反馈确实没有一条提到"上身效果"。模型原稿写了一句正确的自我纠偏：
「三条反馈里没有任何一条提到上身效果或试穿」。闸门把它判成"与输入矛盾"，
补齐节点据此改成「反馈里确实有内容提到上身效果或试穿」——**交给用户的是这句假话。**

行为判定者独立发现了**第二例**：`B05-1` 把原稿正确的「没有外部市场观察」
改成「外部市场观察目前只有一份方向参考」（输入里该槽位是未提供）。
我原以为只有一处。

判据混淆了「槽位本身没送到」与「槽位内容里没有某个话题」。已改死并加双向夹具。
重跑实测：`E07` 判 `CLEAN`、原稿逐字返回、无矛盾误报。

### G-2 · 探索触发漏检（**未修，阻断项**）

留出场景 03 上，候选写了「核心命题是……观察窗口至少一周」——这就是一次受控探索提案。
闸门判 `CLEAN`，探索六项没被要求。三名互不通气的盲评者各自指出：
**缺反证条件与到期处置**——正是第 3 轮漏写、并因此催生整个闸门的那两项。

**这一格直接构成 `M3-AC-18` 判不通过的那一维（实验设计 = A 优且实质）。**

方法上的漏：为了压掉第 4 轮 67/70 的误报，我把触发收紧成"动作性措辞"。
**收紧后我量了误报（0/70），没量漏检。** 这是方法漏，不是运气差。

### G-3 · 连续性对象集退化 + 补齐自证（**未修，阻断项**）

> **重跑轮判定者把根因挖得比我深**：`P1/P2/P3` 这个编号在**产品语义里根本没有依据**——
> 它不是模型偶尔不用，而是产品压根不产生这种形态。我是从第 4 轮某次输出里看到过就当成通例写进了正则。
> `v1.1 §3` 为「不再丢任务位」新增的保护分支，重跑轮 **0 次生效**。
> 更要紧的一条：**在抽取器覆盖面被独立验证之前，`dropped_without_notice: []` 不构成「没有内容被丢掉」的证据**——本轮它全程为空，实际丢了 7 个对象。

纵向判定者机械定位：

```text
baseline_objects.tasks   12 步全为 []      —— 模型写「第一条」，不写 P1
唯一被跟踪的对象          每周3条           —— 且对数字形态敏感
七步用中文数字重述过它，正则只认阿拉伯数字 ⇒ 七步全被误记成"未重述"
```

**漏检与误报同时发生。** 而真正丢的东西没被挡住：`E04` 排进第五周的整条探索任务
（六要素齐全）在 `E05` 消失、此后七步再未出现，`E05` 正文还写着
「所有已排任务的位置和节奏，全部照旧」——闸门判 `CLEAN`。

另有一条自证循环：`E04` 的补齐节点**替模型写了**「其余判断保持不变。」，
投影随后据这句认定"模型履行了连续性义务"。**该判据可以被闸门自己满足，
因此它对模型没有约束力。**

### G-4 · 过期状态压过当轮权威输入（**未修，阻断项**，重跑轮判定者发现）

`E04` 的输入槽位逐字是 `actual_capacity: 3 条/周`（该步只覆盖 `market_observations`，产能已恢复），而交付正文写「当前实际产能只有一条（苏禾请假）」——这是**承重依据**，直接支撑两条取舍、导致继续压着任务二/三不排。`E05` 下一步自行纠正了。

**三套机制全没响**：`input_contradiction` 与其 advisory 只管「正文说槽位没送到」这一个方向；投影一致性检查只管「对象有没有丢」，不管「对象的值对不对」。

```text
G-3  上一轮有的东西，本轮无声丢了        —— 少了不该少的
G-4  上一轮的旧值，本轮压过当轮权威输入  —— 多了不该多的
```

A3 说多算与少算都是错。**这里两种都发生了，各占一个方向。**

---

四条的共同根因是同一件事：**我用词面正则去认产品语义。**
词面收紧就漏检，词面放宽就误报。上一轮基线里有哪些任务位，
本来就该由结构化投影给出，不是从上一轮的散文里正则抠。

---

## 5. Founder 第 4 条四项，逐项结论

| 项 | 结论 | 依据 |
|---|---|---|
| 零产出不得覆盖上一有效周期状态 | **成立** | `E09` 一次真实零产出上实测；判定者独立核到 `E10` 基线与 `E09` 逐字节相同 |
| 投影前后关键对象/版本/数量一致性检查 | **形式上做了，实质退化** | 对象集退化成一个且对形态敏感，见 G-3 |
| 内容丢失必须显式失败，不得静默继续 | **不成立** | 两轮都不成立：STALE 那轮 `E04→E05` 整条探索任务静默消失；重跑轮 8 个对象里 7 个无声消失而 `dropped_without_notice` 全程为 `[]` |
| 增加 `E07→E08` 连续性回归 | **成立** | `account-operations/tests/test_projection_v12.py`，用第 4 轮真实文本 |

**根因不在 M2**，因此仍不登记 M2 后继任务；但正确修法（结构化 `standing_positions[]`）
是**新增产品语义**，需要独立授权，见 §10 一。

---

## 6. A/B 盲评这次是真的盲了

上一轮三名判定者靠方括号标签把候选全部点了出来。这一轮：

**通道拆除的机械证明**：四臂 12 份输出逐份扫描内部字段，**命中 0**。

**盲评有效性的实测**：36 名判定者里 29 名写下了对提示词来源的猜测，按臂分布：

| 臂 | 猜「专用/带骨架」 | 猜「通用」 | 说不知道 |
|---|---|---|---|
| `A`（胜任基线） | 4 | 1 | 4 |
| `Aplus` | **8** | 0 | 1 |
| **`B`（候选）** | 5 | 0 | 4 |
| `Bprime`（39 字提示词） | 4 | 1 | 4 |

**猜测完全不跟臂走**——被认成"专用"最多的是 `Aplus`，连 39 个字的消融臂都有 4 人猜"带结构"，
**没有一个人认出候选**。

**协议纪律的机械核验**：432 条评分理由扫描，使用长度/条数/术语密度/模型自评这类
§5.3 禁用证据的：**0 条**。36 份判定任务提示词逐字同构核验：归一化后**仅 1 种形态**。

**结果**：

```text
M3-AC-18   FAIL(INSUFFICIENT)
           五项硬门零退化（候选干净；只有 Bprime 在场景 01 有一处事实退化）
           七维里只有「产能取舍」B 优，「实验设计」是 A 优且实质 ⇒ 无整体增益
M3-AC-01③ 推导输出 PASS —— 删掉 M3 之后 5/7 维明显变差
           ↑ 这是**推导脚本的输出**。它的**正式等级**在 §9 被降为探索级（NOT_VERIFIED），
             原因是本轮盲评协议在判定冻结、揭盲之后被原地改写（阻断项 B-1）。
```

**说人话**：候选比"什么都不给"强得很明显，但比一份写得好的通用运营基线，
**没证明更强，而且在"实验设计"这一维上确实更弱**——那一维的失分原因就是 G-2。

---

## 7. 执行侧自己的错误（不推给判定者、不推给环境）

1. **G-2 的方法漏**：冻结前只量误报不量漏检（见 §4）。
2. **纵向 runner 字段改名漏改一处打印**，序列在 `E01` 之后崩掉。
   那一份 `E01` 保留在 `ep07-longitudinal-v12-aborted/`，不进入判定；序列整条重跑。
   **执行侧在重跑前已经看过那份 `E01` 的输出**，如实登记，留给独立收口审查者判是否构成择优。
3. **首次部署时 `assemble` 节点漏打共用检查块**（本地能 import 所以没测出来）；
   **闸门新增的返回键没同步到图里声明的 outputs**，整条 workflow `failed`。
   两条都已加部署前断言，且都发生在正式取证之前。
4. **盲评协议里"执行侧判定前不读映射"这条，被执行侧自己违反**（干跑置换算法时打印了映射）。
   如实登记，并把该条换成可核验的更强约束：36 份提示词逐字同构。
5. **`_cases.json` 的冻结件指针与哈希从未版本化更新**，`_run_index.json` 也没记运行时用的哪一版。
   行为判定者据此把「判据先于结果」标 `NOT_VERIFIED`——**这个处理是对的**。
   来龙去脉已查清落盘（`_CASES_PROVENANCE.md`）：原始那份逐字节未动，
   差异是 1 用例 1 字段，方向是向早 6 小时冻结的上位判据对齐，
   上位判据 21 条 AC × 10 个判据字段自冻结以来 0 处变化。
   **但该更正不是判定中性的**，故 `AC-06` 必须带限定写。
6. **判定进行中移动了它正在读的目录**（把纵向证据目录改名又改回）。
   判定者的处置完全正确（快照 + 全量哈希 + 只基于快照判定），责任在执行侧。

---

## 8. 本轮 AC 状态矩阵

**口径**：`成立` = 独立判定者按冻结 Oracle 判定通过。执行侧**不把 `成立` 自行升级为 `PASS`**。

| AC | 状态 | 依据 |
|---|---|---|
| `AC-01` ①② | 成立 | 结构反搜对 v1.2 图重跑，行为标签命中 **0**；责任反搜 12 条测试通过 |
| `AC-01` ③ 消融门 | **`NOT_VERIFIED`（探索级）** | 推导本身干净：B vs `Bprime` 5/7 维 B 优、零硬门退化，独立 Reviewer 从 36 份判定原文逐格复现；盲评有效性已实证（§6）。**但盲评协议在判定冻结、揭盲之后被原地改写**（阻断项 `B-1`），按 A2 只算探索、不产生正式 `PASS`。两条出路见 §9 四 |
| `AC-02` `AC-04` `AC-07` `AC-08` `AC-09` `AC-10` | 成立 | 行为 ECC |
| `AC-03` | **不得 `PASS`** | 八变体两两 28 对里 1 对不满足（长期价值×吸粉只有一轴不同） |
| `AC-05` | 成立 | 结构半 0 命中；运行时半行为 ECC 通过 |
| `AC-06` | 成立**（带限定）** | 行为 ECC 判成立，但依赖 `_cases.json` 的 `B06-1` Oracle 更正；判定者对「判据先于结果」标 `NOT_VERIFIED` |
| `AC-11` | **未独立成立** | g1fix 轮判定者：`E11` 恢复的确实是含 `E10` 变更的当前基线、不是活动开始快照，**这一条是本轮最干净的证据**；判"未独立成立"的真实理由是 `E10` 之后基线被截断（1997 → 546 字符），不能说该能力在**完整**基线上被验证过。（本行原引的是被作废那一轮判定者的话，独立 Reviewer 报为观察 `O-2`，已更正为 g1fix 轮原文） |
| `AC-12` `AC-13` | 对钉死绑定 `df2c595`：成立<br>对当前 `main@a7b8101`：**`STALE`** | 83 条契约/消融/反搜测试全绿；`main` 施工期间改了 `business-persistence/`，M2 现在把可用性与可发布性分成两道闸并加了适用范围过滤，M3 投影未跟上（`ep13-a3-main-moved/`） |
| `AC-14` | 成立**（判定者提示风险）** | 若对 `B14-1` 从严，`AC-14` 也不成立 |
| `AC-15` | **不得 `PASS`** | `B15-DIR-01` 不足 + `B15-CTA` 模型侧零产出（记 `无结果`，按 §7 不重抽） |
| `AC-16` | Workflow 图链路 **`PASS`**<br>浏览器渲染画布 **`NOT_VERIFIED`** | 保真 9/9「成功」；系统提示词绑定已由已发布图逐字节实证（已观察级）；本机无浏览器自动化，按 §12.3 不用运行历史顶替 |
| `AC-17` | **`FAIL(INSUFFICIENT)`** | 修 G-1 后整条重跑，12 步全 `succeeded`、无 `NOT_VERIFIED`、第 4 轮那类「零产出整体覆盖基线」事故**未复现**；但逐步 `E04` `不足`，序列级「局部性」只部分成立（交付正文成立，投影/状态层在 `E10` 处不成立）。判定者独立追踪 8 个对象，投影真正保住 **1 个**，其余 7 个无声消失而 `dropped_without_notice` 全程为 `[]` |
| `AC-18` | **`FAIL(INSUFFICIENT)`** | 硬门零退化但无整体增益；`M3_PROFESSIONAL_GAIN = NOT_VERIFIED`，`M3_MODULE_AB = NOT_PASSED` |
| `AC-19` | 部分成立 | Qwen `NOT_APPLICABLE`（Founder 指示，运行前判定）；**41 名判定者：40 名受隔离约束者机械核验全部 `CLEAN`、零违规；第 41 名是按 Rubric 授权通读全部材料的独立收口 Reviewer，隔离性 `NOT_APPLICABLE`，另核其写入范围为 `CLEAN`（0 条仓库内写入、0 条变更类 git 命令）**；留出强度上限如实记录；**独立收口审查已完成，见 §9** |
| `AC-20` | 部分成立 | 回滚演练实测通过（破坏→恢复逐位相同，已发布版本未被触碰）；**远端与本地 HEAD 一致性可一条命令重验**：`bash account-operations/evidence/ep10-closeout-v12/verify_remote_hash.sh`（零参数、只读）。已验证的提交与完整哈希逐条记在 `remote_hash_ac20.json`——**这里不钉死某个哈希，钉死的是判据**，否则下一次提交就把这句话变成假话；**独立收口审查已完成**（§9）；**Founder 产品接受未发生**——实测包已交付，裁决未请求 |

**进度：`IN_PROGRESS`。终态：未落。**

口径更正（独立 Reviewer 观察 `O-6`）：`IN_PROGRESS` 是**进度**词，不是**终态**词。
合同的 `allowed_final_states` 是 `[INVALID, DONE, BLOCKED, FAILED]`，本任务当前**一个都不落**——
它既没做完，也没被卡死，也没失败。**该落哪个终态词是 Founder 的域，执行侧不自行宣布。**

---

## 9. 独立收口审查的结论（`R-1`～`R-7`）

判据 `M3_INDEPENDENT_REVIEWER_RUBRIC_FROZEN_v1.0.md` 冻结于本任务全部工程结果之前，
Reviewer 未参与任何实现，全文在 `M3_INDEPENDENT_CLOSEOUT_REVIEW_V12_v1.0.md`。
它只问一个问题：**这份交付里，有没有任何一条声明的等级高于它的证据。**

| 项 | 结论 |
|---|---|
| `R-1` 判据先于结果 | **`FAIL`** —— 阻断项 `B-1` |
| `R-2` 声明等级不越级 | **`FAIL`** —— 阻断项 `B-2` |
| `R-3` 失效传播不多算不少算 | `PASS` —— Reviewer**自己重放了 73 次运行**，逐项比对 13 个决定量，独立复现 72 不受影响 / 1 受影响（`E07`），与 `unaffected_proof.json` 逐字相同 |
| `R-4` 边界与受保护资产 | `PASS` —— 543 个文件全部新增、0 修改 0 删除；三个受保护目录零改动；六份既有 Skill 逐字节未动；0 merge、未并入 `main`、reflog 32 条全是普通 commit |
| `R-5` 失败路径只追加不删除 | `PASS` —— 含「重跑前看过 `E01`」一条：**不构成择优** |
| `R-6` 声明上限 | `PASS` —— 11 条措辞全仓反搜，本轮各文件要么零出现，要么全为否定性用法 |
| `R-7` 留出与探索/正式分轨 | `PASS` —— 三条核心声明 Reviewer **全部独立复算并证实为真** |

### 9.1 两条阻断项，以及本轮怎么处置的

**`B-1` · 盲评协议在判定完成、揭盲之后被原地改写，且没换版本号。**

```text
17:10:02  最后一份盲评判定写定（判定冻结点）
17:11:43  揭盲脚本读取 sealed 映射（揭盲点）
17:28:16  ★ ADDENDUM_002 被原地改写 ★
```

被删掉的，正是执行侧**已经违反**的那一条（「执行侧在第 4 步之前不读它」）；
换上去的是一条更强、可核验的约束（36 份提示词逐字同构）。
**约束本身立得住**——Reviewer 独立复算：归一化后仅 1 种形态 × 36。
**但方向是"看到结果之后改协议"**，而 `AC-01③ = PASS` 正骑在这一轮上。

处置（本次提交）：

1. `ADDENDUM_002` **已恢复到冻结原文**（`6bbad98`，sha256 `23ba357e…`），逐字节相同；
2. 被写进去的内容移到新版本 `M3_ECC_MODULE_AB_001_FROZEN_v1.0_ADDENDUM_003.md`，
   **该文件第一行就写明它写于结果之后**；
3. **`AC-01③` 由 `PASS` 降为探索级（正式 `NOT_VERIFIED`）**。执行侧**不为自己补权威事件**；
4. 恢复正式 `PASS` 的两条路交 Founder，见 §10 四。

**`B-2` · 验收判据文件 §7.2 停在第 4 轮，仍写 `AC-12`/`AC-13` = `PASS` 且「转 `CURRENT`」。**

同一份交付里两份都自称"当前"的文档给出冲突状态，而**更高的那个写在验收判据文件里**。
Reviewer 另查到：`main` 早在 **10:25:44** 就到了 `a7b8101`，第 4 轮 11:29 写下"转 `CURRENT`"时该结论已不成立。

处置（本次提交）：`M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md` §7.2 已同步到第 6 轮状态，
并新增 §7.3 记录该节每一次更新的提交与触发事件。
**§3 判据本体一字未改**——重新机械核验：21 条 AC × 10 个判据字段 vs `f5a9aca`，**0 处差异**。

### 9.2 六条非阻断观察的处置

| 观察 | 处置 |
|---|---|
| `O-1` 权威事件在仓库内无独立佐证（Founder 七条只以执行侧转述形式存在） | **未处置，交 Founder 确认一次转述是否准确**。本轮没有任何声明踩着它上行 |
| `O-2` §8 `AC-11` 的依据引自被作废那一轮 | **已更正**为 g1fix 轮原文，见 §8 该行 |
| `O-3` `unaffected_proof.json` 的比对字段集比真实依赖集窄（`check_input_contradiction` 有两个调用点） | Reviewer 已补做 post-gate 扩检：**0 处差异，结论不变**。记为今后取证纪律：比对字段按**调用点**枚举，不按单一报告的字段列 |
| `O-4` "推导代码与全部判定原文一并落盘"没做到 | **已补齐**：36 份判定原文、推导脚本、分配表、已揭盲的映射全部落进 `ep08-module-ab-v12/`，并新增 `derivation/recompute_from_repo.py`。**只用仓库材料重算：84 格 grades + 60 格硬门 + 3 组推导，0 处不一致** |
| `O-5` 第 4 轮的 Founder 面向文件未标注已被取代 | **已加指针**，见该文件头部 |
| `O-6` 三处数字/用词与现状对不上（373→543、39→41、终态用词） | **已全部更正**，见 §2、§8 |

### 9.3 Reviewer 点名判掉的三件事（执行侧自己拿不准的，它给了结论）

- **纵向冻结件 `v1.0 → v1.1` 的合法化：成立。** 新文件、原文保留、触发事件写在第一句、不追溯改判，
  且**没救回任何东西**（`AC-17` 改前改后都是 `FAIL(INSUFFICIENT)`）。
- **「重跑前已经看过那份 `E01`」不构成择优。** 三份 `E01` 输入逐字节相同、闸门 13 项判定全同、
  **被丢弃那份反而最长**（1943 vs 1664 vs 1365）——择优所需的梯度不存在。
- **§2.3.1 那次自曝违反，不该判 `FAIL`。** `R-7` 的两条 `FAIL` 条件都不成立；
  它造成的真问题是"判据文件被后改"，已计入 `B-1`，**同一件事不重复计两次**。

---

## 10. 需要 Founder 决定的四件事

### 一 · 三条阻断类缺陷要不要修（G-2、G-3、G-4）

**三条**（G-2、G-3、G-4）都绑定冻结 AC（`AC-08`/`AC-17`/`AC-18`），按你第 5 条属于可以阻断的类别。
**我没有自己开第六轮。**

正确的修法方向是同一个：**从结构算，不从词面算**——
在 `M2_TO_M3_PROJECTION_v1.0.schema.json` 里加结构化的 `standing_positions[]`，
连续性检查只对它做；触发条件也从输出结构算，不从词表匹配。
**但这是新增产品语义，按 A1 我不能自己创造，按 A4 需要你的授权。**

### 二 · M2 接口漂移（`AC-12`/`AC-13` 的 `STALE`）

与第一条**指向同一个方向**：M2→M3 投影 schema 需要一次有授权的扩展。
G-2、G-3、G-4 与这一条**四件事指向同一次 schema 扩展**，建议合并处理，不要分四次动同一个 schema。

### 三 · `AC-06` 的 Oracle 更正要不要认

第 3 轮按旧口径把 `B06-1` 判为失败，本轮按更正后的口径判成功。
更正方向是向更早冻结的上位判据对齐，我认为站得住；但**这是判据认定，是你的域，不是我的**。

### 四 · `AC-01③` 怎么办（独立收口审查阻断项 `B-1` 的两条出路）

盲评推导本身干净、独立 Reviewer 逐格复现；坏的是**协议在结果之后被改写**。
执行侧已按 A2 把它降为探索级，**不为自己补权威事件**。两条路：

```text
路径甲  你出具权威事件，明确接受"本轮以事后同构约束替换被违反的自觉条款"，
        并认可推导公式全程未动 + 独立 Reviewer 逐格复现。
        代价：接受一次事后判据替换成为先例。收益：AC-01③ 恢复正式 PASS，零重跑。
路径乙  以 ADDENDUM_003 为冻结判据，重跑 12 次 A/B + 36 名判定者。
        代价：一轮运行与判定成本（上一轮实测约 7 CNY、约 1.5 小时）。
        收益：AC-01③ 干净地挣回来，不需要任何事后授权。
```

**不选＝`AC-01③` 保持 `NOT_VERIFIED`。** 执行侧默认按此记账。
**`AC-18 = FAIL(INSUFFICIENT)` 不受这条影响**——两种记法下它都不是 `PASS`。

---

## 11. 恢复入口

```text
分支      task/m3-account-content-operator-v1（远端哈希在 §8 AC-20 行，随每次推送回填）
冻结点    6bbad98   判据与绑定；全部正式取证在其后
G-1 修复  M3_ECC_REBIND_003_FROZEN_v1.0_ADDENDUM_001.md（含 73 次运行的不受影响机械证明）
Dify App  b7fb5b1a-9278-426c-bb8a-f9f288639548
          已发布 m3-cand-v1.2-gate；草稿图 2f317ab9…；已发布图与草稿图哈希相同
闸门源码  account-operations/tools/gate_v12/（与 Dify 三个代码节点同一份源）
夹具/测试 account-operations/fixtures/gate_fixtures_v12.json（28 条）
          account-operations/tests/test_gate_v12.py / test_projection_v12.py
证据      ep06-runtime-fidelity-dify-v12/     保真 9
          ep06b-runtime-behavior-v12/         行为 49
          ep07-longitudinal-v12/              纵向 12（因 G-1 而 STALE，保留不用）
          ep07-longitudinal-v12-g1fix/        纵向 12（修 G-1 后重跑）
          ep07-longitudinal-v12-aborted/      harness 崩溃那次，保留不用
          ep08-module-ab-v12/                 A/B 12 + 泄漏扫描 + 揭盲结果 + 36 份判定原文
                                              + derivation/（推导脚本、分配表、映射、仓库内重算）
          ep10-closeout-v12/                  图结构检查、系统提示词绑定实证、隔离核验、回滚演练
          ep11-gate-v12-precheck/             冻结前预检
          ep12-founder-dify-test-pack/        Founder 实测包
          ep13-a3-main-moved/                 main 前进的影响面
          ep14-gate-v12-defect/               G-1/G-2/G-3/G-4 与不受影响证明
收口审查  M3_INDEPENDENT_CLOSEOUT_REVIEW_V12_v1.0.md（R-1/R-2 FAIL，R-3～R-7 PASS）
B-1 处置  M3_ECC_MODULE_AB_001_FROZEN_v1.0_ADDENDUM_003.md
          （ADDENDUM_002 已恢复冻结原文 sha256 23ba357e…；AC-01③ 降为探索级）
下一步    等 Founder 对 §10 四件事的决定；未决前不动 SKILL.md、闸门与投影 schema
```

```text
END_MARKER = M3-CHECKPOINT-ROUND-6-END
```
