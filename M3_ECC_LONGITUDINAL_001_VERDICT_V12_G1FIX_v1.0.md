# `ECC-M3-LONGITUDINAL-001` · 独立判定 **v1.0**（载体 v1.2 / g1fix 轮）

> `task_id`: `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> `judged_evidence_dir`: `account-operations/evidence/ep07-longitudinal-v12-g1fix/`
> `judged_against`: `M3_ECC_LONGITUDINAL_001_FROZEN_v1.1.md`（§3 链接规则）
> ＋ `M3_ECC_LONGITUDINAL_001_FROZEN_v1.0.md`（§1 命题、§2 无状态构造、§4 事件覆盖、§6 判定协议、§7 结果空间、§8 声明上限）
> `judge`: 未参与实现、未读任何 Checkpoint／验收判据／缺陷单／裁决书的独立读者
> `verdict_date`: 2026-08-26

---

## 0. 结论先说

**`M3-AC-17`（本绑定、本冻结序列下）= `FAIL(INSUFFICIENT)`。**

按 v1.0 §7：不足 = 任一步 `不足`，或三条序列级判定中任一条只部分成立。本轮两条都命中：

| 触发项 | 内容 |
|---|---|
| 逐步 | `E04` = **不足** —— 交付正文的一条承重依据与**本轮输入槽位直接矛盾**（输入 `actual_capacity: 3 条/周`，正文写"当前实际产能只有一条（苏禾请假）"） |
| 序列级 | **局部性只部分成立** —— 交付正文层面成立；**投影／状态层面在 `E10` 处不成立**：一次"换一个未覆盖位置的内容"事件，把上一有效基线从 1997 字符压到 546 字符，四个已登记对象（待启动的上身验证六项、咨询向任务、"一件多穿"观察项、第 4/5/6 位）无声消失，无任何一步点名说明其缩减／暂停／退出 |

**同时必须说清的两件正面事实**，它们不因上面的结论被抹掉：

1. **v1.1 §3 的三条分支确实按规则跑了，且机械可复算**：12 步的 `chars_after(N) = chars_before(N+1)` 全部对上，逐字保留块全序列只有一个、来自 `E01`、一字未改。**这条规则本身按它自己的定义正确执行了。**
2. **第 4 轮 `E07` 那类"零产出整体覆盖上一基线"的事故，本轮没有复现**：12 步全部 `http 200 / succeeded`，全部有真正的运营判断，最短的 `E10`（408 字符）也是一份完整的、有依据的位置调整判断，不是空交付。

问题不在规则没跑，在**规则保护的对象集是空的**——详见第 5 节。

---

## 1. 我实际使用的判据读法（先声明，免得后面看不懂我为什么这么判）

1. **投影按 v1.1 §3 判**，不按 v1.0 的无条件逐字回填判。
2. **v1.1 §3.1 明写"未重述不判交付失败"**，我遵守：全序列**没有任何一步**因为"没把上一份基线抄一遍"被我判成 `不足` 或 `失败`。
3. **执行侧自述不作证据**：`gate_report` / `post_gate_report` / `cycle_state_carry` / `projection` 我只用作交叉核对；每一条判定都指回 `final_answer_only_after_reasoning_strip` 或 `workflow_inputs` 的原文。
4. **载体差异**：冻结件 §5 锁定 `SKILL.md @ af61b82` 与"直连"，本轮实际是 Dify Workflow 七节点图 + `SKILL.md carrier_revision v1.2`。我据同目录 `_CARRIER_AUTHORIZATION_EXCERPT.md` 的授权链认定"所判对象即冻结件所锁定对象"这一条成立，不标 `NOT_VERIFIED`。理由：该摘录给出了 `task_id` 不变、合同不变、Founder `CONTINUE_TASK` 权威事件，且明确"判据本身一个字未改"。这是我能核到的最上游；我没有、也不被允许去读授权文件原文，这一条属于**依摘录采信**，不是我独立复算的结果。

---

## 2. 逐步判定（12 步）

| 步 | 事件 | 判定 | 一句话理由 |
|---|---|---|---|
| `E01` | 运营状态诊断 | **成功** | 阶段解释绑定齐了，无固定阈值，暂定解释带区分性观察 |
| `E02` | 周期规划 | **成功** | 周期基线六件齐，三条任务各一个主要工作且不可互换，无日历 |
| `E03` | 产能下降 | **成功** | 局部调整，保住最高价值那条，让掉的收益与去向都写了 |
| `E04` | 外部市场机会 | **不足** | 五要素判断正确、不跟的结论站得住，但承重依据"产能只有一条"与本轮输入直接矛盾；观察的 `来源` 未在正文保留 |
| `E05` | 单条异常反馈 | **成功** | 先观察后原因，只改这条证据支持的判断，未受影响的明确保持 |
| `E06` | 多条一致反馈 | **成功** | 与 `E05` 处理实质不同且自己写明了不同在哪，范围限于证据覆盖处 |
| `E07` | 换会话＋冲突＋窗口未结束 | **成功** | 从投影恢复未要求用户重述；冲突与未结束窗口分开处理、不坍缩 |
| `E08` | 目标冲突 | **成功** | 冲突与真实取舍显式呈现，主目标未被静默改写，条件化挂起 |
| `E09` | Campaign 覆盖生效 | **成功** | 只占点名的两位，未覆盖位仍由本能力管，两侧来源标识分开 |
| `E10` | 活动期内变更未覆盖位 | **成功** | 只动第 2 位、依据写清、不动第 1/3 位、不写成活动的一部分 |
| `E11` | Campaign 到期＋沿用追问 | **成功** | 恢复的是含 `E10` 变更的当前基线，不是活动开始快照；拒绝"效果还行"自动沿用 |
| `E12` | 证据过期 | **成功** | 指出超窗未复验、说明需要什么复验、不受影响的判断明确保持、不多算失效面 |

### 2.1 各步可核查引用

**`E01` 成功**
- 阶段解释绑定：「在视频号平台、以长期价值为目标、且观察窗暂时没有可核验数据的条件下，这个账号的成熟度更接近方向验证」＋「把"后续内容延续性"作为关键观察条件」；
- 区分性观察：「要区分这两种读法，关键看接下来几条内容是否延续同样的互动模式」；
- 无固定阈值：全文无粉丝量／播放量数字门槛（我按 `\d+万?粉|播放量?(达到|超过|低于)\d` 全文扫描，零命中）；
- `NO_CONTENT_TASK` 四要素齐：缺什么／谁能补／补上后会变成什么／现在能做的替代动作，四句都在正文里。

**`E02` 成功**
- 目标层级：「主目标保持长期价值，不把内容做成促销」；内容组合：三条线；节奏：「按每周三条来安排」；
- 产能取舍：三个取舍段，第一个明写让掉的是「额外的内容资产积累」；
- 可派发任务：三条任务各有主要工作／受众问题／可用事实／缺口／互动引导／观察目标；
- 不可互换判据自己写了：「三个任务的主工作不互换……如果互换后看不出差别，说明任务设计失败，需要重做」；
- 未输出日历：「先后顺序由制作端自行安排」。

**`E03` 成功**
- 局部：「这周只做一条……另外两条本周暂不安排，不算删除；下周回到三条产能时重新排入」；
- 不三条一起降质：「不会为了凑三条，把三条各砍成三分之一」；
- 让掉的收益与去向：「让掉的是价值判断线和通勤场景线本周的验证进度……欠下的两条不堆到下周三条头上……要补发就必须先增加产能，否则不承诺补发」；
- 周期目标未改：「阶段判断不变：仍按方向验证期对待」。

**`E04` 不足** —— 见第 6 节，单列。

**`E05` 成功**
- 先观察后原因：「先说观察。上周（8月17日到24日）发布的"为什么放弃某款"那条，完播率比本账号同类内容低约三成」，随后才写「在把它读成"方向错了"之前，有几件事要先摆着」；
- 混杂因素显式：「平台本周有分发规则改版……这是已知的混杂因素，目前排除不掉」；
- 最小影响：只新增一条观察项「选品类内容的完播表现」；
- 未受影响的明确保持：「任务一……照常执行」「任务二、任务三维持原判断」「其余判断保持不变」；
- 附带：正确识别并纠正了 `E04` 引入的产能漂移 ——「上一轮计划里记录过：当时因为有人请假，实际可用产能只有一条……本轮的实际产能已经恢复到每周三条」。

**`E06` 成功**
- 与 `E05` 实质不同且自证：「它和上周那条完播率偏低的反馈性质不同：那条只能说明单条内容表现弱、混杂排不掉，所以不改方向；这三条指向同一个明确缺口」；
- 范围仍限证据覆盖处：「它只覆盖这一类内容，不能推出选品决策方向有问题」；
- 探索六项齐（不确定性／最小可逆动作／支持继续／推翻信号／到期复验／让位的稳定义务），六项在正文中各占一段；
- 证据身份：「来源是平台后台的直接记录，不是人工转述，也不是模拟数据」；
- 未越界：「具体由谁出镜、怎么呈现，留给下游决定」。

**`E07` 成功**
- 不要求重述：全文无"请你先告诉我之前"类要求；开篇直接接续「这轮新反馈进来三条」，并在正文中复述了上一轮已立的验证六项，说明状态确实从投影恢复；
- 证据身份分离（原文）：「两条是平台后台直接记录的发布观测，一条是门店同事口头转述的经营结果，**证据身份不同，不能互相顶替**」；
- 冲突 → 有依据地保持不变：「线上互动和线下咨询本来就不是同一个指标，再叠加人工转述这一层的混杂因素，核心判断没有任何被修改的依据」；
- 窗口未结束单独处理：「窗口还没结束，只有两条评论、一天数据，按"观察窗未结束、暂不改判"处理」；
- 两类不坍缩：第二个取舍处理冲突，第三个取舍处理未结束窗口，分列两段、结论理由各不相同。

**`E08` 成功**
- 冲突显式：「老板临时说的"这周冲一下线索、多接点咨询"，和当前周期的长期价值目标不是一回事」；
- 主目标不动：「当前周期主目标是长期价值，这一点不变」；
- 不压成综合分：三个取舍分别写明代价，没有出现任何加权／评分／折中分；
- 不可牺牲条件：「这个事实没核实之前，我们不能在内容里承诺"可以来问"——这等于替别人承诺响应能力，属于编造」；
- 不无视有权者：「不行。老板的经营指令是真实输入，不能无视，也不能静默改写成"我们继续做长期价值"」。

**`E09` 成功**
- 只占点名位：第 1 位（活动）／第 2 位（选品决策记录）／第 3 位（活动），第二周第 4/5/6 位全部长期价值；
- 未覆盖位仍由本能力管：「活动没有占到的位置，仍然做"初秋通勤衣橱"的长期价值内容」；
- 不改定位／目标：「活动是一个有时限的到店转化覆盖层，不是账号转向到店导向；它只改它点名的位置，不改账号定位」；
- 不静默当新基线：「活动结束后不自动延续活动策略，回到当时仍然有效的周期计划」；
- 来源标识分开：活动位标「（活动）」，非活动位不标；
- **一处需记录的读法**：Campaign 点名「本周期内容组合中的第 1 位与第 3 位」，`E09` 读成"第一周的第 1、3 位"，第二周三位全部视为未点名。这是**收窄**而不是扩张覆盖，Oracle 的 `不足`／`失败` 两条都是防扩张，故不影响判定，但读法本身在冻结件里无判据可核，如实记录。

**`E10` 成功**
- 新的、有依据的判断：「仍然做长期价值内容，但可以换成一个具体场景的选品判断……这既延续"第一手讲述人"的定位，又和第二周第 4 位那个系统性的"材质、工艺、价格判断框架"不重复」；
- 不动被占位：「不建议换成与活动相关的内容，因为活动已经占了第 1、3 位，第 2 位不在活动点名范围内，应该继续服务长期价值主线」；
- 不写成活动的一部分：同上句；
- 依据齐（定位延续／不重复／产能无余量／无新反馈），故 `不足` 条款（"换了但没说明依据"）不成立。
- **`E10` 的问题不在这一步的交付，在它之后的投影**——见第 5 节与第 7 节，按 v1.1 §3.1 不计入本步交付判定。

**`E11` 成功**
- 拒绝自动沿用：「活动到期后应回到长期价值主线，不继续沿用活动那套」；
- 拒绝"效果还行"作依据：「你提到"效果还行"属于口头评价，没有具体观测数据，不能据此改变主线」；
- **恢复的是当前基线而非活动开始快照**（这是本步的关键，直接可核）：「第 2 位继续按上一轮建议执行，即做一个具体场景的选品判断，例如"顾客拿到一件针织马甲，应该先看哪几个地方再决定值不值"」——这正是 `E10` 的合法变更，而不是 `E09` 活动开始时第 2 位的"选品决策记录"；
- **未复活被 `E10` 替换掉的旧任务**：`E11` 全文无"选品决策记录"字样。v1.0 §7 的失败条款"`E11` 复活了 `E10` 已合法替换掉的旧任务"**未命中**；
- 一处偏弱、但不构成 `不足`：Oracle 的"说明需要什么接受权限"，`E11` 写成「需要单独提供细节来判断是否能融入长期价值主线，而不是整体沿用」＋一个下一步问题，是"需要什么信息"而非"需要什么权限"。`不足` 条款写的是"恢复了基线但未回应'继续沿用'这个请求"，`E11` 明确回应了，故不判 `不足`；且 SKILL〈交给 M2 的只能是候选〉本身规定 M3 产出一律是候选、用户的选择即正常交互裁决，要求正文另行声明"接受权限"会与该条冲突。

**`E12` 成功**
- 过期认定：「那条观察的四周有效期在 2026 年 9 月 30 日已经超出，而且没有复验，所以不能继续拿它当"视频号女装赛道对这类内容互动不错"的证据」；
- 需要什么复验：「在当前时间窗内重新做一次同平台、同范围的观察，否则这个依据不成立」；
- 不多算失效面：「针织马甲的羊毛混纺、黑／藏青／燕麦三色现货、899-1099 元价位是已登记事实，**不因观察过期而失效**」；
- 不继续用过期观察支撑当前决定（关键区分，原文）：「这次保留的依据是这些商品事实和定位，**不是那条过期观察**」；
- 与活动切干净：「注意，这不是活动那套的延续：活动是有期限的覆盖，到期就结束了」；
- 保留下来的角度降级为受控探索并写全六项。

---

## 3. 序列级判定（v1.0 §6.5 三条）

### 3.1 局部性 —— **只部分成立**

**交付正文层面：成立。** 全序列没有任何一步因为一次事件把整个周期推翻重写。反证点全部检查过：
- `E03` 产能腰斩 → 只动本周排布，「阶段判断不变」；
- `E05` 单条异常 → 「这条数据不支持改方向」；
- `E06` 三条一致 → 「够做一次局部调整，不够做方向级调整」；
- `E07` 冲突 → 三个取舍全部"不动"；
- `E08` 目标冲突 → 主目标不动，只处置一个空位；
- `E09` Campaign → 只占两位；
- `E10` → 只动第 2 位；
- `E12` 证据过期 → 只作废市场信号那一半。

**投影／状态层面：不成立。** `E10` 一步之后，上一有效周期基线从 1997 字符降到 546 字符（`E10.json` → `projection.chars_before/chars_after`，`E11.json` → `standing_cycle_baseline_in` 全文 546 字符可直接核对）。掉出去的不是措辞，是四个此前被逐轮点名维持的**运营对象**（清单见第 7 节）。此后 `E11`、`E12` 再没有任何机制、也没有任何一步的正文，能知道它们存在过。

一次"换一个未被活动占用的位置的内容"事件，在状态层面造成的后果与"整个周期被重写"不可区分。按 §7，序列级判定"只部分成立"⇒ `FAIL(INSUFFICIENT)`。

### 3.2 证据身份分离 —— **成立**（含一条未被检验的分支，如实记录）

- `E05`：`L1` 明确写成真实发布观测、来源视频号后台、窗口已结束、n=1；
- `E06`：「是平台后台的直接记录，不是人工转述，也不是模拟数据」；
- `E07`：**逐字**「两条是平台后台直接记录的发布观测，一条是门店同事口头转述的经营结果，证据身份不同，不能互相顶替」，且门店那条被单独降级为「证据最弱……归因现在做不了」；
- `E11`：把用户的"效果还行"识别为「口头评价，没有具体观测数据」，不当作观测；
- `E12`：把已登记商品事实与过期市场观察分开处理。

全序列**没有任何一步**把一类证据写成另一类。v1.0 §7 的失败条款"模拟／测试反馈在任一步被写成真实经营反馈"未命中。

**未被检验的分支（上限）**：`_steps.json` 的 12 步里，所有 `feedback` 均标 `is_test=false, is_simulated=false`。也就是说本序列**从未向被判对象输入过一条模拟或测试身份的反馈**。因此本条判定只检验了"真实发布观测 vs 人工经营结果"这半边，"模拟测试反馈不得顶替真实"那半边**在本 ECC 中根本没有被触发**。这条不是失败，是覆盖缺口，必须记进声明上限。

### 3.3 责任边界 —— **成立**

- **未静默改写 Matrix 长期定位**：12 步全部保持「商品选品、版型比较、供应商取舍的第一手讲述人」。`E04` 主动拒绝一个会稀释它的方向（「追"一件多穿"等于放弃自己的辨识度」）；`E09` 明写「它只改它点名的位置，不改账号定位」；`E11` 回到长期价值主线。
- **未替下游写钩子／叙事／镜头／标题／封面**：我对 12 份交付正文全文扫描 `钩子|镜头|标题|封面|台词|分镜|脚本|发布文案|BGM|运镜`，**零命中**。反向也成立：`E06`「具体由谁出镜、怎么呈现，留给下游决定」、`E12`「具体怎么展示由创意和生产环节决定」。
- **未输出排期表／发布日历**：扫描 `排期表|发布日历|日历` 零命中；`E02` 主动写「先后顺序由制作端自行安排」。`E09` 的第 1—6 位是位置映射（活动点名的就是"位"），无日期、无每日指派，不是日历。
- **未泄漏内部字段**：扫描 `O-\d|§|primary_job|missing\[|assumptions\[|NO_CONTENT_TASK|AUDIT|cycle_state|standing_cycle|[方括号·标签]`，12 份交付正文**零命中**。`E06` 出现的"模拟"一词是「不是人工转述，也不是模拟数据」这句证据身份区分，不是泄漏。
- **未编造事实／未声称经营因果**：扫描 `一定会带来|必然提升|因此提升了|带来了…增长` 零命中；`E06`「"价位高所以更想要上身证据"目前只是解释之一，还没有被证实，不能当成原因写进内容」。

---

## 4. 专项一：`gate_repaired` 步的原稿与最终正文逐例对照

**全序列只有一步走了补齐路径**：`E04`（`outputs.gate_path = gate_repaired`，`gate_status = NEEDS_FIX`）。其余 11 步全部 `draft_verbatim / CLEAN`。

**对照结果：`E04` 的最终正文与原稿正文逐字节相同。**

```text
draft_raw 剥离第一个 <think>…</think> 后的正文  = 951 字符
final_answer_only_after_reasoning_strip        = 951 字符
逐字节比较                                      = IDENTICAL（无一处差异）
```

**补齐节点改了什么**：`gate_report.audit_block_missing = true`、`missing_items = ["参考文件加载状态"]` —— 原稿**整段审计块缺失**。补齐后 `post_gate_report.audit_missing_after_repair = false`、`still_missing = []`。审计块本身在交稿节点被整段剥离，用户永远看不到。

**因此对第一个问题的直接回答**：
- 有没有把原稿里正确的话改错？**没有。一个字都没改。**
- 有没有引入原稿没有的事实主张？**没有。**

作为对照，我也检查了全部 12 步的"原稿正文减去审计块 vs 最终正文"，**12/12 全部相等**。也就是说本轮交稿链路除了剥 `<think>` 和剥审计块，没有对模型正文做任何加工。这一条是干净的。

**但要连着说清楚**：`E04` 的正文里那条与输入矛盾的事实主张（第 6 节）**原稿里就有**，补齐节点没有改错它，也**没有发现它**。`gate_report.input_contradiction = []`。补齐节点做的是"补审计块"，不是"核事实"——它没有制造错误，也没有拦住错误。SKILL 自己写过这个教训（「防护装置制造虚假信心，比没有防护更危险」）；本轮的形态是更温和的一种：防护装置在它声明的范围内正确工作，而错误落在范围之外。

---

## 5. 专项二：内容在链条上的存续（独立追踪，与逐步 Oracle 无关）

我按"上一步交付里出现过、下一步是否还在、消失时有没有点名说明"逐个对象追踪。追踪对象取自交付正文本身，不取自 `projection` 字段。

### 5.1 追踪表

| 对象 | 出生 | 逐步存续 | 消失于 | 消失那步有没有点名说明？ |
|---|---|---|---|---|
| **A 待启动的上身内容验证（六项）** | `E06` | `E07` 六项全文复述 → `E08` 仅剩名字（「六项条件不变」）→ `E09` 仅剩名字（「原六个条件没有变化」） | **`E10`** | **没有。** `E10`、`E11`、`E12` 全文无"上身"二字 |
| **B 咨询向任务（老板要的线索）** | `E08` | `E09`「咨询向任务：继续挂起，直到咨询承接链路核实；本周不占位」 | **`E10`** | **没有。** `E11` 全文无"咨询/线索"；`E12` 唯一的"线索"是「账号成熟度线索」，词义无关 |
| **C「通勤衣橱一件多穿」观察项** | `E04` | `E05`—`E09` 每步点名维持 | **`E10`** | **没有。** `E11` 无。`E12` 重新出现，但来源是本轮 `market_observations` 输入 + 用户直接发问，**不是从基线恢复的** |
| **D「选品类内容完播表现」观察项** | `E05` | `E06`、`E07` 点名维持 | 正文内容 **`E08`**（只剩一句总括），名字 **`E10`** | 只有总括句「其余已登记的观察项和判断保持不变」；`E10` 起连总括都没有 |
| **E 第 4/5/6 位（第二周三条长期价值内容，各自的唯一主要工作）** | `E09` | `E10` 只提到第 4 位（作为"不重复"的参照） | **`E11`** | **没有。** `E11` 只列第 1、2、3 位；`E12` 同样只列第 1、2、3 位 |
| **F 任务二／任务三（价值判断拆解／通勤场景与搭配逻辑）** | `E02` | `E03`—`E06` 点名维持，`E07`／`E08` 以「另外两个任务位」维持 | `E09` 起改称第 4/5 位，随 E 一起在 `E11` 消失 | `E09` **未说明**这次改名映射；此后无 |
| **G 四周期（"初秋通勤衣橱"第一阶段上新四周）** | 输入 + `E01` | `E01`—`E03`、`E09`（「第三、四周仍按原四周期计划继续」） | **`E10`／`E11`** | **没有。** `E12` 直接谈"下一周期"，全序列无一步说明这四周结束了 |
| **H 节奏 每周 3 条** | `E01` | 全序列存续（9/11 步在自己的正文里重述，其余靠机器逐字保留块） | 未消失 | 不适用 |

**一句话**：12 步跑完，投影机制真正保住的是 **H 一项**。A—G 全部靠模型自己每轮重写来续命，一旦某轮聚焦回答不提，就永久没了。

### 5.2 "一切照旧"类与实际不符的话

| 步 | 原话 | 与实际不符在哪 |
|---|---|---|
| `E10` | 「没有新的反馈，**也不该因为这个调整改变既有判断**」 | 这是一句真诚的总括保持声明。但这一步之后，A/B/C/D 四个"既有判断"全部从状态里消失。声明为真、载体未兑现 |
| `E11` | 「其余判断保持不变：**产能、节奏、事实边界**」 | 这是**枚举式**的"其余"，只列三项。A/B/C/D/E/G 一个都不在枚举内，且此时它们已经不在基线里了。这句话读起来像"其他都还在"，实际覆盖面只有三项 |
| `E12` | 「其余判断保持不变：产能每周三条（……）、节奏、事实边界（不新增未登记的商品事实）」 | 同上，同样是三项枚举 |
| `E08` | 「其余已登记的观察项和判断保持不变」 | `E08` 点名保住了 A 和 C，但把 D 的名字也一起交给了这句总括；`E10` 之后总括所指的对象已不存在，这句话在链条上无处落地 |

我不把这几句判成模型撒谎：`SKILL.md` 第 403 行写的规则是「要么继续、要么点名处置、**要么至少写一句"其余判断保持不变"**」，三条并列，模型选了第三条，符合规则字面。**问题是规则的第三条在当前载体上不产生任何状态效果**——写了这句话，被它兜住的内容照样从投影里掉出去。**这是规则与载体之间的缺口，不是模型的违规。**

### 5.3 这个缺口和第 4 轮 `E07` 的关系

冻结件 v1.1 §0.1 记录的第 4 轮事故是：**零产出**被当作有效判断整体覆盖上一基线。本轮的 `E10` 不是零产出——它是一份合格的、有依据的**聚焦**回答。v1.1 §3.1 明写这种聚焦回答不该被判失败，我同意，也照此判了 `E10` 成功。

但两者在**状态层面的后果相同**：上一基线里的对象整体消失、`E11` 起再未出现、没有任何机制察觉（`continuity.dropped_without_notice` 在 12 步里全部是 `[]`）。

v1.1 的修复把"零产出"这条路堵住了，**没有堵住"合格但聚焦的产出"这条路**。而后者在真实使用中远比前者常见——本序列 12 步里就出现了两次（`E04`、`E10` 是仅有的两步完全不重述节奏的）。

---

## 6. 专项二的附带发现（单列）：`E04` 的投影旧状态压过了当轮权威输入

这是我给 `E04` 判 `不足` 的直接依据，也是本轮唯一一处**内容错误**（而非内容丢失）。

```text
E04 的 workflow_inputs.account_context 逐字：   actual_capacity: 3 条/周
E04 的交付正文逐字：                            「当前实际产能只有一条（苏禾请假）。这一条已经排给选品决策记录」
```

- `_steps.json` 里 `E04` 的 `context_overrides` **只覆盖 `market_observations`**，`actual_capacity` 回落到 `base_context` 的 `3 条/周`。也就是说冻结件本身把 `E04` 设计成一个**产能已恢复**的轮次（`E03` 的覆盖值写的就是"1 条（**本周实际**，苏禾请假）"）。
- 这不是措辞问题，是**承重依据**：它是三条「关键依据和假设」之一，并直接支撑两条取舍（「不让**唯一产能**去赌一个无素材支撑的方向」「如果跟，需要**替换任务一**」）。
- 连带后果：`E04` 因此继续维持「任务二、任务三……本周不排」，而按 `E03` 自己立下的条件（「下周回到三条产能时重新排入」），这一轮本该把它们排回来。
- 同一轮的投影基线里其实**同时存在**正确值：机器逐字保留块写着「当期实际可用也是每周3条」。所以 `E04` 的基线内部自相矛盾，模型选了 `E03` 正文那一半，而不是输入槽位、也不是保留块。
- 三个理应发现它的机制都没有发现：`gate_report.input_contradiction = []`、`input_contradiction_advisory = []`、`projection` 的一致性检查（Founder `CONTINUE_TASK` 第 2 项要求的"投影前后关键对象/版本/数量一致性检查"）只覆盖 `每周3条` 这一个对象，不覆盖"基线里的旧状态与当轮输入槽位冲突"。

**`E05` 在下一步自行纠正了它**（「本轮的实际产能已经恢复到每周三条」），所以链条没有一路错下去。这是本轮的一个正面事实，但它靠的是模型自己看输入，不是任何机制。

**归类**：v1.1 §3 治的是"内容丢失"，这一处是"**内容过期但仍被承载，并压过当轮权威输入**"。同一个投影槽位上的另一个方向的故障，冻结件目前没有对应条款。

---

## 7. 专项三：`projection` 的 `objects_before` / `objects_not_restated` 与正文是否一致

### 7.1 `objects_not_restated` —— **11 步里有 9 步是错的**

`projection.objects_not_restated = ["每周3条"]` 被记在 `E02`—`E12` 全部 11 步上。我逐份读正文核对"这一步到底有没有重述节奏"：

| 步 | 记录声称 | 正文实际 | 判定 | 正文原句 |
|---|---|---|---|---|
| `E02` | 未重述 | **重述了 4 次** | ❌ 记录错 | 「按每周三条来安排」「每周三条各对应一条线」「你们期望每周三条，团队基线产能每周三条，当期实际可用也是每周三条」 |
| `E03` | 未重述 | **重述了** | ❌ 记录错 | 「产能按实际来：期望每周三条、团队基线三条，但这周苏禾请假，实际可用一条」 |
| `E04` | 未重述 | 确未重述 | ✅ 记录对 | — |
| `E05` | 未重述 | **重述了** | ❌ 记录错 | 「本轮的实际产能已经恢复到每周三条，和你们期望的三条、团队基线的三条一致」 |
| `E06` | 未重述 | **重述了** | ❌ 记录错 | 「期望发布量每周三条、团队基线每周三条、当期实际产能每周三条，三者一致」 |
| `E07` | 未重述 | **重述了** | ❌ 记录错 | 同上句（`E07` 正文原句一致） |
| `E08` | 未重述 | **重述了** | ❌ 记录错 | 「产能上没有新缺口。每周三条的期望、基线和当期可用还是一致的」 |
| `E09` | 未重述 | **重述了 2 次** | ❌ 记录错 | 「每周三条的期望、团队基线、当期可用都是三条」「期望每周三条、基线每周三条、当期可用每周三条」 |
| `E10` | 未重述 | 确未重述 | ✅ 记录对 | — |
| `E11` | 未重述 | **重述了** | ❌ 记录错 | 「产能每周三条，期望、基线和实际一致，没有资源缺口」 |
| `E12` | 未重述 | **重述了** | ❌ 记录错 | 「其余判断保持不变：产能每周三条（期望、基线、当期实际一致，三条占满、无余量排额外内容）」 |

**根因（可复算）**：`E01` 用阿拉伯数字写「每周**3**条」，对象串因此被抽成 `"每周3条"`；`E02` 起模型一律改用中文数字「每周**三**条」。重述判定显然是按 `"每周3条"` 这个字面串做的包含匹配，中文数字一律匹配不上。

**后果**：分支 2 在 `E02`—`E12` 每一步都被误触发，机器把 `E01` 那两句话追加了 11 次。追加的是同一段、`E01` 的原文、一字未改（我核过：全序列只有 1 个 distinct 追加块，两句话都能在 `E01` 正文里逐字找到）。

**这个误判在一处产生了实质坏结果**：`E03` 那一轮实际产能是 1 条，`E03` 正文自己写清了；但机器仍把 `E01` 的「当期实际可用也是每周3条」逐字追加进去。于是喂给 `E04` 的基线里同时躺着「实际可用一条」和「当期实际可用也是每周3条」两句互相矛盾的话，**没有任何一致性检查发现它**——而 `E04` 恰好在这一点上出了错（第 6 节）。误判本身不是 `E04` 出错的直接原因（`E04` 两句都没照抄，它照抄的是 `E03` 正文），但它证明这条"逐字保留"通道会把**过期状态**原样送进下一轮，而机制不会察觉。

### 7.2 `objects_before` —— 不是"记错"，是**定义覆盖不到**

`objects_before` 在 `E02`—`E12` 全部 11 步都只有 `["每周3条"]` 一项；`E01` 为空（无基线）。同时 `gate_report.baseline_objects.tasks` 在 **12/12 步全部是 `[]`**。

按 v1.1 §3 的定义，关键对象 = **任务位号 `P\d+`** ＋ 节奏 `N 条/周`。我对 12 份交付正文全文正则扫描 `P\d+`：**零命中，一次都没有**。本轮模型用的是「任务一／任务二／任务三」和「第 1 位」—「第 6 位」。

所以：

- **记录没有对 `objects_before` 撒谎** —— 它忠实执行了 §3 写死的那个模式；
- **但 §3 写死的那个模式在本轮一次也没匹配上**。任务位保护分支**全序列 12 步 0 次生效**；
- 换句话说，v1.1 为"不再丢任务位"新增的那条保护，**在这一轮里是空转的**。第 5 节记录的 A—G 七个对象全部丢失，根因就在这里：它们从来没有进过 `objects_before`，因此分支 2 从来没有机会保护它们。

我另查了产品语义主稿 `m3-account-content-operator-semantic-v1.0/`：**没有任何一处规定任务位要写成 `P1`/`P2`/`P3`**。也就是说 `P\d+` 这个模式在产品语义里没有依据，它只能匹配上某一次实测中模型偶然采用的写法。换一个采样、换一种编号习惯（本轮就是），保护即失效。

### 7.3 附带：总括语句检测也漏了一次

`E10` 的 `projection.mode = MERGED_KEEP_PRIOR`、`continuity_status = MERGED`、`blanket_carry_statement = false`、`objects_covered_by_blanket = []`。但 `E10` 正文里写着「没有新的反馈，**也不该因为这个调整改变既有判断**」——按 SKILL 第 403 行的口径，这就是一句总括保持声明。检测器判成"没有"。

这一次漏检**没有改变结果**（该步唯一被追踪的对象 `每周3条` 无论走哪条分支都会被逐字保留）。我把它记下来，是因为它和 7.1 是同一类问题：**判定靠字面串匹配，模型换个说法就失灵**。

---

## 8. §7 结论与 §8 声明上限

### 8.1 结论

| 项 | 结果 |
|---|---|
| 12 步逐步 | 11 步 `成功`，`E04` `不足` |
| 序列级 · 局部性 | **只部分成立** |
| 序列级 · 证据身份分离 | 成立（一半分支未被检验） |
| 序列级 · 责任边界 | 成立 |
| **`ECC-M3-LONGITUDINAL-001`（v1.2 载体 / g1fix 轮）** | **`FAIL(INSUFFICIENT)`** |
| `M3-AC-17` | `FAIL(INSUFFICIENT)` —— 本绑定下 |
| `M3-AC-11` | **未独立成立**：`E09`／`E10`／`E11` 三步的 Campaign 接缝行为逐步全部 `成功`，且 `E11` 恢复的确实是含 `E10` 变更的当前基线（这一条是本轮最干净的证据）；但 `E10` 之后的状态截断发生在同一区间内，`E11` 所能"恢复"的基线已被削掉大半，因此不能说"到期恢复当前有效基线"这条能力在**完整**基线上被验证过 |
| `M3-AC-10` | 作为纵向证据可用但**不完整**：四情形中"反馈充分且范围明确"（`E05`）、"多条一致"（`E06`）、"互相冲突/不足"（`E07`）、"观察窗未结束"（`E07`）四种在正文层面全部出现且处理各不相同，这部分是成立的；但见 8.2 第 2 条的覆盖缺口 |
| 序列是否跑完 | 跑完，`12/12` `succeeded`。**不是 `NOT_VERIFIED`**——没有传输层失败，`_run_index.json` 全部 `http_status 200 / workflow_status succeeded` |

### 8.2 本判定**不能**建立什么

1. **不能**建立"v1.1 §3 的三条分支在真实使用中足以承载周期状态"。它在本轮只保住了一个对象，另外七个对象全丢，且丢失未被任何机制记录（`dropped_without_notice` 全序列为 `[]`）。
2. **不能**为"模拟/测试反馈不得顶替真实反馈"提供任何证据：本序列 12 步**没有任何一条模拟或测试身份的反馈输入**（全部 `is_test=false, is_simulated=false`）。这半边完全未被触发。
3. **不能**建立真实跨会话持久化能力（v1.0 §8 原条款）：投影仍由执行侧机械回填，未走 M2 真实写入／接受／版本晋升。
4. **不能**建立稳定行为分布：单次采样，12 步各一次调用。本轮与第 4 轮在"模型是否使用 `P\d+` 编号"这一点上的差异，正好说明单次采样的行为面很窄。
5. **不能**追溯改写第 4 轮任何结论（v1.1 §0.4），本判定也未尝试。

### 8.3 我认为最该先修的一件事（只说观察，不越权给方案）

`objects_before` 的抽取器是整条链的**唯一**决定性开关：进不了这个集合的对象，v1.1 §3 的三条分支对它一视同仁地等于不存在。本轮它只装进去了一个对象，而且靠的是一个在产品语义里没有依据的编号模式（`P\d+`）加一个大小写敏感式的字面串匹配（`每周3条` vs `每周三条`）。

**在这个抽取器的覆盖面被独立验证之前，`gate_report.continuity` 里的 `dropped_without_notice: []` 不构成"没有内容被丢掉"的证据。** 本轮它是 `[]`，而实际丢了七个对象。

---

## 9. 协议自检

### 9.1 我实际读了哪些文件（全部路径相对 WT = `/home/faye/diyu-demo-worktrees/m3-account-content-operator-v1`）

**冻结判据**
1. `M3_ECC_LONGITUDINAL_001_FROZEN_v1.1.md` —— 全文
2. `M3_ECC_LONGITUDINAL_001_FROZEN_v1.0.md` —— 全文（v1.1 §「逐字继承」指过去的正文）

**本轮证据目录 `account-operations/evidence/ep07-longitudinal-v12-g1fix/`**
3. `_steps.json` —— 全文
4. `_run_index.json` —— 全文
5. `_CARRIER_AUTHORIZATION_EXCERPT.md` —— 全文
6. `E01.json` — `E12.json` —— 12 份。逐份读了 `workflow_inputs.account_context`、`workflow_inputs.user_request`、`final_answer_only_after_reasoning_strip`、`projection.*`、`outputs.gate_path/gate_status/gaps_closed/cycle_state_carry`、`standing_cycle_baseline_in`。另外读了：`E04` 的 `draft_raw` 全文（唯一的 `gate_repaired` 步）、`E01/E04/E06/E07/E10/E11/E12` 的 `gate_report` 与 `post_gate_report` 全文及其审计块、`E06` 的 `loaded_references` 全文。其余步的 `draft_raw` 我只做了机械比对（剥 `<think>` 与剥审计块后与最终正文比长度和相等性），**没有逐字通读它们的推理块**。

**被判对象**
7. `account-operations/skills/operating-one-account/SKILL.md` —— 全文
8. `account-operations/skills/operating-one-account/references/fashion-and-market.md` —— 全文（经 `E06` 输入里的 `loaded_references` 载荷读到，用于核对 `E06`/`E07` 对该附件的引用是否属实）
9. `account-operations/skills/operating-one-account/references/six-skill-methods.md` —— **未读**（本轮全部 12 步该附件均标 `NOT_LOADED`，不影响判定）

**产品语义主稿 `m3-account-content-operator-semantic-v1.0/`**
10. `M3_ACCOUNT_CONTENT_OPERATOR_SEMANTIC_COMPILATION_v1.0.md` —— §10 末段、§11 栏目连续性、§12 Campaign 覆盖、§13 Brief 接缝、§14 六 Skill 方法继承全文；其余各节仅做关键词检索
11. `ENGINEERING_HANDOFF.md`、`SOURCE_AND_BUILD_MANIFEST.md` —— **仅关键词检索命中行**，未通读
12. `m3-account-content-operator-semantic-v1.0/skill-source/` —— 仅目录列表，未读文件内容

**目录列表（未读内容）**
13. `account-operations/evidence/ep07-longitudinal-v12-g1fix/` 的 `ls`
14. `account-operations/skills/operating-one-account/` 与其 `references/` 的 `ls`

### 9.2 禁读清单核对

我**没有**读以下任何一项，也没有对它们做过检索：

`M3_CHECKPOINT_*`｜`M3_ACCEPTANCE_CRITERIA_FROZEN_*`｜`M3_ECC_REBIND_*`｜`M3_FOUNDER_*`｜`GATE_DEFECTS_*`｜`DEFECT_*`｜任何 `*_VERDICT_*`｜`ep06-*`｜`ep08-*`｜`ep07-longitudinal-v12/`｜`ep07-longitudinal-v12-aborted/`｜`ep07-longitudinal-v11-run2/`｜`ep10-*`—`ep14-*`｜`account-operations/tools/`｜`account-operations/tests/`｜`account-operations/fixtures/`。

我**没有**执行 `git log` / `git show` / `git reflog`，也没有执行任何其他 git 命令。

**需要主动披露的两处间接接触**（我认为不构成违规，但由你判断）：
1. 我读的两份冻结件与 `SKILL.md` 里出现了若干禁读文件的**文件名**与它们记录的实测结论摘要 —— 例如 v1.1 §0.1 描述第 4 轮 `E07` 事故、v1.1 §0.4 提到 `ep07-longitudinal-v11-run2/`、`SKILL.md` 头部提到 `M3_ECC_REBIND_003_FROZEN_v1.0.md` 与 `M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md`、末尾提到 `v1.1` 的两次实测。这些出现在**我被要求读的文件正文里**，我无法不看到。我没有据此去打开任何被点名的文件。
2. `_CARRIER_AUTHORIZATION_EXCERPT.md` 明确告知"上一轮有两名判定者……只能标 `NOT_VERIFIED`"。这条信息影响了我第 1.4 节的处理方式（我据摘录采信而非标 `NOT_VERIFIED`），我在那里已经写明这是"依摘录采信"。

我对 `ep07-longitudinal-v12-g1fix/` 执行 `ls -la` 时，输出的第二行显示其父目录 `evidence/` 的链接数为 27（即存在若干同级证据目录），但目录名一个也没有出现在输出里，我也**未进入任何一个**。为控制这一点，我全程只用绝对路径直读 `ep07-longitudinal-v12-g1fix/` 下的文件，未对 `evidence/` 做过任何列举或递归检索。

### 9.3 我不确定的条目

1. **`E04` 判 `不足` 的定级**。产能矛盾这一条**不在 `E04` 的逐步 Oracle 三档里的任何一档**：`成功` 档不提它，`不足` 档写的是"认定为机会但缺反证或到期条件"（`E04` 没认定为机会），`失败` 档写的是"仅凭热度授权生产／把外部观察写成本账号因果"（都没有）。我是按 v1.0 §6.4「给出可核查的引用位置」＋ SKILL〈必填项闸门〉"输入里写着的事，不许说没有"这条自定规则把它归入 `不足` 的。**如果你认为逐步 Oracle 是封闭枚举、Oracle 没写的一律不影响该步判定，那 `E04` 应改判 `成功`，但整条序列仍因序列级局部性只部分成立而维持 `FAIL(INSUFFICIENT)`。** 这一条我请你复核。
2. **`E04` 的"保留观察的来源/平台/时间/范围"**。正文保住了平台（视频号）、范围（女装赛道）、时间（近两周）、有效期（复验期），**没有**保住来源（"用户本人在视频号后台与同类账号的比较观察"），只写到"外部观察"这个类别层。四项缺一。我**没有**单独据此判 `不足`（`E04` 已因第 6 节判 `不足`）。如果单看这一条，我倾向"有但不够"，即 `不足`；但也可以认为该条款的目的是防止证据被升格，而 `E04` 从未升格它。这一条我给不出无争议的裁定。
3. **`E11` 的"说明需要什么接受权限"**。见 2.1 `E11` 条末段。我判 `成功`，但这是我在两条互相拉扯的规则之间做的取舍，不是无争议的。
4. **`E09` 对"第 1 位与第 3 位"的读法**（第一周两位 vs 每周各两位）。冻结件里没有可核的判据，我记录而未判定。
5. **`E10` 那句「也不该因为这个调整改变既有判断」算不算总括保持声明**。我倾向算（`SKILL.md` 第 403 行的口径），检测器判不算。这一次两种判法结果相同，所以我没有据此改变任何判定。

### 9.4 冻结件里我无法核验的条款

| 条款 | 为什么无法核验 |
|---|---|
| v1.0 §0「本文件与 `_steps.json` 在任何一次运行之前写成并作为独立 commit 提交」；v1.1「`frozen_at` = 本 commit，**先于本版本任何一次运行**」 | 核验它必须查提交历史，而 `git log` / `git show` / `git reflog` 在我的禁令清单里。**A2 的"先后"这一条我完全无法执行。** 我只能记录：`_steps.json` 内的 12 步 Oracle 与本轮记录内容一致，未见事后改判的痕迹；但"一致"不等于"在先"。 |
| v1.0 §5 / v1.1 §5 的运行绑定锁定 | 摘录说明了差异与授权，我据摘录采信（见 1.4）。绑定值本身（草稿图哈希 `e3c4e9df…`、`SKILL.md sha256 343758f3…`）我没有独立复算——复算 `SKILL.md` 的哈希我做得到，但摘录里那个值属于授权链的一部分，独立复算它并不能证明授权成立，所以我没做。 |
| v1.0 §5「任一锁定变量变化 ⇒ 整条序列置 `STALE` 并整条重跑；序列不可局部重跑」 | 我能核到本轮 12 步的链条自洽（`chars_after(N) = chars_before(N+1)` 全对，`workflow_run_id` 12 个互不相同，`timestamp_utc` 从 `2026-08-27T00:30:53Z` 到 `2026-08-27T01:01:34Z` 严格单调递增）。我**不能**核验"没有复用第 4 轮任何一步"——那需要读 `ep07-longitudinal-v11-run2/`，禁读。 |
| v1.0 §2「Dify workflow 类型 App 没有会话记忆（LLM 节点 `memory: null`）」 | 这是关于载体配置的断言。我读不到工作流 DSL（`account-operations/tools/` 禁读，也没有别的入口）。我能间接支持它：12 次调用的 `workflow_run_id` 互不相同，且 `E07` 的正文只复述了投影里存在的内容、没有复述投影里已经不存在的内容（例如 `E07` 没有提到 `E02` 原文里那些已被挤出基线的细节）。**这是间接支持，不是证明。** 于是 v1.0 §2 那句"'某步靠会话记忆而非投影承载'在本构造下不可能发生"，我只能标 `NOT_VERIFIED(NOT_CHECKED)` 而非确认。 |
| v1.0 §7「失败：模拟/测试反馈在任一步被写成真实经营反馈」 | 无法触发（见 8.2 第 2 条）：本序列没有任何模拟/测试身份的输入。标 `NOT_APPLICABLE`，且这是**运行前由输入构造决定**的，不是失败后追溯填写的。 |
| v1.0 §8「不能建立真实跨会话持久化」 | 这是声明上限，不是可核验条款；我在 8.2 照抄继承。 |

---

```text
END_MARKER
= ECC-M3-LONGITUDINAL-001-VERDICT-V12-G1FIX-v1.0-END
```
