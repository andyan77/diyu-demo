# M3 · REBIND-006 · DD-1～DD-4 修法判据（版本化冻结 v1.0）

| 项 | 值 |
|---|---|
| `ecc_id` | `M3-ECC-REBIND-006` |
| `task_id` | `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`（不变，本轮不是 `NEW_TASK`） |
| 前身 | `M3_ECC_REBIND_005_FROZEN_v1.0.md` + `_ERRATA_001` + `_ERRATA_002` |
| 与前身的关系 | **后继版本，不覆盖**。REBIND-005 的正文、判据与已落盘证据一字未动 |
| 冻结时间点 | 本文件提交时；**在本轮任何模型调用之前**（本轮零模型调用） |
| 授权 | `CONTINUE_TASK_NO_MODEL_PREFLIGHT`（2026-08-27）第 1 条 |
| 载体版本 | `gate_version: v1.5` / `post_gate_version: v1.5`（**尚未部署进 Dify**） |

---

## 0. 这一轮在修什么

第 8 轮（候选 v1.4.2）的正式取证批次跑完之后，`ep26` 查实：61 例真实运行里 12 例被拒收，
其中 **10 例是误拒**，且 **9 例出自第 8 轮自己的修法**。本轮在同一批已落盘记录上做零模型
重放时又查出**第 11 例误拒**——A/B 的 B 臂 `FX-M3-HOLD-02`，第 8 轮的拒收台账没覆盖 A/B 臂。

四个缺陷共一个病理：**为保护某条判据而加的检查，对着这条判据自己要求的行为开火。**

REBIND-005 §3 给 G-4 定下的方法义务（两个方向都量、在真实语料上量、不许用自己造的句子
出题）继续有效，本轮**照单执行并扩到全部四条**。

---

## 1. 最高失效节点：不修检测器，修成因

第 8 轮的收口建议已经点名：最高失效节点是 `gate_main.py` 的 `required_audit_lines`，
**不是** `D-1`。本轮照此执行。

`D-1`（「模型声明过的持续位在补齐之后消失」）是一个**正确的检测器**。它开火恰恰暴露了
补齐骨架自己的构造缺陷。去改 `D-1` 会让持续位重新静默消失，回到 `REBIND_004 §2.1`
已经被证伪的那句自称上。

---

## 2. 四条修法判据（冻结）

### 2.1 DD-1 · 补齐骨架的义务集

**成因**：`required_audit_lines` 只按**输入侧**持续位生成 `POS ::` 行。模型本轮自己新声明的
位从不进骨架；补齐节点的系统提示词第 5 条写着「一行不多、一行不少」，于是它照做——把
不在骨架里的那些行删掉。`positions_unaccounted` 只算输入侧差集，对新增位天然为空，
于是三个计数器一起显示 `[]` 而内容全丢。第 8 轮 11 例踩中，10 例被拒收。

**判据**：

```text
骨架义务集 = 输入侧持续位  ∪  模型本轮草稿里已声明的持续位
```

- 骨架里只**保留模型自己写下的 id 与状态**，不代模型创造 id、不代模型选状态；
- 锚点仍留占位符，由补齐节点指回最终正文，复检逐字核对；
- **不在复检侧加「自动补回」兜底**。那等于闸门替模型重新写出 `POS ::` 行，正是 D-3
  刚修掉的自证回路。骨架里已经点名还被删掉，那是补齐节点真的出错，该拒。

**与 D-3 的关系**：不冲突。D-3 剥的是「不在草稿里的行」；本条进骨架的行**全部来自草稿**，
剥离逻辑一个字不改。

**作用域边界**：格式不合法的 `POS ::` 行（状态写错、字段不足）进不了 `decls`，因而也不进
骨架。这类行本来就由 `positions_bad_lines` 阻断，不额外处理。

### 2.2 DD-2 · 槽位权威值只认主句

**成因**：`1 条（本周实际，低于基线 3 条）` 里，判据把括注中的对照值 `3 条` 当成了权威值，
于是正文里**正确**的「本周实际产能只有 1 条」反被判成用旧值压输入。

**判据**：

```text
槽位权威速率 = 槽位里**第一个**数量；它后面的数量都是解释性对照，不进权威值。
第一个数量没写周期时，允许从槽位后半段借一个周期词——借的是周期，不是值。
借不到周期 ⇒ 该槽位没有速率主张，G-4 对它整槽豁免。
```

**作用域边界（如实记）**：这条规则认不出反序写法（`陈晚 2 条 + 导购 1 条，合计每周 3 条`）。
真实语料 14 个不同槽位取值里没有这种形状。出现了是**漏检**，不是误报。

### 2.3 DD-3 · 取舍量、部分量与被否掉的方案，都不是速率主张

**成因两处**，都在正文侧：

- `本周必须让掉另外 2 条` 被读成「目标变成 2 条/周」。`SKILL.md` O-3 恰恰**要求**写清让掉了哪几条。
- `一周五条这周做不到，不是安排问题，是真实产能只剩两条` 被读成「当前 5 条/周」。
  被否掉的是「五条」这个方案，真正的赋值在后半句，且与输入一致。

**判据三条**：

```text
a) 部分量／移除类前缀：数字前面挂着「另外／其余／其中／剩下／余下／多出」，
   或「让掉／让出／让渡／去掉／减掉／删掉／压掉／省掉／挪走／推迟／延后／牺牲／留给」
   （14 字回看窗、中间不跨标点）⇒ 这个数是整体里被拿走的那一块，不是整体的新值。
   **赋值类动词不进这张表**：`砍到／减到／降到／压到 2 条` 说的是「现在变成 2 条」，
   是真覆盖，必须仍然抓得到。
b) 后置可行性否定：数字后面 10 字内、中间不跨标点出现
   「做不到／做不了／做不完／办不到／排不下／排不开／达不到／完不成／不可能／不现实／
     撑不住／顶不住／做不出／来不及」⇒ 这个数是被否掉的方案，不是当前值。
c) 前置否定的回看窗不许跨小句：`_negated_at` 的回看在遇到「，、：」时截断。
   「…这周做不到，**不是**安排问题，是真实产能只剩两条」里，前一小句的「不是」
   会把后一小句的真赋值一起否掉——那是漏检。
```

**被排掉的数必须留痕**：`stale_value_override_advisory` 里逐条记 `已按取舍量／后置否定排除 …`。
漏检要看得见，不能因为被排掉就消失。

### 2.4 DD-4 · 否定必须落在参考文件上，不是落在账号事实上

**成因**：`v1.2` 起的检查只要求同一分句里同时出现「参考类名词」与「否定类词」。于是
`对照服装经营参考资料判断了试穿证据的缺失边界——本周期没有试穿素材` 被判成与清单矛盾。
缺的是**试穿素材**（账号事实缺口），不是参考文件；`SKILL.md` O-6 恰恰**要求**把这两类缺口分开写。

**判据**：

```text
否定必须**紧贴**参考类名词才算说的是参考文件本身：
  参考类名词 + ≤4 字（不跨标点） + 否定类词      例：「参考没有附上」「参考资料未加载」
或 否定类词 + ≤4 字（不跨标点） + 参考类名词      例：「拿不到那份参考」
两侧都够不着 ⇒ 否定说的是别的东西，不判矛盾。
```

---

## 3. 方法义务（继承 REBIND-005 §3，扩到四条判据）

1. **两个方向都量**：误报与漏检各自给数，不许只报一个方向。
2. **在真实语料上量**：探针句子取自真实模型输出、一字未改；只允许按写死的机械规则改写
   权威值或数字。不许用执行侧自己撰写的句子当误报率的分母。
3. **探针集合不由被测判据决定**。第一二族的探针由**旧判据**口径生成；第三族（槽位保持真实、
   正文机械改写）的探针由一个**独立解析器**生成，既不属于旧版也不属于新版。
4. **作用域边界单独归类、留在分母里**。旧判据抓到而新判据没抓到的，只有当伪造值来自
   「新判据明确声明不当作速率主张」的那个数时，才计为作用域边界并逐条给出排除理由；
   其余一律计为**新漏检**。
5. **每个新守卫必须可消融**（A5）：单独关掉它，误报集或漏检探针捕获数必须变。
   两个轴都不变的守卫不得留在判据里——本轮据此**删掉了三处**自己先写出来的东西，见 §5。

---

## 4. 本轮如实披露的已知残余

| 编号 | 内容 | 方向 | 处置 |
|---|---|---|---|
| R-1 | `／` 全角斜杠：`4 条／周` 这种写法有 5 个不同槽位取值，数量正则只认半角，这些槽位整槽落进「没有周期」，G-4 对它们从来没生效过 | 漏检 | **本轮不修**。改成 `[/／]` 只是一个字符，但在真实语料的两个轴上都量不出差别，按 A5 不为「看起来更完整」而留代码，留下这条披露 |
| R-2 | 槽位反序写法（`… + …，合计每周 3 条`）取不到正确权威值 | 漏检 | 不修，见 §2.2 作用域边界 |
| R-3 | 第三族探针里有 2 个探针新旧两版都抓不到（`…本周期这三条内容、以及…` 这类主语与数字被顿号隔开的句子） | 漏检 | 先于本轮存在，非本轮引入；不修，记录 |
| R-4 | DD-1 的修法在**补齐路**上依赖一条前提：补齐节点会把骨架里的 `POS ::` 行照发回来。零模型重放证不到这一步 | 证据等级 | 只影响 P7（误拒消失），不影响 P8（真拒恒成立，已穷举）。前提的实测支撑：第 8 轮同一批记录里，必填项骨架行 22/22 次全数复现、0 行丢失，骨架里出现过 `POS ::` 行的 3 次也 3/3 复现。正式取证必须真跑 |

---

## 5. 冻结的验收判据（本轮预检，零模型）

语料：第 8 轮已落盘的 **64 次真实运行**（行为 49 + 纵向 12 + A/B B 臂 3）。
对照组钉在 **`ef35c67`**（= 候选 v1.4.2），不是 `HEAD`——判据一提交，`HEAD` 里就已经是新版。

| 编号 | 判据 | 等级 |
|---|---|---|
| P1 | 旧判据在 128 份正文上的阻断命中全部判读为误报，且新判据全部清零；新判据不得引入任何新的阻断 | 已观察（纯函数重跑） |
| P2 | 探针「旧能抓到的新也要抓到」；新漏检数 = 0；作用域边界探针逐条给出排除理由 | 已观察 |
| P3 | 真实语料里全部「参考类名词 + 否定类词同分句」的句子，配 LOADED / NOT_LOADED 两种清单各跑一遍，行为符合预期 | 已观察 |
| P4 | 六个具名族（真实旧值覆盖／三值分离／等价单位换算／拒绝合并／取舍陈述／后置否定）逐族符合预期 | 已观察 |
| P5 | 本轮每个新守卫单独关掉，误报集或漏检捕获数必须变（A5） | 已观察 |
| P6 | 64 次运行里，新骨架没有漏掉任何一个模型草稿已声明的持续位 | 已观察 |
| P7 | 10 个已判读的误拒 + 本轮新查出的第 11 例全部消失 | **推断**（补齐路，见 R-4） |
| P8 | `E07`、`E08` 两个真拒仍被拒收 | **已观察**（穷举证明：把终稿审计块的持续位声明取遍 `输入位 ∪ 草稿声明位 ∪ {一个外来 id}` 的全部子集，E07 的 4 种、E08 的 8 种可能输出下拒收全部成立，无一反例。与补齐节点怎么写无关） |

**按 A5 删掉的三处**（自己先写出来、量不出差别，不留）：

- `SLOT_ASIDE`（先剥括注再取值）——与「第一个数量是权威值」互相遮蔽，删掉命中集不变；
- `TRADEOFF_VERB`（独立的取舍动词守卫）——与部分量前缀互相遮蔽，合并成一个族；
- `[/／]` 全角斜杠支持——两个轴都量不出差别，回退成 §4 的 R-1 披露。

---

## 6. 本文件不做的事

- 不改 `WHAT / WHY / BOUNDARY / ACCEPTANCE`、产品语义、授权或任何冻结 Oracle；
- 不改 `SKILL.md`（本轮 sha256 与上一轮**逐字节相同**）；
- 不覆盖 REBIND-004 / REBIND-005 的任何正文与证据；
- 不宣布任何合同「已接受」，不把任何状态往上推一级；
- 不部署进 Dify，不调用任何模型，不请求 Founder 产品接受。

`END REBIND-006 v1.0`
