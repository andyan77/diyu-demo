# ECC-M3-MODULE-AB-001 · 冻结判据（EP-08 公平同模型 A/B）

> `task_id`: `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> `bound_ac`: `M3-AC-18`（主）、`M3-AC-01` ③消融门半
> `frozen_at`: 本 commit（**先于任何一次 A/B 运行**）
> `arms_and_holdouts`: [`account-operations/evidence/ep08-module-ab/_arms_and_holdouts.json`](account-operations/evidence/ep08-module-ab/_arms_and_holdouts.json)
> `arms_file_sha256`: `961e74f61f1df3e99b7c57fdf86f3bb7ba7bcc6c8fc285ad0b181344036a56b5`

## 0. 时序声明（A2 先后）

本文件与 `_arms_and_holdouts.json`（含四臂**准确系统提示词全文与哈希**、三个留出场景的**准确输入**）在任何一次 A/B 运行**之前**提交。

判定用的 Rubric **不是本文件新写的**，而是 `M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md` §5.2 / §5.3 —— 那份文件于 `f5a9aca` 冻结，早于本任务全部工程结果。本 ECC 不新增、不放宽、不加严任何一条评分维度。

## 1. 四臂

| 臂 | 身份 | 系统提示词 | 字符数 | SHA-256（前 16） |
|---|---|---|---|---|
| `A` | 对照基线（AC-18 的 A） | 独立撰写的胜任通用单账号运营基线 | 11,266 | `1607429186 53b3f2` |
| `Aplus` | **辅助对照，不闸任何 AC** | A + 与 B 完全相同的两份条件附件 | 16,810 | `11a5247514f120c7` |
| `B` | 候选（AC-18 的 B；AC-01 的 B） | 冻结 M3 `SKILL.md` + `fashion-and-market.md` + `six-skill-methods.md` | 19,826 | `c18fa97a4657d960` |
| `Bprime` | 消融（AC-01③ 的 B′） | 只保留一句通用运营提示（39 字） | 39 | `4a0f99592245a887` |

### 1.1 A 臂的胜任性（§5.1 公平条件之一）

合同要求 A 是"**胜任的**通用单账号运营基线，**不得故意弱化**"。取证方式：

1. **撰写侧隔离**：A 由一个独立撰写者产出，撰写时被禁止读取 `SKILL.md`、`account-operations/**`、`M3_*`、`decision-chain/**`、语义包——否则 A 会变成 B 的派生物，对照失效；
2. **独立复核**：由第二名复核者按 10 项通用运营能力逐项检查"是否被故意写弱"，结论 `VERDICT: COMPETENT`，并明确写出"未发现故意留坑"；复核记录见 `account-operations/evidence/ep08-module-ab/A_baseline_competence_review.md`；
3. **不设长度上限**：撰写指令明确"写到你认为这是你能写出的最好版本为止"。

### 1.2 `Aplus` 为什么存在，以及它为什么不闸 AC

B 比 A 多了两份条件附件。如果 B 赢了，可能是"M3 的专业语义"起作用，也可能只是"多拿到了一份服装知识"。`Aplus` 让 A 拿到**完全相同**的两份附件，把这两个变量分开。

但 `Aplus` **不在冻结的 AC-18 判据里**。按 A2，看到结果之后不能新增闸门；本 ECC 在运行前就声明：`Aplus` 的结果**只作独立观察**，不构成任一 AC 的通过或不通过依据。

## 2. 公平条件（§5.1，任一不满足则 A/B 无效，不是"降级通过"）

```text
provider / model     = DeepSeek，deepseek-v4-flash（四臂同一 model id）
temperature          = 0.4（四臂相同）
max_tokens           = 未设置（四臂相同）
工具权限 / 来源可用性 = 四臂均无工具，均只能用系统提示词与用户消息内的信息
输入事实 / 目标 / 平台 / 产能 / 候选机会 = 逐字相同（同一 account_context + user_request）
输入 / 输出预算       = 相同（无截断、无重试、无二次采样）
```

**载体说明（如实记录的偏离）**：本 ECC 走**直连 DeepSeek API**，不走 Dify 候选 App。原因：任务专用候选 App 的系统提示词已硬编码为 M3 Skill，要把 A/A+/B′ 也跑进去必须再建 App，而合同只授权**一个** task-id 专用 App。四臂改用同一段直连 harness，公平条件因此是**四臂之间**成立的；它不建立"B 在 Dify 画布上也同样优于 A"——那超出本 ECC 的证据范围。

## 3. 留出集（Holdout）

三个留出场景 `FX-M3-HOLD-01/02/03` 的**内容**在本 ECC 之前从不存在，由一名独立撰写者产出，撰写时被禁止读取 `SKILL.md`、`references/`、`evidence/`、任何 `M3_*` 治理文档与语义包。

**留出强度的真实上限（不掩盖）**：

- `SKILL.md` 冻结于 commit `af61b82`，留出夹具在其**之后**才被撰写，**git 提交顺序可证**——因此不存在"用留出集反向优化实现"的路径；
- 但执行侧（= 实现侧）在 EP-08 运行时看得到夹具内容。因此留出强度**低于**"实现者全程不可见"的理想情形。这一条按 §AC-19"Holdout 被泄露 → 该夹具留出证据强度下降须如实记"处理：**如实降级记录，不因此自动判 FAIL，也不假装留出是完美的**；
- 三个夹具在本 ECC 之后即退出留出集，不得再用于任何后续调优。

## 4. 盲评协议

1. 每个留出场景的四臂输出，按 `sha256(case_id + SALT)` 派生的排列映射到 `甲 / 乙 / 丙 / 丁`；
2. `SALT` 与映射表写在**仓库之外**的 sealed mapping 文件里，判定者被明确禁止读取；
3. 判定者只拿到：场景输入、四份无标签输出、`M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md` §5.2/§5.3 的 Rubric 原文；
4. 判定者**不知道**哪一份是候选、哪一份是基线、哪一份是消融，也不知道四份里有一份是"只有一句话的提示词"产出的；
5. 三名判定者各自独立完成全部三个场景，互不可见；
6. **事后核验**：逐一检查每名判定者的执行记录，确认其未访问 sealed mapping、未访问 `_arms_and_holdouts.json`、未访问 `SKILL.md`。核验结果如实记入判定文件；任一名判定者被证实访问过上述文件，其判定作废并重新招募判定者。

## 5. 硬门（§5.2 原文，五项非补偿）

1. **目标忠实**：主目标未被静默改写，优先级与不可牺牲条件保留；
2. **事实**：无编造的商品、库存、价格、门店、顾客或经营事实；
3. **权限**：CTA 与表达风险不超过已授权层级；
4. **风险**：无违规手法、无暗号/藏码/诱导跳转、无无依据现实承诺；
5. **当前任务必要条件**：当前任务真正需要的业务实质未缺失。

**不得用其他维度的优势抵消任一项退化。** B 在任一硬门上相对 A 实质退化 ⇒ `M3-AC-18 = FAIL`，且不能靠增益抵消。

## 6. 增益门（§5.3 原文，七维盲评）

运营判断｜周期组合｜产能取舍｜实验设计｜反馈判断｜内容任务质量｜共同质量底线。

每维给"B 优 / 相当 / A 优 / 不适用"加一句可核查的理由（判定者眼中是"甲优/乙优/…"，映射在判定完成后才揭盲）。

```text
整体增益成立 = 多数适用维度为"B 优" 且 无维度为"A 优且实质"
```

**无效证据**（不得进入评分）：模型自评、输出长度、Skill 调用次数、规则条数、节点数量、术语密度。

## 7. 结果空间（冻结，不得事后增补）

| 情形 | 结论 |
|---|---|
| 硬门无退化 **且** 有整体增益 | `M3-AC-18 = PASS`；`M3_MODULE_AB = PASSED`；`M3_PROFESSIONAL_GAIN = PASS` |
| 硬门无退化 **但** 无整体增益 | `M3-AC-18 = FAIL(INSUFFICIENT)`；`M3_PROFESSIONAL_GAIN = NOT_VERIFIED`；`M3_MODULE_AB = NOT_PASSED`（**不是** FAIL） |
| 任一硬门实质退化 | `M3-AC-18 = FAIL`；`M3_MODULE_AB` 硬门回归失败 |
| 未跑完 | `NOT_VERIFIED`，不得用部分场景宣称通过 |

**AC-01③ 另判**：B 相对 `Bprime` 在盲评下**没有**可辨识的运营增益 ⇒ `M3-AC-01 = FAIL` 且 `M3_PROFESSIONAL_GAIN = NOT_VERIFIED`（判据原文如此，不放宽）。

**禁止重抽**：四臂三场景共 12 次运行，全部保留，失败、超时、空结果一并保留。看到结果后不得重跑任一臂、不得择优、不得丢弃。

## 8. 已知混杂（运行前声明）

1. **提示词长度不对称**：B（19,826 字符）长于 A（11,266 字符）。这是候选本身的形态，不是人为放大；`Aplus` 为分离这一变量而设。
2. **A 的撰写者与判定者同为大模型代理，且共享同一份账户级协作宪法**。这使 A 被写得**更强**，方向对 B **不利**，不构成对 B 的偏袒。
3. **单次采样**：每臂每场景只跑一次。结论是"该绑定下的一次观察"，不是稳定行为分布。
4. **判定者也是大模型**。§5.3 要求的是"盲评者按 Rubric 逐项判定并写出可核查理由"，本 ECC 用三名互相隔离的判定者 + 事后访问核验来逼近它；**这不等于人类专家盲评**，如实记录为证据强度上限。

## 9. 声明上限

本 ECC 通过，只能建立 `M3-AC-18` 与 `M3-AC-01③` 在**本绑定、本留出集、单次采样**下的模块级专业增益证据。**不能**建立：

- M5 集成增益、成品增益；
- 真实经营提升或因果提升；
- 生产就绪；
- "B 在任意场景、任意采样下都优于 A"。

```text
END_MARKER
= ECC-M3-MODULE-AB-001-FROZEN-v1.0-END
```
