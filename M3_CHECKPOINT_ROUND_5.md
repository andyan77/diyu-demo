# M3 Checkpoint 第 5 轮 · 载体 v1.1（修两个缺陷 + 整轮重跑）

> `task_id` = `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> 权威事件：Founder 2026-08-26 裁定——充值完成、推送放开、**修两个缺陷**、不需要 Qwen。
> 判据与绑定冻结于 `b835709`，**全部运行发生在它之后**，提交顺序即时序自证。

---

## 1. 一句话结论

**原来的两个缺陷修好了；修复本身引入了五个新缺陷，其中一个是设计根基错误。任务不能 `DONE`。**

---

## 2. 本轮做了什么

| 项 | 内容 |
|---|---|
| `SKILL.md` | v1.0 `5fbc85d1…` → v1.1 `7ae8e521…`；新增〈必填项闸门〉一节，**产品语义零改动**（63 行差异中仅 2 行删除，且为原地改写延长） |
| Dify 图 | 3 节点 → **7 节点**：`start → 写正文(llm) → 闸门(code) → 补齐(llm) → 取稿(code) → 复检(code) → end`，草稿 hash `e5c30c32…` |
| 运行 | **82 次**（保真 9 + 行为 49 + 纵向 12 + A/B 12），另有一轮因传输故障未跑完、记录全部保留 |
| 判定 | **六名**互相隔离的独立判定者，隔离性已机械核验为仓库产物 |
| 远端 | 任务分支已推送到 `origin/task/m3-account-content-operator-v1` |

---

## 3. 两个原缺陷的修复效果（这是本轮唯一干净的部分）

| 原缺陷 | 修法 | 实测 |
|---|---|---|
| 一 · `missing[]` 自述不可靠 | 加载器写 `<<REFERENCE_MANIFEST>>` 清单，Skill 绑定"只许回抄"，代码逐条核对正文与清单矛盾 | **58 次运行零复发**（保真 9 + 行为 49，`manifest_contradiction` 全为 0） |
| 二 · 必填项未被结构性强制 | 固定标签 + 代码闸门 + 补齐 + 代码复检 | 行为 49 例：`不足` 5→2、`失败` 1→0。第 3 轮 `失败` 的 `AC-06`、`不足` 的 `AC-02`/`AC-08`/`AC-09` 本轮**全部成立** |

**但缺陷二的修复不能宣布完成**——见 §5。行为结果变好是事实，闸门可靠是另一回事，两者不能混为一谈。

---

## 4. 四条 ECC 的判定结果

| ECC | 结果 | 决定性事实 |
|---|---|---|
| `RUNTIME-FIDELITY-001` | **整轮不成立** | 8 例 `成功`，`G6` **`无结果`**：模型零正文，闸门补一句话后报 `gaps_closed: true` |
| `RUNTIME-BEHAVIOR-002` | 47 `成功` / 2 `不足` / 0 `失败` / 0 `无结果` | `B03-N3` 过度保守（全局硬停而非局部降级）、`B14-1` 判据自相矛盾致降档 |
| `LONGITUDINAL-001` | **`失败`** | `E07` `失败`：三条冲突反馈完整在输入里，模型称"核心输入缺了"并零产出，**闸门因自报"冲突反馈=否"而全绿**；`E04` `不足` |
| `MODULE-AB-001/002` | **`NOT_VERIFIED`** | 盲评被三名判定者独立证伪：候选臂的标签是唯一结构指纹，不读任何文件即可去盲 |

### 4.1 A/B 的硬门结果（独立观察，**不作为任一 AC 依据**）

| 臂 | 三名判定者合计判定的硬门实质退化 |
|---|---|
| `B`（候选） | **0** |
| `Aplus`（基线＋附件） | 0 |
| `A`（胜任基线） | 4 |
| `Bprime`（删掉 M3） | **8** |

三名判定者的自检节均显示他们把 `Bprime` 误认为基线、**未识别出候选 `B`**。这提高观察可信度，但盲评的**前提**已被证伪——不得因"通道实际没被利用"把等级往上推（A2）。

---

## 5. 五个新缺陷（全部实测，来源逐条可查，详见 `evidence/ep10-closeout-v11/GATE_DEFECTS_FOUND_v1.1.md`）

| # | 缺陷 | 复现率 | 谁能修 |
|---|---|---|---|
| A | 闸门给**零交付**盖合规章 | 保真 1/9 | 执行侧 |
| **B** | **整个闸门建在"模型如实自报"之上——这正是原缺陷否定的前提** | 行为 3/49 自报行缺失 + 纵向 `E07` 自报错误 | 执行侧 |
| C | 闸门标签是候选臂唯一指纹 ⇒ A/B 盲评失效 | 三名盲评者独立发现 | **需 Founder 决定** |
| D | 闸门标签违反 `SKILL.md` 自己的输出条款 | 盲评者 2 不知情下按 Rubric 判出 | 执行侧 |
| E | **裸标签**形态整体逃逸检测 | 行为 `B08-P` 4 处装饰性填空一处未检出 | 执行侧 |

另有一条**产品级**缺陷（与闸门无关）：`E07` 的零产出回填后把 P1/P2/P3 与四周节奏从投影中挤掉，`E08` 起再未出现，**无任何机制察觉**。静默状态损坏，登记为新任务候选。

### 5.1 缺陷 B 的责任认定

修缺陷一时得出的原则逐字写进了 `SKILL.md` v1.1：

> **凡是能从输入里逐字抄的事实，就不要让模型回忆。**

然后修缺陷二时，把整个闸门建在**模型自报四个触发标志**上。一个会漏写探索六项的模型，同样会自报「探索提案=否」。**用一条原则修好了第一个缺陷，又在第二个缺陷上违反了同一条原则。**

执行侧的 `trigger_crosscheck` 实际抓到了 `E07` 的矛盾，但被设成 advisory 而非阻断。**设成阻断，`E07` 就被拦住了。**

---

## 6. 执行侧自己的三处错误（不推给判定者、不推给环境）

1. **`_steps.json` 未及时复制**：纵向判定者的 allow-list 里没有逐步 Oracle，它用 §4/§6.5/§7 + SKILL 机械判据重建了判准并声明了边界。**后果**：纵向的 `失败`/`不足` 结论稳；10 个 `成功` 降级为「重建判准下成立，未经冻结逐步 Oracle 核对」。
2. **隔离边界画错**：保真与纵向两名判定者**各自独立**撞上同一副作用——冻结件锁定的是直连载体与 `af61b82` 版 Skill，本轮实际是 Dify 图 + `carrier_revision v1.1`，而授权链在被禁读的 `REBIND` 文件里。两人都没有推断、直接标 `NOT_VERIFIED`，**做法完全正确**。责任在执行侧：授权链本该以摘录下发。
3. **隔离核验器两次假阳性**：第一版用 ±120 字符窗口误判清单矛盾（冒烟阶段修）；本轮又把判定者用 `cat > file <<'EOF'` 写进自己判定书的禁读声明当成了读取。第 3 轮修过 `Write.content` 这个形态，本轮它从 `Bash` 的 heredoc 绕了回来。**这个核验工具本身缺正反两向的夹具**，登记为待补。

---

## 7. 本轮 AC 状态矩阵

**口径**：`条件成立` = 判定者按冻结 Oracle 判定通过，但带一个**本轮未由判定者核验**的共同前提（§6 第 2 条的载体授权链）。执行侧**不把 `条件成立` 自行升级为 `PASS`**。

| AC | 本轮状态 | 依据 |
|---|---|---|
| `AC-01` 覆盖半 | 条件成立 | 行为 ECC |
| `AC-01` ③消融门 | `NOT_VERIFIED` | 盲评失效（缺陷 C） |
| `AC-02` | 条件成立 | 行为 ECC（第 3 轮 `不足` 本轮不复现） |
| `AC-03` | **不得 `PASS`** | `B03-N3` `不足` |
| `AC-04` | 条件成立 | 行为 ECC |
| `AC-05` | 条件成立 | 结构反搜对新 7 节点图重跑，四个行为标签命中 **0**；运行时半两例通过 |
| `AC-06` | 条件成立 | 行为 ECC（第 3 轮 `失败` 本轮不复现） |
| `AC-07` / `AC-08` / `AC-09` / `AC-10` / `AC-15` | 条件成立 | 行为 ECC |
| `AC-12` / `AC-13` | `PASS` | 83 条测试对 v1.1 重跑全通过；图结构检查写标记命中 0 |
| `AC-14` | **不得 `PASS`** | `B14-1` `不足` |
| `AC-16` | **不得 `PASS`** | 保真整轮不成立（`G6` `无结果`）；画布半仍 `NOT_VERIFIED`（本机无浏览器自动化） |
| `AC-17` | **`FAIL`** | 纵向 `E07` `失败` |
| `AC-18` | `NOT_VERIFIED` | 盲评失效（缺陷 C） |
| `AC-19` | `NOT_APPLICABLE` | Founder 指示不用 Qwen。**这降低了契约 v1.2 的一项门槛，未回填任何 AC 为 `PASS`**，是否正式 `REBASE` 契约由 Founder 决定 |
| `AC-20` | 远端已推送，哈希待收口时重取 | — |

**任务终态：`IN_PROGRESS`。** 不是 `DONE`，不是 `FAILED`。

---

## 8. 需要 Founder 决定的三件事

### 决定一 · 是否授权第三轮（修 A / B / D / E 后整轮重跑）

- **成本**：约 82 次运行 + 六名判定者，实测一轮约 7 CNY、约 1.5 小时。当前余额充足。
- **不修的后果**：闸门在"模型行为异常"时不可靠，而那正是它该起作用的时候。交付一个会给零交付盖章的防护装置，比不装防护更危险。
- **执行侧建议**：修。缺陷 B 的正确修法是**能从输入算的就别问模型**——冲突反馈可由 `feedback[]` 的证据身份组合直接算出，不必依赖自报。

### 决定二 · 缺陷 C 的处置（不在执行侧权限内）

标签指纹是候选的真实形态，无法靠盲评协议消除。四条路各有代价：

| 路径 | 代价 |
|---|---|
| 盲评前做去标签规范化 | 改变了被评对象，评的不再是真实交付 |
| 逐场景单发给不同判定者 | 挡住跨场景对齐，**挡不住**标签指纹本身 |
| 换取证方法（非盲评） | 需要新的判据设计与冻结 |
| 接受 `AC-18` 长期 `NOT_VERIFIED` | 专业增益这一条永远不被证明 |

### 决定三 · 是否新开任务修产品级缺陷

链式投影因一次零产出永久丢内容且无察觉（§5 末）。这是持续运营场景里最危险的一类失效——用户看不到，下游也看不到。修法涉及持久化与投影语义，不在本轮修复面内。

---

## 9. 恢复入口（换会话直接从这里接）

```text
分支      task/m3-account-content-operator-v1（已推远端）
冻结点    b835709  判据与绑定，全部运行在其后
Dify App  b7fb5b1a-9278-426c-bb8a-f9f288639548  草稿 hash e5c30c32…
凭据      worktree 内 .env（已 gitignore），DeepSeek 可用
证据      evidence/ep06-runtime-fidelity-dify-v11/      保真 9
          evidence/ep06b-runtime-behavior-v11-run2/     行为 49（v11/ 是传输故障的那轮，保留）
          evidence/ep07-longitudinal-v11-run2/          纵向 12（v11/ 是断链的那轮，保留）
          evidence/ep08-module-ab-v11/                  A/B 12 + 三份盲评 + 揭盲映射
          evidence/ep10-closeout-v11/                   缺陷清单、隔离核验、图结构检查
判定      M3_ECC_*_VERDICT_V11_v1.0.md  三份
下一步    等 Founder 对 §8 三件事的决定；未决前不动 SKILL.md 与图
```

**未做、且明确未做的**：独立收口审查（`R-1`～`R-7`）本轮尚未跑；回滚演练需按新提交重做；`AC-20` 远端哈希待重取。这三项应在 Founder 决定第三轮是否进行之后一并完成——若要修缺陷再跑一轮，现在做收口审查会立刻失效。

```text
END_MARKER = M3-CHECKPOINT-ROUND-5-END
```
