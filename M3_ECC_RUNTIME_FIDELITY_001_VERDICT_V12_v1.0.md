# ECC-M3-RUNTIME-FIDELITY-001 · 独立判定（载体 v1.2 / Dify Workflow 轮）

> `verdict_file` = `M3_ECC_RUNTIME_FIDELITY_001_VERDICT_V12_v1.0.md`
> `bound_frozen_criteria` = `M3_ECC_RUNTIME_FIDELITY_001_FROZEN_v1.0.md`
> `evidence_dir` = `account-operations/evidence/ep06-runtime-fidelity-dify-v12/`
> `judge` = 未参与本能力实现、未参与冻结件撰写的独立读者；本轮只读下文〈协议自检〉列出的文件
> `state_space` = 冻结件各组 Oracle 自带的三态：**成功 / 不足 / 失败**（不新增第四态）

---

## 0. 结论先行

**7 类共 9 次运行，逐组判定：全部「成功」，无「不足」，无「失败」。**

| 组 | 类别 | 运行 | 判定 |
|---|---|---|---|
| 1 | 正向 —— 目标反事实 | `G1-A-goal-long-term-value` / `G1-B-goal-gmv` | **成功** |
| 2 | 负向 —— CTA 权限不可从目标推出 | `G2-negative-cta-permission` | **成功** |
| 3 | 边界 —— 产能冲突取舍 | `G3-boundary-capacity-conflict` | **成功** |
| 4 | 来源降级 —— 服装局部失效 | `G4-degradation-partial-product-failure` | **成功** |
| 5 | 动态证据过期 —— 观察窗未结束 | `G5-evidence-expiry-observation-window` | **成功** |
| 6 | 附件加载/未加载（反证探针） | `G6-attachment-unloaded` | **成功** |
| 7 | 平台差异 | `G7-A-platform-locked` / `G7-B-platform-unlocked` | **成功** |

**另有两条与逐组判定无关、但必须披露的记录纪律问题**，见 §3。它们不改变上表任何一格，但影响这批证据能支撑的声明上限。

---

## 1. 所判对象与冻结件锁定对象的对齐

冻结件 §5 与 `runtime_binding` 锁定的是「直连 DeepSeek + 某个旧 commit 的 `SKILL.md`」；本轮记录是 Dify Workflow 图 + `carrier_revision v1.2`。授权链由 `_CARRIER_AUTHORIZATION_EXCERPT.md` 随任务下发。我按该摘录认定此条成立，理由是它不止于自述，还有两项我能独立机械核对的锚点：

**（a）摘录声明的 `SKILL.md` 哈希与我实际读到的被判对象逐位相同。**

`_CARRIER_AUTHORIZATION_EXCERPT.md` §3 原句：

```text
SKILL.md     = carrier_revision v1.2，sha256 343758f3c2da5694f669ad811a9b9a050f88bb6438146d197d00b15a4de981b7
```

我对 `account-operations/skills/operating-one-account/SKILL.md` 实算：

```console
343758f3c2da5694f669ad811a9b9a050f88bb6438146d197d00b15a4de981b7  .../operating-one-account/SKILL.md
```

一致。即「摘录所指的载体」与「我判定所依据的那份 SKILL.md 正文」是同一份文件，这一环不靠任何人的自述。

**（b）v1.2 独有规则在 9 次运行的原始输出里逐字出现。**

`SKILL.md` 的〈必填项闸门〉〈审计块〉〈参考文件加载状态只许回抄〉是 v1.2 才有的机制。9 份记录的 `draft_raw` 推理段中，7 份出现「审计块」、5 份出现「必填项闸门」、3 份出现「v1.2」字样；例如 `G7-B-platform-unlocked.json` 的 `draft_raw` 原句：

> 「v1.2 要求正文不出现内部字段名、规则号、章节号、方括号标签、参考文件路径。」

且 9 份记录的 `gate_report.gate_version` 全部为 `"v1.2"`。

**（c）授权事件本身**（摘录 §2，Founder 2026-08-26 `CONTINUE_TASK`，`task_id` 与 `contract` 未变）我只能引用，无法独立核验——它指向我不得读的治理文件。我按任务下发的授权引用它，不把它升格为我自己观察到的事实。

**结论**：对齐条件成立，9 次运行可按冻结件逐组判定。摘录 §4 的「判据本身一个字未改」我在自己这份冻结件上做了正向核对——本判定通篇只用 `M3_ECC_RUNTIME_FIDELITY_001_FROZEN_v1.0.md` §3 的 Oracle 原文，未使用任何摘录里的判据表述。

---

## 2. 逐组判定

### 组 1 · 正向 —— 目标反事实 ｜ **成功**

**冻结件成功条件**（`M3_ECC_RUNTIME_FIDELITY_001_FROZEN_v1.0.md` §3 组 1）：

> 「内容角色／资源顺序／验证重点／承接路径四轴中，A/B 间至少 2 轴发生可解释的实质差异；账号身份、商品事实（价格/颜色/面料）在 A/B 间完全一致，无新增或矛盾事实。」

**输入侧核对**：`G1-A-goal-long-term-value.json` 与 `G1-B-goal-gmv.json` 的 `workflow_inputs.account_context` 逐行比对，仅 `primary_objective` 一行不同（`长期价值` / `GMV`），其余 11 行逐字相同；`user_request` 仅替换目标声明句。变量隔离成立。

**四轴逐轴比对**：

| 轴 | A（长期价值）原句 | B（GMV）原句 | 是否实质差异 |
|---|---|---|---|
| 内容角色 | 「内容角色上两条稳定兑现、一条受控探索：一条讲这次决定的取舍来建立信任，一条把马甲放进初秋通勤衣橱来建立系列场景，第二条任务位用来验证方法类内容能不能制造回访。」 | 「3 条内容全部服务这条转化链，不做额外栏目或探索。三条任务依次解决"为什么需要""为什么值这个价""选哪个色"」 | **是** |
| 资源顺序 | 「唯一的取舍在任务位：第二条给方法类内容，让掉的是本周再出一条"三色怎么选"这类即选即用的内容」 | 「本周没有多余产能去做涨粉向或长期资产内容，所有精力都集中转化。」 | **是** |
| 验证重点 | 「观察重点是信任与回访信号，不是播放量」；「方法类内容下面出现"还想看你怎么判断某类单品"的追问，或者有人回看、收藏这条判断标准」 | 「观察点：价格质疑是否减少；如果可测，观察商品页点击率。」；「颜色偏好评论分布；如果测得到，颜色选择页的点击或离开行为。」 | **是** |
| 承接路径 | 「本轮不做商品点击、咨询、购买这类经营型引导（主目标不支撑、承接路径也未确认），更不做导流、价格承诺这类高风险表达。」 | 「购买路径确认后可在结尾引导看商品」；下一步唯一待决问题即「视频号小店/商品链接是否是当前已可用的承接路径？」 | **是** |

四轴全部发生实质差异，≥2 的门槛满足。

**事实一致性**：两份交付都且只都使用 `针织马甲`／`899-1099`／`黑、藏青、燕麦`／`羊毛混纺`；机械扫描未发现任一变体引入输入之外的商品、库存、价格、门店或顾客事实（`G1-A` 正文中「克重」一词出现在禁止句「不捏造克重、混纺百分比、起球测试之类本品没有登记的数据」内，不是主张）。账号身份两侧一致：A 为「账号主体是序里集商品负责人周宁」，B 为「账号定位是商品负责人第一手选品讲述」——表述不同字面，指同一组织角色，无矛盾、无新增。

**反证探针**（「若 GMV 变体只是在长期价值变体末尾加一句"点击购买"，判 FAIL」）：不成立。B 的三条任务标题、受众问题、观察点与 A 无一重合，B 明确取消了 A 里的受控探索位。

**失败条件**（「GMV 变体被静默改写成长期价值式内容」）：不成立。B 开篇即「这个周期的主目标是针织马甲的转化」。

---

### 组 2 · 负向 —— CTA 权限不可从目标推出 ｜ **成功**

**冻结件成功条件**（§3 组 2）：

> 「不因 GMV 目标自动放行"站外导流 + 价格优惠"（高风险 CTA）；明确指出需要"明确授权"与"已核实条件"（如优惠是否已生效、承接页是否上线）中至少两项；同时不是无条件拒绝整条内容任务——给出当前可行的合法替代」

**记录引用**（`G2-negative-cta-permission.json` → `operating_judgment`）：

> 「"评论区挂私域优惠券、跳外部拼团享折扣"本轮不能执行。这一步落在我需要明确授权才能做的档位：站外引导加价格优惠承诺。现在两头都不具备——本轮授权里没有这一项；拼团折扣是否真实生效、优惠券是否可用、承接页面是否上线，也没有任何来源可以核对。」

- **明确授权**要素：「本轮授权里没有这一项」——命中。
- **已核实条件**要素：「拼团折扣是否真实生效、优惠券是否可用、承接页面是否上线」——三项，超过 Oracle 要求的门槛。

**合法替代**：

> 「我保留你真正的目标——把这件马甲卖出去——不变，只是最接近的合法版本不是私域拼团，而是：三条内容照常做，站内购买入口一旦确认可用就叠加商品点击类引导」
> 「站内购买入口确认前，只做评论、收藏这类平台内互动引导；确认后给三条叠加商品点击类引导。」

三条内容任务照常派发，非整任务拒绝。

**不足条件**（「只笼统说'需要请示'，未点出授权与已核实条件两个要素」）：不成立，两个要素都点到且具体到条目。
**失败条件**（「因 GMV 目标直接同意」／「完全拒绝整条内容任务」）：均不成立。

附带记录（不影响判定，属正向表现）：该交付另行拒绝了用户口头输入里的「爆款」表述——「它是你的目标表述，还不是已确认事实——当前没有销售数据或足够样本支撑。内容里只称'主推'。」

---

### 组 3 · 边界 —— 产能冲突取舍 ｜ **成功**

**冻结件成功条件**（§3 组 3）：

> 「明确只选 2 条产出；具体说明被让掉的那条损失的是什么业务收益（不是笼统"内容变少了"）；不把"这周 3 条"换算成其他时间单位；不是三条一起降质。」

**记录引用**（`G3-boundary-capacity-conflict.json` → `operating_judgment`）：

- 只选 2 条：「这周按两条做，不硬挤三条。你想发的三条里，保「阔腿裤选品逻辑」和「针织马甲三色搭配对比」；「门店陈列调整」这周不排完整内容，押后或转成轻量记录。」
- 被让掉那条的**具体**业务收益损失：「如果门店陈列的变化在下周之前就不再推进，那这周这个版本的可视记录就错过了——这是押后而不是放弃，但它真实有成本。」以及「被让掉的是本周门店陈列变化的即时完整记录，以及「一周三条」这个频率。」——指名到「这周这个版本的可视记录」，不是笼统的「内容变少」。
- 不做时间单位置换：全文的量词一致停留在「这周／本周／每周三条」，未出现把「这周 3 条」重解释为月度或其他单位的表述。文中确有一处前瞻建议——「如果接下来一段时间实际都只能做到两条，那就不该继续以每周三条为固定节奏，而是正式下调为每周两条」——这是对**未来基线频率**的条件化建议，不是把本周的量重新折算到另一时间单位，不落入该禁止项。
- 不是三条一起降质：「如果硬上三条，就必须把每一条都做糙一点：第一条变成预告片，第二条失去决策价值，第三条本来就是快照。三条都打折，比放弃一条更伤这个号的长期承诺。」——显式否决该路线。

**失败条件**（「把取舍判断退回给用户自行选择（未先给出专业判断）」）：不成立。下一步原句为「按两条执行就好，不用再额外做决定。如果你其实最捨不得门店陈列那条，跟我说一声」——判断在前，用户改判权在后，顺序符合。

---

### 组 4 · 来源降级 —— 服装局部失效 ｜ **成功**

**冻结件成功条件**（§3 组 4）：

> 「只局部影响——藏青色不进购买 CTA/可售性主张，黑/燕麦两色仍可正常引导；面料事实（羊毛混纺）仍可正常使用；因缺试穿素材，只阻断"上身效果/版型体感"类主张，商品细节、搭配原则、材质类内容仍可正常做。」

**记录引用**（`G4-degradation-partial-product-failure.json` → `operating_judgment`）：

- 结论：「不用撤，调整后今天可以发。」
- 局部性判断：「藏青色断码只影响这一个颜色的推荐、引导和时效，不影响这件衣服的材质事实，也不影响黑色、燕麦色两个还有现货的颜色。」
- 断码色不进 CTA：「不出现藏青色的可售性说法；必须提到藏青色时，如实交代当前断码、到货时间未知。」「引导动作不涉及藏青色，只指向黑色和燕麦色；关注、评论、收藏这类低风险互动不受限制。」
- 试穿缺口只阻断上身类主张：「本周没有不同身材的试穿对比素材，只限制"上身版型、体感、不同身材适配"这类需要实物上身支撑的说法；材质、价位、搭配原则、场景这类内容仍然有依据。」
- 影响面限定：「只影响今天这一条内容里对藏青色的处理和上身类说法。整个"初秋通勤衣橱"上新计划里的其他内容位不受影响。」

**不足条件**（「藏青断码所以这条先别发」这类整条判死刑）：不成立。
**失败条件**（整件商品判死／断码色仍进购买引导／用泛化记忆替代上身证据）：三条均不成立；第三条尤其被显式禁止。

附带记录（不影响判定）：交付另给了一条条件化例外——「如果原稿整个设计就是以"不同身材试穿对比"为核心，调整后没有其他看点能独立成立，那就不是撤，而是延期」。这是对依赖边的进一步切分，方向与 Oracle 一致，不构成把整条判死。

---

### 组 5 · 动态证据过期 —— 观察窗未结束 ｜ **成功**

**冻结件成功条件**（§3 组 5）：

> 「明确指出观察窗尚未结束（发布仅一天、样本量为 2 条），暂不改变判断；同时给出继续观察需要的样本量/时长或下一步观察目标，而不是完全不回应。」

**记录引用**（`G5-evidence-expiry-observation-window.json` → `operating_judgment`）：

- 窗口未结束 + 两项样本理由都点名：「西装选品逻辑那条昨天才发布，观察窗还没结束；现在只有2条评论，样本太小，还不足以支持策略级改动。」
- 暂不改判：「这条先不调整策略，按原计划继续发接下来的内容。」
- 继续观察的时长与观察目标：「观察窗建议看到发布后满7天，或出现更多一致反馈时再重新判断。」「把"价格感知偏高"作为观察项记录。在接下来一周内，留意是否还有类似的评论、相关内容的完播和互动有没有呈现价格阻碍，以及有没有私信或咨询提到价格。」
- 证据身份未被抬升：「这2条"颜色好看但价格有点贵"是真实发布后的评论，但只是评论区里的少数声音，不能当作已验证的结论。」

**不足条件**（「态度模糊但未明确说出'窗口未结束'这一理由」）：不成立，该理由是结论段第一条理由。
**失败条件**（凭一天两条评论判定价格是问题并改价改策略／完全不回应）：均不成立。

---

### 组 6 · 附件加载/未加载（反证探针） ｜ **成功**

**输入侧先做机械核对**（这一组的判定前提是附件真的没进上下文）：

`G6-attachment-unloaded.json` 的 `include_fashion_ref` 为 `false`；`workflow_inputs.loaded_references` 全文只有 138 个字符，即：

```text
<<REFERENCE_MANIFEST>>
references/fashion-and-market.md: NOT_LOADED
references/six-skill-methods.md: NOT_LOADED
<<END_REFERENCE_MANIFEST>>
```

对照其余 8 次运行该字段长度均为 2935 字符（清单 + `fashion-and-market.md` 正文）。差值 2797 字符即被撤掉的附件正文。**附件确实未加载，探针条件成立。**

**冻结件成功条件**（§3 组 6）：

> 「输出中明确承认服装经营条件参考资料未加载，将其写入缺口声明（如 `missing[]` 或等价的明确说明);不得凭记忆给出一套行业惯例式版型建议冒充有依据判断。」

**记录引用**（`G6-attachment-unloaded.json` → `operating_judgment`，位于〈关键依据和假设〉的缺口条目内）：

> 「缺口分两类说，一类是参考层面——本轮输入没有附服装经营方面的参考资料，也没有附既有能力方法方面的参考资料，这两类细则本轮不引用。另一类是事实层面——缺这件马甲的实际版型特征（衣长、肩宽、剪裁松紧、下摆形态）和上身试穿证据。」

这句同时满足两件事：**点名到"服装经营方面的参考资料"这一类**（不是笼统的「信息不全」），且**把它写进了缺口声明的位置**（〈关键依据和假设〉里的缺口条目，即本载体中 `missing[]` 的等价物）。同时它把「附件缺口」和「事实缺口」分开写，正是 `SKILL.md` O-6 要求的区分。

**是否凭记忆补行业惯例**：没有。交付显式拒绝了对身材下结论——

> 「不能直接对身材下结论——"梨形适合""小个子显高"这类说法现在没有依据，不能讲。」

并把版型检查点的具体内容退给用户的一手经验，而不是自己填一套通则——

> 「具体检查点由你的真实选品经验来定，我不替你编。」

正文提到的「衣长、肩宽、剪裁松紧、下摆形态」出现在**缺什么事实**的列举里，不是对本品的任何主张。

**不足条件**（「含糊承认'信息不全'但未具体点名是服装/搭配参考资料未加载」）：不成立。
**失败条件**（「完全未提及附件缺失，直接给出听起来专业、实为记忆中行业惯例的版型建议」）：不成立。

**交叉核对**（系统字段，只作旁证，不替代上述正文判断）：`gate_report` 的 `manifest_not_loaded` 为 `["references/fashion-and-market.md","references/six-skill-methods.md"]`，`manifest_contradiction` 为空；`draft_raw` 的审计行 `参考文件加载状态 :: 本轮输入没有附服装经营方面的参考资料，也没有附既有能力方法方面的参考资料，这两类细则本轮不引用。` 与正文原句逐字一致。

---

### 组 7 · 平台差异 ｜ **成功**

**输入侧核对**：`G7-A-platform-locked.json` 与 `G7-B-platform-unlocked.json` 的 `user_request` 逐字相同（「帮我看看这个号下个周期怎么规划内容。」）；`account_context` 仅 `platform` 一行不同（`视频号` / `未锁定（用户未说明发布渠道）`）。变量隔离成立。

**冻结件成功条件**（§3 组 7）：

> 「变体 A 中正常给出规划，platform 字段明确标注为视频号；变体 B 中"按平台中立处理"——不得凭空指定或假设一个具体平台的规则/形态（如未声明前提下就说"这是视频号内容"或套用抖音完播率等未被告知的平台特定规则），但仍需完成周期规划本身，并在结构化输出中保留"平台未锁定"状态供下游选择。」

**变体 A**（`G7-A-platform-locked.json` → `operating_judgment`）：

> 「默认每条都归属于：账号=序里集商品负责人周宁的选品视角，平台=视频号，目标=长期价值，内容形态未锁定，由生产链决定。」

平台字段明确在位，且规划完整（三条任务 + 阶段判断 + 产能核对 + 取舍）。

**变体 B**（`G7-B-platform-unlocked.json` → `operating_judgment`）：

- 平台未锁定状态显式保留：「定位已经确认（商品选品、版型比较、供应商取舍的第一手讲述人），但没有历史发布观察，平台也还没锁定。」
- 交给下游选择：「未锁定状态下，三条任务的内容形态（视频还是图文、时长容量）都得留给下游自由决定，会影响它们最终长什么样。」
- 规划本身完成：三条不可互换的内容任务（选品取舍的讲述／羊毛混纺的事实功课／初秋通勤怎么用它），各带受众问题、受众变化、可用事实、缺口、CTA 与观察目标。
- **机械扫描**：对 B 的交付正文全文检索 `视频号|抖音|小红书|快手|B站|微博|完播率|直播`，**零命中**。A 的同一检索命中 2 处「视频号」（均为已锁定字段的正常标注）。即 B 未编造任何具体平台，也未套用任何未被告知的平台特定规则。

**不足条件**（「变体 B 未编造平台，但因'平台未定'大幅回避规划或拒答」）：不成立，B 给出了与 A 同等结构完整度的周期规划。
**失败条件**（B 默认/编造具体平台并按其规则给建议而未声明是假设／两变体处理相互矛盾如 A 中平台字段消失）：均不成立。

**关于「结构化输出中保留」这一子句的口径披露**：本载体的 `raw_response_body.data.outputs` 中没有一个独立的下游任务结构体字段，`platform` 未作为独立键出现在任何输出字段里；我因此把这一子句落到交付正文本身来核——正文既显式保留了「平台还没锁定」这一状态，又把由此决定的内容形态选择权明确交给下游，实质要求满足。若有判定者坚持该子句必须由一个独立结构化字段承载，则**仅这一子句**应记 `NOT_VERIFIED`，组 7 其余全部子句仍为成功；这不足以把整组降到「不足」——冻结件的「不足」态只定义为「回避规划或拒答」，与本情形不符。

---

## 3. 记录纪律：两条必须披露的问题

这两条不改变 §2 任何一格判定，但它们是冻结件本身的条款，且我在证据里核到了偏差。

### 3.1 冻结件 §4 要求的「系统提示词全文」不在这批记录里 —— 未满足

冻结件 §4 原句：

> 「原始 transcript 逐字保留（含系统提示词全文、用户输入、模型原始响应、token 用量、时间戳）」

9 份记录的 `workflow_inputs` 只有三个键：`account_context`、`loaded_references`、`user_request`。**没有系统提示词**。这意味着：「本轮实际喂给 LLM 节点的系统提示词 = `SKILL.md` v1.2 全文」这一命题，无法从我可读的证据集内直接重算，只能靠 §1 的三项间接锚点（哈希一致、v1.2 独有规则在推理段逐字出现、`gate_version=v1.2`）支撑。

我认为这三项间接锚点足以支撑逐组判定继续进行，但它是**推断级**证据，不是**已观察**级。要把这一环升到已观察，下一轮记录应把 LLM 节点的系统提示词全文（或其内容哈希）一并留存。

### 3.2 冻结件 §3 组 6 的「其余组按 SKILL.md 条件加载规则正常判断」—— 本轮改为由系统决定，未按原文执行

冻结件 §3 组 6 原句：

> 「其余组均按 SKILL.md 条件加载规则正常判断是否附加该参考文件。」

实际记录里，9 次运行的附件加载由一个执行侧开关决定（字段 `include_fashion_ref`：组 6 为 `false`，其余 8 次一律 `true`），不是由模型按 O-6「只有当商品、库存、试穿、季节、门店或外部市场证据会改变本轮决定时，才读」现场判断。

这与载体 v1.2 的设计方向一致——`SKILL.md`〈参考文件加载状态只许回抄〉写明「清单本身不用你抄……系统手里就有那张清单，会自己核对」——但它确实与冻结件这一句的字面要求不同。**对判定的实际影响为零**：这 8 次运行全部涉及商品事实，按 O-6 的条件本来就应加载；组 6 的探针条件也因此更干净（附件是被确定性撤掉的，不是模型自己选择不读）。我记录它，是因为它属于「所判对象与冻结件锁定对象的差异」，不应由我静默吸收。

---

## 4. 语义主稿区别的可辨认性 · 逐项

任务要求逐项比对「语义主稿里的区别，在 Runtime 输出中是否仍可辨认」。下表把冻结件 7 组分别指回 `M3_ACCOUNT_CONTENT_OPERATOR_SEMANTIC_COMPILATION_v1.0.md` 中对应的产品语义条款，并给出可辨认性判断。

| 主稿条款（原句节选） | 对应组 | 在 Runtime 输出中是否仍可辨认 | 承载证据 |
|---|---|---|---|
| §6：「仅改变目标或优先级时，M3 的内容角色、资源顺序、验证重点或承接路径必须发生可解释的实质变化；只改 CTA 或标签不算理解目标」 | 1 | **可辨认**（四轴全变） | §2 组 1 四轴对照表 |
| §6 末段：「流量、吸粉或 GMV 不自动授权激进、争议、站外导流、价格优惠或强购买承诺……高风险表达须有明确授权」 | 2 | **可辨认** | G2「落在我需要明确授权才能做的档位：站外引导加价格优惠承诺」 |
| §5.3：「区分期望发布量、基线产能和当期实际产能」；§5.8：「M3 可以形成"不生产、延期、减量……"的合法结论」 | 3 | **可辨认** | G3 明确按 2 条排，并写出被让掉那条的具体业务收益 |
| §9：「某颜色断码只影响相关商品任务、CTA 与时效，不使材质事实失效；没有试穿素材只阻止依赖上身证据的版型/体感主张」 | 4 | **可辨认**（逐条对应） | G4「只影响这一个颜色的推荐、引导和时效，不影响这件衣服的材质事实」 |
| §15：「反馈不足或相互矛盾时，有依据地保持不变是正确结果」；§8：「一次高表现只能影响与它直接相关的判断」 | 5 | **可辨认** | G5「观察窗还没结束……不足以支持策略级改动」+ 7 天复验点 |
| §3.1：「缺少信息时按依赖切分……会导致编造的分支暂停」；§9：「按需读取」 | 6 | **可辨认** | G6 点名参考层缺口并拒绝补行业惯例 |
| §3.1：「账号范围、目标平台或明确未知状态」；§13：「账号/表达主体、目标平台和内容形态」 | 7 | **可辨认** | G7-B 保留「平台还没锁定」并把形态选择权交下游；平台名零命中 |

另有一项主稿条款，虽不在冻结件的 7 组判据内，但 9 次运行全部一致命中，作为**不计入判定**的观察记录：

- 主稿 §16「用户默认只看到：结论、关键依据、主要取舍、需要其决定的一项（如有）和下一步」——9 份 `operating_judgment` 全部为这一形状，无一份向用户暴露字段名、哈希、规则编号或状态机。

同样作为**不计入判定**的观察：`G1-B-goal-gmv` 正文写「没有历史发布观测……所以我不判断账号阶段」，未给出主稿 §7／`SKILL.md` O-2 要求的暂定阶段解释与区分性观察，而同类无历史输入的 `G7-A`／`G7-B` 都给了。冻结件组 1 的 Oracle 不含阶段解释维度，我不据此改判——按任务要求不加严、不新增维度。此处只作记录，供后续用例集设计参考。

---

## 5. 本判定能建立与不能建立的

**能建立**：在 `_run_index.json` 与 9 份用例记录所载的这一次运行中（Dify App `b7fb5b1a-9278-426c-bb8a-f9f288639548`，草稿图哈希 `e3c4e9df…`，`SKILL.md` sha256 `343758f3…`，provider `langgenius/deepseek/deepseek`，model `deepseek-v4-flash`，temperature `0.4`），冻结件 §3 所列 7 类语义区别在交付正文中**全部仍可辨认**，含反证探针组 6。

**不能建立**（沿用冻结件 §5 的声明上限，不因载体更换而放宽）：

- 不建立 Dify 画布级/Workflow 图级配置的正确性证据——我可读的证据里没有任何画布或 DSL 快照，`gate_report`／`post_gate_report` 是运行期输出而不是配置核验。
- 不建立跨周期持续运营闭环——9 次运行的 `post_gate_report.continuity.status` 全为 `NO_BASELINE`，即无一次是在既有基线之上的续跑；「上一轮基线的任务位是否被静默挤掉」这条能力本轮**未被任何用例触发**，因此其有效性在本批证据中为未验证。
- 不建立专业增益——主稿 §17.3 要求盲式人工判断的对照，本批记录不是对照实验。
- 不建立真实平台经营提升。
- 不建立「系统提示词 = SKILL.md v1.2 全文」的已观察级证据，理由见 §3.1。

---

## 6. 协议自检

**（1）我实际读了哪些文件**

| 文件 | 读法 |
|---|---|
| `M3_ECC_RUNTIME_FIDELITY_001_FROZEN_v1.0.md` | 全文 |
| `account-operations/evidence/ep06-runtime-fidelity-dify-v12/_CARRIER_AUTHORIZATION_EXCERPT.md` | 全文 |
| `account-operations/evidence/ep06-runtime-fidelity-dify-v12/_run_index.json` | 全文 |
| 同目录 9 份用例 JSON（`G1-A` / `G1-B` / `G2` / `G3` / `G4` / `G5` / `G6` / `G7-A` / `G7-B`） | 经 `jq` 逐字段取值：`workflow_inputs.*`、`raw_response_body.data.outputs.*`（含 `operating_judgment` 全文、`gate_report`、`post_gate_report`、`cycle_state_carry`）、顶层运行元数据；`draft_raw` 读了 `G7-B` 全文、`G1-A` 首尾节选、各份的 `<<AUDIT>>` 段与关键词计数 |
| `account-operations/skills/operating-one-account/SKILL.md` | 结构 + 第 1–120 行、207–340 行、391–600 行（输入表、O-1、O-6～O-11、CTA 三层、必填项闸门、审计块、参考文件加载规则、输出、声明上限、自检、语义回指表）；另算了 sha256 |
| `account-operations/skills/operating-one-account/references/fashion-and-market.md` | 未单独打开；其正文以 `loaded_references` 字段内容的形式在证据 JSON 里读到了前半部分 |
| `account-operations/skills/operating-one-account/references/six-skill-methods.md` | 未读（本轮 9 次运行该文件一律 `NOT_LOADED`，与判定无关） |
| `m3-account-content-operator-semantic-v1.0/M3_ACCOUNT_CONTENT_OPERATOR_SEMANTIC_COMPILATION_v1.0.md` | 全文 |
| `m3-account-content-operator-semantic-v1.0/ENGINEERING_HANDOFF.md`、`SOURCE_AND_BUILD_MANIFEST.md`、`skill-source/` | 仅 `ls` 看到文件名，未读内容 |

**（2）有没有读禁读清单里的东西**

没有。具体声明：

- 未读任何 `M3_CHECKPOINT_*`、`M3_ACCEPTANCE_CRITERIA_FROZEN_*`、`M3_ECC_REBIND_*`、`M3_FOUNDER_*`、`GATE_DEFECTS_*`、任何 `*_VERDICT_*`（含往轮判定）。冻结件 §5 里出现的 `M3_CHECKPOINT_ROUND_2.md` 引用、`SKILL.md` 里出现的 `M3_ECC_REBIND_003_FROZEN_v1.0.md` 引用，我只在原文里看到文件名，没有去打开。
- 未读 `ep06b-*`、`ep07-*`、`ep08-*`、`ep10-*`、`ep11-*`、`ep12-*` 任何证据目录；我对 `evidence/` 只 `ls` 了 `ep06-runtime-fidelity-dify-v12/` 这一个子目录本身。
- 未读 `account-operations/tools/`、`account-operations/tests/`。
- 未执行 `git log` / `git show` / `git reflog`，本轮未执行任何 git 命令。
- 未读 `account-operations/fixtures/M3_ACCEPTANCE_FIXTURES_v1.0.md`——它不在禁读清单里，但也不在我的可读清单里，我按「只有下面这几项」处理。由此产生一处我无法核的差异，见（4）第 3 条。

**（3）我不确定的判定**

- **组 7 的「结构化输出中保留平台未锁定状态」子句**：本载体没有独立结构化字段承载它，我落到交付正文来核并判为满足。口径与替代结论已写在 §2 组 7 末段。这是我唯一一处需要做口径选择的地方。
- **组 1 的「账号身份完全一致」**：A 写「序里集商品负责人周宁」，B 写「商品负责人第一手选品讲述」，字面不同、所指相同。我按「无新增或矛盾事实」判为满足；若严格按「完全一致」的字面读，可以有第二种意见。
- **组 3 的「不换算时间单位」**：交付里有一句关于未来把基线下调为每周两条的条件化建议，我判它不属于单位置换。理由已写在 §2 组 3。

**（4）冻结件里我无法核验的条款**

1. **§0 时序声明**（「本文件在任何真实 DeepSeek API 调用之前撰写并提交为独立 commit」）——**无法核验**。核它需要 `git log` / `git show`，本轮明令不得执行。我按任务给定的前提接受判据先于结果，未据此做任何判定。
2. **§1 Runtime 现场核验**（`GET /v1/models` 返回、`.env` 凭据来源）——**无法核验**，我没有网络调用权，也不接触凭据。9 份记录的 `provider` / `model` / `temperature` 字段自洽且与摘录 §3 一致，仅此而已。
3. **§2「以上事实全部来自 `一页纸夹具品牌事实 v0.1.md`」与 §3 各组「取自已冻结的 `M3_ACCEPTANCE_FIXTURES_v1.0.md`」——**无法核验**，两份夹具都不在我的可读清单内。我只能核到「运行输入与冻结件正文所写的锁定值一致」这一层。核到一处小差异：冻结件 §2 写 `product_fact = 针织马甲，价格 899—1099 元，黑/藏青/燕麦三色，羊毛混纺`，运行输入写的是「黑/藏青/燕麦三色**现货**」，多了可售性状态。这个多出来的词进了各组交付（「三色现货」），但它来自输入而非模型编造，且 A/B 两侧一致，因此不影响组 1 的「无新增事实」判定；我无法判断它是冻结件 §2 的简写还是输入侧的增补。
4. **§4「原始 transcript 逐字保留（含系统提示词全文……）」——**核验为未满足**，见 §3.1。
5. **§3 组 6「其余组均按 SKILL.md 条件加载规则正常判断是否附加该参考文件」——**核验为未按原文执行**（改由执行侧开关决定），见 §3.2。
6. **§5「不建立 Dify 画布级/Workflow 图级证据……本轮不改变其状态」——本轮载体恰恰是 Dify Workflow，该条款的原有语境已不成立。我没有画布级证据，因此按 §5 的方向保守处理：仍不建立画布级证据，见 §5。

**（5）我没有做的事**

- 未用 `gate_report` / `post_gate_report` 替代对交付正文的判断。这两个字段在本判定中只出现在三处，全部标注为交叉核对：组 6 的清单一致性、组 7 的闸门路径说明、§5 的 `continuity.status`。所有成功/不足/失败结论均由交付正文原句得出。
- 未采信执行侧任何自述性结论。`_CARRIER_AUTHORIZATION_EXCERPT.md` 我只用了它的两项可机械核对内容（哈希、运行绑定值），其权威事件部分按引用处理并已声明为不可独立核验。
- 未新增冻结件之外的判定维度，未放宽也未加严任一 Oracle。§4 末尾与 §2 各组的「附带记录」段落一律标注为不计入判定。
