# 结转清单 CF-01 ~ CF-06 逐条处置 v1.0（E1-5）

```yaml
task_id: DIYU-V1-THREE-SKU-EXTRACTION-001
report_date: "2026-09-02"
source: 笛语商业SKU验收体系_索引与启动规则_v1.0.md §10（规则仓库只读，读当前版本）
rule: 遗漏或沉默不构成处置，「不适用」是合法答案，须说明理由
```

## CF-01 · 每 Case 跑 k≥3 次（硬门结论须在 k 次上一致）

**处置：不适用于本任务，推迟到阶段三各 SKU 实测设计。** 本任务零模型调用，不产生任何"跑 k 次"的实测数据；这是每个 SKU 各自进入阶段三之后才会执行的判据，不是摘取/语义对齐阶段能够处置的对象。

## CF-02 · 同源对照位两侧各留一个

**处置：不适用于本任务，推迟到阶段三各 SKU 评测设计。** 同源对照组设计属于每个 SKU 各自的实测取样方法论，与摘取/语义对齐无关。

## CF-03 · 计分门也要 k 次一致

**处置：不适用于本任务，推迟到阶段三各 SKU 判据设计。** 本任务未跑任何计分测试。补充：S5 独立复验已针对 P0 交付过一次"篇幅是否有上下界约束、哪些 G2 维度受篇幅波动影响"的静态事实输入（`pp-architecture/REVERIFICATION_REPORT_v1.0.md` R3），本任务未对 P1/P1.5 重复这一检查——CF-03 在 P1/P1.5 上的静态事实收集本身不在本任务的 E1-1~E1-5 范围内，留给各自的阶段二静态验证（E2）一并处理，因为 E2 就是"对摘出的三个 SKU 各自跑一遍 P0-1~P0-8"，P0-4（确定性配置审计）本身就会覆盖到这一类问题。

## CF-04 · 参考文件防静默陈旧（原 FU-01）

**处置：推迟到 S6（阶段二目录重构），不阻断本任务、不阻断实测。** 与 S5 独立复验时的处置一致（`pp-architecture/REVERIFICATION_REPORT_v1.0.md` R5·CF-04）。本任务额外发现：现在 `platforms.md`/`industry-conditions.md`/`examples.md` 三份文件在仓库里有**四处物理副本**（`packaging-content-for-release/`、`writing-creative-scripts/`、`directing-content-production/` 三份源目录 + 本任务新增的 `core/context-compiler/references/` 一份"代表性共用副本"），CF-04 的防陈旧机制覆盖范围届时需要同时管住这四处，而不只是各 DSL 内嵌的字节快照与其中一份源文件的对比——如实登记为该跟进项范围的扩大，处置仍为推迟到 S6，不在本任务内处理。

## CF-05 · 顾问性三条判据

按索引 §10 CF-05 表的双侧失败形态，对 P1 / P1.5 的 DSL 与 SKILL.md 做与 P0（S5 已查）同样的独立静态承载检查。**只查有没有承载，不评价好不好。**

### P0（已知，S5 已查，本表引用不重复检查）

①无承载　②局部承载（仅平台数值参数场景，v1_4.yml 第 1507–1535 行）　③无承载

### P1（本轮新查）

| 判据 | 双侧失败形态 | 检查结果 |
|---|---|---|
| ① 识别错误提问方式但不拒绝回答 | A：识别就拒答／B：不识别照错问法直接答 | **无承载**。全文档搜索"拒绝"/"拒答"命中的均是 Return 处置枚举值（`REJECT_WITH_AUTHORITY`）与"系统里只有这一处锦标赛代码路径……一律拒绝"（拒绝新建重复路径，非拒答用户问题）——都不是"识别输入问法本身有问题后仍继续作答"这一判据。CS-7 详尽处理"如何不编造"，但不处理"提问方式本身是否有诱导性/装了多个预设"这一维度 |
| ② 给方法但不伪造精度 | A：给了方法但编造精度／B：怕编造而不给方法 | **局部承载，范围窄**——与 P0 同一处共用机制：`ref_projection` 模板内"数值型参数若在主本中无当前有效条目，置 `PLATFORM_SPEC_UNVERIFIED` 并改写为定性制作要求，不得自造数字"（P1 DSL 第 483 行），仅覆盖平台数值参数这一类事实，不覆盖创意判断本身（如"这个方向的收藏率会有多高"一类问题，SKILL.md 没有对应的"不伪造精度但仍给方向性判断"的双向约束） |
| ③ 有立场但不固执 | A：不表态／B：表态后不认边界 | **无承载**。全文档搜索"表态"/"立场"/"条件反转"均无命中（唯一"表态"命中是转发心理学领域内容，与本判据无关）。**值得单独指出**：Q-COMM-05 §7 M4"专业比较与取舍"（15分）本身要求"什么条件改变后裁决会反转"，这是**验收标准层面**已经写出的评分点，但 DSL/SKILL.md 没有把它变成任何输出字段或自检项——`creative_concept` 字段只要求"选中的方向+为什么选它"，不要求声明反转条件。这是一处验收标准与构建物之间的具体缺口，同型于 CF-05 原文"评什么，系统里就得有承载它的地方" |

### P1.5（本轮新查）

| 判据 | 双侧失败形态 | 检查结果 |
|---|---|---|
| ① 识别错误提问方式但不拒绝回答 | 同上 | **无承载**。"拒绝"命中仅为 Return 处置枚举值（"依据权威/事实/边界拒绝"），非本判据对应的机制 |
| ② 给方法但不伪造精度 | 同上 | **局部承载，范围窄**——同一处共用机制，P1.5 DSL 第 483 行同样的平台数值参数处置规则，范围同样仅限该类事实 |
| ③ 有立场但不固执 | 同上 | **无承载**。全文档搜索"表态"/"立场"/"条件反转"/"条件变化"均无命中 |

### 三 SKU 汇总

| 判据 | P0 | P1 | P1.5 |
|---|---|---|---|
| ① | 无承载 | 无承载 | 无承载 |
| ② | 局部承载（窄） | 局部承载（窄，同一机制） | 局部承载（窄，同一机制） |
| ③ | 无承载 | 无承载 | 无承载 |

**结论**：CF-05 在三个 SKU 上呈现**完全一致**的缺口分布——①③ 全部无承载，② 的局部承载来自三者共用的同一处"平台数值参数不伪造精度"规则（THREE_WAY_COMPARISON b) 已确认 `ref_projection` 是共用机制），不是三个 SKU 各自独立设计出的三份局部承载。这意味着**为 CF-05 建立承载机制这件事，本身就应该按"三 SKU 共用"的方式设计一次，而不是分别给三个 SKU 各建一遍**——与本任务"一次摘三个，共用件比出来"的方法论完全吻合。

**处置：不在本任务内建设，推迟到"S5 判定之后、阶段三之前"（与 S5 复验时的处置一致，索引 §10 CF-05 自身指定的时点）。** 本任务受"不改任何 DSL/SKILL 内容"的绝对约束，即使认为有必要也不得顺手建。

## CF-06 · 独立仓库拆分的时点与禁带项

**处置：本轮只需确认一件事——`eval/` 是否建在顶层。**

确认结果：**是**。目录树核对：

```text
/home/faye/diyu-demo/
├── core/           （guards/, context-compiler/）
├── eval/           ← 顶层，与 core/ 平级，本轮只建目录，内含 .gitkeep 占位（非内容），暂空
├── products/       （p0-publishing-packaging/, p1-creative-director/, p1_5-production-director/）
└── adapters/       （顶层空目录，.gitkeep 占位，未建 douyin/xiaohongshu 子目录——见 THREE_WAY_COMPARISON）
```

`eval/` 未建在 `core/` 下面，满足索引 §10 CF-06"`enforced_at: E1-3 建树时即放顶层，不是拆分时再挪"的要求。

完整禁带清单（盲评素材、封存映射、对照 Prompt、评审意见、各 SKU 历史证据链）**在真正触发拆分时才逐项过，本轮不做**——与索引 §10 CF-06 原文一致。远程仓库沿用 `andyan77/diyu-demo`，本轮不拆分（拆分触发条件是"外部拿到访问权"，不是"摘完了"），本任务照常推送到 `task/three-sku-extraction-v1`，未新建仓库。
