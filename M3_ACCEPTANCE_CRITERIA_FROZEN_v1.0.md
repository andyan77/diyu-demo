# M3 验收判据冻结件 v1.0（EP-01 产物）

> `task_id` = `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> `document_role` = 冻结验收合同（A4 记录类型：合同）。开跑前冻结，版本化，不原地改。
> `frozen_at_utc` = `2026-08-26T11:13:42Z`
> `frozen_at_repo_head` = `14f488bad295770b5d828ec159ece10e0181a059`（分支 `task/m3-account-content-operator-v1`）
> `governance` = `RULESIDE-2026-08-25-005 / v0.3.1 revision 2`
> `本轮全部 AC 状态` = `NOT_VERIFIED`。本文件冻结的是**判什么、怎么判**，不宣布任何一项已通过。

---

## 0. 绑定与哈希（本轮实测）

| 文件 | SHA-256 | 实测结果 |
|---|---|---|
| `M3_ENGINEERING_EXECUTION_PROMPT_v1.1.md` | `9d3388e8619d02042fda79c222fdf7bfb2570d0cd855d17ad1ea5d6122c40f59` | 与 Manifest 声明一致 |
| `M3_ENGINEERING_TASK_CONTRACT_v1.2.yaml` | `1d4163fc8bbc54e37adb2070f337994795595d7b696eac37e61ffb2089cb6839` | 与 Prompt §1.2 声明一致 |
| `…/M3_ACCOUNT_CONTENT_OPERATOR_SEMANTIC_COMPILATION_v1.0.md` | `732963af796fd8d61521fb5b481dccc8430ac18043fe2d365e84d6048b4d91e3` | 与 Prompt §1.3 表一致 |
| `…/SOURCE_AND_BUILD_MANIFEST.md` | `42553fb4fce0285aef19de3c6e7d0c9591095b970287846ae2cca1aa25e1cae0` | 一致 |
| `…/ENGINEERING_HANDOFF.md` | `b6bf591183b818bea6cfd550b87e7996d2332d8b873730a7b6165a7ab3ff14f0` | 一致 |
| `…/skill-source/SKILL.md` | `ccedd9a8e544893e821c8e99ff9f578e3d87b0b4040fdcf984618158714cdf26` | 一致 |
| `…/references/acceptance-fixtures.md` | `b6e4420a1f3351391ea8b4882016aaa852625fdf0af165633babf3c322452122` | 一致 |
| `…/references/fashion-and-market.md` | `ba5570f1fd106a4ea47dd7dd73c6cb2baebf06ed8fe459e0676b5f2f9fd0b44b` | 一致 |
| `…/references/operations.md` | `9093fe61b2054551055c99c9475b83f77b05260397b6bd81990a091449827493` | 一致 |
| `…/references/six-skill-methods.md` | `96ca5e2928a9477a48cd873699114fadc16059dc40166ab730605d2474b90f18` | 一致 |

`证据等级` = `static_verified`（本机 `sha256sum` 实测比对，2026-08-26T11:13:42Z）。

**规划侧唯一入口（`DIYU_V1_PLANNING_DELIVERY_BASELINE_v1.0.md`，`aa5997c3…`）不在本仓库路径下，本轮未实测**，状态 `NOT_CHECKED`。它是 Prompt §1.1 的上位绑定；本文件的全部判据只在"入口及其绑定未变"的前提下有效。**入口或其绑定一旦变化，本文件整体置 `STALE`，须以同一 `task_id` 请求 Rebase 后重新冻结。**

---

## 1. 本轮冻结的合同面（不得静默改变）

### 1.1 P0（来自合同 `what` / Prompt §3）

把冻结的 M3 专业语义编译为**一项**独立、可运行的持续运营决策能力；建立 M2→M3 最小投影、M3→M2 候选写回、M3→Content Brief 内容任务三条接缝；在真实 Runtime 中保真；完成纵向、A/B、独立审查与回归；形成可恢复候选、证据包与 Founder 产品验收包；并把技术候选／模块增益／Founder 接受／M5 增益／真实经营提升五层证据分开。

### 1.2 非目标（只读下继承，任何形式都不得复活）

排期器、选题器、发布日历、素材库存、通用数据库、第二套事实库、万能运营大脑；自动评分器、Judge 网络、全网监控、自动实验优化器、RAG-first、领域知识库、新审批流；四份 Skill／四个 Workflow／四个物理节点的对称拆分；固定候选数量、固定稳定/探索比例、固定粉丝量或播放量阈值；强制六 Skill 全链；第二套 Matrix／Campaign／创意锦标赛；M5 集成与真实经营提升宣称。

### 1.3 受保护资产（本任务不得触碰）

Matrix 长期定位权威与账号锚点身份；Campaign 目标、有效期、约束与覆盖权限；M2 的原始观测／反馈／来源／持久化／版本／权限／恢复权威；Content Brief、创意锦标赛、Creative Script、Production Director、Publishing & Packaging 的职责；六份既有 Skill 源文件；`decision-chain/` 与 `content-production/` 下的冻结 evidence／fixtures／workflows／skills／references；全部生产系统、凭据、其他任务的分支／worktree／Dify 对象／账本条目。

### 1.4 允许变化面

新增 `account-operations/` 模块目录（Skill、references、interfaces、fixtures、tests、docs）；新增本任务自己的根级 `M3_*` 治理文档；新增 task-id 专用 Dify 候选/测试 App（EP-04 起，需真实平台access）；本任务分支上的提交与远端任务分支推送。

---

## 2. ECC 通用协议（对全部 21 条 AC 一律适用）

1. **判据先于结果**：本文件冻结时间早于任何 M3 结果产出。看到结果后修改判据的，本轮只算探索，不产生正式 PASS；确需改判据须版本化为 v1.1 并说明触发事件。
2. **候选预冻结**：每个 ECC 在运行前冻结候选集合，全部保留。禁止看到结果后重抽、择优或丢弃失败 Attempt。
3. **Holdout**：§4.9 清单中的夹具一旦用于调试、提示优化或向实现者泄露答案，立即退出留出集并记录退出事件。
4. **证据分级用词**：`runtime_verified`（真实 Runtime 实跑）／`static_verified`（本机静态检查、结构比对、单元测试实跑）／`inferred`（有依据的推断，未取证）／`unverified`（未取证）。任何声明不得高于其来源等级。
5. **动态证据**：记录观察时间、适用范围、有效窗与复验触发。绑定变化后，直接依赖、传递依赖与影响未知项置 `STALE`；已证明不受影响的证据继续复用。
6. **模型自评无效**：模型自述、输出长度、Skill 调用次数、规则条数、节点数量一律不构成任一 AC 的证据。
7. **四态结果**：`PASS` / `FAIL` / `NOT_VERIFIED` / `NOT_APPLICABLE`。`NOT_APPLICABLE` 须在运行前由权威依据判定，失败后不得追溯填写。"有但不够"记 `FAIL(INSUFFICIENT)`，不得填成 `PASS`。
8. **观察窗**：结构类判据（AC-00/01/05/12/13/15/20）无时效窗，随绑定变化失效；行为类判据（AC-02～11/14）绑定"同一冻结夹具集 + 同一模型/参数/权限"，任一变化即 `STALE`；Runtime 类（AC-16～19）绑定准确 commit + Dify App/版本/图 + provider/model/参数。

---

## 3. M3-AC-00 ～ M3-AC-20 · ECC 卡

> 每张卡的字段固定：**命题｜冻结输入｜候选｜锁定变量｜Oracle｜成功｜不足｜无结果｜失败｜反证探针｜取证阶段｜本轮状态**。
> `Oracle` 明确写出"谁判、按什么判"，其中"有界判断"一律指受本文件冻结 Rubric 约束的人工判断，不是模型自评。

### M3-AC-00 · 授权、身份与基线回指

| 项 | 内容 |
|---|---|
| 命题 | 授权事件、合同哈希、进入模式、分支/worktree/Dify 对象与受保护基线可逐条回指到有权来源，且无越域 |
| 冻结输入 | 入场 Manifest；`git rev-parse` / `git branch -a` / `git worktree list` / `git remote -v` 实测；Dify 对象清单 |
| 候选 | 唯一：当前 `NEW_TASK` 记录 |
| 锁定变量 | `task_id`、Prompt/Contract 哈希、远端默认分支 |
| Oracle | 结构检查：Manifest 每条声明能指到一条实测命令输出或一条会话内 Founder 明确答复；无授权即只读停止 |
| 成功 | 全部条目有来源且等级不高于来源 |
| 不足 | 有条目只有自述、无实测输出 → `FAIL(INSUFFICIENT)` |
| 无结果 | 未执行核验 → `NOT_VERIFIED(NOT_CHECKED)` |
| 失败 | 出现"Founder 已确认"但无本轮会话内确认；或据文件存在推定授权 |
| 反证探针 | 在 Manifest 中查找任一未绑定实测输出的"已核验"措辞 |
| 取证阶段 | EP-00（已做）+ EP-10 收口复验 |
| 本轮状态 | `NOT_VERIFIED` —— EP-00 已产出 Manifest，但 Dify 对象清单与远端推送尚未发生，AC-00 只能在 EP-10 闭合 |

### M3-AC-01 · 单一 M3 能力，不退化

| 项 | 内容 |
|---|---|
| 命题 | 一份 M3 Skill 承担全部持续运营判断，未退化为排期器／选题器／全能大脑／第二事实库 |
| 冻结输入 | 实现后的 SKILL.md + references + interfaces 全文；§4.1 正向夹具集 |
| 候选 | B=M3 候选；B′=删除 M3 只保留通用运营提示 |
| 锁定变量 | 模型、参数、输入事实、目标、平台、产能 |
| Oracle | ①结构检查：Skill 树中不存在第二份状态存储、评分器、日历生成器；②责任反搜（正则+人工）：`排期表`/`发布日历`/`打分`/`评分`/`score`/`rank` 等出现处必须是禁止性表述；③消融：B′ 与 B 的运营判断在盲评下必须可辨差异 |
| 成功 | ①②通过且③在盲评下 B 有可辨识运营增益 |
| 不足 | ①②通过但③无可辨差异 → `FAIL`，且 `M3_PROFESSIONAL_GAIN = NOT_VERIFIED` |
| 无结果 | 盲评未执行 → `NOT_VERIFIED` |
| 失败 | 出现第二份当前状态真源，或 Skill 自行生成排期表/日历 |
| 反证探针 | 给一个"帮我排下个月的发布日历"请求：M3 必须给运营判断与内容任务，不得输出日历表 |
| 取证阶段 | EP-05（①②）／EP-08（③） |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-02 · 锚点与 Matrix 非隐性前门

| 项 | 内容 |
|---|---|
| 命题 | 单次任务不强制建档；持续运营只要求最小可恢复锚点（个人／企业／空白账号均可）；Matrix 不是隐性前门；暂定锚点不被升级为确认事实 |
| 冻结输入 | 四个变体：①无任何 Matrix 产物 + 自然语言描述；②明确"空白新号"；③企业账号但定位暂定；④定位确实缺失且当前分支真实依赖定位 |
| 候选 | 每变体一条冻结候选 |
| 锁定变量 | 目标、平台、产能、事实集 |
| Oracle | 有界判断 + 结构检查：①②③必须继续产出判断，且锚点标注为"暂定/来源/不确定性/复验触发"；④只阻断依赖定位的那一支，无关分支继续 |
| 成功 | 四变体全部符合，且无一处把暂定写成"已确认定位" |
| 不足 | ④整任务停止 → `FAIL` |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | 任一变体要求"请先完成 Matrix"作为继续条件 |
| 反证探针 | 变体②后追问"这个号的定位是什么"，输出若给出确定性定位陈述而无暂定标记即 `FAIL` |
| 取证阶段 | EP-05 结构 + EP-06 Runtime |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-03 · 八类目标产生非互换判断

| 项 | 内容 |
|---|---|
| 命题 | 只改主目标时，内容角色／资源顺序／验证重点／承接路径发生可解释的实质变化；GMV／线索／到店不坍缩；短期与长期不互改 |
| 冻结输入 | 冻结一个账号 + 事实 + 平台 + 产能 + 权限；八个变体只改 `primary_objective`（长期价值／起号／吸粉／流量／GMV／线索／到店／混合） |
| 候选 | 每变体一条，全部预冻结保留 |
| 锁定变量 | 除主目标外全部 |
| Oracle | 有界判断（盲评者不知变体身份）：任两个变体必须在"内容角色、资源顺序、验证重点、承接路径"四轴中至少两轴不同，并能说清怎么不同 |
| 成功 | 八变体两两满足，且事实与身份不随目标漂移 |
| 不足 | 仅 CTA/标签不同 → `FAIL` |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | GMV/线索/到店给出同一套"转化"方案；或流量目标被静默改写成长期价值 |
| 反证探针 | ①流量目标 + 一份强长期价值参考资产 → 若静默改写成长期教育即 `FAIL`；②GMV 目标 + 无有效购买路径 → 若编造购买 CTA 而不局部降级即 `FAIL` |
| 取证阶段 | EP-06 |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-04 · 周期多目标、单条一主有限次

| 项 | 内容 |
|---|---|
| 命题 | 周期可承载多目标；每条内容任务只有一个 `primary_job` 和有限 `secondary_contributions`；不为满足"一主"机械拆成多条低质量内容 |
| 冻结输入 | 一个混合目标周期（如 吸粉主 + GMV 次），产能 = 3 条 |
| 候选 | 冻结 3 条候选内容任务 |
| 锁定变量 | 目标集、产能、事实 |
| Oracle | 结构检查（每条任务恰有一个 primary_job）+ 有界判断（3 条是否各自承担不可互换的业务工作） |
| 成功 | 结构通过且 3 条互不可换 |
| 不足 | 3 条中有 2 条互换后无差别 → `FAIL`（A5 消融） |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | 单条 Brief 塞入全部周期目标；或把 1 条拆成 3 条只为满足"一主" |
| 反证探针 | 把产能降为 1：输出必须选出 1 条并说明被让掉的收益，而不是把 3 条压缩进 1 条 |
| 取证阶段 | EP-06 |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-05 · 四类行为未被冻结为物理枚举

| 项 | 内容 |
|---|---|
| 命题 | `ACCOUNT_STATE_DIAGNOSIS` / `CYCLE_PLANNING` / `DAILY_CONTENT_DECISION` / `REVIEW_UPDATE` 可单独或组合运行，未冻结为 Runtime 枚举、互斥状态、数据库枚举、四节点、四 Workflow、四份 Skill 或固定顺序；历史 `DAILY_DECISION` 仅作兼容别名 |
| 冻结输入 | Skill 树 + interfaces schema + Dify 图导出 + M2 迁移脚本 |
| 候选 | 唯一实现 |
| 锁定变量 | — |
| Oracle | 反搜：四个标签不得出现在任何 `enum`／`CHECK` 约束／节点分支条件／必填字段中；组合行为夹具（一次调用同时完成诊断+周期规划）与跳序夹具（直接复盘）须正常产出 |
| 成功 | 反搜为空 + 两个夹具通过 |
| 不足 | 标签仅出现在说明文字里但输出结构要求四选一 → `FAIL` |
| 无结果 | Dify 图尚未创建 → 该半 `NOT_VERIFIED` |
| 失败 | 出现四个物理 Workflow 或四份 Skill |
| 反证探针 | 输入一个同时含"看看现在怎么样"和"顺便把这周排一下"的请求 |
| 取证阶段 | EP-05（代码/schema）／EP-06（Dify 图） |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-06 · 三类产能分离与真实取舍

| 项 | 内容 |
|---|---|
| 命题 | 期望发布量、基线产能、当期实际产能三值分别承载；超载时呈现减量／延期／调整结构／增加资源／不发等真实选择 |
| 冻结输入 | 用户要求每天 3 条，实际可完成 2 条；给 3 个价值不等的业务工作 |
| 候选 | 冻结 3 个业务工作 |
| 锁定变量 | 目标、事实、平台 |
| Oracle | 结构检查（三值在投影与输出中分别可见、不坍缩）+ 有界判断（是否选出 2 条并说明被让掉的收益与第三条去向） |
| 成功 | 二者均通过 |
| 不足 | 选了 2 条但未说明被让掉的收益 → `FAIL(INSUFFICIENT)` |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | 把"每天 3 条"改写成"每周 3 条"；或三条全部降质凑数；或把普通优先级问题退回用户 |
| 反证探针 | 把三值改成完全相同的数字：输出不得因此丢失三值区分 |
| 取证阶段 | EP-05（投影结构）／EP-06（判断） |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-07 · 共同质量底线与合法演绎

| 项 | 内容 |
|---|---|
| 命题 | 起号／流量／GMV 内容仍满足适用的活人感、人设辨识度、创意差异、可拍性、事实纪律与成品完整度；适用义务不是全能硬门；合法剧情/二创不以真实经营事件为前提；资料不足只降低事实专属性，不降低创意质量 |
| 冻结输入 | ①流量目标 + 完整事实；②GMV 目标 + 部分事实缺口；③无真实经营事件、只有表达裁量授权 |
| 候选 | 每变体一条 |
| 锁定变量 | 模型、参数、产能 |
| Oracle | 有界判断（盲评 Rubric §5.3：大模型总结腔／模板拼装／正确但无用；以及"删掉品牌名后是否还认得出"） |
| 成功 | 三变体均无廉价 AI 味，且③在无真实事件时仍给出有创意深度的合法方向 |
| 不足 | ③降级为"资料不足，建议补充"而未产出 → `FAIL` |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | 对列表型/对比型内容强加叙事或共情硬门；或②中编造缺口事实 |
| 反证探针 | 要求"日更 3 条"：输出不得机械复制同一内容的三种说法 |
| 取证阶段 | EP-06 |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-08 · 稳定兑现／受控探索／有限实验

| 项 | 内容 |
|---|---|
| 命题 | 二者按证据动态判断、无固定比例；实验有限且具备一个主要假设、可观察结果、反证与停止/继续条件；单条异常不触发全面改向；有依据地保持不变是合法结果 |
| 冻结输入 | 一个重复验证过的机制 + 一个单次高表现机制 + 一个未验证但适配的机会 |
| 候选 | 冻结三机制的处置候选 |
| 锁定变量 | 目标、产能、事实 |
| Oracle | 结构检查（每个探索提案是否含假设/最小可逆动作/支持观察/反证观察/到期条件/让位的稳定义务）+ 有界判断（证据成熟度与当前使用决定是否分开） |
| 成功 | 二者通过，且单次高表现未被推广为全账号模板 |
| 不足 | 探索提案缺反证或到期条件 → `FAIL(INSUFFICIENT)` |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | 输出固定比例（如"7:3"）；或要求任何条件下探索都必须非零；或无依据地"保持不变" |
| 反证探针 | 给一条与既有判断矛盾的异常反馈：不得据此全面改向 |
| 取证阶段 | EP-06 |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-09 · 外部市场证据五方责任与声明上限

| 项 | 内容 |
|---|---|
| 命题 | M2／M3／创意锦标赛／M4／PP 的市场责任分层成立；时效、权限、范围匹配被保留；同质化检查顺序为"平台同类 → 账号自身历史"；无可靠资料时不得声明平台稀缺/唯一/已避开同质化 |
| 冻结输入 | 四类观察：强信号弱适配／中等信号强适配／拥挤题材无正需求信号／过期宽泛证据；外加"完全无市场资料"变体 |
| 候选 | 每类一条处置候选 |
| 锁定变量 | 账号、目标、产能 |
| Oracle | 结构检查（观察是否带来源/平台/时间/范围/有效期，`is_expired` 是否被使用）+ 反搜（"平台唯一""平台稀缺""已经避开同质化"在无当前范围匹配证据时出现即 `FAIL`）+ 有界判断（四类处置是否正确区分机会/追热点/同质化防御/不适配机会） |
| 成功 | 三者全部通过 |
| 不足 | 处置正确但未保留时效/范围 → `FAIL(INSUFFICIENT)` |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | 把外部证据当成账号因果；或 M3 写出钩子/创意机制 |
| 反证探针 | 只给"某题材很火"而无适配与承接：不得据此直接授权生产 |
| 取证阶段 | EP-05（反搜/结构）／EP-06（判断） |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-10 · 原始反馈与解释判断分离

| 项 | 内容 |
|---|---|
| 命题 | M2 原始反馈与 M3 解释假设/局部影响判断分离；充分、冲突、不足、观察窗未结束四种情形不坍缩为同一处理 |
| 冻结输入 | ①单条异常反馈；②多条一致反馈；③相互冲突反馈；④观察窗未结束；每条带 `kind`/`is_test`/`is_simulated`/`is_manual_entry`/`window_start`/`window_end`/`source` |
| 候选 | 四情形各一条 |
| 锁定变量 | 账号、周期、目标 |
| Oracle | 结构检查（M3 输出中不存在对原始反馈的改写/覆盖字段；观察先于原因）+ 有界判断（四情形处理是否实质不同） |
| 成功 | 二者通过，且④明确"窗口未结束、暂不改判"而非静默改判 |
| 不足 | ①与②处理相同 → `FAIL` |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | 把相关性写成因果；或把模拟反馈当真实市场效果 |
| 反证探针 | 给一条模拟反馈并要求"这说明我们的内容在平台上有效吗" → 必须拒绝该推论 |
| 取证阶段 | EP-06 / EP-07 |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-11 · Campaign 覆盖与到期恢复

| 项 | 内容 |
|---|---|
| 命题 | Campaign 只占用点名位置；未覆盖位置仍由 M3 管理；到期默认恢复**仍有效**的周期基线（不复活已合法变更的旧任务）；沿用活动策略须形成新的有依据判断并满足接受权限 |
| 冻结输入 | 现行周期基线 → 对两个点名位置施加有期限 Campaign → 活动期内合法变更一个未覆盖位置 → 到期 |
| 候选 | 一条纵向序列 |
| 锁定变量 | 账号、目标、产能 |
| Oracle | 结构检查（`targeted_positions` 之外的位置在覆盖期内仍产出 M3 判断；到期后恢复的是当前基线）+ 反搜（"永久""长期改为"等把 Campaign 写成基线的措辞） |
| 成功 | 二者通过 |
| 不足 | 到期恢复了活动开始时的快照而非当前基线 → `FAIL` |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | Campaign 静默保留；或 Campaign 改写账号定位 |
| 反证探针 | 到期后追问"继续用活动那套吧" → 必须形成新的有依据判断，不得直接沿用 |
| 取证阶段 | EP-07 |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-12 · M2→M3 最小投影

| 项 | 内容 |
|---|---|
| 命题 | 投影只含当轮最小必要输入；保留来源、权限、时效、以及缺失／拒绝／暂定／过期／不适用五类状态，不坍缩为同一空值；不过量读取 |
| 冻结输入 | `account-operations/interfaces/M2_TO_M3_PROJECTION_v1.0.schema.json` + 投影构建器 + M2 实际响应样本 |
| 候选 | 唯一实现 |
| 锁定变量 | M2 接口版本（绑定 `main@df2c595` 的 `business-persistence/`） |
| Oracle | 单元/契约测试（确定性工具）：①五类状态两两不等；②schema `additionalProperties:false` 拒绝越界字段；③字段消融——删除任一投影字段后至少一条测试失败，否则该字段不成立 |
| 成功 | ①②③全部通过 |
| 不足 | ③中有字段删除后无测试失败 → 该字段须删除或合并，否则 `FAIL` |
| 无结果 | 未实现 → `NOT_VERIFIED` |
| 失败 | 投影直接读库表结构；或把整个账号历史投给模型 |
| 反证探针 | 构造"用户拒绝提供产能"与"产能未知"两条输入，投影结果必须可区分 |
| 取证阶段 | EP-04（实现）／EP-05（正式取证） |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-13 · M3→M2 只写候选

| 项 | 内容 |
|---|---|
| 命题 | M3 只产出诊断／周期计划／内容任务／复盘更新**候选**；接受与当前有效版本由 M2/用户负责；M3 不覆盖原始反馈、不修改来源、不直接晋升、不建第二状态库 |
| 冻结输入 | `M3_TO_M2_WRITEBACK_CANDIDATE_v1.0.schema.json` + Skill 全文 + Dify 图 |
| 候选 | 唯一实现 |
| 锁定变量 | M2 接口版本 |
| Oracle | ①反搜：M3 侧不存在指向 M2 写接口的直接调用、凭据或 `is_current=true` 的设置；②schema 检查：候选信封必含 `candidate_status ∈ {proposed}` 且不含"当前有效"语义字段；③结构检查：不存在 M3 自有的持久化状态存储 |
| 成功 | ①②③通过 |
| 不足 | 候选信封允许调用方直接标记为已接受 → `FAIL` |
| 无结果 | 未实现 → `NOT_VERIFIED` |
| 失败 | 出现覆盖 `feedback_records` 或 `market_observations` 的路径 |
| 反证探针 | 尝试用候选信封表达"把这条反馈改成正面" → schema 必须无法表达 |
| 取证阶段 | EP-04（实现）／EP-05（正式取证） |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-14 · Content Brief 可消费 + `NO_CONTENT_TASK`

| 项 | 内容 |
|---|---|
| 命题 | 内容任务可被 Content Brief 直接消费且只有一个主要工作；`NO_CONTENT_TASK` 含具体理由、依据事实与类别、受影响与不受影响范围、明确触发器与重新判断条件，且不能用于逃避本可完成的工作 |
| 冻结输入 | ①生产条件充分的任务；②必要商品事实不可得、无合法替代承诺、正确结果是等待；③模型犹豫型（信息略少但可条件化） |
| 候选 | 三变体各一条 |
| 锁定变量 | 目标、平台、产能 |
| Oracle | ①下游消费测试：把①的内容任务作为"合法等价输入"交给 Content Brief 能力，检查其所需业务实质是否齐备（见 §6.3 已知缺口）；②schema 检查 `NO_CONTENT_TASK` 四要素齐备；③有界判断：③必须产出条件化结论而非空结果 |
| 成功 | ①②③通过 |
| 不足 | ②齐备但理由是"信息不足"这类无类别表述 → `FAIL(INSUFFICIENT)` |
| 无结果 | 下游消费未跑 → 该半 `NOT_VERIFIED` |
| 失败 | ③返回 `NO_CONTENT_TASK`；或②编造替代承诺来填满输出结构 |
| 反证探针 | 给一个只缺一项非阻塞信息的任务：返回 `NO_CONTENT_TASK` 即 `FAIL` |
| 取证阶段 | EP-05（结构 + 下游消费）／EP-06（判断） |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-15 · 相邻模块接缝与 CTA 权限

| 项 | 内容 |
|---|---|
| 命题 | Matrix／Campaign／M2／M4／Brief／创意锦标赛／Script／PD／PP 的责任接缝清楚；合法直接入口不被 M3 强制拉回完整链路；CTA 三层权限成立 |
| 冻结输入 | Skill 全文；三个直接入口夹具（已有完整脚本求生产准备／明确选题直接进 Brief／已有成片信息直接进 PP）；三类 CTA 请求 |
| 候选 | 每夹具一条 |
| 锁定变量 | — |
| Oracle | ①责任反搜：Skill 中不得出现钩子/叙事/台词/镜头/标题/封面/发布文案的具体产出；②直接入口夹具中 M3 必须让路或只提供其真正增加的判断；③CTA：低风险互动可自提；商品/咨询/线索/到店需目标+有效承接路径；站外导流/价格优惠/强购买承诺需明确授权 |
| 成功 | ①②③通过 |
| 不足 | ③中把"有经营目标"当成高风险 CTA 的授权 → `FAIL` |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | M3 为直接入口任务伪造周期字段；或强制补跑 Matrix/Campaign |
| 反证探针 | "我们要冲 GMV，直接挂个站外优惠链接" → 必须要求明确授权与承接条件，不得因 GMV 目标自动放行 |
| 取证阶段 | EP-05（①）／EP-06（②③） |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-16 · Runtime 保真

| 项 | 内容 |
|---|---|
| 命题 | 唯一语义经 Skill → 条件引用 → Workflow → Dify → 准确 DeepSeek 运行后，关键区别、限定语与模块边界没有丢失；Qwen 只作独立核验，不作默认 fallback |
| 冻结输入 | `ECC-M3-RUNTIME-FIDELITY-001`（EP-06 冻结）：正向、负向、边界、来源降级、动态证据过期、附件加载/未加载、平台差异，共 ≥7 组 |
| 候选 | 冻结候选集，全部保留 |
| 锁定变量 | provider、准确 model id、参数、Workflow 图、commit、权限 |
| Oracle | 逐项比对"语义主稿中的区别"是否在 Runtime 输出中仍可辨认；由未参与实现者按冻结清单判定 |
| 成功 | 全部 ≥7 组保真 |
| 不足 | 有区别在某一组丢失 → `FAIL` |
| 无结果 | Dify/模型不可得 → `BLOCKED` 或 `NOT_VERIFIED`，不得用教程或模拟替代 |
| 失败 | 出现自动 fallback 到 Qwen；或 Prompt 文件存在/一次成功响应被当作通过 |
| 反证探针 | 附件未加载条件下运行服装类请求：输出必须承认未加载而非凭记忆补行业惯例 |
| 取证阶段 | EP-06 |
| 本轮状态 | `PASS`（Skill → 条件引用 → 直连 DeepSeek 链路）＋ `PASS`（Skill → 条件引用 → **Dify Workflow 图** → DeepSeek 链路）；`NOT_VERIFIED`（**浏览器渲染画布**）。三条链路的分界见下方"EP-06 实测记录"与"第 3 轮追加" |

**EP-06 实测记录（2026-08-26）**：

- 第 1 轮（commit `874bea1`，Skill 版本 `874bea1` 前）：9 次真实 DeepSeek 调用（`deepseek-v4-flash`，`temperature=0.4`，全部 `HTTP 200`），独立 Reviewer（无先前对话记忆、未读任何 Checkpoint）判定 6/7 `成功`，组 6（附件未加载）为`不足`——模型未凭记忆编惯例（不构成`失败`），但从未点名"参考资料本身未加载"，只报告了账号事实缺口，两类缺口未分列。判据本身零改动。见 `M3_ECC_RUNTIME_FIDELITY_001_VERDICT_v1.0.md`（commit `22e1600`）。
- 修复（commit `af61b82`）：`SKILL.md` O-6 与文末〈参考文件〉两处补一句，要求缺口条目必须明确点名是附件本身未加载，不能只用具体事实缺口代替。按 A3，Skill 内容变更使全部 9 组前序证据相对新版本 `STALE`，非仅组 6。
- 第 2 轮（commit `a990d68`，Skill 版本 `af61b82`）：全部 9 次调用重跑，另一个独立 Reviewer（新实例，未见第 1 轮判定、未见任何 Checkpoint）判定 **7/7 `成功`**。见 `M3_ECC_RUNTIME_FIDELITY_001_VERDICT_v1.1.md`（commit `de13ec1`）。
- **未计入判定、已记录为独立观察**：组 1 变体 A 的 system prompt 实测确实附有 `references/fashion-and-market.md` 全文（`include_fashion_ref=true`），但模型输出自称"行业/季节参考文件本轮未加载"——一次关于自身上下文使用状态的不准确自述（方向是少报而非多报，未造成事实污染或编造）。这类自述准确性不在本组冻结 Oracle 范围内（Oracle 只要求账号身份/商品事实一致），按 A2 不得看到结果后加严判据，本轮不计入 AC-16 判定；登记为独立观察，需要新开版本化判据才能纳入未来验收范围，不在本 commit 内处理。
- **画布级证据仍未建立**：Dify Console 访问凭据缺失（见 `M3_CHECKPOINT_ROUND_2.md` §8），本轮全部证据来自直连 DeepSeek API，不经过 Dify Workflow/画布；按 §12.3，画布级 AC-16 证据保持 `NOT_VERIFIED`，不受本轮直连结果影响。

**第 3 轮追加（Dify Workflow 图链路，2026-08-26 同日晚些时候）**：

- Dify Console 凭据缺口于同日解除（Founder 本人执行 `flask reset-password`），随后建立 task-id 专用候选 App `b7fb5b1a-9278-426c-bb8a-f9f288639548`（图 = `start → llm → end` 三节点）并发布。
- `ECC-M3-RUNTIME-FIDELITY-001` 的判据**一个字未改**，只更换绑定中的"Workflow 图"这一个锁定变量：9 例经 `POST /v1/workflows/run` 全部 `succeeded`。第三名独立判定者（未读任何 Checkpoint、未读前两轮判定、未读本文件）判 **7/7 成功**，见 `M3_ECC_RUNTIME_FIDELITY_001_VERDICT_DIFY_v1.0.md`。
- **必须分清的两件事**：本轮建立的是"语义经 **Workflow 图** 之后没有丢失"。它**不是** Prompt §12.3 意义上的**画布验收**——§12.3 明确写着"Dify 交付真相是当前草稿图、实际配置和**浏览器渲染画布**，不是单次 API 运行历史"。草稿图与实际配置已逐项核验（`evidence/ep10-closeout/dify_graph_structural_check.json`），**浏览器渲染那一层没做**：本机无 playwright / chromium，不具备浏览器自动化能力。该半继续保持 `NOT_VERIFIED`，**不用运行历史顶替**。
  （此处曾在 `M3_FOUNDER_ACCEPTANCE_PACKAGE_v1.0.md` 初稿中被合并成一个 ✅，由独立收口 Reviewer 指出后更正；更正记录见该文件 §13 与 `M3_INDEPENDENT_CLOSEOUT_REVIEW_v1.0.md`。）
- **同一现象第二次复现**：第 3 轮 `G7-A` 在 `loaded_references` 实际传入 2799 字符（sha256 与其余 7 组完全相同）的情况下，正文写"专业参考附件（`fashion-and-market.md`）本轮未加载"。这与第 2 轮 `G1-A` 是同一失效模式，由**两名不同的**独立判定者各自发现，执行侧两次均亲自复核原始记录属实。按 A2 不得在看到结果后加严判据，本轮仍不计入 AC-16 判定；**但"一次性观察"已升级为"可复现失效"**，登记为新任务候选。
- **判定者隔离性已机械核验**：五名独立判定者（保真 3 轮 + 纵向 + 行为）的执行记录逐条解析，只扫会造成文件访问的参数，结果全部 `CLEAN`、零违规，见 `evidence/ep10-closeout/judge_isolation_verification.json`。此前这一点只有判定者自述，由独立收口 Reviewer 判为未核验（阻断项 B-2），本轮以补证据关闭，**不是**靠降级措辞关闭。
- **成本**：两轮共 18 次真实调用，`prompt_tokens≈165,268`，`completion_tokens≈200,637`（含真实 `reasoning_tokens`，`deepseek-v4-flash` 为推理模型）。

### M3-AC-17 · 纵向多周期可复现

| 项 | 内容 |
|---|---|
| 命题 | 同一虚拟账号多周期序列中，诊断→周期计划→日常决定→复盘→Campaign 返回→局部失效可复现，且状态经由定义的投影承载 |
| 冻结输入 | `ECC-M3-LONGITUDINAL-001`：产能下降、市场机会、单条异常、多条一致、冲突反馈、观察窗未结束、目标冲突、Campaign 覆盖/到期、证据过期，共 ≥9 事件 |
| 候选 | 一条完整序列，全程保留 |
| 锁定变量 | 账号、模型、参数 |
| Oracle | 结构检查（每步状态经投影传递，不靠对话记忆）+ 有界判断（变化是否局部、证据身份是否保持分离） |
| 成功 | 全序列通过且无一步静默改写 Matrix 或下游创意责任 |
| 不足 | 某步靠会话记忆而非投影承载 → `FAIL` |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | 模拟/测试反馈被写成真实经营反馈 |
| 反证探针 | 中途换会话重入：必须能从投影恢复，不要求用户重述 |
| 取证阶段 | EP-07 |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-18 · 公平同模型 A/B

| 项 | 内容 |
|---|---|
| 命题 | 同时通过五项非补偿硬门（目标忠实、事实、权限、风险、当前任务必要条件）与盲评整体增益门 |
| 冻结输入 | `ECC-M3-MODULE-AB-001`（EP-08 冻结）：A=胜任的通用单账号运营基线（不故意弱化）；B=冻结 M3 候选 + 必要附件 |
| 候选 | 全部预冻结候选保留，不择优、不重抽 |
| 锁定变量 | 同一准确 DeepSeek model/provider、参数、权限、输入/输出预算、输入事实、目标、平台、产能、候选机会 |
| Oracle | 盲评者不知 A/B 身份，按 §5.3 Rubric 逐项判定 |
| 成功 | 五项硬门无实质退化 **且** 存在盲评可识别的整体增益 |
| 不足 | 硬门通过但无整体增益 → `M3_PROFESSIONAL_GAIN = NOT_VERIFIED`，`M3_MODULE_AB = NOT_PASSED` |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | 任一硬门实质退化（不得用其他优势抵消） |
| 反证探针 | A 基线故意弱化检查：由独立方复核 A 提示词是否胜任 |
| 取证阶段 | EP-08 |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-19 · Qwen 隔离核验、独立 Review、留出与分轨

| 项 | 内容 |
|---|---|
| 命题 | Qwen 只读语义核验成立且无产品裁决权；独立 Reviewer 未参与实现、上下文隔离、无写权限；受影响回归完成；Holdout 身份与探索/正式分轨成立 |
| 冻结输入 | `ECC-M3-QWEN-SEMANTIC-001`；Reviewer 冻结 Rubric；Holdout 清单（§4.9）；Attempt 账本 |
| 候选 | — |
| 锁定变量 | Reviewer 身份、Holdout 清单冻结时间 |
| Oracle | 结构检查（Attempt 时间戳与哈希先后；Holdout 未被用于调试）+ Reviewer 结论记录 |
| 成功 | 全部成立，正式 Review 一轮 + 定向修复一轮内闭合，且修复后完成影响面复验 |
| 不足 | Holdout 被泄露 → 该夹具退出留出集并记录，AC 不因此自动 `FAIL`，但留出证据强度下降须如实记 |
| 无结果 | 未跑 → `NOT_VERIFIED` |
| 失败 | Qwen 单独放行；或 Reviewer 参与过实现；或用预算遮蔽新失败 |
| 反证探针 | 检查是否存在"看到结果后修改判据"的记录 |
| 取证阶段 | EP-09 |
| 本轮状态 | `NOT_VERIFIED` |

### M3-AC-20 · 收口、回滚、远端与 Founder 接受

| 项 | 内容 |
|---|---|
| 命题 | 最终 commit、专用 Dify App/版本/图、模型参数、证据索引、回滚演练、远端任务分支与 Founder 产品接受闭合；声明上限准确（不含 M5、生产上线或经营提升） |
| 冻结输入 | 远端完整 hash；Dify 导出/恢复演练记录；Reviewer 关闭项；Founder 自然语言验收包与答复 |
| 候选 | — |
| 锁定变量 | 最终候选绑定 |
| Oracle | 确定性工具（远端 hash 复读、导出文件比对）+ Founder 明确 `ACCEPT` / `RETURN` |
| 成功 | 全部闭合且终态声明不超过证据 |
| 不足 | 技术候选完成但 Founder 未验收 → 记 Checkpoint，输出 `AWAITING_FOUNDER`，不得写 DONE |
| 无结果 | 未推送 → `NOT_VERIFIED` |
| 失败 | 出现"生产就绪""真实经营提升""单账号纵向切片完成"等超出证据的声明 |
| 反证探针 | 全文反搜 `REAL_OPERATION_LOOP_VERIFIED` / `OPERATIONAL_UPLIFT_PROVEN` 是否被无证据使用 |
| 取证阶段 | EP-10 |
| 本轮状态 | `NOT_VERIFIED` |

---

## 4. 计划夹具身份清单（冻结身份，内容在对应 EP 冻结）

> 身份先冻结，防止后续"临时造一个更容易过的夹具"。每条标明类型与是否留出。

| 夹具 ID | 类型 | 覆盖 AC | 留出 |
|---|---|---|---|
| `FX-M3-POS-01` 目标反事实族（八变体） | 正向/反事实 | AC-03 | 否 |
| `FX-M3-POS-02` 同目标不同阶段（四变体 + 一模糊变体） | 正向/边界 | AC-02 | 否 |
| `FX-M3-POS-03` 转化覆盖与衰退假设（A/B 两例） | 边界 | AC-02, AC-10 | 否 |
| `FX-M3-POS-04` 产能冲突（日三条 vs 可完成两条） | 正向 | AC-06 | 否 |
| `FX-M3-POS-05` 稳定兑现 vs 探索（三机制） | 正向 | AC-08 | 否 |
| `FX-M3-POS-06` 服装局部失效（双色断一色、无试穿素材） | 正向/边界 | AC-07, AC-09 | 否 |
| `FX-M3-POS-07` GMV／线索／到店三变体 | 反事实 | AC-03 | 否 |
| `FX-M3-POS-08` 市场机会 vs 追热点（四类信号） | 正向/负向 | AC-09 | 否 |
| `FX-M3-POS-09` 反馈局部影响（充分/冲突/不足/窗口未完） | 正向/边界 | AC-10 | 否 |
| `FX-M3-POS-10` Campaign 覆盖与回归 | 纵向 | AC-11 | 否 |
| `FX-M3-POS-11` 栏目缩减/暂停/退出（三变体） | 边界 | AC-15 | 否 |
| `FX-M3-NEG-01` 单 Brief 塞全部目标 | 负向 | AC-04 | 否 |
| `FX-M3-NEG-02` 流量目标被改写为长期价值 | 负向 | AC-03 | 否 |
| `FX-M3-NEG-03` 无有效承接路径却编造购买 CTA | 负向 | AC-03, AC-15 | 否 |
| `FX-M3-NEG-04` 用 `NO_CONTENT_TASK` 逃避可完成工作 | 负向 | AC-14 | 否 |
| `FX-M3-NEG-05` 无证据宣称"平台唯一/已避开同质化" | 负向 | AC-09 | 否 |
| `FX-M3-NEG-06` 因 GMV 目标自动放行站外导流 | 负向 | AC-15 | 否 |
| `FX-M3-NEG-07` 机械拆条凑"一主" | 负向 | AC-04 | 否 |
| `FX-M3-ABL-01` 删除目标忠实段落 | 消融 | AC-03 | 否 |
| `FX-M3-ABL-02` 逐项删除投影字段 | 消融 | AC-12 | 否 |
| `FX-M3-ABL-03` 不加载条件附件运行服装请求 | 消融 | AC-16 | 否 |
| `FX-M3-ABL-04` 删除 M3、只留通用运营提示 | 消融 | AC-01 | 否 |
| `FX-M3-DIR-01` 已有完整脚本求生产准备 | 直接入口 | AC-15 | 否 |
| `FX-M3-DIR-02` 明确选题直接进 Brief | 直接入口 | AC-15 | 否 |
| `FX-M3-LONG-01` 跨周期连续运行（≥9 事件） | 纵向 | AC-17 | 否 |
| `FX-M3-HOLD-01` 留出目标变体（不在 POS-01 中） | 留出 | AC-03, AC-18 | **是** |
| `FX-M3-HOLD-02` 留出产能/反馈组合场景 | 留出 | AC-06, AC-10, AC-18 | **是** |
| `FX-M3-HOLD-03` 留出服装局部失效变体 | 留出 | AC-07, AC-18 | **是** |

**留出规则**：`FX-M3-HOLD-*` 在 EP-08 之前不得被任何实现者查看、调试或用于提示优化。一旦被查看即退出留出集并在账本记录退出事件与时间。

---

## 5. A/B 公平条件与盲评 Rubric（冻结）

### 5.1 公平条件（任一不满足则 A/B 无效，不是"降级通过"）

同一 provider 与准确 model id（DeepSeek V4 Flash）；同一参数集；同一工具权限与来源可用性；同一输入事实、目标、平台、产能、候选机会；同一输入/输出预算；A 为**胜任的**通用单账号运营基线提示词（由未参与实现者复核，不得故意弱化）。

### 5.2 硬门（非补偿，五项，任一实质退化即 `FAIL`）

1. **目标忠实**：主目标未被静默改写，优先级与不可牺牲条件保留；
2. **事实**：无编造的商品、库存、价格、门店、顾客或经营事实；
3. **权限**：CTA 与表达风险不超过已授权层级；
4. **风险**：无违规手法、无暗号/藏码/诱导跳转、无无依据现实承诺；
5. **当前任务必要条件**：当前任务真正需要的业务实质未缺失。

**不得用其他维度的优势抵消任一项退化。**

### 5.3 增益门 Rubric（盲评，整体判断，不要求逐项获胜）

评审者在不知 A/B 身份的条件下，就以下维度给出"B 优 / 相当 / A 优 / 不适用"，并写一句可核查的理由：

| 维度 | 判什么 |
|---|---|
| 运营判断 | 阶段解释是否绑定证据与窗口；是否给出可辨识的机会与约束 |
| 周期组合 | 每条任务是否承担不可互换的业务工作；是否存在删掉也不变的任务 |
| 产能取舍 | 三值是否分离；被让掉的收益是否说清 |
| 实验设计 | 是否有一个主要假设、最小可逆动作、反证与到期条件 |
| 反馈判断 | 观察是否先于原因；变化是否局部；证据身份是否分离 |
| 内容任务质量 | Brief 是否拿到足以开始的业务实质，且创意空间未被占用 |
| 共同质量底线 | 是否出现大模型总结腔、模板拼装、正确但无用的废话 |

**整体增益成立** = 多数适用维度为"B 优"且无维度为"A 优且实质"。
**无整体增益** → `M3_PROFESSIONAL_GAIN = NOT_VERIFIED`、`M3_MODULE_AB = NOT_PASSED`，不是 `FAIL`（硬门退化才是 `FAIL`）。

**无效证据**（不得进入评分）：模型自评、输出长度、Skill 调用次数、规则条数、节点数量、术语密度。

---

## 6. 验收充分性反查（EP-01 强制项）

**问题**：若 AC-00～20 全部按本文件判据、以当前证据、绑定最终候选通过，是否等于 WHY 被解决？

WHY（合同 `why`）= *给一个账号一项以证据为界的持续运营能力，能决定周期组合、节奏、产能、实验、反馈应对与有依据的不变，而不变成排期器、选题器、定位权威、创意总监、生产系统或万能运营大脑。*

### 6.1 逐项映射

| WHY 的组成 | 由哪些 AC 覆盖 | 是否充分 |
|---|---|---|
| "一个账号" | AC-02（锚点，个人/企业/空白）、AC-17（同一账号跨周期） | 充分 |
| "以证据为界" | AC-09（市场证据时效/范围）、AC-10（反馈四情形）、AC-12（投影保留来源/权限/时效）、AC-19（留出与分轨）、§2 通用协议 | 充分 |
| "持续"运营 | AC-17（≥9 事件纵向）、AC-12/13（状态由投影承载而非会话记忆） | 充分 |
| 决定周期组合 | AC-03（目标非互换）、AC-04（一主有限次） | 充分 |
| 决定节奏与产能 | AC-06（三值分离与真实取舍） | 充分 |
| 决定实验 | AC-08（有限实验四要素） | 充分 |
| 反馈应对与有依据的不变 | AC-08、AC-10、AC-11 | 充分 |
| "不变成排期器/选题器/万能大脑" | AC-01（结构+责任反搜+消融） | 充分 |
| "不变成定位权威" | AC-02（Matrix 非前门、暂定不晋升）、AC-15（接缝） | 充分 |
| "不变成创意总监/生产系统" | AC-15（责任反搜）、AC-14（Brief 拿到实质但创意空间未占） | 充分 |
| "不建第二事实库" | AC-13（只写候选、无自有状态存储） | 充分 |
| 语义未在工程化中丢失 | AC-16（Runtime 保真 ≥7 组） | 充分 |
| 增益不是自述 | AC-18（非补偿硬门 + 盲评增益门） | 充分 |
| 结论不超过证据 | AC-20（声明上限反搜）、§2.4 分级用词 | 充分 |

### 6.2 反查结论

**全绿 = WHY 被解决**，成立。理由：WHY 的每个组成都至少有一条 AC 用**可失败**的判据覆盖（不是"有文档"就算），且三类最容易"全绿但没解决问题"的漏洞被单独堵住：

1. **只有文档没有运行** → 被 AC-16（Runtime 保真）与 AC-17（纵向）堵住，两者都不接受"文件存在""一次成功响应"；
2. **能跑但没有专业价值** → 被 AC-01 消融门与 AC-18 增益门堵住，二者都用盲评而非模型自评；
3. **有价值但越界** → 被 AC-13/AC-15 的反搜与 AC-01 的结构检查堵住。

### 6.3 反查中发现的两处真实缺口（已登记，不带缺口施工）

**缺口 A（已在本文件内解决）**：Prompt §15 的 AC 表把"证据分层"分散在 AC-19/AC-20，但"技术候选完成 ≠ 模块增益 ≠ Founder 接受 ≠ M5 增益 ≠ 真实经营提升"这条**五层分离**本身没有独立判据。若只按原表，可能出现"AC 全绿但终态声明越级"。处置：在 AC-20 的 Oracle 与失败条件中显式加入终态声明反搜（`REAL_OPERATION_LOOP_VERIFIED` / `OPERATIONAL_UPLIFT_PROVEN` / "生产就绪" / "纵向切片完成"），并在 §2.4 冻结分级用词。**这是判据的收紧，不是放宽，且在看到任何结果前完成。**

**缺口 B（本轮不能在工程侧解决，须登记）**：AC-14 要求"Content Brief 可直接消费 M3 内容任务"。已接受的[共享合同二](decision-chain/docs/V1_M0_SHARED_CONTRACT_EIGHT_CAPABILITIES_v0.1.md)已冻结"持续运营决策"是 Content Brief 的**第一条合法上游**；但仓库内现行 `decision-chain/skills/Content_Brief_Architect_v0.1.md` §1／§3.2 仍把"已被接受的 Campaign 决策包"写成必需输入，输入不足时输出 `INPUT_INSUFFICIENT`。

- 这是**已接受合同与现行 Skill 正文之间的真实错位**，不是 M3 的实现缺陷；
- 修改 Content Brief Architect 属于六份既有 Skill，**本合同 `never_authorized_by_this_contract` 明确禁止**；
- 处置：M3 侧的义务收窄为"内容任务必须携带 Brief §1 要求识别的全部业务实质，并显式标注 `upstream_kind = continuous_operation_decision`"；**Brief 端是否真的接受，本轮记 `NOT_VERIFIED`**，在 EP-05 下游消费测试中如实取证；若测试确认 Brief v0.1 拒绝，则 AC-14 的下游消费半记 `FAIL(ABSENT)` 并作为**新任务候选**上报 Founder，不在本任务内改 Brief。

---

## 7. 状态矩阵

### 7.1 冻结时（`f5a9aca`，2026-08-26T11:13:42Z）

全部 21 条 = `NOT_VERIFIED`。冻结的是"判什么、怎么判"，不宣布任何一项已通过。

### 7.2 当前（2026-08-26 第 6 轮 · 载体 v1.2 · 独立收口审查之后）

> 本表是**状态投影**，随证据变化更新；判据本体（§3 各卡的命题／冻结输入／候选／锁定变量／
> Oracle／成功／不足／无结果／失败／反证探针）**一个字未改**——第 6 轮独立收口 Reviewer
> 已机械核验「21 条 AC × 10 个判据字段自 `f5a9aca` 冻结以来 0 处变化」，`git log -p` 可验。
>
> **本节与 `M3_CHECKPOINT_ROUND_6.md` §8 是同一份状态的两处呈现，必须一致。**
> 第 4 轮它们曾不一致（本节停在第 4 轮而 Checkpoint 已推进），由独立收口 Reviewer 报为阻断项 `B-2`，
> 本次同步修复。**任一方今后再更新，另一方必须同轮更新**——这是 A3「载体变化必须同步全部引用」。

| AC | 当前状态 | 依据 |
|---|---|---|
| M3-AC-00 | `NOT_VERIFIED` | 授权、合同哈希、进入模式、分支/worktree/Dify 对象均可回指；远端已推送，本条随 `AC-20` 一并闭合 |
| M3-AC-01 ①② | 成立（不写 `PASS`） | 结构反搜对 v1.2 图重跑，四类行为标签命中 **0**；责任反搜 12 条测试通过 |
| M3-AC-01 ③ 消融门 | **`NOT_VERIFIED`（探索级结论）** | 推导本身干净：B vs `Bprime` 5/7 维 B 优、零硬门退化，独立 Reviewer 从 36 份判定原文逐格复现。**但本轮盲评协议在判定冻结、揭盲之后被原地改写**（`ADDENDUM_003` §1），按 A2 只算探索、不产生正式 `PASS`。恢复正式 `PASS` 的两条路见 `ADDENDUM_003` §4 |
| M3-AC-02 `AC-04` `AC-07` `AC-08` `AC-09` `AC-10` | 成立 | 行为 ECC（v1.2 轮 49 例） |
| M3-AC-03 | **不得 `PASS`** | 八变体两两 28 对里 1 对不满足（长期价值 × 吸粉只有一轴不同） |
| M3-AC-05 | 成立 | 结构半 0 命中；运行时半行为 ECC 通过 |
| M3-AC-06 | 成立**（带限定）** | 行为 ECC 判成立，但依赖 `_cases.json` 的 `B06-1` Oracle 更正（`_CASES_PROVENANCE.md`，七项机械声明已被独立 Reviewer 逐条复算属实）；判定者对「判据先于结果」标 `NOT_VERIFIED`。**这次更正认不认，是 Founder 的域** |
| M3-AC-11 | **未独立成立** | g1fix 轮判定者：`E11` 恢复的确实是含 `E10` 变更的当前基线、不是活动开始快照，**这一条是本轮最干净的证据**；但 `E10` 之后基线被截断（1997 → 546 字符），不能说该能力在**完整**基线上被验证过 |
| M3-AC-12 / M3-AC-13 | 对钉死绑定 `main@df2c595`：**成立**<br>对当前 `main@a7b8101`：**`STALE`** | 83 条契约/消融/反搜测试全绿；`main` 于 10:25:44 前进到 `a7b8101` 并改了 `business-persistence/`，M2 现在把可用性与可发布性分成两道闸并加了按账号/任务/期间的适用范围过滤，M3 投影未跟上（`evidence/ep13-a3-main-moved/`）。**`STALE` 是时效旗标不是结果**，需一次有授权的投影 schema 扩展才能复验 |
| M3-AC-14 | 成立**（判定者提示风险）** | 若对 `B14-1` 从严，本条也不成立 |
| M3-AC-15 | **不得 `PASS`** | `B15-DIR-01` 不足 + `B15-CTA` 模型侧零产出（记 `无结果`，按 §7 不重抽） |
| M3-AC-16 | Workflow 图链路 **`PASS`**<br>浏览器渲染画布 **`NOT_VERIFIED`** | 保真 9/9「成功」；系统提示词绑定已由已发布图逐字节实证（`SKILL.md` sha256 `343758f3…`，已观察级）。**画布那一半仍未做**：本机无浏览器自动化，按 Prompt §12.3 不用运行历史顶替 |
| M3-AC-17 | **`FAIL(INSUFFICIENT)`** | 修 G-1 后整条重跑，12 步全 `succeeded`、第 4 轮那类「零产出整体覆盖基线」事故**未复现**；但 `E04` `不足`，序列级「局部性」只部分成立。判定者独立追踪 8 个对象，投影真正保住 **1 个**，其余 7 个无声消失而 `dropped_without_notice` 全程为 `[]`（缺陷 G-3） |
| M3-AC-18 | **`FAIL(INSUFFICIENT)`** | 12/12 跑完，硬门零退化但无整体增益；`M3_PROFESSIONAL_GAIN = NOT_VERIFIED`、`M3_MODULE_AB = NOT_PASSED`。**`AC-01③` 的降级不改变本条**——两种记法下它都不是 `PASS` |
| M3-AC-19 | 部分成立 | Qwen `NOT_APPLICABLE`（Founder 指示，运行前判定）；**41 名判定者：40 名受隔离约束者机械核验全部 `CLEAN`、零违规，1 名按 Rubric 授权通读全部材料的独立收口 Reviewer 隔离性 `NOT_APPLICABLE` 并另核写入范围为 `CLEAN`**；留出强度上限如实记录；**独立收口审查已完成**（`M3_INDEPENDENT_CLOSEOUT_REVIEW_V12_v1.0.md`：R-1/R-2 `FAIL`、R-3～R-7 `PASS`，两条阻断项已在本轮闭合） |
| M3-AC-20 | 部分成立 | 回滚演练实测通过（破坏→恢复逐位相同，已发布版本未被触碰）；远端完整哈希 `83a11cf4…` 与本地 HEAD 逐字符一致（`evidence/ep10-closeout-v12/remote_hash_ac20.json`，可用 `verify_remote_hash.sh` 一条命令重验）；**Founder 产品接受未发生**——实测包已交付（`M3_FOUNDER_DIFY_TEST_PACK_v1.0.md`），裁决未请求 |

**汇总**：`PASS` 3 条（AC-05 的结构半＋AC-16 的 Workflow 图半，以及对钉死绑定成立的 AC-12/AC-13——后者对当前 `main` 为 `STALE`）｜
`FAIL(INSUFFICIENT)` 2 条（AC-17 / AC-18）｜`不得 PASS` 2 条（AC-03 / AC-15）｜其余成立或 `NOT_VERIFIED`。

**没有任何一条被"部分满足即通过"地读。** 本轮相对第 4 轮**只有下调没有上调**：
`AC-01③` 由 `PASS` 降为探索级、`AC-12`/`AC-13` 由 `CURRENT` 降为 `STALE`、`AC-11` 由"本 ECC 内成功"降为"未独立成立"。

### 7.3 本节（§7.2）的更新记录

> 记这张表是因为 §7.2 是**唯一一处允许随证据变化的地方**，它每改一次都必须留痕，
> 否则就会重演第 4→6 轮那次"判据文件停在旧状态、Checkpoint 已推进"的冲突。

| 更新 | 提交 | 改了什么 | 触发事件 |
|---|---|---|---|
| 初版 | `f5a9aca` | §7.1 全部 21 条 `NOT_VERIFIED` | 判据冻结 |
| 第 1 次 | `edcccb2` | 写入第 4 轮状态 | EP-10 第一次收口 |
| 第 2 次（本次） | 本次提交 | 同步到第 6 轮状态；`AC-01③` 降级；`AC-12`/`AC-13` 改为双绑定分列并标 `STALE`；`AC-11` 改引 g1fix 轮；`AC-17`/`AC-18`/`AC-19`/`AC-20` 更新 | 独立收口 Reviewer 阻断项 `B-2` |

| 第 3 次（本次） | 本次提交 | `AC-20` 回填远端完整哈希 | 推送完成 |

**§3 判据本体在以上四次里一字未改。**

---

```text
END_MARKER
= DIYU-V1-M3-ACCEPTANCE-CRITERIA-FROZEN-v1.0-END
```
