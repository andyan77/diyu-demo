# M3 Checkpoint · 第 7 轮（载体 v1.3）

> Checkpoint 是**索引**，不是真源。这里每一条都指向能被重新算一遍的东西。
> 本文件 §8 的状态表由 `account-operations/tools/ac_state/render_ac_state.py`
> 从单一真源 `evidence/ep18-ac-recompute-v13/AC_STATE_v13.json` 渲染，
> 判据文件 §7.2 渲染自同一份。**两处不可能不一致，因为它们不是两份数据。**

## 1. 身份与授权

```text
task_id       DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001   （不变，未新建根任务）
contract      M3_ENGINEERING_TASK_CONTRACT_v1.2.yaml     （不变，哈希未改）
权威事件       Founder 2026-08-26 第二次 CONTINUE_TASK（七条）
分支          task/m3-account-content-operator-v1
HEAD          950db73954b2edaa0d85e44cd31eb62f035a9ece
本轮提交数     52 个（相对 main@a7b8101）
仓库文件数     970
```

**本轮没有做的事**（逐条，都是明令禁止的）：未改 M2 真源、未改 M2 责任、
未合并或直推 main、未碰生产 Dify（除任务专用候选 App 外任何 App）、
未改其他任务的账本条目、未 force-push / amend / reset / squash / 改写历史。

## 2. 这一轮要修的四件事，各自落到哪

| 缺陷 | 修法 | 实测结果 |
|---|---|---|
| `G-2` 探索触发漏检 | 结构优先（声明了 `kind=exploration` 即必答）+ 措辞兜底扩为「提案形态」 | 夹具 48/48，**误报 0/17、漏检 0/25**；真实序列 `E01` 结构层命中 |
| `G-3` 持续位从散文正则抠 | 端到端结构化 `standing_positions[]`，闸门做集合比对 | 通道内零无声消失，但**覆盖面 1/21**，且自身有两扇后门（见 §5） |
| `G-4` 旧值压过当轮输入 | 新增 `check_stale_value_override` | **失败**：61 例命中 12 次，误报 11 次，6 次拒收合格交付（见 §5） |
| M2 接口兼容 | 投影 schema v1.1，五组语义分别承载 | 定向复验 9/9，`AC-12`/`AC-13` 的 `STALE` 解除 |

## 3. 一次完整正式重跑（Founder 第 5 条）

```text
保真   9/9  succeeded   （1 次传输重跑，失败那次保留在案）
行为  49/49 succeeded   （3 次传输重跑，判别器只认传输签名，产出过的内容绝不重跑）
纵向  12/12 succeeded   （零传输故障）
A/B   12/12 succeeded   （零失败、零泄漏）
```

**两次被丢弃的尝试，都在案，不并进上面的叙述：**

- `evidence/ep07-longitudinal-v13-aborted/` —— 纵向首次尝试跑完 `E01` 才因字段漏改中断；
  README 写明**执行侧在整条重跑之前已经看过那份 `E01` 的输出**。
- `evidence/ep08-module-ab-v13-aborted/` —— A/B 首次尝试 12 次调用全部发起后，
  在写盘那一行崩掉（沙箱下 worktree 只读），12 份产出未落盘即丢失。
  四项可核对事实支撑"一份都没看到"；重跑不是择优，但被丢弃这件事本身如实披露。

两次都装了挡板：纵向的投影契约断言改为**解析自己的源码**得出所需字段，不手工维护；
A/B 加了开跑前写盘探针，任一落盘目标不可写就直接退出、不发起任何调用。

## 4. 独立判定（Founder 第 4 条 / `AC-19`）

| 判定 | 结论 |
|---|---|
| 保真 ECC | 七组全「成功」；一处证据缺口标 `NOT_VERIFIED`（系统提示词全文未入 transcript） |
| 行为 ECC | 47 成功 / 1 不足（`B14-1`）/ 0 失败 / 1 无结果（`B09-5`） |
| 纵向 ECC | **`FAIL(INSUFFICIENT)`**，触发步 `E06` |
| A/B | `AC-01③` = `PASS`；`AC-18` = `FAIL(INSUFFICIENT)` |

**隔离核验 40/40 CLEAN**（4 份 ECC 执行记录含行为判定者中断的那一次 + 36 名盲评者）。
核验器本身又被两类假阳性打磨了一次，两类都写死为判据——其中一例是
某判定者的自检句「未执行 git 命令」被旧版读成执行了 git，**把否认当成了违规**。

**A/B 的预冻结是本轮与第 6 轮的实质差别。** 第 6 轮阻断项 `B-1` 是协议在揭盲后被原地改写；
本轮的回答不是保证而是机制：揭盲脚本开跑先自校验工作区哈希 == git 冻结版，
看到结果后回来改推导公式，脚本自己拒绝输出。36 份提示词由单一模板**构造性生成**、
生成时当场核验同构、落盘冻结逐份哈希，判定者读的就是那 36 个文件。
事后核验只能说"确实同构"，构造性生成还能说"不可能不同构"。

## 5. 本轮新发现的三条缺陷（机械复核已坐实，证据在 `evidence/ep19-gate-v13-defects/`）

| | 是什么 | 违反了哪一句冻结文本 |
|---|---|---|
| `D-1` | `E04` 模型声明的新持续位被补齐节点删除，首闸/复检/投影三处计数器全为 `[]` | `REBIND_004 §2.1`：「不会再出现字段全程为空而内容全丢这种事」——该断言对**本轮新增的位**不成立，因为 `positions_unaccounted` 只看输入差集，新增位被删后既不在输入也不在声明 |
| `D-3` | `E12` 模型没写审计块，首闸正确阻断，**补齐节点替它写出了 POS 行** | `REBIND_004 §2.1`：「判据能被闸门自己满足，就对模型没有约束力」——`blanket_introduced_by_gate` 只盯概括句，不盯代写的机器行 |
| `D-2` | `stale_value_override` 61 例命中 12 次、**误报 11 次**、6 次拒收合格交付 | `REBIND_004 §2.3` 自述作用域「只挡用旧值压当轮输入」——误报集中打在冻结判据**要求**的行为上：三值分离原句、Founder 第 3 条授权的等价换算、Oracle 要求的拒绝合并 |

**方法上的根因**：`REBIND_004 §2.2` 只给 `G-2` 冻结了「误报与漏检两个数字都要报」的义务，
`G-4` 没有对应条款。本轮夹具的旧值覆盖族误报 0——但那些句子是执行侧自己造的。
**在自己造的样本上量误报，等于自己给自己出题。** 这是 `G-2` 当初栽的同一个跟头换了个位置。

## 6. 相对第 6 轮，状态只有一处是"因为改了判据而变好"

`AC-01③` 从探索级升为 `PASS`，靠的是 Founder 第 4 条给的路径乙（预冻结判据重跑一次），
不是事后授权。其余变化都是重新取证的结果：`AC-03`/`AC-15` 由不得 `PASS` 变成立
（第 6 轮那几处在 v1.3 未复现）、`AC-12`/`AC-13` 由 `STALE` 变成立、
`AC-09`/`AC-14` 是**本轮新出现**的不得 `PASS`。

`AC-17`/`AC-18` 仍 `FAIL(INSUFFICIENT)`；`AC-17` 的触发点换了——
不再是模型丢状态，而是执行侧这一轮修的闸门自己误报把合格交付整份拒收。

## 7. Founder 实测包

`M3_FOUNDER_DIFY_TEST_PACK_v1.1.md`，绑定最终候选（App `b7fb5b1a-9278-426c-bb8a-f9f288639548`，
已发布版本 `m3-cand-v1.3-pos`）。七个场景，每个给全：精确 Dify 入口、完整三格输入、
操作步骤、系统实际输出、场景含义、业务影响、一个自然语言判断问题。
三格输入与输出**逐字取自运行记录**，脚本生成不手抄，已机械核对全部通过。
v1.0 已加取代指针，原文一字未删。

**只请求产品语义接受或退回，不请求技术方案裁决。**
包里主动写明了两件对执行侧不利的事：`G-4` 误报是执行侧缺陷不是模型不行；
以及 A/B 的结论——M3 被证明的是「比什么都不做好很多」，
**还没有被证明是「比一份好提示词更好」**。

## 8. AC-00 ～ AC-20 完整状态

| AC | 当前状态 | 依据 | 还差什么 |
|---|---|---|---|
| M3-AC-00 | 成立 | 授权事件、合同哈希、进入模式、分支/worktree/Dify 对象逐条可回指；绑定为实测非自述——M2 容器内两文件哈希 == `git show a7b8101`、SKILL sha256 == 冻结记录、Dify App 与已发布版本名落盘；远端已推送且本地 HEAD == 远端 HEAD | — |
| M3-AC-01 | 成立 | ①② 责任反搜 12 条测试通过 + 结构反搜对 v1.3 **已发布图**重跑，四类行为标签命中 0；③ 消融门 B vs B′ 在 ADDENDUM_003 预冻结判据下 `PASS`（七维六维 B 优、零实质退化、零硬门退化）；行为 ECC `B01-P` 反证探针成功 | — |
| M3-AC-02 | 成立 | 行为 ECC `B02-1/2/2P/3/4` 五例全成功，无一例把暂定锚点写成已确认定位，无一例要求先完成 Matrix | — |
| M3-AC-03 | 成立 | 行为 ECC `B03-1…8` + `N2` + `N3` 十例全成功；判定者逐对核过八变体两两**至少两轴**实质差异。第 6 轮那处「一对只有一轴不同」在 v1.3 运行上未复现 | 第 6 轮与本轮是两次不同运行、两名不同判定者；本条成立于 v1.3 这一次观察，不是跨轮稳定结论 |
| M3-AC-04 | 成立 | 行为 ECC `B04-1/1P/N1/N7` 四例全成功 | — |
| M3-AC-05 | 成立 | 结构半：对 v1.3 已发布图反搜，四类行为标签命中 0、落在结构位（enum／分支条件／必填字段）0；运行时半：行为 ECC `B05-1/2` 两例成功 | — |
| M3-AC-06 | `成立（带限定）` | 行为 ECC `B06-1/P` 两例成功，按**冻结在先**的 Oracle v2.0 口径（Oracle mtime 19:53 早于全部运行记录 20:49–21:41，判定者独立核过时序） | 限定项：`B02-4` 因 G-4 误报触发补齐，补齐把三值分离原句压成一句「输入给的期望发布数是每周三条」——为保护 AC-06 而建的检查反而删掉了 AC-06 要保护的东西。该例判定仍为成功，但这一处降质如实登记 |
| M3-AC-07 | 成立 | 行为 ECC `B07-1/2/3/P` 四例全成功；资料不足时未降级为「建议补充」而仍产出合法创意方向 | — |
| M3-AC-08 | 成立 | 行为 ECC `B08-1/P` 两例成功；单次高表现未被推广为全账号模板，未输出固定比例 | — |
| M3-AC-09 | `不得 PASS` | 行为 ECC `B09-5-no-market-data` = `无结果`：模型零正文（`draft_raw` 301 字符，`</think>` 之后只有空围栏与审计块），非传输故障（200 / succeeded / 单次尝试 / 7 步 / 14636 tokens），并编造了输入中不存在的持续位 id。闸门 `HARD_FAIL_MIN_OUTPUT` 拒绝补齐——这一处闸门做对了。用户可见的是一句系统说明，**资料稀薄导致整任务停摆**，正是该例设计要防的方向 | 另半（适用范围可机械排除）已在 `ep16` 闭合：M2 的范围排除结论逐字带回，三种「空」仍可区分 |
| M3-AC-10 | 成立 | 行为 ECC `B10-1/2/3/4/P` 五例全成功，①单条异常与②多条一致的处理实质不同；纵向序列 `E05/E06/E07/E12` 四情形均出现且处理正确（后者随纵向整体不给 PASS，只作追加证据） | — |
| M3-AC-11 | `未独立成立` | 纵向 `E09`→`E10`→`E11` 的 Campaign 覆盖—期内合法变更—到期恢复链完整且正确，判定者确认恢复的是**含 E10 变更的当前基线**而非活动开始快照。但冻结件 §7 不给部分通过留出口，纵向 ECC 整体 `FAIL(INSUFFICIENT)`，本条因此不取得正式 `PASS` | 要正式成立，需纵向 ECC 整体不再 `FAIL` |
| M3-AC-12 | 对钉死绑定 `main@df2c595`：`成立`<br>对当前 `main@a7b8101`：`成立` | 第 6 轮对当前 main 为 `STALE`。本轮定向复验 9/9：来源四分不坍缩且四值互异、可用性与可发布性两道闸互不推导（3 条当前可用者对外发布权限全为 `UNKNOWN`）、权限未知时 fail-closed、适用范围四键齐备、证据身份进入投影。全部从运行中的 M2 真实响应推导，容器绑定当场哈希实证 | 复验项 V-6 的命题是在看到 FAIL 之后改的，按 A2 该项记探索级，不充当「判据先于结果」的正式取证（见 `AC12_AC13_REVERIFY.json` 的 `criteria_provenance`） |
| M3-AC-13 | 对钉死绑定 `main@df2c595`：`成立`<br>对当前 `main@a7b8101`：`成立` | M2 在 `a7b8101` 零删除路由、新增两条读/权限路由；M3 写回候选校验不含 permission（设权限不是 M3 的域）。结构半对 v1.3 已发布图重跑：无 http_request 节点、无 tool 节点、无环境变量与会话变量，提示词散文外写入标记命中 0 | — |
| M3-AC-14 | `不得 PASS` | 行为 ECC `B14-1-brief-consumable` = `不足`：事实链无确认人且未显式写出该缺口，逐字落进该例 Oracle 的「不足」条款。同轮同载体的 `B15-DIR-02` 把这一栏写全了，因此不是判定者从严 | — |
| M3-AC-15 | 成立 | 行为 ECC `B15-DIR-01/02/03` + `CTA` + `N6` 五例全成功。第 6 轮的两处（`B15-DIR-01` 不足、`B15-CTA` 模型侧零产出）本轮均未复现 | — |
| M3-AC-16 | Workflow 图链路：`成立`<br>系统提示词全文入证据：`NOT_VERIFIED`<br>浏览器渲染画布：`NOT_VERIFIED` | 保真 ECC 七组全「成功」；判定者不采信自述，自算 `SKILL.md` sha256、九件绑定四项、注入参考文件逐字节；对 `G6` 用三条独立证据判本轮无「补齐替模型写正文」事故（最硬一条：SKILL 正文范例断言参考已加载，G6 清单是 `NOT_LOADED`，模型没照抄而写成「本轮没有附上」） | 两处证据缺口，都是执行侧未采集而非判定失败：①冻结件 §4 要求 transcript 含系统提示词全文，Dify 把提示词留在画布里，九件证据均无——判定者只有旁证，标 `NOT_VERIFIED`；②本机无浏览器自动化，画布渲染半仍未做，按 Prompt §12.3 不用运行历史顶替 |
| M3-AC-17 | `FAIL(INSUFFICIENT)` | 纵向 ECC 整体 `FAIL(INSUFFICIENT)`，触发步 `E06`：交付合格（1652 字符，闸门 hard_fail/missing/hollow 全空）却被 `check_stale_value_override` 整份拒收，被指控的句子无任何发布量主张。判定者独立追踪 12 步正文得出：**21 个跨步对象，进结构化通道 1 个，13 项无处置永久消失** | 根因在判据与闸门实现，不在模型交付——判定者逐条查过 12 份交付，无一处产品语义错误、证据身份混写、越界写创意或静默改写定位。三条缺陷已机械复核：G-3 的两扇后门（`E04` 新增位被补齐删除且计数器定义上看不见、`E12` 补齐节点代写 POS 行）与 G-4 的误报（全量普查 12 命中 11 误报，6 次真的拒收交付） |
| M3-AC-18 | `FAIL(INSUFFICIENT)` | A/B B vs A：七维六维「相当」、仅「产能取舍」B 优，五项硬门零实质退化，但不构成整体增益 ⇒ `M3_PROFESSIONAL_GAIN = NOT_VERIFIED`、`M3_MODULE_AB = NOT_PASSED`。四道预冻结闸全过（自身未被事后修改／判定写定早于映射读取／提示词唯一 1 种形态且逐份哈希一致／36 份齐备） | 本轮换协议、换载体、重新招募 36 名判定者后结论与第 6 轮一致——这条现在是被独立复现过的，不是一次运气 |
| M3-AC-19 | 部分成立 | Qwen `NOT_APPLICABLE`（Founder 指示，运行前判定）；本轮 40 名判定者（4 份 ECC 执行记录含行为的中断那次 + 36 名盲评者）机械核验**全部 CLEAN、零违规、零缺记录**；盲评有效性另由 `_leak_scan_v3.json` 扫描（0/12 命中）与 36 份自检（全部写「不知道」，无一份指认某一臂）双向支撑；留出集身份与探索/正式分轨成立 | 本轮尚未做独立收口审查（EP-09）。第 6 轮的收口 Reviewer 已完成并闭合两条阻断项，但那是对 v1.2 的审查，不覆盖 v1.3 |
| M3-AC-20 | 部分成立 | 远端已推送，本地 HEAD == 远端 HEAD（`cbb8d36`），可由 `evidence/ep10-closeout-v12/verify_remote_hash.sh` 零参数只读重验（本次实跑 EQUAL）；专用 Dify App／已发布版本／图哈希／模型参数／证据索引齐备；回滚演练第 6 轮实测通过；**实测包已按最终候选重新生成**（`M3_FOUNDER_DIFY_TEST_PACK_v1.1.md`，三格输入与输出逐字取自运行记录，已机械核对） | **Founder 产品接受未发生**。按冻结件本条应输出 `AWAITING_FOUNDER`，不得写 `DONE`。这是终态停在 `PARTIAL` 的四个原因之一，且是唯一一个不在执行侧手里的 |

**汇总**（按状态值计数，AC-12/13/16 各带多个绑定分列，AC 总数恒为 21）：
成立 15 · 成立（带限定）1 · 不得 `PASS` 2 · 未独立成立 1 · `NOT_VERIFIED` 2 ·
`FAIL(INSUFFICIENT)` 2 · 部分成立 2

## 9. 终态

```text
PARTIAL   ·   AC-20 输出 AWAITING_FOUNDER
```

逐条推导记在 `AC_STATE_v13.json` 的 `terminal_state`。`DONE` 被四件事挡着：
`AC-17`/`AC-18` `FAIL(INSUFFICIENT)`、`AC-09`/`AC-14` 不得 `PASS`、
Founder 产品接受未发生。不是 `BLOCKED`——每条未闭合项都有可执行的下一步。

**不声明**：生产就绪、真实经营提升、单账号纵向切片完成、任何 M5 结论。

## 10. 需要 Founder 决定的一件事

`D-1`/`D-2`/`D-3` 属于「已冻结 AC 下的技术缺陷」，按第 1 条该在本任务修。
但第 5 条写了「不得在看到正式结果后原地修改判据」与「再做一次完整正式重跑」（一次）。
两者在 `D-2` 上真的冲突：

- `D-1`/`D-3` 是**实现没做到它自己冻结的判据**，修它不改判据；
- `D-2` 相反——`§2.3` 把主语锚正则**逐字冻结**了，收窄它就是看到结果之后改判据，
  按 A2 会使本轮 `G-4` 相关取证降为探索级。

因此执行侧**不自行开第二轮修复重跑**，把决定交出来。三条路，代价各不相同：

```text
甲  修 D-1/D-3（不改判据），D-2 维持现状并在 AC-17 记为已知缺陷。
    代价：G-4 继续以 11/12 的误报率拒收合格交付，AC-17 大概率仍 FAIL。
    收益：不产生任何事后改判据，本轮全部取证的正式等级不变。

乙  三条全修，以 REBIND-005 版本化冻结新的 G-4 判据，然后重跑受影响取证。
    代价：一轮完整运行与判定成本；本轮 G-4 相关取证按 A2 降为探索级。
    收益：AC-17 有机会干净地挣回来。

丙  三条全修但只重跑纵向与行为，A/B 与保真不重跑。
    代价：需要一次 A3 影响面核算证明 A/B 与保真不在失效面内（执行侧认为成立，但要写出来）。
    收益：介于甲乙之间。
```

**不选＝按甲记账。** 执行侧默认按此。

## 11. 换会话怎么接上

```text
1. 读 collab-ledger/COLLAB_CONTINUITY_PROTOCOL.md（协作连续性规则正文）
2. 读本文件 §8 / §9 / §10
3. 状态真源：evidence/ep18-ac-recompute-v13/AC_STATE_v13.json
   核对两处呈现是否一致：python3 account-operations/tools/ac_state/render_ac_state.py --check
4. 远端一致性：bash account-operations/evidence/ep10-closeout-v12/verify_remote_hash.sh
5. 缺陷现场：evidence/ep19-gate-v13-defects/
6. 下一个可立即执行的动作：等 Founder 对 §10 三条路与实测包的裁定
```

```text
END_MARKER = M3-CHECKPOINT-ROUND-7-END
```
