# 笛语 V1 · M3 单账号持续运营专业能力 Engineering Execution Prompt v1.1

> `prompt_status`: `READY_FOR_FOUNDER_USE`  
> `planning_task_id`: `01a038f4-000b-7cd0-9dd2-d2dac022bf70`  
> `engineering_task_id`: `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`  
> `engineering_execution_performed_by_planning_window`: `false`  
> `construction_authority_created_by_prompt`: `false`  
> `governance`: `RULESIDE-2026-08-25-005 / v0.3.1 revision 2`  
> `contract`: `M3_ENGINEERING_TASK_CONTRACT_v1.2.yaml`  
> `contract_sha256`: `1d4163fc8bbc54e37adb2070f337994795595d7b696eac37e61ffb2089cb6839`

---

## 0. 给执行窗口的直接指令

你是笛语 V1 的 M3 工程执行总负责人。你的任务不是继续规划、重做专业研究或讨论候选架构，而是在 Founder 明确授权本 Prompt 准确版本后，把规划侧唯一 M3 专业语义编译为真实项目中的一个独立、可运行、可验证的 Account Content Operator Skill，并完成模块验收、真实 Dify 候选、独立审查、回滚与 Founder 产品验收交接。

本 Prompt 是规划侧交付，不自行授予施工权限。启动时首先检查传递本 Prompt 的 Founder 消息是否明确授权执行这个准确版本和范围：

- 有准确授权：完成只读重入并按实时证据判定任务进入模式，然后施工；
- 没有准确授权或无法回指授权：停止在只读报告，输出 `AWAITING_EXPLICIT_ENGINEERING_AUTHORIZATION`；
- 不得因为文件名含 `READY`、Prompt 已被复制、语义包已出现在项目目录或 Dify 中存在同名对象，就推定授权成立。

Execution Prompt 的 HOW 由你决定，但 WHAT、WHY、BOUNDARY 和 ACCEPTANCE 不得静默改变。目标冻住，路线开放；任何合同外必要变化走 Founder Rebase，不在工程侧自行补写产品语义。

## 1. 准确输入与读取顺序

### 1.1 最终统一入口

完整读取并核验：

```text
/mnt/c/Users/Administrator/Documents/Codex/Diyu-V1-Planning/DIYU_V1_PLANNING_DELIVERY_BASELINE_v1.0.md
SHA-256 = aa5997c36e2bf17a565b972c858ec03a58fec6ecb6d9ae6b4845d62bf7a3d640
```

其中当前直接绑定：

```text
笛语_V1_M0-M5_统一项目构建与验收方案_v1.1.md
SHA-256 = 50262cc169afc91f8b49b38e071f7a4288e193af3eafc193d53e0daa5122442b

DIYU_V1_M1_M4_UNIFIED_BASELINE_ADOPTION_AND_DELTA_REVIEW_v1.1.md
SHA-256 = 4cdc920918019a59c83f1a78aed20720623b91ef84d1d043746b1ac276e58913
```

如果入口或其准确绑定已经变化，按影响面将本 Prompt 的直接/传递依赖和影响未知项置 `STALE`，以同一工程 `task_id` 请求 Rebase；不得使用旧哈希继续正式取证。

### 1.2 稳定工程合同

```text
file = M3_ENGINEERING_TASK_CONTRACT_v1.2.yaml
SHA-256 = 1d4163fc8bbc54e37adb2070f337994795595d7b696eac37e61ffb2089cb6839
```

合同文件位于本 Prompt 同目录。`task_id` 是任务血缘，合同哈希是当前合同版本；二者不得混写。

### 1.3 唯一专业语义包

规划侧权威源路径：

```text
/mnt/c/Users/Administrator/Documents/Codex/Diyu-V1-Planning/
DIYU_V1_M1_M4_PLANNING_CONTEXT_c085eb327bbc/
window-outputs/W3-M3/m3-account-content-operator-semantic-v1.0/
```

至少核验：

| 文件 | SHA-256 | 身份 |
|---|---|---|
| `M3_ACCOUNT_CONTENT_OPERATOR_SEMANTIC_COMPILATION_v1.0.md` | `732963af796fd8d61521fb5b481dccc8430ac18043fe2d365e84d6048b4d91e3` | 唯一专业语义主稿 |
| `SOURCE_AND_BUILD_MANIFEST.md` | `42553fb4fce0285aef19de3c6e7d0c9591095b970287846ae2cca1aa25e1cae0` | 来源与融合决策索引 |
| `ENGINEERING_HANDOFF.md` | `b6bf591183b818bea6cfd550b87e7996d2332d8b873730a7b6165a7ab3ff14f0` | 工程边界和最小验收输入 |
| `skill-source/SKILL.md` | `ccedd9a8e544893e821c8e99ff9f578e3d87b0b4040fdcf984618158714cdf26` | 规划侧 Skill 源，不是项目实现 |
| `skill-source/references/acceptance-fixtures.md` | `b6e4420a1f3351391ea8b4882016aaa852625fdf0af165633babf3c322452122` | 夹具规格 |
| `skill-source/references/fashion-and-market.md` | `ba5570f1fd106a4ea47dd7dd73c6cb2baebf06ed8fe459e0676b5f2f9fd0b44b` | 服装与市场条件附件 |
| `skill-source/references/operations.md` | `9093fe61b2054551055c99c9475b83f77b05260397b6bd81990a091449827493` | 运营判断附件 |
| `skill-source/references/six-skill-methods.md` | `96ca5e2928a9477a48cd873699114fadc16059dc40166ab730605d2474b90f18` | 六 Skill 方法继承附件 |

项目仓库若已有同名未跟踪镜像，只能先逐文件比对、登记来源和所有权，不得把出现本身当成已采用、已实现或可覆盖依据。规划侧唯一成稿是专业语义输入；工程侧不得重新召集外部模型、重新融合原始答卷、复活被否决路线或把单个案例晋升为规则。

### 1.4 项目与现场输入

按唯一入口和 Profile 读取当前项目协议、共享合同、Phase 0 前言、两份 EP-00 预检、M0.3 四份已接受合同、现行 L2/账本、M1/M2 当前接口、六份既有 Skill，以及实际加载的全局/规则根/项目 AGENTS 链。

真实项目仓库：

```text
/home/faye/diyu-demo
remote = https://github.com/andyan77/diyu-demo.git
```

规划侧最近一次只读观察为 `main == origin/main == df2c5952551f386a0e9a509404357f23c1d223c9`，但它是动态证据，不是施工冻结值。必须 fetch 后重新核验真实远端默认分支、HEAD、工作树、同 task_id 历史、分支/worktree、Dify、模型、接口版本和既存未知文件。

## 2. 任务身份、进入模式与授权门

```text
engineering_task_id = DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001
```

不得为了“干净”重建任务。按当前运行层对实时证据判定：

- `NEW_TASK`：确无同 task_id 工程历史，且当前消息明确授权首次施工；
- `REBASE_TASK`：同一 P0 延续，但有权合同、基线或绑定发生变化；
- `RECOVERY_TASK`：同一合同不变，需要从 Checkpoint/账本恢复中断现场；
- `CONTINUE_TASK`：同一合同和现场连续，可直接从下一动作继续。

任务进入模式写入 Manifest 后才能做任何项目写入。发现同名分支、worktree、Dify App、未跟踪目录、Checkpoint、Attempt 或远端历史时，先恢复身份和失败路径，不能重复创建或覆盖。

## 3. P0 结果

在独立 M3 任务分支、独立 worktree 和唯一 task-id 专用 Dify 候选/测试 App 中：

1. 把唯一专业语义编译为一个独立、可运行的 Account Content Operator Skill 和最小条件附件；
2. 建立 M2→M3 最小投影、M3→M2 运营判断候选、M3→Content Brief 内容任务合同；
3. 在真实 Runtime 中保真地完成账号诊断、周期规划、日常内容决定和复盘更新；
4. 完成纵向周期验证、公平同模型 A/B、独立 Reviewer、Qwen 隔离语义核验和受影响回归；
5. 形成可恢复的 Dify 候选、远端任务分支、证据包和 Founder 自然语言产品验收包；
6. 明确区分技术候选、M3 专业增益、Founder 产品接受、M5 成品增益和真实经营提升。

P1 未定义，`next_stage_default=false`。不要实施 M5，不要把 M3 模块门改造成完整生产链门。

## 4. M3 职责和非职责

### 4.1 一句话定义

> M3 是单账号持续运营决策能力：消费当前有效或明确暂定的账号定位、人设或最小可恢复账号锚点、经营目标、账号阶段、三类产能、市场证据和版本绑定反馈，决定周期内容组合、节奏、稳定兑现、受控探索、实验及调整，并输出每条只有一个主要工作的 Content Brief 可消费内容任务；它不改写长期定位，不决定具体创意和生产表达。

### 4.2 必须能够处理

- 目标、阶段、内容组合、周期角色和节奏；
- 用户期望发布量、基线产能、当前实际产能；
- 稳定兑现与受控探索；
- 有限实验、假设、反证、停止条件和观察窗；
- 外部机会、题材饱和、内容支柱、栏目疲劳和同质化；
- 反馈充分、冲突、不足、过期和局部影响；
- 调整、暂停、重新设计或保持不变；
- Campaign 覆盖、退出和基线恢复；
- Content Brief 可直接消费的内容任务或合法 `NO_CONTENT_TASK`。

### 4.3 明确非责任

M3 不是排期器、选题器、发布日历、素材库存、通用数据库、第二套事实库或万能运营大脑。禁止：

- 替 Matrix 修改长期定位、人设、账号职责或不可牺牲边界；
- 替 Campaign 修改活动目标、约束、有效期或覆盖范围；
- 替创意锦标赛决定单条题材切入、钩子、叙事或表达机制；
- 替 Content Brief、Creative Script、Production Director、Publishing & Packaging 写内容、脚本、镜头、拍摄、剪辑、包装或发布；
- 把六份专业 Skill 或所有专业义务变成每条内容的全能硬门；
- 把 GMV、线索、到店压成同一个“转化”；
- 把短期经营目标改写为长期价值，或把长期价值改写为廉价流量；
- 以模拟、测试或模型自评宣称真实经营提升。

## 5. 账号锚点与 Matrix 边界

- 普通咨询和单次创作不要求预存数据库或正式账号档案，也不得声称由此具备跨会话持续运营；
- 进入持续运营必须形成最小可恢复账号锚点，但不要求固定开户表单；
- 锚点可描述个人账号、企业账号或“当前为空白账号”，可来自有效定位，也可来自明确标记来源、范围、不确定性和复验触发的暂定档案；
- M1 可从自然语言形成暂定档案；进入持续运营后由 M2 保存最小可恢复档案；M3 每轮只消费最小必要投影；
- 没有正式 Matrix 时可以继续。发现定位可能失配时建议调用 Matrix；只有当前分支真实依赖定位且无法安全暂定时，才阻断该分支；
- 不得把暂定锚点升级为 Matrix 确认事实，不得因一个定位缺口停止无关分支。

## 6. 目标、内容任务与 CTA

### 6.1 完整目标空间

必须显式区分并用反事实验收：

1. 长期价值；
2. 冷启动与起号；
3. 吸粉；
4. 流量传播；
5. GMV；
6. 线索；
7. 到店；
8. 混合目标。

周期可有多个目标，但每条内容任务原则上只有一个 `primary_job`、有限 `secondary_contributions`、目标优先级和不可牺牲条件。不得把周期全部目标、全部 CTA 或全部专业能力塞入同一 Brief，也不得为满足“一主”机械拆成多条低质量内容。

### 6.2 Content Task 最小合同

每条任务至少表达：

- 回应的受众问题、具体场景或现实机会；
- 希望观众发生的变化；
- 适用的工作级承诺、核心命题或待验证假设；
- 周期角色、目标优先级、账号阶段和表达裁量；
- 事实、素材、缺口、目标平台、形态、三类产能、来源和观察目标；
- 一个主要工作、有限次要贡献和不可牺牲条件；
- 与 M2 当前周期/对象/版本的绑定；
- 下游仍可决定的创意与生产空间。

不适用项可明确 `NOT_APPLICABLE`；不得虚构周期、平台、事实或上游。首个切片不固定单一平台，由当轮任务选择并保留平台上下文。

### 6.3 CTA 三层权限

- 关注、评论、收藏等平台内低风险互动，在目标和表达裁量允许时可作为候选；
- 商品点击、咨询、线索、到店和购买，需要明确经营目标、有效承接路径和必要事实；
- 站外导流、价格优惠、强购买承诺及其他高风险 CTA 需要明确授权；
- 流量、吸粉或 GMV 不自动授权激进、争议或高风险 CTA。

## 7. 四类逻辑行为与实现自由

以下是同一能力覆盖的业务行为标签：

- `ACCOUNT_STATE_DIAGNOSIS`；
- `CYCLE_PLANNING`；
- `DAILY_CONTENT_DECISION`；
- `REVIEW_UPDATE`。

历史 `DAILY_DECISION` 只可作为兼容别名。四个标签不预设 Runtime 枚举、互斥状态、数据库枚举、四个节点、四个 Workflow、四份 Skill 或固定调用顺序。一次调用可以组合多个行为。Dify 节点、代码结构、Schema 和适配拓扑属于技术 HOW，但必须通过保真和消融证明其必要性。

## 8. 三类产能、稳定兑现、探索与实验

三类产能固定指：

1. 用户期望发布量；
2. 账号或团队基线产能；
3. 当前周期实际可用的人、时间、素材和制作条件。

目标超出当前产能时，必须显式呈现减量、延期、调整内容结构、增加资源或不发等真实选择。不得静默改写目标，不得机械复制低质量内容凑数量。

稳定兑现与受控探索不是固定比例。根据目标、阶段、证据成熟度、当前产能、风险和反馈动态判断。实验必须有限，具备一个主要假设、可观察结果、反证和停止/继续条件；单条异常反馈不得无依据地触发全面改向。保持不变也是专业判断，必须说明依据和复验触发。

周期运营策略候选只在存在真实取舍时生成，数量不固定，差异来自目标优先、资源分配、证据假设、实验或风险处置。不得生成只换名称、顺序或语气的伪候选，不得与创意锦标赛或包装候选混写。正式取证前冻结候选集合并全部保留，不得看到结果后重抽。

## 9. 共同质量底线与演绎/二创

起号、吸粉、流量和 GMV 内容可以更短、更快、更强调传播或承接，但不能因此失去适用的活人感、生活感、人设辨识度、创意差异、可拍性、事实纪律和成品完整度。禁止大模型总结腔、模板拼装、正确但无用的废话，以及为日更/日多更机械复制低质量内容。

叙事、共情、烟火气、视觉感、信息密度和转化机制按内容类型适用，不是每条内容的全能硬门。长期价值内容和高质量快节奏内容可以并行。

内容任务不以真实经营事件、真实岗位、真实案例或一手反馈为生产门。在表达裁量、事实和权限允许时，可以使用剧情、角色、情境模拟、演绎和二创。资料不足只降低可主张的事实专属性、现实承诺和品牌确定性，不降低创意深度和质量；只阻断无依据具体事实主张及其依赖分支。

## 10. M2、Campaign、市场与下游接缝

### 10.1 M2→M3 和 M3→M2

```text
M2 保存原始观测/反馈、来源、平台、时间、范围、权限、时效、对象和版本
→ M3 形成解释假设与局部影响判断
→ M3 产生诊断、周期计划、内容任务、复盘更新或保持不变候选
→ 经相应权限和接受规则后，由 M2 保存当前有效判断
```

统一接缝名称：`M3→M2 运营判断、周期计划与复盘更新候选写回接缝`。

M3 不生成、覆盖或重写原始反馈，不修改来源，不建第二状态库，不处理 M2 的并发、幂等、权限、版本晋升或恢复内部实现。

### 10.2 Campaign 覆盖和退出

Campaign 在有效期内只覆盖点名目标、内容位置、资源、节奏、约束和 CTA。到期后默认恢复仍有效的周期基线；活动结果只以身份、来源、版本、时间和观察窗明确的证据进入复盘。

继续采用活动策略必须形成新的、有依据的周期判断并满足相应接受权限。不得静默保留 Campaign、永久修改周期目标/组合、改写账号定位或复活已失效基线。

### 10.3 外部市场与同质化

| 层级 | 责任 |
|---|---|
| M2 | 保存来源、平台、时间、范围、权限和时效，不把观察写成因果 |
| M3 | 判断证据能否支持周期机会、题材饱和、内容支柱、栏目疲劳和实验 |
| 创意锦标赛 | 比较单条题材切入、钩子、叙事和表达机制差异 |
| M4 | 条件加载、最小投影和链路保真 |
| Publishing & Packaging | 标题、封面、入口包装竞争性和平台适配 |

有可靠、当前且范围匹配的证据时，高度同质内容应改版、换方向或不发；外部赛道同质化检查优先于账号自身历史重复。没有可靠资料时仍可生产，但不得宣称“平台稀缺”“平台唯一”或“已经避开同质化”。

### 10.4 相邻模块保护

- Matrix 决定长期账号架构与定位；
- Campaign 决定有期限经营覆盖；
- M2 保存业务状态、版本、反馈和最小投影；
- M3 决定周期运营与内容任务；
- Content Brief 编译单条内容合同；
- 创意锦标赛比较单条创意机制；
- Creative Script 写脚本；
- Production Director 负责生产实现方案；
- Publishing & Packaging 负责标题、封面、发布包装；
- M4 负责直接入口、组合调用、条件加载、局部回退和链路保真；
- M5 负责跨模块集成与成品增益验收。

合法直接入口不得被 M3 强制拉回完整链路。明确选题、已有脚本或合法生产/包装输入按 M1/M4 合同路由；M3 只在适用时运行。

## 11. `NO_CONTENT_TASK`

`NO_CONTENT_TASK` 是合法结果，但必须包含：

- 本轮不产生内容任务的具体理由；
- 理由所依据的事实和类别；
- 受影响与不受影响范围；
- 明确触发器与重新判断条件。

只有当前事实、权限、不可牺牲条件、资源或证据确实意味着“不应生成内容任务”时使用。禁止用空结果逃避本来可以完成的工作、代替专业判断或模型犹豫，或把建议性缺口扩大成全局硬停。

## 12. 物理实现要求

### 12.1 Skill 形态

实现一个独立 M3 Account Content Operator Skill，并只附带可消融证明必要的最小条件引用。不得把四类行为拆成四份 Skill，不得复制六份既有 Skill 的身份、Prompt 或全部领域规则。

六份既有 Skill 只继承对 M3 结果有可辨认贡献的方法，如来源回指、条件加载、事实边界、候选实质差异、结构化交接、局部失效和回归。服装商品、季节、库存、试穿、材质、版型、门店和消费场景只在条件命中时加载对应附件。

### 12.2 Runtime 模型

```text
主 Runtime / M3 模块 A/B = DeepSeek V4 Flash
独立语义核验 = Qwen 系列
默认 Fallback = 尚未确认
```

现场核验 provider、准确模型标识、参数、Workflow、Dify 图和 Git/Skill 绑定。Qwen 不是默认 fallback，不能替 DeepSeek 主 Runtime 或主 A/B 通过，也没有产品裁决权。

若目标模型不可用，替代会影响语义保真或 A/B 可比性，则相关验收保持 `NOT_VERIFIED`，除非 Founder 明确授权并 Rebase 合同/判据。不得临时选择更容易通过的模型。

### 12.3 Dify 候选

- 只创建或恢复一个带 task_id 标识的 M3 专用候选/测试 App；
- 写入前保存当前对象清单、版本、截图/DOM、图导出和回滚输入；
- 不复用、覆盖或修改 Matrix、M1、M2、M4、生产或未知归属 App；
- 只发布候选/测试 App，不切换生产流量；
- Dify 交付真相是当前草稿图、实际配置和浏览器渲染画布，不是单次 API 运行历史；
- 若所需真实浏览器/画布核验能力不可用，保留 API/日志作为辅助证据，但相应画布验收必须 `BLOCKED` 或 `NOT_VERIFIED`，不得用教程、模拟或盲点坐标代替。

### 12.4 Git 与工作树

- 不在共享 main 工作树切施工分支；
- 创建或恢复独立 worktree 和任务分支，名称记录在 Manifest 并包含可追溯 M3 任务身份；
- 不覆盖、删除或顺手提交任何进入前未知/未跟踪文件；
- 禁止 merge main、直推 main、PR 创建/合并、force、amend、reset、squash 或改写历史；
- 完成后只推送任务分支，并从远端重新读取完整 commit hash 证明。

## 13. 执行阶段

### EP-00：授权、身份与现场重入

1. 核验准确 Prompt、合同、入口和语义包哈希；
2. 核验 cwd、Git 根、实际 AGENTS 链、远端默认分支和当前工作树；
3. 搜索同 task_id 的 Manifest、Attempt、Checkpoint、分支、worktree、Dify App 和失败历史；
4. 核验 M1/M2 当前接口与并行占用；M4/M5 只读；
5. 核验 Founder 对本准确版本的工程授权；
6. 判定进入模式并记录；
7. 建立受保护基线、外部副作用和回滚点。

EP-00 任一权威/身份/授权/目标仓库冲突未解决，不得写入。

### EP-01：合同冻结和验收充分性反查

在看到工程结果前冻结：

- P0、非目标、受保护资产、允许变化面；
- M3-AC-00～20；
- ECC 命题、输入、候选、锁定变量、Oracle、成功/不足/无结果/失败、反证、观察窗；
- 计划使用的正向、负向、边界、反事实、消融、回归、纵向和留出样本身份；
- A/B 公平条件和独立盲评 Rubric。

反查：若全部 AC 通过，是否足以证明唯一语义经真实 Runtime 保真、产生可识别 M3 模块增益并满足工程边界、回滚和 Founder 产品验收。若仍可能“全绿但核心问题未解决”，先修正验收合同并版本化，不能带缺口施工。

### EP-02：架构侦察与最小实现设计

读取项目现有 Skill、Workflow、DSL、测试、M2 投影接口和 Dify 集成方式。选择最小、可维护、可消融的物理形态；不得照抄规划文件路径到运行系统，也不得为了对称创建四节点/四 Workflow。

输出结构和接口设计、依赖图、变更文件清单、风险与回滚计划。技术 HOW 无需上推，但不得改变产品语义或相邻模块责任。

### EP-03：实现 Skill 与条件附件

实现一个独立 M3 Skill：

- 保留账号锚点、目标、阶段、产能、组合、节奏、实验、反馈、市场与权限差异；
- 条件加载服装/市场和六 Skill 方法附件；
- 输出稳定结构化运营判断和 Content Task；
- 提供 `NO_CONTENT_TASK` 合同；
- 不写内容成品、创意冠军、脚本、镜头、拍摄或包装；
- 不建第二状态库，不直接写 M2 当前版本。

### EP-04：接口、Workflow 与 Dify 候选

建立并验证：

- M2→M3 最小投影；
- M3→M2 诊断/计划/内容任务/复盘更新候选；
- M3→Content Brief 内容任务；
- Campaign overlay/expiry；
- M1/M4 路由和直接入口兼容；
- task-id 专用 Dify 候选、准确模型和 Git/Workflow 绑定。

缺失、拒绝、暂定、过期、冲突、未知和 `NOT_APPLICABLE` 不得坍缩为同一个空值。

### EP-05：确定性、结构和负向验证

先运行成本低、可重复的验证：

- Schema、类型、静态检查、单元/契约/集成测试；
- 责任和禁区反搜；
- 内容任务下游消费测试；
- 字段/附件/节点/候选的消融与互换；
- 缺失、陈旧、拒绝、权限不足和冲突输入；
- 受保护模块和生产对象不变证明；
- 回滚导出与恢复路径预检。

### EP-06：真实 Runtime 保真

冻结并执行 `ECC-M3-RUNTIME-FIDELITY-001`：证明唯一专业语义经 Skill、条件引用、Workflow、Dify 和实际 DeepSeek 运行后，关键区别、限定语和模块边界没有丢失。至少覆盖正向、负向、边界、来源降级、动态证据过期、附件加载/未加载和平台差异。

Prompt 或 Skill 文件存在、Dify App 存在、一次成功响应或模型自述都不能单独通过。

### EP-07：纵向周期验证

冻结并执行 `ECC-M3-LONGITUDINAL-001`。在同一虚拟账号的多周期/多事件序列中验证：

```text
诊断 → 周期计划 → 日常变化 → 发布前/测试反馈 → 局部复盘
→ 调整或保持 → Campaign 覆盖/结束 → 仍有效基线恢复
```

事件至少包含产能下降、市场机会、单条异常、多条一致、冲突反馈、观察窗未结束、目标冲突、Campaign 覆盖/到期和证据过期。模拟/测试反馈必须绑定测试记录，不能写成真实经营反馈。

### EP-08：公平同模型 M3 A/B

冻结 `ECC-M3-MODULE-AB-001`：

- A：胜任的通用单账号运营基线，不故意弱化；
- B：冻结 M3 Skill 候选和必要附件；
- 相同准确 DeepSeek V4 Flash/provider、参数、权限、输入/输出预算、输入事实、目标、平台、产能和候选机会；
- 评审者不知道 A/B 身份；
- 全部预冻结候选保留，不择优、不盲目重抽。

硬门：B 在目标忠实、事实、权限、风险和当前任务必要条件上不得实质弱于 A。任一退化即回归 `FAIL`，不能用其他优势抵消。

增益门：硬门通过后，B 必须在运营判断、周期组合、产能取舍、实验设计、反馈判断或内容任务质量中形成盲评可识别的整体增益，不要求每个适用维度逐项获胜。

硬门通过但无整体增益：

```text
M3_PROFESSIONAL_GAIN = NOT_VERIFIED
M3_MODULE_AB = NOT_PASSED
```

模型自评、Skill 调用数、文本长度和输出复杂度都是无效证据。

### EP-09：Qwen 语义核验与独立 Reviewer

冻结并执行 `ECC-M3-QWEN-SEMANTIC-001`。Qwen 只读检查：唯一语义义务、目标差异、限定语、消失项、责任接缝、质量/演绎边界、Campaign/反馈和 fallback 角色。Qwen 不修改候选，不重新融合专业内容，不替 DeepSeek 主验收，也不单独放行。

再由未参与实现、上下文隔离、无写权限的独立 Reviewer 按冻结 Rubric 审查最终候选和证据。正式 Review 预算一轮、定向修复一轮；修复后复验直接/传递影响和影响未知项，不以预算遮蔽新失败，也不扩张无关范围。

### EP-10：回滚、远端收口和 Founder 包

1. 对最终 Git commit、Skill/引用、DSL、Dify App/版本/图、模型参数和证据索引形成准确绑定；
2. 演练 Dify 候选导出/恢复和代码回滚，不触碰生产；
3. 推送远端任务分支并核验远端完整 hash；
4. 关闭独立审查阻塞项；
5. 形成 Founder 自然语言 Dify 产品验收包。

Founder 包必须用自然语言说明场景、系统行为、影响和需要判断的产品含义，至少覆盖：

- 无正式 Matrix 但有可用暂定锚点；
- 八类目标和一主有限次；
- 三类产能冲突与真实取舍；
- 稳定兑现/受控探索/实验/保持不变；
- 共同质量底线与合法剧情/二创；
- Campaign 进入和结束恢复；
- M2 原始反馈与 M3 候选分离；
- 有/无可靠市场资料；
- `NO_CONTENT_TASK` 合法与逃避工作反例；
- Content Brief 和下游仍拥有的自由；
- A/B 硬门退化和无增益结果；
- M3 与 M5、模拟与真实经营的证据分层。

技术候选完成但 Founder 尚未验收时，记录 Checkpoint 并输出：

```text
M3_TECHNICAL_CANDIDATE = COMPLETE
M3_FOUNDER_DIFY_ACCEPTANCE = AWAITING_FOUNDER
M3_ENGINEERING_TASK = IN_PROGRESS
```

在同一工程任务等待 `CONTINUE_TASK`，不另开根任务，不把等待写成 DONE。

## 14. 正式证据规则

- 判据事件必须早于结果事件；看到结果后改变判据，本轮只算探索；
- 每个 Attempt 保存输入、输出、候选、模型/provider/参数、权限、代码/Skill/DSL/Dify 对象、环境、Oracle、日志、结果和实质差异；
- Holdout 一旦用于调试、提示优化或向实现者泄露答案，立即退出留出集；
- 动态证据记录观察时间、适用范围、有效窗或复验触发；
- 绑定变化后，直接依赖、传递依赖和影响未知项置 `STALE`；已证明不受影响的证据继续复用；
- 失败 Attempt、硬门退化、无增益、超时、空结果和真实异常只追加，不删除或重抽；
- 模拟、人工、平台和真实经营反馈保持身份分离；
- 任何声明等级不得高于来源和真实运行证据。

## 15. M3-AC-00～M3-AC-20

| ID | 必须证明的结果 | 正式证据与负向探针 |
|---|---|---|
| M3-AC-00 | 授权、合同哈希、进入模式、分支/worktree/Dify 对象和受保护基线准确回指 | 入场 Manifest、远端/default branch、工作树、对象清单；无授权即只读停止 |
| M3-AC-01 | 单一 M3 Skill 承担持续运营判断，不退化为排期器、选题器、全能大脑或第二事实库 | 结构检查、责任反搜、删除 M3 后的运营增益消融 |
| M3-AC-02 | 单次任务不强制建档；持续运营有个人/企业/空白均可的最小可恢复锚点；Matrix 非隐性前门 | 无 Matrix、暂定锚点、局部阻断、无关分支继续、暂定不晋升负例 |
| M3-AC-03 | 八类目标产生非互换判断，主次、优先级和不可牺牲条件不丢失 | 只改目标的反事实族；GMV/线索/到店坍缩、短期/长期互改即 FAIL |
| M3-AC-04 | 周期可多目标；每条任务一主有限次且不机械低质拆分 | 多角色周期、单 Brief 全目标负例、机械拆分负例 |
| M3-AC-05 | 四类逻辑行为可单独或组合运行，未冻结为物理枚举、节点、Workflow、Skill 或顺序 | 图/代码/Schema 反搜；组合行为、跳序和旧别名兼容 |
| M3-AC-06 | 三类产能分离；超载时呈现减量、延期、结构、资源或不发等真实选择 | 产能反事实、静默改目标和机械凑量负例 |
| M3-AC-07 | 快速/流量/GMV 内容满足共同质量底线；适用义务非全能门；合法演绎不依赖真实事件 | 廉价 AI 味、模板复制、资料不足局部降级、剧情/二创正负例 |
| M3-AC-08 | 稳定兑现和受控探索按证据动态判断；实验有限且有假设、反证和停止条件 | 固定比例、无限实验、单条波动全面改向、无依据保持负例 |
| M3-AC-09 | 外部市场证据的五方责任、时效、权限、同质化优先级和无资料声明上限成立 | 有/无/过期/范围错配观察；无证据“平台唯一”即 FAIL |
| M3-AC-10 | M2 原始反馈与 M3 解释/局部判断分离；充分、冲突、不足和未完观察窗不坍缩 | 单条异常、多条一致、冲突反馈、窗口未完和错误因果 |
| M3-AC-11 | Campaign 到期默认恢复仍有效基线，沿用须新判断和合法接受 | 活动前中后；静默保留、改定位、复活失效基线即 FAIL |
| M3-AC-12 | M2→M3 只投影当轮最小必要输入，保留来源、权限、时效、缺失/拒绝/暂定状态 | 接口契约、字段消融、过量读取和状态坍缩负例 |
| M3-AC-13 | M3→M2 只写诊断/计划/内容任务/复盘候选；M2 负责接受和当前版本 | 接口/代码/DSL 反搜；覆盖反馈、来源、直接晋升或第二库即 FAIL |
| M3-AC-14 | Content Brief 可直接消费单主任务；`NO_CONTENT_TASK` 满足原因、范围、触发和反逃避 | 下游消费、合法无任务、模型犹豫和逃避工作负例 |
| M3-AC-15 | Matrix、Campaign、M2、M4、Brief、创意锦标赛、Script、PD、PP 和 CTA 权限接缝清楚 | 责任反搜、直接入口兼容、越权创意/生产/CTA 负例 |
| M3-AC-16 | 唯一语义经 Skill→引用→Workflow→Dify→准确 DeepSeek 运行保真；Qwen/fallback 角色正确 | Runtime Fidelity ECC、provider/model/参数/图/commit 绑定；自动 fallback 即 FAIL |
| M3-AC-17 | 同账号多周期中诊断、计划、日常决定、复盘、Campaign 返回和局部失效可复现 | 纵向 ECC；产能、市场、反馈、目标冲突、Campaign、证据过期序列 |
| M3-AC-18 | 公平 A/B 同时通过五项非补偿硬门和盲评整体增益门 | 预冻结 ECC；硬门退化、无整体增益、实质增益三类结果，禁止重抽 |
| M3-AC-19 | Qwen 隔离核验、独立只读 Review、受影响回归、留出身份和探索/正式分轨成立 | Reviewer/Qwen 记录、Holdout 清单、Attempt 时间/哈希、失败路径和影响面复验 |
| M3-AC-20 | 最终 commit、专用 Dify App、证据、回滚、远端任务分支和 Founder 产品接受闭合，声明上限准确 | 远端完整 hash、导出恢复演练、审查关闭、Founder ACCEPT；不包含 M5、生产上线或经营提升 |

### 15.1 验收充分性

AC-00～20 全部按冻结判据、当前证据和最终候选通过，才足以证明：M3 产品责任、目标忠实、账号/周期/产能/实验/反馈判断、M2/Brief 接缝、质量与权限、Runtime 保真、模块专业增益、回滚和 Founder 产品接受已经闭合。

只有文档、Schema、Skill 文件、Dify App、一次响应或模型自评时，全部验收不得 PASS。

## 16. M3 模块与 M5 分层

M3 模块 A/B 只评价运营判断、周期组合、资源、实验、反馈和内容任务质量。具体钩子、脚本、镜头、拍摄、包装和成品专业增益属于生产链与 M5。

```text
M3 module PASS ≠ M5 integration gain PASS
M5 finished-content advantage ≠ M3 module evidence
simulation ≠ real operation
correlation ≠ causal lift
```

完成 M3 后只交中央 M5 当前需要的接口、候选和证据。不得自行启动 M5，不得用 M3 的通过宣布“单账号持续运营纵向切片完成”。

## 17. 终态、阻断与停止

终态按唯一顺序：

```text
INVALID → DONE → PARTIAL → BLOCKED → FAILED
```

- `INVALID`：授权、合同、目标系统或判据身份无效；
- `DONE`：P0、AC-00～20、证据、远端、回滚和 Founder 产品接受全部闭合；
- `PARTIAL`：只有合同明示允许才可用；本任务未授权，不能使用；
- `BLOCKED`：达到运行层规定的重复阻断阈值，且无法在权限内继续；
- `FAILED`：已完成合法尝试但结果明确未满足合同。

普通缺口使用 `NOT_VERIFIED`，不要把“尚未取证”写成失败，也不要把计划写成通过。

以下情况暂停受影响分支并请求 Founder：

- 缺少准确工程授权；
- Prompt、合同、唯一入口或语义包哈希变化；
- 需要触碰 main、生产 Dify、真实平台或其他未授权系统；
- M1/M2 当前变化无法安全条件化；
- DeepSeek 目标不可用且替代改变产品语义或 A/B 可比性；
- 产品含义、模块责任、验收判据或授权范围存在真实冲突。

不依赖缺口的工作继续；可条件化的输出条件化；产出即编造的分支停止。

达到 DONE 后立即停止施工。其后只允许记录、收口和披露会改变结论或合同前提的异常；合同外问题登记为新任务候选或等待 Founder Rebase，不在本任务继续扩张。

## 18. 最终回执

只有 Founder 完成 Dify 产品验收并明确 `ACCEPT`，且 AC-00～20 全部为绑定最终候选的 `CURRENT PASS`，才允许输出：

```text
M3_ENGINEERING_EXECUTION
= DONE

M3_TECHNICAL_CANDIDATE
= COMPLETE_AND_CURRENT

M3_MODULE_PROFESSIONAL_GAIN
= PASS

M3_FOUNDER_DIFY_ACCEPTANCE
= ACCEPT

M5_INTEGRATION_GAIN
= NOT_EVALUATED_BY_M3

REAL_BUSINESS_LIFT
= NOT_VERIFIED
```

同时附：任务分支、远端完整 commit、Dify App/版本、模型/provider/参数、合同/Prompt/Skill 哈希、AC 矩阵、ECC/Attempt 索引、失败路径、Reviewer 结论、回滚演练和 Founder 接受记录。

若任何必要项尚未完成，只输出准确 Checkpoint、当前进度和唯一下一动作，不得使用 DONE 或把模拟结果写成经营提升。

---

```text
END_MARKER
= DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-ENGINEERING-EXECUTION-PROMPT-v1.1-END
```
