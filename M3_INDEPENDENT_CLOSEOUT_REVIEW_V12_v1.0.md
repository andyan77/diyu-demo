# M3 独立收口审查（EP-09 / AC-19）· 载体 v1.2 轮

> **Reviewer 身份声明**：未参与本任务任何实现，未撰写任何冻结件、Skill、闸门、投影或判定文件。
> 本轮我被明确授权通读全部材料（含 Checkpoint、验收判据、REBIND 件、判定件、证据目录、git 历史），
> 因为本角色的工作正是找声明与证据的落差，**因此不存在"被禁读且确实未读"的文件清单**。
> 我只写本文件，未修改仓库任何其他文件（`git status` 在我开工前后均为干净）。
>
> 判据：`M3_INDEPENDENT_REVIEWER_RUBRIC_FROZEN_v1.0.md`，冻结于本任务全部工程结果之前。
> 我**未放宽、未加严、未新增**任何维度。唯一问题：**有没有任何一条声明的等级高于它的证据。**

---

## 汇总

| 项 | 结论 | 一句话 |
|---|---|---|
| R-1 · 判据先于结果 | **`FAIL`** | 四份核心 ECC 冻结件时序全部干净，但 `MODULE_AB ADDENDUM_002` 在**判定完成、揭盲之后**被原地改写、未换版本号，且它自陈的时序合法性对其现有内容已经不成立 |
| R-2 · 声明等级不越级 | **`FAIL`** | Checkpoint 6 §8 那张矩阵逐条经得起核；但 `M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md` §7.2 至今仍写着 `AC-12`/`AC-13` = `PASS` 且"转 `CURRENT`"，与本交付自己第 6 轮的 `STALE` 直接冲突 |
| R-3 · 失效传播不多算不少算 | `PASS` | 我自己把 73 次运行重放了一遍，逐项比对 13 个决定量，**独立复现出完全相同的结果**：72 不受影响、1 受影响（`E07`）。无多算、无少算 |
| R-4 · 边界与受保护资产 | `PASS` | 相对入场基线 543 个文件**全部为新增**，零修改零删除；三个受保护目录零改动；六份既有 Skill 逐字节未动；一个 App；无 merge/force/amend/reset/squash |
| R-5 · 失败路径只追加不删除 | `PASS` | 全部失败轮次逐条保留；**「重跑前看过 E01」不构成择优**——三份 E01 输入逐字节相同、闸门判定十项全同，且被丢弃的那份反而最长，择优所需的梯度不存在 |
| R-6 · 声明上限 | `PASS` | 11 条措辞全文反搜：本轮各文件中要么零出现，要么全部是否定性用法 |
| R-7 · 留出与探索/正式分轨 | `PASS` | 「36 份提示词逐字同构」「判定者隔离」「432 条理由零禁用证据」三条我全部独立复算并**证实为真**；A/B 推导我从原始判定重算，逐格复现 |

**阻断项 2 条，非阻断观察 6 条。两条阻断项都是文档同步问题，不需要重跑任何一次运行、不需要重跑任何一次判定。**

---

## 逐项

### R-1 · 判据先于结果（A2 §时序）—— `FAIL`

**查了什么**
`git log --follow` 每一份 `M3_ECC_*_FROZEN_*.md`；每个 evidence 目录的首次落盘 commit 与时间；
`git show` 每一次对冻结件的修改；`_CASES_PROVENANCE.md` 全部机械声明的逐条复算；
36 份盲评判定文件的 mtime 与揭盲脚本读取时刻。

**发现**

*一、四份核心 ECC 冻结件时序干净，逐条可核：*

| 冻结件 | 提交次数 | 冻结时刻 | 其判定的运行落盘时刻 |
|---|---|---|---|
| `RUNTIME_FIDELITY_001_FROZEN_v1.0` | **1** | `c64d762` 08:19:37 | `ep06-runtime-fidelity` 08:37:31 |
| `RUNTIME_BEHAVIOR_002_FROZEN_v1.0` | **1** | `4bcaaa0` 10:23:58 | `ep06b-runtime-behavior` 10:52:14 |
| `LONGITUDINAL_001_FROZEN_v1.0` | **1** | `4bcaaa0` 10:23:58 | `ep07-longitudinal` 10:47:44 |
| `MODULE_AB_001_FROZEN_v1.0` | **1** | `7564896` 10:33:32 | `ep08-module-ab` 10:52:14 |
| `REBIND_002_FROZEN_v1.0` | **1** | `b835709` 12:22:08 | v11 证据 14:13:39 |
| `REBIND_003_FROZEN_v1.0` | **1** | `6bbad98` 16:29:46 | v12 运行 16:34 起 |

四份核心冻结件**各只有 1 次提交，结果产出后一字未改**。执行侧在 `edcccb2` 提交信息里做过同样的自证，属实。

*二、纵向冻结件 `v1.0 → v1.1` 的合法化：**成立**，但附一条我无法核验的依赖。*

我按 R-1 的 `FAIL` 条件逐项对：

- **新版本号**：有。`v1.1` 是**新文件**，`v1.0` 原文逐字节保留在仓库里（我 `diff` 过两份原文）。
- **触发事件说明**：有，而且写在 §0.1 第一句，不藏：「**这个改动是看到 E07 之后才做的**」。
- **不追溯改判**：§0.4 明写第 4 轮 `AC-17 = FAIL(INSUFFICIENT)` 保持原判、原始记录不删不改。我核过 `ep07-longitudinal-v11-run2/` 仍在。
- **有没有救回什么**：**没有**。改了链接规则之后重跑，`AC-17` 仍然是 `FAIL(INSUFFICIENT)`。放宽方向上没有任何一条声明因此上行。

**因此这条合法化在 R-1 的判据下成立**，我不因为"它自己承认了"就放过，而是因为四项逐条对得上、且它没救回任何东西。

**但要单列一条**：§0.2 引的权威事件（Founder 2026-08-26 `CONTINUE_TASK` 第 4 条）**在仓库内没有任何独立佐证**，
它只以执行侧转述的形式存在。我无权也无法核验 Founder 说过什么。
本条不影响结论（因为没有任何声明踩着它上行），但需要 Founder 本人确认——见非阻断观察 O-1。

*三、`_CASES_PROVENANCE.md`：**我逐条复算，全部属实**。*

| 它的声明 | 我的复算 | 结论 |
|---|---|---|
| 原始 `_cases.json` = `1da81ea6…`，逐字节未动 | `sha256sum` 实测一致；`git log` 显示该文件**只有 1 次提交**（`4bcaaa0`） | ✅ |
| v11 那份 = `ab355b88…`，是**新建**不是原地改 | `git log --diff-filter=A` 确认 `b835709` 为新增；v11 / v11-run2 / v12 三份哈希全同 | ✅ |
| 两份差异 = **1 用例 1 字段** | 49 例逐例逐字段机械比对：差异恰为 `B06-1-capacity-conflict` 的 `oracle.失败`，其余 48 例及该例其他两态逐字节相同 | ✅ |
| 上位判据 §`M3-AC-06` 失败条款逐字为"改写成每周 3 条" | 原文核对一致 | ✅ |
| 上位判据早 6 小时冻结 | `f5a9aca` 04:19:03 vs `4bcaaa0` 10:23:58 | ✅ |
| 上位判据 **21 条 AC × 10 个判据字段自冻结以来 0 处变化** | 我把 `f5a9aca` 版与 `HEAD` 版的 21 张卡、10 个判据字段全部抽出来逐项比对：**0 处差异** | ✅ |
| `_run_index.json` 的 `case_file_sha256_at_run` 为 `null` | 实测确为 `null` | ✅（自曝属实） |

执行侧自曝的两处程序缺失（冻结件指针/哈希从未版本化更新、运行记录未记所用版本）**都是真的**，
它把 `AC-06` 写成"成立（带限定）"并把判定者的 `NOT_VERIFIED` 保留在依据里，处理是对的。

*四、`REBIND_003_ADDENDUM_001` 与结果同 commit —— 不构成 `FAIL`。*

该附录落在 `6963829`（17:28:16），与 v12 证据同一次提交。但它**不是判定用的 Oracle**，
而是"闸门被改了、影响面是什么"的绑定变更记录：新版本号有（`ADDENDUM_001`，原文不覆盖），
触发事件有（G-1 现场，`ep14-gate-v12-defect/DEFECT_G1_*.md`）。R-1 的 `FAIL` 条件不成立。

*五、`FAIL` 触发点：`M3_ECC_MODULE_AB_001_FROZEN_v1.0_ADDENDUM_002.md` 被原地改写。*

这是本项唯一、也是本次审查最实的一条。逐条摆事实：

```text
16:29:46  6bbad98  ADDENDUM_002 首次落盘（此时它的内容是合法的：早于本轮 A/B）
16:34:38           A/B 第一次运行（FX-M3-HOLD-01__A，timestamp_utc 23:34:38Z）
16:41:08           A/B 最后一次运行
16:57:54           第一份盲评判定写定（verdict mtime 1787788674）
17:10:02           最后一份盲评判定写定（verdict mtime 1787789402）
17:11:43           揭盲脚本读取 sealed 映射（mapping_read_at 1787789503）
17:28:16  6963829  ★ ADDENDUM_002 被原地修改 ★  ——  与 v12 结果同一次提交
```

改动内容（单一 diff hunk，`@@ -56,15 +56,45 @@`，落在 §2.3 与新增的 §2.3.1）：

- **删掉**了 §2.3 第 1 步里的「执行侧在第 4 步之前不读它」；
- **新增** §2.3.1，坦白执行侧本轮已经违反了这一条（干跑置换算法时打印了完整映射）；
- **替换**成一条新约束：36 份提示词逐字同构，揭盲脚本先断言。

问题在三个地方，逐条说：

1. **没有换版本号。** 文件仍叫 `ADDENDUM_002`，没有 `ADDENDUM_003`、没有内部 `v1.1`。
   R-1 的 `FAIL` 条件写得很死：「内容在其结果产出后被修改，**且未以新版本号与触发事件说明**」。
   触发事件有，新版本号没有。条件成立。

2. **它自己的时序声明现在是假的。** 文件第 9 行至今写着：
   > 时序合法性：本修订与其全部参数在**本轮任何一次 A/B 判定之前**提交

   这句话对 16:29 那一版是真的，对**现在这一版**是假的——§2.3.1 和同构规则是 17:28 提交的，
   在全部 36 份判定写定之后 18 分钟、在揭盲之后 16 分钟。
   同一份文件第 10 行还写着「`v1.0` 与 `ADDENDUM_001` 原文不覆盖、不删除，本文件作为后继版本追加」——
   **它写下了不覆盖前身的纪律，然后自己被原地覆盖了。**

3. **被删掉的那条，正是被违反的那条。** 在结果已经存在之后，把一条已被违反的闸门条件移出协议、
   换成一条可以满足的条件，方向上就是"看到结果之后改判据以保住本轮"。
   本交付在别处完全知道正确做法：纵向 `v1.0 → v1.1` 是新文件、`REBIND_003 → ADDENDUM_001` 是新文件。
   `ADDENDUM_002` 这一次是自己破了自己的例。

**后果有多大，我也要说准，不夸大：**

- 我核过这次 diff **只有一个 hunk**，`§2.4`（硬门推导）、`§2.5`（增益门公式）、`§2.6`（消融门）、`§3`（不变清单）
  **一个字节都没被碰**。决定结果的公式确实冻结在判定开始之前。
- 我从 36 份原始判定重算了整条推导链（见 R-7），`AC-18` 与 `AC-01③` **逐格复现**。
- 我独立核验了那条替换上去的同构规则**确实成立**（36 份归一化后仅 1 种形态）。
- `AC-18` 的结果是 `FAIL(INSUFFICIENT)`，这次改动没有救回它。
- **但 `AC-01③ = PASS` 骑在这一轮上。** 这是唯一一条踩着被后改协议的正式 `PASS`。

所以这是**记录纪律的失效，不是结论被污染**。修法是文档级的：把 §2.3/§2.3.1 以 `ADDENDUM_003` 重新发布、
恢复 `ADDENDUM_002` 原文、并按 A2 为"看到结果后改协议"补一个权威事件（或明确 `AC-01③` 只算探索）。
**不需要重跑 12 次 A/B，不需要重跑 36 名判定者。**

**结论：`FAIL`（阻断项 B-1）**

---

### R-2 · 声明等级不越级（A2 §可信度阶梯）—— `FAIL`

**查了什么**
`M3_CHECKPOINT_ROUND_6.md` §8 全表逐条对照可指认证据；
`M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md` §7.2 全表；两表交叉比对；
四份 V12 判定件的结论行；`M3_FOUNDER_ACCEPTANCE_PACKAGE_v1.0.md`。

**发现**

*一、Checkpoint 6 §8 那张矩阵，我逐条核，经得起：*

| AC | CP6 的写法 | 我核到的 | 判断 |
|---|---|---|---|
| `AC-16` | 图链路 `PASS`／**画布 `NOT_VERIFIED`** | 画布半在 CP6 §8 与 AC 冻结件 §7.2 **两处都仍是 `NOT_VERIFIED`**，没有被运行历史顶替；图链路半另有 `system_prompt_binding_proof.json` 把"喂给 LLM 的就是 SKILL.md v1.2"从推断级抬到已观察级（我实测 `SKILL.md` sha256 = `343758f3…`，与该证明一致） | ✅ 没有美化 |
| `AC-18` | **`FAIL(INSUFFICIENT)`** | 我从 36 份原始判定重算：仅「产能取舍」B 优、「实验设计」A 优且实质 ⇒ `overall_gain = False`。写的就是 `FAIL(INSUFFICIENT)`，不是 `NOT_VERIFIED` 兜底、也不是"硬门零退化"当卖点 | ✅ 没有美化 |
| `AC-12`/`AC-13` | 对钉死绑定成立／**对当前 `main` `STALE`** | 双绑定分列，`STALE` 写在状态格里，并在 §9 二作为**需 Founder 决定的三件事之一**单列。**没有藏进非阻断观察，也没有被写成 `FAIL`** | ✅ 处理正确（`STALE` 是时效旗标不是结果，写成 `FAIL` 反而是错的） |
| `AC-06` | 成立**（带限定）** | 限定写足：点名依赖 `_cases.json` 的 `B06-1` Oracle 更正、保留判定者对「判据先于结果」的 `NOT_VERIFIED`、并在 §9 三把"认不认这次更正"交回 Founder 域 | ✅ 限定充分 |
| `AC-03`/`AC-15` | **不得 `PASS`** | 与判定件一致 | ✅ |
| `AC-01`①② 等 | 成立（不写 `PASS`） | §8 口径明写"执行侧不把成立自行升级为 `PASS`"，并照做 | ✅ |

*二、`AC-11` 标"未被检验"——属实性存疑，但方向是保守的。*

CP6 §8 `AC-11` 的依据写的是：「纵向判定者指出：`E10` 未产生任何变更，`E11` 的『恢复当前基线而非活动开始快照』这一分句本轮根本没被检验」。

这句话出自 **STALE 那一轮**（`M3_ECC_LONGITUDINAL_001_VERDICT_V12_v1.0.md`），
不是本轮 g1fix 判定者说的。**g1fix 判定者在同一分句上的结论正好相反**：

> `E11` 恢复的确实是含 `E10` 变更的当前基线，不是活动开始快照 …… **这一条是本轮最干净的证据**

g1fix 判定者判 `AC-11`「未独立成立」的理由是另一个：`E10` 之后基线被截断，
所以不能说这条能力在**完整**基线上被验证过。CP6 的 `AC-17` 用的是 g1fix 轮，`AC-11` 用的却是被作废那一轮。

方向上 CP6 比现有证据**更保守**（写低不写高），**没有制造任何假通过**，因此不构成 R-2 的 `FAIL`；
但它把一条已被本交付自己判定"不用于判定"的结论搬进了当前矩阵，依据引用必须更正 —— 见 O-2。

*三、`FAIL` 触发点：`M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md` §7.2 至今仍是第 4 轮的状态。*

该文件最后一次修改是 `edcccb2`（11:29:51，第 4 轮）。§7.2 的标题是「**当前**（2026-08-26 EP-10 收口时点）」，
并且带一句「本表是状态投影，**随证据变化更新**」。它没有随证据更新。现在它写着：

```text
| M3-AC-12 | `PASS` | …… 市场观察半原对 main@df2c595 为 STALE，
                       现经逐文件实测确认容器代码与 main@a7b8101 逐字节一致且容器未重启，转 CURRENT |
| M3-AC-13 | `PASS` | …… |
```

而本交付自己第 6 轮的结论是：

```text
| AC-12 AC-13 | 对钉死绑定 df2c595：成立 / 对当前 main@a7b8101：STALE |
```

**这是同一份交付里两份都自称"当前"的文档给出互相冲突的状态，而更高的那个写在验收判据文件里，且没有任何一处标注它已被取代。**

我另外核到一件让这条更要紧的事：`main` 到达 `a7b8101` 的时间是 **10:25:44**，
也就是说第 4 轮（11:29）写下"转 `CURRENT`"时，`main` 早已前进。
那一轮核的是"容器代码与 `a7b8101` 逐字节一致"（真），推出的却是"`AC-12` 市场观察半 `CURRENT`"（假）——
真正变的是 M2 接口语义（可用性与可发布性拆成两道闸、新增适用范围过滤），而 M3 投影没跟上。
第 6 轮把这件事查出来了，但只改了 Checkpoint，**没有回写验收判据文件**。

同一节里同源的其余过期（严重度低一档，一并列出，修的时候一起改）：

- `AC-17` 依据写"序列级三条均成立" → 第 6 轮：局部性只部分成立；
- `AC-18` = `NOT_VERIFIED`，理由"12 次只跑完 6 次" → 第 5 轮已跑完 12/12，结果是 `FAIL(INSUFFICIENT)`；
- `AC-19` 写"独立 Reviewer 已完成并报 2 条阻断项" → 那是第 4 轮的 Reviewer；本候选的收口审查是**本文件**。

**结论：`FAIL`（阻断项 B-2）**

---

### R-3 · 失效传播不多算不少算（A3）—— `PASS`

**查了什么**
我没有采信 `unaffected_proof.json`，而是自己重算了一遍：
`git show 6bbad98:…/shared_checks.py` 取改前源码、`HEAD` 取改后源码，
确认 `gate_main.py` 改前改后**逐字节相同**（改动只在 `shared_checks.ABSENCE_NEAR` 一个正则），
然后把每一次运行记录里的 `draft_raw` / `loaded_references` / `account_context`
分别喂进改前闸门与改后闸门，逐项比对该 json 声明的 13 个 `compare_fields`。

**发现**

*一、我的独立重算与它的结论完全一致：*

```text
我重放的门内运行   73 次
  逐项相同          72 次
  存在差异           1 次  ——  ep07-longitudinal-v12/E07
                              差异字段 ['gate_status', 'input_contradiction']
                              改前：NEEDS_FIX + 一条 input_contradiction 误报
                              改后：CLEAN     + input_contradiction 为空
```

**与 `unaffected_proof.json` 逐字相同。** 声明属实，不是口头保证。

*二、分母对不对——我查了"为什么是 73 不是 82"：*

82 次运行里有 9 次没进重放，就是 A/B 的 `A`／`Aplus`／`Bprime` 三臂。
我核过这 9 份运行记录：`runtime` 全为 `single_call`，**全部没有 `gate_trace` 字段**——
它们根本不经过闸门。排除正确，不是挑着排。剩下 9（保真）+ 49（行为）+ 12（纵向 v12）+ 3（A/B 候选臂）= **73**，对得上。

*三、我做了一项它没做的检查，用来找"少算"：*

`check_input_contradiction` **有两个调用点**：`gate_main.py:34` 和 `post_gate_main.py:39`。
`unaffected_proof.json` 的 13 个 `compare_fields` **只覆盖第一道闸门的报告**，
不含 post-gate 的 `gaps_closed`／`cycle_state_carry`／`carry_reject_reason`——
而后者恰恰决定"这份交付够不够格成为当前有效周期判断"。
post-gate 跑的是**补齐后的最终正文**，与 `draft_raw` 内容不同，理论上可能出现第一道闸门没差、post-gate 却有差的运行。

我把改前改后的检查在全部 73 次运行的**最终交付正文**上各跑一遍：

```text
最终正文层面的差异：0 次
```

**结论不变，但比对字段集确实比真实依赖集窄。** 这是一条记录问题，不是判定问题——见 O-3。

*四、"影响关系无法判断的项：无"——属实。*

`ABSENCE_NEAR` 在 `shared_checks.py` 里被引用的位置**只有 1 处**（第 469 行，在 `check_input_contradiction` 内），
该函数**只有 2 个调用点**。依赖边确实可枚举完毕，不是假装知道。

*五、纵向整条作废算不算多算——不算。*

`E01`–`E06` 在闸门层面确实不受影响，但整条 12 步作废依据的是
**冻结件 `v1.0` §5 与 `v1.1` §5 的「序列不可局部重跑」**，这条规则冻结在结果之前，不是事后发明的；
而且作废的是"能不能用于 `AC-17`"，**证据本身一条没删**（`ep07-longitudinal-v12/` 完整保留 + `_WHY_STALE.md`）。
A3 意义上的"使有证据不受影响的项失效"没有发生。

*六、`main` 前进的影响面核算——正确。*

我核了 `git diff df2c595 a7b8101 -- business-persistence`：确有 6 个文件变动
（`A3_IMPACT_MAIN_MOVED.md` 列了 5 个，漏的那个是 `M2_POST_DONE_REBASE_v1.2_RECORD.md`，一份记录文档，不影响结论）。
它把"钉死绑定下证据仍成立（不置 `STALE`）"与"与 M2 当前接口一致（置 `STALE`）"拆成两条命题分别处理，
这正是 A3 要求的不多算不少算。

**结论：`PASS`**（另附 O-2、O-3 两条观察）

---

### R-4 · 边界与受保护资产（A4）—— `PASS`

**查了什么**
`B = git merge-base main HEAD = df2c595…`（**入场基线**，不是 `main` 当前值）；
`git diff --name-status $B..HEAD` 全量与按目录；每一次提交的非新增变动；
`git reflog`；`git log --merges`；`git merge-base --is-ancestor HEAD main`；
全仓 `SKILL.md` 清点；全 evidence 树的 Dify app_id 抽取。

**发现**

| 检查 | 结果 |
|---|---|
| `content-production/` | **零改动** |
| `decision-chain/` | **零改动** |
| `business-persistence/` | **零改动** |
| `collab-ledger/` | 只多 1 个新增文件：`collab-ledger/tasks/DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001.md`（本任务自己的账本条目，第 4 轮已主动披露为超出冻结允许变化面并给了处置口径） |
| 全分支相对入场基线 | **543 个文件，全部 `A`（新增）；0 修改、0 删除、0 重命名** |
| 六份既有 Skill | `content-production/skills/` 3 份 + `decision-chain/skills/`（Matrix / Campaign / Content Brief）3 份，两个目录**零 diff ⇒ 逐字节未动**。分支内唯一被改的 `SKILL.md` 是本任务自己的新增件 `account-operations/skills/operating-one-account/SKILL.md` |
| Dify App | 全 evidence 树 298 处引用**只出现一个 app_id** `b7fb5b1a-9278-426c-bb8a-f9f288639548`；4 个 `workflow_id` 是同一个 App 的历次已发布版本；回滚演练第 7 步实测已发布版本未被触碰 |
| merge / 直推 `main` | 分支上 **0 个 merge commit**；`git merge-base --is-ancestor HEAD main` 为否 ⇒ **未并入 `main`** |
| force / amend / reset / squash | reflog 32 条**全部是普通 `commit:`**；唯一一条 `reset: moving to HEAD` 位于基线、任何提交之前（建 worktree 的空操作），未改写任何历史 |
| 凭据 | `.env` 存在于 worktree 但**未被追踪**；分支新增文件中未扫到私钥/API key 形态 |

**必须写明的核验边界**：「没有触碰任何其他 App、凭据或运行记录」这一条，
我只能证明**仓库记录范围内**为真（没有第二个 app_id 出现）。
Dify 工作区里其他对象的状态我在本机无法核验，这一条的强度是"仓库内无反证"，不是"已实测无触碰"。

**结论：`PASS`**

---

### R-5 · 失败路径只追加不删除 —— `PASS`

**查了什么**
第 1 轮 EP-06 证据目录与判定件是否仍在；余额耗尽记录的留存数；
全分支删除操作扫描；`ep07-longitudinal-v12-aborted/` 与 `ep07-longitudinal-v12/` 的保留与说明；
**三份 `E01` 的输入哈希、输出与闸门判定逐项对照**。

**发现**

*一、留存情况：*

| 项 | 结果 |
|---|---|
| 第 1 轮 EP-06 失败证据（组 6 不足） | `evidence/ep06-runtime-fidelity/` 9 组**全在**，第 2 轮另起 `-v2` 目录，未覆盖；第 1 轮判定件 `..._VERDICT_v1.0.md` 也在 |
| 余额耗尽的失败运行 | **22 个文件**仍带 `Insufficient Balance` 记录，散在 `ep06b-runtime-behavior/` 与 `ep08-module-ab/`，逐条保留 |
| 因 G-1 而 STALE 的那轮 | `ep07-longitudinal-v12/` 完整 12 步保留，附 `_WHY_STALE.md` 写明**为何 STALE、为何保留、不用于 `AC-17`** |
| harness 崩溃那次 | `ep07-longitudinal-v12-aborted/` 保留，附 README 写明崩溃原因（`KeyError: 'objects_dropped_without_notice'`）、不进入判定、整条重跑 |
| 全分支删除操作 | **0 次**（累计 diff 与逐 commit 检查均无 `D`） |

*二、「执行侧重跑前已经看过那份 `E01`」构不构成择优——我的判断：**不构成**。*

这条不能靠动机声明来判，只能靠有没有"择优梯度"。我把三份 `E01` 摆在一起机械比对：

```text
输入（三份逐字节相同）
  account_context     sha256 前 12 位  56799b4eff57  ==  56799b4eff57  ==  56799b4eff57
  loaded_references                    be47ecc91770  ==  be47ecc91770  ==  be47ecc91770
  user_request                         af299bdbc968  ==  af299bdbc968  ==  af299bdbc968

输出与闸门判定
                     aborted        v12            g1fix
  workflow status    succeeded      succeeded      succeeded
  gate_status        CLEAN          CLEAN          CLEAN
  gate_path          draft_verbatim draft_verbatim draft_verbatim
  cycle_state_carry  ACCEPTABLE     ACCEPTABLE     ACCEPTABLE
  gaps_closed        yes            yes            yes
  其余 10 个决定量    ── 三份逐项完全相同 ──
  正文字数           1943           1664           1365
                     ↑ 被丢弃的那份反而最长
```

**三点结论：**

1. **输入没有被换过**——三次跑的是同一份冻结的第 1 步输入，不存在"换个说法再试一次"。
2. **没有任何一个机械可见的维度上，保留的那份比被丢弃的那份好。** 闸门判定十项全同，
   被丢弃那份的正文还最长。**择优所需的梯度不存在**——即使执行侧想择优，这里也无优可择。
3. **崩溃原因可独立佐证**：三份记录的 `projection` 都用**新**字段名 `objects_not_restated`，
   与 README 说的"改名时漏改了 runner 里的一处打印（仍在读旧键 `objects_dropped_without_notice`）"完全吻合。
   这是与模型输出内容**无关**的执行侧代码错误，任何输出都会崩。
4. 从第 1 步整条重跑**不是自由裁量**——冻结件 §5「序列不可局部重跑」冻结在结果之前，强制如此。

R-5 的 `FAIL` 条件是「任一失败 Attempt 被删除、覆盖或**择优**丢弃」。
删除没有、覆盖没有、择优的梯度不存在。**执行侧把这条主动登记出来交给我判，登记本身是对的，判下来不成立。**

**结论：`PASS`**

---

### R-6 · 声明上限（AC-20 反证探针）—— `PASS`

**查了什么**
Rubric §R-6 那张表的 11 条措辞，全仓反搜（`.md` / `.json` / `.yaml` / `.py` / `.txt`），
再对本轮各claim 文档逐条看极性。

**发现**

```text
本轮各文件命中情况
  M3_CHECKPOINT_ROUND_6.md                       0 处
  M3_FOUNDER_DIFY_TEST_PACK_v1.0.md              0 处
  M3_ROLLBACK_PLAN_v1.1.md                       0 处
  M3_ECC_MODULE_AB_001_FROZEN_v1.0_ADDENDUM_002  0 处
  M3_ECC_REBIND_003_FROZEN_v1.0（+ ADDENDUM_001） 0 处
  M3_ECC_LONGITUDINAL_001_FROZEN_v1.1.md         0 处
  M3_FOUNDER_ACCEPTANCE_PACKAGE_v1.0.md          3 处，**全部否定性**：
      「不能声称平台稀缺、已避开同质化或平台唯一」
      「❌ 真实经营提升 —— 全部反馈都是构造输入，与真实经营无关」
      B09-N5 那条负向探针没跑成，如实标为"最关键的一条，充值后第一个补"
```

全仓其余命中，无一例外落在**失败条款**、**声明上限章节**、**禁止清单**或 Rubric 自身里。
**没有任何一条措辞在无对应证据时被肯定性使用。**

终态登记同样守住上限：`M3_PROFESSIONAL_GAIN = NOT_VERIFIED`、`M3_MODULE_AB = NOT_PASSED`、
`REAL_BUSINESS_LIFT = NOT_VERIFIED`、`M3_ENGINEERING_TASK = IN_PROGRESS`（明写"不是 DONE，也不是 FAILED"）。

一条**不算 R-6 命中**但顺手记下的用词问题：`IN_PROGRESS` 是**进度**词，不是**终态**词
（终态词是 `INVALID / DONE / PARTIAL / BLOCKED / FAILED`，合同 `allowed_final_states` 为 `[INVALID, DONE, BLOCKED, FAILED]`）。
CP6 §8 把它标成"任务终态"是分类串档。方向是**写低不是写高**，所以不构成声明上限突破；
到底该落哪个终态词是 Founder 的域，不是我的。见 O-6。

**结论：`PASS`**

---

### R-7 · 留出与探索/正式分轨（AC-19 本体）—— `PASS`

**查了什么**
留出夹具与 `SKILL.md` 的 git 提交顺序；`holdout_integrity` 的强度上限记录；
`judge_isolation_verification_v12.json` 的方法与 40 名判定者逐条；
**36 名盲评者的原始执行记录（harness transcript）——我自己读了，没有采信执行侧的扫描结果**；
`_arms_and_holdouts_v3.json` / `_leak_scan_v3.json` / `_unblinded_results_v3.json`；
从 36 份原始判定重算整条 A/B 推导链。

**发现**

*一、留出夹具在 `SKILL.md` 冻结之后——git 提交顺序可证：*

```text
04:31:09  c327f98  SKILL.md 首次落盘
08:48:10  af61b82  SKILL.md 冻结（holdout_integrity 声明的冻结点）
10:33:32  7564896  ★ 三个留出场景 FX-M3-HOLD-01/02/03 首次出现 ★
```

✅ 顺序成立，不存在"用留出集反向优化实现"的时间窗。

*二、留出强度上限**如实记录，没有宣称"完美留出"**：*

`holdout_integrity.claim` 原文自己写着：执行侧（= 实现侧）在运行时看得到夹具内容，
所以强度**低于**"实现者全程不可见"的理想情形。这是往低了写，不是往高了写。

*三、「36 份提示词逐字同构」——我独立复算，**证实为真**。*

我从 harness 取出 36 名盲评者各自收到的任务提示词原文，
只按冻结件允许的两处（`unit_NN` 与 `jN`）归一化，然后哈希：

```text
36 份提示词  →  归一化后  →  1 种形态（sha a2c1675531b0a04b × 36）
覆盖         →  12 个单元 × 每单元 3 名判定者，一个不多一个不少
j 序号分布   →  j1 × 12、j2 × 12、j3 × 12
```

**声明属实。** 这条是 §2.3.1 用来替换被违反那条协议的核心约束，它确实立得住。

*四、判定者隔离性——我读了原始执行记录，**证实为真**，而且比那份 json 更严地查了一遍。*

36 名盲评者合计 `Read` 72 次、`Bash` 73 次、`StructuredOutput` 36 次。我逐条扫会造成文件访问的参数：

```text
访问过 /home/faye/diyu-demo 或 /home/faye/diyu-demo-worktrees 下任何路径的：  0 名
执行过任何 git 命令的：                                              0 名
访问过自己单元以外任何 unit 目录的：                                   0 名   ← 我额外加的一项
```

（那份 json 里 `forbidden_hits` 为空是对的：所有含 `diyu-demo` 字样的参数都是会话 scratchpad 路径
`/tmp/claude-1000/-home-faye-diyu-demo/…` 本身造成的假阳性。它的 `method` 字段明写第 3、4 轮各修过一次同类假阳性，
处理正确。）

那份 json 自己写明「本核验能证明判定者没读过去盲文件，**不能**证明盲评有效」——这个上限声明是准确的。
`summary.total = 40` = 4 名仓库内判定者 + 36 名盲评者，与实际 transcript 数对得上。

*五、「432 条评分理由零禁用证据」——我独立复算，**证实为真**。*

从 36 份结构化判定里抽出全部理由字段：**恰好 432 条**（12 单元 × 3 名 × 12 项 = 5 硬门 + 7 维度），
按 §5.3 禁用证据（模型自评／输出长度／字数／Skill 调用次数／规则条数／节点数量／术语密度／"更长"／"更结构化"）机械扫描：**0 条命中**。

*六、A/B 推导——我从原始判定重算，**逐格复现**。*

按 `ADDENDUM_002` §2.4／§2.5 的冻结公式（等第映射、三名取中位数、硬门取多数、
`B优`／`A优且实质`／`相当` 的阈值定义）自己算一遍，与 `_unblinded_results_v3.json` 比：

```text
grades（84 格）      不一致  0
hard_gate_marks（60 格） 不一致  0
B vs A       七维逐维复现：产能取舍=B优、实验设计=A优且实质、其余相当 ⇒ overall_gain = False
B vs Bprime  五维 B 优、零实质退化                              ⇒ overall_gain = True
B vs Aplus   实验设计 = Aplus 优且实质                          ⇒ overall_gain = False
⇒ M3-AC-18 = FAIL(INSUFFICIENT)   M3-AC-01③ = PASS   —— 与记录逐字相同
```

揭盲时序断言我也核了：最后一份判定写定 17:10:02，映射读取 17:11:43，断言成立。

*七、§2.3.1 那条自曝的违反，该不该判 `FAIL`——**我判不该，理由在下面，不是放过。***

R-7 的 `FAIL` 条件只有两条：「留出被泄露却未记录」或「判定者读过禁读文件却未被发现与披露」。逐条对：

- **留出泄露？** 没有。泄露的对象是"执行侧看到了单元→臂的映射"，
  而判定者的隔离性我**亲自从原始记录核过**：没有一名判定者接触过任何去盲通道。
  而且**它被记录了**——写在冻结件里、写在提交信息里、写在 Checkpoint §7 里，一处没漏。
- **判定者读过禁读文件？** 没有，且核验方法我复算过。

所以在 R-7 的判据下不成立。**但这次违反确实造成了一个真问题：一份判据文件在结果之后被原地改写。
那属于 R-1，我已经在 R-1 记成阻断项 B-1。同一件事不重复计两次——重复计一次就是假阻塞。**

*八、探索与正式分轨——分开了：*
冻结前的闸门预检独立成目录 `ep11-gate-v12-precheck/`；
STALE 轮与 aborted 轮各自独立成目录并逐一标注不进入判定；正式取证轮全部落在 `6bbad98` 冻结之后。未见混用。

**结论：`PASS`**（另附 O-4 一条观察）

---

## 阻断项（必须修复才能收口）

> 两条都是**文档同步与版本纪律**问题。
> **不需要重跑任何一次运行，不需要重跑任何一次判定，不需要动 `SKILL.md`、闸门或投影 schema。**

### B-1 ·（R-1）`ADDENDUM_002` 在判定完成、揭盲之后被原地改写，且未换版本号

**事实**：`M3_ECC_MODULE_AB_001_FROZEN_v1.0_ADDENDUM_002.md` 于 `6963829`（17:28:16）被修改，
而该轮 A/B 运行结束于 16:41、36 份判定全部写定于 17:10:02、揭盲于 17:11:43。
改动删除了 §2.3 第 1 步中**已被执行侧违反**的「执行侧在第 4 步之前不读它」，换成了同构约束。
**没有发布 `ADDENDUM_003`，没有内部版本号**；文件第 9 行至今仍声称
「本修订与其全部参数在本轮任何一次 A/B 判定之前提交」——这句话对该文件**现有内容**不成立；
第 10 行还写着"原文不覆盖、不删除"的纪律，而它自己被原地覆盖了。

**为什么阻断**：R-1 的 `FAIL` 条件是「结果产出后被修改，且未以**新版本号与触发事件**说明」——
触发事件有，新版本号没有。更要紧的是被删掉的正是被违反的那条闸门条件，
方向上属于"看到结果之后改协议以保住本轮"，而 **`M3-AC-01③ = PASS` 正骑在这一轮上**。

**范围（我已核实，不要扩大修复）**：这次 diff **只有一个 hunk**，
`§2.4`／`§2.5`／`§2.6`／`§3` **一个字节未动**；我从 36 份原始判定重算，`AC-18` 与 `AC-01③` 逐格复现；
替换上去的同构约束我独立验证**确实成立**。**结论没有被污染，坏的是记录纪律。**

**建议处置（Founder 域，我不替你裁决）**：
把 §2.3／§2.3.1 以 `ADDENDUM_003` 重新发布、恢复 `ADDENDUM_002` 原文并标注被取代；
并按 A2 为"看到结果之后改协议"补一个权威事件——**或者**，若不补权威事件，
则按 A2「看到结果后改判据，本轮只算探索、不产生正式 `PASS`」，把 `AC-01③` 从 `PASS` 降为探索级结论。
二选一，但不能两条都不做。

### B-2 ·（R-2 / A3）验收判据文件 §7.2 仍宣称 `AC-12`/`AC-13` = `PASS` 且 `CURRENT`

**事实**：`M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md` 最后修改于 `edcccb2`（11:29，第 4 轮）。
其 §7.2 标题为「**当前**（2026-08-26 EP-10 收口时点）」、并承诺"随证据变化更新"，
至今仍写 `AC-12` = `PASS`、依据含「…转 `CURRENT`」，`AC-13` = `PASS`。
本交付自己第 6 轮的结论是二者对当前 `main@a7b8101` 为 **`STALE`**（依据 `ep13-a3-main-moved/`）。
另核实：`main` 早在 **10:25:44** 就到了 `a7b8101`，即第 4 轮写下"转 `CURRENT`"时该结论已经不成立。

**为什么阻断**：同一份交付里两份都自称"当前"的文档给出冲突状态，而**更高的那个写在验收判据文件里**，
且没有任何一处标注它已被取代。Founder 打开验收判据看状态，看到的是 `PASS`。
这既命中 R-2 的 `FAIL` 条件（声明状态高于可指认证据），也是 A3 的引用未同步
（失效已传播到 Checkpoint，未传播到它的直接引用）。

**同源、同时该改（严重度低一档）**：同节 `AC-17` 依据"序列级三条均成立"、
`AC-18` = `NOT_VERIFIED`（理由"只跑完 6 次"）、`AC-19`"独立 Reviewer 已完成"——三处都停在第 4 轮。

**建议处置**：把 §7.2 更新到第 6 轮状态，**或**给该节加一条明确的"已被 `M3_CHECKPOINT_ROUND_6.md` §8 取代"指针。
判据本体（§3 的 21 张卡）**不要动**——我已机械核验它自 `f5a9aca` 以来 21 条 AC × 10 个判据字段 **0 处变化**，这是这份交付最干净的一条。

---

## 非阻断观察（登记为新任务候选，不在本任务内处理）

> 以下**都不阻断收口**。写成阻断项就是假阻塞。

**O-1 ·（R-1）权威事件在仓库内无独立佐证。**
纵向冻结件 `v1.1` §0.2 引的 Founder `CONTINUE_TASK` 第 4 条、以及 CP6 通篇引的"Founder 七条"，
在仓库里只以执行侧转述的形式存在，无独立留痕。本轮**没有任何声明踩着它上行**
（`AC-17` 在改判据前后都是 `FAIL(INSUFFICIENT)`），故不阻断。
建议：Founder 确认一次转述是否准确；今后把权威事件原文落进账本，让它可回指。

**O-2 ·（R-2 / R-3）CP6 §8 `AC-11` 的依据引自被作废那一轮。**
CP6 引的「`E10` 未产生任何变更…这一分句本轮根本没被检验」出自 STALE 轮判定件；
g1fix 轮判定者在同一分句上的结论相反（「`E11` 恢复的确实是含 `E10` 变更的当前基线…**这一条是本轮最干净的证据**」），
其判"未独立成立"的真实理由是基线被截断。CP6 的 `AC-17` 用 g1fix 轮、`AC-11` 却用作废轮，**混轮了**。
方向保守（写低不写高），不制造假通过，故不阻断；但依据引用应改为 g1fix 轮原文。

**O-3 ·（R-3）`unaffected_proof.json` 的 `compare_fields` 比真实依赖集窄。**
`check_input_contradiction` 有两个调用点，第二个在 `post_gate_main.py`，
其结果决定 `cycle_state_carry`／`gaps_closed`，而 13 个比对字段只覆盖第一道闸门。
我把最终交付正文层面也比了一遍：**0 处差异，结论不变**。
建议：今后比对字段集按调用点枚举，不按单一报告的字段列。

**O-4 ·（R-7）"推导代码与全部判定原文一并落盘"这句没做到。**
`ADDENDUM_002` §2.3 第 5 步承诺推导代码与全部判定原文落盘供独立重算。
仓库里**没有** 36 份 `verdict_unit_*.json`，**也没有**推导脚本，只有它们的 sha256 与 mtime。
我确实完成了全部重算，但材料来自仓库之外的 harness 执行记录——那不是交付物的一部分，也不持久。
**仓库单独拿出来，不足以支撑那句话。** 建议把 36 份判定原文与推导脚本落进 `ep08-module-ab-v12/`。

**O-5 ·（R-2）第 4 轮的 Founder 面向文件未标注已被取代。**
`M3_FOUNDER_ACCEPTANCE_PACKAGE_v1.0.md` 停在第 4 轮（"A/B 只跑完 6/12"、"远端推送被拦"、"13 例断在余额"），
均已被第 5、6 轮推翻。方向是**写低**（不构成越级声明），故不阻断；
但它是 Founder 面向文件，应加一行指针指向 `M3_FOUNDER_DIFY_TEST_PACK_v1.0.md` 与 CP6 §8。

**O-6 ·（R-6）三处数字/用词与现状对不上，方向均为写低。**

| 位置 | 写的 | 实际 |
|---|---|---|
| CP6 §2 | 相对入场基线 **373** 个文件全部新增 | 现为 **543** 个，"全部新增、0 修改 0 删除"仍成立 |
| CP6 §8 `AC-19` | **39** 名判定者隔离性全 `CLEAN` | 当前 json 为 **40** 名（该文件历经 37 → 39 → 40 三版，最后一版未回写 CP6） |
| CP6 §10 | 分支"**已推远端，逐字符一致**" | 远端跟踪引用停在 `cbe3822`，最后一次提交 `eb9eabf` **未推**（§8 `AC-20` 那格点名 `cf09788`，写法是准确的） |
| CP6 §8 | "任务**终态**：`IN_PROGRESS`" | `IN_PROGRESS` 是进度词不是终态词；合同 `allowed_final_states = [INVALID, DONE, BLOCKED, FAILED]` |

三处都不是越级声明（全部写低或写平），故不阻断。终态该落哪个词是 Founder 的域，我不裁。

---

## 我这一轮做过的独立复算（供复核者重跑）

不是读文档得出的，是自己算出来的，列在这里以便任何人推翻我：

1. **闸门影响面**：取 `6bbad98` 与 `HEAD` 两版 `shared_checks.py`，对 73 次门内运行的 `draft_raw` 各跑一遍
   `gate_main.main()`，逐项比对 13 个 `compare_fields` → **1 处差异（`ep07-longitudinal-v12/E07`），复现 `unaffected_proof.json`**。
2. **post-gate 扩检**：同两版对 73 次运行的**最终交付正文**跑 `check_input_contradiction` → **0 处差异**。
3. **判据本体零变化**：抽出 `f5a9aca` 版与 `HEAD` 版的 21 张 AC 卡 × 10 个判据字段逐项比对 → **0 处差异**。
4. **`_cases.json` 差异**：49 例逐例逐字段比对两份 → **恰 1 例 1 字段**。
5. **36 份提示词同构**：从 harness 取原文，仅归一化 `unit_NN` 与 `_jN` → **1 种形态 × 36**。
6. **盲评者隔离**：36 份执行记录逐条扫文件访问参数 → **0 名越界、0 名跨单元、0 条 git**。
7. **432 条理由**：全部抽出并按禁用证据扫描 → **恰 432 条，0 条命中**。
8. **A/B 推导**：按冻结公式从原始判定重算 → **grades 0 不一致、hard gates 0 不一致，三组对照逐维复现**。
9. **三份 `E01`**：输入哈希与 13 项闸门决定量逐项对照 → **输入全同、判定全同、被丢弃那份最长**。

---

```text
END_MARKER
= M3-INDEPENDENT-CLOSEOUT-REVIEW-V12-v1.0-END
```
