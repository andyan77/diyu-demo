# `M3_ECC_REBIND_005_FROZEN_v1.0` · 勘误 001

- 关系：`M3_ECC_REBIND_005_FROZEN_v1.0.md` **不修改、不覆盖**；本文件是它的后继勘误
- 影响面：只改**候选**（`SKILL.md` 一段文字），**判据一个字未改**
- 候选：`M3 载体 v1.4` → `M3 载体 v1.4.1`

---

## 1. 发生了什么

第 8 轮第一次正式取证批次跑到「保真 9/9、行为 19/49、纵向 8/12、A/B 3/12」时被**执行侧主动中止**。
中止原因不是结果不好——**没有任何一份判定书产生过**，判据也一个字没动。
中止原因是**被测对象本身构造错了**。

`REBIND_005 §6` 第二行为 `AC-09` 加进 `SKILL.md` 的那一句写成了：

> 输入里没有 `standing_positions` 这一段，或者它是空数组 ⇒ 一行 `POS ::` 都不要写。

这句话的本意是挡「基线为空却抄文档示例编号、编造持续位」。
它实际做到的是**把新增位一起禁掉了**。`新增·探索` 与 `新增·常规` 本来就不在输入里——
那是模型本轮新提出的东西，`NEW:<slug>` 的编号由模型自己起。
基线为空时不许新增，持续位这条链就永远起不来：**第一轮的基线必然是空的。**

模型把这条规则原样引用了回来，因果链是直的：

```text
E01 审计块：POS 行：none（standing_positions 为空数组）。
E03 审计块：POS行：standing_positions为空，所以一行都不写。
```

## 2. 损害有多大（机械计量，作废前只读结构化计数字段）

| | 声明了新持续位 | 产生结构性探索位 |
|---|---|---|
| 第 7 轮行为 49 例 | 10 例 | 9 例 |
| 本次作废批次行为 19 例 | **0 例** | **0 例** |
| 第 7 轮纵向 12 步 | 2 步（`E01`、`E04`） | 2 步 |
| 本次作废批次纵向 8 步 | **0 步** | **0 步** |

后果不是「这几项通过了」，而是**根本没被触发**：`G-2` 的结构判定层、`G-3` 的持续位链条、
以及本轮新修的 `D-1`（新增位不得在补齐中被删）在这批证据上全部无从检验。
纵向链的 `standing_positions` 全程是空数组。

## 3. 为什么是中止重跑，不是「看到结果后改判据」

三件事同时成立，缺一条就不该这么处理：

1. **判据一个字没改**。`REBIND_005` §2/§3/§5 全文有效，本勘误不碰它们；
2. **没有任何判定产生**。三名 ECC 判定者、36 名盲评者一个都没派发；
3. **中止前只读了结构化计数字段**——`declared_position_ids`、`new_positions`、
   `structural_exploration_positions`、`stale_value_override`，以及 `draft_raw` 里的 `POS` 行。
   **没有读任何一份交付正文的实质内容**，因此不存在按内容择优的梯度。

同一批次里四十分钟前还有一次同类处置：A/B 的臂规格沿用了第 7 轮那份 JSON，
而 B 臂的 `system_prompt` 是那一轮**快照进去的 v1.3 全文**，于是 A/B 在拿旧候选重跑。
发现后同样立即中止、原样保留、记录、重建。两次的判断标准是同一条：
**花掉的调用在测一个已知错的东西，就停。**

## 4. 改法

禁令收窄到三种状态，并明写新增不受限制：

> 输入里没有 `standing_positions` 这一段，或者它是空数组 ⇒ 一行 `继续`／`处置`／`替换` 都不要写。
> …
> **但 `新增·探索` 和 `新增·常规` 不受这条限制，基线为空时照写。** 新增位本来就不在输入里——
> 它是你本轮新提出的东西，`NEW:<slug>` 的编号由你起。持续位链条正是从这里开始的：
> 第一轮基线必然是空的，如果空基线也不许新增，这条链永远起不来。

另附一组 ❌/✅ 对照，把作废批次里模型的原话放进 ❌ 那一栏。

## 5. 新的冻结值

| 对象 | v1.4 | v1.4.1 |
|---|---|---|
| `SKILL.md` sha256 | `c8f6805b…` | `04b571795bafd4ca6899c21b5c73b20f1509caac9e27fadad489494820884f49` |
| 系统提示词 sha256 | `068a30af…` | `d85a994d625c0d398a88c30c59e0a7ee4a3a1761accb95fa6f6f1bc761586197` |
| Dify 图哈希 | `ea04fa92…` | `61b7cec626618818f7a9f70a12afcd5b28fb35be25c5be66c3a65d29285ad4e8` |
| 已发布版本名 | `m3-cand-v1.4-fix` | `m3-cand-v1.4.1` |

`gate_v13/` 四份代码、两份参考文件、夹具、Oracle **一字未改**，哈希与 `REBIND_005 §8` 相同。
A 基线、A+ 臂、B′ 臂同样一字未改。

## 6. 作废批次的处置

四个目录原样保留，一份不删不改，**不进入任何判定**：

```text
account-operations/evidence/ep06-runtime-fidelity-dify-v14-aborted-pos-defect/   12 份
account-operations/evidence/ep06b-runtime-behavior-v14-aborted-pos-defect/       20 份
account-operations/evidence/ep07-longitudinal-v14-aborted-pos-defect/             9 份
account-operations/evidence/ep08-module-ab-v14-aborted-pos-defect/                6 份 + 本批次作废记录
account-operations/evidence/ep08-module-ab-v14-aborted/                           7 份 + 臂绑错版本的作废记录
```

## 7. 这一版的账要怎么记

`REBIND_005 §7` 关于 `AC-18` 的三条自限继续有效，本勘误不放宽任何一条。
「只进行这一个合并后的最终正式取证批次」这条约束**没有被消耗掉**：
被中止的那一批没有产生任何正式结论，正式批次是接下来这一次。
如果接下来这一批跑完之后又发现候选有构造缺陷，那就是**第二次**——
到那时应当如实记 `FAIL`，而不是再中止一次。

`END_MARKER: M3-ECC-REBIND-005-ERRATA-001-END`
