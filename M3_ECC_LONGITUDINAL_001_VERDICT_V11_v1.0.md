# ECC-M3-LONGITUDINAL-001 · 独立判定（v11-run2）

> `verdict_of`: `account-operations/evidence/ep07-longitudinal-v11-run2/`（12 步，`_run_index.json` 记 `executed_steps: 12`）
> `frozen_criteria`: `M3_ECC_LONGITUDINAL_001_FROZEN_v1.0.md`
> `judged_skill`: `account-operations/skills/operating-one-account/SKILL.md`（`carrier_revision = v1.1`）+ `references/` 两份
> `judged_by`: 未参与本任务实现、未读任何 Checkpoint／其他 VERDICT／其他验收冻结件／其他 evidence 子目录、未执行任何 git 历史命令的独立读者
> 用词只用 `成功` / `不足` / `失败` / `无结果`

---

## 0. 我实际读了什么（供机械核验）

- `M3_ECC_LONGITUDINAL_001_FROZEN_v1.0.md`
- `account-operations/skills/operating-one-account/SKILL.md`、`references/fashion-and-market.md`、`references/six-skill-methods.md`（后两份仅列目录与行数，未逐行引用）
- `account-operations/evidence/ep07-longitudinal-v11-run2/` 下 `E01.json`—`E12.json`、`_run_index.json`

**没有**读 `_steps.json`。冻结件 §6.3 规定判定者应被提供 `_steps.json`，而它位于 `evidence/ep07-longitudinal/`，属本轮判定禁读范围。因此**本判定没有使用逐步 Oracle**，改用冻结件 §4 事件表、§6.5 三条序列级判据、§7 四态定义，以及 `SKILL.md` 自身写明的机械判据。这一缺口见 §7(a)，它是真实的判据可执行性问题，不替任何一侧圆场。

### 链条与绑定的独立复核

- 12 步逐字回填**全部成立**：`E(N).standing_cycle_baseline_in` 与 `E(N-1).final_answer_only_after_reasoning_strip` 逐字节相等（长度分别 1398/3775/2722/995/1473/1605/523/1415/1333/886/997）；`E01` 为 `尚无 —— 本账号还没有被接受的周期基线`。
- 每步 `account_context` 内嵌的 `standing_cycle_baseline:` 字段与该步 `standing_cycle_baseline_in` 也逐字节相等。
- §5 运行绑定在 12 步全部一致：`b7fb5b1a-9278-426c-bb8a-f9f288639548` / `langgenius/deepseek/deepseek` / `deepseek-v4-flash` / `0.4` / `dify_workflow`。
- 12 步 `loaded_references` 内容哈希完全相同（`be47ecc91770…`），即 `fashion-and-market.md: LOADED`、`six-skill-methods.md: NOT_LOADED` 在全序列不变。
- 12 步 `final_answer` 均无 `<think`、无 `<<TRIGGERS>>`、无 `<<REFERENCE_MANIFEST>>` 泄漏。
- 12 步 `event_kind` 与冻结件 §4 事件表逐项对应，无缺项、无错位。

**链条本身没有发现不一致。** 我发现的不一致在别处，见 §3 的 E04、E07 与 §7(b)(e)。

---

## 1. 逐步判定表

| 步 | 事件 | 判定 | 一句话理由 |
|---|---|---|---|
| E01 | 运营状态诊断 | `成功` | 阶段绑定五要素齐、给竞争解释+区分性观察、三类产能分写、无编造事实 |
| E02 | 周期规划 | `成功` | 三支柱不可互换、探索六项实质齐、拒绝把上轮自述升级为事实 |
| E03 | 产能下降 | `成功` | 期望/基线/当期分别承载、只让掉两条并写清让掉的收益、局部性保住 |
| E04 | 市场机会 | `不足` | 结论方向正确，但承重前提「本周实际产能 1 条」与本轮投影 `actual_capacity: 3 条/周` 直接矛盾，且未标注、未调和 |
| E05 | 单条异常反馈 | `成功` | 先写观察后写原因、点名混杂因素、拒绝单条改判、明确声明未受影响判断保持 |
| E06 | 多条一致反馈 | `成功` | 只改证据覆盖范围内的一条任务、不推广成账号模板、不假设试穿素材存在 |
| E07 | 换会话重入＋冲突反馈＋观察窗未结束 | `失败` | 本轮输入含 L5/L6/L7 三条反馈，输出称「新反馈的本体还没有进来」；四个 `[冲突反馈·*]` 必填标签全缺；该步三类事件全部未处理 |
| E08 | 目标冲突 | `成功` | 按有期限覆盖处理、硬前置承接路径、拒绝从经营目标推出 CTA 权限、探索六项实质齐 |
| E09 | Campaign 覆盖 | `成功` | 只接管点名的第 1、3 位、未点名位仍归本能力、到期不自动延续、CTA 卡在承接路径上 |
| E10 | 活动期内变更未覆盖位置 | `成功` | 给出实际处理结果、写明代价、未让活动接管未点名位的管辖权（另见 §7(c)） |
| E11 | Campaign 到期＋沿用追问 | `成功` | 拒绝「效果还行」自动延续、正确判定其证据身份为人工评价、未复活 E10 已替换的任务 |
| E12 | 证据过期 | `成功` | 判定观察 M1 超出有效窗未复验即失效、机会五要素的时间窗要件不成立、恢复基线 |

**计数**：`成功` 10 · `不足` 1（E04）· `失败` 1（E07）· `无结果` 0。

---

## 2. 逐步引用证据

### E01 · `成功`

- 阶段绑定五要素齐：「账号＝序里集·周宁；目标＝长期价值；平台＝视频号；观察窗＝暂无可用的时间窗数据；关键条件＝是否存在持续回访/信任证据」。
- 竞争解释 + 一条区分性观察（O-2 要求）：「若实际已有较长发布历史而只是数据未提供，则可能是稳定经营期——区分两者需要：内容条数/时间跨度、近30天完播/互动集中度」。
- 三类产能分写：「期望发布量＝3条/周；基线产能＝3条/周；当期实际产能＝3条/周」。
- 声明上限守住：「`market_observations` 未提供 → 只能表述为"外部比较不完整"，不能声称已避开同质化或平台稀缺」。
- 不补事实：「**没有试穿素材**，因此版型、垂坠、体感类主张没有证据支撑」。
- 未触发 `NO_CONTENT_TASK` 四标签，但诊断轮的最小交付本就不含内容任务；「受影响任务：无——本轮是诊断轮」已带范围与复判触发，判为未触发而非漏写。

### E02 · `成功`

- 任务互换判据（O-4）：三条支柱各自 `primary_job` 不同（价格构成／三色选择标准／选品判断标准），互换会改变周期结果。
- 拒绝把上一轮自己的话升级成事实：「当前投影 `stage_evidence` 为未提供，上一轮提到的互动集中信号在本轮输入中**无法核对**。所以本轮不把它当作已确认事实」。这是本序列里最干净的一次 A2 棘轮遵守。
- 探索六项均带实质，非模板：`[探索·推翻信号]`「评论始终只有价格询问/购买意图，没有判断性问题；或评论集中在无法承接的私人服务需求；或连续两周无回访证据」。
- 下游自由留住：「具体钩子、叙事、镜头、标题、封面由下游决定」。

### E03 · `成功`

- 三值不静默取其一，且拒绝换算时间单位：「**本周不是把周频改成 1 条/周**：期望发布量 3 条/周、基线产能 3 条/周都不变，这是苏禾请假造成的临时实际产能缺口」。
- 让掉的收益写清（O-3 判据）：「**让掉 P1（本周）**：代价＝上新第一周没有同步消除"凭什么 899+"的疑虑」「**让掉 P2（本周）**：代价＝三色决策服务最常见的收藏/分享信号本周拿不到」。
- 拒绝平均降质：「本周**没有**"把三条合成一条"的选项——合并会改变各自业务工作」。
- 探索到期条件按证据量而非结果改写：「若期间产能持续不足，复验结论标"观察量不足"而非"机制无效"」。

### E04 · `不足`

结论「这个方向现在不值得跟」本身站得住，账号适配弱、无试穿素材两条依据都成立。判 `不足` 的是一处承重前提与本轮输入直接矛盾：

- 本轮 `account_context` 写明：`actual_capacity: 3 条/周`。
- 输出写：「产能：本周实际产能 1 条，只够发 P3。」
- 该错误进入结论链：「机会五要素评估：有范围信号（存在），但账号适配（弱）、真实供给/承接（不足：无试穿素材）、**当前可逆动作（无产能空间）**均不满足。**缺三项**，不是机会。」若按本轮投影的 3 条/周，第三项不成立，缺的是两项不是三项。
- `SKILL.md` 输入规则 2 逐字规定：「**工作流里被调用**（无人可问）—— 用当轮投影里的值」。模型取了上一轮的值，且**既未标注也未说明**为何不采用当轮投影值。
- 该步能读到 `account_context`：同一份输出复述了只存在于 `account_context` 的观察 M1 细节「该观察来自你自己在视频号后台与同类账号的比较」。因此不是输入未送达，是取值错误。

E05 沿用了同一处过期产能状态（「本周仍执行 P3；P1、P2 继续延期」），但 E05 的结论不依赖产能，故只记为传染，不重复计分。E08 起恢复正确（「三类产能均为 3 条/周」）。

### E05 · `成功`

- 顺序正确（O-10 第 3 步）：「先写观察：该条内容完播率低于本账号同类内容约三成；窗口已结束，只有单条观测；来源是视频号后台。」
- 证据身份不越级：「证据身份是真实发布观测，只能说明该次发布在该窗口内的表现，不能直接指向内容策略或账号方向的因果结论。」
- 混杂因素照抄输入，不美化：「已知混杂因素：平台分发规则改版；同类对比的"同类"是否同期不明确」。
- 明确声明未受影响判断保持（O-10 第 6 步）：「之前关于"一件多穿"方向不跟的判断也保持不变——那条判断基于机会五要素，与本条数据无关，不被这条反馈推翻。」
- 拒绝上推给 Matrix 的越界：「这里说的"方向"如果指账号长期定位，更不该由单条数据决定」。
- 四个 `[冲突反馈·*]` 标签实质回答，见 §5 对其中第一项的单独讨论。

### E06 · `成功`

- 多条一致 → 支持更大调整但仍限于证据覆盖范围：「**不改变账号方向和周期目标**，但在内容组合内补上这条缺失的环节」「账号定位、周期目标、事实主张（材质、价格、现货）均不变」。
- 不假装素材存在：「如果这个假设不成立，则本任务只能延后或改用替代形式」，并把它作为唯一的追问。
- 探索六项实质齐，`[探索·让位的稳定义务]` 写了具体让位对象：「让出本周一条细节讲解内容的产能（P3 或同等位次任务）」。
- 边界擦边两处已记录在 §4「责任边界」，判定为未构成越界。

### E07 · `失败`

四条独立证据：

1. **对自身输入的陈述为假。** 本轮 `account_context` 的 `feedback:` 字段完整载有三条：`反馈L5`（真实发布观测，「三色对比」互动高于同类）、`反馈L6`（人工经营结果，陈晚口头转述，门店问针织马甲的顾客变少）、`反馈L7`（真实发布观测，`window_start=2026-08-25`、`window_end=2026-09-01`**尚未结束**，仅 1 天数据、2 条评论提「价格有点贵」）。输出第一句写：「**核心输入缺了——新反馈的本体还没有进来**」，并据此宣布「本轮先不产出任何改判」。
2. **必填闸门命中却全缺。** L5 与 L6 相互冲突（线上互动升 / 线下到店问询降），L7 是窗口未结束的单条不足证据——两条触发路径都命中〈必填项闸门〉的冲突反馈行。四个标签 `[冲突反馈·冲突在哪]` `[冲突反馈·各自证据身份]` `[冲突反馈·为什么保持或改变]` `[冲突反馈·还需要什么观察]` **一个都没有出现**。`SKILL.md` 自身对此的定性是逐字的：「触发命中却缺任一标签 ⇒ **本次交付无效**，补全再交，不是"下次注意"」。
3. **该步承担的三类事件全部落空。** 冻结件 §4 把「冲突反馈」「观察窗未结束」「换会话重入」三项全部挂在 E07，且这三项在 12 步中**没有第二个承载点**。本步零产出即意味着 §4 宣称的事件全覆盖对这三类不成立。
4. **失效沿链条传播。** E07 的交付正文仅 523 字符（全序列最短，次短者 886），逐字回填后成为 E08 的 `standing_cycle_baseline`。P1/P2/P3 三条任务定义、四周节奏、探索提案全部在此处从投影中消失，E08 起只能写「若原计划里没有合适条，让位信息量最低的一条」。**没有任何一步察觉或标注投影内容发生了丢失。**

机制补充（取自被剥离的推理块，仅用于说明成因，不作为判定依据）：`draft_raw` 中模型写「The previous assistant output was a complete M3 response about the 序里集 account, **handling feedback L5/L6/L7**」——它把本轮输入里的 L5/L6/L7 误记为上一轮已处理过的反馈（E06 实际处理的是 L2/L3/L4），随后按「对话里被调用」的输入规则 1 只问一句，而非按规则 2 使用当轮投影。这不是投影载体失效，是模型没读当轮投影的 `feedback:` 字段。

按冻结件 §7，`失败` = 任一步失败。E07 命中。

### E08 · `成功`

- 拒绝把目标变化压成换 CTA（O-1 四轴）：「老板这个要求不是"换一句 CTA"，而是把本周临时叠加一个"线索"目标——它会改变内容角色、验证重点和承接路径」，并具体给出线索目标下的差异「谁来接、多久回、能给到什么信息、咨询资格边界是什么」。
- CTA 三层权限守住：「在承接路径确认前，本周建议先维持原计划」「若承接路径未确认：本周不新增任何咨询 CTA」。
- 覆盖语义正确：「应视为有期限的临时覆盖，只影响本周被点名的内容位置和节奏，不推翻账号定位」「临时覆盖到期后，回到当时仍然有效的周期计划」。
- 探索六项含硬停止条件：「出现任一，下周立即回到全长期价值基线」「若老板要求继续，需要重新形成有依据的周期判断，不能因"效果不错"自动延续」。

### E09 · `成功`

- 只占点名位置：「每周第 1、第 3 位被活动点名，承担**到店预约**目标；每周第 2 位保持**长期价值**」。
- 未点名位仍归本能力管理，且到期语义预先写明：「9 月 9 日活动到期后，第 1、3 位自动恢复长期价值内容，不延续活动策略」。
- 活动约束照抄不放宽：「不做价格优惠、不做站外导流（活动约束）」。
- 承接缺口不靠编造填：「现在上下文里没有到店预约的承接路径（预约入口、门店能否接住、服务时段）。所以活动位先做到店价值传达，**暂不挂预约 CTA**」。
- 跨事件的判断保持声明：「此前的"线索目标"判断保持不变……这次活动是到店预约，与线上线索不是一回事」。

### E10 · `成功`

- 明确拒绝让活动接管未点名位的管辖权：「Campaign C1 只点名第 1、3 位；第 2 位仍归长期价值基线管理，目标层级不变」。
- 产生了实际处理结果而不是只解释原方案：给出第 2 位的新 `primary_job`「建立"这批针织马甲值得亲手看、摸、试"的判断依据」与次要贡献，并保留一个开放选项。
- 代价写明不含糊：「第 2 位的内容会部分受活动主题牵动，不再"纯粹"独立」。
- 该步未能为 AC-11 建立可判的区分条件，但成因在步骤设计而非本步输出，见 §7(c)。

### E11 · `成功`

- 拒绝自动延续（O-8 判据）：「不能因为"效果还行"就自动让活动策略留下来」「继续沿用需要形成新的、有依据的周期判断」。
- 证据身份判定正确：「"效果还行"是人工评价，没有绑定具体内容版本、观察窗口、指标或对照，证据身份不足以支持把活动组合整体转为长期基线」。
- 不复活失效任务：E10 替换掉的是活动窗口内（2026-08-27 → 09-09）第 1、2 周的第 2 位，该窗口在 E11 时点已过；E11 处理的是「下一周期」的位次，与 E10 替换掉的对象不是同一批。冻结件 §7 的「E11 复活了 E10 已合法替换掉的旧任务」失败条件**未命中**。
- 恢复对象写明：「原活动点名的第 1、3 位恢复为长期价值基线任务；第 2 位恢复为独立系列内容，不再是"活动期信任支撑位"」——与 E10 自己的时限标注（「活动期」「第 1 周第 2 位、第 2 周第 2 位」）一致。

### E12 · `成功`

- 失效判定基于输入里的时效字段，不靠感觉：「基于的是 2026-08 近两周的市场观察，有效期四周，当前 2026-09-30 已超过有效窗且未复验——按规则它已经不能作为当前决策依据」。
- 身份不越级：「观察 M1 的身份是"用户本人在后台的比较观察"，不是已验证的平台数据」。
- 机会五要素逐项对照：「市场机会五要素里"当前时间窗"已经不成立」。
- 不为填满结构编造替代：「本轮不安排探索：没有可拆解成验证假设的具体证据，且产能满负荷。复验触发：出现新的市场观察、反馈或指标。」

---

## 3. 我发现的不一致（按任务要求单列）

1. **E04 内部不一致**：输出的「本周实际产能 1 条」与本轮 `account_context` 的 `actual_capacity: 3 条/周` 矛盾（详见 §2·E04）。E05 沿用同一状态。
2. **E07 内部不一致**：输出的「新反馈的本体还没有进来」与本轮 `account_context` 的 `feedback: 反馈L5…反馈L6…反馈L7…` 矛盾（详见 §2·E07）。
3. **冻结件 §8 与运行输入不一致**：§8 写「全部反馈为构造输入，`is_simulated`/`is_test` 身份见 `_steps.json`」，而运行记录中每一条反馈都逐字标注 `is_test=false，is_simulated=false`。详见 §7(b)。
4. **冻结件 §5 与被判 Skill 的版本不一致**：§5 锁定 `skill_commit = SKILL.md 于 commit af61b82 冻结，本轮零改动`；被判的 `SKILL.md` 头部声明 `carrier_revision = v1.1`（2026-08-26），并写明它「使 `v1.0` 的全部 ECC 证据按 A3 失效并整轮重跑」。重绑定文件在禁读范围内，**我无法核验 §5 的绑定对本轮成立**。详见 §7(e)。

逐字回填链条本身无不一致。

---

## 4. 序列级判定（冻结件 §6.5，按其自身定义，不自创维度）

### 4.1 局部性 —— `成功`

全序列没有出现「一次事件导致整个周期被推翻重写」。逐条核对：

| 事件 | 实际影响面 | 引用 |
|---|---|---|
| E03 产能腰斩 | 只重排本周三条中的取舍，期望与基线不动 | 「期望发布量 3 条/周、基线产能 3 条/周都不变」 |
| E04 外部机会 | 零任务变更 | 「本周唯一任务 P3 不变；P1、P2 继续延期。无新任务产生。」 |
| E05 单条异常 | 零判断变更 | 「这条反馈只影响对"为什么放弃某款"这条具体内容的既往表现判断，不扩散到整个周期计划或长期定位。」 |
| E06 多条一致 | 一条任务形态 | 「无其他任务受影响；账号定位、周期目标、事实主张（材质、价格、现货）均不变。」 |
| E08 目标冲突 | 3 条中的 1 条 | 「其余 2 条保持长期价值」 |
| E09/E10 Campaign | 只动点名位 + 用户点名要求变更的一位 | 「未点名位置不在覆盖范围内」被逐字遵守 |
| E12 证据过期 | 只废掉该方向 | 「下个周期不把"一件多穿"作为内容支柱、探索方向或周期性主题」 |

另记一项与本判据方向相反但必须写出的事实：E07 的近空输出使投影**在传递中丢失**了 P1/P2/P3 与四周节奏（§2·E07 第 4 点）。这不是「一次事件推翻周期」，是「一次空转把周期漏掉」，不改变本判据的 `成功`，但直接削弱冻结件 §1 的命题「状态经由定义的投影承载」——投影确实承载了状态，同时也证明了它**没有任何丢失检测**。

### 4.2 证据身份分离 —— `不足`

已被行使的地方全部正确：

- E04 把外部观察限定为「属外部市场观察，不是平台核验数据；只能作为有范围的注意力信号」；
- E05 把真实发布观测限定为「只能说明该次发布在该窗口内的表现，不能直接指向……因果结论」；
- E11 把「效果还行」判为人工评价并拒绝据以晋升；
- E12 把过期的外部市场观察判为失效证据。

判 `不足` 的两条理由：

1. **最硬的一次三方对照没有被处理。** E07 的输入同时给了真实发布观测（L5）、人工经营结果（L6，陈晚口头转述）、以及一条窗口未结束的真实发布观测（L7）。这是全序列唯一一次要求「真实发布观测 / 人工经营结果」当场分开并防止互相顶替的场合，而该步零产出。冻结件 §6.5 的这一条因此只在容易的地方成立、在设计上最难的地方**未被检验**。
2. **模拟／测试反馈这一支从未进入序列。** 12 步的全部反馈都标 `is_test=false，is_simulated=false`，§4 事件表也没有「模拟／测试反馈」这一行。§7 把「模拟/测试反馈在任一步被写成真实经营反馈」列为失败条件，但本轮构造使该条件**不可能被触发**，因而也不产生任何正面证据。

按冻结件 §7，「序列级判定中任一条只部分成立」即 `FAIL(INSUFFICIENT)`。

### 4.3 责任边界 —— `成功`

- **没有任何一步静默改写 Matrix 长期定位**，且多处主动挡回：E05「如果指账号长期定位，更不该由单条数据决定」；E08「不推翻账号定位」；E11「账号不向活动型内容漂移」。12 步中 `positioning` 与 `account_anchor` 从未被重述为新的定位。
- **没有任何一步写出钩子、叙事、台词、镜头、标题或封面成品。** 全序列无一条标题、无一句文案成品、无镜头表。
- 我逐处检查并**未判为越界**的两处擦边，为可复核起见点名如下：
  - E02 `[探索·最小可逆动作]` 给了一句示例互动问句「关于这件针织马甲，你最想知道什么？」。判为未越界的理由：它属于〈CTA 三层权限〉允许 M3 自行提出的低风险平台内互动意图，且 E03 立即把措辞交回下游——「结尾可留一个开放式问题（沿用上轮设定，**具体措辞由下游决定**）」。
  - E06 在推翻信号与替代方案里出现「换出镜者、换场景、多角度展示」「平铺+细节+尺码参考」「文案或旁白」。判为未越界的理由：三处都以并列举例出现在「需要重新设计」与「如果没有素材」的条件从句里，未规定任一条内容必须采用的表达机制，下游的真实选择空间未被取走。**但这是本序列离生产链边界最近的一处**，如实记录。

---

## 5. 空心合规计数与逐处点名

**空心合规计数：1 处。**

判定口径按冻结的加严要求：空洞、同义反复、把问题原样抄回、或与本轮事实无关的填充。

### 5.1 判为空心的 1 处

**E07 `[参考文件加载状态]`** —— 该标签内容本身逐字回抄清单、事实无误，但它是这份交付里**唯一被满足的结构要求**，而整份交付的实质产出为零、且建立在一句对自身输入的假陈述之上。同一份输出里本该出现的四个 `[冲突反馈·*]` 标签一个也没有。格式完整度与工作量在这一步完全脱钩，正是加严要求所指的情形。附带一处不可核验的填充：括号里写「已记入 missing[]」，而本轮真正缺的东西（并不缺）与它无关。

### 5.2 我逐处检查过、判为**不空心**的四处（写出来供复核，不计入）

1. **E05 `[冲突反馈·冲突在哪]`：「不存在相互冲突的反馈，但单条观测不足以支持改判方向。」** 看起来像否定标签前提，实为准确回答：该行触发条件有两支（互相冲突／单条不足），本轮命中的是第二支，标签名只覆盖第一支。它没有把问题抄回，而是说清了本轮实际是哪一支。判为实质。
2. **E03 三处 `不变：…` 的探索项**（不确定性、支持继续的观察、让位的稳定义务）逐字沿用 E02。判为实质而非同义反复：同一份输出里 `[探索·推翻信号]` 与 `[探索·到期或复验]` 被实质改写（「单周单条无互动**不算**推翻」「复验结论标"观察量不足"而非"机制无效"」），且 O-10 第 6 步本就要求对未受影响的判断明确说明保持。
3. **E02 与 E03 的 `[探索·让位的稳定义务]`「不占用额外产能」。** 〈必填项闸门〉对应的语义原文是「它让位掉的稳定义务（**如果有**）」，「无」是合法答案；两处都补了具体的次级代价（「第 1 周结尾可能带一点'互动导向'」）。判为实质。
4. **E08／E09／E10／E11／E12 的 `[参考文件加载状态]` 尾缀**「按规则跳过，不影响本轮判断／本轮不依赖」。清单回抄部分全部正确，不构成违规自述。但这句尾缀是**对一份没读过的文件的影响力断言**，其中 E08（判断商业目标是否挤掉专业义务）与 E09（形成下游任务）恰是 `SKILL.md` 指明应当读 `six-skill-methods.md` 的场景。判为「未获支持的附加断言」，与空心合规不是一类，故不计入计数，但单列在此。

### 5.3 与「标签齐」相关的一个机制事实

`gate_report` 的 `missing_labels` / `decorative_labels` 由 `triggers_self_reported` 驱动，而后者是模型自报。E07 自报 `冲突反馈=否`，闸门随即给出 `missing_labels: []`、`post_gate_report.first_pass_needs_fix: false`、`gaps_closed: true`——即**闸门对一份按 Skill 自身规则属于「本次交付无效」的输出报了全绿**。`trigger_crosscheck_advisory` 确实抓到了「conflict: 自报=否，正文出现该类措辞」，但它是 advisory，无人据以拦截。同类 advisory 在 E04、E10、E11、E12 也各出现一次，同样无后续动作。

`SKILL.md` v1.1 引入闸门的理由逐字是「**"我有没有拿到 X"这类关于自身上下文的自述不可靠**」，并对 `[参考文件加载状态]` 用「只许回抄清单」修掉了这一点。另外四个触发项没有等价的回抄源，仍然跑在自报上。这不是我要提改进方案——它是判定「标签齐 ≠ 该项成立」时必须记录的事实依据。

---

## 6. 整体结论

**`失败`。**

按冻结件 §7 逐条对照：

- §7「**失败**：任一步 `失败`」 —— **命中**。E07 判 `失败`（对自身输入的假陈述 + 四个命中的必填标签全缺 + 该步独占的三类事件全部未处理）。
- §7「**不足**：任一步 `不足`，或序列级判定中任一条只部分成立」 —— 也独立命中两次：E04 `不足`；序列级「证据身份分离」`不足`。即便对 E07 采取最宽松的读法（判 `不足` 而非 `失败`），整体结论也只能是 `不足` / `FAIL(INSUFFICIENT)`，**不会**是「成功」。
- §7「**成功**：12 步逐步 `成功`，**且** §6.5 三条序列级判定全部成立」 —— 两个条件都不满足。
- §7「**无结果**」 —— 不适用。12 步 `http_status` 全 200、`workflow_status` 全 `succeeded`，序列跑完了。

因此按冻结件 §8，本轮**不能**为 `M3-AC-17`、`M3-AC-11` 建立证据，也**不能**为 `M3-AC-10` 追加纵向证据。具体到三个绑定 AC：

- `M3-AC-17`（纵向可复现）：投影确实承载了状态——12 步逐字回填成立、E01→E06 与 E08→E12 的判断连贯——但序列在 E07 断了一次，且断裂**未被任何机制检出**，投影内容随之丢失。不成立。
- `M3-AC-11`（Campaign 覆盖与到期恢复）：E09、E11 的行为正确且证据充分，唯独冻结件点名要靠 E10 才能判的那一条区分（「恢复的是新基线，不是活动开始快照」）**没有被建立**，见 §7(c)。这一项按其自身设计得到的是 `无结果`，不是通过。
- `M3-AC-10`（反馈四情形的纵向出现）：四情形出现三种——反馈充分且范围明确（E06）、多条一致（E06）、有依据地保持不变（E05）——而「互相冲突」与「观察窗未结束」两情形全部押在 E07，随该步一并落空。不完整。

---

## 7. 判据本身的问题

只写我确实认为判据有问题的地方；不替执行侧圆场，也不为显得严格而挑无关的刺。

### (a) 判定协议与隔离规则互斥，判据实际上没有被完整执行

冻结件 §6.3 规定判定者「只被提供：本文件、`_steps.json`、原始 transcript、`SKILL.md`、语义主稿」，§7 又把 `step_file` 指向 `account-operations/evidence/ep07-longitudinal/_steps.json`。本轮判定的隔离规则禁读 `evidence/` 下的其他子目录，恰好把 `_steps.json` 挡在外面。结果是：**逐步 Oracle 没有被使用**，我只能用 §4 事件表、§6.5、§7 与 `SKILL.md` 的机械判据重建判准。

后果不是对称的：凡逐步 Oracle 比我的重建**更严**的地方，我给出的 `成功` 可能偏松；我给出的 `不足` 与 `失败` 则是在更宽松的判准下也已命中，方向上不会因此翻转。但「判定者用了什么判准」这件事本身不该由判定者临场决定——这是判据可执行性的缺陷，应当解决，不该靠下一位判定者自行拼凑。

顺带：`_steps.json` 的 `step_file_sha256` 与 `total_steps: 12` 也因此无法核验。我能核验的只有运行记录侧：12 步齐、`event_kind` 与 §4 表逐项对应。

### (b) §8 的表述与运行输入矛盾，使 §7 的一条失败条件无法被触发

§8 写「全部反馈为构造输入，`is_simulated`/`is_test` 身份见 `_steps.json`」，而 12 步实际输入中每条反馈都写着 `is_test=false，is_simulated=false`。两件事只能有一件成立：要么反馈在语义上是构造的、输入标签写错了；要么输入标签是对的、§8 的括号说明写错了。

无论哪种，效果一致：§7 把「模拟/测试反馈在任一步被写成真实经营反馈」列为**失败条件**，§4 事件表却不含任何一步供给模拟或测试身份的反馈。这条失败条件在本冻结序列中不可能被触发，因此既不产生失败，也不产生任何正面证据。它现在是一条装饰性判据。

### (c) E10 的步骤设计不足以判 §4 自己点名要它判的那件事

§4 事件表逐字写：「（额外）活动期内合法变更未覆盖位置 | `E10` —— AC-11 的"恢复当前基线而非活动开始快照"必须有这一步才可判」。

但 E10 的用户输入是「**活动期间**没被占用的那个位置，我们想换成别的内容，你觉得换成什么合适？」——请求本身就把变更框在活动期内。模型给出的相应也是时限内的：「活动期的"信任支撑位"」「第 1 周第 2 位、第 2 周第 2 位，均调整为……」。到 E11 时活动窗已过，这个变更**按其自身时限自然到期**，于是没有任何「活动期间形成、到期后仍然有效」的新基线可供 E11 恢复。

结果是这个区分无从检验：E11 恢复到 E10 之前的形态，既符合 E10 自己的时限标注、也无法区别于「恢复活动开始快照」。要判这一条，E10 需要一次**不带活动时限、明确进入周期基线**的变更，而当前的用户输入措辞不产生这个条件。这是判据的设计缺口，不是模型的失误——所以我判 E10 `成功`、并把 AC-11 的这一项记为 `无结果`。

### (d) §2 的「不可能发生」论证覆盖面小于它读起来的样子

§2 论证：workflow 无会话记忆 ⇒ 步间唯一信息载体是投影 ⇒ 「某步靠会话记忆而非投影承载」在本构造下**不可能发生**。这个推理对「靠记忆」这一支是对的。

它没有覆盖的两支，本轮各出现了一次：

- **某步不去读给它的投影**（E07 没读 `feedback:`，E04 没用当轮 `actual_capacity`）；
- **投影内容在传递中丢失且无人察觉**（E07 的 523 字符输出把周期计划挤出了 baseline）。

§2 如实记录了另一条上限（「没有证明实现在有记忆的载体上也不会偷懒用记忆」），却没有记录这两条。措辞上的问题是：「不可能发生」很容易被读成「投影这一环已经安全」，而本轮证明的其实只是「投影**足以**承载」，没有证明「每一步都会去读它」，更没有证明「投影不会在传递里变薄」。链路上缺一个丢失检测——判据里也缺一条要求检测它的验收项。

### (e) §5 的锁定变量已经无法在本判定的可读范围内核验

§5 写 `skill_commit = SKILL.md 于 commit af61b82 冻结，本轮零改动`，并规定「任一锁定变量变化 ⇒ 整条序列置 `STALE` 并整条重跑」。而我读到的 `SKILL.md` 头部逐字声明 `carrier_revision = v1.1`（2026-08-26），并写明它「使 `v1.0` 的全部 ECC 证据按 A3 失效并整轮重跑，见 `M3_ECC_REBIND_002_FROZEN_v1.0.md`」。

该重绑定文件与 git 历史都在禁读范围内。所以我能确认的只有：本轮 12 步之间绑定自洽（同一 app、同一模型、同一温度、同一参考载荷哈希），**但无法确认本轮跑的 Skill 就是 §5 所锁定的那一版**。一份冻结判据在自己的 §5 里指向一个已被后继版本作废的锁定值，而作废关系写在判定者读不到的另一份文件里——这是判据版本管理的问题，不是执行侧的问题。

---

```text
END_MARKER
= ECC-M3-LONGITUDINAL-001-VERDICT-V11-v1.0-END
```
