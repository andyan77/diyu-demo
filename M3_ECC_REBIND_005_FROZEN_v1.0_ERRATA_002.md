# `M3_ECC_REBIND_005_FROZEN_v1.0` · 勘误 002

- 关系：`REBIND_005` 与 `ERRATA_001` 均**不修改、不覆盖**；本文件是第二次勘误
- 影响面：仍然只改**候选**（`SKILL.md` 三句话），**判据一个字未改**
- 候选：`M3 载体 v1.4.1` → `M3 载体 v1.4.2`

---

## 1. `ERRATA_001` 没修回来

`ERRATA_001` 把「基线为空 ⇒ 一行 `POS ::` 都不要写」收窄成「一行 `继续`／`处置`／`替换` 都不要写」，
并补了一段「新增位不受限制」。**上线后定向探针仍然零命中。**

原因是同一节里还有两句话在做同样的事，两句都没被 `ERRATA_001` 碰到：

| 出处 | 原文 | 问题 |
|---|---|---|
| v1.4 新加（执行侧） | 「真实的编号只有一个来源：本轮输入里那一段。」 | 把**所有**编号的来源锁死在输入，新增位无处安身 |
| v1.3 起就有（原文） | 「清单里没有的编号**不要写**。编一个不存在的位，同样是交付无效。」 | 字面上正好覆盖 `NEW:` —— 新增位的编号按定义就不在清单里 |
| v1.4 新加（执行侧） | 「你要做的**只有一件事**：审计块里对每一个 `id` 各写一行。」 | 把义务框成"覆盖输入清单"，清单为空即无事可做 |

第三句是 `ERRATA_001` 之前就在的原文措辞，前两句一句是我加的、一句是原有的。
**三句叠在一起，`新增·*` 在文本层面被封死了三重。**

## 2. 改法：把禁令按状态拆开

```text
继续／处置／替换   编号只能来自输入清单；给这三种状态编一个不存在的位 ⇒ 交付无效
新增·探索／新增·常规  反过来：编号**必须**是清单里没有的，写成 NEW:<你起的名字>；
                      新排一个栏目、提一次探索却不写这一行 ⇒ 这一位从没存在过
```

义务从「一件事」改成「两件事」，并写明第二件与第一件同级。

## 3. 上线前定向探针（这一次选对了样本）

`ERRATA_001` 那次探针用的是纵向 `E01`。那一步走「无内容任务」诊断路径，
**本来就可以不产生任何持续位**——探针零命中不能证明任何事，是探针选错了样本。

这一次换成第 7 轮 `new_positions` 非空的三例（`B02-1`／`B03-2`／`B03-5`），输入逐字节复用：

| 例 | 第 7 轮 | v1.4.2 |
|---|---|---|
| `B02-1-no-matrix` | 有新增位 | 审计块内 0（模型在草稿推理里写了三行 `NEW:`，但没进审计块） |
| `B03-2-objective-02` | 有新增位 | **`NEW:冷启动定位验证`，`新增·探索`，结构性探索位触发** |
| `B03-5-objective-05` | 有新增位 | 0 |

**1/3。** 探针的通过线在跑之前就定了：三例里至少一例能声明出 `新增·*` 即算机制修回。
机制确实修回来了——那一例完整走通了「模型声明 → 闸门解析 → `structural_exploration_positions` 触发」。

**同时如实记下差距**：第 7 轮同三例是 3/3。1/3 与 3/3 的差别，可能是三次单样本的抖动，
也可能是文本层面仍有残留的抑制。**本轮不再改 `SKILL.md` 去追这个数字**——
每改一次就多一次过度修正的机会，前两次都是这么来的。
正式批次跑完之后，用 49 例行为 + 12 步纵向的**新增位命中率**与第 7 轮的 `10/49`、`2/12`
逐项对比，结果直接进 Checkpoint，不管好看不好看。

## 4. 新的冻结值

| 对象 | v1.4.1 | v1.4.2 |
|---|---|---|
| `SKILL.md` sha256 | `04b57179…` | `245ee2ab3fe683acc2835cf3453ce367461c394e97ebd0bca34b8f17b6879837` |
| 系统提示词 sha256 | `d85a994d…` | `76d51208f091e89c55436e830728cbdcdbeb7979ed7374d844510f286ebb4346` |
| Dify 图哈希 | `61b7cec6…` | `3bc0950b500af9b5dc984106edbe64eb0e733dbd733f29c96666db896a8a73a9` |
| 已发布版本名 | `m3-cand-v1.4.1` | `m3-cand-v1.4.2` |
| A/B 的 B 臂 sha256 | `fd3dd8b3…` | `f0093f6eafd6e3bd…`（28962 字符） |

`gate_v13/` 四份代码、两份参考文件、夹具、Oracle、A 基线、A+ 臂、B′ 臂**一字未改**，
哈希与 `REBIND_005 §8` 相同（A/A+/B′ 由 `prepare_ab_v5.py` 每次生成时机械断言）。

## 5. 账怎么记

`ERRATA_001 §7` 说的「被中止的那一批没有产生任何正式结论，正式批次是接下来这一次」继续成立。
`ERRATA_001` 与本勘误加起来是**同一次中止的两步修复**，不是两次中止：
第一批作废之后**没有再跑过任何取证**，只跑了四次定向探针（1+1+3 次调用，全部记录在案）。
探针不产生验收结论，也不进任何证据目录。

`END_MARKER: M3-ECC-REBIND-005-ERRATA-002-END`
