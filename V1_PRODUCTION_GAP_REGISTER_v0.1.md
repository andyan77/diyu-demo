# 笛语 V1 Demo 生产差距登记 v0.1

> **只登记，不在本轮实现。**
> 本文件列出的差距**不构成**把一个已满足 Demo Hard Gate 的结果降为 `PARTIAL` 的理由。
> `milestone = V1_DEMO_DONE`，`production_readiness = NOT_IN_SCOPE`。

每项固定记录四件事：当前 Demo 处理方式、生产风险、未来触发条件、推荐迁移方向。

---

## G-01 同 conversation 并发控制

- **当前 Demo 处理方式**：不做任何并发控制。测试请求由脚本严格顺序发送，同一 conversation 同一时间只有一个在途 Run。Dify 本身不阻止同一会话并发收消息。
- **生产风险**：两条消息同时进入同一会话时，两个 Run 会各自读到同一份旧 `task_snapshot_json`，各自计算状态，后写者整体覆盖先写者。表现为「用户明明取消了，任务还在跑」或「确认丢失」。
- **未来触发条件**：任何一个真实用户在同一会话里连点两次发送，或接入任何可重试的客户端。
- **推荐迁移方向**：会话级串行队列（每个 conversation 一个单飞行槽），入队即拒绝或排队第二条消息；配合前端在途禁用发送。

## G-02 CAS 或数据库事务

- **当前 Demo 处理方式**：状态以「整读—整算—整写」方式落在一个字符串会话变量上，无版本比对、无乐观锁。
- **生产风险**：并发下丢失更新（lost update）。`revision` 只用于授权绑定，不用于写入冲突检测。
- **未来触发条件**：出现任何非串行写入路径——多端登录、后台任务回写、重试中间件。
- **推荐迁移方向**：状态落独立表并加 `version` 列，写入用 `UPDATE … WHERE version = :expected`；冲突时整轮重算而非合并。

## G-03 一次性授权原子消费

- **当前 Demo 处理方式**：`authorization.consumed` 在状态机内置为 `true`，与后续的 Tool 调用**不在同一事务里**。合同中已明确不声称 exactly-once。
- **生产风险**：进程在「已标记 consumed、尚未调用 Tool」之间崩溃，授权被吃掉但 Skill 没跑；或反过来 Tool 已跑而状态未落盘，用户看不到结果却已计费。
- **未来触发条件**：Skill 调用产生真实成本、或对外产生副作用（发布、下单、通知）。
- **推荐迁移方向**：授权表加唯一约束的消费记录，`INSERT … ON CONFLICT DO NOTHING` 作为消费凭证；调用前写 intent、调用后写 outcome，崩溃后按 intent 补偿。

## G-04 结果与状态原子提交

- **当前 Demo 处理方式**：产物写入会话变量与状态写入快照是**两个相邻但独立**的赋值节点。产物先写，状态后写。
- **生产风险**：两次写入之间失败，会出现「产物已存在但状态里没有这份 Artifact」的孤儿数据；下一轮读状态时看不到它。当前的缓解是产物先于状态写入，因此不会出现「状态声称有产物但产物不存在」这一更危险的方向，且整轮中止时用户不会收到任何完成声明。
- **未来触发条件**：任何需要对外声称「已保存」的场景。
- **推荐迁移方向**：产物与状态同表同事务提交；或产物走对象存储＋状态里只存不可变引用，引用写入成功即视为提交点。

## G-05 正式 Artifact Store

- **当前 Demo 处理方式**：三份完整产物存在三个会话变量里（`matrix_artifact` / `campaign_artifact` / `content_brief_artifact`），单个变量上限 200 KB（`MAX_VARIABLE_SIZE`）。**会话变量是 Demo 存储方式，不是 Artifact Store。**
- **生产风险**：无版本历史（同一槽位被覆盖后旧版不可取回）、无跨会话检索、无生命周期管理、单体积上限硬性。当前 Content Brief 实测约 33 KB，Campaign 约 23 KB，离上限尚有余量，但无任何超限保护之外的降级路径。
- **未来触发条件**：需要对比两版产物、需要跨会话复用产物、或任一产物接近 200 KB。
- **推荐迁移方向**：内容寻址存储（以 `content_hash` 为键）＋ Artifact 元数据表；会话里只留不可变引用。

## G-06 服务端用户身份

- **当前 Demo 处理方式**：Service API 的 `user` 字段由调用方自填，服务端不校验。测试脚本填的是 `v1-demo-<场景号>`。
- **生产风险**：任何拿到 API Key 的调用方都能伪装成任意用户，读写该用户的会话与产物。「用户明确授权」这一前提在服务端无法被验证。
- **未来触发条件**：系统对第一个真实外部用户开放。
- **推荐迁移方向**：服务端会话绑定已认证身份，`user` 由服务端签发的令牌解出，不接受客户端自报。

## G-07 tenant / brand 权限隔离

- **当前 Demo 处理方式**：单一序里集夹具品牌，夹具内嵌在适配 Workflow 中。无租户概念，无品牌维度的权限判断。影子节点被明令禁止输出 tenant 或权限字段，但这只是提示词纪律，不是机制。
- **生产风险**：多品牌接入后，A 品牌的会话可以调用到 B 品牌的夹具与产物；夹具 SHA 校验只能证明「用的是这份夹具」，不能证明「这份夹具属于当前品牌」。
- **未来触发条件**：接入第二个品牌。
- **推荐迁移方向**：夹具与产物均带 `tenant_id` / `brand_id`，在数据访问层强制过滤；Tool 调用时把品牌标识作为受服务端控制的参数注入，而非提示词内容。

## G-08 生产知识库

- **当前 Demo 处理方式**：不建知识库。夹具以 SHA 冻结的全文形式内嵌进适配 Workflow 的提示词，Skill 每次都看到完整原文。
- **生产风险**：夹具增长后提示词体积线性上升（当前 Campaign bundle 约 42000 字符）；无检索、无增量更新、改一个字要重新发布 Workflow。
- **未来触发条件**：夹具超出单次提示词可承载范围，或需要按需检索而非全量投喂。
- **推荐迁移方向**：隔离的品牌级知识库＋检索节点；保留 SHA 冻结语义（检索结果集也要能算出可校验的 bundle 指纹）。

## G-09 日志保留、脱敏与删除

- **当前 Demo 处理方式**：Dify 自身的 `workflow_runs` / `workflow_node_executions` 保留全部输入输出，其中包含完整夹具原文与完整产物。无保留期、无脱敏、无删除接口。凭据不入库、不入仓库，但对话内容全量留痕。
- **生产风险**：真实品牌接入后，经营事实、商品数据与人员信息会长期明文留在运行记录里；无法响应删除请求。
- **未来触发条件**：接入任何真实品牌数据。
- **推荐迁移方向**：运行记录分级保留（元数据长期、正文短期）；正文字段加密存储；提供按会话与按品牌的删除路径。

## G-10 完整自动化回归

- **当前 Demo 处理方式**：40 类测试目录已冻结但只执行了 10 个多轮场景；确定性状态机与合同检查有 40+ 条单元测试，但它们不经过 Dify 运行时。回归靠人工触发脚本。
- **生产风险**：改一个提示词或一个节点配置后，没有任何机制阻止已修好的缺陷复发。本轮已实证三个缺陷（形式主义确认、影子嵌套输出、状态字误判）都是靠真实运行才暴露的。
- **未来触发条件**：第二次修改主 Chatflow。
- **推荐迁移方向**：40 类逐类多轮 conversation 的自动化回归套件，接入 CI；安全关键 12 类每次必跑，其余按变更面选跑。

## G-11 性能、容量与高可用

- **当前 Demo 处理方式**：单机自托管 Dify 1.16.1，单副本。未做压力测试、未测容量、无灾难恢复演练。实测单次 Skill 调用约 150—240 秒，一条全链约 10—13 分钟。
- **生产风险**：并发用户下 Skill 调用排队、超时；plugin daemon 或上游模型抖动直接表现为用户可见失败（本轮已实测到一次 DeepSeek DNS 解析失败）；无副本、无故障转移。
- **未来触发条件**：同时有第二个用户在跑 Skill。
- **推荐迁移方向**：Skill 调用异步化（提交—轮询—回调），前端呈现进度；上游模型多供应商兜底；关键组件多副本＋健康检查。

---

## 汇总

| 编号 | 差距 | 当前是否阻塞 `V1_DEMO_DONE` |
|---|---|---|
| G-01 | 同 conversation 并发控制 | 否 |
| G-02 | CAS 或数据库事务 | 否 |
| G-03 | 一次性授权原子消费 | 否 |
| G-04 | 结果与状态原子提交 | 否 |
| G-05 | 正式 Artifact Store | 否 |
| G-06 | 服务端用户身份 | 否 |
| G-07 | tenant / brand 权限隔离 | 否 |
| G-08 | 生产知识库 | 否 |
| G-09 | 日志保留、脱敏与删除 | 否 |
| G-10 | 完整自动化回归 | 否 |
| G-11 | 性能、容量与高可用 | 否 |

以上 11 项全部**不阻塞** `V1_DEMO_DONE`，但其中任意一项在真实用户接入前都必须重新裁决。
