# M3 第 9 轮 · 确定性修复与零模型预检报告

| 项 | 值 |
|---|---|
| `task_id` | `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`（不变） |
| 授权 | `CONTINUE_TASK_NO_MODEL_PREFLIGHT`（2026-08-27） |
| 进度 | `IN_PROGRESS`（**不是** `PARTIAL` / `DONE` / `AWAITING_FOUNDER`） |
| 本轮模型调用 | **0 次**。DeepSeek、ECC 判定者、36 名盲评者、Founder 产品验收，一个都没启动 |
| 冻结判据 | [`M3_ECC_REBIND_006_FROZEN_v1.0.md`](M3_ECC_REBIND_006_FROZEN_v1.0.md)（后继版本，不覆盖 REBIND-005） |
| 证据目录 | `account-operations/evidence/ep28-rebind006-precheck/` |
| 载体版本 | `gate_version: v1.5` / `post_gate_version: v1.5`，**尚未部署进 Dify** |

---

## 1. 逐条对照授权

| # | 授权要求 | 状态 | 落点 |
|---|---|---|---|
| 1 | 版本化冻结 DD-1～DD-4 修法，不覆盖历史判据 | 完成 | `M3_ECC_REBIND_006_FROZEN_v1.0.md`；REBIND-004 / 005 及其 ERRATA 一字未动 |
| 2 | 修 `required_audit_lines`：同时保留输入侧持续位与模型本轮新声明的持续位 | 完成 | `gate_main.py`，义务集改为并集 |
| 3 | 修数量主句解析、取舍数量误识别、账号事实缺口与附件缺口混淆 | 完成 | `shared_checks.py` 四处：`_slot_authority` / `PARTITIVE_PREFIX` / `NEG_AFTER` / `REF_NEG_ATTACHED`（另加 `NEG_CLAUSE_CUT` 修一处连带漏检） |
| 4 | 用现有 49 例行为、12 步纵向、3 个 B 臂原始草稿重放闸门，不调用模型 | 完成 | `replay_v15.py`，64 次运行全量重放，0 次模型调用 |
| 5 | 六项必须证明 | 见 §3 | 五项已观察、一项推断并给出实测支撑 |
| 6 | 预检不通过时只继续修确定性代码，不调用模型 | 已执行 3 轮 | 见 §4，前两轮预检 FAIL 都靠改确定性代码收口 |
| 7 | 预检全通过后提交证据、准确影响面和最小正式重跑预算，然后停止 | 完成 | §5 影响面、§6 预算；提交后停止 |
| 8 | 状态保持 `IN_PROGRESS` | 保持 | 全文无 `PARTIAL` / `DONE` / `AWAITING_FOUNDER` |

---

## 2. 改了什么（三个文件，逐处）

| 文件 | 改动 | 对应 |
|---|---|---|
| `gate_v13/gate_main.py` | 补齐骨架的持续位义务集 = 输入侧位 **∪** 模型草稿已声明位；报告新增 `skeleton_position_ids`；`gate_version` → `v1.5` | DD-1 |
| `gate_v13/shared_checks.py` | `_slot_authority()`：槽位权威值只取第一个数量，周期可从后半段借、值不借 | DD-2 |
| 同上 | `PARTITIVE_PREFIX`：部分量／移除类前缀后的数字不算速率主张（赋值类动词 `砍到/减到/降到/压到` **不进**这张表） | DD-3a |
| 同上 | `NEG_AFTER`：数字后 10 字内出现可行性否定 ⇒ 是被否掉的方案 | DD-3b |
| 同上 | `NEG_CLAUSE_CUT`：前置否定的回看窗遇「，、：」截断，不跨小句 | DD-3 连带漏检 |
| 同上 | `REF_NEG_ATTACHED`：否定必须紧贴参考类名词（前后 4 字、不跨标点） | DD-4 |
| `gate_v13/post_gate_main.py` | 只改版本号字符串 → `v1.5` | 载体标注 |

**`SKILL.md` 本轮一字未改**，sha256 = `245ee2ab3fe683acc2835cf3453ce367461c394e97ebd0bca34b8f17b6879837`，
与第 8 轮逐字节相同。这一条是 §5 影响面里「A/A+/B′ 三臂可零调用复用」的机械依据。

**按 A5 删掉的三处**（自己先写出来、在真实语料两个轴上都量不出差别）：
`SLOT_ASIDE`（先剥括注再取值）、`TRADEOFF_VERB`（独立的取舍动词守卫）、`[/／]` 全角斜杠支持。
最后一处回退成如实披露的已知漏检（REBIND-006 §4 R-1）。

---

## 3. 六项证明，逐项给数

语料：第 8 轮已落盘的 **64 次真实运行**（行为 49 + 纵向 12 + A/B B 臂 3），
共 **128 份正文**（每次运行的草稿正文 + 终稿正文）。对照组钉在 `ef35c67`（候选 v1.4.2）。

### 3.1 十个误拒全部消失 —— 实际是**十一个**

第 8 轮拒收台账只覆盖行为 49 + 纵向 12，**没覆盖 A/B 臂**。本轮零模型重放在同一套判据下
查出第 11 例误拒：**`FX-M3-HOLD-02__B`**。它属于 A/B 的 B 臂，第 8 轮就被拒收了，台账里没有它。

拒收理由：`actual_capacity 输入权威值 2条/周，正文却断言当前是 5条/周 ——
「一周五条这周做不到，不是安排问题，是真实产能只剩两条」`。
这句话说的恰恰是「五条做不到、实际就是两条」，与输入完全一致。DD-3b 的成因。

| | v1.4.2 | v1.5 |
|---|---|---|
| 64 次运行里被拒收 | **13** | **2** |
| 其中误拒 | 11 | 0 |
| 其中真拒 | 2（E07、E08） | 2（E07、E08） |
| 新增拒收 | — | **0** |

11 例全部清除，逐例见 `ep28-rebind006-precheck/REPLAY_V15.json`。

### 3.2 E07、E08 两个真拒仍被拒收 —— 且与补齐节点怎么写无关

穷举证明（`verify_e07_e08_invariance.py`）：把终稿审计块里的持续位声明当自由变量，
取遍 `输入位 ∪ 草稿声明位 ∪ {一个外来 id}` 的**全部子集**：

| 例 | 枚举的可能输出 | 其中被拒收 | 反例 |
|---|---|---|---|
| E07 | 4 | 4 | 无 |
| E08 | 8 | 8 | 无 |

E08 顺带修掉一处**措辞不准**：第 8 轮它的三条拒收理由里有一条是「内容被静默丢掉」，
而补齐节点做的其实是纠正一个编造的 id。v1.5 下这条理由不再出现，改为如实报
`fabricated: 选品逻辑固定位`。结论没变，理由变准了。

### 3.3 新增持续位零删除

| 指标 | 值 |
|---|---|
| 检查的运行次数 | 64 |
| 新骨架漏掉模型草稿已声明持续位的次数 | **0** |
| 骨架因此多出行的运行次数 | 30 |
| 多出的行总数 | 45 |

**等级说明（不含糊）**：「骨架不漏」是**已观察**（纯确定性，64/64）。
「因此误拒消失」在**补齐路**上是**推断**——中间隔着一个 LLM 补齐节点，零模型重放拿不到它的
新输出。前提是「补齐节点把骨架里的 `POS ::` 行照发回来」，实测支撑取自第 8 轮同一批记录：

- 22 次补齐路运行里，必填项骨架行 **22/22 全数复现，0 行丢失**；
- 骨架里出现过 `POS ::` 行的 3 次，**3/3 复现**。

即便如此，这一步只能记成推断。**正式取证必须真跑**，这正是 §6 预算存在的理由。

### 3.4 DD-2 / DD-3 / DD-4 零复发

| 方向 | v1.4.2 | v1.5 |
|---|---|---|
| 128 份正文上的阻断命中（G-4 + 清单） | **7** | **0** |
| 7 处逐条判读 | 全部误报（4 处 B04-1P 的 G-4、2 处 FX-M3-HOLD-02 的 G-4、1 处 B07-1 的清单） | — |
| v1.5 新引入的阻断 | — | **0** |

14 个不同的槽位取值，v1.5 全部解析出**语义正确**的权威值（v1.4.2 只有 4 个解析正确）。

### 3.5 不引入新误报、新漏检

**误报方向**：见上表，新增阻断 0 处。

**漏检方向**，三族探针，判据一律是「旧能抓到的新也要抓到」：

| 族 | 构造规则 | 探针数 | v1.4.2 抓到 | v1.5 抓到 |
|---|---|---|---|---|
| 一 · 同单位 | 句子取自真实运行一字未改；权威值改成「正文每周值 + 1 条/周」 | 42 | 42 | 40 |
| 二 · 跨单位换算 | 同上，权威值改成「N 条/天」，7N 必然仍冲突 | 42 | 42 | 40 |
| 三 · 正文机械改写 | **槽位保持真实原值**，把正文里的「N 条」机械改写成（槽位每周值+1）条/周；探针集合由一个**独立解析器**决定，不由被测的任何一版判据决定 | 54 | 50 | **52** |

- **新漏检 = 0。**
- 族一二里旧抓到而新没抓到的 4 个，全部是**作用域边界**：伪造的权威值本身就是从
  「新判据明确声明不当作速率主张」的那个数推出来的（`让掉另外 2 条`、`一周五条…做不到`），
  且该句里没有其他主张性速率。四个探针**留在分母里、单独归类、逐条给出排除理由**，
  不从分母里悄悄拿掉——拿掉了作用域边界就看不见了。
- 族三 v1.5 比 v1.4.2 多抓到 2 个；两版都没抓到的 2 个先于本轮存在，已记为 R-3。

**六个具名族**（含 Founder 第 3 条点名的四族）逐族符合预期：

| 族 | 应否开火 | v1.4.2 | v1.5 |
|---|---|---|---|
| 真实旧值覆盖（`砍到 2 条/周`、`当前实际产能是每周 2 条`） | 应开火 | 否 | **是** |
| 三值分离 | 不应开火 | 否 | 否 |
| 等价单位换算 | 不应开火 | 否 | 否 |
| 拒绝合并 | 不应开火 | 否 | 否 |
| 取舍陈述（O-3 要求写的） | 不应开火 | **是（误报）** | 否 |
| 后置否定（被否掉的方案） | 不应开火 | **是（误报）** | 否 |

注意第一行：v1.4.2 连**真覆盖都抓不到**（槽位解析被全角斜杠挡在门外）。v1.5 抓得到。

### 3.6 不引入补齐自证

- D-3 的剥离逻辑**一个字没改**：剥的仍是「不在草稿里的行」；本轮进骨架的行**全部来自草稿**。
- **没有**在复检侧加「自动补回」兜底——那等于闸门替模型重新写出 `POS ::` 行，正是 D-3 刚修掉的回路。
- 回归：`audit_gate_authored_anchor.py` 在 v1.5 下重跑，20 次补齐路运行，D-3 残余嫌疑 **0**。
- 回归：`verify_d1_d3.py`（重构保真 + 缺陷仍被抓）与 `verify_g4_v2.py`（REBIND-005 §3 全套）
  在 v1.5 下**全部仍 PASS**。
- **一处如实交代**：`verify_g4_v2.py` 的输出被改写到了 `ep28-.../regression-rebind005/`；
  另两个验证器的输出路径不是同名常量，我的改写没生效，它们**重写了 ep22 目录下自己的那两个
  json**。逐文件与 `HEAD` 比对：`D1_D3_OLD_VS_NEW.json`、`D3_RESIDUAL_ANCHOR_AUDIT.json`、
  `G4_V2_FP_AND_MISS.json` **三个都逐字节相同**，历史记录没有被改动，改变的只有 mtime。
  这同时是一条独立证据：v1.5 把 REBIND-005 的全部结论**原样复现**，一个字都没变。

### 3.7 消融（A5）

每个新守卫单独关掉，误报集或漏检探针捕获数必须变。基线：误报 0 处、漏检捕获 132 个。

| 守卫 | 关掉后误报 | 关掉后漏检捕获 | 可消融 |
|---|---|---|---|
| `PARTITIVE_PREFIX` | 2 | 136 | 是（两轴都变） |
| `NEG_AFTER` | 2 | 132 | 是 |
| `REF_NEG_ATTACHED` | 1 | 132 | 是 |
| `NEG_CLAUSE_CUT` | 0 | **128** | 是（只在漏检轴上变——只看误报轴会把它误判成无差别单元） |
| `_slot_authority` | 2 | 136 | 是 |

---

## 4. 预检不通过时做了什么（第 6 条）

预检跑了三轮，前两轮 FAIL，**全部只靠改确定性代码收口，一次模型调用都没有**：

| 轮 | FAIL 项 | 根因 | 处置 |
|---|---|---|---|
| 1 | P2 新漏检 8 个、P5 有守卫不可消融 | ① 前置否定回看跨了小句，把后半句的真赋值一起否掉；② 消融只在误报轴上量，量不到「专治漏检」的守卫 | ① 加 `NEG_CLAUSE_CUT`；② 消融改两轴，并新增独立解析器生成的第三族探针 |
| 2 | P5 全角斜杠不可消融 | 它在两个轴上都量不出差别 | 按 A5 **删掉**，回退成如实披露的已知漏检 R-1 |
| 3 | P4 一族 FAIL | **我自己的探针写错了**——权威值用了刚被回退的全角斜杠 | 探针改半角 |

第 3 轮之后七项判据全部 PASS。

---

## 5. 影响面（A3，两侧都算）

「什么变了」不靠印象：逐个文件把工作区当前内容与 `ef35c67` 的内容做 sha256 比对
（`impact_surface_v15.py`，结果落 `IMPACT_SURFACE_V15.json`）。

**变了**：`shared_checks.py`、`gate_main.py`、`post_gate_main.py` ⇒ 闸门三节点 ⇒ 图哈希。
**未变**：`SKILL.md`、`assemble_main.py`、`projection_v13.py`、`gate_pipeline_v14.py`、
`update_m3_app_v14.py`（含 `FINALIZE_SYS`）、`business-persistence/`（受保护基线，一字未动）。

| 取证 | 载体 | 结论 |
|---|---|---|
| EP-06 保真 9 例 | Dify 已发布图 | **STALE ⇒ 必须重跑** |
| EP-06b 行为 49 例 | Dify 已发布图 | **STALE ⇒ 必须重跑** |
| EP-07 纵向 12 步 | Dify 已发布图 + 投影 | **STALE ⇒ 必须重跑** |
| EP-08 A/B · B 臂 3 例 | 直连镜像 `gate_pipeline_v14` | **STALE ⇒ 必须重跑** |
| EP-08 A/B · A / A+ / B′ 9 例 | 直连单次调用，**不过闸门** | 不受影响 ⇒ 复用，**0 次模型调用** |
| EP-08 盲评 36 份判词 | 冻结提示词 + 盲评单元 | **STALE**（B 臂正文会变 ⇒ 盲评单元会变） |
| AC-16 系统提示词绑定 + 画布实证 | 已发布图的读回 | **STALE**（提示词文本本身没变，但绑定的已发布版本会变） |
| M2→M3 投影契约 | 纯确定性代码 | 不受影响 ⇒ 复用 |
| AC-12 / AC-13 定向复验 | 运行中的 M2 容器 + 投影 | 不受影响 ⇒ 复用 |
| 下游 Brief 消费 | 纯确定性代码 | 不受影响 ⇒ 复用 |
| Founder 实测包 v1.1 | 从证据逐字取材 | 继续 `STALE`，本轮不重生成 |

**复用的机械证据**（按 A3「不多算」，不让有证据、不受影响的项失效）：

- `SKILL.md` 本轮 sha256 与上一轮逐字节相同；
- A/B 记录里 B 臂的 `system_prompt_sha256 = f0093f6eafd6e3bd…` 用当前 `SKILL.md` 重算**仍然相等**，
  A / A+ / B′ 三臂提示词本轮完全未触及 ⇒ 这 9 次运行的原始输出可逐字复用；
- `projection_v13.py` 与 `business-persistence/` 未动 ⇒ 投影契约与 AC-12 / AC-13 的定向复验结论复用。

**判断不了因而需要标 `STALE` 的项：无。** 本轮所有依赖边都能由文件哈希与载体确定。

---

## 6. 最小正式重跑预算

分三段。**第一段零模型**，第二段是本轮修法的正式取证，第三段（盲评）**未获授权，不在本次预算内启动**。

### 第 0 段 · 先补一个前置缺口（0 次模型调用）

部署与运行脚本依赖的四个共用模块存放在上一会话的 scratchpad 里，**已随会话消失**：
`dify_client.py`、`create_m3_app.py`、`manifest.py`、`ep06_runtime_fidelity_v2.py`。
仓库里只有引用它们的脚本，没有它们本身。

- 后果：现在**无法部署 v1.5，也无法发起任何正式取证**。
- 处置：重建这四个模块，并把它们移进仓库（`account-operations/tools/`），不再放 scratchpad——
  这类事故不该再发生第二次。凭据继续只放 gitignore 的 `.env`，不进仓库。
- 本轮已用桩把**输出契约自检**跑通（三个代码节点声明的 outputs 与实际返回逐一相等，
  end 节点引用全部存在），桩只补 `read` / `WORKTREE` 两个平凡助手，不碰构建逻辑，
  且 `Console` 做成一用就抛错的空壳，不会静默走一条假的部署路径。

### 第 1 段 · 部署与非模型取证（0 次模型调用）

| 项 | 说明 |
|---|---|
| 部署 v1.5 进任务专用 App `b7fb5b1a-9278-426c-bb8a-f9f288639548` | 只动这一个 App，不动生产 |
| 图结构检查 `graph_structural_check_v14` | 确定性 |
| AC-16 系统提示词读回 + 浏览器画布实证 | 走 CDP，不调模型 |

### 第 2 段 · 正式取证（需要新授权）

预算按第 8 轮**同一批取证的实际消耗**估，不是拍的：

| 取证 | 运行次数 | 第 8 轮实际 tokens | 本轮是否要跑 |
|---|---|---|---|
| EP-06 保真 | 9 | 361,747 | 是 |
| EP-06b 行为 | 49 | 1,898,302 | 是 |
| EP-07 纵向 | 12 | 458,259 | 是 |
| EP-08 A/B · **仅 B 臂** | 3 | 188,940 | 是 |
| EP-08 A/B · A / A+ / B′ | 0 | 0 | **否，逐字复用** |
| **合计** | **73** | **≈ 2,907,000** | |

对照：第 8 轮正式批次是 85 次调用 / 3,126,151 tokens。本轮靠 A/A+/B′ 复用省下 9 次运行。

跑完后必须重算的验收：误拒是否真的归零、E07/E08 是否仍被拒、DD-1 在补齐路上的推断是否被真实运行证实。

### 第 3 段 · 盲评（**本次不启动**）

36 份盲评判词依赖第 2 段产出的 B 臂正文。第 2 段没跑完之前它没有输入；
且当前授权明确禁止启动盲评者。**不在本次预算内，等新授权。**

---

## 7. 本轮没有做、也不会替 Founder 做的事

- 没有部署进 Dify，没有调用任何模型；
- 没有生成新的 Founder 实测包，没有请求产品接受；
- 没有把任何验收项从 `NOT_VERIFIED` 往上推——`AC` 状态本轮**一项未动**，
  因为本轮零模型，没有任何新的运行证据；
- 没有把 `workflow succeeded` 当成验收 PASS；
- 没有替 Founder 在第 2 段是否开跑上做选择。执行侧给预算，不设默认，不自行开跑。

**下一动作：等待 Founder 或规划侧对第 0 段与第 2 段的授权。未获授权前不启动任何模型调用。**

`END M3_PRECHECK_ROUND_9`
