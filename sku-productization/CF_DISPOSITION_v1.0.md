---
task_id: DIYU-V1-THREE-SKU-PRODUCTIZATION-001
section: T5 · 结转清单逐条处置
source: 笛语商业SKU验收体系_索引与启动规则_v1.0.md §10
note: 不适用是合法答案，遗漏不是；本表逐条给出处置
---

# T5 · CF_DISPOSITION v1.0

| 条目 | 处置 | 说明 |
|---|---|---|
| **CF-01** 每 Case 跑 k≥3 次 | **不适用** | 本任务零模型调用，不涉及任何 empirical 跑测或 Case 执行；k≥3 次一致性检验是运行期验证机制，属阶段三（empirical 测试）范畴，本任务只做静态代码/文本层面的拆除与移植 |
| **CF-02** 同源对照位两侧各留一个 | **不适用** | 涉及盲评素材分配，本任务不做盲评，无对照位概念 |
| **CF-03** 计分门也要 k 次一致 | **不适用** | 涉及 G2 计分门运行期的 k 次评分一致性，本任务零模型调用不产生任何评分结果 |
| **CF-04** 参考文件防静默陈旧 | **推迟到 S6（阶段二目录重构）** | 本轮未改动任何 `references/` 文件的加载/构建流程：P0 继续用独立 `references/` 目录（E1 已摘取），P1/P1.5 继续引用源目录既有三份文件，均未新增或修改参考文件本身；CF-04 明文写着处置时点为"阶段二目录重构（S6），与参考文件加载器搬进 `core/` 一并做"，与本任务（产品字段/机制清理）不同阶段 |
| **CF-05** 顾问性三条判据 | **推迟到共用层改造**（沿用 E1 结论，未变） | E1 已确认三个 SKU 在③项判据上的缺口分布一致，应在共用层一次性解决，不逐个打补丁；本轮 T3 给 P1 新增的 `reversal_conditions[]` 客观上加深了③"有立场但不固执"判据的承载，但这是 T3 任务本身的产物，不是专门为 CF-05 设计的判据承载机制——CF-05 要求的"跨四 SKU 共用反向错误门"仍未搭建，如实登记为仍在推迟状态，不因这个副作用声称已处理（详见 `P1_REVERSAL_FIELD.md` 末节） |
| **CF-06** 独立仓库拆分的时点与禁带项 | **已处理（本轮层面）** | 本任务未触发拆分条件（外部拿到访问权）：远程仓库仍为 `andyan77/diyu-demo`，未创建新仓库，未做 `git subtree split`；`eval/` 目录在 E1 阶段已建在顶层（与 `core/` 平级），本轮未新增或移动任何文件到 `core/`/`eval/`/`products/`/`adapters/` 之外的位置，`eval/` 顶层位置未受影响，继续满足"E1-3 建树时即放顶层，不是拆分时再挪"的要求；完整禁带清单（盲评素材、封存映射、对照 Prompt、评审意见等）按 §10 原文推迟到真正触发拆分时逐项复核，本轮不涉及 |

## 机制核对

本文档即《笛语商业SKU验收体系_索引与启动规则_v1.0.md》§11「机制一」要求的"每份 Execution Prompt 必须逐条处置结转清单"在本任务的落地——CF-01~06 六条全部给出处置，无遗漏、无沉默。
