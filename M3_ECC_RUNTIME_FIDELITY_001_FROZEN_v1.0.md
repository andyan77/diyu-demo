# ECC-M3-RUNTIME-FIDELITY-001 · 冻结判据（EP-06）

> `task_id`: `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> `bound_ac`: `M3-AC-16`
> `frozen_at`: 本 commit（先于任何真实 DeepSeek 调用，见 §0 时序声明）
> `frozen_by`: 执行侧（非独立判定者）；判定阶段另由未参与本文件撰写与实现的独立读者执行，见 `M3_ECC_RUNTIME_FIDELITY_001_VERDICT_v1.0.md`
> `runtime_binding`: provider=DeepSeek, model_id=`deepseek-v4-flash`（现场核验，见 §1），base_url=`https://api.deepseek.com/v1/chat/completions`
> `evidence_source`: 全部 7 组的账号/商品/价格/人物事实取自已接受夹具 [`decision-chain/fixtures/一页纸夹具品牌事实 v0.1.md`](decision-chain/fixtures/一页纸夹具品牌事实%20v0.1.md)（序里集 XULI SELECT），不新增未登记事实
> `probe_source`: 全部 7 组的场景结构取自已冻结的 [`account-operations/fixtures/M3_ACCEPTANCE_FIXTURES_v1.0.md`](account-operations/fixtures/M3_ACCEPTANCE_FIXTURES_v1.0.md)（FX-M3-POS-01/04/06/08/09、FX-M3-NEG-06、FX-M3-ABL-03）与 `SKILL.md` 输入表 `platform` 槽位规则（组 7），不新增产品语义

## 0. 时序声明（A2 先后）

本文件在任何真实 DeepSeek API 调用**之前**撰写并提交为独立 commit。判据、Oracle、成功/不足/失败标准均在看到任何模型输出前冻结。若后续执行发现判据本身有缺陷，只能新开版本化修订，不得在看到结果后原地改判据。

## 1. Runtime 现场核验（先于用例执行）

```console
$ curl -s https://api.deepseek.com/v1/models -H "Authorization: Bearer $DEEPSEEK_API_KEY"
{"object":"list","data":[
  {"id":"deepseek-v4-flash","object":"model","owned_by":"deepseek"},
  {"id":"deepseek-v4-pro","object":"model","owned_by":"deepseek"},
  {"id":"deepseek-v4-flash-vision-exp","object":"model","owned_by":"deepseek"}]}
```

`deepseek-v4-flash` 确认存在，与合同 §12.2「主 Runtime = DeepSeek V4 Flash」一致。凭据来源：Founder 本轮会话内直接提供，经真实 API 调用核验（`GET /v1/models` → `200`），写入本 worktree 根 `.env`（已 gitignore，不进仓库）。

## 2. 统一账号上下文（除各组显式声明的变量外锁定不变）

```text
account       = 序里集 商品负责人「周宁」账号（已确认组织角色，见品牌夹具 §五）
positioning   = 已确认：商品选品、版型比较、供应商取舍的第一手讲述人
platform      = 视频号（除组 7 外锁定；组 7 显式改动此变量）
current_task  = "初秋通勤衣橱"第一阶段上新（见品牌夹具 §七）
product_fact  = 针织马甲，价格 899—1099 元，黑/藏青/燕麦三色，羊毛混纺（已登记面料事实）
permission    = 低风险互动 CTA 可自主提出；经营型 CTA 需目标+有效承接路径；高风险 CTA 需明确授权（本轮未授权）
campaign      = 无覆盖
```

以上事实全部来自 `一页纸夹具品牌事实 v0.1.md`，未新增任何未登记的商品、库存、价格、门店或顾客事实。

## 3. 七组冻结用例

### 组 1 · 正向 —— 目标反事实（对应 FX-M3-POS-01）

**锁定**：§2 全部账号上下文；产能 = 期望 3 条/周 = 基线 3 条/周 = 当期实际 3 条/周（三者一致，不引入产能冲突变量）；本轮为周期规划，非复盘。

**变体 A**：`primary_objective = 长期价值`
**变体 B**：`primary_objective = GMV`

用户输入（两变体仅替换目标声明句，其余逐字相同）：

```text
【变体 A】我们号的主目标是长期价值，不是短期冲量。帮我做这个周期的内容规划。
【变体 B】我们号这个周期主目标是 GMV，要把针织马甲的转化做起来。帮我做这个周期的内容规划。
```

**Oracle（先于运行冻结）**：
- **成功**：内容角色／资源顺序／验证重点／承接路径四轴中，A/B 间至少 2 轴发生可解释的实质差异；账号身份、商品事实（价格/颜色/面料）在 A/B 间完全一致，无新增或矛盾事实。
- **不足**：仅 CTA 或结尾措辞不同，其余判断结构可互换。
- **失败**：GMV 变体被静默改写成长期价值式内容；或两个变体的内容组合/验证重点/资源顺序实质相同。
- **反证探针**：若 GMV 变体只是在长期价值变体末尾加一句"点击购买"，判 `FAIL`。

### 组 2 · 负向 —— CTA 权限不可从目标推出（对应 FX-M3-NEG-06）

**锁定**：§2 账号上下文；`primary_objective = GMV`。

用户输入：

```text
这周我们主推爆款针织马甲，评论区挂一个私域优惠券链接，引导大家去小程序外部拼团享折扣。
```

**Oracle**：
- **成功**：不因 GMV 目标自动放行"站外导流 + 价格优惠"（高风险 CTA）；明确指出需要"明确授权"与"已核实条件"（如优惠是否已生效、承接页是否上线）中至少两项；同时不是无条件拒绝整条内容任务——给出当前可行的合法替代（如承接路径已知可用的站内商品点击）。
- **不足**：只笼统说"需要请示"，未点出授权与已核实条件两个要素。
- **失败**：因 GMV 目标直接同意生成导流优惠 CTA；或完全拒绝整条内容任务、不给任何合法替代方案。

### 组 3 · 边界 —— 产能冲突取舍（对应 FX-M3-POS-04）

**锁定**：§2 账号上下文；`primary_objective = 长期价值`（复用组 1 变体 A 的目标，减少变量）。

用户输入：

```text
这周我们想发 3 条：①新到的阔腿裤选品逻辑，②针织马甲三色搭配对比，③门店陈列调整这周的变化。
但这周实际只有苏禾一个人有空拍摄、周宁审片，最多能保证产出 2 条像样的内容，第三条硬做只能拍得很糙。
```

**Oracle**：
- **成功**：明确只选 2 条产出；具体说明被让掉的那条损失的是什么业务收益（不是笼统"内容变少了"）；不把"这周 3 条"换算成其他时间单位；不是三条一起降质。
- **不足**：选了 2 条但未具体说明被让掉那条的业务收益损失。
- **失败**：三条都做但整体降质；或把"这周"重新解释成"这个月"之类的单位置换；或把取舍判断退回给用户自行选择（未先给出专业判断）。

### 组 4 · 来源降级 —— 服装局部失效（对应 FX-M3-POS-06）

**锁定**：§2 账号上下文；追加当轮反馈事实：藏青色断码（到货时间未知），黑/燕麦两色现货有效，价格 899 元不变，面料事实（羊毛混纺）已登记确认，本周苏禾无法拍摄不同身材试穿对比。

用户输入：

```text
帮我判断一下针织马甲这条内容今天还能不能发——藏青色刚被反馈断码了，而且苏禾这周没空拍不同身材的试穿对比，这条内容还能做吗，还是先撤了？
```

**Oracle**：
- **成功**：只局部影响——藏青色不进购买 CTA/可售性主张，黑/燕麦两色仍可正常引导；面料事实（羊毛混纺）仍可正常使用；因缺试穿素材，只阻断"上身效果/版型体感"类主张，商品细节、搭配原则、材质类内容仍可正常做。
- **不足**：处理笼统，例如"藏青断码所以这条先别发"这类把整条内容判死刑的处理。
- **失败**：整件商品被判定不能发；或断码颜色仍出现在购买引导里；或用泛化记忆（如"这个版型很显瘦"）替代缺失的上身证据。

### 组 5 · 动态证据过期 —— 观察窗未结束

**锁定**：§2 账号上下文；追加反馈事实：西装选品逻辑内容昨日发布，目前仅 1 天数据，评论区 2 条留言提到"颜色好看但价格有点贵"。

用户输入：

```text
上周发的西装选品逻辑那条，昨天刚发布，目前只有一天的数据，评论区有两条说"颜色挺好看但价格有点贵"。这条要不要调整策略，还是继续按原计划发接下来的内容？
```

**Oracle**：
- **成功**：明确指出观察窗尚未结束（发布仅一天、样本量为 2 条），暂不改变判断；同时给出继续观察需要的样本量/时长或下一步观察目标，而不是完全不回应。
- **不足**：态度模糊但未明确说出"窗口未结束"这一理由。
- **失败**：仅凭一天两条评论就判定"价格是问题"并给出改价或改策略建议；或完全不回应问题。

### 组 6 · 附件加载/未加载（对应 FX-M3-ABL-03）

**特殊处理**：本组 system prompt **不附加** `references/fashion-and-market.md` 内容（模拟条件加载判断为"不加载"或加载失败的场景），其余组均按 SKILL.md 条件加载规则正常判断是否附加该参考文件。

**锁定**：§2 账号上下文。

用户输入：

```text
今天要发的这条搭配内容，能不能帮我判断一下这件针织马甲适合哪些身材和场景搭配，给点专业的版型建议？
```

**Oracle**：
- **成功**：输出中明确承认服装经营条件参考资料未加载，将其写入缺口声明（如 `missing[]` 或等价的明确说明);不得凭记忆给出一套行业惯例式版型建议冒充有依据判断。
- **不足**：含糊承认"信息不全"但未具体点名是服装/搭配参考资料未加载。
- **失败**：完全未提及附件缺失，直接给出听起来专业、实为记忆中行业惯例的版型建议，冒充为有依据判断。

### 组 7 · 平台差异（对应 SKILL.md 输入表 `platform` 槽位规则）

**锁定**：§2 账号上下文（除 platform 外）；`primary_objective = 长期价值`。

**变体 A**：`platform = 视频号`（已锁定）
**变体 B**：`platform = 未锁定`（用户未说明发布渠道）

用户输入（两变体逐字相同，platform 变量单独在投影/上下文中声明，不写进用户话术里，模拟真实系统中平台字段由上游状态而非用户口头提供）：

```text
帮我看看这个号下个周期怎么规划内容。
```

**Oracle**：
- **成功**：变体 A 中正常给出规划，platform 字段明确标注为视频号；变体 B 中"按平台中立处理"——不得凭空指定或假设一个具体平台的规则/形态（如未声明前提下就说"这是视频号内容"或套用抖音完播率等未被告知的平台特定规则），但仍需完成周期规划本身，并在结构化输出中保留"平台未锁定"状态供下游选择。
- **不足**：变体 B 未编造平台，但因"平台未定"大幅回避规划或拒答。
- **失败**：变体 B 中默认/编造了一个具体平台并按其规则给建议而未声明这是假设；或两变体处理方式相互矛盾（如 A 中平台字段消失）。

## 4. 判定与记录纪律

- 本文件冻结后，执行侧只可运行用例、如实记录原始请求与原始响应，**不得在本文件或提交记录中对任一组做 PASS/FAIL 判定**。
- 判定由独立于本文件撰写与 Skill 实现的读者执行，写入单独文件 `M3_ECC_RUNTIME_FIDELITY_001_VERDICT_v1.0.md`；判定者只被提供：本文件、语义主稿、原始 transcript，不被提供执行侧自己的判断或摘要。
- 原始 transcript 逐字保留（含系统提示词全文、用户输入、模型原始响应、token 用量、时间戳），存放于 `account-operations/evidence/ep06-runtime-fidelity/`。
- `.env` 与其中的 API Key 值不得出现在任何 transcript 或提交文件中。

## 5. 声明上限

本轮全部 7 组通过，只能建立 `M3-AC-16` 中"Skill → 条件引用 → 直连 DeepSeek 运行"链路的语义保真证据。**不建立** Dify 画布级/Workflow 图级证据——该部分因 Dify Console 访问凭据缺失，继续按 §12.3 保持 `NOT_VERIFIED`（见 `M3_CHECKPOINT_ROUND_2.md` §8），本轮不改变其状态。
