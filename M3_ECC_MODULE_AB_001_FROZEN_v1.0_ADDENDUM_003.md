# `ECC-M3-MODULE-AB-001` 冻结件 · 附录修订 003（**结果之后写的，因此不产生正式 `PASS`**）

> 修订对象：`ADDENDUM_002` §2.3「揭盲时序」。`v1.0`、`ADDENDUM_001`、`ADDENDUM_002` 三份原文
> **一律不覆盖、不删除**；`ADDENDUM_002` 已恢复到冻结原文（`6bbad98`，sha256 `23ba357e5a9d895d…`）。
> 触发事件：独立收口 Reviewer 的阻断项 `B-1`（`M3_INDEPENDENT_CLOSEOUT_REVIEW_V12_v1.0.md` R-1）。
>
> **时序如实声明（本文件最重要的一行）：本文件的全部内容写于本轮 A/B 结果产出之后。**
> 它不是判据，是对"一份判据在结果之后被原地改写"这件事的更正记录。
> 执行侧**不为自己补权威事件**——见 §4，本轮 A/B 的正式等级因此下调。

---

## 1. 发生了什么（时刻逐条可核）

```text
16:29:46  6bbad98  ADDENDUM_002 首次落盘        ← 此时内容合法：早于本轮任何一次 A/B
16:34:38           A/B 第一次运行
16:41:08           A/B 最后一次运行
16:57:54           第一份盲评判定写定
17:10:02           最后一份盲评判定写定（= 判定冻结点）
17:11:43           揭盲脚本读取 sealed 映射（= 揭盲点）
17:28:16  6963829  ★ ADDENDUM_002 被原地改写 ★  ← 判定冻结后 18 分钟、揭盲后 16 分钟
（本次提交）        ADDENDUM_002 恢复原文；被写入的内容移到本文件
```

那次原地改写做了三件事：**删掉** §2.3 第 1 步里「执行侧在第 4 步之前不读它」、**新增** §2.3.1
坦白执行侧已违反该条、**替换**成"36 份提示词逐字同构"。触发事件写了，**新版本号没写**。

两处自相矛盾也随之产生，此处一并作废：

- `ADDENDUM_002` 第 9 行「本修订与其全部参数在本轮任何一次 A/B 判定之前提交」——
  这句话对 `6bbad98` 那一版是真的，对 `6963829` 那一版是**假的**；恢复原文后重新为真。
- 同文件第 10 行写着「原文不覆盖、不删除，本文件作为后继版本追加」的纪律，**而它自己被原地覆盖了**。

**方向上这是"看到结果之后改协议"**，无论替换上去的约束多强，方向不因此改变。

---

## 2. 被删掉的那条，以及它确实被违反了（原样保留，不改口径）

**事实**：在 A/B 运行进行中、判定尚未开始时，执行侧为了核验置换算法是否合法，
**实际计算并打印了完整的单元→(场景,臂) 映射**。`ADDENDUM_002` §2.3 第 1 步
「执行侧在第 4 步之前不读它」，在本轮**已经不成立**。

**不掩盖，也不假装补救**：12 次运行中已完成 6 次，冻结件 §7「禁止重抽」不允许作废重跑，
因此不换种子、不重来。这条协议就是被违反了。

**它到底影响什么，逐条说清：**

| | 是否受影响 |
|---|---|
| 判定者是否知道自己手上是哪条臂 | **不受影响**——36 份判定任务的提示词由同一模板生成，只替换单元号与判定者序号，不含任何身份信息 |
| 判定者能否从别处推出身份 | **不受影响**——他们只拿到一个不透明单元目录，且执行记录被机械核验（独立 Reviewer 复算：0 名越界、0 名跨单元、0 条 git） |
| 执行侧能否在**汇总**阶段做手脚 | **理论上能**——但汇总公式（`ADDENDUM_002` §2.4／§2.5／§2.6）在判定开始前冻结且此后一字节未动；独立 Reviewer 已从 36 份判定原文重算，grades 84 格、hard gates 60 格**零不一致** |
| 执行侧能否在**下发**阶段做手脚 | **这才是被违反那条本该防的风险面** |

---

## 3. 替换上去的约束：可核验，但来得太晚

```text
36 份判定任务的提示词必须逐字同构：两两相比，只允许在单元号（unit_NN）与
判定者序号（j1/j2/j3）两处不同，其余每一个字节相同。
揭盲脚本先做这项核验，不成立就拒绝输出结果。
```

**这条约束本身立得住**，且不是自证——独立收口 Reviewer 从 harness 原始记录取出 36 份提示词，
只按允许的两处归一化后哈希，得到**唯一 1 种形态**（`sha a2c1675531b0a04b × 36`），
覆盖 12 单元 × 3 名，`j1/j2/j3` 各 12。它确实比"执行侧自觉不看"强：**自觉不可核验，同构可核验。**

**但它是在结果已经存在之后才写进协议的。** 按通用内核 A2：
「判据事件必须早于结果事件；判据在看到结果后才定或改，本次运行只算探索，不产生正式 `PASS`。」
**一条事后补上的约束再强，也不能把它保护的那一轮从探索抬成正式取证。**

---

## 4. 因此，本轮 A/B 的正式等级下调（执行侧只往下调，不往上推）

| 项 | 改前 | 改后 | 理由 |
|---|---|---|---|
| `M3-AC-01③`（消融门 B vs B′） | `PASS` | **探索级结论，正式等级 `NOT_VERIFIED`** | 它是唯一一条踩着被后改协议的正式 `PASS`。A2 不允许事后改判据产生正式 `PASS` |
| `M3-AC-18`（专业增益） | `FAIL(INSUFFICIENT)` | **`FAIL(INSUFFICIENT)`（不变）** | 降级不救回任何东西。无论本轮算正式还是算探索，AC-18 都不成为 `PASS` |

**探索级结论的内容如实保留，不删**：删掉 M3 后（`Bprime`，39 字提示词）五维明显变差、零实质退化，
三名判定者的身份猜测完全不跟臂走。这仍然是一次**内容上可信、程序上不合格**的观察。

**两条出路，二选一，都在 Founder 域，执行侧不替他选：**

```text
路径甲  Founder 出具权威事件，明确接受"本轮以事后同构约束替换被违反的自觉条款"，
        并认可推导公式全程未动 + 独立 Reviewer 逐格复现。
        代价：接受一次事后判据替换成为先例。收益：AC-01③ 恢复正式 PASS，零重跑。
路径乙  以本文件（ADDENDUM_003）为冻结判据，重跑 12 次 A/B + 36 名判定者。
        代价：一轮运行与判定成本。收益：AC-01③ 干净地挣回来，不需要任何事后授权。
```

**不选＝AC-01③ 保持 `NOT_VERIFIED`。** 执行侧默认按此记账。

---

## 5. 没有受影响的部分（A3：不多算）

失效只沿依赖边传播，不因为"出了一件坏事"就把整轮作废。逐条列明**继续有效**的：

| 项 | 为什么不失效 |
|---|---|
| `ADDENDUM_002` §2.4（硬门推导）／§2.5（增益门公式）／§2.6（消融门）／§3（不变清单） | 那次 diff **只有一个 hunk**，落在 §2.3；这四节一个字节未动。独立 Reviewer 已核实 |
| 12 次 A/B 运行记录 | 运行早于改写，输入与输出未被触碰 |
| 36 份判定原文 | 全部写定于 17:10:02 之前，早于改写 18 分钟 |
| 判定者隔离性 | 与本条协议无关；独立 Reviewer 亲自读原始执行记录复核为真 |
| `AC-18 = FAIL(INSUFFICIENT)` | 见 §4：改判据没救回它，降级也不改变它 |
| 保真／行为／纵向三条 ECC | 与 A/B 协议无依赖边 |

**受影响的只有一条**：`AC-01③` 的正式等级。

---

## 6. 顺带补上 `ADDENDUM_002` §2.3 第 5 步没做到的那件事（Reviewer 观察 O-4）

原文承诺「推导代码与全部判定原文一并落盘供独立重算」，而仓库里此前只有它们的 sha256 与 mtime。
本次提交把 **36 份判定原文**（`verdict_unit_NN_jN.json`）、**推导脚本**（`unblind_v3.py`）、
**分配表**（`assignments.json`）与 **sealed 映射**（已揭盲，落盘不再泄露任何东西）
一并放进 `account-operations/evidence/ep08-module-ab-v12/`。

**此后任何人只用仓库内材料就能把 §2.4／§2.5 的推导重算一遍**，不必依赖会话 harness 记录。

---

```text
END_MARKER
= M3-ECC-MODULE-AB-001-ADDENDUM-003-END
```
