# 这一份是被 harness bug 中断的那次尝试，保留不删

执行侧把连续性字段从 `dropped_without_notice` 改名为 `not_restated` 时，
漏改了 runner 里的一处打印，序列在 `E01` 写完记录之后、进入 `E02` 之前崩掉
（`KeyError: 'objects_dropped_without_notice'`）。

- **崩溃发生在任何判定之前**，与模型输出内容无关，是纯粹的执行侧代码错误；
- 这一份 `E01` 的原始记录**逐条保留**，不删不改，但**不进入本轮判定**；
- 序列按冻结件 §5「序列不可局部重跑」整条从第 1 步重跑，重跑记录在上级目录。

如实登记的一点：执行侧在重跑前**已经看过**这一份 `E01` 的输出。
它不构成"看到结果后择优"——重跑的原因是 harness 崩溃，不是结果不理想，
而且 `E01` 那次的 workflow 状态是 `succeeded`。这一条留给独立收口审查者核验。
