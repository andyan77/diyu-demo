# 未跑完的盲评包（余额耗尽，2026-08-26）

`FX-M3-HOLD-02` 缺 `B` / `Bprime` 两臂，`FX-M3-HOLD-03` 四臂全缺——原因是 DeepSeek 账户
余额在本轮运行中途耗尽（`GET /user/balance` → `total_balance: -1.06 CNY`，
`is_available: false`；失败请求返回 `402 Insufficient Balance`）。

这两个场景的盲评包因此**不完整**，移出 `blind/` 以免判定者拿到空白对照臂并据此产生
无意义的比较。**已跑完的 6 次运行原始记录逐条保留在上级目录**，未删除、未重跑、未择优。

按 `M3_ECC_MODULE_AB_001_FROZEN_v1.0.md` §7："未跑完 → `NOT_VERIFIED`，不得用部分场景
宣称通过。" 因此 `M3-AC-18` 与 `M3-AC-01③` 本轮**不得** `PASS`。

余额恢复后的正确做法：补齐 `FX-M3-HOLD-02` 的 B/Bprime 与 `FX-M3-HOLD-03` 全部四臂，
**四臂十二次运行的绑定不变**（同 model／参数／系统提示词哈希），然后一次性对三个场景
做完整盲评。不得只补跑失败的那几次、再与本轮已有结果混判——那会让"哪次采样进入判定"
变成事后选择。
