#!/usr/bin/env python3
"""生成绑定到最终候选的 Founder 亲自实测包。

Founder 第 7 条逐字要求：每个需要他判断的场景必须提供
  精确 Dify 入口 · 完整三格输入 · 操作步骤 · 系统实际输出 ·
  场景含义 · 业务影响 · 自然语言判断问题
并且**只请求产品语义接受或退回，不请求技术方案裁决**。

因此这份文件由脚本从证据文件生成，不手抄：三格输入与系统实际输出逐字取自
运行记录，Founder 在 Dify 里粘贴的就是我们跑过的那一份。手抄会引入差异，
而"他测的和我们测的是不是同一件事"正是这份包唯一的价值。
"""
import json, os, textwrap

WT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
EV = os.path.join(WT, "account-operations/evidence")
FREEZE = json.load(open(os.path.join(EV, "ep23-candidate-v14-freeze/CANDIDATE_FREEZE_v1.4.json"),
                        encoding="utf-8"))
OUT = os.path.join(WT, "M3_FOUNDER_DIFY_TEST_PACK_v1.2.md")

# 每一场景：证据文件 · 标题 · 场景含义 · 业务影响 · 给 Founder 的自然语言问题
# 挑选原则：只放**需要产品语义裁决**的，技术结论一律不进来。
SCENES = [
    ("ep06b-runtime-behavior-v14/B03-1-objective-01.json", "operating_judgment",
     "主目标＝长期价值时，它怎么排一周",
     "这是最常见的一轮：目标明确、产能够用、没有异常。看它排出来的东西像不像一个懂行的人排的。",
     "如果这一轮的输出你不认，那后面所有更难的场景都不用看了——这是地板。",
     "这份东西，你愿意照着它去安排下周的拍摄吗？如果不愿意，是哪一句让你不愿意？"),

    ("ep06b-runtime-behavior-v14/B07-P-daily-three.json", "operating_judgment",
     "用户说「我们想每天发 3 条」，而实际产能是每周 3 条",
     "你在第 3 条里定的口径：允许把「每天 3 条」换算成「每周约 21 条」来做同单位比较，"
     "禁止改写成「每周 3 条」或以其他方式缩小目标。这一场景就是那条口径的现场。",
     "换算错了，用户的目标会被系统悄悄砍掉 85%，而他看不出来。",
     "它有没有把你的目标偷偷改小？它讲清楚「你要的」和「现在能做到的」差在哪里了吗？"),

    ("ep06b-runtime-behavior-v14/B09-5-no-market-data.json", "operating_judgment",
     "什么外部市场资料都没有的时候（本轮它没给出正文）",
     "这一例是专门用来测「资料不足时不得整任务拒绝」的。本轮它**没有产出任何运营判断**，"
     "用户看到的是一句系统说明，让你补齐输入再来。",
     "真实经营里，资料不全是常态。一遇到资料不全就整轮停摆，这个能力在你店里就用不起来。",
     "你看到这样一句回复，会怎么做？这是你能接受的「说不知道」，还是你认为它本该在没有市场资料的情况下也给出点什么？"),

    ("ep06b-runtime-behavior-v14/B14-1-brief-consumable.json", "operating_judgment",
     "内容任务交给下游拍摄时，事实是谁确认的",
     "输出把「已核实可用的商品事实」列了出来，但没有写这些事实是谁确认的，也没有说「这里缺一个确认人」。"
     "独立判定者按冻结判据判为「不足」。",
     "拍摄团队拿到一张没有确认人的事实清单，出了错找不到人；而系统也没有提醒你这一栏是空的。",
     "这张任务单直接发给拍摄的人，你放心吗？「谁确认的」这一栏空着，对你来说是小事还是不能接受？"),

    ("ep06b-runtime-behavior-v14/B02-4-blocking-dependency.json", "operating_judgment",
     "三类产能被系统自己的检查压成了一句话",
     "这一例的模型草稿本来写全了三个数（期望发布量、基线产能、当期实际产能），"
     "被我们这一轮新加的一道检查误判，补齐环节把三句压成了一句「输入给的期望发布数是每周三条」。"
     "**这是执行侧的缺陷造成的降质**，不是模型写不出来。",
     "三个数分开讲，你才知道「想发多少」「平时能发多少」「这周实际能发多少」差在哪、要让掉什么。压成一句，取舍就看不见了。",
     "只看这份输出，你还看得出「想发的」和「实际能发的」差多少吗？这样的表达对你够不够用？"),

    ("ep07-longitudinal-v14/E06.json", "operating_judgment",
     "连续第 6 周：多条一致的反馈进来之后（这一份被系统整份拒收了）",
     "模型这一步写出了合格的判断（1652 字），但被我们新加的那道检查误报命中，整份被拒收，"
     "于是它这一步独有的三项安排在后面 6 步里再没出现过。",
     "持续运营最怕的就是「上周定的事下周没人记得」。这一次是系统自己把它弄丢的。",
     "假设这份判断当时没被拒收——它写的东西，你会希望它在接下来几周继续被记着吗？"),

    ("ep07-longitudinal-v14/E11.json", "operating_judgment",
     "一场活动结束之后，要不要接着按活动那套来",
     "活动期间的安排到期了。系统要决定：恢复到活动前的老计划，还是恢复到活动期间已经合法改过的当前计划，"
     "还是接着沿用活动那套。三种做法在生意上完全不同。",
     "恢复错了，等于把活动期间学到的东西全丢掉，或者把临时安排永久化。",
     "活动结束后它给的这个安排，是你想要的那一种吗？"),
]


def read(path, key):
    d = json.load(open(os.path.join(EV, path), encoding="utf-8"))
    o = d["raw_response_body"]["data"]["outputs"]
    return d, o.get(key) or ""


def fence(text, lang="text"):
    return f"```{lang}\n{text.rstrip()}\n```"


def main():
    dfy = FREEZE["dify"]
    parts = [textwrap.dedent(f"""\
        # M3 单账号持续运营 · Founder 亲自实测包 **v1.1**

        > 这一版**绑定到最终候选**，与 v1.0 不是同一套东西：v1.0 测的是载体 v1.2，
        > 本版测的是 v1.4。下面每一处三格输入与系统输出都逐字取自实际运行记录，
        > 不是重新誊写的——你在 Dify 里粘进去的，就是我们跑过的那一份。

        ```text
        Dify App     {dfy['app_id']}
        已发布版本    {dfy['published_version_name']}
        草稿图哈希    {dfy['draft_graph_hash']}
        SKILL.md     sha256 {FREEZE['source_sha256']['SKILL.md']}
        模型          deepseek-v4-flash · temperature 0.4
        ```

        **这份包只请你做一件事：对产品语义说「接受」或「退回」。**
        技术结论（哪条 AC 通过、终态是什么）已由冻结判据与证据推导完毕，不需要你裁决，
        也不该由你裁决。你只回答「这东西在我的生意里好不好使」。

        ---

        ## 1. 怎么打开

        1. 浏览器打开 Dify，进入 App `{dfy['app_id']}`；
        2. 左侧选 **Workflow**，右上角确认版本是 **`{dfy['published_version_name']}`**
           （如果不是，在版本列表里切过去——**不要点发布**）；
        3. 右上角 **运行 / Run**，会出现三个输入框。

        ## 2. 三个输入框分别是什么

        | 框 | 名字 | 是什么 |
        |---|---|---|
        | 第一格 | `account_context` | 账号上下文。真实使用时由 M2 自动投影进来，这里我们手工粘贴，内容与真实投影一致 |
        | 第二格 | `user_request` | 你这一轮想让它干什么，一句话 |
        | 第三格 | `loaded_references` | 本轮附了哪些参考资料的清单。**照抄即可**，它不是给你填的 |

        > 每个场景下面都给了这三格的完整内容。**逐格复制粘贴，不要改动**——
        > 改一个字，你看到的就不是我们测过的那一次了。

        ---

        ## 3. 场景

        每个场景的结构一样：先给你三格输入，再告诉你我们跑出来的实际输出，
        然后说这个场景在测什么、错了会怎样，最后是要你回答的那个问题。

        **你可以只读「系统实际输出」直接回答问题，也可以自己在 Dify 里跑一遍。**
        自己跑的话，模型是有随机性的，输出不会逐字相同——这很正常，
        你要看的是它的**判断**对不对，不是字面。
        """)]

    for i, (path, key, title, meaning, impact, question) in enumerate(SCENES, 1):
        d, body = read(path, key)
        wi = d["workflow_inputs"]
        parts.append(textwrap.dedent(f"""\
            ---

            ### 场景 {i} · {title}

            **第一格 `account_context`**

            {fence(wi['account_context'])}

            **第二格 `user_request`**

            {fence(wi['user_request'])}

            **第三格 `loaded_references`**

            {fence(wi['loaded_references'])}

            **操作**：三格粘好 → 点运行 → 等 1–5 分钟（这个模型慢，属正常）→ 看 `operating_judgment`。

            **我们跑出来的实际输出**

            {fence(body)}

            **这个场景在测什么**：{meaning}

            **错了会怎样**：{impact}

            **请你回答**：{question}
            """))

    parts.append(textwrap.dedent("""\
        ---

        ## 4. 跑完之后的三个总问题

        1. 这七个场景里，**哪一个**让你觉得「这东西真懂我的生意」？如果一个都没有，直说。
        2. 哪一个让你觉得「这我不敢用」？具体是哪句话。
        3. 如果现在只能二选一：**接受**（认为产品语义可用，进入下一步）还是
           **退回**（认为产品语义不可用，需要重做）？

        ## 5. 有两件事我先说清楚，免得你误判

        **第一，场景 5 和场景 6 里的问题是我们造成的，不是模型不行。**
        这一轮我们新加了一道检查，本意是防止系统拿上一轮的旧数字压过你这一轮给的新数字。
        实际跑下来，这道检查在 61 次运行里命中 12 次，**其中 11 次是误报**，
        6 次真的把合格的交付整份拒收了。被它拦下的恰恰是我们要保护的那些正确行为——
        包括你在第 3 条里点名授权的那个「每天 3 条 = 每周约 21 条」的换算。
        这是执行侧的缺陷，已登记，等你决定要不要专门修一轮。

        **第二，有一条结论对我们不利，但必须让你知道。**
        我们做了 12 次 A/B、36 名互不通气的盲评者，比较「有 M3」和「没有 M3、只给一句话提示」。
        结果是：**去掉 M3 明显变差**（七维里六维变差），
        但**M3 相对一份写得很好的通用提示词，没有整体增益**（七维里六维「相当」）。
        换句话说，M3 的价值目前被证明的是「比什么都不做好很多」，
        还没有被证明是「比一份好提示词更好」。这一条换了协议、换了载体、重招判定者之后复现了两次。

        这两件事都不需要你现在裁决对错，但你在回答上面三个问题时应该知道它们。
        """))

    open(OUT, "w", encoding="utf-8").write("\n".join(parts))
    print(f"written: {OUT}")
    print(f"场景数 {len(SCENES)}，全文 {len(''.join(parts))} 字")


if __name__ == "__main__":
    main()
