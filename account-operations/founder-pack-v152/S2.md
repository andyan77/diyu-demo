# S2 · 上个周期走长期价值，这个周期改以到店为主，GMV 与线索都还在

> 这是七个场景里的第 2 个。**只跑一次。**

## 1 这一格在模拟什么真实问题

同一账号同时有 GMV、线索、到店三条有效路径；上个周期偏长期价值，本周期明确以到店为主。

## 2 唯一的主要验收目的

三类转化会不会被压成同一个'转化'，长期基线会不会被本周期的目标切换冲掉。

## 3 对应的产品义务

- GMV、线索、到店分别处理，不得统称转化
- 保留上一周期的长期基线与在跑的持续位
- 按到店组织本周期任务，承接路径要真实存在

## 4 这一格会碰到的硬失败条款

- 把 GMV、线索、到店压成同一个转化
- 把长期价值改写成廉价流量内容
- 持续位无声消失

**这一格属于整体硬失败判断**：是（命中上面任何一条，按合同整组不得判 PASS）。

## 5 输入从哪来（可复算）

- 来源类型：由冻结记录机械改写（只改目标槽位与用户这一句话）
- 来源记录：`account-operations/evidence/ep07-longitudinal-v15/E08.json`
- 来源记录 SHA-256：`a610cfb650d4e135284aaec4e32c1f97d91b6a680fd368b327290cb820b7c2d8`

**机械改写处，逐条列明：**

1. account_context：逐字取自 E08 记录，**只替换两行** —— `primary_objective: 长期价值` → `到店（本周期主要目标）`；`secondary_objectives: 未提供` → `GMV（次要，有限次）；线索（次要，有限次）`。替换用词全部是冻结用例集里已经出现过的写法（B03-6 线索 / B03-7 到店 / B04-1P '（次要，有限次）'）。
2. standing_cycle_baseline 与 standing_positions 逐字保留 —— '上个周期偏长期价值'这件事就由这段真实的上一轮基线承载，不是我写的设定。
3. user_request：本场景唯一新写的一句话，只陈述目标切换，不含任何商品、库存、价格、面料、顾客或经营事实。

## 6 三个输入文件与逐字核验

| 输入框 | 文件 | SHA-256 | 字符数 | 写回读出逐字节一致 |
|---|---|---|---|---|
| 账号上下文（M2→M3 最小投影） | `S2_account_context.txt` | `20a1b0642f73e1dc…` | 2190 | 是 |
| 用户本轮请求（自然语言） | `S2_user_request.txt` | `a39e2b9a0cf74942…` | 66 | 是 |
| 参考文件加载清单 + 本轮已加载的条件附件全文 | `S2_loaded_references.txt` | `be47ecc91770971b…` | 2935 | 是 |

## 7 怎么跑

1. 打开 Dify 控制台 <http://localhost/apps>，找到应用
   **M3 单账号持续运营候选 | …CANDIDATE TEST ONLY**（App ID `b7fb5b1a-9278-426c-bb8a-f9f288639548`）。
2. **先确认加载的是本次冻结的候选**：进入该应用 → 右上角「发布」下拉 → 看版本记录，
   最新一条的名字必须是 `m3-cand-v1.5.2`，发布时间 `2026-08-27 19:46:47.281053`。不是这一条就先停下告诉我，不要开始跑。
3. 回到应用「概览」页，点应用访问地址旁边的「预览」，打开这个应用**已发布版本**的运行页
   （不要用画布右上角那个「运行」按钮——那个跑的是草稿）。
4. 运行页上有三个输入框，按下面这张表逐字粘贴。**三个都要贴，一个都不能少**：

   | 输入框标题 | 贴哪个文件 |
   |---|---|
   | 账号上下文（M2→M3 最小投影） | `inputs/S2_account_context.txt` |
   | 用户本轮请求（自然语言） | `inputs/S2_user_request.txt` |
   | 参考文件加载清单 + 本轮已加载的条件附件全文 | `inputs/S2_loaded_references.txt` |

5. 点「开始运行」。**不要改任何参数、不要改输入、不要改设置。**
6. 跑完的标志：页面出现结果，左侧「追踪」里 7 个节点全部走完。
   正常一次大约 1～3 分钟。
7. 保存下面这些，放进 `results/S2/`：
   - 完整原始输出全文（`operating_judgment` 那一大段，**别删别改别摘要**）；
   - 运行 ID、运行时间、模型名、token 数（在「追踪」页右上角）；
   - 整页截图 1 张 + 「追踪」页截图 1 张。
8. 记完就换下一个场景。**一个输入只跑一次。**

## 8 三种结局怎么区分

- **正常完成**：7 个节点走完，页面出结果。→ 记录，进下一场景。
- **纯传输故障**：请求根本没进模型节点、没有任何模型输出（网络断、SSL 错误、
  Dify 明说服务不可用）。→ **只有这一种**允许用同一份输入重跑一次，
  且第一次失败的截图和重跑结果都要留下。
- **产品失败**：跑完了，但输出不好、为空、漏内容、违反要求、被闸门拒收、或者你不满意。
  → **不重跑**，按第一次的真实结果记下来。这条是你自己在第七节定的规矩。

## 9 你要看的问题（自然语言，不用管内部字段）

1. 到店、GMV、线索这三件事，它有没有分开处理，还是混成一个'转化'？
2. 上个周期那几个还在跑的持续位，它有没有逐个交代（继续／缩减／暂停／退出／被替换）？
3. 它有没有为了到店，把长期价值那条线直接抹掉？

## 10 执行侧的初步专业判断

本组合没有历史运行记录（S2 是本轮新编译的）。执行侧不预判结果。

## 11 结果放哪

- 原始输出全文：`account-operations/founder-pack-v152/results/S2/raw_output.txt`
- 运行信息：`results/S2/run_meta.json`（字段：run_id、started_at、model、total_tokens、elapsed_seconds）
- 截图：`results/S2/screen_result.png`、`results/S2/screen_trace.png`
- 纯传输故障（如有）：`results/S2/transport_failure_1.txt` + 重跑件一并保留

---

候选 `v1.5.2` · App `b7fb5b1a-9278-426c-bb8a-f9f288639548` · 已发布版本 `m3-cand-v1.5.2` （2026-08-27 19:46:47.281053）· 图 `91980f1aae8c988f…` · 生成时 Git HEAD `5e1b6ee9b8d3`
