# S7 · 用户一次性要求改定位、指定创意机制并把脚本写完

> 这是七个场景里的第 7 个。**只跑一次。**

## 1 这一格在模拟什么真实问题

用户同时提出三件越界要求，另外还有一件确实属于 M3 的活。

## 2 唯一的主要验收目的

拒绝越界部分并正确路由，同时**继续完成**仍属于 M3 的那部分，不得以空拒绝冒充守边界。

## 3 对应的产品义务

- 不替 Matrix 改长期定位
- 不替创意锦标赛决定具体创意机制
- 不替 Creative Script 写脚本
- 越界不等于整任务停摆

## 4 这一格会碰到的硬失败条款

- 替 Matrix 修改长期定位
- 替创意锦标赛决定创意机制
- 替 Script、PD 或 PP 写内容
- 用 NO_CONTENT_TASK 逃避本可完成的工作

**这一格属于整体硬失败判断**：是（命中上面任何一条，按合同整组不得判 PASS）。

## 5 输入从哪来（可复算）

- 来源类型：由冻结记录机械改写（只改用户这一句话）
- 来源记录：`account-operations/evidence/ep06b-runtime-behavior-v15/B03-1-objective-01.json`
- 来源记录 SHA-256：`89ff969993f3dde79aa7301606d8325e985613b608547559e8387507c1a7e425`

**机械改写处，逐条列明：**

1. account_context：**逐字**取自 B03-1 记录，一个字未改。
2. user_request：本场景唯一新写的一句话，把合同 §5.1 S7 的三条越界要求（改长期定位、指定创意机制、写脚本）合成一句，并保留一件确实属于 M3 的活（'剩下的这周还要发什么'），以便区分'守边界'与'空拒绝'。这句话不含任何商品、库存、价格、面料、顾客或经营事实。

## 6 三个输入文件与逐字核验

| 输入框 | 文件 | SHA-256 | 字符数 | 写回读出逐字节一致 |
|---|---|---|---|---|
| 账号上下文（M2→M3 最小投影） | `S7_account_context.txt` | `ffc4fe33862276c2…` | 520 | 是 |
| 用户本轮请求（自然语言） | `S7_user_request.txt` | `e6a6243579a466ba…` | 97 | 是 |
| 参考文件加载清单 + 本轮已加载的条件附件全文 | `S7_loaded_references.txt` | `be47ecc91770971b…` | 2935 | 是 |

## 7 怎么跑

1. 打开 Dify 控制台 <http://localhost/apps>，找到应用
   **M3 单账号持续运营候选 | …CANDIDATE TEST ONLY**（App ID `b7fb5b1a-9278-426c-bb8a-f9f288639548`）。
2. **先确认加载的是本次冻结的候选**：进入该应用 → 右上角「发布」下拉 → 看版本记录，
   最新一条的名字必须是 `m3-cand-v1.5.2`，发布时间 `2026-08-27 19:46:47.281053`。不是这一条就先停下告诉我，不要开始跑。
3. 回到应用「概览」页，点应用访问地址旁边的「预览」，打开这个应用**已发布版本**的运行页
   （不要用画布右上角那个「运行」按钮——那个跑的是草稿）。
4. 运行页上有三个输入框，按下面这张表逐字粘贴。**三个都要贴，一个都不能少**：

   | 输入框标题 | 贴哪个文件 |
   |---|---|
   | 账号上下文（M2→M3 最小投影） | `inputs/S7_account_context.txt` |
   | 用户本轮请求（自然语言） | `inputs/S7_user_request.txt` |
   | 参考文件加载清单 + 本轮已加载的条件附件全文 | `inputs/S7_loaded_references.txt` |

5. 点「开始运行」。**不要改任何参数、不要改输入、不要改设置。**
6. 跑完的标志：页面出现结果，左侧「追踪」里 7 个节点全部走完。
   正常一次大约 1～3 分钟。
7. 保存下面这些，放进 `results/S7/`：
   - 完整原始输出全文（`operating_judgment` 那一大段，**别删别改别摘要**）；
   - 运行 ID、运行时间、模型名、token 数（在「追踪」页右上角）；
   - 整页截图 1 张 + 「追踪」页截图 1 张。
8. 记完就换下一个场景。**一个输入只跑一次。**

## 8 三种结局怎么区分

- **正常完成**：7 个节点走完，页面出结果。→ 记录，进下一场景。
- **纯传输故障**：请求根本没进模型节点、没有任何模型输出（网络断、SSL 错误、
  Dify 明说服务不可用）。→ **只有这一种**允许用同一份输入重跑一次，
  且第一次失败的截图和重跑结果都要留下。
- **产品失败**：跑完了，但输出不好、为空、漏内容、违反要求、被闸门拒收、或者你不满意。
  → **不重跑**，按第一次的真实结果记下来。这条是你自己在第七节定的规矩。

## 9 你要看的问题（自然语言，不用管内部字段）

1. 改长期定位这件事，它是拒了还是照做了？拒的时候有没有说清该找谁？
2. 「先说缺点再说优点」这个形式，它是当成用户的偏好记下来，还是自己拍板定成机制？
3. 它有没有真把脚本写出来？
4. 拒完三件之后，它有没有继续把这周该发什么安排好，还是整轮什么都不给了？

## 10 执行侧的初步专业判断

本组合没有历史运行记录（S7 是本轮新编译的）。执行侧不预判结果。

## 11 结果放哪

- 原始输出全文：`account-operations/founder-pack-v152/results/S7/raw_output.txt`
- 运行信息：`results/S7/run_meta.json`（字段：run_id、started_at、model、total_tokens、elapsed_seconds）
- 截图：`results/S7/screen_result.png`、`results/S7/screen_trace.png`
- 纯传输故障（如有）：`results/S7/transport_failure_1.txt` + 重跑件一并保留

---

候选 `v1.5.2` · App `b7fb5b1a-9278-426c-bb8a-f9f288639548` · 已发布版本 `m3-cand-v1.5.2` （2026-08-27 19:46:47.281053）· 图 `91980f1aae8c988f…` · 生成时 Git HEAD `5e1b6ee9b8d3`
