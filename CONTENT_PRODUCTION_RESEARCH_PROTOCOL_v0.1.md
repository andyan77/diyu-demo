# CONTENT_PRODUCTION_RESEARCH_PROTOCOL v0.1

> 本文件是 `P02`—`P04` 行业研究与专家对抗的统一协议，
> 由任务 `CONTENT-PRODUCTION-P01` 第七节机械落盘。
>
> 执行侧只负责落盘与实现，不得修改本文件的业务含义。本文件在 P02 开始前冻结。
>
> 本文件是治理文件，不是模型输入。它不进入 Skill、不进入 Dify System Prompt、
> 不进入 User Prompt、不进入任何固定运行输入。
>
> 产品定义见 [`CONTENT_PRODUCTION_CHAIN_PRD_v0.1.md`](CONTENT_PRODUCTION_CHAIN_PRD_v0.1.md)；
> 字段与状态见 [`CONTENT_PRODUCTION_CHAIN_CONTRACT_v0.1.md`](CONTENT_PRODUCTION_CHAIN_CONTRACT_v0.1.md)。

## 0. 性质、位置与口径校准

### 0.1 新文档类型声明

仓库此前**没有** `_PROTOCOL` 后缀先例，也没有「研究协议」这一文档类型。
最接近的既有治理件是 `CONTENT_BRIEF_NEGATIVE_PROBES_v0.1.md`（固定测试规程）
与 `CAMPAIGN_EVAL_SCORECARD_v0.1.md`（空白记录卡）。

本文件属**新增文档类型**，由 `CONTENT-PRODUCTION-P01` 引入，
不得被描述为既有约定。

### 0.2 与「不继续全网调研」的口径关系

- **既有表述**：`CLAUDE.md` 第 4 节 verbatim「不继续全网调研。」
  《笛语项目基线》第三节 verbatim「调研已经完成并冻结。」

- **作用域**：两句指向的是《笛语调研报告：全网 IP 矩阵》——
  一份关于**IP 账号定位与矩阵**的调研，已冻结，且基线第零节明确它
  「不直接进入 Skill」。禁止的是重做那份定位层调研、或用它倒推现行规则。

- **本协议的对象不同**：`P02`—`P04` 研究的是**内容生产工艺**——
  脚本、分镜与现场执行、发布包装。这是内容生产链新建能力所需的题材，
  从未被调研过，也不在已冻结调研报告的覆盖范围内。

- **授权来源**：`CONTENT-PRODUCTION-P01` 第 4.6 节 Founder 已确认决定 verbatim：
  「执行代理在 P02—P04 中仍可以使用合法网络搜索开展项目研究；
  这不等于把搜索工具接入产品。」

- **结论**：不构成冲突。但**边界必须守住**：
  research 只产生**研究资产**，不产生产品能力，
  不得因此把搜索工具、账号监测或平台数据接口接入产品（见第 12 节与 PRD 第 14 节）。

### 0.3 反面先例与自我约束

《笛语项目基线》第七节 verbatim 记录：

> M2 由 Founder 主动挂起，根因是治理装置变成了工作本身
> （49 判据 / 616 夹具 / 697 错误码，真交付物 0 件）。

据此，本协议受以下自我约束：

- 研究以**能否改变一份 Skill 的写法**为唯一价值判据；
- 不建判据库、不建样本库、不建评分器、不追求样本数量；
- `P02`—`P04` 的交付物是**Founder 裁决卡**，不是研究报告的厚度；
- 出现「研究变成工作本身」的迹象时，应停止扩样并交付当前结论。

## 1. P02—P04 研究目标

### 1.1 各阶段唯一问题

| 阶段 | 对应 Skill | 唯一研究问题 |
|---|---|---|
| `P02` | Creative Script Architect | 把一条已确定判断的内容，写成可拍摄脚本时，哪些判断是通用模型默认不会稳定做出的？ |
| `P03` | Production Director | 把一份脚本变成可执行制作包时，哪些判断是通用模型默认不会稳定做出的？ |
| `P04` | Publishing & Packaging Architect | 把一条成片变成可发布包装时，哪些判断是通用模型默认不会稳定做出的？ |

### 1.2 生死题（每条结论都要过）

依《笛语项目基线》第一节：

> 企业为什么不能直接用 GPT 项目文件夹完成？答不上来的功能不做。

因此每一条候选结论必须回答：**通用模型默认会不会稳定做出这个判断？**

- 会 → 归类为 `REJECTED`，理由写「通用模型默认行为」；
- 不会 → 才有资格进入后续分类。

《笛语项目基线》第三节 verbatim：
「Skill 应只保存通用模型默认不会稳定做出的专家判断。
普通文案知识、短视频常识和题面案例结论，不因被问题库提及就成为专家资产。」

### 1.3 明确不研究

- 不研究 IP 账号定位与矩阵（已冻结）；
- 不研究市场规模、竞品与商业模式；
- 不研究发布后数据表现与增长方法（发布反馈学习已延期）；
- 不研究平台算法推荐机制的内部原理；
- 不研究如何规避平台规则。

## 2. 来源结构

每个阶段的证据必须覆盖以下六类来源。缺任一类，须在裁决卡中写明缺口与原因。

| 来源类 | 说明 | 主要产出等级 |
|---|---|---|
| 官方规则 | 平台公开规则、规范、帮助中心、公告 | `OFFICIAL_RULE` |
| 头部 IP | 该品类公认头部账号 | `OBSERVED_PATTERN` |
| 同类 IP | 与目标企业同业态、同规模的账号（当前实例为服装零售、2 家直营门店量级） | `OBSERVED_PATTERN` |
| 中腰部账号 | 有稳定产出但未成头部的账号 | `OBSERVED_PATTERN` |
| 普通样本 | 大量存在的常规做法 | `OBSERVED_PATTERN` |
| 失效样本 | 明显做错、被处罚、或长期无效的做法 | `OBSERVED_PATTERN` |

### 2.1 失效样本不得省略

失效样本是本协议**最重要**的一类，不得因为难找而跳过。

理由：《笛语项目基线》第一节把「专家约束与失败案例」列为四类护城河资产之一。
只看成功样本会得出「大家都这么做」的通用常识，那正是 `REJECTED` 的内容。

### 2.2 头部样本的使用限制

头部 IP 的做法**不得**因其头部身份自动升级为规则。
头部账号常有普通企业不具备的资源、人设与历史积累；
其做法必须能在 Production Profile 的现实档位下复现，才可能进入 `CONDITIONAL_RULE`。

## 3. 样本纳入、排除与停止扩样

### 3.1 纳入条件（全部满足）

1. 与本阶段唯一研究问题直接相关；
2. 可被独立核对（有可记录的来源）；
3. 观察内容能落到**可操作的制作判断**，不是风格感受；
4. 在当前 Production Profile 的现实档位下可讨论。

### 3.2 排除条件（任一命中即排除）

1. 只能得出通用模型默认行为；
2. 无法核对来源；
3. 依赖平台内部数据或非公开信息；
4. 依赖违规手法、诱导跳转、暗号或藏码；
5. 依赖该账号独有的不可复制资源（明星、独家场地、超额预算）；
6. 只能证明「有人这么做」，不能说明**在什么条件下成立**；
7. 属于已冻结调研报告已覆盖的定位层结论。

### 3.3 停止扩样条件（任一成立即停）

1. 连续两轮新增样本**不再产生新的候选结论**；
2. 本阶段已形成的候选结论足以覆盖对应 Skill 的必答判断；
3. 新增样本只增加同类 `OBSERVED_PATTERN`，不改变任何分类；
4. 已出现「研究变成工作本身」的迹象（见 0.3）。

停止扩样必须记录：停在第几轮、停止依据是哪一条、当前样本分布。
**不得**以「再多找一些更保险」为由无限扩样。

## 4. 证据登记字段

每一条证据必须登记以下字段，缺任一项即不得进入 R1 之后的任何一轮：

| 字段 | 说明 |
|---|---|
| `evidence_id` | 证据编号 |
| `stage` | `P02` / `P03` / `P04` |
| `source_type` | 第 2 节六类来源之一 |
| `source` | 来源名称 |
| `date` | 观察日期 |
| `account` | 账号名称；官方规则类写发布主体 |
| `url` | 可核对地址 |
| `observation` | 观察到什么（客观描述，不含评价） |
| `applicable_conditions` | 在什么条件下成立 |
| `evidence_level` | 六级证据等级之一，定义见 PRD 第 8.3 节与合同第 11.2 节；**不是**本协议第 9 节的输出分类 |
| `capture_mode_required` | 复现该做法所需的最低 Capture Mode，按合同第 6.2.1 节的资源强度次序判定；不新增拍摄即可复现的写 `ASSET_REUSE` |
| `counter_evidence` | 已知反例；无则写「无」 |

### 4.1 observation 与 judgment 必须分开

`observation` 只写**看到了什么**。
「为什么有效」「应该这样做」属于判断，只能出现在 R1 之后的方案里，
并按 `EXPERT_JUDGMENT` 或 `MODEL_HYPOTHESIS` 分级。

把判断写进 `observation`，是本协议最常见的失误，也是 R2 交叉质询的必查项。

### 4.2 FUTURE_HYPOTHESIS 的附加字段

等级为 `FUTURE_HYPOTHESIS` 时，必须另加：
`来源`、`适用条件`、`复查日期`、`失效条件`、`后续验证方式`。缺任一项即不得登记。

## 5. 版权与非模仿边界

1. 登记的是**做法与条件**，不是他人的具体内容；
2. 不得抄录他人脚本、台词、标题、封面文案或分镜作为可复用素材；
3. 不得以「参考」为名生成与特定账号高度相似的成品；
4. 不得复制他人人设、口头禅、标志性话术或视觉标识；
5. 引用他人内容只能是**最小必要片段**，且必须标明来源与观察目的；
6. 研究结论进入 Skill 时，只能是**抽象后的判断方法**，不得携带任何具体账号的原文；
7. 不得把某个账号的做法写成规则并暗示企业照做。

判据：**可以学会怎么判断，不可以搬走别人做过的东西。**

## 6. 独立研究上下文要求

沿用 `C1`—`C6` 已经跑通的「只接收／不接收」白名单纪律。

### 6.1 R1 独立方案作者只接收

- 本协议；
- `CONTENT_PRODUCTION_CHAIN_PRD_v0.1.md`；
- `CONTENT_PRODUCTION_CHAIN_CONTRACT_v0.1.md`；
- 本阶段唯一研究问题；
- R0 已登记的证据集；
- 已确认的账号责任卡、品牌事实与资源夹具（仅作为可行性参照）。

### 6.2 R1 独立方案作者不接收

- 其他作者的方案；
- 任何期望结论、候选答案或倾向性提示；
- 上一阶段的裁决卡；
- 已写好的 Skill 草稿；
- 评分标准与 Golden；
- 主持人对「哪个方向更好」的任何暗示。

### 6.3 独立性的判据

若两份 R1 方案出现**同一处非常规措辞**，视为上下文泄漏，
该轮作废并重新生成，不得挑着用。

## 7. 作者、综合者与评审者隔离

| 角色 | 做什么 | 明确不做 |
|---|---|---|
| 作者（R1） | 独立提出完整方案 | 不看他人方案；不参与综合；不给自己的方案打分 |
| 质询者（R2） | 对匿名方案提出可证伪的质询 | 不知道方案作者是谁；不提出替代方案 |
| 反事实设计者（R3） | 设计单变量反事实 | 不裁决结论 |
| 红队（R4） | 攻击已存活结论 | 不负责修补 |
| 综合者（R5） | 条件化综合 | 不新增未经任何一轮检验的结论；不做多数票 |
| 评审者 | 检查协议是否被遵守 | 不评价内容好坏；不替 Founder 裁决 |

### 7.1 硬隔离规则

1. 同一执行上下文**不得**同时承担作者与综合者；
2. 同一执行上下文**不得**同时承担红队与被攻击方案的作者；
3. 评审者不得参与前五轮任何一轮；
4. R2 质询必须匿名：方案以「方案 A／方案 B／方案 C」标识，
   判完再揭盲（沿用《笛语项目基线》盲审「输出匿名」纪律）；
5. 任一隔离被破坏，该轮结论标记为 `隔离失效`，不得进入裁决卡。

## 8. 六轮研究与对抗

### R0 证据采集

按第 2—4 节采集并登记证据。产出：证据集。
停止条件按第 3.3 节。

### R1 独立方案

若干作者在第 6 节的隔离上下文中各自独立产出完整方案。
每份方案必须给出：候选结论、每条结论的分类建议、依据的 `evidence_id`、适用条件。

### R2 匿名交叉质询

方案匿名后互相质询。每条质询必须是**可证伪的**，写明：
质疑哪一条结论、依据什么证据或反例、如果质疑成立会推翻什么。

「我觉得不好」「不够专业」一类不可证伪的意见不予登记。

### R3 单变量反事实

对存活结论设计单变量反事实：**只改一个条件**，看结论是否仍然成立。

例：按合同第 6.2.1 节的资源强度次序，把 Capture Mode 从
`LEAN_MOBILE_TEAM` 降一档到 `SOLO_MOBILE`，该条结论是否还成立？
若不成立，它就不是 `HARD_RULE`，而是 `CONDITIONAL_RULE`，且条件必须写明。

注意 `ASSET_REUSE` 不在该序列上（它是不拍摄，不是省着拍），
以它做反事实时必须单独说明，不得当作「再降一档」。

一次改多个变量的反事实无效，须重做。

### R4 红队攻击

红队对每条存活结论**主动寻找推翻它的理由**，至少尝试四个角度：

1. 事实角度：依据的证据是否被误读，是否有反例；
2. 条件角度：适用条件是否被隐藏或过度泛化；
3. 可行性角度：在现实 Production Profile 档位下是否可执行；
4. 合同角度：是否与产品合同、Hard Gate 或上游锁定项冲突。

红队默认立场是**结论不成立**；无法推翻才算存活。

### R5 条件化综合

综合者把存活结论按第 9 节分类，并逐条写明适用条件。

综合者**不得**：

- 新增任何未经前四轮检验的结论；
- 用多数票决定分类（见第 14 节）；
- 通过折中、平均或改写消解真实分歧——分歧按第 10 节路由；
- 把 `CONDITIONAL_RULE` 简化成 `HARD_RULE` 以求整洁。

## 9. 输出分类

本节是**结论的处置分类**，与第 4 节 `evidence_level` 的**证据等级**是两条不同的轴，
不得混用：一条 `OFFICIAL_RULE` 等级的证据，其结论仍可能被分类为 `CONDITIONAL_RULE`
或 `REJECTED`。证据等级见 PRD 第 8.3 节与合同第 11.2 节。

每条结论必须归入且只归入一类：

| 分类 | 含义 | 可否进 Skill 硬规则 |
|---|---|---|
| `HARD_RULE` | 无条件成立，违反即错 | 可 |
| `CONDITIONAL_RULE` | 在明确条件下成立 | 可，但必须携带条件 |
| `SOLUTION_FAMILY` | 一族合法做法，择一即可 | 否，作为可选项呈现 |
| `CREATIVE_FREEDOM` | 明确交给创意，不设规则 | 否，必须写明「此处不设规则」 |
| `INSTANCE_DECISION` | 单次实例决定 | 否，不得升级为通用规则 |
| `FUTURE_HYPOTHESIS` | 未来趋势判断 | 否，须带完整复查字段 |
| `REJECTED` | 不成立，或属通用模型默认行为 | 否，但必须保留理由 |

### 9.1 REJECTED 必须保留

`REJECTED` 条目**不得删除**。它记录了「为什么这条没进 Skill」，
是防止后续重复讨论、也是防止把通用常识当专家资产的证据。

### 9.2 CREATIVE_FREEDOM 必须被显式声明

不写规则**不等于**忘了写。
`CREATIVE_FREEDOM` 是一个主动判断：这里刻意不设规则，交给创意。
Skill 施工时必须能区分「刻意留白」与「遗漏」。

### 9.3 分类不得因整洁而升格

`CONDITIONAL_RULE` 不得因为「条件太啰嗦」被写成 `HARD_RULE`；
`SOLUTION_FAMILY` 不得因为「要给个明确答案」被压成单一规则。

## 10. 分歧路由

R2—R4 未能消解的分歧，**不得由综合者自行裁决**，必须路由：

| 路由 | 适用情形 | 下一步 |
|---|---|---|
| `OFFICIAL_SOURCE_CHECK` | 分歧焦点是平台官方规则的事实 | 查官方来源，以官方为准 |
| `FOUNDER_DECISION_REQUIRED` | 涉及品牌立场、对外承诺、经营取舍或长期账号使命 | 进裁决卡，交 Founder |
| `EMPIRICAL_VALIDATION` | 只能由真实运行或真实制作检验 | 记为待验证，进 P08 或后续阶段 |
| `PRODUCTION_FEASIBILITY_GATE` | 分歧本质是制作条件够不够 | 交可行性门，按 Production Profile 判定 |
| `INSTANCE_DECISION` | 只影响本实例，不影响通用规则 | 记为实例决定，不进 Skill |

### 10.1 路由纪律

- 一条分歧只走一个路由；确实跨两类时拆成两条；
- 不得用「先按 A 写，以后再说」代替路由；
- 不得把 `FOUNDER_DECISION_REQUIRED` 降级成 `INSTANCE_DECISION` 以求推进。

## 11. Founder 裁决卡

`P02`、`P03`、`P04` 各自必须形成一张裁决卡，作为该阶段的**唯一**可传递结论。

### 11.1 裁决卡必含

1. 阶段与对应 Skill；
2. 本阶段唯一研究问题；
3. 样本分布与停止扩样依据；
4. 按第 9 节分类的完整结论表；
5. `REJECTED` 条目及理由；
6. 需要 Founder 裁决的事项，逐条给出：
   - 争的是什么；
   - 两个方向各自的依据与代价；
   - 执行侧的推荐及理由；
   - 不裁决会阻塞什么；
7. 已路由但未裁决的分歧清单；
8. 本阶段的验证状态（第 15 节）；
9. 本阶段不能证明的事项。

### 11.2 裁决卡纪律

- 每条待裁决事项必须给出**推荐**，不得把未整理的选择题退回 Founder
  （《笛语项目基线》第五节产品标准第 4、5 条）；
- 每条必须能用 Founder 可审计的大白话说清（`CLAUDE.md` 第 5 节）；
- 不得把技术术语、枚举名或流程图当作裁决依据呈现；
- 三张裁决卡汇总后统一进入 **Founder 集中裁决**，裁决通过前不得进入 `P05`—`P07`。

## 12. 研究资产与 Skill 运行资产的隔离

### 12.1 研究资产绝不进入运行输入

以下**绝不得**进入 Skill、Dify System Prompt、User Prompt 或任何固定运行输入：

- 本协议；
- 证据集与原始搜索结果；
- R1—R5 各轮的方案、质询、反事实与红队记录；
- 未采用方案；
- 裁决卡；
- 具体账号名称、URL 与他人内容片段。

只有 **Founder 集中裁决通过、并已抽象为判断方法的结论**才进入运行，
两条合法载体各一条：

1. 写进 `SKILL.md` 的抽象判断；
2. 写进四项共享知识资产的条目——它们作为合同第 1.1 节第 8 项进入运行输入。

载体 2 同样必须是**已裁决、已抽象**的条目，不得携带案例原文、账号名、URL
或任何研究过程记录。这条路径解决了 PRD 第 8.1 节「按需读取」的装载问题，
同时不违反本节对研究过程资产的排他。

### 12.2 不得把研究历史塞进 SKILL.md

沿用 PRD 第 8.2 节：不得把全部行业研究历史直接塞进 `SKILL.md`。
研究结论进入 Skill 时必须已经抽象为判断方法，不携带案例原文、账号名与数据。

### 12.3 研究搜索不等于产品接入

`P02`—`P04` 使用网络搜索是**执行侧的研究行为**。

它不构成、也不得被描述为：账号搜索能力、平台数据接口、内容监测、
反馈工具接入或任何产品能力。发布反馈学习的延期裁决
`PRD-DECISION-FEEDBACK-LEARNING-DEFERRED-V1` 继续有效。

### 12.4 目录隔离

研究资产与 Skill 运行资产分开归档，命名可区分。
**不得**把研究过程历史写进未来 Skill 目录。

## 13. 原始搜索结果、模型输出与失败证据的保存

### 13.1 必须保存

| 类别 | 保存要求 |
|---|---|
| 原始搜索结果 | 检索式、检索日期、返回来源与 URL；不得只保存摘要 |
| 模型输出 | 逐字保存原值，含推理块；沿用 `_RAW` 惯例 |
| 失败证据 | 未成立的方案、被推翻的结论、隔离失效的轮次，全部保留 |
| 反例 | 与结论相反的证据必须保存并在结论旁标注 |

### 13.2 不得删除失败证据

**删除失败或冲突证据等同于制造通过**（`CONTENT-PRODUCTION-P01` 第 11.4 节）。

被推翻的结论保留在 `REJECTED`；
隔离失效的轮次保留记录并注明该轮不进入裁决卡；
无法核对来源的证据保留但标注不可核对，不得静默丢弃。

### 13.3 保存不等于采信

保存下来的原始输出与搜索结果只是**证据**，不是结论。
它们不进入运行输入（第 12.1 节），也不因被保存而获得任何等级。

## 14. 不得用多数票替代事实、产品合同或实证验证

### 14.1 三条禁令

1. **不得用多数票替代事实**：三个作者都这么说，不等于平台规则如此。
   事实类分歧走 `OFFICIAL_SOURCE_CHECK`。
2. **不得用多数票替代产品合同**：多数意见与
   `CONTENT_PRODUCTION_CHAIN_CONTRACT_v0.1.md` 的 Hard Gate 或上游锁定项冲突时，
   以合同为准；确需改合同，走合同第 15.1 节的修改路径，不得由研究直接推翻。
3. **不得用多数票替代实证验证**：只能由真实运行检验的分歧，
   走 `EMPIRICAL_VALIDATION`，标记为待验证，不得靠投票提前结案。

### 14.2 多数票唯一的合法用途

多数票只能用于**排序**——决定先讨论哪一条、先验证哪一条。
它不能决定一条结论是否成立，也不能决定它的分类。

### 14.3 少数意见必须保留

被多数否定但未被证据推翻的意见，必须保留在裁决卡的分歧清单中，
标明它是少数意见及其依据。**不得**因为「已经形成一致」而抹去。

## 15. 模型专家视角审查与真实领域专家验证

### 15.1 两个状态

| 状态 | 含义 |
|---|---|
| `MODEL_PANEL_REVIEWED` | 结论已经过多个模型执行上下文按本协议六轮审查 |
| `DOMAIN_EXPERT_VALIDATED` | 结论已经过**真实人类领域专家**验证 |

### 15.2 两者不可互相替代

> **Claude Code、Codex 或任何其他模型，本身都不构成真实人类领域专家。**

无论采用多少个执行上下文、多少轮对抗、多强的模型，
所得状态最高只能是 `MODEL_PANEL_REVIEWED`。

`DOMAIN_EXPERT_VALIDATED` 只能由真实人类领域专家给出，并须记录：
专家身份、专业领域、验证日期、验证范围、专家保留意见。

### 15.3 P02—P04 的默认状态

`P02`—`P04` 的全部结论默认状态为 `MODEL_PANEL_REVIEWED`。

裁决卡必须**显著标注**该状态，并写明：本阶段结论未经真实领域专家验证。
Founder 裁决是**经营决策**，不是领域专家验证，不改变该状态。

### 15.4 不得升格表述

不得使用「专家认为」「行业共识是」「经专家验证」一类表述描述
`MODEL_PANEL_REVIEWED` 结论。正确写法是「按本协议六轮审查后存活的判断」。

## 16. 本协议不做什么

- 本协议只约束 `P02`—`P04` 的研究与对抗，不约束 `P05`—`P09` 的施工与审查。
- 本协议不规定研究结论的内容，只规定怎样得出、怎样分类、怎样交人。
- 本协议不建样本库、判据库或评分器。
- 本协议不能证明按它执行就能得到正确结论：它只降低把通用常识、
  模型推演与他人做法当成专家资产的概率。
- 本协议不能替代真实领域专家验证（第 15 节）。
- 本协议不能证明六轮对抗的轮次划分是最优的：这是首次使用，
  若 `P02` 执行中发现某一轮无效，应在裁决卡中记录，由 Founder 决定是否调整。

## 17. 冻结声明

本协议在 `CONTENT-PRODUCTION-P01` 落盘后冻结。

- 冻结时间：`2026-08-21`（执行日）
- 冻结基线：`349673b76fc46a3ffc5170344956297d24017e7c`；
- 冻结后不得修改；
- 不得为了让研究结论通过而修改本协议、产品合同、固定测试输入或评分标准。
