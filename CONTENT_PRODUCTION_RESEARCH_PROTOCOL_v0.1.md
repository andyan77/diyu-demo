# CONTENT_PRODUCTION_RESEARCH_PROTOCOL v0.1

> 本文件是 `P1`—`P4` 的研究协议。
> 由任务 `CONTENT-PRODUCTION-P0-R` 收缩落盘，取代同名文件的上一版全文。
>
> 上一版把研究设计成六轮强制代理编排；本版**取消该编排**。
> 研究由 Founder 手动在两个窗口执行，协议只规定**边界与最小交付物**，不规定流程编排。
>
> 本文件是治理文件，不是模型输入。
> 它不进入 Skill、不进入 Dify System Prompt、不进入 User Prompt、不进入任何固定运行输入。
>
> 本协议受 [PRD 第 2 节](CONTENT_PRODUCTION_CHAIN_PRD_v0.1.md) 轻文档化与轻治理化裁决约束。
> 研究是为了让 Dify 画布做对，不是为了产出研究档案。

## 1. 研究目标与边界

### 1.1 各阶段的唯一问题

| 阶段 | 对应 Skill | 唯一研究问题 |
|---|---|---|
| `P1` | Creative Script Architect | 把一条已确定判断的内容写成可拍摄脚本时，哪些判断值得固化进 Skill？ |
| `P2` | Production Director | 把一份脚本变成可执行生产包时，哪些判断值得固化进 Skill？ |
| `P3` | Publishing & Packaging Architect | 把一条成片变成可发布包装时，哪些判断值得固化进 Skill？ |

### 1.2 生死题

依《笛语项目基线》：

> 企业为什么不能直接用 GPT 项目文件夹完成？答不上来的功能不做。

每条候选判断都要回答：**通用模型默认会不会稳定做出这个判断？**

**但答案不得在未经测试时用来淘汰候选。**
判断「通用模型是否已经稳定做到」属于实证问题，
留到 `P8` 的 Skill / No-Skill 同条件对照验证。

研究阶段只能标注三种倾向：`可能是通用常识` / `可能是专家判断` / `不确定`，
并写明理由。**不得据此直接删除候选。**

### 1.3 明确不研究

- 不研究 IP 账号定位与矩阵（已冻结）；
- 不研究市场规模、竞品与商业模式；
- 不研究发布后数据表现与增长方法（发布反馈学习已延期）；
- 不研究平台算法推荐机制的内部原理；
- 不研究如何规避平台规则。

### 1.4 研究阶段不做什么

- 不进入 Dify，不创建工作流，不修改任何 DSL；
- 不启动后台代理群，不做多角色自动编排；
- 不编写任何 `SKILL.md`；
- 不跨界研究另外两份 Skill 的职责范围。

## 2. 双窗口隔离

`P1`—`P3` 每个阶段由 Founder **手动开启两个窗口**：

```text
窗口 A：Claude Code
窗口 B：Codex
```

三个阶段共产出 **六份研究 memo**。

### 2.1 隔离规则

1. 两个窗口读取**同一个 `P0` Commit**，使用**同一组核心研究问题**；
2. 两个窗口使用**不同的优化视角**（见第 3 节）；
3. **首轮提交前，任一窗口不得读取对方的研究结果**；
4. 不得把一个窗口的中间结论转述给另一个窗口；
5. 两份 memo 都提交并冻结之后，才允许互相阅读。

### 2.2 隔离不要求什么

- **不要求措辞不同。** 两份 memo 出现相似表述是正常的——
  同一份输入本来就会导出相近结论。
  **不得因为措辞相似而作废任何一轮研究。**
- 不要求角色扮演、不要求匿名化处理、不要求交叉质询轮次。

### 2.3 研究结果的身份

Claude Code 与 Codex 的研究结果，身份是：

```text
MODEL_RESEARCH_OUTPUT
```

**不得**称为、写成或暗示为：
`DOMAIN_EXPERT_VALIDATED`、人类领域专家结论、行业已证明规律、平台算法事实。

模型再多、意见再一致，都不构成人类领域专家验证。

## 3. 三份 Skill 的不同研究视角

同一阶段的两个窗口使用不同视角，目的是让分歧**自然出现**，不是为了对抗表演。

| 阶段 | 窗口 A 视角 | 窗口 B 视角 |
|---|---|---|
| `P1` Creative Script | 内容效果优先：什么样的表达真的能让顾客改变判断 | 事实安全与可执行优先：什么样的表达不会越界、不会写不出来 |
| `P2` Production Director | 现场可执行优先：真实人手与时间下能不能拍出来 | 成片质量优先：怎样保住内容判断不在制作环节丢失 |
| `P3` Publishing & Packaging | 触达优先：怎样让对的人看到并愿意点开 | 合规与承接优先：怎样不越承诺边界、不伪装平台定制 |

两个视角**都不是正确答案**，它们的冲突正是 `P4` 要评审的材料。

## 4. 单份研究 memo 的最小结构

一份 memo 就是该窗口在该阶段的**唯一可传递交付物**。
除 memo 之外**不产出**证据数据库、过程记录或流程文档。

必含十项：

1. **执行头**：Prompt ID、执行环境（Claude Code / Codex）、日期、输入 `P0` Commit；
2. **一套完整主方案**：这份 Skill 应该怎么做判断；
3. **一套最强相反方案**：如果主方案是错的，最可能对的是什么（见第 6 节）；
4. **候选判断，最多 8 条**；
5. **决定性来源，最多 12 个**；
6. 每条候选判断的**成立条件、资源要求与失效条件**；
7. **最危险反例**：最可能让主方案翻车的那一个情形；
8. **建议进入 Skill 的内容**；
9. **应保留为创作自由的内容**；
10. **需要 Founder 裁决的问题**，每条给出推荐与理由。

### 4.1 上限是硬上限

8 条候选判断、12 个来源是**上限，不是目标**。
少于上限不算不完整；**超过上限必须先合并或取舍，不得追加。**

理由：候选越多，`P4` 越无法裁决，Skill 越容易变成规则堆。

### 4.2 候选判断的分类

每条候选判断归入且只归入一类：

| 分类 | 含义 | 可否进 Skill 硬规则 |
|---|---|---|
| `HARD_RULE` | 无条件成立，违反即错 | 可 |
| `CONDITIONAL_RULE` | 在明确条件下成立 | 可，但必须携带条件 |
| `SOLUTION_FAMILY` | 一族合法做法，择一即可 | 否，作为可选项呈现 |
| `CREATIVE_FREEDOM` | 明确交给创意，不设规则 | 否，必须写明「此处不设规则」 |
| `INSTANCE_DECISION` | 只影响本实例 | 否，不得升级为通用规则 |
| `FUTURE_HYPOTHESIS` | 未来趋势判断 | 否，须带研究日期与复查条件 |

`CREATIVE_FREEDOM` 是**主动判断**，不是遗漏。
施工时必须能区分「刻意留白」与「忘了写」。

分类不得因为整洁而升格：
`CONDITIONAL_RULE` 不得因「条件太啰嗦」写成 `HARD_RULE`；
`SOLUTION_FAMILY` 不得因「要给个明确答案」压成单一规则。

## 5. 来源、证据与观察边界

### 5.1 证据规则

1. **只有当前有效的产品合同或平台官方规则，才可形成 `HARD_RULE`**；
2. **公开账号样本只能证明「出现过某种做法」**，不能证明该做法有效；
3. **公开表现数据不能直接证明经营因果**——播放高不等于生意好；
4. **模型综合判断只能作为假设**，不得写成结论；
5. **趋势判断必须标注研究日期、适用条件与不确定性**；
6. 「通用模型是否已经稳定做到」留到 `P8` 对照验证（第 1.2 节）。

### 5.2 观察与判断必须分开

写「看到了什么」时只写客观描述。
「为什么有效」「应该这样做」是判断，必须分开写，并标明它是判断。

把判断混进观察，是本协议最常见的失误。

### 5.3 来源覆盖是建议，不是门槛

建议覆盖：官方规则、头部账号、同业态同规模账号、中腰部账号、常规做法、**失效样本**。

- **不要求六类缺一不可。** 缺哪类就在 memo 里写明缺口与原因。
- **失效样本最有价值**：只看成功样本会得出「大家都这么做」，那恰恰是通用常识。
- **头部做法不因其头部身份自动成为规则**——
  头部账号常有普通企业不具备的资源与历史积累，
  其做法必须在真实制作条件下可复现，才可能进入 `CONDITIONAL_RULE`。

### 5.4 版权与非模仿边界

- 登记的是**做法与条件**，不是他人的具体内容；
- 不得抄录他人脚本、台词、标题、封面文案或分镜作为可复用素材；
- 不得复制他人人设、口头禅、标志性话术或视觉标识；
- 不得以「参考」为名生成与特定账号高度相似的成品；
- 结论进入 Skill 时只能是**抽象后的判断方法**，不得携带具体账号原文。

判据：**可以学会怎么判断，不可以搬走别人做过的东西。**

## 6. 自我攻击与相反方案

每份 memo 必须自己攻击自己，**不依赖另一个窗口来提供反面**。

最低要求两条：

1. **一套最强相反方案**：假设主方案整体是错的，最可能对的是什么。
   要写成一个**认真的方案**，不是稻草人。
2. **最危险反例**：最可能让主方案翻车的那一个具体情形，
   以及出现该情形时应该怎么办。

**不做**：多轮交叉质询、红队角色扮演、单变量反事实矩阵、匿名化流程。
自我攻击的目的是**暴露风险**，不是走完轮次。

## 7. P4 集中评审方法

`P4` 读取**六份已冻结的 memo**，集中评审四件事：

| 评审对象 | 要回答什么 |
|---|---|
| **冲突** | 两份 memo 给出互斥判断的地方，各自依据是什么、代价是什么 |
| **重叠** | 两份 memo 说的是同一件事的地方，能不能合并成一条 |
| **空档** | 六份 memo 都没有覆盖的判断，是真不需要还是漏了 |
| **条件差异** | 同一条判断在不同制作条件下是否仍然成立 |

### 7.1 裁决纪律

- **模型只能提出推荐、代价与路由，不得作最终产品裁决**；
- **Founder 作最终裁决**；
- **不按模型数量、模型品牌或多数票裁决**——
  两个窗口意见一致不构成结论正确的证据；
- 每条待裁决事项必须给出**推荐与理由**，
  不得把未整理的选择题退回 Founder；
- 每条必须能用 Founder 可审计的大白话说清，
  不得把枚举名、流程图或术语当作裁决依据呈现。

### 7.2 少数意见必须保留

被否决的方案与理由要留在评审结论里。
它记录了「为什么这条没进 Skill」，防止后续重复讨论。

## 8. Founder 裁决路由

评审中未能消解的分歧，**不得由评审者自行裁决**，必须路由到其中一类：

| 路由 | 适用情形 | 下一步 |
|---|---|---|
| `OFFICIAL_SOURCE_CHECK` | 争的是平台官方规则的事实 | 查官方来源，以官方为准 |
| `FOUNDER_DECISION_REQUIRED` | 涉及品牌立场、对外承诺、经营取舍或长期账号使命 | 交 Founder |
| `EMPIRICAL_VALIDATION` | 只能由真实运行或真实制作检验 | 记为待验证，进 `P8` |
| `PRODUCTION_FEASIBILITY_GATE` | 本质是制作条件够不够 | 交可行性判断 |
| `INSTANCE_DECISION` | 只影响本实例 | 记为实例决定，不进 Skill |

纪律：一条分歧只走一个路由；确实跨两类时拆成两条；
不得用「先按 A 写，以后再说」代替路由；
不得把 `FOUNDER_DECISION_REQUIRED` 降级成 `INSTANCE_DECISION` 以求推进。

## 9. 入库与追溯

### 9.1 必须留下

- 六份 memo 本身；
- 每条决定性来源的**可核对地址与观察日期**；
- `P4` 的评审结论与 Founder 裁决记录；
- 被否决的候选判断及理由。

### 9.2 明确不要求留下

- 隐藏推理、思维链或模型中间输出；
- 全部搜索结果 dump；
- 逐轮过程记录；
- 独立的证据数据库或索引文件。

### 9.3 研究资产不进运行输入

以下**绝不得**进入 Skill、System Prompt、User Prompt 或任何固定运行输入：
本协议、PRD、合同、六份 memo、评审记录、未采用方案、裁决记录、
Golden 与评分标准。

进入 Skill 的只能是**裁决后抽象出的判断方法**，
不是研究过程、不是案例原文、不是账号名与 URL。

### 9.4 目录

研究产物放在与运行产物分开的位置，
具体目录名由 `P1` 首次执行时确定并沿用，本协议不预先规定。

## 10. 停止与失败条件

### 10.1 什么时候停止扩样

任一成立即停：

1. 连续两轮新增样本不再产生新的候选判断；
2. 已形成的候选判断足以覆盖该 Skill 的必答问题；
3. 新增样本只增加同类做法，不改变任何分类；
4. 已达到 8 条候选判断或 12 个来源的上限；
5. 出现「研究本身变成工作」的迹象——
   开始整理格式、补充目录、优化排版，而不是回答研究问题。

**不得**以「再多找一些更保险」为由无限扩样。

### 10.2 什么时候判为失败

- 该阶段的唯一研究问题始终无法形成任何一条可操作判断；
- 全部候选判断都只能得出「通用模型默认也会这么做」，且有实证支持；
- 研究必须依赖不可核对来源、平台内部数据或违规手法才能成立。

判为失败时**如实报告**，不得用扩充篇幅或增加分类掩盖。
失败本身是有效结论：它说明这一段可能不需要独立 Skill。

### 10.3 本协议不做什么

- 不规定研究流程编排、轮次与角色；
- 不建设证据数据库、注册表或索引；
- 不替代 `P8` 的实证对照；
- 不能证明研究结论正确——它只保证结论的来源与限制被写清楚。

## 11. 阶段顺序

```text
P0  最小研究边界收口
P1  Creative Script 双窗口独立研究
P2  Production Director 双窗口独立研究
P3  Publishing & Packaging 双窗口独立研究
P4  六份研究集中评审与 Founder 裁决
P5  Creative Script Skill 施工与真实运行
P6  Production Director Skill 施工与真实运行
P7  Publishing & Packaging Skill 施工与真实运行
P8  纵向 E2E 与 Skill / No-Skill 价值验证
P9  独立只读审计与 V1 冻结裁决
```

`P1`—`P3` 可并行；`P4` 裁决通过前不得进入 `P5`—`P7`。

## 12. 冻结声明

本协议在 `CONTENT-PRODUCTION-P0-R` 落盘后冻结。

- 冻结时间：`2026-08-21`（执行日）；
- 不得为了让研究结果通过而修改本协议、PRD、合同或评审标准；
- 确需修改时由 Founder 裁决并以 `v0.2` 落盘。
