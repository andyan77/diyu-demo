# `M3_ECC_REBIND_005_FROZEN_v1.0`

- `task_id`: `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`（不变，非 NEW_TASK）
- 上位授权：Founder `CONTINUE_TASK`（2026-08-27），第 1—8 条
- 前一版：[`M3_ECC_REBIND_004_FROZEN_v1.0.md`](M3_ECC_REBIND_004_FROZEN_v1.0.md)（不覆盖、不修改，本文件是后继版本）
- 冻结时刻：本文件进入 git 的那一次 commit；**本轮全部正式取证在该 commit 之后发起**
- 候选：`M3 载体 v1.4`

---

## 1. 为什么要有这一版

第 7 轮查实三条缺陷，其中 `D-2` 不是一处写错，是**方法上少了一条**。

`REBIND_004 §2.2` 给 `G-2` 冻结了一条方法义务：收紧判据以后，**误报与漏检两个数字都要报**。
`G-4` 没有对应条款。后果是 `G-4` 的夹具族（`S1`–`S4`）由执行侧自己撰写，在自己写的句子上量出误报 0；
而同一条判据在 61 例真实运行上命中 12 次、**误报 11 次**、其中 6 次直接拒收了合格交付。

**在自己造的样本上量误报，等于自己给自己出题。** 这是 `G-2` 当初栽的同一个跟头换了个位置。

所以本版做两件事，缺一不可：改判据本身（§2），以及把 `G-2` 那条方法义务补给 `G-4`（§3）。

---

## 2. `G-4` v2 判据（冻结全文）

`G-4` 挡的一直是同一件事：**正文用一个现时性的数量断言，压过当轮输入里该槽位的权威值。**
命题不变，改的是"什么算数量断言"。

### 2.1 核心分界：没有周期的数字不是速率主张

v1 把正文里任何一个「N 条」都拿来和槽位里的「3 条/周」比。
于是「用**一条**内容同时覆盖四个目标」里的量词、「先用**三条**立住可积累的东西」里的计数，
全被当成发布速率。v2 把两者分开：

| 形态 | 怎么认 | 处置 |
|---|---|---|
| **带周期** | 后缀 `3 条/周`；前缀 `每周约 3 条`；数字前 12 字内出现周期词（`本周`／`这周`／`一周`／`每天`） | **正式判据，可阻断** |
| **谓语式** | 主语后 14 字内、中间不跨标点、且夹着系动词或量度词（`是`／`只有`／`只能`／`剩下`／`支撑`／`降到`…） | **正式判据，可阻断** |
| **其余无周期数字** | 量词、序数、计数 | **只记 advisory，不阻断** |

### 2.2 四条豁免（写死，不由判断决定）

1. **数值一致即豁免**：段内任一带周期的数与槽位归一后相等 ⇒ 整段豁免。三值分离的原句
   （「你期望一周三条，账号基线产能一周三条，当前实际可投入也是一周三条」）落在这里。
2. **等价换算即豁免**：`每天 N 条 ⇒ 每周 7N 条`。Founder 第 3 条逐字授权的换算方向，
   在两个方向上都不构成矛盾。缩小目标量才是。
3. **序数与指代不算速率**：数字前紧邻 `第下上这那另某每前后同各` 之一 ⇒ 跳过。
   不排掉它，一句「本周三条内容：第一条讲价格」就会被读成「本周 1 条」。
4. **否定语境不是主张**：数字前 16 字内出现否定词（复用本文件已有的 `NEGATION_BEFORE`，
   不另造词表）且其后没有 `而是／实际是／改为` 这类转折 ⇒ 跳过。
   「不为凑三条把本周压缩成一条内容里」说的是**不这么做**。

### 2.3 作用域（继承 `REBIND_004 §2.3`，逐字保留）

> 这一条只挡「用旧值压当轮输入」，不挡「解释为什么产能会变」。

再加两句：不挡历史对比（段内出现 `上周／之前／原来…` ⇒ 整段跳过）；
不挡逐字引用输入原值之后的比较。

### 2.4 advisory 是明账，不是遮羞布

无周期、且紧邻主语、且数值对不上的数字，记进 `stale_value_override_advisory`，
**看得见、不阻断**。`G-4` v2 的漏检就漏在这一格里。把它单列出来，是为了让作用域边界
留在证据里，而不是消失在"没有命中"这四个字里。

---

## 3. 方法义务（把 `REBIND_004 §2.2` 补给 `G-4`）

**从本版起，`G-4` 的正式判据不再由执行侧撰写的夹具承担。** 理由见 §1。

`G-4` 每次改动都必须在**真实模型输出**上量四个数字，四个都要写进证据：

| # | 量什么 | 语料 | 通过条件 |
|---|---|---|---|
| A | 误报 | 61 例真实运行（`ep06b-runtime-behavior-v13` 49 + `ep07-longitudinal-v13` 12） | 第 7 轮判读为误报的 11 例，v2 一例不得再命中 |
| B | 真阳性 | 同上 | 第 7 轮判读为真阳性的 `E04`，v2 必须仍然命中 |
| C | 漏检 | 真实句子 × 机械改动过的权威值 | 主张句探针必须全数命中；命不中的逐条列出 |
| D | 具名族 | `三值分离`／`等价单位换算`／`拒绝合并` 各自钉真实运行例 | 三族一例不得被拦 |

**C 的构造规则写死，不挑句子**：取正文里带周期的速率断言，把权威值改成
①`该值 + 1 条/周`（同单位）与 ②`该值 + 1 条/天`（跨单位，`7N` 必然不等）。
**句子取自真实运行，未改一字；只有配对是构造的，构造规则公开可复算。**
落在否定语境里的探针**保留在分母里、单独归类**——从分母里拿掉，作用域边界就看不见了。

判据与工具：[`account-operations/tools/rebind005/verify_g4_v2.py`](account-operations/tools/rebind005/verify_g4_v2.py)
（`sha256=9c8b1446f4f516fb9d2401afb144d2367181844d232db10b6abf07ca3cec9b72`）。
对照组的 v1 由脚本从 `git show HEAD` 取，**不手抄**——手抄的对照组等于没有对照组。

---

## 4. 旧 `G-4` 正式证据的处置

第 5—7 轮里由 `G-4` v1 产生的 `stale_value_override` 结论，**全部降为探索级**。

- **不追溯修改**任何一份已落盘的判定书、证据文件或 Checkpoint；
- **不补授权事件**；那几轮的判据在当时就是当时那一版，事后改判据不会让它变成另一回事；
- 具体影响的验收项：`M3-AC-17`（纵向 `E06` 被 v1 整份拒收）与 `M3-AC-06` 的 `G-4` 相关半。
  两者本轮按 v2 重跑，取新结论；旧结论作为历史记录保留，不再承载 `PASS`／`FAIL`。

---

## 5. `D-1` 与 `D-3` 的修法

两条都不需要新判据——它们违反的是 `REBIND_004 §2.1` 自己已经写下的那两句话。

### 5.1 `D-1`：义务集从「输入里的位」扩到「输入 ∪ 本轮已声明」

`positions_unaccounted` 只算输入差集。本轮**新增**的持续位不在输入里，被补齐节点删掉之后
既不在输入也不在声明，于是三个计数器一起显示 `[]`——内容全丢而系统全绿。
`REBIND_004 §2.1` 那句「不会再出现字段全程为空而内容全丢」对新增位不成立。

修法：复检把首闸记下的 `draft_declared_position_ids` 一并纳入义务集；
草稿里声明过、最终不见了的位记 `positions_dropped_after_draft`（其中新增位另记
`positions_dropped_new`），阻断且不得承载为新基线。

**回落分支同步修正**：补齐节点没重发审计块时的回落原本只回落「继续」那一类，
会让新增位在回落里凭空消失、再被 `D-1` 计成「删位」。按 A3 这属于**多算**，
所以回落改成继续／处置／新增三类一起回落。

### 5.2 `D-3`：代写的机器行剥掉再复算

`blanket_introduced_by_gate` 只盯散文里的概括句，盯不住补齐节点代写的 `POS ::` 机器行。
`REBIND_004 §2.1`：「判据能被闸门自己满足，就对模型没有约束力。」

修法不是记一笔就放行，而是**剥掉再复算**：`gate_repaired` 路径下，最终审计块里
草稿从未声明过的 `POS` 行一律计入 `positions_introduced_by_gate` 并从 `decls` 中剔除；
剔除之后原本对不上的地方会重新对不上，判据这才重新落回模型身上。

### 5.3 两条的验证

[`account-operations/tools/rebind005/verify_d1_d3.py`](account-operations/tools/rebind005/verify_d1_d3.py)
（`sha256=8e45de7ad9720d8bd6486cee663f2faad33a2af35acd49d651a561be205969b3`）
在第 7 轮真实运行记录（`E04`／`E12`）上做旧码—新码对照。
复检输入按记录机械重建；**重建的忠实性先由旧码逐字段复现记录里的 `positions` 证明**，
对不上就不算数、不往下走。旧码同样从 `git show HEAD` 取。

---

## 6. `SKILL.md` 的四处修改（不改产品语义，只改已有要求的落地）

| 绑定 | 改了什么 | 为什么不是新 WHAT |
|---|---|---|
| `AC-09` | 外部市场资料整格为空时必须照常做完整轮判断；只挡「平台稀缺／唯一／已避开同质化」三句 | `CLAUDE.md` §4「资料不足时不得整任务拒绝」与 §5、〈共同质量底线〉末段本来就这么写；本轮只是把它写进 `O-6` 的可执行位置 |
| `AC-09` | 持续位示例编号改成尖括号占位符；明写「输入里没有这一段 ⇒ 一行 `POS ::` 都不要写」 | 原文已有「清单里没有的编号不要写」。实测里模型抄的是**文档示例**里的 `POS-A`／`POS-B`——示例本身成了可抄的来源 |
| `AC-14` | 事实链必须点名确认人，或明说「本轮无确认人」并写清后果 | `M3_CONTENT_TASK_v1.0.schema.json` 的 `fact_confirmer` 早就是 required-且-可为 null，理由逐字写着「省略这个键会让『没有确认人』和『忘了填』不可区分」。`SKILL.md` 的清单漏了这一格 |
| `AC-18` | 五处判据（阶段推翻信号／删掉会丢什么／探索停止条件／反馈不授权什么／最可能错的那一步） | 见 §7 |

---

## 7. `AC-18` 的增益修法与它的三条自限

Founder 第 7 条：不降低 A 基线、不修改硬门、不靠内部标签或格式取胜。

- **A 基线一字未改**：`ab_v4/A_baseline_prompt.md` 本轮不动，哈希随证据一并落盘；
- **五项硬门一字未改**：`ECC-M3-MODULE-AB-001` 的目标忠实／事实／权限／风险／当前任务必要条件，
  连同 `ADDENDUM_003` 的推导公式，本版不碰；
- **不进闸门**：五处新判据**刻意不做成必填项闸门的检查项**。做成机械检查，模型会去满足检查
  而不是去想，补齐节点还可能替它写出来——那正是 `D-3` 刚证明过的病。它们只写在
  `SKILL.md` 的散文里，靠不靠得住由盲评说了算。

### 修法的依据

第 7 轮 A/B 的结果是七维里六维「相当」，唯一 B 优的是**产能取舍**。
产能取舍恰好是 `SKILL.md` 里唯一一条**要求把具体东西写进交付**的判据
（`O-3`：输出必须能回答"哪一条被让掉了、让掉的是什么收益"）。
其余各维的判据要么是跨轮比较（`O-1` 四轴、`O-4` 互换后周期会变），单份交付里看不见；
要么是禁令（`O-5` 无固定配比），禁令产出不了任何可辨识的东西。

所以修法是同一个形状：**把判据从"我自己算得出"改成"读者在这一份交付里看得见"**。

| 维度 | 新判据要求写进交付的东西 | 为什么通用好提示词不会自发产出 |
|---|---|---|
| 阶段判断 | 一条**本周期内看得见的推翻信号** + 结论指回输入里哪一句 | 「再观察一段时间」没有发生的那一刻，因此永远不会推翻任何东西 |
| 内容组合 | 每条任务一句**删掉它会丢什么** | 通用输出写的是任务做什么，不是没有它会少掉什么 |
| 实验设计 | 停止条件写成「看到什么就停」；不探索时也要写重新开的触发 | 「效果不好再说」没有观察时刻，等于没有停止条件 |
| 反馈判断 | 这条反馈**不授权**什么 | 最常见的失败是顺着一条真实反馈把没观察到的判断一起改了 |
| 交付整体 | 关键假设里必须有一条是**本轮最可能错的那一步** | 通用输出倾向于呈现确定性，不主动交出自己的薄弱环节 |

### 第三次不成立时怎么办

Founder 第 7 条后半段已经写死：同等基线、同模型、同输入、冻结盲评下第三次仍未证明整体增益 ⇒
**不继续循环修复，不请求 Founder 用产品接受替代技术 PASS**，如实输出
`M3_PROFESSIONAL_GAIN_NOT_VERIFIED`，并提交「模块是否仍值得独立存在」的产品级 Rebase 候选。
本文件不为这一条留任何回旋余地。

---

## 8. 冻结清单（候选 `M3 载体 v1.4`）

| 对象 | sha256 |
|---|---|
| `skills/operating-one-account/SKILL.md` | `c8f6805bb8e54ed85eced02af42f0af8c0ce0c276a0c079313d4221f3ce3b978` |
| `skills/.../references/fashion-and-market.md` | `cee04beadafa604e6be3e8bf6c0c8677a000f854fe53f7d3d78b0703950f32aa` |
| `skills/.../references/six-skill-methods.md` | `e3798308988c2b12e1d450a610b429772944ec88c9f2cf1d26f4b16f4465ec6b` |
| `tools/gate_v13/shared_checks.py` | `6c8c4f78d0152e3da99bc2604d08d599b40d7acd42625502273642c81965aa30` |
| `tools/gate_v13/gate_main.py` | `9f0e96c02552adb721aa7b508727e2213dc4001b6b7122cedafec8b27b764ae5` |
| `tools/gate_v13/assemble_main.py` | `2d1a5fd4e1ba91e50889e7b997618479a196827959032a1577bd843147448a76` |
| `tools/gate_v13/post_gate_main.py` | `ac0d644055b367b65db105a6ceba1ef9dac5ec7d95079fa39417731841d3e931` |
| `tools/gate_v13/projection_v13.py` | `6b9b31443b68e4574625bd2274929d0c4413ee87b206a25a2449245e2fcbdd35` |
| `fixtures/gate_fixtures_v13.json` | `87d84a1e8af2e4cde3fe1dec019abd953c80b48bf44836dd99a24120ac7f362e` |
| `evidence/_oracle/BEHAVIOR_CASES_v2.json` | `13e95684e387980267f2f4c2479c2b94bd04caf280cdb37e4b0d762f80286274` |

Dify：App `b7fb5b1a-9278-426c-bb8a-f9f288639548`，已发布版本 `m3-cand-v1.4-fix`，
图哈希 `ea04fa92c6b8ad34b35025bd4e75c2dce214fa30bda816aaecc9a3c4023fe3cc`，
7 节点 6 边（形状与 v1.3 相同）。系统提示词全文见
`evidence/ep24-ac16-canvas-and-prompt/published_system_prompt_full.txt`
（`sha256=068a30af07f089714fd9c6b428a879010dd04707b6f1134ee7d51dd815aff8c5`，
从 **publish** 端点读回，非草稿）。

---

## 9. 影响面（A3）

Skill 与三个代码节点都变了 ⇒ **全部依赖它们的取证一并 `STALE`，本轮全跑**：

| 取证 | 是否重跑 | 依据 |
|---|---|---|
| EP-06 保真（≥7 组） | **重跑** | 系统提示词变了，语义保真直接依赖它 |
| EP-06b 行为（49 例） | **重跑** | 同上，且闸门行为变了 |
| EP-07 纵向（12 步） | **重跑** | `D-1`/`D-3`/`G-4` 全在这条链上 |
| EP-08 A/B（12 运行 + 36 盲评） | **重跑** | B 臂的载体就是它 |
| M2→M3 投影契约、下游 Brief 消费、责任反搜、字段消融 | **重跑**（确定性，秒级） | 已跑，全绿 |
| AC-12/AC-13 对 `main@a7b8101` 的定向复验 | **不重跑** | M2 侧一字未动；本轮改动全在 M3 域内，与 M2 接口无边 |

**不得为省运行量自行宣布 A/B 或保真不受影响**——Founder 第 9 段已写死，本文件不重复裁量。

`END_MARKER: M3-ECC-REBIND-005-FROZEN-v1.0-END`
