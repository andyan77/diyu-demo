# CONTENT_PRODUCTION_CHAIN_CONTRACT v0.1

> 本文件是 Founder 对内容生产链三份 Skill（`Creative Script Architect`、
> `Production Director`、`Publishing & Packaging Architect`）的共享产品合同，
> 由任务 `CONTENT-PRODUCTION-P01` 第七节机械落盘。
>
> 执行侧只负责落盘与实现，不得修改本文件的业务含义。本文件在 P02 开始前冻结。
>
> 本文件是治理文件，不是模型输入。它不进入 Skill、不进入 Dify System Prompt、
> 不进入 User Prompt、不进入任何固定运行输入。
>
> 本文件不修改、不替代 `CONTENT_BRIEF_CONTRACT_v0.1.md`，
> 也不修改已落盘的三份决策 Skill。两份合同并列使用：
> 前者约束 Content Brief 之前，本文件约束 Content Brief 之后。
>
> 产品定义见 [`CONTENT_PRODUCTION_CHAIN_PRD_v0.1.md`](CONTENT_PRODUCTION_CHAIN_PRD_v0.1.md)；
> 研究纪律见 [`CONTENT_PRODUCTION_RESEARCH_PROTOCOL_v0.1.md`](CONTENT_PRODUCTION_RESEARCH_PROTOCOL_v0.1.md)。

## 0. 位置

```text
Matrix Architect
→ Campaign Orchestrator
→ Content Brief Architect
→ Production Profile 与 Production Feasibility Gate
→ Creative Script Architect
→ Production Director
→ Publishing & Packaging Architect
```

前三份由 `CONTENT_BRIEF_CONTRACT_v0.1.md` 及各自 Skill 约束，本轮不改动。
本文件约束后三份及其共享前置合同。

## 0.1 最小充分原则

本合同使用**最小充分字段**。明确不建设：

- 金融级事务；
- 并发锁；
- 多租户权限系统；
- 正式生产数据库；
- 状态机、自动回退系统、自动异常分级器、自动评分器；
- 校验服务、工作流引擎、规则激活流水线。

本文件中的全部状态与字段是**写在产物文件里、由人判断与标注的业务状态词**，
不是运行时工程设施。

**本合同不改变 `C6_FOUNDER_CONFIRMED_v0.1.md` 第十一节的非建设范围。**
该节作用域被其首句「本原则只是业务判断路由」锁定在 C6 异常回退原则上；
本合同不触碰该原则，也不把它实现成流程。

本合同第 9 节的失效传播，直接沿用 `C6` 的大白话表述——
「什么事实变了、哪项结论因此失效、谁重新判断、哪些后续内容需要复核」——
不另起工程术语。

本合同的合法性依据见
[`CONTENT_PRODUCTION_CHAIN_PRD_v0.1.md`](CONTENT_PRODUCTION_CHAIN_PRD_v0.1.md) 附录 A.1，
其中逐条列证《笛语项目基线》第三节解禁条款的两个条件
（真实运行问题、Founder 新授权）本轮均已满足。

## 1. 三份 Skill 的输入合同

### 1.1 共同输入

三份 Skill 的输入都可以是一份合并文本，不要求固定字段名，但必须能够识别：

1. 上游已被接受（`USER_ACCEPTED`）的产物；
2. 当前有效品牌与顾客事实；
3. 已确认的账号责任与人格；
4. 已登记的一手事实、素材与资源快照；
5. 已接受的 Production Profile，且含 `accepted_mode`；
6. 涉及行动号召时，上游已确认的承接入口、承接人、替补、受理边界与确认方式；
7. 目标发布平台，或明确状态 `PLATFORM_UNCONFIRMED`；
8. 已发布的共享知识资产（第 11.4 节四项）中与本段相关的部分，若已建设。

**不属于运行输入**：上游专家讨论过程、对抗审查记录、未采用方案、
Golden 与预期答案、评分标准、研究协议本身、本合同本身。

### 1.2 Creative Script Architect

必须能够识别：

- 一条已被接受的 Content Brief 内容单元，其状态为
  `BRIEF_READY` 或 `BRIEF_READY_WITH_CONDITIONS`；
- 该内容单元 `brief_id` 之下的**全部 25 个字段**（见 1.2.1）；
- 已接受的 Production Profile；
- 账号人格与关系姿态的正式来源（账号责任卡）。

状态为 `NOT_TRIGGERED`、`NO_PUBLISHING_BRIEF` 或 `CANCELLED` 的内容单元
**不进入生产链**，不得为其生成脚本。

若 `进入脚本与拍摄设计前必须确认的事项` 中列有**尚未确认、且确认结果会改变脚本核心判断**
的事项，按 `INPUT_INSUFFICIENT` 停止。仅影响制作形式、不影响核心判断的未决事项，
按 `READY_WITH_CONDITIONS` 条件化处理。

#### 1.2.1 Content Brief 单条 25 字段（按序，逐字）

| # | 字段 | # | 字段 |
|---:|---|---:|---|
| 1 | 上游决策来源 | 14 | 与上一条和下一条的接力 |
| 2 | 账号与本轮责任 | 15 | 必须使用的素材 |
| 3 | 内容类型与顺序 | 16 | 可选素材 |
| 4 | 当前状态 | 17 | 出镜人与事实确认人 |
| 5 | 唯一顾客情境与问题 | 18 | 制作要求 |
| 6 | 顾客当前卡点 | 19 | 必须表达 |
| 7 | 内容后需要形成的唯一新判断 | 20 | 明确不得表达 |
| 8 | 核心内容判断 | 21 | CTA 或无 CTA 的决定 |
| 9 | 证据地图 | 22 | 发布条件 |
| 10 | 事实、观察、专业判断与待验证变量的区分 | 23 | 降级条件 |
| 11 | 叙事结构 | 24 | 取消或不发条件 |
| 12 | 真实摩擦或决定性张力 | 25 | 进入脚本与拍摄设计前必须确认的事项 |
| 13 | 账号关系姿态 | | |

字段与值之间使用全角冒号，值为自由文本，无机器可解析结构。

**计数口径更正**：`CONTENT_BRIEF_DEEPSEEK_V4_FLASH_RUN_001_EVAL.md` 的 `HG-09`
写作「24 个必需字段」，机械计数为 **25 个字段 + `brief_id`**。
本合同以 25 为准。该更正**不修改** `_EVAL` 文件本身（运行证据不得改动）。

#### 1.2.2 Brief 只给指针、不给事实值

Brief 的 `必须使用的素材` 等字段给出的是**素材编号与商品编号**，
不含面料成分、价格、库存等事实值本身。

因此 Creative Script 的运行输入**必须同时接入品牌事实与素材资源夹具**，
否则脚本无法在不编造的前提下写出事实句。缺夹具时按 `INPUT_INSUFFICIENT` 停止。

#### 1.2.3 账号口吻真源不在 Brief 里

Brief 把账号人格压缩为一行 `账号关系姿态`。
口吻、人格内核与张力场景的真源是**账号责任卡**（字段：
`夹具已确认的人物事实`／`Founder 已确认的人格内核`／`关系姿态`／
`张力场景一`／`张力场景二`／`长期关注理由`）。

Creative Script 必须接入责任卡原文，不得仅凭 Brief 的一行姿态描述推断人格。

### 1.3 Production Director

必须能够识别：

- 一份已被接受的 Creative Script（状态 `USER_ACCEPTED`）；
- 该脚本的逐句口播、对白、旁白、屏幕文字与内容节奏；
- 已接受的 Production Profile，含 `accepted_mode` 与已声明的人员时间预算；
- 已登记素材清单及其可用边界；
- 出镜人身份及其真实属性标注要求。

### 1.4 Publishing & Packaging Architect

必须能够识别：

- 一份已被接受的 Production Package（状态 `USER_ACCEPTED`）；
- **真实成片状态**，或在无成片时的**成片夹具**（明确标注为夹具）；
- 上游已确认的统一承接口径与承接条件当前是否成立；
- 平台状态：已锁定平台，或 `PLATFORM_UNCONFIRMED`。

成片状态与成片夹具都缺失时，不得生成发布包装，按 `INPUT_INSUFFICIENT` 停止。
不得以「按脚本推测成片应该是什么样」代替成片状态。

### 1.5 运行输入的隔离边界

本节只规定**什么不得进入运行输入**，不规定 Dify 节点编排、DSL、模型参数或提示词写法
（那些不属于本合同，见第 16 节）。

**绝不得**进入 Skill、System Prompt、User Prompt 或任何固定运行输入：

- 本合同、PRD 与研究协议本身；
- Golden、评分标准与期望答案；
- 研究过程记录、对抗审查记录与未采用方案；
- 上游专家讨论过程；
- 其他内容单元的产物（除非本合同第 1.2—1.4 节明确要求）。

**必须**能从运行输入中识别的最小集合，见第 1.1—1.4 节。

运行输入具体怎样拼装、产物怎样归档，沿用编译链既有做法，
由 P05—P08 按当时的实际形态决定，本合同不预先规定。

## 2. 三份 Skill 的输出合同

### 2.1 顶层状态（三份共用）

| 状态 | 含义 |
|---|---|
| `READY` | 全部条件成立 |
| `READY_WITH_CONDITIONS` | 可生成，但存在未成立条件，且每个条件写明影响哪一条内容 |
| `INPUT_INSUFFICIENT` | 缺少必需的上游正式决定、事实链或最低条件 |
| `INPUT_CONFLICT_REQUIRES_FOUNDER` | 两条同等效力正式结论不兼容，且无更高优先级裁决 |
| `PRODUCTION_NOT_FEASIBLE` | 输入完整，但可行性门判定为 `BLOCKED` |

前四个沿用 `CONTENT_BRIEF_CONTRACT_v0.1.md` 第 5 节已有枚举，不新造同义词。
`PRODUCTION_NOT_FEASIBLE` 是本合同**新增**枚举，此前仓库中不存在，
不得被描述为既有约定。

### 2.2 停止时的输出结构

沿用编译链已有格式，不新增字段：

```text
INPUT_CONFLICT_REQUIRES_FOUNDER
冲突事项：
两条结论各自的出处与效力：
受影响的内容单元：
为什么不能继续：
需要最终经营决策人确认的唯一问题：
```

```text
INPUT_INSUFFICIENT
阻塞缺口：
它会阻塞哪一个内容单元：
当前仍可安全完成什么：
需要企业回答的唯一问题：
```

```text
PRODUCTION_NOT_FEASIBLE
不可行的内容单元：
requested_mode 与 available_mode 的差距：
已尝试的降级路径与为什么仍不成立：
需要补齐的唯一资源：
当前仍可安全完成什么：
```

三者都**最多提出一个区分力最高的问题**，不连续追问，不用行业惯例代填。

### 2.3 Creative Script Architect 输出

正常输出第一行必须是：

```text
# Creative Script
```

至少包含：

| 段 | 内容 |
|---|---|
| 0. 运行结论 | 顶层状态；来源 `brief_id`；已接受 `accepted_mode`；上游锁定项；当前成立条件；当前未成立条件 |
| 1. 创意命题 | 核心创意命题；为什么这个命题能推动 Brief 指定的唯一新判断 |
| 2. 钩子与开场 | 内容钩子；从哪个具体事实进入 |
| 3. 叙事结构 | 叙事节拍；真实摩擦或决定性张力出现在哪一拍 |
| 4. 内容正文 | 完整逐句口播稿；对白；旁白；**屏幕文字要显示的字**；图文形态下为完整图文正文（见 2.6） |
| 5. 人格与关系姿态 | 本账号在本条内容中的关系姿态；口语化口径；禁用话术 |
| 6. 事实引用位置 | 每一条事实、观察、专业判断、设计情境、待验证变量出现在哪一句，及其来源与确认人 |
| 7. 内容节奏 | 节奏安排与不得压缩的部分 |
| 8. 表达边界 | 必须表达；明确不得表达 |
| 9. 进入分镜与拍摄设计前必须确认的事项 | 交给 Production Director 的未决项 |

**不得输出**：逐镜头编号；每个镜头的景别、机位、运动与秒数；拍摄日计划；
最终标题；最终封面文案；最终发布文案；标签与话题。

### 2.4 Production Director 输出

正常输出第一行必须是：

```text
# Production Package
```

至少包含：

| 段 | 内容 |
|---|---|
| 0. 运行结论 | 顶层状态；来源脚本编号与版本；已接受 `accepted_mode`；可行性状态；上游锁定项；成立与未成立条件 |
| 1. 分镜与镜头清单 | 逐镜头编号；景别；机位；运动；时长；对应口播句 |
| 2. 人物动作与表演提示 | 出镜人、动作、表演提示；出镜身份的真实属性标注方式 |
| 3. 场景、商品与道具 | 场景；出现的商品编号；道具；不得出现的物品 |
| 4. 声音 | 收音方式；配音；音乐；音效；无音乐时的说明 |
| 5. 字幕与屏幕信息 | 成片字幕全文（口播转写）；身份与情境标注的呈现方式；**脚本已定屏幕文字的出现时机、位置与呈现形式**（不改字） |
| 6. 剪辑节奏 | 剪辑节奏；不得剪成的效果 |
| 7. 素材分级 | 必拍素材；可选素材；可删除素材；可复用的已登记素材及其来源标注 |
| 8. 拍摄日执行计划 | 在已声明人员时间预算内的安排；每人占用时长；不得超出的部分 |
| 9. 降级方案 | 条件不足时删除哪些镜头、最低可交付版本；**若删减会影响结论强度，只作影响说明并按 9.1 触发脚本重判，不得自行下调** |
| 10. 进入发布包装前必须确认的事项 | 交给 Publishing & Packaging 的未决项 |

**不得输出**：改写后的口播、对白、旁白或屏幕文字用字；新增企业事实；超出已声明人员时间预算的安排；
新增人员、场地、设备或预算；最终标题、封面文案与发布文案。

### 2.5 Publishing & Packaging Architect 输出

正常输出第一行必须是：

```text
# Publishing Package
```

至少包含：

| 段 | 内容 |
|---|---|
| 0. 运行结论 | 顶层状态；来源制作包编号与版本；成片状态或成片夹具标注；平台状态；成立与未成立条件 |
| 1. 候选标题 | 多个候选标题及各自适用条件；每个标题对应正文的哪一句判断 |
| 2. 封面信息层级与封面文案 | 封面要传达什么、先看到什么、不得出现什么；**最终封面文案**；图文形态下另指定首图 |
| 3. 发布文案 | 正式发布文案 |
| 4. 关键词、话题与标签 | 必要标签；不使用的标签及理由 |
| 5. 置顶评论 | 置顶评论内容，或明确「无」 |
| 6. CTA | 在 Brief 已定「有／无 CTA」之内，给出 CTA 的**具体措辞与出现位置**；引用的统一承接口径原文 |
| 7. 平台规格适配 | 已锁定平台时的规格适配；`PLATFORM_UNCONFIRMED` 时输出「平台规格待适配清单」 |
| 8. 发布前检查 | 逐条检查项与结论 |
| 9. 退回意见 | 包装与真实成片状态不匹配时的退回意见，或明确「无」 |

**不得输出**：改写后的正文事实；新的品牌立场；正文未支撑的结论；
新增承接承诺；平台未确认时的平台定制表述；
**改变 Brief 已确认的「有 CTA／无 CTA」决定**。

### 2.6 内容形态适配：不只是短视频

夹具第八节登记的制作形态只有两类，verbatim：

> 七天内最多完成 3 条主要短视频＋2 条轻量图文或短切片

`RUN_001` 的内容单元索引中，实际出现过的**发布类**内容类型为
`主要短视频`、`轻量图文或短切片`、`条件性备用内容`；
其余取值（如 `事实确认与交接准备`）属非发布类，不进入生产链。

`轻量澄清` 是**内容单元名称**，不是形态；其内容类型登记为 `轻量图文或短切片`。
`条件性备用内容` 触发后按其实际形态归入前两类之一。

三份 Skill 对**非视频形态**同样负责，职责边界不变，只是段落取值不同：

| 形态 | Creative Script | Production Director | Publishing & Packaging |
|---|---|---|---|
| 主要短视频 | 逐句口播、对白、旁白、屏幕文字 | 分镜、镜头、声音、字幕、剪辑 | 标题、封面、发布文案、标签、CTA |
| 轻量图文 | **完整图文正文**、配图说明文字 | 图片清单、拍摄或选图要求、版式与必要标注；**按 Publishing 指定的首图完成生产**，不自行选定首图 | 标题、发布文案、标签、CTA；**指定首图与封面文案** |
| 短切片 | 切片保留哪几句、是否需要补录新口播（只提需求，不排期） | 从哪一段素材截取、时长、字幕；补录是否新增拍摄及其对 `accepted_mode` 的影响 | 标题、封面、发布文案、标签、CTA |

**归属声明**：`Content_Brief_Architect_v0.1.md` 第 5 节拒绝输出的
`完整图文正文` 归 **Creative Script Architect**，
因为它是内容本身要说什么，与短视频的逐句口播同层。

Production Director 对图文负责的是**版式与图片生产**，不改写正文、不选定首图；
Publishing & Packaging 负责标题、首图指定、封面文案、发布文案、标签与 CTA。

**首图归属**：图文的首图即封面，由 Publishing & Packaging 指定并配文案；
Production Director 负责把它生产出来。二者不重叠。

非视频形态下，Production Director 输出的第 1、4、6 段
（分镜与镜头清单、声音、剪辑节奏）写「不适用」，不得为凑格式硬填。

### 2.7 Content Brief 九项拒绝输出的归属对照

`Content_Brief_Architect_v0.1.md` 第 5 节列明 Brief **不得输出**的九项，
在本合同中逐项有且只有一个承接者：

| # | Brief 拒绝输出的项 | 承接 Skill | 本合同位置 |
|---:|---|---|---|
| 1 | 完整逐句口播稿 | Creative Script | 2.3 第 4 段 |
| 2 | 完整对白 | Creative Script | 2.3 第 4 段 |
| 3 | 逐镜头编号 | Production Director | 2.4 第 1 段 |
| 4 | 每个镜头的景别、机位、运动与秒数 | Production Director | 2.4 第 1 段 |
| 5 | 最终标题 | Publishing & Packaging | 2.5 第 1 段 |
| 6 | 最终封面文案 | Publishing & Packaging | 2.5 第 2 段 |
| 7 | 完整图文正文 | Creative Script | 2.6 |
| 8 | 最终发布文案 | Publishing & Packaging | 2.5 第 3 段 |
| 9 | 成片字幕全文 | Production Director | 2.4 第 5 段 |

九项**零遗漏、零重叠**。任何一项被两份 Skill 同时生成，或被三份都不生成，
即为职责划分失效，须回到本合同修订，不得由执行侧临时分配。

### 2.7.1 看起来共享、实际已切分的交付项

以下六项容易被两份 Skill 同时认领。本合同已逐项切开，
P05—P07 不得重新分配，也不得双方都做：

| 交付项 | 归 A | 归 B | 切分依据 |
|---|---|---|---|
| 屏幕文字 | Creative Script：**要显示哪些字** | Production Director：**何时出现、放哪里、什么形式** | 说什么归脚本，怎么呈现归导演 |
| 字幕 | — | Production Director：成片字幕全文（口播转写） | 字幕是口播的转写，不是新内容 |
| CTA | Content Brief（上游）：**有还是没有** | Publishing：**具体措辞与出现位置** | 要不要引导是决策层，怎么说是包装层 |
| 封面 | Publishing：指定首图、写封面文案 | Production Director：把首图生产出来 | 定什么是封面归包装，做出来归制作 |
| 补录 | Creative Script：提出需要补一句什么 | Production Director：是否新增拍摄、排进哪一天、对 `accepted_mode` 的影响 | 内容缺口归脚本，资源决定归导演 |
| 结论强度 | Creative Script／Content Brief：**决定** | Production Director：只作删减的**影响说明**，触发向上重判 | 判断完整性不得由制作层削减 |

判据仍是第 2.7 节那一条：**任何一项被两份 Skill 同时生成，即为职责划分失效。**
「切开」不等于「共有」——每一行的 A 与 B 是两件不同的东西。

### 2.8 通用输出纪律

- 只输出实例产物，不复述方法、不输出思考过程、自检过程、评分或结尾寒暄；
- 无事项时明确写「无」，不得为填满格式重复同一结论；
- 谱系字段（第 8 节）**不由模型输出**，由归档侧填写，见 8.3.1。

## 3. 不得重新选择的上游决定

三份 Skill 只能继承、引用与执行以下已确认事项，**不得重新选择、扩大或缩减**。

### 3.1 来自 Matrix 与账号责任卡

- 账号唯一使命；
- 核心顾客状态；
- 不可替代的组织能力；
- 持续表达者；
- 账号人格内核与关系姿态；
- 跨账号引用纪律；
- 确认与决定权限；
- 哪些角色未独立建号。

### 3.2 来自 Campaign 决策包

- 经营任务与目标顾客；
- 唯一主要顾客变化；
- 主讲账号；
- 参战账号组合与各自身份；
- 哪个账号本轮不发布；
- 内容数量上限；
- 已确认的核心内容顺序；
- 已确认的接力关系；
- 统一行动入口；
- 主承接人与替补；
- 申请、确认与履约的边界；
- 已确认的事实负责人；
- Founder 升级边界。

### 3.3 来自 Content Brief

- 唯一顾客情境与问题；
- 顾客当前卡点；
- 内容后需要形成的唯一新判断；
- 核心内容判断；
- 选定的证据链；
- 必须使用的素材；
- 出镜人与事实确认人；
- 必须表达与明确不得表达；
- CTA 或无 CTA 的决定；
- 发布、降级、取消或不发条件。

### 3.4 来自 Production Profile

- `accepted_mode`；
- 已声明的人员时间预算；
- 已声明的素材可用边界。

### 3.5 发现更优方案时

任何时候都不得因为「这样更好、更完整、更对称、更容易拍、更容易火」而改动锁定项。

发现更优方案时，只能写入产物的
`进入下一环节前必须确认的事项`，由人决定是否回到上游。

需要回到上游时，沿用 `C6_FOUNDER_CONFIRMED_v0.1.md` 的
**最上游实际失效结论回退原则**：先判断新事实实际破坏了哪一个已确认结论，
再返回其中最上游的一项，只重新判断实际受影响的下游结论。

## 4. Production Profile：性质与来源

### 4.1 它不是第四份 Skill

Production Profile 与 Production Feasibility Gate 是三份 Skill 的共享前置合同。
不单独建设为大型 Skill，不产生内容判断，不参与创意决策。

### 4.2 声明优先于推断

> `requested_mode` 与 `available_mode` 必须由人**声明**，不得由系统从夹具**推断**。

系统可以列出已登记的证据（人员时间预算、制作团队编制、预算限制、
已有素材清单），但把证据折算成哪一档模式是人的声明。

未获声明时记为未声明，可行性门按 `NEEDS_RESOURCE` 处理，不得默认取任何一档。

### 4.3 新增建模词声明

`Production Profile`、`Capture Mode`、`Augmentation Mode`、
`Production Feasibility Gate` 及其全部取值，在本任务之前的仓库中零命中，
属**本轮新增建模词**，不得被描述为既有约定，不得回写进任何历史文件。

## 5. Production Profile 最小字段

| 字段 | 必填 | 含义 |
|---|---|---|
| `profile_id` | 是 | 本次制作条件声明的编号 |
| `profile_version` | 是 | 版本号 |
| `declared_by` | 是 | 声明人角色 |
| `declared_at` | 是 | 声明日期 |
| `scope` | 是 | 适用范围（哪个 Campaign／哪些内容单元／哪个时间窗） |
| `requested_mode` | 是 | 用户希望的制作方式，双轴取值 |
| `available_mode` | 是 | 当前真实可用条件，双轴取值；未声明时写 `NOT_DECLARED` |
| `minimum_viable_mode` | 是 | 最低可运行方式，双轴取值 |
| `recommended_mode` | 是 | 系统推荐方式，双轴取值 |
| `accepted_mode` | 是 | 用户最终接受方式，双轴取值；未裁决时写 `NOT_ACCEPTED` |
| `people_time_budget` | 是 | 逐人可投入时间与形式（如集中拍摄／补录／事实确认／访谈） |
| `crew` | 是 | 制作岗位编制 |
| `reusable_assets` | 是 | 可复用已登记素材编号及其可用边界 |
| `on_camera_authorized` | 是 | 已授权出镜的人或身份，及其必须标注的真实属性 |
| `budget_constraints` | 是 | 明确不具备的预算项 |
| `venue` | 是 | 可用拍摄场地；未登记时写 `NOT_REGISTERED` |
| `equipment` | 是 | 可用设备；未登记时写 `NOT_REGISTERED` |
| `platform_status` | 是 | 已锁定平台名称，或 `PLATFORM_UNCONFIRMED` |
| `feasibility_status` | 是 | 第 7 节四值之一 |
| `feasibility_reason` | 是 | 判定理由；非 `FEASIBLE` 时必须写明差距与已尝试的降级路径 |

`NOT_DECLARED`、`NOT_ACCEPTED`、`NOT_REGISTERED` 是合法取值，
表示「尚未声明／尚未裁决／未登记」。**不得用推断值填补**，也不得留空。

`accepted_mode` 为 `NOT_ACCEPTED` 时：

- Creative Script 可以生成，但顶层状态最高为 `READY_WITH_CONDITIONS`；
- Production Director **不得**生成详细分镜与拍摄日执行计划；
- Publishing & Packaging **不得**生成发布包装。

## 6. Capture Mode 与 Augmentation Mode

### 6.1 Capture Mode

| 值 | 含义 |
|---|---|
| `ASSET_REUSE` | 不新增拍摄，只使用已有素材 |
| `SOLO_MOBILE` | 一人自拍自录 |
| `LEAN_MOBILE_TEAM` | 小规模团队 |
| `STANDARD_TEAM` | 常规制作团队与常规设备 |
| `CAMPAIGN_PRODUCTION` | 战役级制作，含外部人员、场地或预算 |

### 6.2 Augmentation Mode

| 值 | 含义 |
|---|---|
| `NONE` | 不使用生成式增强 |
| `AI_ASSISTED` | 使用生成式能力辅助，不替代真实素材 |
| `AI_HYBRID` | 真实素材与生成素材混合成片 |

### 6.2.1 资源强度次序（降级时使用）

四个**新增拍摄**档位按资源强度从低到高排列：

```text
SOLO_MOBILE < LEAN_MOBILE_TEAM < STANDARD_TEAM < CAMPAIGN_PRODUCTION
```

「降级一档」指在该序列上向左移动一位。

`ASSET_REUSE` **不在该序列上**。它不是「更省的拍摄」，而是**不拍摄**。
因此从任一档转为 `ASSET_REUSE` 不是降一档，是换一条路径，必须单独写明：
哪些内容改由已有素材承担、哪些判断因此不能再成立。

`Augmentation Mode` 同样有序：`NONE < AI_ASSISTED < AI_HYBRID`，
但**提高 Augmentation 不算降级补偿**——不得以「加 AI 生成」抵消拍摄条件不足。

### 6.3 组合与书写

两轴独立取值，组合书写为 `Capture Mode / Augmentation Mode`，
例如 `LEAN_MOBILE_TEAM / NONE`。

### 6.4 枚举名不等于事实

枚举名中的 `MOBILE`、`TEAM`、`PRODUCTION` 只是档位名称。
选择某一档**不构成**对设备、场地或人员的事实登记。
设备与场地未登记时，`equipment` 与 `venue` 必须写 `NOT_REGISTERED`，
不得因为选了 `SOLO_MOBILE` 就推断「已确认使用手机拍摄」。

### 6.5 Augmentation 非 NONE 时的附加义务

`Augmentation Mode` 不为 `NONE` 时，Production Profile 必须同时声明：

- 生成内容的标注方式；
- 不得生成的对象（至少包括：真实顾客、真实试穿效果、
  未登记的商品外观与性能、真实经营场景、真实服务过程）；
- 生成素材与真实素材在成片中的区分方式。

**不得用生成素材冒充真实拍摄事实**，不得用生成画面充当一手证据。

## 7. 可行性状态

| 状态 | 含义 | 后续动作 |
|---|---|---|
| `FEASIBLE` | 在 `accepted_mode` 下可完整执行 | 正常进入下一段 |
| `FEASIBLE_WITH_REDUCTION` | 降级后可执行 | 必须写明降级到哪一档、删除了什么、结论强度是否下降 |
| `NEEDS_RESOURCE` | 缺一项明确资源即可执行 | 只提出一项区分力最高的缺口；补齐后重新过门；仍不成立则转 `BLOCKED` |
| `BLOCKED` | 当前条件下不可执行 | 不得生成纸面产物；顶层状态 `PRODUCTION_NOT_FEASIBLE` |

### 7.1 降级顺序

降级必须按 `C6_FOUNDER_CONFIRMED_v0.1.md` 已确认的顺序，不得跳步：

> 删除无依据内容 → 降低结论强度 → 改到现有事实能够完整回答的位置
> → 延期或取消 → 返回最上游实际失效的结论

生产层的降级**只削减覆盖面、数量与制作复杂度**，
不削减事实确认、判断完整性与顾客价值。

### 7.2 BLOCKED 的作用域

`BLOCKED` 只作用于当前内容单元。
其余不依赖该条件的内容单元继续有效，不得因一条不可行而停止整条 Campaign。

## 8. Artifact 状态、父子引用、内容 Hash 与版本

### 8.1 五个状态

| 状态 | 含义 | 可否作为下游输入 |
|---|---|---|
| `DRAFT` | 已生成，未经检查 | 否 |
| `VALIDATED` | 已通过硬门与一致性检查，未经人接受 | 否 |
| `USER_ACCEPTED` | 已由对应负责人接受 | 是 |
| `STALE` | 上游发生变化，需重新检查 | 否 |
| `FAILED` | 明确不成立 | 否 |

**只有 `USER_ACCEPTED` 可以作为下游输入。**

合法迁移：

```text
DRAFT → VALIDATED → USER_ACCEPTED
DRAFT → FAILED
VALIDATED → FAILED
USER_ACCEPTED → STALE      （上游变化）
STALE → VALIDATED          （重新检查后仍成立）
STALE → USER_ACCEPTED      （重新检查并重新接受）
STALE → FAILED             （重新检查后不成立）
```

不得由 `DRAFT` 直接跳到 `USER_ACCEPTED`。
不得把 `STALE` 直接改回 `USER_ACCEPTED` 而不记录重新检查的判断。

### 8.2 产物编号

| 产物 | 编号前缀 | 示例 |
|---|---|---|
| Content Brief（上游既有） | `BRF-` | `BRF-SUHE-001` |
| Creative Script | `SCR-` | `SCR-SUHE-001` |
| Production Package | `DIR-` | `DIR-SUHE-001` |
| Publishing Package | `PUB-` | `PUB-SUHE-001` |

格式：`<前缀>-<账号姓名全拼大写>-<三位零填充序号>`，分隔符为半角连字符。

序号是 **Pack 级全局发布顺位**，不是每个账号各自从 `001` 起编。
（`RUN_001` 实例中，先发布的内容单元得 `001`、后发布的得 `002`，即为此义。）

子产物沿用父产物的账号段与序号段，只改前缀，使谱系可直接读出。

**格式来源声明**：`brief_id` 的这一格式此前**从未被任何合同规定**——
`Content_Brief_Architect_v0.1.md` 第 8 节模板只写 `### brief_id：`，
`CONTENT_BRIEF_CONTRACT_v0.1.md` 第 6 节只列 `brief_id` 一项，
现有格式是 `RUN_001` 运行时自行形成的既成惯例。
本合同**在此把它固化下来**，以保证后续运行一致；
这是新增约定，不得被描述为既有合同条款，也不回写进已冻结文件。

### 8.3 谱系字段（归档侧必填）

| 字段 | 含义 |
|---|---|
| `artifact_id` | 本产物编号 |
| `artifact_type` | `CREATIVE_SCRIPT` / `PRODUCTION_PACKAGE` / `PUBLISHING_PACKAGE` |
| `artifact_version` | 本产物版本，从 `v1` 起递增 |
| `artifact_status` | 第 8.1 节五值之一 |
| `parent_id` | 父产物编号 |
| `parent_version` | 父产物版本 |
| `parent_content_hash` | 父产物内容的 SHA-256 |
| `profile_id` | 所依据的 Production Profile 编号 |
| `profile_version` | 所依据的 Production Profile 版本 |
| `accepted_mode` | 生成时依据的 `accepted_mode` |
| `platform_status` | 生成时的平台状态 |
| `status_changed_reason` | 当前状态的原因；转 `STALE` 时必须写明由哪一项上游变化触发 |

#### 8.3.1 谁填这些字段

**模型不填谱系字段。** 归档侧（执行人）填。

理由有二：其一，语言模型算不出 SHA-256；其二，`CONTENT_PRODUCTION_CHAIN_PRD_v0.1.md`
第 12.2 节非目标明确「不新增自动评分器、RAG、数据库、代码节点或正式前端」，
不得为算哈希新增代码节点。

这与仓库既有做法完全一致：`CONTENT_BRIEF_DIFY_RUN_MANIFEST_v0.1.md` 中的
`DSL SHA-256`、`Skill SHA-256`、`Contract SHA-256`、`Golden SHA-256`、
`输入 SHA-256`、`原始输出 SHA-256`，全部由归档侧在运行后回填，模型从不接触。

因此：

- **模型输出**只需按第 2.3—2.5 节给出业务内容，
  并在第 0 段「运行结论」写出人可读的来源标识（父产物编号与版本、
  所依据的 `accepted_mode` 与平台状态）；
- **归档侧**在归档时补齐第 8.3 节全部字段，包括 `parent_content_hash`
  与 `artifact_status`；
- **`artifact_status` 由人判断**，不由模型自称。
  模型不得在输出中自称 `VALIDATED` 或 `USER_ACCEPTED`。

### 8.4 内容 Hash 口径

- 算法：SHA-256；
- 对象：父产物文件的**业务内容部分**，即从产物首行标题起至文件末尾；
- 不含：谱系字段块本身、归档说明、运行 Manifest 表；
- 用途：判断父产物是否发生实质变化；
- 与仓库既有 `原始输出 SHA-256` / `输入 SHA-256` / `DSL SHA-256` 口径并列，
  不互相替代。

### 8.5 版本递增规则

- 父产物内容变化 → 父产物 `artifact_version` 递增；
- 子产物重新生成 → 子产物 `artifact_version` 递增，
  并更新 `parent_version` 与 `parent_content_hash`；
- 仅状态变化、不改内容 → 版本不变，只更新
  `artifact_status` 与 `status_changed_reason`。

## 9. 上游变化导致下游失效的规则

### 9.1 传播表

| 上游变化 | 直接失效 | 连带失效 |
|---|---|---|
| Content Brief 内容变化 | `SCR-` | `DIR-`、`PUB-` |
| `accepted_mode` 收紧 | `SCR-`、`DIR-`、`PUB-` | — |
| `accepted_mode` 放宽 | `DIR-`、`PUB-` | `SCR-` 保持原状态，须记录未重写的判断 |
| Creative Script 内容变化 | `DIR-` | `PUB-` |
| Production Package 内容变化 | `PUB-` | — |
| 真实成片状态与制作包不符 | `PUB-` | — |
| 平台由 `PLATFORM_UNCONFIRMED` 变为已锁定 | `PUB-` | — |
| 承接条件失效 | `PUB-`（仅 CTA 部分） | 若 Brief 的 CTA 决定被推翻，则回到 Content Brief |
| 制作包降级改变了结论强度 | `SCR-` → `STALE`（向上） | `PUB-` |
| 发布包退回意见指出包装与成片不匹配 | `DIR-` → `STALE`（向上） | — |
| 上游被判 `FAILED` | 全部下游 → `FAILED` | — |

### 9.2 STALE 的含义

`STALE` 是**必须重新检查**，不是必须重做。

重新检查后可以判定仍然成立并恢复 `VALIDATED` 或 `USER_ACCEPTED`，
但必须在 `status_changed_reason` 中记录该判断及依据。
**不得默认沿用**，也不得不经检查就恢复。

### 9.3 只检查真正依赖的下游

沿用 `C6_FOUNDER_CONFIRMED_v0.1.md` 第七节的收敛纪律：
返回上游后只检查真正依赖该结论的下游项，
不把「上游变化」理解为删除全部历史产物或重跑整条链。

### 9.3.1 两条向上传播是例外，不是常态

第 9.1 节有两行标注「向上」。它们是**下游发现上游已不成立**的情形，
不是下游有权修改上游。

处理方式：下游只**报告**并把上游置 `STALE`，由对应负责人重新判断；
下游不得代为改写上游内容。这与第 3.5 节一致。

### 9.4 与决策链的边界

生产链的失效传播**不自动向上传播到决策链**。
需要重开 `C1`—`C6` 或 Content Brief 时，按第 3.5 节走人工判断，
不由生产链自行触发。

## 10. 平台确认与未确认状态

### 10.1 两个状态

| 状态 | 含义 |
|---|---|
| 已锁定平台 | Founder 已明确锁定唯一发布平台，记录平台名称与锁定日期 |
| `PLATFORM_UNCONFIRMED` | 平台未确认 |

当前状态为 `PLATFORM_UNCONFIRMED`（对应《笛语项目基线》第八节 `D49`）。

### 10.2 平台未确认时的义务

| Skill | 行为 |
|---|---|
| Creative Script | 输出平台中立脚本；不写平台专属钩子、时长惯例与形式套路 |
| Production Director | 不锁定画幅、时长、字幕规格、封面尺寸；把平台规格列为待适配项 |
| Publishing & Packaging | 输出平台中立包装，附「平台规格待适配清单」；顶层状态 `READY_WITH_CONDITIONS` |

统一禁止：

- 自动选择任何平台；
- 使用平台特有挂载、评论、私信、链接或尺寸能力；
- 把包装描述为某平台定制；
- 以「大多数平台都这样」代替平台规则。

平台未确认**不等于**整条生产链无法运行。

### 10.3 平台锁定后

相关 `PUB-` 产物按第 9.1 节转 `STALE`，补做平台规格适配。
`SCR-` 与 `DIR-` 是否需要重新检查，按实际是否依赖平台规格判断。

## 11. 事实、观察、专家判断与假设的分类

系统中存在**两条不同的分级轴**，严禁混用与跨轴改写。

### 11.1 内容证据分级（既有，沿用）

出处：`Content Brief Architect v0.1` 第 1 节（五类之分）与第 4 节步骤三（证据地图逐条标注）。
用于**单条内容里出现的每一条信息**：

| 类别 | 含义 |
|---|---|
| 已登记事实 | 企业已登记、可确认的事实 |
| 亲历观察 | 本人经手过程中的观察 |
| 专业判断 | 基于事实形成的专业判断 |
| 明确标注的设计情境 | 允许虚构但必须打标的情境 |
| 待验证变量 | 尚不能下结论、需继续确认的变量 |

三份生产 Skill 必须逐条继承 Brief 证据地图的分类，**不得升格**：
不得把「专业判断」写成「已登记事实」，
不得把「明确标注的设计情境」在脚本、字幕或发布文案中丢失标注，
不得把「待验证变量」写成已解决。

### 11.2 知识资产证据分级（本轮新增）

用于**平台、行业与趋势知识条目**：

| 等级 | 含义 | 可否作为硬规则 |
|---|---|---|
| `OFFICIAL_RULE` | 平台官方公开规则 | 可，须带来源、生效日期与复查日期 |
| `OBSERVED_PATTERN` | 可观察到的做法与规律 | 否，只能作为条件性建议 |
| `EXPERT_JUDGMENT` | 领域专家判断 | 否，须标注适用条件 |
| `MODEL_HYPOTHESIS` | 模型推演 | 否，不得进入正式事实 |
| `FUTURE_HYPOTHESIS` | 未来趋势判断 | 否，须带完整复查字段 |
| `INSTANCE_DECISION` | 单次实例决定 | 否，不得升级为通用规则 |

`FUTURE_HYPOTHESIS` 必须同时包含：来源、适用条件、复查日期、失效条件、后续验证方式。
缺任一项即不得登记。

### 11.3 跨轴禁令

- 一条 `OBSERVED_PATTERN`、`EXPERT_JUDGMENT`、`MODEL_HYPOTHESIS` 或
  `FUTURE_HYPOTHESIS`，**不得**在内容中被表达为「已登记事实」；
- 一条企业「已登记事实」，**不得**被登记为平台知识条目；
- 平台观察、头部 IP 做法、模型判断与未来趋势
  **不得自动进入正式事实，也不得自动变成 Skill 硬规则**。

### 11.4 共享知识资产的读取边界

四项共享知识资产（`Platform Intelligence Pack`、`Current Content Benchmark Pack`、
`Voice & Expression Profile`、`Creative & Production Quality Gate`）
本轮**只定义、不建设**。

**装载路径（本轮定义，待建设后生效）**：

四项资产建成后，作为第 1.1 节共同输入的第 8 项进入运行输入，
形态是**已经过 Founder 集中裁决、抽象为判断方法的条目**，
不是研究过程记录、不是案例原文、不是账号名与 URL。

区分：

| | 可否进运行输入 |
|---|---|
| 研究过程资产（证据集、R1—R5 记录、未采用方案、裁决卡） | 否，见研究协议第 12.1 节 |
| 已发布的共享知识资产（裁决后抽象条目） | 是，作为第 1.1 节第 8 项 |

未建设前，该项为空，不影响三份 Skill 运行。

读取纪律：

- 是被读取的参考，不是裁决者；
- 不得覆盖企业已确认事实、上游锁定项或 Founder 裁决；
- 与已确认事实冲突时，以已确认事实为准；
- 不得把全部行业研究历史直接塞进 `SKILL.md`。

## 12. 三份 Skill 共同 Hard Gate

以下为三份 Skill **共同必守**的硬门。任一违反，产物判 `FAILED`，不得以条件化通过。

### 12.1 事实与身份

1. 不得新增任何未登记的商品功能、质量、效果、适穿人群、洗护与耐用性结论；
2. 不得由库存数字推断库存压力、稀缺或促销理由；
3. 不得把内部演示人员包装成现实顾客；出镜身份的真实属性必须在成片中可见地标注；
4. 不得把不同来源的事实拼接成同一个真实人物或同一段真实经历；
5. 不得把匿名记录改写成带身份、职业、单位或购买金额的具体顾客；
6. 不得把他人经手的一手过程写成本账号亲历；跨账号引用必须保留原始来源与确认责任；
7. 不得虚构顾客反馈、试穿差异、退换货、未成交原因或经营冲突；
8. 不得用通用知识、行业惯例或 Founder 背书代替一手事实确认。

### 12.2 承诺与承接

9. 不得新增未经确认的服务政策、时效、容量或对外承诺；
10. 不得由不同内容各自创造入口、联系人、时效或服务承诺；
    引用承接方式时只能引用上游已确认的唯一正式入口；
11. 不得把「已收到、稍后联系、已转门店、正在查询、已登记意向、请等待回复」
    一类表述描述为申请已被确认；
12. 不得制造名额、排队、稀缺、抢约或紧迫感；
13. 不得把内容发布平台自动等同于承接入口；
14. 发布账号、出镜人、素材提供者、事实确认人、申请承接人与现场服务人员
    是不同角色，不得自动合并。

### 12.3 表达与合规

15. 不得制造年龄、身材、身份焦虑，不得俯视或评判顾客；
16. 不得通过暗号、藏码、诱导跳转等方式规避平台规则；
17. 不得因用户愿意承担风险而生成明确违规手法；
18. 平台未确认时不得伪装成平台定制；
19. 不得以夸张包装弥补正文不足；标题、封面与发布文案不得超出正文已支撑的结论。

### 12.4 资源与越界

20. 不得超出已接受的 `accepted_mode`；
21. 不得自动增加人员、场地、设备或预算；
22. 不得生成明显超出 Production Profile 的纸面大片；
23. 不得重新决定第 3 节列明的任何上游锁定项。

### 12.5 内容质量底线

24. 沿用 `C1` 已确认的五项底线：**共情、观点、烟火气、真实感、内容叙事化**。
    五项不是五个栏目，不得机械打卡，不得拆给不同段落各承担一项。
    若产物事实安全、格式完整，却整体退化为知识清单、参数表、培训课、
    品牌说教或机械模板，应改写角度；无法修正时写明不发理由。

### 12.6 沉没成本否决

25. 已经投入拍摄、剪辑或排期，**不构成必须发布的理由**；
    账号需要保持活跃、产能尚有空余或计划已经写好，同样不构成理由。

## 13. Fail-Open 与 Fail-Closed 边界

### 13.1 Fail-Open（继续，但写成条件）

- 发布平台未确认；
- 部分素材未就位；
- 承接入口、人员、受理边界或确认方式未确认；
- 产能存在不确定性；
- 成片尚未拍摄，但成片状态可由制作包与明确标注的成片夹具确定；
- `venue` 或 `equipment` 为 `NOT_REGISTERED`，但不影响当前 `accepted_mode` 成立。

处理方式：顶层状态 `READY_WITH_CONDITIONS`，
并写明**每个条件影响哪一条内容**，不得把局部缺口写成整体无法生成。

### 13.2 Fail-Closed（停止，不得生成）

- 缺少已被接受（`USER_ACCEPTED`）的上游产物；
- 两条同等效力正式结论不兼容；
- 没有任何一条可确认、可公开、可制作的事实链；
- 可行性门为 `BLOCKED`；
- `accepted_mode` 为 `NOT_ACCEPTED` 且当前段要求它（见第 5 节）；
- Publishing 段缺成片状态且无成片夹具；
- 继续生成必须依靠编造事实、越权承诺或伪装平台定制；
- 继续生成必然超出 `accepted_mode`。

### 13.3 判据

> **缺条件可以条件化，缺事实与缺授权不能条件化。**

不得为了让运行结果通过而放宽本节边界，
不得把 Fail-Closed 情形改写成 `READY_WITH_CONDITIONS`。

## 14. 普通执行问题与 Founder 升级边界

沿用 `C6_FOUNDER_CONFIRMED_v0.1.md` 第九节，**不新增升级路径**。

### 14.1 由对应负责人处理，不交 Founder

- 拍摄窗口变化、剪辑延期、发布时间调整；
- 镜头缺失、单条内容删改、素材替换；
- 内容数量调整与单条内容延期；
- 普通负面评论、事实误解、日常解释与内容撤回；
- 一般容量波动与当日配额无法确认。

### 14.2 必须交 Founder

- 正式品牌立场；
- 品牌级公开回应；
- 重大经营取舍；
- 长期账号使命；
- 组织权责变化；
- 重大资源投入；
- 新增或改变对外政策与承诺。

### 14.3 判据

「可能影响声誉」或「评论较负面」本身**不足以**触发 Founder。
只有需要以品牌名义对争议、经营立场或对外责任作统一正式表态时才升级。

### 14.4 谁可以决定不发

沿用 `C6` 第八节：「不发」是正常专业权限，不是失败，不要求每次交 Founder。
对应账号负责人、原始事实负责人与制作执行可在各自权限内决定
删除、降级、改题、延期或不发。

## 15. P02—P09 不得修改的冻结项

以下条目在本合同落盘后冻结。P02—P09 **不得修改、不得重新解释、不得增删枚举值**：

1. 三份生产 Skill 的名称与不可互换职责（第 2、3 节与 PRD 第 6 节）；
2. Production Profile 不是第四份 Skill（第 4.1 节）；
3. Production Profile 最小字段表（第 5 节）；
4. `Capture Mode` 五值与 `Augmentation Mode` 三值（第 6 节）；
5. 四种可行性状态（第 7 节）；
6. 五个 Artifact 状态及合法迁移（第 8.1 节）；
7. 谱系字段表与内容 Hash 口径（第 8.3、8.4 节）；
8. 失效传播表（第 9.1 节）；
9. 五个顶层输出状态（第 2.1 节）；
10. 两条证据分级轴与跨轴禁令（第 11 节）；
11. 四项共享知识资产的名称与读取边界（第 11.4 节）；
12. 二十五条共同 Hard Gate（第 12 节）；
13. Fail-Open 与 Fail-Closed 判据（第 13 节）；
14. Founder 升级边界（第 14 节）；
15. 发布反馈学习延期裁决 `PRD-DECISION-FEEDBACK-LEARNING-DEFERRED-V1`（PRD 第 14 节）。

### 15.1 修改路径

确需修改冻结项时：

- 不得由执行侧自行修改；
- 不得通过新建同义文档绕过；
- 必须由 Founder 明确裁决，并以 `v0.2` 版本落盘，说明改了什么、为什么、影响哪些已有产物。

### 15.2 明确禁止制造通过

不得为了让运行结果通过而修改本合同、PRD、研究协议、Golden、固定测试输入或评分标准。
不得删除失败或冲突证据。

## 16. 本合同不做什么

- 本合同只约束 Content Brief 之后的三份生产 Skill，不约束 Matrix、Campaign
  与 Content Brief，也不重新解释它们的任何结论。
- 本合同只定义字段、状态与边界，不定义 Dify 节点编排、模型参数与提示词写法。
- 本合同不建设注册表、判据库、错误码表，也不为完整性增设层级。
- 本合同不替代 Founder 裁决：它写明什么必须交人，不代替人作出裁决。
- 本合同不能证明三份 Skill 做得出好内容——本轮未编写 Skill、未调用模型、未运行 Dify。
- 本合同不能证明这些字段够用：它们尚未经任何一次真实运行检验，检验发生在 P05—P08。

## 17. 冻结声明

本合同在 `CONTENT-PRODUCTION-P01` 落盘后冻结。

- 冻结时间：`2026-08-21`（执行日）
- 冻结基线：`349673b76fc46a3ffc5170344956297d24017e7c`；
- 冻结后不得修改；
- 不得为了让运行结果通过而修改本合同、Golden、固定测试输入或评分标准。
