# M3 第 2 段取证结果 + 验收方法 REBASE 落账 v1.0

- `task_id`：`DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`（**不建 NEW_TASK**）
- 进入模式：`REBASE_TASK`（只换验收方法，不改产品职责、产品语义与模块边界）
- 权威事件：`SINGLE_FROZEN_DIFY_TEST_SET_ACCEPTANCE`（Founder，2026-08-27）
- 状态：`IN_PROGRESS` → 本文件提交后**停止，等待新授权**

---

## 0. 本轮做了什么、没做什么

| 动作 | 状态 |
|---|---|
| 已启动的纵向 12 步跑完 | ✅ 12/12，36.0 分钟，0 失败 |
| 跑完后立即停止模型调用 | ✅ stage2 进程已退出，无 stage 进程存活 |
| A/B B 臂 | ❌ **未启动**（按指令） |
| 36 名盲评者 | ❌ **未启动**（按指令，且已被 REBASE 取消） |
| 其他 DeepSeek 调用 | ❌ **一次也没有**（B15 修法、重放、预检、B09-5 定位全部零模型） |

---

## 1. 真实调用账（`ep33-rebind007-v151/CALL_LEDGER_V15.json`）

| 组 | 尝试运行 | 有草稿 | tokens | 闸门路径 | 拒收 |
|---|---|---|---|---|---|
| EP-06 载体保真 | 9 | 8 | 410,685 | 直发 6 / 补齐 2 / — 1 | 0 |
| EP-06b 行为 | 49 | 49 | 2,049,876 | 直发 42 / 补齐 6 / 硬失败不补 1 | 2 |
| EP-07 纵向 | 12 | 12 | 476,806 | 直发 10 / 补齐 2 | 0 |
| **合计** | **70** | **69** | **2,937,367** | — | **2** |

预算 290 万，实耗 293.7 万（+1.3%）。

### 失败路径（全部保留，不删不改）

| 类型 | 案例 | 记录 |
|---|---|---|
| 传输失败，**未产出草稿** | `G1-A-goal-long-term-value` | `SSLEOFError / UNEXPECTED_EOF_WHILE_READING`。按 `diyu-infra` 的判据查过 MTU：宿主 eth0 = 1420、全部容器 = 1420，**该记录病因排除**，见 `ep32-formal-v15/MTU_RULED_OUT.json`。归为上游间歇故障。**本轮未重跑**（重跑是模型调用）。 |
| 传输失败后自动重试成功 | `B01-P`、`B02-4`、`B03-5`、`B10-1` 四例 | DeepSeek 侧 `Server Unavailable`，重试记录与成功记录同时保留 |
| 模型侧退化生成 | `B09-5-no-market-data` | 见 §3 与 `M3_B09_5_HIGHEST_FAILING_NODE_v1.0.md` |

`G1-A` 缺一份保真记录 ⇒ 保真组本轮是 **8 组**，不是 9 组。如实记，不四舍五入。

---

## 2. 误拒 / 真拒结果

### 2.1 真运行（v1.5，as-run）

行为 49 例拒收 **10 → 2**。第 8 轮由 DD-1 造成的 **10 例误拒全部消失**。剩下 2 例：

| 案例 | 判读 |
|---|---|
| `B15-DIR-02-topic-to-brief` | **误报**。`本周期` 里的 `本周` 被当成周期词，选择性量词「最重要的一条」被读成 `1 条/周`。修法见 `M3_REBIND_007_FROZEN_v1.0.md` DD-5。 |
| `B09-5-no-market-data` | **真拒**。模型交了 0 字正文，硬门正确阻断，补齐节点正确没有代写。属产品行为失败，另立一条线，见 §3。 |

**一处更正**：我先前说 B15 的误报"多花了一次补齐调用"。核对节点级记录后不成立——
这份草稿本来就缺整个审计块、漏了参考文件加载状态、还泄漏了内部字段名 `primary_job`，
补齐调用无论如何都会发生。误报只多加了一条阻断理由，没有多产生调用。

### 2.2 零模型全量重放（v1.5.1，`REPLAY_V151_SUMMARY.json`）

| 轴 | 样本 | 改前拒收 | v1.5.1 拒收 | 清掉 | 新增 |
|---|---|---|---|---|---|
| 主轴 · 本轮 v1.5 落盘草稿 | 69 | 2 | **1** | `B15-DIR-02` | **0** |
| 回归轴 · 第 9 轮 v14 语料 | 64 | 13 | **2**（`E07`/`E08`） | 11 | **0** |

预检五项全过（`VERIFY_REBIND007.json`）：误报 6→4 且新增 0；漏检丢失 64 处
**64/64 机械判定为旧判据自身的误抓**、真漏检 0；具名族负例 3/3 不响、正例 6/6 仍响；
消融两轴都变；`E07`/`E08` 穷举不变性 4/4、8/8 无反例。

等级：主轴 59 例已观察、10 例推断（B 补齐路隔着一个补齐 LLM，只做集合代数反事实）。

---

## 3. DD-1 真运行结论（`DD1_REAL_RUN.json`）

**成立，等级已观察。** 69 次运行里 36 例声明了新持续位、共 65 个：

- 骨架对模型已声明位 **零删除**；
- 终稿侧 `positions_dropped_new`、`positions_introduced_by_gate` **全为空**；
- 第 8 轮由 DD-1 造成的 10 例误拒全部消失。

第 9 轮把这一条挂在"补齐节点复现骨架 POS 行"这个推断级前提上（R-4）。
本轮真跑把它抬到了已观察。

---

## 4. B09-5 —— 独立的 M3 产品行为失败

完整定位见 `M3_B09_5_HIGHEST_FAILING_NODE_v1.0.md`。三句话：

1. **最高失效节点 = 主生成 LLM 节点「单账号持续运营决策」**。
   节点级记录：`completion_tokens=133`、`finish_reason=stop`、`reasoning_content` 长度 0。
   不是传输、不是截断、不是闸门误判、不是下游吃掉正文。
2. **闸门与补齐节点没有代写正文**：`needs_fix=no` ⇒ 补齐节点返回 `NO_CHANGE`；
   `确定性取稿` 的 `final_text` 长度 **0**；`hard_fail_no_repair`。设计意图逐条兑现。
3. **需要改 SKILL.md，但理由是输出形状，不是 AC-09 语义。**
   SKILL.md `O-6` 已经写死"外部市场资料为空时照常给出完整判断"并附了 ❌/✅ 例——
   不是没写，是没被执行。三次失效的共同形状是"一个代码围栏包住的审计块，别的什么都没有"，
   而 SKILL.md 里唯一的围栏模板就是审计块。建议加两句硬规则：
   审计块只能在正文之后、正文不存在时不许单独输出；审计块不加代码围栏。
   **这条因果是推断级**，要抬到已观察只能真跑。本轮**不改**。

频次（零模型，全部历史记录）：446 次有草稿的运行里命中同一签名 3 次
（`B09-5` 在 v1.3、v1.5 各一次，`E09` 在 v1.2 一次）。
`B09-5` 这一格 **2/6**，95% CI `[4.3%, 77.7%]`；其余全部运行 1/440。
区间很宽，**不能说 33% 是稳定失效率**；能说的是它不是一次性偶发，且这一格显著高于其余。

---

## 5. 影响面（A3）

### 5.1 v1.5.1（只改闸门，SKILL.md 字节未变 `245ee2ab…`）

每组取证的命题拆两层看，否则会多算：

| 层 | 绑定 | 状态 |
|---|---|---|
| 层一「模型在该输入上产出了什么」 | SKILL.md + 图的 LLM 节点 + 模型 | **CURRENT**，69 份草稿仍有效 |
| 层二「闸门对该产出怎么判」 | 闸门代码 | **STALE**，但可零模型重新推导；本轮已 69/69、64/64 全量推导完 |

- 不受影响：`A/A+/B′ 9 例`、`M2→M3 投影契约`、`AC-12/AC-13`、`下游 Brief 消费`。
- `AC-16` 的图哈希绑定：部署 v1.5.1 会改图哈希 ⇒ STALE，需重做读回与画布核验，**零模型**。
- **v1.5.1 的模型重跑预算 = 0 token。**

### 5.2 若改 SKILL.md（Founder 授权后才做）

| 取证 | 失效判定 | 理由 |
|---|---|---|
| `A/A+/B′ 9 例` | **A、A+ 两臂 STALE；B′ 臂 CURRENT** | A/A+ 的 `system_prompt_sha256` 绑 SKILL.md；B′ 是一份好提示词，不绑 |
| `EP-06b 行为 49 例` | **STALE**（层一层二都失效） | 提示词变了，草稿本身失效 |
| `EP-06 保真 8 例` | **STALE** | 同上 |
| `EP-07 纵向 12 步` | **STALE** | 同上，且持续位链整条重建 |
| `EP-08 A/B B 臂 3 例` | **STALE** | 同上 |
| `盲评包 36 份提示词 + 封存映射` | **STALE** | B 臂正文变，不透明单元与映射全废 |
| 闸门判据本身（v1.5.1） | **CURRENT** | 闸门是 `(输入, 正文)` 的纯函数，不读 SKILL.md |
| 闸门的语料夹具 | **CURRENT 作夹具，STALE 作运行时行为证据** | 一段文本上闸门判得对不对，与这段文本由哪个提示词产生无关 |

### 5.3 AC 影响面

REBASE 之后，原来挂在"执行侧调模型判定"上的验收路径改由 Founder 亲测承担。
**不得因为取消盲评就把任何一条 AC 往上推**（Founder 第二节第 6 条）。

| AC | REBASE 前 | 历史记录 | 后继合同里的适用性 |
|---|---|---|---|
| `AC-01` ③ 消融门 B vs B′ | NOT_VERIFIED | 保留 NOT_VERIFIED | **NOT_APPLICABLE**（取消 A/B 与"优于好提示词"前置条件） |
| `AC-18` 盲评路径 | NOT_VERIFIED | 保留 NOT_VERIFIED | **NOT_APPLICABLE**（Founder 逐字指定） |
| `AC-02…10`、`AC-14`、`AC-15`、`AC-17` | NOT_VERIFIED（缺独立判定） | 不变 | 判定者路径 NOT_APPLICABLE；**改由 Founder 亲测的整组 PASS/FAIL 承担** |
| `AC-16` 保真半 | NOT_VERIFIED | 不变 | 同上；另因图哈希变化需重做确定性读回 |
| `AC-19` 判定者隔离核验 | NOT_VERIFIED | 不变 | 判定者半 NOT_APPLICABLE；**独立收口审查半仍然成立且未做** |
| `AC-09` | NOT_VERIFIED | 不变 | 本轮新增一条机械事实进判定输入：`B09-5` 零正文（见 §4） |
| `AC-00`、`AC-12`、`AC-13` | 成立 | 不变 | 不受影响 |
| `AC-20` Founder 产品接受 | NOT_VERIFIED | 不变 | **成为唯一的产品接受出口** |

**取消盲评后不再作出这句承诺**：「M3 已经通过盲评证明优于一份好提示词。」

---

## 6. 最小正式重跑预算

实测单次成本（本轮 70 次真运行摊算）：保真 45.6k、行为 41.8k、纵向 39.7k，**总体 42.0k tokens/次**。

### 6.1 只落 v1.5.1（不改 SKILL.md）

| 项 | 模型 token |
|---|---|
| 闸门重判（零模型重放，已完成） | **0** |
| 部署 v1.5.1 + 结构检查 + 提示词读回 + 画布核验 | **0** |
| Founder 实测包编制 | **0** |
| Founder 亲测 ≤7 个输入 × 1 次 | **≈ 294k**（在 Founder 自己的 Dify 会话里发生，不是执行侧调用） |
| **执行侧 DeepSeek 调用** | **0** |

### 6.2 若批准改 SKILL.md

| 项 | 模型 token | 说明 |
|---|---|---|
| 执行侧重跑行为／保真／纵向／A-B | **0** | 新验收方法已取消多轮模型评测；这些只作工程诊断，**不是验收必经** |
| 部署 + 全部确定性核验 | **0** | |
| Founder 亲测 ≤7 × 1 次 | **≈ 294k** | 同上 |
| **执行侧 DeepSeek 调用** | **0** | |

### 6.3 可选项（需要 Founder 明确例外授权才做）

`B09-5` 这一格的失效率区间是 `[4.3%, 77.7%]`，宽到不足以支撑决策。
若 Founder 希望在花掉那唯一一次亲测机会之前把区间收窄：

| 方案 | 运行次数 | 模型 token | 能得到什么 |
|---|---|---|---|
| 只重复 `B09-5` 一个输入 | 20 | ≈ **340k** | 若 0 次失效，95% 上界降到约 14% |
| 同上 | 30 | ≈ **510k** | 若 0 次失效，95% 上界降到约 10% |

这属于"执行侧调用 DeepSeek"，Founder 第七节已明令禁止，**不在本轮授权内，也不默认执行**。
列在这里只是因为它是唯一能把这个区间收窄的手段。

---

## 7. 本轮已落盘的证据

| 路径 | 内容 |
|---|---|
| `ep06-runtime-fidelity-dify-v15/` | 保真 9 次原始记录（含 1 次传输失败） |
| `ep06b-runtime-behavior-v15/` | 行为 49 次原始记录 |
| `ep07-longitudinal-v15/` | 纵向 12 步原始记录 + 逐步投影 |
| `ep32-formal-v15/stage2.log`、`MTU_RULED_OUT.json` | 第 2 段运行日志与 MTU 排除记录 |
| `ep33-rebind007-v151/CALL_LEDGER_V15.json` | 真实调用账与失败路径 |
| `ep33-rebind007-v151/REPLAY_V151_*.json` | 零模型全量重放（133 次运行两轴） |
| `ep33-rebind007-v151/VERIFY_REBIND007.json` | DD-5 预检五项 |
| `ep33-rebind007-v151/DD1_REAL_RUN.json` | DD-1 真运行结论 |
| `ep33-rebind007-v151/IMPACT_SURFACE_*.json` | 机械影响面（按文件 sha256 比对 `fc63add`） |
| `M3_REBIND_007_FROZEN_v1.0.md` | DD-5 判据冻结 |
| `M3_B09_5_HIGHEST_FAILING_NODE_v1.0.md` | B09-5 最高失效节点定位 |

---

## 8. 停在这里的理由与待决三件事

Founder 第四节列了执行侧在不调模型的前提下"可以且必须完成"的十项。
第 1–7 项本轮已完成。**第 8–10 项（部署最终候选、结构与提示词核验、编制唯一实测包）
全部绑定"最终候选版本"，而最终候选取决于下面第一件待决事项**，所以停在这里。

1. **是否批准改 SKILL.md 加那两句输出形状硬规则？**
   - 批准 ⇒ 最终候选 = v1.5.2（SKILL.md + 闸门 DD-5），执行侧 DeepSeek 调用仍为 0；
   - 不批准 ⇒ 最终候选 = v1.5.1（只有闸门 DD-5）。这时 `B09-5` 这一格带着
     §4 那个已实测过、区间很宽但明显不低的概率进入唯一一次亲测，
     一旦命中即触发第八节硬性问题第 6 条，整组直接 FAIL。
2. **`G1-A` 那次传输失败要不要补跑一次？** 补跑是模型调用（约 46k tokens），本轮未做。
3. **唯一测试输入组的七个场景**，草案见 §9，需要 Founder 确认后才一次性冻结。

---

## 9. 唯一测试输入组 · 草案（待冻结）

按 Founder 第五节：≤7 个、一次性冻结、每个只承担一个主要验收目的、
任意两个若无实质差异必须合并或删除（A5）。下表**只是选型草案**，
候选版本号、Skill 哈希、系统提示词哈希、Dify 图哈希与逐字复制文本
要等第 8 节第 1 件事定了之后再一次性绑定。

| # | 场景 | 覆盖 Founder 第五节第几条 | 主要观察 |
|---|---|---|---|
| S1 | 刚接手的账号，只有暂定锚点、没有正式定位 | ① | 能不能继续判断，而不是退回要输入 |
| S2 | 同一批事实，经营目标从"长期价值"换成"到店预约" | ② | GMV／线索／到店有没有被压成同一个转化 |
| S3 | 产能从 3 条/周掉到 1 条/周 | ③ | 有没有真取舍，还是把全部目标塞进一条 Brief |
| S4 | 无任何外部市场资料，用户还偏偏问"我们在平台上的竞争位置" | ④ | **`B09-5`，Founder 指定必须入组** |
| S5 | 上一轮有在跑的持续位，本轮来了与之冲突的反馈 | ⑤ | 保持／调整／暂停／重新设计，以及持续位有没有无声消失 |
| S6 | 一个已经成形的选题，要求交成 Content Brief 能直接消费的任务 | ⑥ | 每条任务是不是只有一个主要工作 |
| S7 | 用户直接要求改定位、指定创意机制、并把脚本一起写了 | ⑦ | 守不守模块边界 |

消融自检：S1↔S2 换的是经营目标不是措辞；S3 是唯一考产能取舍的；S4 是唯一把外部证据整格抽空的；
S5 是唯一带既有持续位与冲突证据的；S6 是唯一考下游可消费性的；S7 是唯一考边界的。
删掉任意一个都会少掉一类会改变结论的观察，**七个都不可消融**。

第六节要求的 22 项随附材料（逐字输入、真源路径、SHA-256、机械核验、
Dify 逐步操作、要保存什么、要截什么图、执行侧的初步专业判断、硬门还是整体可用性判断）
在冻结时一次性补齐，不在草案阶段先写一半。

---

`END_MARKER: M3-STAGE2-V15-REPORT-AND-REBASE-v1.0-END`
