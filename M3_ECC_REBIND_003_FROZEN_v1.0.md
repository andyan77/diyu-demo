# `M3-ECC-REBIND-003` · 载体 v1.2 重绑定冻结件

> `task_id` = `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`（不变；不新建根任务）
> `contract` = `M3_ENGINEERING_TASK_CONTRACT_v1.2.yaml`（不变）
> `authority_event` = Founder 2026-08-26 `CONTINUE_TASK`：A/B/D/E 与链式投影缺陷均属**当前 M3 技术阻断**，
> 技术 HOW 归执行侧，缺陷 C 的取证方法亦归执行侧；不接受 `AC-18` 长期 `NOT_VERIFIED`；不降低原增益门。
> `frozen_at` = 本 commit —— **本文件与其全部绑定在本轮任何一次正式取证运行之前提交**，提交顺序即时序自证。

---

## 0. 一句话

v1.1 的闸门修好了原来的两个缺陷，代价是引入五个新缺陷，其中一个是**设计根基错误**：
整个闸门跑在"模型如实自报"之上，而这正是原缺陷已经否定的前提。
v1.2 把「**凡是能从输入里确定性算出来的事实，就不要问模型**」这条原则贯穿到底。

---

## 1. 改了什么（锁定变量的新值）

| 绑定 | v1.1 | v1.2 |
|---|---|---|
| `SKILL.md` | `7ae8e521…`（37,091 B） | `343758f3c2da5694…`（42405 B） |
| Dify 草稿图 hash | `e5c30c32…` | `e3c4e9df45563c0a…` |
| 闸门共用检查块 `shared_checks.py` | —（v1.1 三段各自实现） | `99d8e1813b3b38ac…` |
| 闸门节点 `gate_main.py` | `gate_code.py` `e8dcc164…` | `ad99abf35ed8f085…` |
| 装配节点 `assemble_main.py` | `assemble_code.py` `ad322f75…` | `2d1a5fd4e1ba91e5…` |
| 复检节点 `post_gate_main.py` | `post_gate_code.py` `6a1ec698…` | `02a842c84eb8589a…` |
| 投影规则 `projection_v12.py` | —（无条件全量覆盖，写死在 runner 里） | `60d05b69549c2916…` |
| Dify App / provider / model / temperature | 不变 | 不变（`b7fb5b1a-9278-426c-bb8a-f9f288639548` / `langgenius/deepseek/deepseek` / `deepseek-v4-flash` / `0.4`） |
| 图节点数 / 边数 | 7 / 6 | 7 / 6（**形状不变**，改的是三个代码节点的实现与两个新输出字段） |

### 1.1 五个缺陷各自的修法（逐条对应实测证据）

| 缺陷 | 实测证据 | v1.2 修法 |
|---|---|---|
| **A** 闸门给零交付盖合规章 | `ep06-runtime-fidelity-dify-v11/G6-attachment-unloaded.json`：正文 0 字，交付 23 字符，`gaps_closed=true` | 最低实质产出硬门（正文 ≥200 字、实质句段 ≥3、去掉谈参考文件的句段后 ≥120 字）。命中即 `HARD_FAIL_MIN_OUTPUT`，**不进补齐节点**，`gaps_closed` 强制 false，对用户显式说明本轮没有产出 |
| **B** 闸门建在模型自报之上 | 行为 3/49 自报行缺失即静默跳过；`ep07-longitudinal-v11-run2/E07.json` 自报「冲突反馈=否」而输入里有三条不同证据身份的反馈 | ①冲突反馈与暂定锚点**由代码从投影算出**，模型自报压不过它；②算不出的（探索提案、无内容任务）自报缺失或与正文措辞冲突时 **fail-closed 按触发处理**；③审计块整体缺失 ⇒ 硬失败，不是静默跳过；④**输入槽位事实回抄核对**：槽位有内容而正文说它没进来 ⇒ 阻断 |
| **C** 闸门标签是候选唯一指纹，A/B 盲评失效 | 三名互不通气的盲评者各自独立写出该结论；揭盲逐场景吻合 | ①标签整族移出用户可见正文（见 D）⇒ 指纹消失；②盲评协议改为**逐场景、单臂、独立随机分配**，判定者看不到任何其他臂或场景 ⇒ 跨场景轮换通道在构造上不存在；③映射封存、判定冻结后由脚本断言时序再揭盲。详见 `M3_ECC_MODULE_AB_001_FROZEN_v1.0_ADDENDUM_002.md` |
| **D** 闸门标签违反 Skill 自己的输出条款 | 盲评者 2 在完全不知情下按 §5.3「共同质量底线」判出「机器状态泄漏」 | 必填项强制手段整体移到正文之后的审计块，由代码剥离；正文侧加确定性**内部字段泄漏检测**（方括号标签、裸标签、控制标记、内部字段名、规则编号、章节号、文件路径） |
| **E** 裸标签形态整体逃逸检测 | `ep06b-runtime-behavior-v11-run2/B08-P-single-anomaly.json` 4 处装饰性填空，`decorative_labels` 为 `[]` | 必填项改判「锚定行必须逐字指回正文里**互不重叠**的实质文字」：缺行/指不回/太短/空洞短语/引用重叠/触发未命中却写了，六类分别命中。裸标签与残缺 `[]` 由 D 的泄漏检测整族拦掉 |
| **F** 链式投影因一次零产出永久丢内容 | 纵向判定者发现：E07 的零产出回填后把 P1/P2/P3 与四周节奏挤出投影，E08 起再未出现 | 见 §3 |

---

## 2. A3 影响面（多算和少算都是错）

### 2.1 失效（`STALE` → 未验证，必须整轮重跑）

`SKILL.md` 是全部四条 ECC 的**共同锁定变量**，改了一个字节即全部失效：

```text
ECC-M3-RUNTIME-FIDELITY-001     9 例      → STALE
ECC-M3-RUNTIME-BEHAVIOR-002     49 例     → STALE
ECC-M3-LONGITUDINAL-001         12 步     → STALE（且序列不可局部重跑）
ECC-M3-MODULE-AB-001/002        12 次     → STALE（B 臂系统提示词就是 SKILL.md）
```

连带失效：全部依赖上述运行的判定文件、`M3_CHECKPOINT_ROUND_5.md` 的 AC 状态矩阵、
`M3_FOUNDER_ACCEPTANCE_PACKAGE_v1.0.md`、`M3_INDEPENDENT_CLOSEOUT_REVIEW_v1.0.md` 的运行时结论、
`AC-20` 的远端哈希与回滚演练。

### 2.2 **不**失效（有证据且已证明不受影响，不得多算）

```text
AC-12 / AC-13 的 schema 与单元/契约测试   —— 绑定 interfaces/ 与 M2 接口版本，SKILL.md 不是其锁定变量
                                            （本轮仍按新提交重跑一次以取当前证据，不是因为它失效）
AC-05 的结构反搜                          —— 绑定 Skill 树结构与图结构，随本轮新图重跑
A 臂系统提示词与其胜任性复核               —— 逐字未动，`1607429186 53b3f2`
Bprime 臂系统提示词                        —— 逐字未动，`4a0f99592245a887`
三个留出场景 FX-M3-HOLD-01/02/03 的内容    —— 逐字未动
第 3、4 轮的全部原始运行记录               —— 只追加不删改，逐条保留
```

### 2.3 影响关系无法判断的项

**无。** 本轮变化面完全落在 `account-operations/skills/operating-one-account/SKILL.md`、
任务专用 Dify App 的三个代码节点与补齐节点提示词、以及执行侧的 runner/投影脚本之内；
依赖边逐条可枚举，不存在需要标 `STALE` 待定的未知依赖。

---

## 3. 链式投影缺陷（Founder 第 4 条）：为什么它可以、也应该在本任务内修完

### 3.1 根因

冻结件 `M3_ECC_LONGITUDINAL_001_FROZEN_v1.0.md` §3 把链接规则写成：

> 第 N 步的 `standing_cycle_baseline` = 第 N-1 步模型最终答复正文，**逐字回填**

**同一节**又写明它模拟的是 Prompt §10.1 的最后一环：

> 「经相应权限和**接受规则**后，由 M2 保存当前有效判断」

**接受规则这一步当时没有实现。** 一份自称"核心输入缺了、本轮不产出任何改判"的交付，
被无条件当成"用户接受了的当前有效判断"回填了进去。根因不在 M2，在这条投影规则少了它自己写过的那一半。

### 3.2 修法（全部落在 M3 允许变化面内）

| Founder 的要求 | v1.2 怎么做 |
|---|---|
| 零产出不得覆盖上一有效周期状态 | 新增工作流输出 `cycle_state_carry`。闸门未闭合 ⇒ `REJECTED_KEEP_PREVIOUS`，投影保持上一有效值不变 |
| 投影前后必须做关键对象/版本/数量一致性检查 | 投影前从 `standing_cycle_baseline` 抽关键对象（任务位号 `P\d+`、节奏 `N 条/周`；「」《》命名项作仅记录项）；投影后逐个查：继续 / 被点名处置 / 静默消失 |
| 内容丢失必须显式失败，不得静默继续 | 静默消失 ⇒ 交付侧判 `continuity=DROPPED`（可修复项，补齐节点被要求补上处置说明）；投影侧**逐字保留**上一基线中提到这些对象的原句，并在记录里显式标 `MERGED_KEEP_PRIOR`。三条分支全部显式，没有静默路径 |
| 增加 `E07→E08` 连续性回归 | `v12/test_projection_v12.py`，用第 4 轮的**真实文本**证明：v1.1 规则下 P1/P2 必然丢失，v1.2 规则下 `KEPT_PREVIOUS` 且三个任务位全部保住 |

### 3.3 为什么**不**登记 M2 后继任务

Founder 第 4 条末句：「只有证明根因必须修改 M2 内部且超出 M3 允许变化面时，才登记 M2 后继任务。」

该条件**不成立**，逐条：

1. 根因是投影规则少实现了它自己声明的接受语义，不是 M2 的持久化、版本或权限实现有缺陷；
2. 修法只动了两处：任务专用 Dify App 的输出字段（`M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md` §1.4 允许变化面明文包含）
   与执行侧 runner 的投影脚本（`account-operations/` 内）；
3. `AC-13`「M3 只写候选」**不受损反而更强**：`cycle_state_carry` 是候选自带的一句
   "这份候选不够格成为当前有效判断"，它**不设** `is_current`、不写 M2、不晋升任何东西，
   接受权仍完全在 M2/用户手里；
4. 受保护资产清单（§1.3）中的「M2 的原始观测／反馈／来源／持久化／版本／权限／恢复权威」**一个字节未动**。

因此本缺陷**留在当前任务内修完**，不移出、不新开根任务。

### 3.4 A2 时序：改冻结件的链接规则合不合法

**这是本轮唯一一处改动了已冻结判据的地方，必须单独交代，不藏在别处。**

- **事实**：链接规则的改动发生在看到 E07 结果**之后**；
- **性质**：按 A2，看到结果后改判据，**本轮该项只算探索，不产生正式 PASS**——除非有权威事件；
- **权威事件**：Founder 2026-08-26 `CONTINUE_TASK` 第 4 条明文要求把它作为 `AC-17` 阻断在本任务内修复，
  并逐条给出四项要求。这是 A1 意义上的有权者决定，构成 A4 的 `REBASE`：同一 `task_id` 下建立新合同哈希、继承旧证据；
- **方向**：改动是**收紧**——原规则无条件接受任何输出，新规则增加了接受条件；不存在"把不通过改成通过"的路径；
- **仍然如实记录的上限**：`AC-17` 在新链接规则下通过，**只能**建立"在带接受规则的投影下纵向序列可复现"，
  **不能**追溯建立"v1.1 那条无条件回填规则下也可复现"。两者是不同的绑定，不得混用。

---

## 4. 判据继承（逐字，不放宽）

- `M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md` 全文：**一个字未改**，21 条 AC 的命题、Oracle、
  成功/不足/无结果/失败四态、反证探针，全部原样继承；
- 四条 ECC 冻结件的 §「命题」「事件覆盖」「判定协议」「结果空间」「声明上限」：**一个字未改**；
- 唯二的判据侧改动，都已单独版本化并写明方向：
  1. 纵向链接规则 → 本文件 §3，**收紧**；
  2. A/B 盲评协议与增益门判定形式 → `ADDENDUM_002`，**收紧**（门槛本身一字未改）。

---

## 5. `AC-19` 的 Qwen 项

Founder 2026-08-26 明确指示「不需要 QWEN 或者另外模型测试」，本轮 `CONTINUE_TASK` 未撤销该指示。
`ECC-M3-QWEN-SEMANTIC-001` 继续为 `NOT_APPLICABLE`（运行前由权威依据判定，非失败后追溯填写）。
**这降低了契约 v1.2 的一项门槛，未回填任何 AC 为 `PASS`**；是否正式 REBASE 契约仍由 Founder 决定。

---

## 6. 运行前先声明的风险（不等出了事再说）

1. **绝对等第比并排对照噪声大**：A/B 改单臂判定后，小幅但真实的增益更容易被判成`相当`。
   这是收紧方向的代价，已写进 `ADDENDUM_002` §2.5 与 §8 新增混杂第 5 项。
2. **审计块本身可能被伪造**：模型可以写一段格式正确但引用无关句子的审计块。
   已用"逐字指回正文 + 互不重叠 + 最短长度 + 空洞短语黑名单"四重确定性检查压制，
   但**不能**声称它挡得住一切伪造——它挡得住的是漏写与敷衍，挡不住蓄意的高质量造假。
3. **连续性检查的关键对象抽取是保守的**：只把任务位号与节奏数量作为阻断集，
   「」《》命名项只记录不阻断。因此**漏检是可能的**，误伤被压到最低。
   第 4 轮 70 例实测重放：最低产出门只命中 G6，输入槽位矛盾只命中 E07，误伤为 0。
4. **本轮仍是单次采样**：每例每臂各一次。结论是"该绑定下的一次观察"，不是稳定行为分布。
5. **画布级验收仍未做**：本机无浏览器自动化，按 Prompt §12.3，`AC-16` 的画布那一半继续 `NOT_VERIFIED`，
   不用运行历史顶替。

---

## 7. 执行顺序自证（A2 先后）

```text
本 commit（判据与绑定冻结）
   ↓  确定性正负夹具 20 条全绿 + 70 例历史重放误伤普查（探索阶段，先于冻结，已如实标注）
   ↓  定向复验 G6 / E07 / B08-P（探索阶段）
   ↓  ——— 本文件冻结于此 ———
   ↓  完整正式重跑：保真 9 + 行为 49 + 纵向 12 + A/B 12
   ↓  六名 ECC 独立判定者 + 36 名单臂盲评者（互相隔离，隔离性机械核验）
   ↓  独立收口审查 R-1～R-7 + 回滚演练 + 远端完整哈希 + AC-20
   ↓  Founder 可亲自运行的 Dify 实测包
```

夹具与重放属**探索阶段**：它们用第 4 轮已有数据标定阈值，结果不构成任何 AC 的证据，
只用来证明"闸门本身不误伤、且能命中三个已知缺陷"。正式取证从本文件冻结之后开始。

```text
END_MARKER = M3-ECC-REBIND-003-FROZEN-v1.0-END
```
