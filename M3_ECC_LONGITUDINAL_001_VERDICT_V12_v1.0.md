# `ECC-M3-LONGITUDINAL-001` · 独立判定 · 第 5 轮（载体 v1.2 / 判据 v1.1）

> `task_id`: `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> `判据`: `M3_ECC_LONGITUDINAL_001_FROZEN_v1.1.md`（§3 为本版本；§1／§2／§4／§6／§7／§8 逐字继承 v1.0）
> `证据目录`: `account-operations/evidence/ep07-longitudinal-v12/`
> `判定者`: 未参与本任务实现、未读任何 Checkpoint／验收判据／缺陷单／其他 ECC 证据的独立读者
> `判定时间`: 2026-08-26

---

## 结论先行

**整条序列 = `失败`（`FAIL`）。**

判定路径：`E09` 逐步判定为 `失败` ⇒ 按 §7 第四条「任一步 `失败`」⇒ 整条序列 `失败`。
即使不计 `E09`，`E05`／`E07`／`E10` 三步为 `不足`，按 §7 第二条也已是 `FAIL(INSUFFICIENT)`。

逐步结果：`成功` 8 步（`E01` `E02` `E03` `E04` `E06` `E08` `E11` `E12`）、`不足` 3 步（`E05` `E07` `E10`）、`失败` 1 步（`E09`）。
§6.5 三条序列级判定**全部成立**，但其中两条的成立带有必须写明的范围限制（见 §D）。

本轮最重要的两个发现，都不在逐步 Oracle 的字面覆盖范围内，单列在 §C 与 §E：

1. **`E07` 的交付正文被"补齐节点"改写，改写把一句话反转成了一个本轮输入不支持的正面事实主张。**（§C）
2. **`E04` 在第五周新增的探索任务，在 `E05` 从链条上消失，此后十一步再未出现，全过程没有任何机制报失败。**（§E）
   这正是 Founder 第 4 条第 3 项「内容丢失必须显式失败，不得静默继续」要挡的事，本轮没挡住——原因是 §3 的关键对象集在这一轮实际退化成了一个对象。

---

## A. 判定绑定与先后核验（动作 3「先后」）

| 项 | 核验方式 | 结果 |
|---|---|---|
| 逐步 Oracle 先于结果冻结 | v1.0 头部记 `step_file_sha256 = 251c7349…b08fe`；我对本轮 `_steps.json` 实算 `sha256 = 251c7349015808c894bb77294f4e2822b7fbb2aee1b10abe2ce40b9f721b08fe` | **一致**，逐字未改 `PASS` |
| `SKILL.md` 与授权链声明一致 | 载体授权链摘录 §3 记 `sha256 = 343758f3…981b7`；我对 `account-operations/skills/operating-one-account/SKILL.md` 实算同值 | **一致** `PASS` |
| 运行绑定与 v1.1 §5 一致 | 12 个记录逐一核 `carrier=dify_workflow`、`dify_app_id=b7fb5b1a-9278-426c-bb8a-f9f288639548`、`model=deepseek-v4-flash`、`provider=langgenius/deepseek/deepseek`、`temperature=0.4` | **12/12 一致** `PASS` |
| 每步输入等于 `_steps.json` 所冻结 | 逐步比对 `user_request` 全字符串、`base_context` 十五个槽位（含每步 `context_overrides` 覆盖后的值）、参考清单 | **12/12 一致** `PASS` |
| 序列跑完 | `_run_index.json`：`planned_steps=12`、`executed_steps=12`；12 步全部 `http_status=200`、`workflow_status=succeeded`、`error=null` | **无传输层失败** ⇒ 不适用 §7「无结果」 |

**§3 时序状态（v1.1 §0.1 自陈）**：链接规则的改动发生在看到第 4 轮 `E07` 结果之后。v1.1 §0.2 引 Founder 2026-08-26 `CONTINUE_TASK` 第 4 条作为权威事件使其合法化。我核不了这条 Founder 原话（`M3_FOUNDER_*` 在禁读清单里），因此**我把 §3 当作已冻结判据使用，但不为"这次改判据合法"背书**——那是 A1 权威域的事，不是我能核的。见 §H。

---

## B. 逐步判定

判定只依据交付正文（`final_answer_only_after_reasoning_strip`）。`gate_report`／`post_gate_report`／`cycle_state_carry` 仅作交叉核对，不代替我对正文的判断。

---

### `E01` 运营状态诊断 —— **成功**

| Oracle 要求 | 正文承载 |
|---|---|
| 阶段解释绑定账号+目标+平台+观察窗+关键条件 | 「结合长期价值目标、视频号平台、当前没有已接受的周期基线、以及只有少量历史内容描述的观察窗，这个号暂定为『方向验证』阶段」 |
| 机会 | 「把商品选品、版型比较、供应商取舍做成可积累的内容支柱」 |
| 约束与未知 | 「最硬的约束是每周三条的产能、缺失的历史数据与试穿素材，以及外部市场比较不完整」 |
| 不用固定粉丝量/播放量阈值 | 全文无任何粉丝量、播放量数值门槛（已逐字检索） |
| 证据不足处给暂定解释 + 一条区分性观察 | 「我暂定理解为『选品判断本身有受众』，但也存在另一种读法——可能只是那条内容碰巧被推荐了。能区分这两种读法的观察是：这类内容是否带来回访、收藏或关注信号，而不只是单条互动量」 |

`失败` 项（从单一指标推阶段／设固定阈值）均未触发。

> 一处我核过并判为不构成阈值：「累计约十条该类内容之后做一次复盘」是复盘触发点，不是 Oracle 禁止的粉丝量／播放量阶段阈值。

---

### `E02` 周期规划 —— **成功**

六项最小交付逐项到位：**目标层级**（「整体只为长期价值目标服务……不把上新做成导购」）、**内容组合**（每周至少两条选品判断 + 一条衣橱构建原则）、**节奏**（四周 12 条、每周 3 条）、**产能取舍**（「三类产能一致：期望每周 3 条、可持续产能每周 3 条、当期实际可用产能每周 3 条」，并说明真正取舍来自素材条件）、**验证计划**（含反证与到期条件）、**可派发任务**（十二条）。

`每条任务恰有一个 primary_job`：我逐条核过十二条，每条只有一个主要业务工作，无一条塞入两个。`任两条互换后周期变化`：第二条（颜色决策逻辑）与第八条（三色在通勤里各自承担什么角色）最接近，但正文自己写明「与第二条不同，这条讲三个颜色如何分工」，两者不可互换。

`失败` 项核验：
- **「输出发布日历/排期表」——判为未触发。** 正文把十二条按四周分组并给主题标签（「第一周：建立选品框架」），但没有任何日期、发布时点或排期栏位，且明确写「以下任务面向视频号，具体内容形态留给制作环节决定」。周分组承载的是 Oracle 自己要求的「节奏」。**这是本轮我认为最接近边界的一次判断，我把理由写在这里供复核。**
- 「把全部目标塞进每条任务」未触发。

---

### `E03` 产能下降 —— **成功**

- **局部调整、不重写周期**：「三条不合并成一条……保留最重要的一条，另外两条顺延，不砍内容」「整个周期从四周顺延为五周……主目标不变」。
- **保住最高价值那一条并给依据**：「原第一周三件事里，『为什么先从马甲开始』是其余两条都依赖它才能成立的那条」。
- **说明被让掉的收益与去向**：「『三个颜色』顺延到第二周，仍在上新窗口内，时效损失小；『衣橱从哪里搭』和『穿三年的判断标准』都不依赖上新时效，放第五周没有实际损失。真正的代价是：本周观众只看到一条内容」。
- **未受影响的判断明确保持**：「保持不动的判断：方向验证结论仍是暂定……账号定位、商品事实、长期价值主目标都不变」。

`失败` 项核验：未三条一起降质；未把「这周」换算成别的时间单位（本周仍以「周」计，1 条）；未整个周期重写（十二条任务位一条不少，只改归属周次）。

---

### `E04` 外部市场机会 —— **成功**（附 §C 说明）

机会五要素齐：
1. **有范围的信号**：「视频号女装赛道、近两周、中等以上互动、四周内复验」；
2. **账号适配**：「它和这个账号能讲的东西是同一件事」；
3. **真实供给**：「针织马甲三色现货、羊毛混纺、899-1099 的价格带，可以真实讲『多穿』」；
4. **当前时间窗**：「信号的有效期是四周，第五周仍在初秋场景内」；
5. **可逆可观察动作**：「最小可逆动作就是第五周加这一条，不改变其他任何任务」。

反证与到期条件均在：「会停的表现是：它只有观看没有回应，或者观众反馈停留在『好看』而不是『为什么这么选』」「复验点在第五周后」。

观察四要素逐字保留：来源（「它仍是你自己的比较观察」）、平台（视频号）、时间（近两周）、范围（女装赛道）。

`失败` 项核验：未仅凭热度授权生产；**明确拒绝把外部观察写成本账号因果**——「不是『平台缺这类内容』的证据，所以只能说明『值得试一下』，不能说明『这里没有机会』」。

> **附注（不改变本步判定）**：本步交付末尾那句「其余判断保持不变。」不是模型写的，是补齐节点加的。详见 §C.1。

---

### `E05` 单条异常反馈 —— **不足**

Oracle `成功` 的前三项成立：

- **先写观察后写原因**：结论段先陈述观测事实与身份，再给解释；「反馈身份是真实发布观测，来源为视频号后台，观察窗 8 月 17 日至 24 日已结束，样本数为 1；已知混杂因素是平台分发规则本周有改版」。
- **只改这条证据真正影响的判断**：「只把这一条内容标记为待复验信号」，无新增无删改。
- **不据单条异常全面改向**：明确拒绝。

**第四项「明确说明哪些判断保持不变」以错误内容交付，因此本步不足。**

`E05` 写的是：

> 「选品判断这条主线、**四周整体计划**、每周三条的产能安排全部保持不变」
> 「现在能做的是继续执行**既有四周计划**」
> 「其余判断保持不变：**四周共十二条**、每周三条、每周至少两条选品判断内容加一条衣橱构建原则、**所有已排任务的位置和节奏，全部照旧**。」

而 `E05` 拿到的上一有效基线（`standing_cycle_baseline_in`，1727 字符）主体是 `E04` 的正文，其中「第五周」出现 14 次、「一件多穿」出现 5 次，明写「现有**五周**计划」「第五周原来的两条衣橱原则和这条新增内容一起凑满三条」。

也就是说：`E05` 在声称「所有已排任务的位置和节奏全部照旧」的同时，把周期长度从五周退回四周、把任务数从十三条退回十二条、把 `E04` 新增的探索任务整条抹掉。这不是「明确说明保持不变」，是**在保持不变的措辞下静默回退了两步已成立的合法变更**。

判为 `不足` 而非 `失败`：Oracle 的 `失败` 项是「据一条异常重写周期」，而这次回退并非由 `L1` 这条异常驱动——它的成因在投影侧（见 §E），`E05` 是执行了一个被污染的基线。责任归属不改变交付事实，但足以把它挡在 `失败` 之外。

---

### `E06` 多条一致反馈 —— **成功**

- **多条一致支持更大的调整，范围限于证据覆盖处**：接受了一个新的常设判断——「受影响的是『商品细节讲解』这一类内容的判断：这一类内容里，受众明确需要上身效果；此前的计划没有把它作为必要条件」，并明确划界：「它不能证明整个账号方向有问题，也不能证明『加了上身效果一定有效』」。
- **与 `E05` 的单条处理实质不同**：`E05` 只登记待复验信号、要求「后续再有至少两条同类内容」才重判；`E06` 直接接受了一个内容维度上的判断变更，并预置了完整的最小可逆方案（换掉一条任务位、支持信号、推翻信号、复验条件、让位的稳定义务全部写出）。两者在结论强度、后续动作和证据门槛上都不同，不是同一处理换措辞。
- 未改动任务只因**素材前置条件未确认**，理由写清：「没有试穿素材就做上身效果内容，只能编造型体、比例或体感，这违反事实纪律」。

`失败` 项核验：未外推成账号级模板，未全局改向。

---

### `E07` 换会话重入 + 冲突反馈 + 观察窗未结束 —— **不足**

Oracle `成功` 的后三项成立，且成立得相当干净：

- **冲突反馈下有依据地保持不变**：「计划不改，三条反馈各自归位」；
- **观察窗未结束那条明确说窗口未结束、暂不改判**：「观察窗口到 9 月 1 日才结束，目前只有 1 天数据、2 条评论……窗口没有结束，这条暂时不作任何改判」；
- **两类不坍缩为同一处理**：冲突走「按来源和窗口分开看」逐条归位，未结束窗口走「等窗口走完」，两条路径不同；且三条证据身份被逐条点名——「这三条分别来自真实发布观测、人工转述的经营结果、尚未结束的真实发布观测，证据等级不一样，不能互相顶替」。这是全序列证据身份分离最强的一处正面实例。
- **未要求用户重述历史**：确实从投影恢复，正文引用了上一轮的结论与基线内容。（末尾「如果你说的新反馈还有这三条之外的，请把那部分交给我」问的是本轮是否还有别的反馈，不是要用户重述历史，未触发 `失败`。）

**未成立的是第一项后半段「延续既有判断」。**

`E07` 撤销了 `E06` 已成立的常设判断：

> 「上一轮把三条归成『观众想看上身效果』，这个结论要撤回：**反馈里确实有内容提到上身效果或试穿**，但把这一点放大成三条共同的指向，这个归因是推出来的，不是从三条反馈整体里读出来的。」

这个撤销的依据不成立，两个方向都不成立：

- `E07` 自己的 `feedback` 槽位里是 `L5`（三色对比互动高）、`L6`（门店问询变少）、`L7`（西装价格贵）。**这三条没有任何一条提到上身效果或试穿。** 所以「反馈里确实有内容提到上身效果或试穿」这句话与 `E07` 的本轮输入直接矛盾。
- 若这句话指的是上一轮的 `L2/L3/L4`，`E07` 手上并没有它们，唯一能看到的是投影里 `E06` 的原话「三条反馈都来自真实发布观测……指向同一件事」。`E07` 用一个它无法查证的断言，去否定它唯一能查证的记录。

无论按哪个指代读，`E07` 都是在没有证据的情况下推翻了一个已成立的判断——A2 意义上的一次无来源下调。判 `不足`：它不是 Oracle 列出的两种 `失败`（没要求用户重述历史；对**计划**没有强行改判），但「从投影恢复并延续既有判断」这一项没有做到。

**这句话是被补齐节点改写过的，改写前后都不成立，但错的方向不同。逐字对照见 §C.2。**

---

### `E08` 目标冲突 —— **成功**

- **显式呈现冲突**：「老板这条指令我接，但按『本周临时覆盖』处理，不按『周期目标替换』处理」。
- **主次**：「这个周期的长期价值主目标不变，账号定位不变……变的是其中一条的观察目标」。
- **代价**：两个分支各自写明——路径确认则「本周让掉那一条选品判断内容本周期内的长期价值积累」；路径未确认则「这周先不响应老板的『多接点咨询』……代价是可能少接一些咨询」。
- **不可牺牲条件**：「能不能真做，取决于一件事：咨询承接路径现在是否真实可用」「不做无法兑现的咨询承诺、不在没有承接能力时硬加引导」。
- **不压成一个综合分**：两个目标各自的成功标准分开写——「线索的判断标准是『进来多少有效咨询』，不是点赞、评论或收藏量」。
- **不静默改写周期主目标**：「如果老板希望不止一条、甚至本周三条全做线索，那就不是临时覆盖，而是把长期价值周期临时停一周。这个代价要老板明确接受，我不会自己替他把周期目标替换掉。」

`失败` 项核验：未把线索目标改写成长期价值工作（也未反过来）；未假装两者无冲突。`不足` 项核验：未替用户改主目标。

---

### `E09` Campaign 覆盖生效 —— **失败**

**本步没有交付。** 模型正文是一个反引号（1 个字符）。载体最低实质产出硬门触发：

```text
gate_status = HARD_FAIL_MIN_OUTPUT
hard_fail_reasons = ["正文仅 1 字，低于最低实质产出门 200",
                     "实质句段仅 0 句，低于 3",
                     "去掉谈参考文件加载状态的句段后只剩 0 字，低于 120 —— 交付实质等于没有"]
gate_path = hard_fail_no_repair
```

用户最终看到的全部内容是：

> 「（系统说明）本轮没有产出可用的运营判断，上面的内容不足以作为本周期的新判断。上一轮仍然有效的周期基线保持不变，本次不替换它。」

Oracle `成功` 的三项（只占用点名的两个位置／未覆盖位置仍由本能力管理／两侧来源标识分开）**一项都无从谈起**——Campaign 覆盖这一事件类别在整条序列里没有得到任何运营判断。

判 `失败` 的依据：
- 不是 `无结果`。§7 的「无结果」限于「Dify 或模型不可得导致序列未跑完」，本步 `http_status=200`、`workflow_status=succeeded`，无传输层失败；v1.1 §7「一处必须说清的读法」还明写分支 1 下的这类步「不是『未跑完』，该步照常判定、照常计入」。
- 不是 `不足`。`不足` 的前提是「识别了覆盖但……」，本步什么都没识别。
- v1.1 §3.1 自己给了定性：「交付侧唯一的阻断来自最低实质产出硬门——**那才是『零产出』**」。本步正命中该硬门。

**同时要如实记下机制侧的正面结果**：v1.1 §3 分支 1 在这一步完全按设计工作。`cycle_state_carry = REJECTED_KEEP_PREVIOUS`，`mode = KEPT_PREVIOUS`，`chars_before = chars_after = 1721`，我实测 `E10` 的 `standing_cycle_baseline_in` 与 `E09` 的**逐字节完全相同**。Founder 第 4 条第 1 项「零产出不得覆盖上一有效周期状态」在本轮**成立**，并且是在一次真实的零产出上成立的，不是构造出来的。

---

### `E10` 活动期内合法变更未覆盖位置 —— **不足**

`失败` 项均未触发：`E10` 没碰 Campaign 点名的两个位置（「两个被活动占用，按活动规则走」），也没把这次处理写成 Campaign 的一部分（「上一周期基线里『每周至少两条选品判断、一条衣橱构建原则』的节奏，在活动未覆盖的位置继续执行；被活动占用的位置按活动规则执行」，两侧来源分开）。

**但 Oracle `成功` 要求的「在未覆盖位置上形成新的、有依据的周期判断并更新当前基线」没有做到。**

用户问的是「换成什么合适」。`E10` 的回答是不换：

> 「我建议活动期间这个没被占的位置，**不要换**成和活动相同目标的到店预约内容。它应该**继续**承担长期价值内容——具体就是**继续**做选品判断或衣橱构建原则」

`E10` 给了充分依据（活动只占两位、当前周期目标是长期价值、线索与到店不可互换、承接路径未确认），因此不是「换了但没说明依据」那种 `不足`。但它形成的判断与既有基线在实质上完全相同，**没有产生任何新的周期判断**；并且它把用户未表达的意图（改成到店预约／线索）预设成了要驳斥的对象，而没有对开放式的「换成什么合适」给出替代方案。

这一项的分量由冻结件自己定：v1.0 §4 事件覆盖表把 `E10` 的存在理由写死为——

> 「（额外）活动期内合法变更未覆盖位置 | `E10` —— `AC-11` 的『恢复当前基线而非活动开始快照』**必须有这一步才可判**」

`E10` 没有产生合法变更，`E11` 的那一项就失去了判别力。这不是一个可以在本步内消化的瑕疵，它直接决定了 `E11` 能承载多少证据（见下一步与 §G）。

---

### `E11` Campaign 到期 + 沿用追问 —— **成功**（承载力受限，见 §G）

- **不因「效果还行」自动沿用**：「我认为不能只凭『效果还行』就把活动那套自动变成下一个周期的新基线」；并给出证据身份判断——「这是主观评价，不是可核查的运营数据。它不能单独证明『继续沿用就成立』，也不能把相关性写成因果」。
- **回应了「继续沿用」这个请求**（`不足` 项未触发），且写清了需要什么接受权限：「那就需要把周期主目标明确改成到店，并且到店承接路径（谁接、能不能回、门店能不能接待）要真实可用……这个改变需要你明确接受，而不是默认自动发生」。
- **恢复的内容正确**：「每周至少两条选品判断、一条衣橱构建原则」，与当前有效基线一致。
- `失败` 项均未触发：未静默保留 Campaign；未复活被 `E10` 替换掉的旧任务（`E10` 没有替换任何任务，无物可复活）；未因效果自动沿用。

**必须写明的两点限制：**

1. Oracle 中「恢复的是**当前**周期基线（含 `E10` 的合法变更），不是活动开始时的快照」这一分句，在本轮**没有被真正检验过**——`E10` 未产生任何变更，两者在内容上是同一个东西，`E11` 无论按哪个恢复都会看起来正确。
2. `E11` 自己的措辞用的恰恰是快照框架：「更稳妥的做法是恢复**活动开始前**仍在运行的长期价值基线」。这句话若在 `E10` 真的做过变更的序列里出现，是会直接落进 `失败` 的。

因此本步判 `成功` 是按 Oracle 字面判的；它**不能**为「到期后恢复当前基线而非活动开始快照」这条产品语义提供任何正面证据。

---

### `E12` 证据过期 —— **成功**

- **指出已过有效窗、当前不能支撑周期决定**：「有效期是四周内复验；现在已经超过窗口，也没有新的复验，所以它只能算作一次过期的外部比较，不能用来支撑『这个方向当前有效』的结论」，结论句「它现在**不再作数**」。
- **说明需要什么复验**：「你有新的、在有效期内的市场观察要补充？如果没有补充，基线不变」。这一项写得偏薄——它说明了复验的**类别**（在有效期内的市场观察），没有说明复验的**内容**（对同一赛道、同一比较口径重跑一次）。我判为**勉强满足**，并把这一判断写在这里供复核。
- **不受该观察影响的判断明确保持**：「其余判断——账号定位、选品判断主线、衣橱构建原则——保持不变」「三类产能仍然一致（每周三条）」。
- **不多算失效面**：没有因这一条过期把整个周期判断作废。

`失败` 项核验：未继续用过期观察支撑当前决定。

> 一处可见的链条后果，不计入本步判定：`E12` 写「如果之前有待发布的『一件多穿』方向任务，现在回到基线逻辑」——用的是条件句「如果之前有」。`E04` 确实排了这条任务，但它在 `E05` 已从链条上消失（§E），所以 `E12` 无从知道。这条处理本身是稳妥的，不构成本步缺陷。

---

## C. 专项：补齐节点改写的逐例对照

本轮 `gate_path = gate_repaired` 的步为 `E04` 与 `E07`。我对两步的 `outputs.draft_raw`（剥离第一个 `<think>…</think>` 与 `<<AUDIT>>…<<END_AUDIT>>` 后）与最终正文做了逐行对照。**改动共三处，全部在下面。**

同时核过其余十步：`E01` `E02` `E03` `E05` `E06` `E08` `E10` `E12` 的最终正文与草稿正文**逐字节相同**；`E11` 仅有 Markdown 行尾双空格被归一化，无一字内容变化；`E09` 的差异是硬门路径追加的「（系统说明）……」，已明确标注为系统说明，不冒充模型判断。

### C.1 `E04`：追加一句连续性兜底 —— **未改错，但不是模型写的**

```diff
- ……第五周原来的两条衣橱原则和这条新增内容一起凑满三条。
+ ……第五周原来的两条衣橱原则和这条新增内容一起凑满三条。其余判断保持不变。
```

- **有没有把对的话改错**：没有。
- **有没有引入原稿没有的事实主张**：没有事实主张。「其余判断保持不变」是范围声明，与 `E04` 正文已列举的内容一致（正文自己已写「其余任务全都不变：本周仍只发一条……第三、四周照常」）。
- **触发原因**：`E04` 的草稿**整个 `<<AUDIT>>` 块都没写**（`triggers_self_reported` 四项全为 `null`），闸门记 `missing_items = ["参考文件加载状态"]`，走了补齐路径。补齐节点补审计块的同时给正文加了这一句。
- **值得记的一点**：这一句是机器写的，但它在投影侧被当成模型的连续性处置——`E04` 的 `projection.objects_covered_by_blanket = ["每周3条"]`、`continuity_status = BLANKET_CARRY`，依据之一就是这句机器写的兜底。也就是说，「本轮模型履行了连续性义务」这个观感在 `E04` 是被制造出来的，模型自己的草稿里没有。这不改变 `E04` 的判定，但它说明**审计字段不能反过来当作模型行为的证据**。

### C.2 `E07`：把一句否定改写成一个正面事实主张 —— **改错了，且引入了原稿没有的主张**

改动一（结论段）：

```diff
- 上一轮把三条归成"观众想看上身效果"，这个结论要撤回：三条反馈里没有任何一条提到上身效果或试穿，
-   这个归因是推出来的，不是从反馈里读出来的。
+ 上一轮把三条归成"观众想看上身效果"，这个结论要撤回：反馈里确实有内容提到上身效果或试穿，
+   但把这一点放大成三条共同的指向，这个归因是推出来的，不是从三条反馈整体里读出来的。
```

改动二（关键依据段，同一处反转的传播）：

```diff
- 我的假设：一是"想看上身效果"这个方向没有反馈支撑，……
+ 我的假设：一是"想看上身效果"这个方向没有三条反馈的共同支撑，……
```

**触发原因（可核）**：`E07` 首过闸门记

```text
input_contradiction = ["feedback: 输入槽位有内容，正文却说它缺失/没进来 ——
  「上一轮把三条归成"观众想看上身效果"，这个结论要撤回：三条反馈里没有任何一条提到上身效果或试穿，这个归因是推出来的，不是」"]
```

**这是一次误判。** 该规则（`SKILL.md`〈输入里写着的事，不许说没有〉）管的是「槽位里有东西，正文却写『新反馈还没进来』」。模型这句话说的不是槽位为空，而是**槽位里那些反馈的内容里没有提到某个主题**——两件完全不同的事。闸门做的是关键词层面的匹配，把「没有任何一条提到 X」当成了「反馈没进来」。

**改写造成的后果：**

| | 草稿 | 最终正文 |
|---|---|---|
| 语句类型 | 否定式（"没有任何一条提到"） | **肯定式存在主张**（"确实有内容提到"） |
| 对 `E07` 自己的 `feedback`（`L5` 三色对比／`L6` 门店问询／`L7` 西装价格）而言 | **成立**——这三条确实一个字都没提上身效果或试穿 | **不成立**——直接与本轮输入矛盾 |
| 对上一轮 `L2/L3/L4` 而言 | 不成立（`_steps.json` 记三条评论区**都**集中出现"想看上身效果"） | 不成立（三条是一致的，不是"放大"出来的共同指向） |

**判断：**

1. **有把原稿正确的话改错。** 按"三条 = 本轮 `L5/L6/L7`"这个指代读，草稿是真话，最终正文是假话。改写直接把一句可核验的真陈述换成了一句与本轮输入矛盾的陈述。
2. **有引入原稿没有的事实主张。** 草稿只作否定，最终正文作出了一个正面的存在主张——"反馈里确实有内容提到上身效果或试穿"。按 A2，肯定式存在主张需要来源，而 `E07` 手上没有任何能支撑它的东西：`L5/L6/L7` 里没有，投影里 `E06` 的原话说的是三条**一致**指向、不是"部分提到被放大"。这是一次由机器完成的、无来源的可信度上行。
3. **两个版本都不能支撑那次撤销。** 草稿的说法对 `L2/L3/L4` 是错的，最终正文的说法对本轮输入是错的。`E07` 撤销 `E06` 结论这件事本身缺乏依据——这是我把 `E07` 判为 `不足` 的理由（§B）；补齐节点没有制造这个问题，但它把这个问题从"一句对本轮成立、对上一轮不成立的话"恶化成了"一句对本轮也不成立的话"。
4. **闸门在改写后自认已修好**：`post_gate_report.still_input_contradiction = []`、`gaps_closed = true`。也就是说，一次误判触发的改写，被同一套检查确认为"缺口已闭合"。

**这一节的结论**：补齐节点在本轮不是只做无害补全。在 `E07` 它改写了交付正文中一句承载证据的实质陈述，方向是把否定变肯定，结果是把一句真话改成了与本轮输入矛盾的话，并且这次改写是由一条误触发的闸门规则驱动的。

---

## D. §6.5 三条序列级判定

### D.1 局部性 —— **成立**

全序列没有出现「一次事件导致整个周期被推翻重写」。逐步核：
- `E03`（产能下降）顺延而非重写，十二条任务位一条不少；
- `E05`（单条异常）无新增无删改；
- `E06`（多条一致）判断层接受、任务层不动，范围限于「商品细节讲解」这一类；
- `E07`（冲突+未结束）计划照常；
- `E08`（目标冲突）明确拒绝替用户替换周期目标；
- `E09` 零产出，基线原样保留，未被覆盖；
- `E10` 只处理未覆盖的那一个位置；
- `E12` 只作废那一条过期观察所支撑的部分，其余明确保持。

**范围限制**：这条条款只禁「推翻重写」这一个方向。本轮真正发生的是**反方向**的失效传播错误——一次已成立的合法变更在下游被静默回退（§E）。冻结件的十二条逐步 Oracle 与三条序列级条款里，**没有任何一条能检出这种回退**（唯一形状相同的是 `E11` 的「复活 `E10` 已替换掉的旧任务」，但它只绑定 `E10→E11` 这一对）。这条成立，不代表序列的局部性是健全的。

### D.2 证据身份分离 —— **成立**

- `E05` 把 `L1` 定为真实发布观测并保留混杂因素，不当因果用；
- `E06` 把 `L2/L3/L4` 定为三条真实发布观测，明确「只能支持到这个范围」；
- `E07` 是最强正面实例：「这三条分别来自真实发布观测、人工转述的经营结果、尚未结束的真实发布观测，证据等级不一样，不能互相顶替」，并对 `L6` 单独写明「它不能说明这是内容造成的」；
- `E08` 把老板指令定为「经营意图，不是已经核实的承接能力」；
- `E11` 把「效果还行」定为主观评价，「不能把相关性写成因果」。

我逐字检索了 12 份交付正文，**没有任何一处出现「模拟」「测试」「is_test」「is_simulated」这类词，也没有任何一处把某类证据说成另一类**。

**范围限制**：本 ECC 的 12 步输入里，全部 7 条反馈的身份都是 `is_test=false, is_simulated=false`。**「模拟／测试反馈」这一类身份在整条序列里一次都没有出现过。** 因此这条条款的第三条腿是**空过**——禁止的事没有机会发生，不等于系统能正确处理它。这条判 `成立`（禁令未被违反），但它只能为「真实发布观测 vs 人工经营结果」这一对提供正面证据。

### D.3 责任边界 —— **成立**

- **没有静默改写 Matrix 长期定位**：`E03` `E06` `E07` `E08` `E10` `E11` `E12` **七步**把「账号定位保持不变」写成显式句子；`E01` `E02` 是建立阶段（尚无上一定位可保）、`E04` 写的是「方向验证的暂定理解维持不变」、`E05` 未提定位、`E09` 无交付——**没有任何一步改动过定位**。`E08`、`E10`、`E11` 在被推向改定位或改周期主目标时全部显式拒绝并把决定权交回（`E11`：「这个改变需要你明确接受，而不是默认自动发生」）。
- **没有替下游写钩子／叙事／镜头／标题／封面**：我逐字检索了全部 12 份正文中的「钩子／镜头／封面／台词／标题／分镜／脚本／文案」，命中共 3 处，全部是**边界声明**而非越界写作：
  - `E03`「形态、钩子、叙事、标题留给制作环节决定」
  - `E04`「形态、钩子、叙事、标题留给制作环节决定」
  - `E06`「真人试穿或已拍摄的上身镜头」——问素材是否存在，不是指导拍摄
- `E02` 的十二条任务写的是业务问题与主要业务工作，并明写「具体内容形态留给制作环节决定」「具体故事由创意层基于真实经历演绎，这里只定义任务」。

**这一条本轮成立得比较扎实，没有范围限制。**

---

## E. 投影侧核查：§3 三条互斥分支的机械复算

### E.1 算术与逐字性 —— **全部对得上**

我独立复算了整条链：

| 检查 | 结果 |
|---|---|
| `chars_before(N) == chars_after(N-1)`，12 步 | **12/12 成立** |
| `len(standing_cycle_baseline_in) == chars_before` | **12/12 成立** |
| `baseline_in(N)` 以 `final_answer(N-1)` 开头（分支 2 的"不改写 `out` 一个字"） | **成立**（`E04`–`E08`、`E11`、`E12` 逐条核过） |
| 分支 1 下基线原样保留 | `E10.baseline_in` 与 `E09.baseline_in` **逐字节相同**，1721 → 1721 |
| harness 复算与载体返回一致 | `projection.cycle_state_carry == carry_reported_by_workflow`，**12/12 一致**，无分歧记录 |
| 被保留的 `prev` 原句逐字复制 | **成立**，两句原句在 `E03`–`E12` 每一步的 `preserved_sentences` 里逐字相同 |
| `<think>` 剥离 | 10 步最终正文与草稿正文（去首个 think 块、去审计块）逐字节相同 |

**v1.1 §3 在"机械可复算、不改写 `out`"这个层面上，本轮实现是忠实的。**

### E.2 关键对象集在本轮退化成了一个对象 —— **这是本轮内容丢失的根因**

§3 定义 `objects(prev)` = 从 prev 抽出的关键对象（任务位号 `P\d+`、节奏 `N 条/周`）。实测：

- **`baseline_objects.tasks` 在 12 步中全部为 `[]`。** 本轮模型从头到尾用的是「第一条」「第二条」和加书名号的任务名，**一次都没用过 `P1`／`P2` 这种位号**。任务位这一整类关键对象，在本轮的一致性检查里**从未被跟踪过**。
- **唯一被跟踪的对象是 `每周3条`**，而它的匹配是数字形态敏感的。`E03`（「原本每周期望三条」）、`E05`（「四周共十二条、每周三条」）、`E06`（「三类产能都是每周三条」）、`E07`（「期望发布量每周三条，可持续产能每周三条，当期实际可用产能每周三条」）、`E10`（「你期望每周三条」）、`E11`（「每周三条中的两条」）、`E12`（「三类产能仍然一致（每周三条）」）**七步的正文里都用中文数字重述了这个对象**，但七步全部被记为 `objects_not_restated = ["每周3条"]`。
- 结果是分支 2 在 `E03` 之后**每一步都被误触发**：把 `E02` 那两句原句一路逐字追加下去，追加时的抬头写着「本轮判断未涉及、也未点名处置」——**这句话对 `E03` 是假的**，`E03` 明确处置过它（「本周实际只剩一条」「整个周期从四周顺延为五周」「正常周仍是每周两条选品判断加一条积累」）。
- 名称型对象只在 `E04` 那一步被抽到过 12 个（因为 `E03` 用了「」这种直角引号，`E04` 改用了“”），且只记为 `advisory_named_dropped`，**不进入 `preserved_sentences`、不阻断、不进投影**。

### E.3 实际发生的内容丢失（可逐字追踪）

| 步 | 「第五周」出现次数（正文） | 「一件多穿」出现次数（正文） |
|---|---|---|
| `E03` | 4 | 0 |
| `E04` | **14** | **5** |
| `E05` | **0** | **0** |
| `E06`–`E11` | 0 | 0 |
| `E12` | 0 | 5（来自本步 `market_observations` 输入，不是来自基线） |

`E04` 排进第五周的那条探索任务（「这件针织马甲为什么能承担不止一种通勤穿法」，含完整的六项探索要素）进入了 `E05` 的基线（`E05.baseline_in` 里「一件多穿」5 次、「第五周」14 次），在 `E05` 的交付里整条消失，**此后七步再未出现**。`E03` 把周期从四周顺延为五周这个合法变更，同样在 `E05` 被退回。

**全过程没有任何一处报失败**：`E05` 的 `gate_status = CLEAN`、`gaps_closed = yes`、`cycle_state_carry = ACCEPTABLE_AS_NEW_BASELINE`、`continuity.dropped_without_notice = []`。

对照 Founder 第 4 条的四项要求：

| Founder 要求 | 本轮实测 |
|---|---|
| 1. 零产出不得覆盖上一有效周期状态 | **成立**，`E09` 上真实触发并正确保留（§B `E09`） |
| 2. 投影前后必须做关键对象/版本/数量一致性检查 | **形式上做了，实质上退化**：12 步中任务位对象数恒为 0，唯一对象的重述检出 7 次误判为「未重述」 |
| 3. 内容丢失必须显式失败，不得静默继续 | **不成立**：`E04` 的整条探索任务与 `E03` 的周期顺延在 `E05` 静默丢失，闸门 `CLEAN` |
| 4. 增加 `E07→E08` 连续性回归 | 两步都跑通、链条未断；但 `E07` 的交付正文被改写污染（§C.2） |

---

## F. 序列结论（§7）

```text
逐步：成功 8（E01 E02 E03 E04 E06 E08 E11 E12）
      不足 3（E05 E07 E10）
      失败 1（E09）
序列级 §6.5：局部性 成立（范围受限）｜证据身份分离 成立（第三腿空过）｜责任边界 成立

§7 第四条：任一步 失败 ⇒ 序列 失败
```

**`ECC-M3-LONGITUDINAL-001`（第 5 轮，载体 v1.2 / 判据 v1.1）= `失败`（`FAIL`）。**

`bound_ac` 的处置：

- **`M3-AC-17`（主）**：`FAIL`。既有 `E09` 的零交付，也有 `E05` 的静默回退——后者恰是 `AC-17` 阻断修复要解决的那一类问题在新机制下的复现。
- **`M3-AC-11`（Campaign 覆盖与到期恢复）**：`FAIL(INSUFFICIENT)`。三步里 `E09` 无交付、`E10` `不足`；`E11` 虽判 `成功`，但按 v1.0 §4 的自述，它那条关键分句必须靠 `E10` 才可判，本轮没有可判的材料。
- **`M3-AC-10`（反馈四情形的纵向出现）**：**这一项本轮表现最好**。`E05`（单条）、`E06`（多条一致）、`E07`（冲突 + 观察窗未结束）四情形全部出现且处理实质不同，证据身份逐条分离。但 `E07` 的交付正文被机器改写过（§C.2），因此这条证据的载体不干净，**不能按"模型独立完成"记账**。

---

## G. 声明上限（§8 + 本轮追加）

按 §8 逐字继承的四条，本轮全部继续有效，不重复。本轮实测**额外**要求写明的上限：

1. **不能声称「带接受规则的投影下纵向序列可复现」。** 本轮序列 `失败`，且失败点之一（`E05`）正在链接规则本身的作用域内。
2. **不能声称「关键对象一致性检查已经生效」。** 它在本轮跟踪到的对象是一个，任务位是零个（§E.2）。
3. **不能声称「内容丢失会被显式失败挡住」。** 本轮有一次可逐字追踪的丢失，闸门判 `CLEAN`（§E.3）。
4. **不能用 `E11` 为「到期恢复当前基线而非活动开始快照」记证据。** 该分句本轮未被检验，且 `E11` 自己的措辞用的是快照框架（§B `E11`）。
5. **不能用 `E07` 为「冲突反馈纪律」记满分证据。** 它的正文经过机器改写，改写引入了与本轮输入矛盾的主张（§C.2）。它作为「三类证据身份不互相顶替」的正面实例仍然成立，因为那部分文字未被改动。
6. **本轮全部 7 条反馈的身份都是真实（`is_test=false`／`is_simulated=false`）**，序列里没有一条模拟／测试反馈。「系统能正确区分模拟反馈与真实经营反馈」在本轮**没有夹具**，`NOT_CHECKED`。
7. **本轮不能建立「补齐节点是无害的」。** 两例里有一例改动了实质陈述并使其变假（§C）。

---

## H. 协议自检

### H.1 我实际读了哪些文件

**授权清单内：**

| 文件 | 读取范围 |
|---|---|
| `M3_ECC_LONGITUDINAL_001_FROZEN_v1.1.md` | 全文（134 行） |
| `account-operations/evidence/ep07-longitudinal-v12/_steps.json` | 全文 |
| 同目录 `E01.json` – `E12.json` | 12 份全部，含 `final_answer_only_after_reasoning_strip`、`standing_cycle_baseline_in`、`workflow_inputs`、`projection`、`outputs.draft_raw`、`outputs.gate_report`、`outputs.post_gate_report` |
| 同目录 `_run_index.json` | 全文 |
| 同目录 `_CARRIER_AUTHORIZATION_EXCERPT.md` | 全文 |
| `account-operations/skills/operating-one-account/SKILL.md` | 全文（590 行） |
| `account-operations/skills/operating-one-account/references/fashion-and-market.md` | 全文（它逐字内嵌在 `E01.json` 的 `workflow_inputs.loaded_references` 里，读运行记录时一并读到） |
| `m3-account-content-operator-semantic-v1.0/M3_ACCOUNT_CONTENT_OPERATOR_SEMANTIC_COMPILATION_v1.0.md` | **未通读**，只按关键词（调整／Campaign／保持不变／基线）检索并读了命中的 16 行 |

**授权清单外、禁读清单外，我主动读了一份，理由如下：**

| 文件 | 读取范围 | 理由 |
|---|---|---|
| `M3_ECC_LONGITUDINAL_001_FROZEN_v1.0.md` | 第 1–58 行（§0–§4）与第 70–99 行（§6–§8）。**未读**第 59–69 行（§5，已被 v1.1 §5 取代） | 任务要求我「按冻结件 §6.5 判三条序列级条款，按 §7 给结论」，而 v1.1 **不含** §6 与 §7 正文——v1.1 明写这几节「逐字继承 v1.0……见 v1.0 原文」。不读 v1.0 就无法执行被指派的判定。该文件不在禁读清单的任何一条模式里（`M3_CHECKPOINT_*`／`M3_ACCEPTANCE_CRITERIA_FROZEN_*`／`M3_ECC_REBIND_*`／`M3_FOUNDER_*`／`GATE_DEFECTS_*`／`*_VERDICT_*` 均不匹配）。**我在这里显式披露，由授权方裁定是否接受。** 它带来的额外信息只有判定协议与结果空间本身，不含任何一轮的结果或结论 |

### H.2 有没有读禁读清单里的东西

**没有。** 逐条对照：

- `M3_CHECKPOINT_*`（5 份）、`M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md`、`M3_ECC_REBIND_002/003_FROZEN_v1.0.md`、`M3_FOUNDER_ACCEPTANCE_PACKAGE_v1.0.md`、任何 `*_VERDICT_*`（含本 ECC 前几轮的 `VERDICT_v1.0` 与 `VERDICT_V11_v1.0`）——**均未打开**；
- 其他 ECC 证据目录（`ep06-*`、`ep08-*`）、`ep07-longitudinal-v12-aborted`、`ep10-*`／`ep11-*`／`ep13-*`／`ep14-*`——**均未打开**；
- `account-operations/tools/`、`tests/`、`fixtures/`——**均未打开**；
- **未执行** `git log` / `git show` / `git reflog`。
  **执行过一次 `git status --porcelain`**，用途只有一个：交付前确认我除本文件外没有改动过工作树里的任何文件（结果：工作树里唯一由我产生的改动就是本文件）。它不在禁令列举的三个子命令里，也不显示任何历史或内容，但我把它写出来而不是让核验去发现。

**需要如实说明的一点**：我执行过 `ls` 列目录（仓库根、`account-operations/evidence/`、skill 目录）。这使部分禁读文件的**文件名与修改时间**出现在我的执行记录里。我没有读取其中任何一个字节的内容，且文件名本身不含结论信息。我把这件事写在这里，而不是留给核验去发现。

### H.3 判定过程中目标目录发生了变动 —— 必须报告

判定进行到一半时（我读完 `E01`–`E04`、正要继续读时），发生了以下事件：

```text
17:25  account-operations/evidence/ep07-longitudinal-v12/  →  重命名为  ep07-longitudinal-v12-stale-g1/
17:26  ep07-longitudinal-v12-stale-g1/                      →  改回     ep07-longitudinal-v12/
17:26  该目录内出现一份新文件 _WHY_STALE.md（806 字节）
```

同一时段 `evidence/` 下还出现了 `ep14-gate-v12-defect/`（17:23）。交付前的最后一次工作树状态检查显示，`evidence/ep07-longitudinal-v12-g1fix/` 正在被逐步写入（`E02`–`E06` 已落盘），另有 `ep12-founder-dify-test-pack/`、`M3_FOUNDER_DIFY_TEST_PACK_v1.0.md`、`ep14-gate-v12-defect/DEFECT_G2_*.md` 出现。**这些我一个都没有打开**——我知道的只有文件名。这说明本轮判定进行期间，执行侧正在并行做一次带 `g1fix` 标签的重跑。

我采取的处置：

1. **立刻把整个目录快照到会话临时区**，此后全部判定都基于这份快照，避免判定对象在判定过程中继续变动。快照内容哈希（供核验判定对象身份）：

```text
sha256
ea05eb6a2cc2adcf7ee9150133d383f472d4e6cc717d2d7fa016c4463758a5a7  E01.json
8fc17e9a08a4d4e13a80247c4091f99ca64f874eadcd2cc379774e034c442604  E02.json
a0445aa5e2966da37e2d25dd3bc95235906d5288966c72d95226eddb1aac8cce  E03.json
ccf6b0e8969b2b5d48b3380e2c16b32c2abc5fc53d1d366f31c918045ac08556  E04.json
d06a7b63813a97bcc50ccdad8de1f40938adaa807709abd2c4ea823229a0b2ec  E05.json
cb46e5fa8745c8b6d5fb52a6c9908e2bbd9e2faffc63e17a7963caa3136de4ac  E06.json
ba95abaf3ffca833be43662228a04dba5da0a48e462986a3b980a39baa094929  E07.json
fe3daf3267fd06e37134cf8bbcf8da4102aa67e5822bc3c02418e170a8a114e3  E08.json
8e1528676f0c22de858fc8730e3097c2fc297a73203d72c3b69ae269d9804333  E09.json
c51a84ca88aabbf14ef3bd1f1ca472d17651a5d18b0dc472feb6affb9ff0e651  E10.json
159ad628be08e5fb9e4d3903a81fca3e7d8a473690759b979dcf332a7f527c8d  E11.json
3a7925cf96a51591ea68e596a205e38b7096f35855903b95d9de855a13dd5653  E12.json
251c7349015808c894bb77294f4e2822b7fbb2aee1b10abe2ce40b9f721b08fe  _steps.json
d1e39458a40d350103e32c897649838b0bb8728a83c828764dbfec6d65427a18  _run_index.json
4dc8fc2c29f5a3397f23671fb7d801cedfb995210a44663a5dc9d403a1127f4e  _CARRIER_AUTHORIZATION_EXCERPT.md
```

（`_steps.json` 的完整哈希与 v1.0 头部登记的 `step_file_sha256` 逐字相同，已在 §A 核过。）

2. **我没有读 `_WHY_STALE.md`。** 它不在授权清单里，且它是执行侧对本轮运行的自述；任务明确规定「执行侧的自述不构成证据」。在做完独立判定之前读它，会污染判定。我把它的存在报告出来，由授权方决定要不要把它交给判定环节。

3. **这件事对本判定的效力有直接影响，我不替谁下结论**：如果执行侧已按 A3 把本轮证据置 `STALE`，那么本判定的绑定也随之 `STALE`——但那要由掌握失效理由的一侧宣布，不是我。本判定的绑定就是上面那组哈希。

### H.4 我不确定的条目

| 条目 | 不确定在哪 | 我的处置 |
|---|---|---|
| `E02` 的四周分组是否构成「发布日历/排期表」 | 无日期无发布时点，但确实把 12 条任务分派到了具体周次；`SKILL.md`「不做」段列了「排期表、发布日历」 | 判**未触发**，理由写在 §B `E02`，供复核推翻 |
| `E12` 的「说明需要什么复验」是否充分 | 只说了复验的类别（在有效期内的市场观察），没说复验的口径 | 判**勉强满足**，已在 §B `E12` 标出 |
| `E07` 那句被改写的话里，「三条」指 `L5/L6/L7` 还是上一轮 `L2/L3/L4` | 两种指代在语法上都通 | 两种读法**分别核算**，§C.2 表格里都列了；结论在两种读法下一致 |
| `E10` 判 `不足` 还是 `成功` | 产品语义（`O-9`「有依据地保持不变」）容许它的行为；冻结 Oracle 要求「形成新的、有依据的周期判断」 | 按**冻结 Oracle 字面**判 `不足`，并把两种读法都写进 §B `E10`。这是本轮我最可能被推翻的一步 |
| `E05` 判 `不足` 还是 `成功` | 严格照 `E05` 的三条 Oracle 字面，它可判 `成功`；我把「明确说明哪些判断保持不变」读作要求内容属实 | 判 `不足`，理由与原文引用在 §B `E05`。即使推翻这一步，序列结论不变（`E09` 已是 `失败`） |

### H.5 冻结件里我无法核验的条款

1. **v1.1 §0.2 的权威事件**。Founder 2026-08-26 `CONTINUE_TASK` 第 4 条我核不了原文（`M3_FOUNDER_*` 禁读）。因此「§3 这次改判据是否合法」我**不背书**，只把 §3 当作已冻结判据使用。同理，载体授权链摘录 §2 引的 Founder 授权我也核不了。这两处都是 A1 权威域的事。
2. **载体图本身**。`b7fb5b1a-…` 这个 Dify App 的七节点图（`start → llm → code(闸门) → llm(补齐) → code(取稿) → code(复检) → end`）我无法访问，`account-operations/tools/` 在禁读清单里。我只判它的**输出记录**，不判它的实现。因此：闸门规则的实际代码、补齐节点的提示词、`gate_report` 各字段的计算方式，**全部 `NOT_CHECKED`**。§C.2 里我说闸门那次是"误判"，依据是它自己 `input_contradiction` 字段里写出的判定理由与 `SKILL.md` 里那条规则的适用范围不符——这是可核的；但我没核过实现。
3. **`draft_raw` 是否确为模型未经改动的首轮输出**。这是记录的自陈，记录里没有 Dify 侧的节点级 trace 可以独立佐证。§C 的全部结论都建立在「`draft_raw` = 补齐前正文」这个前提上。
4. **`E04` 补齐节点写进审计块的内容**。记录里只保留了补齐**前**的 `draft_raw`（无审计块）和补齐**后**已剥离审计块的正文，中间那一版带审计块的全文没有留存。因此我无法核验补齐节点补写的审计行是否真的能逐字指回正文——`post_gate_report.still_missing = []` 是系统自述，按任务规定不能代替核验。**这是本轮证据链的一处实质缺口。**
5. **v1.0 §5 运行绑定原文**（第 59–69 行）我没读，它已被 v1.1 §5 取代，且载体授权链摘录 §4 已逐条说明差异。
6. **§2「无状态构造」的载体前提**（Dify workflow 类型 App 的 LLM 节点 `memory: null`）我无法独立核验——那要看图。我只能核到构造的**间接证据**：12 步 `workflow_run_id` 两两不同、每步的 `standing_cycle_baseline_in` 与上一步 `chars_after` 严格相等、`E09` 断链后 `E10` 的基线与 `E09` 逐字节相同。这些与"状态只经投影承载"一致，但不构成对 `memory: null` 的直接核验。

```text
END_MARKER
= ECC-M3-LONGITUDINAL-001-VERDICT-V12-v1.0-END
```
