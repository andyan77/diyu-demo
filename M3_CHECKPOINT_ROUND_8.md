# M3 第 8 轮 Checkpoint · 只读收口

- `task_id`：`DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`（不变，非 NEW_TASK）
- 进度：**`IN_PROGRESS`**。本合同不授权终态，本文件不输出任何终态词
- 远端：`task/m3-account-content-operator-v1` 已推送，本地 `HEAD` == 远端 `HEAD`
- 授权事件：Founder `CONTINUE_TASK`（2026-08-27，八条）→ Founder `PAUSE_NEW_MODEL_CALLS`（同日，九条）
- 候选：`M3 载体 v1.4.2`，Dify App `b7fb5b1a-9278-426c-bb8a-f9f288639548`，已发布版本 `m3-cand-v1.4.2`，图哈希 `3bc0950b500af9b5dc984106edbe64eb0e733dbd733f29c96666db896a8a73a9`
- 本文件按 `PAUSE_NEW_MODEL_CALLS` 九条逐条作答，**全程零模型调用**

---

## 0. 暂停指令下达时的现场

指令到达时有四路模型工作正在跑，**已全部主动中止**：

| 中止对象 | 状态 | 产出 |
|---|---|---|
| 36 名盲评者（workflow） | 已停 | **0 份判定文件**（verdicts 目录空） |
| 保真 ECC 独立判定者 | 已停 | 无判定书，停在读冻结件阶段 |
| 行为 ECC 独立判定者 | 已停 | 无判定书 |
| 纵向 ECC 独立判定者 | 已停 | 无判定书 |

仓库里**没有任何 `*_VERDICT_V14_*` 文件**。这一点决定了下面 §3 的绝大多数状态：
证据齐备，判定没做。

---

## 1. `ep26` 跑后缺陷证据已固化（第 1 条）

提交 `67da66c`，**只追加不覆盖**，历史文件一字未改。

```text
account-operations/evidence/ep26-gate-v14-defects/GATE_V14_DEFECTS.json    四条缺陷 + 拒收归因
account-operations/evidence/ep26-gate-v14-defects/REJECTION_LEDGER_v14.json 12 例拒收逐例判读
account-operations/tools/rebind005/verify_gate_v14_defects.py              可零参数复算
account-operations/tools/rebind005/audit_gate_authored_anchor.py           D-3 残留缺口离线计量
account-operations/tools/rebind005/impact_surface_v14.py                   A3 影响面
```

一并保留、未改动的历史：第 7 轮全部证据目录、两次作废批次的五个目录与两份作废记录、
`REBIND_005` 正文与两份勘误、`M3_FOUNDER_DIFY_TEST_PACK_v1.1.md`（只在文件头加了 `STALE` 指针，正文未动）。

---

## 2. `DD-1`～`DD-4`：根因、影响场景、绑定 AC（第 2 条）

### `DD-1` · 补齐骨架里根本没有新增位那一行

| | |
|---|---|
| **根因** | `gate_main.py` 生成 `required_audit_lines` 时，`POS ::` 行**只按输入侧持续位**生成：`for pid in input_pos["positions"]`。模型本轮新声明的 `NEW:*` 从不进骨架。而补齐节点的系统提示词第 5 条写着「`<required_audit_skeleton>` 就是本轮必须出现的全部行，**一行不多、一行不少**」——它照做，把新增位那一行删掉 |
| **机械坐实** | 逐例复算首闸骨架：11 例里 **10 例骨架中一行 `POS` 都没有**，1 例（`E08`）骨架只有输入那一行、没有模型新声明那一行 |
| **影响场景** | 任何「模型本轮新提出持续位或探索」+「该轮进了补齐环节」的组合。本轮 61 例中命中 11 例，其中 8 例是**纯误拒**（去掉这一条之后其余理由全清） |
| **绑定 AC** | `AC-05`（探索位结构判定）、`AC-08`（稳定兑现 vs 探索）、`AC-17`（纵向连续性）、`AC-11`（Campaign 覆盖与到期链）；间接波及所有走补齐路径的行为例 |
| **与 Founder 第 1 条的关系** | 第 1 条要的是「新增持续位**不得在补齐链路中静默丢失**」。现状：`D-1` 让它**不再静默**，但它**仍然丢失**，而且赔上整份交付。按原文口径，**这一条未满足** |

`D-1` 本身是**真检测**——它检测出来的是闸门自己的构造缺陷。这一点要分开记：
检测器没错，被检测的对象错了，而那个对象是执行侧写的。

### `DD-2` · `G-4` v2 读槽位时把解释性从句里的数字当成权威值

| | |
|---|---|
| **根因** | `_qty_scan` 对槽位值做全串扫描并取第一个**带周期**的数量。槽位 `actual_capacity: 1 条（本周实际，低于基线 3 条）` 里，「1 条」无周期词被跳过，「3 条」因为前 12 字内有「本周」而被判为 `3 条/周` —— 权威值被读成 3，而真值是 1 |
| **影响场景** | 任何槽位值带解释性对照从句的输入。本轮命中 1 例（`B04-1P-capacity-1`），且该例的正文是**正确的**（「本周实际产能只有 1 条，所以按 1 条来排，不假装能做 3 条」）——正确行为被判成违规 |
| **绑定 AC** | `AC-06`（三类产能分离与等价换算） |

### `DD-3` · `G-4` v2 正文侧把非速率句读成速率主张

| | |
|---|---|
| **根因** | 同一例里「按先保主目标和最高价值的一条来排，**本周必须让掉另外 2 条**」被读成「当前目标 = 2 条/周」。「让掉 2 条」是取舍陈述，不是速率主张；周期词「本周」落在回看窗口内，把它激活成了速率 |
| **影响场景** | 产能不足时说明"让掉了几条"的表述——而 `SKILL.md` `O-3` 的判据**恰恰要求**「输出必须能回答哪一条被让掉了」 |
| **绑定 AC** | `AC-06` |

### `DD-4` · 清单矛盾检查把「附件用了但缺账号事实」读成「声称附件未加载」（非本轮改动）

| | |
|---|---|
| **根因** | `check_manifest` 在同一句段内看到参考类名词 + 缺失类措辞就判矛盾。`v1.2` 起就有，本轮未碰 |
| **命中原句** | 「面料/成分已登记（来源=商品负责人周宁），但**没有试穿事实或试穿素材**，必须限制上身类主张」 |
| **为什么是误报** | 缺的是**试穿素材**（账号事实缺口），不是参考文件。`SKILL.md` `O-6` 明确**要求**把这两类缺口分开写，这句话正是照做的 |
| **影响场景** | 任何一句里同时提到参考资料与某个事实缺口的表述 |
| **绑定 AC** | `AC-07`（服装局部失效）、`AC-16`（附件加载状态的反证探针） |

**四条的共同病灶与 `D-2` 完全一样**：为保护判据而建的检查，打在判据**要求**的行为上。
第 7 轮我把这件事写进了 `REBIND_005 §1`，本轮自己又犯了三次。

---

## 3. 当前候选的 AC 状态（第 3 条）

真源：[`evidence/ep27-ac-recompute-v14/AC_STATE_v14.json`](account-operations/evidence/ep27-ac-recompute-v14/AC_STATE_v14.json)。

> **硬规则（写在真源文件里，逐条自查过）**：`workflow_status == "succeeded"` 只说明这一次 Dify
> 运行没有崩，**它不是任何验收项的 `PASS` 依据**。本轮没有一条状态是从 `succeeded` 推出来的；
> 凡以运行成功为唯一支撑的项，一律记 `NOT_VERIFIED`。

按绑定计（`AC-01`／`AC-05`／`AC-12`／`AC-13`／`AC-16` 五条分列绑定，共 27 个绑定）：

| 状态 | 数量 |
|---|---|
| 成立 | 9 |
| `NOT_VERIFIED` | 18 |

**成立的 9 个绑定，全部是确定性可复算的，没有一个依赖模型判断**：

| 绑定 | 依据 |
|---|---|
| `AC-01` ①② 责任边界 | 12 条确定性测试 + 对 v1.4.2 已发布图的结构反搜，四类行为标签命中 0 |
| `AC-05` 结构半 | 同上，落在 enum／分支条件／必填字段 0 |
| `AC-12` 双绑定 | M2 侧一字未动；M3 侧承载该项的 `projection_v13.py` sha256 未变 ⇒ 按 A3 **不多算**，复用第 7 轮 9/9 定向复验 |
| `AC-13` 双绑定 | 同上 + 结构半对 v1.4.2 图重跑：无 `http_request`／`tool` 节点、无环境变量与会话变量、写入标记命中 0 |
| `AC-16` 系统提示词全文 | 从 **publish 端点**（非草稿）读回全文 23317 字符落盘，与 `SKILL.md` + 图里那行占位符**逐字节相等**；`SKILL.md` 工作区哈希 == `git HEAD` 哈希 |
| `AC-16` 浏览器画布 | chromium + 自写 CDP over WebSocket，真实登录态打开画布，从**渲染出来的 DOM** 数出 7 节点 6 边、七个标题齐、无 `http_request`／`tool` 节点；点开 LLM 节点确认面板上挂着本轮新加的判据原文；两张截图带 sha256 |

**`AC-00` 成立**：绑定半全部实测落盘；远端半在本次收口提交后闭合——已推送，本地 `HEAD` == 远端 `HEAD`，可由 `evidence/ep10-closeout-v12/verify_remote_hash.sh` 零参数只读重验。

### 逐条（由真源渲染，`--check` 机械核对两处呈现一致）

| AC | 当前状态 | 依据 | 还差什么 |
|---|---|---|---|
| M3-AC-00 | 成立 | 绑定半成立且为实测非自述：候选 v1.4.2 的 SKILL/四份闸门源码/参考文件哈希由 CANDIDATE_FREEZE_v1.4 记录；系统提示词从 **publish 端点**读回全文并与 SKILL.md 逐字节相等（sha256 76d51208…）；Dify App、已发布版本名 m3-cand-v1.4.2、图哈希 3bc0950b… 落盘。任务身份、合同哈希、分支/worktree 全程未变，非 NEW_TASK。远端半本次收口后闭合：已推送，本地 HEAD == 远端 HEAD（`e95ed04`），可由 `evidence/ep10-closeout-v12/verify_remote_hash.sh` 零参数只读重验 | — |
| M3-AC-01 | ①②责任边界（结构 + 反搜）：`成立`<br>③消融门 B vs B′：`NOT_VERIFIED` | ①② 责任反搜 12 条确定性测试通过；结构反搜对 **v1.4.2 已发布图**重跑，四类行为标签命中 0、落在结构位 0（ep25）。③ 需要盲评揭盲才能推导，本轮 36 名盲评者被中止、0 份判定，`unblind_v5.py` 未运行 | ③ 需要一次完整盲评 + 揭盲；判定者与揭盲脚本均已冻结待跑 |
| M3-AC-02 | `NOT_VERIFIED` | 行为 ECC 本轮 49 例运行齐备（B02-1/2/2P/3/4 等在列），但**冻结件要求由未参与实现者判定**，本轮判定者被中止、无判定书。运行成功不等于验收通过 | 行为 ECC 独立判定 |
| M3-AC-03 | `NOT_VERIFIED` | 同上：B03-1…8 + N2 + N3 十例运行齐备，无独立判定 | 行为 ECC 独立判定（含八变体两两至少两轴实质差异的逐对核对） |
| M3-AC-04 | `NOT_VERIFIED` | 同上：B04-1/1P/N1/N7 四例运行齐备，无独立判定。另注：B04-1P 本轮被系统拒收，而拒收理由已机械判定为**误报**（DD-2/DD-3），这一点须进入判定者的输入而不是替代判定 | 行为 ECC 独立判定 |
| M3-AC-05 | 结构半（对 v1.4.2 已发布图反搜）：`成立`<br>运行时半（B05-1/2）：`NOT_VERIFIED` | 结构半确定性可复算：四类行为标签在 v1.4.2 图上命中 0，落在 enum／分支条件／必填字段 0（ep25）。运行时半的两例运行齐备但无独立判定 | 运行时半的行为 ECC 独立判定 |
| M3-AC-06 | `NOT_VERIFIED` | B06-1/P 两例运行齐备，Oracle v2.0 未再改动（哈希与第 7 轮相同，冻结在先）。但无独立判定。另注：B06-P 本轮被拒收，理由属 DD-1 误拒 | 行为 ECC 独立判定 |
| M3-AC-07 | `NOT_VERIFIED` | B07-1/2/3/P 四例运行齐备，无独立判定。B07-1 与 B07-P 本轮被拒收，理由分别属 DD-4 与 DD-1 误拒 | 行为 ECC 独立判定 |
| M3-AC-08 | `NOT_VERIFIED` | B08-1/P 两例运行齐备，无独立判定 | 行为 ECC 独立判定 |
| M3-AC-09 | `NOT_VERIFIED` | 第 7 轮此项为『不得 PASS』，触发例 B09-5-no-market-data 是一次零正文交付。本轮该例已重跑且产出正文，但**是否满足该例 Oracle 必须由独立判定者判**，执行侧不得代判。B09-N5 本轮被拒收，理由属 DD-1 误拒 | 行为 ECC 独立判定（B09 全族） |
| M3-AC-10 | `NOT_VERIFIED` | B10-1/2/3/4/P 五例运行齐备，无独立判定。B10-3 本轮被拒收，理由属 DD-1 误拒 | 行为 ECC 独立判定 |
| M3-AC-11 | `NOT_VERIFIED` | 纵向 E09→E10→E11 三步运行齐备，投影记录完整。冻结件 §7 不给部分通过留出口，本项随纵向 ECC 整体判定，而纵向判定本轮未产出 | 纵向 ECC 独立判定 |
| M3-AC-12 | 对钉死绑定 `main@df2c595`：`成立`<br>对当前 `main@a7b8101`：`成立` | 本轮**不重跑，按 A3 不多算**：M2 侧（`business-persistence/`）一字未动；M3 侧承载该项的 `projection_v13.py` sha256 6b9b3144… 与第 7 轮相同（CANDIDATE_FREEZE_v1.4 的 unchanged_vs_v13 列表里）。第 7 轮 9/9 定向复验的结论未被任何本轮变化触及 | — |
| M3-AC-13 | 对钉死绑定 `main@df2c595`：`成立`<br>对当前 `main@a7b8101`：`成立` | M2 侧未动；写回候选校验逻辑未动。结构半对 **v1.4.2 已发布图**重跑：无 http_request 节点、无 tool 节点、无环境变量与会话变量、提示词散文外写入标记命中 0（ep25） | — |
| M3-AC-14 | `NOT_VERIFIED` | 第 7 轮此项为『不得 PASS』（事实链无确认人且未显式写出该缺口）。本轮 SKILL.md 已加确认人条款并重跑 B14 全族，但**是否满足该例 Oracle 的『不足』条款必须由独立判定者逐字对照**，执行侧不得代判 | 行为 ECC 独立判定（B14 全族） |
| M3-AC-15 | `NOT_VERIFIED` | B15-DIR-01/02/03 + CTA + N6 五例运行齐备，无独立判定 | 行为 ECC 独立判定 |
| M3-AC-16 | Workflow 图链路（≥7 组保真）：`NOT_VERIFIED`<br>系统提示词全文入证据：`成立`<br>浏览器渲染画布：`成立` | 后两半本轮由确定性证据关闭：①系统提示词从 publish 端点（非草稿）读回**全文落盘** 23317 字符，与 `SKILL.md` + 图里那行参考占位符**逐字节相等**，`SKILL.md` 工作区哈希 == git HEAD 哈希；②浏览器画布用 chromium + 自写 CDP over WebSocket 打开真实登录态画布，从**渲染出来的 DOM** 数出 7 节点 6 边、七个节点标题全在、画布上无 http_request/tool 节点，并点开 LLM 节点确认面板上挂着本轮新加的判据原文，两张截图带 sha256。第一半的 ≥7 组保真判定需要独立判定者，本轮未产出 | 保真 ECC 独立判定（≥7 组） |
| M3-AC-17 | `NOT_VERIFIED` | 纵向 12/12 步运行齐备且投影链完整（本轮持续位链真正建立起来：E02 起 3 位、E03 起 1 位延续到 E10）。但整体判定需要独立判定者，本轮未产出。**已知会进入判定输入的机械事实**：E07/E08 两步被系统拒收，两例均为 D-3 真检测（补齐节点代写 POS 行），在 v1.3 下会被放行并承载为新基线 | 纵向 ECC 独立判定 |
| M3-AC-18 | `NOT_VERIFIED` | A/B 四臂 12 份输出齐备、零泄漏、盲评包已生成（12 个不透明单元、36 份提示词由单一模板生成并当场核验唯一 1 种形态、归一后 sha256 3e928d39…、落盘冻结）。36 名盲评者被中止、0 份判定，`unblind_v5.py`（含四道预冻结闸）未运行。**没有揭盲就没有 AC-18 的任何结论**——不得由执行侧读封存映射代替 | 36 份盲评 + 揭盲推导 |
| M3-AC-19 | `NOT_VERIFIED` | Qwen 半 NOT_APPLICABLE（Founder 指示，运行前判定，未变）。本轮判定者隔离核验未跑（无判定者执行记录可核）；独立收口审查未做 | 判定者隔离核验 + 独立收口审查（冻结 Rubric R-1…R-7 未改，可直接用） |
| M3-AC-20 | `NOT_VERIFIED` | 本轮**按 Founder PAUSE_NEW_MODEL_CALLS 第 8 条不生成新的 Founder 实测包、不请求产品接受**。远端已推送，本地 HEAD == 远端 HEAD（`e95ed04`）。回滚演练与证据索引沿用第 7 轮记录，未因本轮变化重做 | Founder 产品接受（本轮按 PAUSE 第 8 条不请求）；其余各项待技术验收闭合 |

**18 个 `NOT_VERIFIED`，绝大多数不是证据缺失，是证据齐备但判定被中止。** 这两件事必须分开记，
否则下一轮会误以为需要重跑取证。不需要任何新的 DeepSeek 调用就能判的，见 §8。

---

## 4. 12 例拒收的完整清单：10 例误拒、2 例真拒（第 4 条）

真源：[`evidence/ep26-gate-v14-defects/REJECTION_LEDGER_v14.json`](account-operations/evidence/ep26-gate-v14-defects/REJECTION_LEDGER_v14.json)

### 误拒 10 例

| # | 例 | 归因 | 说明 |
|---|---|---|---|
| 1 | `B02-3-enterprise-provisional` | `DD-1` | 声明 `NEW:搭配判断互动探索` ⇒ 骨架无 `POS` 行 ⇒ 被删 ⇒ 拒收。其余理由全清 |
| 2 | `B02-4-blocking-dependency` | `DD-1` | 声明两个新增位，同上。其余全清 |
| 3 | `B03-1-objective-01` | `DD-1` | 声明两个新增位，同上。其余全清 |
| 4 | `B03-2-objective-02` | `DD-1` | 声明 `NEW:起号方向验证`，同上。其余全清 |
| 5 | `B04-1P-capacity-1` | `DD-1` + `DD-2`/`DD-3` | 另叠加 `G-4` 误报：槽位真值 1 条被读成 3 条/周，正文里**正确的**「本周实际产能只有 1 条」反被判成用旧值压输入 |
| 6 | `B06-P-identical-values` | `DD-1` | 声明 `NEW:选品逻辑内容支柱验证`，同上。其余全清 |
| 7 | `B07-1-traffic-full-facts` | `DD-1` + `DD-4` | 另叠加清单矛盾误报，见 §2 `DD-4` |
| 8 | `B07-P-daily-three` | `DD-1` | 声明 `NEW:周宁选品判断固定位`，同上。其余全清 |
| 9 | `B09-N5-uniqueness-probe` | `DD-1` | 声明 `NEW:验证「观众是不是因为第一手选品判断而留下来」`，同上。其余全清 |
| 10 | `B10-3-conflicting` | `DD-1` | 声明 `NEW:线上兴趣到门店询问链路验证`，同上。其余全清 |

### 真拒 2 例

| # | 例 | 说明 |
|---|---|---|
| 11 | `E07` | 模型**一行 `POS` 都没写**，首闸正确判 `unaccounted`；补齐节点替它写出 `POS` 行。`v1.4.2` 把代写的行剥掉后 `unaccounted` 重新成立 ⇒ 拒收。**`v1.3` 下这一例会被放行并承载为新基线** |
| 12 | `E08` | 模型写的 id 缺 `NEW:` 前缀，首闸已判 `fabricated` + `unaccounted` 双重阻断；补齐节点把 id 改成正确的。剥离后重新阻断 ⇒ 拒收，**结论正确**。但三条理由里「…在补齐环节之后消失了—内容被静默丢掉」这一句**措辞不准**：补齐做的是纠正一个编造的 id，不是丢内容。结论对、有一条理由不对，如实记 |

### 对照第 7 轮

| | 第 7 轮（v1.3） | 第 8 轮（v1.4.2） |
|---|---|---|
| 拒收总数 | 7 | 12 |
| 其中误拒 | 6（全是 `G-4` 误报） | **10** |
| 其中真拒 | 1 | 2 |

**误拒从 6 涨到 10，其中 9 例出自本轮自己的修法。** `G-4` 那一族误报确实清零了
（第 7 轮那 5 例误拒本轮全部 `CLEAN` 通过），但换来了 `DD-1` 这一族新的。

### 同时要记的正面数字

| | 第 7 轮 | 第 8 轮 |
|---|---|---|
| `G-4` 阻断命中 | 12 例（11 例误报） | 2 例（均属 `DD-2`/`DD-3` 误报，见上） |
| 声明新增持续位 | 12/61 | **26/61** |
| 结构性探索位 | 11/61 | **18/61** |
| `D-3` 残留缺口（同 id 锚点被代写） | 未测 | 20 例进补齐，**0 例命中** |

---

## 5. 本轮调用、tokens、传输失败与重试（第 5 条）

### 正式批次（候选 v1.4.2）

| 批次 | 载体 | 调用 | tokens | 非 succeeded |
|---|---|---|---|---|
| EP-06 保真 | Dify 图 | 9（+1 保留的失败记录） | 361,747 | `G1-B` 传输失败 1 次 |
| EP-06b 行为 | Dify 图 | 49 | 1,898,302 | 0 |
| EP-07 纵向 | Dify 图 | 12 | 458,259 | 0 |
| EP-08 A/B | 直连 chat/completions | 15（12 臂 + 3 次补齐调用） | 407,843 | `FX-M3-HOLD-02 B` 传输失败 1 次 |
| **正式小计** | | **85** | **3,126,151** | 2 |

### 作废批次（原样保留，不进任何判定）

| 批次 | 调用 | tokens | 作废原因 |
|---|---|---|---|
| 保真（POS 缺陷） | 10 | 309,000 | 见 `ERRATA_001` |
| 行为（POS 缺陷） | 20 | 868,285 | 同上 |
| 纵向（POS 缺陷） | 9 | 341,728 | 同上 |
| A/B（POS 缺陷） | 6 | 183,263 | 同上 |
| A/B（臂绑错版本） | 7 | 188,355 | B 臂 `system_prompt` 是第 7 轮快照进去的 v1.3 全文 |
| **作废小计** | **52** | **1,890,631** | |

### 上线前定向探针（不进证据目录，不产生任何验收结论）

`smoke_v14` 3 例 × 1 次；`probe_newpos`（E01 输入）× 2 次（v1.4.1、v1.4.2 各一）；
`probe_newpos2`（`B02-1`/`B03-2`/`B03-5`）× 3 次。**合计 8 次**，tokens 未逐次落盘。

### 合计

**138 次落盘调用 + 8 次探针 = 146 次；落盘 tokens 5,016,782。**

### 传输失败与重试（逐条）

| 例 | 故障签名 | 处置 | 结果 |
|---|---|---|---|
| `G1-B-goal-gmv`（保真） | `SSLEOFError` / `UNEXPECTED_EOF_WHILE_READING` | 用第 7 轮就冻结的 `rerun_transport_failures.py` 重跑 1 次；失败那次保留为 `__transport_failure_1.json` | `succeeded` |
| `FX-M3-HOLD-02 B`（A/B） | `IncompleteRead(7194 bytes read)` | `run_ab_v5.py --resume` 重跑 1 次；另外 11 份**逐字节沿用、一次 API 都没花**；失败那次保留 | 200，2308 字符 |
| `G5-evidence-expiry`（作废批次） | 同 SSL EOF | 重跑 1 次仍失败；该批次随后整体作废，记录保留 | — |

**重跑的三条硬规则**（与已冻结的 `rerun_transport_failures.py` 同一份词表）：只认传输故障签名；
模型侧签名（`content_filter`／`invalid_param`／余额）一律拒绝重跑；输入逐字节复用同一份冻结输入。
**模型产出的坏结果属于证据，一次都没有重跑过。**

---

## 6. 影响面（第 6 条）

真源：[`evidence/ep27-ac-recompute-v14/IMPACT_SURFACE_v14.json`](account-operations/evidence/ep27-ac-recompute-v14/IMPACT_SURFACE_v14.json)，
依赖边由「载体是什么」+「文件 sha256 是否变化」两样东西定，不靠印象。

| 取证 | 载体 | 结论 |
|---|---|---|
| EP-06 保真 | Dify 已发布图 | `STALE` ⇒ **已重跑** 9/9 |
| EP-06b 行为 | Dify 已发布图 | `STALE` ⇒ **已重跑** 49/49 |
| EP-07 纵向 | Dify 图 + 投影 | `STALE` ⇒ **已重跑** 12/12 |
| EP-08 A/B（B 臂） | 直连镜像 `gate_pipeline_v14` | `STALE` ⇒ **已重跑**，B 臂重绑 v1.4.2 |
| EP-08 A/B（A／A+／B′ 三臂） | 直连单次调用 | **不受影响** —— 三臂 sha256 与第 7 轮**逐字节相同**，由 `prepare_ab_v5.py` 每次生成时机械断言，不符即拒绝生成 |
| 责任反搜／字段消融 | 纯确定性代码 | `STALE`（依赖 `SKILL.md`）⇒ **已重跑**，全绿 |
| M2→M3 投影契约、下游 Brief 消费 | 纯确定性代码 | **不受影响** ⇒ 复用 |
| `AC-12`/`AC-13` 定向复验 | 运行中的 M2 容器 + 投影 | **不受影响** ⇒ 按 A3 不多算，复用第 7 轮 9/9 |

**Founder 实测包**：`v1.0`（载体 v1.2）与 `v1.1`（载体 v1.3）均 `STALE`。
`v1.1` 已在文件头加 `STALE` 指针、正文一字未改。**`v1.2` 本轮不生成**（见 §7）。

**被中止、未产出**：三名 ECC 独立判定、36 名盲评 + 揭盲、判定者隔离核验、独立收口审查。

---

## 7. 本轮不做的事（第 7、8 条）

- 进度保持 **`IN_PROGRESS`**。本文件不输出 `PARTIAL`、`DONE`、`AWAITING_FOUNDER` 中的任何一个；
  `AC_STATE_v14.json` 的 `terminal_state` 字段为 `null`；
- **不生成** `M3_FOUNDER_DIFY_TEST_PACK_v1.2` 或任何后继版本；
- **不请求** Founder 产品接受，不寻求 `ACCEPT`／`RETURN`；
- 未启动 v1.5、未发起新的 DeepSeek 调用、未派发判定者或盲评者。

**不声称**：生产就绪｜真实经营提升｜单账号纵向切片完成｜M5 相关任何结论｜
任何验收项因「运行 succeeded」而 `PASS`。

---

## 8. 不需要新调用就能推进的部分（供规划侧参考，本轮不执行）

以下四项**都不需要再调用 DeepSeek**，只需要判定者读已落盘的记录：

| 项 | 已备齐的输入 |
|---|---|
| 行为 ECC 独立判定 | 49 例运行记录 + Oracle v2.0（哈希未变）+ 判定者 allow-list + 授权链摘录 |
| 纵向 ECC 独立判定 | 12 步运行 + 逐步投影 |
| 保真 ECC 独立判定 | 9 次运行（含 1 例传输失败重跑成功的完整记录） |
| A/B 揭盲 | 12 份输出 + 12 个不透明单元 + 36 份冻结提示词（唯一 1 种形态，归一后 sha256 `3e928d39…`）+ 封存映射；`unblind_v5.py` 的 T-1 自校验当前可过 |

**但请注意**：在 `DD-1` 修复之前跑判定，判定者会看到 10 例被误拒的交付。
这不影响他们判「交付是否满足 Oracle」，但会影响 `AC-17` 这类**看整条链**的结论。
先修还是先判，是 §9 要交出来的决定的一部分。

---

## 9. 最高失效节点修复建议（第 9 条 · **只提建议，未执行、未调用模型**）

按 A3「修复指向最高失效节点，不在下游打补丁」。

### 最高失效节点：`gate_main.py` 的 `required_audit_lines` 生成

**它是最高节点，不是 `D-1`。** 理由：

- `D-1` 是**检测器**，它的行为正确——草稿声明过的位在最终不见了，它报出来了；
- 真正错的是**骨架**：它按输入侧生成，而补齐节点被要求「一行不多、一行不少」。
  两者相乘，等于**系统主动指示补齐节点删掉模型的新增位**；
- 在 `D-1` 上打补丁（比如"新增位丢了只记不阻断"）是下游补丁：位还是会丢，
  只是又变回静默——正好回到 `REBIND_004 §2.1` 被证伪的那句话。

**建议改法（一处，约 3 行）**：`required_audit_lines` 除输入侧持续位外，
再把**首闸已从草稿审计块解析出来的声明**（`decls`，含 `NEW:*`）一并写进骨架。
这样「一行不多、一行不少」的语义从「只保留输入位」变成「保留本轮全部已声明位」，
补齐节点不再有删位的指示。

**连带的两处（同一批，均为判据收窄，不新增判据）**：

| | 改哪里 | 怎么改 |
|---|---|---|
| `DD-2` | `check_stale_value_override` 的槽位解析 | 槽位权威值只取**第一个**数量；括号或破折号引出的解释性从句不参与取值 |
| `DD-3` | 同上，正文侧 | 「让掉／砍掉／推迟 N 条」这类**取舍陈述**排除在速率主张之外（与已有的否定守卫同一层） |
| `DD-4` | `check_manifest`（**非本轮改动**，v1.2 起就有） | 缺失类措辞必须与**参考文件本身**同指才算矛盾；指向账号事实缺口的不算 |

### 修完之后必须重跑什么（A3，供规划侧核价）

改的是闸门代码 ⇒ 系统提示词不变、`SKILL.md` 不变，但**图哈希会变**（代码嵌在图里）。
依赖 Dify 图与直连镜像的四项取证全部 `STALE`：

```text
必须重跑   EP-06 保真 9 次 · EP-06b 行为 49 次 · EP-07 纵向 12 次 · EP-08 A/B 12 次（B 臂）
可复用     A／A+／B′ 三臂（不经闸门）· M2→M3 投影契约 · 下游 Brief 消费 · AC-12/AC-13 定向复验
量级参考   本轮正式批次 85 次调用、3,126,151 tokens
```

### 一个必须由 Founder 决定的取舍

`DD-1` 的修法会让补齐节点保留新增位。**这一步本身没有争议**。有争议的是它之后：

- **甲**：先修 `DD-1`～`DD-4`，再跑一批取证，然后判定。代价是又一个完整批次（约 85 次调用）；
- **乙**：先用**现有证据**跑判定（不需要任何新调用），拿到三份 ECC 判定书与 `AC-18` 的揭盲结论，
  再据此决定要不要修、修完是否值得重跑。代价是判定者会在 10 例误拒的交付上工作，
  `AC-17` 这类看整条链的结论会被污染；
- **丙**：两者都不做，把 `DD-1`～`DD-4` 与「模块是否仍值得独立存在」一起提交产品级 Rebase。

**执行侧不选，也不设默认。** 三条路的成本与污染面都已量化在上面。

---

## 10. 会话交接（换会话必读）

```text
task_id      DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001
分支          task/m3-account-content-operator-v1
worktree     /home/faye/diyu-demo-worktrees/m3-account-content-operator-v1
候选          M3 载体 v1.4.2 · Dify m3-cand-v1.4.2 · 图 3bc0950b…
判据          REBIND_005 + ERRATA_001 + ERRATA_002（三份都不覆盖，逐级后继）
进度          IN_PROGRESS
下一动作      等 Founder 或规划侧审查本文件；未获新授权前不启动任何模型调用
```

`END_MARKER: M3-CHECKPOINT-ROUND-8-END`
