# `REBIND-002` 冻结件 · 载体 v1.1 重绑定与整轮重跑

> `task_id` = `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> 权威事件：Founder 2026-08-26 裁定——**充值完成、推送权限放开、两个实测缺陷修、不需要 Qwen 或其他模型**。
> 本文件在**任何 v1.1 证据运行开始之前**落盘。四份 ECC 冻结件**一个字节都不改**（各自仍只有 1 次提交）；
> 本文件只做三件事：换绑定、如实登记一处判据工具与其自身合同的对齐更正、声明一个新增的失效面。

```text
END_MARKER 见文末
```

---

## 1. 变了什么（锁定变量）

| 锁定变量 | v1.0 | v1.1 |
|---|---|---|
| `SKILL.md` | `5fbc85d18fe40e683c90093dca5638769d23a1ee0664364c3bde4a83d478d938`（31,704 B / 470 行） | `7ae8e5215e53213dd99019444fd2c69fe537b1e3da25bcd969e4303a4a78be1e`（37,091 B / 529 行） |
| Dify 图 | `start → llm → end`，3 节点 2 边，草稿 hash `689df355…fe29c` | `start → llm → code → llm → code → code → end`，**7 节点 6 边**，草稿 hash `e5c30c32d503e05323f621c60222a666af44fcd2f30844aaae6981810d6a3fe5` |
| 代码节点 | 无 | `gate_code` `e8dcc1640e071fdc50f25eb5b838349129bb8f7c94fffb4eb5d6d81a62e8d252`<br>`assemble_code` `ad322f757bffda60817469a77a0b2ed1fc1ee3e5f51e133dab7ccadb33f92a4b`<br>`post_gate_code` `6a1ec698dc98d3e3568fa8b33c451191f67e021d480566535472fe3155b99766` |
| `loaded_references` 输入 | 附件全文或空 | **机器写好的 `<<REFERENCE_MANIFEST>>` 清单** + 附件全文 |

### 1.1 `SKILL.md` v1.0 → v1.1 的全部改动

`diff` 实测：**63 行有差异，其中只有 2 行是删除**，且两行都是原地改写、原意保留并延长：

```console
$ diff SKILL.md.v1.0 SKILL.md | grep '^<'
< **拿不到这份附件时**：照常产出，写进 `missing[]`，……
< **参考文件拿不到时**：直接跳过，照常产出，……
```

其余全部是新增，集中在一节：〈必填项闸门〉。**产品语义零改动**——闸门不新增任何一项必须回答的问题，只是不允许语义主稿**已经**要求的那些被静默省略。`SKILL.md` 文末〈语义回指表〉下已加一段说明，明写闸门不承载任何新的 WHAT。

### 1.2 修的是哪两个缺陷

| # | 缺陷（第 3 轮实测，两名不同独立判定者各自发现，执行侧两次亲自复核） | v1.1 怎么修的 |
|---|---|---|
| 一 | `missing[]` 自述不可靠：附件全文实际就在上下文里（2,799 字符，哈希与其余各组完全相同），模型仍写"本轮未加载"。第 2 轮 `G1-A`、第 3 轮 `G7-A` 两次复现 | **把自述换成回抄**。加载器写清单，`SKILL.md` 绑定"清单说 `LOADED` 就禁止声称未加载"，代码闸门再逐条核对正文与清单是否矛盾。模型对自身上下文的回忆被彻底移出回路 |
| 二 | 必填项没有被结构性强制：同一份能力在 `E10`/`B02-4` 写全、在 `E04`/`B02-1`/`B08-1` 全漏，漏掉的那次读起来一样顺 | **把散文换成固定标签 + 代码闸门**。触发命中即必须逐字出现标签；缺了由**代码**（不是模型自查）检出并触发补齐；补齐后再由代码复检是否真的补上 |

### 1.3 为什么不是"再加一个 LLM 节点让它自己检查一遍"

因为那还是自查。第 3 轮的诊断原话是「输出是自由文本，单个 LLM 节点，**没有门**」。一个会忘记写六项的模型，同样会忘记检查六项。**闸门必须是代码**，这是本次修复的唯一设计约束。

链路里两个 LLM 节点各自只做一件它做得了的事：第一个写正文，第二个**只**按代码给出的缺失清单补齐（`needs_fix=no` 时输出 `NO_CHANGE`）。**"没缺失就逐字返回原稿"由代码节点保证**，不靠让模型照抄——那会引入新的失效面。

### 1.4 上线前的探索性冒烟（不是证据）

冒烟在冻结之前跑，按 A2 属探索，不产生任何正式结论，此处只记录它导致的两处工程修正：

1. `G7-A`（第 3 轮那条假声明的原案例）首跑即被闸门抓到 `manifest_contradiction` 并进入补齐路径；
2. 复检节点抓出**我自己写的检查器的一个假阳性**：原实现用 ±120 字符窗口找否定词，会把 `A.md 已加载；B.md 未加载` 里 B 的"未加载"算到 A 头上。已改为按句读切分，只认**同一句**内的否定，并加了正反两向单元测试（真阴不报、真阳照报）。修复后重跑 `G7-A`：`draft_verbatim`、`gaps_closed: true`、无控制行泄漏、无 `<think>` 泄漏。

---

## 2. 失效传播（A3）——逐项算，不多算也不少算

### 2.1 失效（`STALE` → `NOT_VERIFIED`，必须整轮重跑）

`SKILL.md` 是四份 ECC 的**共享**锁定变量，改一个字节即全部失效：

| 证据族 | 目录 | 处置 |
|---|---|---|
| `ECC-M3-RUNTIME-FIDELITY-001` 三轮 | `evidence/ep06-runtime-fidelity/`、`-v2/`、`-dify/` | 原样保留为历史，**不删不改**；结论降为历史记录，不再支撑任何 AC |
| `ECC-M3-RUNTIME-BEHAVIOR-002` | `evidence/ep06b-runtime-behavior/` | 同上 |
| `ECC-M3-LONGITUDINAL-001` | `evidence/ep07-longitudinal/` | 同上 |
| `ECC-M3-MODULE-AB-001` | `evidence/ep08-module-ab/` | 同上（本来就只跑完 6/12，本轮整 12 次重跑） |

受其影响、本轮起点回到 `NOT_VERIFIED` 的验收项：
`AC-01`（反证探针与③消融门）、`AC-02`、`AC-03`、`AC-04`、`AC-05`、`AC-06`、`AC-07`、`AC-08`、`AC-09`、`AC-10`、`AC-14`、`AC-15`、`AC-16`、`AC-17`、`AC-18`。

`AC-05` 的**结构半也失效**——不是因为 `SKILL.md`，是因为图从 3 节点变成 7 节点，反搜必须对新图重跑。已重跑，见 §2.3。

### 2.2 **不**失效（这一侧同样是判据，多算也是 FAIL）

| 项 | 为什么不受影响 |
|---|---|
| `AC-12` / `AC-13` 的 schema、`projection.py`、M2 接口与迁移 | `SKILL.md` 不是它们的输入。实测 `diff` 未触及 `interfaces/`；83 条测试对 v1.1 重跑**全通过** |
| 83 条测试中读 Skill 树的 2 条（`test_responsibility_reverse_search.py`、`test_projection_contract.py`） | 它们**确实**绑定 `SKILL.md`，所以不是"不受影响"，而是**已对 v1.1 重新取证**：`Ran 83 tests … OK` |
| M2 实况夹具族（`fixtures/m2_live_capture_v1.json`）与市场观测证据 | 绑定运行中的 `diyu-m2-app` 容器与 `main@a7b8101`，与 `SKILL.md` 无依赖边 |
| A/B 的 `A` 臂与 `Bprime` 臂提示词、`A_baseline_competence_review.md` | 两臂提示词逐字未动（`A` 仍为 `160742918653b3f2…`，`Bprime` 仍为 `4a0f99592245a887…`）；胜任性复核结论继续有效 |
| 三个留出夹具 `FX-M3-HOLD-01/02/03` | 由独立撰写者在 Skill 冻结之后撰写，与本次改动无依赖边 |
| 第 3 轮五名判定者的**隔离性**核验（`judge_isolation_verification.json`） | 那是关于他们执行记录的历史事实，永远为真；但他们的**判定结论**所依据的产物已失效，故结论降为历史记录 |

### 2.3 影响关系判断不了的项

**没有**。本次改动面完全可枚举（一份 Skill、一张图、三段代码节点、一个输入字段），依赖边逐条可查，因此不存在需要标 `STALE` 待定向复验的未知项。

### 2.4 需要在收口时重做的（绑定 git/Dify 当前态，不是本次改动造成的）

`git_rollback_drill.md`、`dify_rollback_drill.json` —— 绑定提交与已发布版本，本轮末重做。

---

## 3. 判据：正文逐字继承，只换绑定

四份 ECC 冻结件**不修改**，`git log --follow` 可证各自仍只有 1 次提交：

| ECC | 冻结件 | 提交 | 本轮新证据目录 |
|---|---|---|---|
| `RUNTIME-FIDELITY-001` | `M3_ECC_RUNTIME_FIDELITY_001_FROZEN_v1.0.md` | `c64d762` | `evidence/ep06-runtime-fidelity-dify-v11/` |
| `RUNTIME-BEHAVIOR-002` | `M3_ECC_RUNTIME_BEHAVIOR_002_FROZEN_v1.0.md` | `4bcaaa0` | `evidence/ep06b-runtime-behavior-v11/` |
| `LONGITUDINAL-001` | `M3_ECC_LONGITUDINAL_001_FROZEN_v1.0.md` | `4bcaaa0` | `evidence/ep07-longitudinal-v11/` |
| `MODULE-AB-001` ＋ `ADDENDUM_001` | `M3_ECC_MODULE_AB_001_FROZEN_v1.0.md` | `7564896` | `evidence/ep08-module-ab-v11/` |

- 纵向 12 步文件 `_steps.json` 哈希 **`251c7349015808c894bb77294f4e2822b7fbb2aee1b10abe2ce40b9f721b08fe`，与第 3 轮逐字节相同**；
- A/B 新绑定文件 `_arms_and_holdouts_v2.json` = `3bc5a55c05067c722d4c53fc70fb35241d73974d8f03e828258a23015465f04f`；
- 行为 49 例文件 `_cases.json` = `ab355b8867ca2f85b5f8e3fc37febbd8d470aa1f67314b8a80e95b760bf473f4`，与第 3 轮的差异**仅一处**，见 §4。

`ADDENDUM_001` 的收紧盲评协议本轮**首次实际执行**：盲评包写在仓库之外，事后核验范围覆盖整个 `ep08-module-ab*` 目录、Skill 目录与 sealed mapping。

---

## 4. 一处判据工具与其自身合同的对齐更正（**收窄，必须单独看**）

### 4.1 事实

| 文件 | 冻结时间 | AC-06 失败条款原文 |
|---|---|---|
| `M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md` §M3-AC-06（**被实现的合同**） | commit `f5a9aca`，2026-08-26 04:19:03 | 「把"每天 3 条"**改写成"每周 3 条"**；或三条全部降质凑数；或把普通优先级问题退回用户」 |
| `_cases.json` 的 `B06-1` Oracle（**实现它的工具**） | commit `4bcaaa0`，2026-08-26 10:23:58 | 「把'每天 3 条'**换算成其他时间单位**；……」 |

工具比它所实现的合同**宽**。第 3 轮模型把每天 3 条折成"约 21 条/周"以便与"基线 3 条/周、实际 2 条"同单位比较——**保幅度、且把冲突放大**，不是合同禁止的"静默缩小目标量"。判定者按工具字面判了 `失败`，并**明确拒绝**替执行侧收窄条款（那属于看到结果后放宽判据）。判定者做得对。

### 4.2 处置

- v1.1 的 `_cases.json` 把 `B06-1` 的失败条款**回到被实现的判据原文**，并在同一字段内写明这是更正、更正理由与两份文本的提交时间；
- **第 3 轮的 `FAIL` 原样保留在第 3 轮记录里，不追溯抹除**；
- 实测证明只改了这一处：49 例中**仅 `B06-1` 一例、仅 `oracle` 一个字段**有差异，其余 48 例与全部顶层字段逐字节相同。

### 4.3 为什么这不是"看到结果后放宽判据"

因为**被放宽的不是判据，是一个比判据更严的工具**。判据本身自 04:19:03 起一字未改，且其冻结时间**早于**工具。让工具回到它所实现的合同，是修一个实现缺陷；把一个已知错误的工具带进新一轮，才是更坏的选择。

**这条留给独立收口审查者按 `R-1` 逐条复核。** 若审查者认为这仍构成事后放宽，处置是把 `B06-1` 本轮结果降为 `NOT_VERIFIED`，而不是回填 `PASS`。

---

## 5. 新增的失效面：闸门可能制造"空心合规"

必须先说清楚，不能等判定完了再说。

**闸门保证标签在，不保证标签后面那句话有内容。** 补齐节点可以写 `未知`、可以写一句正确但空洞的话，而标签照样齐。这是一个**新的**失效模式——v1.0 的失效是"漏写"，v1.1 的失效可能变成"写了等于没写"。

因此本轮判定**新增一条对判定者的强制要求**（收紧，冻结于本文件，早于任何 v1.1 运行）：

> 标签齐 **≠** 该项成立。判定者必须看标签后面的实质：
> 空洞、同义反复、把问题原样抄回、或与本轮事实无关的填充，一律判 `不足`，**不得因为"格式完整"判成功**。
> 判定者另需单独报告：本次输出里有几处标签属于空心合规。

同时明确：**代码闸门不闸任何一条 AC。** 它是证据，不是验收门——它无法制造冻结判据未定义的 `PASS`。要让它成为验收门，需要 Founder 的判据改版事件，不在本轮。

---

## 6. Qwen

Founder 2026-08-26 明确指示本任务不需要 Qwen 或其他模型核验。按 A1 属有权者决定域，`EP-09` 按 `NOT_APPLICABLE` 处理。

**但这降低了正式契约 v1.2 的一项验收门槛**（契约原文要求 Qwen 独立语义核验且 `M3-AC-00 through M3-AC-20 all PASS`）。执行侧不把它推成"已满足"，也不回填任何 AC 为 `PASS`；是否正式 `REBASE` 契约由 Founder 决定。`AC-19` 本轮终值为 `NOT_APPLICABLE`，不是 `PASS`。

---

## 7. 本轮执行顺序（时序自证）

```text
1. 修 SKILL.md v1.1                          （已完成）
2. 重建并发布 Dify 图 v1.1                    （已完成，草稿 hash e5c30c32…）
3. 探索性冒烟 + 修检查器假阳性                 （已完成，非证据）
4. 结构反搜对新图重跑：AC-05 标签命中 0        （已完成）
5. 83 条测试对 v1.1 重跑：全通过               （已完成）
6. ★ 本文件与三份重绑定输入文件落盘提交 ★      ← 判据事件，必须早于结果事件
7. 四份 ECC 整轮重跑（9 + 49 + 12 + 12 = 82 次）
8. 独立判定（互相隔离，机械核验隔离性）
9. 独立收口审查、回滚演练、远端推送、Founder 验收包
```

**第 6 步之后、第 7 步之前，任何一次运行都不算数。** 提交顺序即证据。

```text
END_MARKER = M3-ECC-REBIND-002-FROZEN-v1.0-END
```
