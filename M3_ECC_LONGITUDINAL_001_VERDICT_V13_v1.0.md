# `ECC-M3-LONGITUDINAL-001` · 独立判定书（第 13 轮运行 / `ep07-longitudinal-v13`）

> `verdict_file` = `M3_ECC_LONGITUDINAL_001_VERDICT_V13_v1.0.md`
> `judged_against` = `M3_ECC_LONGITUDINAL_001_FROZEN_v1.1.md`（含其逐字继承的 v1.0 §1/§2/§4/§6/§7/§8）
> `judged_evidence` = `account-operations/evidence/ep07-longitudinal-v13/`（12 步 + `_run_index.json` + `_CARRIER_AUTHORIZATION_EXCERPT.md`）
> `judge` = 未参与本任务实现、未读任何 Checkpoint / 往轮判定 / 其他 ECC 证据的独立读者
> 结论先行：**`FAIL(INSUFFICIENT)`**。触发条款是冻结件 §7「不足：任一步 `不足` → `FAIL(INSUFFICIENT)`」，触发步是 **`E06`**。
> 触发原因**不是**招募我时提出的那个连续性问题，而是我在交叉核对系统记录时独立撞上的另一件事，见 §3.6 与 §5.2。
> 招募我时提出的连续性问题，答案在 §4：**正文里不止 1 个跨步对象，我追踪到 21 个；结构化通道全程只装着 1 个。**

---

## 0. 判定边界与我实际用的判据

### 0.1 我判的是什么

冻结件 v1.1 §1 命题（逐字继承 v1.0）：

> 同一虚拟账号的多周期／多事件序列中，`诊断 → 周期计划 → 日常变化 → 发布前/测试反馈 → 局部复盘 → 调整或保持 → Campaign 覆盖/结束 → 仍有效基线恢复` 可复现，且**状态经由定义的投影承载，不靠对话记忆**。

判定协议按 v1.0 §6，结果空间按 v1.0 §7 的四态（`成功` / `不足` / `无结果` / `失败`），声明上限按 v1.0 §8。我没有新增维度，也没有自创中间态。

### 0.2 绑定对不上：我接受授权链，理由如下

冻结件 §5 锁定的绑定与证据目录不一致。摘录《载体授权链与判据变更摘录》§4 逐条列了差异（`SKILL.md af61b82` → `carrier_revision v1.3`；直连载体 → Dify Workflow 七节点图；`gate_v12` → `gate_v13`；行为 Oracle v1.1 → v2.0），依据是摘录 §2 的 Founder 第二次 `CONTINUE_TASK` 第 1 条：

> `G-2`、`G-3`、`G-4` 均为**已冻结 AC 下的技术缺陷**，必须在本任务修复；用不用结构化对象、用什么节点拓扑，属**执行侧技术 HOW**，不再请 Founder 选择。

我做了两项能自己做的独立核对，两项都通过：

| 我核对的 | 摘录 §3 声称 | 我算出的 | 结果 |
|---|---|---|---|
| `SKILL.md` sha256 | `d91eae1f3a842ff1233f9cd0e6c9501efd06503d935f46451643f20ca0cfb9b3` | `d91eae1f3a842ff1233f9cd0e6c9501efd06503d935f46451643f20ca0cfb9b3` | 一致 |
| 12 份证据文件的 `dify_app_id` / `model` / `provider` / `temperature` | `b7fb5b1a-9278-426c-bb8a-f9f288639548` / `deepseek-v4-flash` / `langgenius/deepseek/deepseek` / `0.4` | 12 步全部同一组值，无第二组 | 一致 |

**时序（A2 §3 先后）我也自己核了一遍**：摘录 §5 声明两处判据变更冻结于 `70a121bc58123fe5775c66f5818b2049608ef2bf`，`2026-08-26 19:54:28 -0700`。证据里最早的一步 `E01.json` 的 `timestamp_utc` 是 `2026-08-27T04:07:38Z`，即 `2026-08-26 21:07 -0700`；该步 `elapsed_seconds = 229.0`，倒推开跑约 `21:03 -0700`。**判据冻结早于本轮第一次调用约 69 分钟**，判据在结果之前。这一条我认为成立，不标 `NOT_VERIFIED`。

### 0.3 但有一件事必须写明：我手上的 §3 不是本轮实际运行的投影规则

冻结件 v1.1 §3 写的是**三条互斥分支**：

```text
分支 1  carry = REJECTED_KEEP_PREVIOUS  ⇒ standing = prev，mode = KEPT_PREVIOUS
分支 2  carry = ACCEPTABLE 且 objects(prev) 中存在本轮未重述、也未被点名处置的对象
        ⇒ standing = out + 机器写的一段：prev 里提到这些对象的原句，逐字保留
        ⇒ mode = MERGED_KEEP_PRIOR
分支 3  其余 ⇒ standing = out（整体替换），mode = REPLACED
```

本轮实际运行的投影只有两个 mode。`_run_index.json` 12 步逐条：`ADVANCED` × 11、`KEPT_PREVIOUS` × 1（`E06`）。**`MERGED_KEEP_PRIOR` 一次都没有出现，因为 v1.3 的实现里没有这个分支。** 摘录 §5.1 描述的新投影是：

> 投影　新基线的 `standing_positions` 由**声明**生成，不由散文重建：`continued` → 保留该位…`disposed` / `superseded` → 移出活动集…`NEW:*` → 新增

这是授权范围内的技术 HOW 改动，我接受。但它有一个**必须记录的覆盖面后果**，而且这个后果没有出现在摘录 §5.1 的自述里：

- v1.1 §3 分支 2 的保护对象是 **`objects(prev)`——从上一基线里抽出的关键对象**，抽取由投影侧做，模型不参与，"未重述且未被点名处置"就逐字保留；
- v1.3 的保护对象是 **模型自己在审计块里声明过的持续位**，模型不声明就不存在。

保护范围因此从"投影侧抽得出来的东西"收窄成"模型愿意声明的东西"。摘录 §5.1 说新字段 `positions_unaccounted[]`「非空即阻断，因此不会再出现"字段全程为空而内容全丢"这种事」——这句话在它自己的定义域内是对的（该字段定义为"输入里有、声明里没有的 id"），但它**只覆盖已经进了通道的 id**。§4 是我对这个覆盖面的实测。

**我无法核验 v1.1 §3 分支 2。** 本轮没有它的实现，也就没有它的运行记录。这一条我按"无法核验"如实记，不按"通过"记，也不按"失败"记。

---

## 1. 事件覆盖（冻结件 §4）

冻结件 §4 要求 12 步覆盖 ≥9 类事件。我用 12 份证据文件里的 `event_kind` 字段逐条对照 §4 的表：

| §4 要求的事件 | §4 指定的步 | 证据里的 `event_kind` | 对上？ |
|---|---|---|---|
| 产能下降 | `E03` | `capacity_drop` | ✓ |
| 市场机会 | `E04` | `market_opportunity` | ✓ |
| 单条异常 | `E05` | `single_anomaly` | ✓ |
| 多条一致 | `E06` | `multi_consistent` | ✓ |
| 冲突反馈 | `E07` | `reentry_conflict_openwindow` | ✓ |
| 观察窗未结束 | `E07`（同轮并置） | 同上 | ✓ |
| 目标冲突 | `E08` | `objective_conflict` | ✓ |
| Campaign 覆盖 | `E09` | `campaign_overlay_start` | ✓ |
| Campaign 到期 | `E11` | `campaign_expiry_and_reuse_probe` | ✓ |
| 证据过期 | `E12` | `evidence_expiry` | ✓ |
| 换会话重入（AC-17 反证探针） | `E07` | 同上 | ✓ |
| 活动期内合法变更未覆盖位置 | `E10` | `baseline_change_during_campaign` | ✓ |

12/12 对上，§4 满足。

**核验限度**：冻结件 v1.0 引用了 `account-operations/evidence/ep07-longitudinal/_steps.json`（`step_file_sha256 = 251c7349…`）作为准确用户输入与**逐步 Oracle** 的真源，v1.0 §6.3 也把它列为判定者应被提供的材料之一。**这份文件不在下发给我的可读清单里，`ep07-longitudinal-v13/` 目录下也没有它。** 因此：

- 我**没有**逐步 Oracle，我的逐步判定是按冻结件 §1 的链路阶段、§4 的事件语义、§6.5 的三条序列级判定，以及可读清单里的产品语义主稿（`M3_ACCOUNT_CONTENT_OPERATOR_SEMANTIC_COMPILATION_v1.0.md`）与 `SKILL.md` 做的；
- 我**没有**核验 `_steps.json` 的哈希，也没有核验每步用户输入是否与冻结的那份逐字一致。这一条我不能说通过。

---

## 2. 无状态构造（冻结件 §2）

§2 说 Dify workflow 类型 App 没有会话记忆，步与步之间唯一的信息载体是 `account_context` 里的槽位。证据支持这一点：12 步各有独立的 `workflow_run_id`，各自 `http_status: 200` / `workflow_status: succeeded`，输入全部通过 `workflow_inputs` 显式传入。

因此 §1 命题里"不靠对话记忆"这半句**由构造保证**，不是本轮实测出来的成绩；§2 自己也把这写成上限。我不在这一点上给任何加分。

**但要点明一件事**：由构造保证的是"没有会话记忆"，**不是**"投影承载了该承载的全部状态"。这两件事在本轮被明显区分开了——见 §4 与 §5.3 里 `E11` 自己写的那句话。

---

## 3. 逐步判定（冻结件 §6.4 / §7 四态）

### 3.1 `E01` · 运营状态诊断 —— `成功`

- 阶段解释绑定齐全，符合语义主稿 §7「阶段判断必须绑定账号、目标、平台、观察窗口和关键条件」。原句：「阶段判断绑定的是：视频号上的序里集周宁账号、长期价值目标、初秋通勤衣橱第一阶段的四周观察窗，关键条件是互动能否随判断结构在不同商品或决策问题上重复出现。」
- 不用虚假确定性填空：「所以这张牌是"有早期信号、未被验证"，不是"已被证实"。」
- 三类产能分开说：「期望产量、账号能持续维持的基线产量、当期实际可用产能，三者一致，都是三条一周，产能没有缺口。」符合主稿 §5.3。
- 合法的"不生产"结论 + 四项缺口交代齐全（主稿 §4「日常决策…或无任务决定」）：「这一轮不绑定新的内容任务，因为绑定周期内容任务所需要的观察证据还没有进来」＋「这些数据只有从视频号后台拉取，或由你们直接提供」＋「数据到位后，就能判断"选品判断"这条支柱能不能立」＋「现在能做的替代动作是：照常执行…诊断不改变现有节奏」。
- 只问一项区分力最高的问题（主稿 §3.1）：「真正需要你们回答的只有一个：周宁本人能不能出镜，或者有没有上身试穿素材？」
- 系统记录交叉核对：`gate_status: CLEAN`，`gate_path: draft_verbatim`，无补齐。

### 3.2 `E02` · 周期规划 —— `成功`

- 交付了主稿 §4 表里"周期规划"要求的全部最小交付：目标层级、内容组合、节奏、产能取舍、验证计划、可派发任务。
- 明确不越界（主稿 §10 末段、§13）：「每条任务的具体钩子、叙事、画面、标题、封面由下游决定；这里只定业务工作、受众问题、内容承诺、可用事实与观察目标，以及明确不做的主张。」
- 拒绝凑数（主稿 §2.2「不是选题清单生成器、平均配额器」）：「这一轮只排完成验证所需的任务，不为凑满 12 个位置新增内容。」
- 探索六项写全（不确定性 / 最小可逆动作 / 支持继续的观察 / 推翻信号 / 到期 / 让位的稳定义务），全在〈验证与复验〉那一段里，逐句可指。
- `gate_status: CLEAN`，`gate_path: draft_verbatim`。

**同时记录一处与判定无关但与 §4 直接相关的事实**：`E02` 新排了六个内容任务位（任务一至任务四 ＋ 知识类两条），审计块里 `新增·常规` 声明数 = **0**（`positions_final.new_positions: []`）。按 `SKILL.md`〈持续位〉表，`新增·常规` 的定义正是「你本轮新排的一个常规任务位或栏目」。这不改变 `E02` 的交付判定（冻结件 v1.1 §3.1 明确"未重述不阻断交付"，同理未声明也不构成交付失败），但它是 §4 覆盖面缺口的起点。

### 3.3 `E03` · 产能下降 —— `成功`

- 三类产能分开，且按实际能力排：「你期望的发布量是每周三条，账号正常情况下能稳定维持每周三条，但本周实际可用的人力只有一条（苏禾请假）。按实际能力排，本周发布量就是一条。」与输入槽位 `actual_capacity: 1 条（本周实际，苏禾请假）` 逐字对得上。
- 影响面限定（主稿 §5.9）：「原排的上新任务不取消，只是本周空出来的位置顺延到剩余三周里。」
- 拒绝形式补齐：「不为凑每周三条把本周压缩成一条内容里同时讲价格、打理、层叠…宁可少发，不混发。」
- 逐条交代上一基线里的持续位：「选品判断支柱验证继续推进，本周的判断内容就是它的第一格观察点；验证窗口和序列不动。」

**系统记录里的一处误报，我照实记**：`gate_report.stale_value_override` 命中了

> `expected_publish_count: 输入权威值 3条/周，正文却断言当前是 3条/?、1条/? —— 「2. 不为凑三条把本周压缩成一条内容里同时讲价格、打理、层叠…」`

被命中的这句话是**在拒绝**把三条压成一条，句里"三条"正是槽位原值。摘录 §5.3 自己写了豁免条款「同句段同时逐字引用了槽位原值 ⇒ 不阻断」——槽位写作 `3 条/周`、正文写作"三条"，阿拉伯数字与汉字数字没有归一，豁免没生效。本步经补齐后 `post_gate.stale_value_override: []`，`carry: ACCEPTABLE_AS_NEW_BASELINE`，未造成后果。**这条误报本身不改变 `E03` 的判定**，但它与 §3.6 是同一个根因，先记在这里。

### 3.4 `E04` · 市场机会 —— `成功`

- 机会五要素齐全（主稿 §10）：信号来源与平台时间（「视频号女装赛道里"通勤衣橱一件多穿"这类内容近两周互动稳定」，与输入槽位 `观察M1` 对得上，未编造）；适配（「和账号现在的"初秋通勤衣橱"任务、针织马甲商品直接相关」）；最小验证（「新增一条"一件多穿"方向的最小验证内容，作为探索位」）；反证条件（「如果这条验证内容互动明显低于账号平均水平，或者与选品判断支柱冲突，就停止这个方向，回到原排」）；到期（「观察有效期四周，下周验证仍在窗口内」）。
- 不因外部信号改排期（主稿 §10「外部证据可以支持轻量可逆试验，但不能证明账号表现因果」）：「这一格不能挪，挪了后面三格全错位，等于放弃验证设计。所以本周不插入新方向，下周再腾一格来做验证。」
- 输入槽位对得住：`actual_capacity: 3 条/周` 在本步恢复，正文最终写「本周排期是三条」。

**这一步的 `G-4` 闸门是一次真阳性，工作正常**：模型初稿写了「3. 本周实际产能只有一条，已经排给四周验证序列的第一格…」——那是 `E03` 的旧值压过本轮权威输入，`gate_report.stale_value_override` 命中，经补齐后 `post_gate.stale_value_override: []`。这一条我确认闸门做对了。

**但同一步里有一件必须单列的事，见 §5.1**：`E04` 的模型初稿在审计块里**声明过第二个持续位**，被补齐节点删掉了，没有任何计数器记录这次删除。这不改变 `E04` 的交付判定（正文里那条探索安排完好无损地留在最终交付里），但它是 §4 里最锋利的一条证据。

### 3.5 `E05` · 单条异常反馈 —— `成功`

- 证据身份绑定准确（主稿 §15.1/§15.2）：「这条反馈来自视频号后台，是真实发布观测，观察窗口已经结束。」与输入 `反馈L1：kind=真实发布观测，is_test=false，is_simulated=false…source=视频号后台` 逐字对得上。
- 混杂因素显式：「而且你自己标了平台分发规则本周改版。单条低完播率加一个已知混杂，无法区分是方向问题、这条内容自己的问题、还是平台改版的问题。」
- 有依据地保持不变（主稿 §15.7）：「不要改方向。」并给出收紧后的复验条件。
- 不越界（主稿 §14 Creative Script 行）：「这是制作层面的调整，不是换方向。具体怎么做由下游决定，我不在这里规定创意。」
- `gate_status: CLEAN`，`gate_path: draft_verbatim`。

### 3.6 `E06` · 多条一致反馈 —— **`不足`**

**交付本身是合格的。** 三条一致允许扩大范围但不外推（主稿 §8「一次高表现只能影响与它直接相关的判断」的对称面）：「三条一致允许我把调整范围从"单条内容"扩大到"商品细节讲解这一类内容"，但仍然只覆盖这一类内容，不能推广成整个账号模板。」区分观看偏好与账号价值：「评论区的"想看上身效果"是观众主动表达的观看偏好，但它不等于观众会因此更关注、更信任或更愿意回访。」局部失效纪律正确（主稿 §9）：「没有试穿素材，只阻止版型、垂坠、体感这类需要上身证据的主张，不阻止面料、工艺、价格、现货色彩、搭配原则类内容。」闸门侧也没找出实质问题：`hard_fail_reasons: []`、`missing_items: []`、`still_hollow: []`、`still_unanchored: []`、`internal_leaks: []`、`body_chars: 1652`（比通过的 `E10` 的 789 长一倍还多）。

**判 `不足` 的理由是：这份交付整份被投影拒收，而拒收依据在它自己写明的作用域之外，导致本步的实质判断在 `E07`–`E12` 中永久消失。**

拒收记录（`E06.json` `post_gate_report`，逐字）：

```text
carry_blocking       = true
cycle_state_carry    = "REJECTED_KEEP_PREVIOUS"
carry_reject_reason  = ["用过期值压过本轮权威输入：expected_publish_count: 输入权威值 3条/周，
                        正文却断言当前是 1条/? —— 「2. 条件性内容任务：如果本周能安排试穿、
                        或拿到可信的上身素材，下一条同类商品细节内容就以上身证据作为验证点，
                        这条任务承」"]
```

被指控的那句话，逐字是：

> 「2. 条件性内容任务：如果本周能安排试穿、或拿到可信的上身素材，下一条同类商品细节内容就以上身证据作为验证点，这条任务承担的主要业务工作是验证上身证据能否改变受众对选品判断类内容的反应，**不承载其他目标**；如果没有素材，这条不启动，照常发布不依赖上身证据的内容，并且不写版型、垂坠、体感类主张。」

**这句话里没有任何关于发布量的主张。** 按摘录 §5.3 写死的机械条件逐项对：主语锚 `目标`（命中"不承载其他**目标**"）＋ 现时性词 `本周`（命中"如果**本周**能安排试穿"）＋ 数量（"下一条"／"这条"被读成 1 条）＋ `1 ≠ 3` ⇒ 阻断。规则按字面执行了，但摘录 §5.3 给自己划的作用域是：

> **这一条只挡"用旧值压当轮输入"，不挡"解释为什么产能会变"**

`E06` 这句既不是前者也不是后者——它压根不在谈产能。规则的机械形态比它自己声明的作用域宽，"目标"这个词被当成 `expected_publish_count` 的主语锚，"这条／下一条"被当成数量主张。`E03` 已经出现过同类误报（§3.3），`E03` 侥幸被补齐清掉，`E06` 没有——因为这里根本没有可补的东西，补齐节点把那句改写成"…这条任务**承担**…"（对比初稿的"…这条任务**只**…"），检测器照样命中。

**后果是可核查的内容丢失。** 分支 1 触发后 `standing = prev`（`E05` 的正文），`chars_before = 1238`、`chars_after = 1238`，`E07` 的 `standing_positions_in.last_restated` 停在 `E05`。`E06` 独有的三样东西从此不再出现在任何一步：

| `E06` 独有的判断 | `E07`–`E12` 出现情况（我逐份正文搜过） |
|---|---|
| 「上身证据」作为选品判断内容的新验证变量 | `E07` 无、`E08` 无、`E09` 仅出现在到店语境「目前没有试穿素材或上身证据」、`E10`–`E12` 无 |
| 条件性内容任务（有素材则下一条以上身证据为验证点，无素材则不启动） | 全序列再未出现 |
| 四维观察目标（完播率＋互动质量＋关注变化＋评论区是否继续出现上身诉求） | `E07` 写的是「完播率、互动、评论和关注变化一起看」，其来源是 `E05` 的基线，不是 `E06` |

**与冻结件的对照**：v1.1 §3 分支 1 的授权语义写在它自己的注脚里——「这是 Founder 第 4 条第 1 项"**零产出**不得覆盖上一有效周期状态"」。`E06` 不是零产出。保护零产出的机制被用在了一份 1652 字符、闸门查不出实质缺陷的交付上，把它整份丢掉了。按 §1 命题的后半句「状态经由定义的投影承载」，`E06 → E07` 这一环没有承载住。按 §7，这是"有但不够"，即 `不足`。

**我也照实记它做对的一半**：这次丢失**不是静默的**。`projection.explicit_failure` 写着「本轮交付不够格成为当前有效判断，上一有效基线与持续位原样保留」，`carry_reject_reason` 完整留档，`carrier_reported` 与 harness 复算一致（摘录 §3 要求的双算）。Founder 第 4 条第 3 项"内容丢失必须显式失败，不得静默继续"这一项**满足**了。不满足的是第 1 项的适用条件——被保护的不该是这一份。

### 3.7 `E07` · 换会话重入 ＋ 冲突反馈 ＋ 观察窗未结束 —— `成功`

这是三个探针并置的一步，三个都过：

- **三种证据身份不互相顶替**（§6.5 第二条、主稿 §15.2），逐条分开：「三条反馈分别来自视频号后台、陈晚口头转述和一条尚未结束窗口的发布观测，可信度和来源都不同，必须分开处理。」与输入 `反馈L5 kind=真实发布观测` / `反馈L6 kind=人工经营结果，is_manual_entry=true，source=陈晚口头转述` / `反馈L7 window_end=2026-09-01（尚未结束）` 一一对得上。
- **人工经营结果不被当成内容效果**（主稿 §15 末段）：「它不能证明内容失效，因为中间隔着门店客流、天气、销售节奏等混杂因素。」
- **观察窗未结束不坍缩成结论**：「它现在不能用来做复验判断，也不能据此调整价格表达。」
- **不合并成趋势**：「几条反馈方向不一致，不能合并。」
- 闸门侧：`triggers_computed_from_input.conflict` 由系统从输入算出，`gate_status: CLEAN`。

**换会话重入探针**：用户说「接着上次的继续」，模型答「接着上一轮，结论仍然是不改方向」，其接续对象来自 `standing_cycle_baseline` 槽位——由构造只能如此（§2）。探针形式上通过。

**但要写明一件事**：用户口中的"上次"是 `E06`，模型拿到的"上一轮"是 `E05`——因为 `E06` 被拒收了。模型无从知道这一点，也没有任何提示告诉它中间少了一轮。这不是 `E07` 的问题，代价已经计在 `E06` 头上，不重复计。

### 3.8 `E08` · 目标冲突 —— `成功`

- 混合目标处理符合主稿 §6 末行：「明确主次、代价和不可牺牲条件；不把所有目标压成一个综合分或塞进每条内容」。原句：「可以把它当作一个有时限的临时重点…验证继续，线索不铺开，老板的要求有边界地落地。」
- 阶段与覆盖分离（主稿 §7）：「老板的要求是"这周"的临时覆盖，不是把账号改成咨询号。」
- CTA 三层权限守住（主稿 §6 末段）：「咨询类动作必须有真实能接住的路径。目前我没有看到关于谁接咨询、用什么接、能不能及时回复的事实，所以在确认之前，我不会安排任何明确的咨询引导。」与输入 `expression_permission: …经营型 CTA 需目标+有效承接路径` 一致。
- 拒绝不可逆的全量转向：「无论哪种，都不让三条内容全部转向咨询。」
- `gate_status: CLEAN`。

### 3.9 `E09` · Campaign 覆盖生效 —— `成功`

主稿 §12 四条逐条对得上：

| 主稿 §12 | `E09` 原句 |
|---|---|
| 只占用它点名的内容位置 | 「这两周（8月27日—9月9日）按"活动只占它指定的位置，不接管整个号"来排」 |
| 未覆盖位置仍由 M3 管理 | 「第1条和第3条是活动位置…第2条继续做选品验证」 |
| 到期后回到仍有效的周期基线 | 「9月10日起回到当时仍然有效的验证基线，不自动延续」 |
| 不静默改写对方真源 | 「主目标仍然是长期价值，活动是两周的临时覆盖」 |

活动约束逐字回抄输入（`约束=不得使用站外导流与价格承诺`）：「活动内容只做平台内的到店预约引导，不承诺优惠，不把人导向站外。」承接路径未确认就不放入口：「所以在确认前不放预约入口。」

**还做了一次显式处置**：「之前"某一条加咨询入口"的待定方案，这两周被活动位置替代，不重复排。」——这是全序列里少数几次对上一基线里某个待定安排的点名处置，记一笔正面。

### 3.10 `E10` · 活动期内合法变更未覆盖位置 —— `成功`

这一步是 `M3-AC-11` 的关键探针（§4 表：「AC-11 的"恢复当前基线而非活动开始快照"必须有这一步才可判」）。它必须在活动期内对未覆盖位置产生一次**当前基线的变化**，`E11` 才有得可判。它做到了：

> 「第2位：继续承担选品判断验证，观察重点变成活动互动中暴露的选品决策问题，比如哪些颜色、版型、搭配问题被反复问。这不改变它的主要业务工作。」

- 未覆盖位置确实由 M3 管理，没有让活动吃掉：「它现在是唯一继续承担选品判断验证的位置；换掉它，整个活动周期里验证就完全中断了。」
- 用户给了具体方向时给出可见处理结果（主稿 §16）：不是硬拒，而是「如果你确实想换，我可以给一个替代…但代价是验证暂停」。
- `gate_status: CLEAN`，`body_chars` 短（789）但五样齐全，未触发最低实质产出。

### 3.11 `E11` · Campaign 到期 ＋ 沿用追问 —— `成功`

- 到期恢复的对象是**当前有效基线**而不是活动开始快照，这一点是 `AC-11` 的判定点，它明确落在 `E10` 造成的那次变化上：「上一轮把它的观察重点偏到活动互动，现在活动结束，这个通道关闭，改为通过常规互动继续收集大家关心的选品问题。」——恢复的是 `E10` 的当前状态，不是 `E09` 的快照。
- 拒绝把偏好升级成证据（主稿 §15.2、§8）：「"效果还行"是你个人的偏好判断，没有配套观测数据。」「另外，你说的是"还行"，这个判定强度本身也不足以把活动内容升级成常规内容。」
- 拒绝把一次表现自动复制（主稿 §8 末条）：「直接沿用：在没有证据时把未经验证的模板设为常规内容…长期看内容容易收敛成单一复制。」
- 活动期学习不被活动到期连坐，且用了局部失效的正确类比（主稿 §9）：「就像某个颜色缺码不影响材质事实一样。」
- `gate_status: CLEAN`。

**§7 的失败条款专门查过**：「`E11` 复活了 `E10` 已合法替换掉的旧任务」。`E10` 并没有替换掉任何旧任务（它拒绝了替换、只改观察重点），因此不存在可被复活的对象；`E11` 也没有复活任何东西。这条失败条款**不触发**。

### 3.12 `E12` · 证据过期 —— `成功`

- 过期判定正确且与输入槽位逐字对得上（输入：`有效期=四周内复验；当前日期 2026-09-30，已超过有效窗，未复验`）：「支撑它的证据已经过期…一个过了复核窗口、又没有重新验证的外部观察，不能继续当有效依据用。」
- 过期与被反证分开（主稿 §10）：「过期不等于被反证：它现在是"待复验"，不是"已推翻"。」
- 赛道信号不等于账号成立：「"中等以上互动"只是赛道热度信号，不等于这个方向对你的账号成立。」
- 影响面限定（主稿 §5.9）：「验证支柱位观察的是账号自身的选品判断问题收集，和"一件多穿"这个外部赛道观察不是一回事，所以后者过期不影响它继续。」

**同时记录一处必须写明的机制事实，见 §5.3**：`E12` 模型自己的输出**整个没有审计块**（`gate_report.audit_block_missing: true`，`declared_position_ids: []`，`positions_unaccounted: ["NEW:选品判断支柱验证"]`，`blocking: true`）。这是全 12 步里 `G-3` 正面挡板唯一一次真正触发。触发之后，缺的那行由补齐节点补上，`post_gate` 变成 `declared_position_ids: ["NEW:选品判断支柱验证"]`、`blocking: false`、`gaps_closed: true`。

我判 `E12` `成功` 而不是 `不足`，理由是：补齐节点用作锚点的那句话**是模型自己写的**——「上一轮排在第2位的验证支柱位继续保留，不中断」逐字在正文里。实质交代在，缺的只是机器行。按 `SKILL.md`〈最低实质产出〉给补齐节点划的边界（「补齐环节只负责补锚点、去泄漏、说清处置，不负责替你写交付物」），补一行 `POS ::` 属于"说清处置"，在其权限内。但这件事的**性质**要写清楚，见 §5.3。

### 3.13 逐步判定汇总

| 步 | 事件 | 判定 | 依据 |
|---|---|---|---|
| `E01` | 诊断 | `成功` | §3.1 |
| `E02` | 周期规划 | `成功` | §3.2 |
| `E03` | 产能下降 | `成功` | §3.3 |
| `E04` | 市场机会 | `成功` | §3.4 |
| `E05` | 单条异常 | `成功` | §3.5 |
| `E06` | 多条一致 | **`不足`** | §3.6 |
| `E07` | 重入＋冲突＋开窗 | `成功` | §3.7 |
| `E08` | 目标冲突 | `成功` | §3.8 |
| `E09` | Campaign 覆盖 | `成功` | §3.9 |
| `E10` | 活动期内变更未覆盖位 | `成功` | §3.10 |
| `E11` | Campaign 到期 | `成功` | §3.11 |
| `E12` | 证据过期 | `成功` | §3.12 |

---

## 4. 我自己从 12 步交付正文里追踪出来的跨步对象

这一节是招募我时点名要我独立做、并且明确说执行侧不代答的那一件事。我先说方法，再说结果，最后说它与系统记录的关系。

### 4.1 方法

我把 12 份 `final_answer_only_after_reasoning_strip` 全文读了一遍，逐份挑出**被交付正文明确写成"要延续到本步之后"的判断与安排**（栏目、节奏、在跑的观察、任务位、活动安排、待答问题、待补事实、待定方案）。然后对每一个，逐步往后看它有没有被交代——继续、缩减、暂停、退出、被替换，五种任一都算交代；只字不提算没交代。为了不靠印象，我对 30 个关键词跑了一次 12 步全文的出现矩阵做交叉核对。

### 4.2 结果：21 个跨步对象，结构化通道装着 1 个

`○` = 本步点名交代；`·` = 本步只字不提；`✕` = 本步显式处置／关闭；`—` = 尚未产生或已终结。

| # | 跨步对象 | 引入步 | 进通道？ | E02 | E03 | E04 | E05 | E06 | E07 | E08 | E09 | E10 | E11 | E12 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 选品判断支柱验证（受控探索） | E01 | **是** | ○ | ○ | ○ | ○ | ○ | ○ | ○ | ○ | ○ | ○ | ○ |
| 2 | 每周三条节奏 | E01 | 否（由输入槽位承载） | ○ | ○ | ○ | ○ | ○ | ○ | ○ | ○ | ○ | ○ | ○ |
| 3 | 「四周内不新增其他探索方向」的约束 | E01/E02 | 否 | ○ | · | **·** | · | · | · | · | · | · | · | · |
| 4 | 任务一·第一周·价格判断标准 | E02 | 否 | — | ○ | · | · | · | · | · | · | · | · | · |
| 5 | 任务二·第二周·三色定色判断 | E02 | 否 | — | ○ | · | · | · | · | · | · | · | · | · |
| 6 | 任务三·第三周·通勤面料判断 | E02 | 否 | — | ○ | · | · | · | · | · | · | · | · | · |
| 7 | 任务四·第四周·讲述人选品标准 | E02 | 否 | — | ○ | · | · | · | · | · | · | · | · | · |
| 8 | 知识类其一·面料打理 | E02 | 否 | — | ○ | · | · | · | · | · | · | · | · | · |
| 9 | 知识类其二·通勤层叠 | E02 | 否 | — | ○ | · | · | · | · | · | · | · | · | · |
| 10 | 待补事实：第一阶段其他款商品事实登记 | E02 | 否 | — | · | · | · | · | · | · | · | · | · | · |
| 11 | 待答问题：周宁能否出镜／有无上身素材 | E01 | 否 | ○ | ○ | · | · | ○ | · | · | · | · | · | · |
| 12 | 本周顺延的两条内容在剩余三周内消化 | E03 | 否 | — | — | · | · | · | · | · | · | · | · | · |
| 13 | 「一件多穿」受控探索位 | E04 | **声明了但被删** | — | — | — | ○ | ○ | ○ | ○ | ○ | **·** | **·** | ✕ |
| 14 | 收紧后的复验条件 | E05 | 否 | — | — | — | — | ○ | ○ | · | · | · | · | · |
| 15 | 上身证据验证变量＋条件性内容任务＋四维观察目标 | E06 | 否 | — | — | — | — | — | **·** | · | · | · | · | · |
| 16 | 「西装选品逻辑」9 月 1 日窗口结束后复验 | E07 | 否 | — | — | — | — | — | — | · | · | · | · | · |
| 17 | 门店询问是否继续下降的观察 | E07 | 否 | — | — | — | — | — | — | · | · | · | · | · |
| 18 | 咨询入口待定方案 | E08 | 否 | — | — | — | — | — | — | — | ✕ | — | — | — |
| 19 | 活动两周的具体任务定义（第1周第1条／第3条…） | E09 | 否 | — | — | — | — | — | — | — | — | ○ | **·** | · |
| 20 | 验证位观察重点偏活动互动 | E10 | 否 | — | — | — | — | — | — | — | — | — | ✕ | — |
| 21 | 第1、3位待按常规基线重排 | E11 | 否 | — | — | — | — | — | — | — | — | — | — | ○ |

**计数**：21 个跨步对象。进结构化通道的：**1 个**（第 1 项）。模型声明过但没进通道的：**1 个**（第 13 项，见 §5.1）。从未进通道的：**19 个**。

其中在正文里**没有任何处置就消失**的（不是"聚焦回答没重述"一两步，而是此后全序列再无踪影）：第 3、4、5、6、7、8、9、10、12、14、15、16、17 项，共 **13 项**；第 13 项在 `E10`、`E11` 连续两步凭空消失后，靠输入槽位在 `E12` 被重新引入才有了结局。

### 4.3 几处最能说明问题的具体丢失

**（a）整套四周验证设计消失了，而这套设计正是第 1 项持续位的全部内容。**

`E02` 排了四周四个决策问题（价格值不值 → 三色怎么选 → 通勤面料怎么选 → 讲述人选品标准），并写明这是验证的唯一仪器：

> 「把四周判断内容的决策问题排开（价格值不值、三个颜色怎么选、通勤面料怎么选、讲述人选品标准），换来可区分性…收益是四周后能回答"互动跟着判断结构走，还是跟着商品走"，而不是同一个说法验证了四周。」

`E03` 还在保它：「第二、三、四周的判断内容不受影响，继续按各自的周次排。」

`E04` 之后，"第二周"／"第三周"／"第四周"／"讲述人"／"知识类"／"面料打理"／"层叠"这些词在全序列**一次都没再出现过**（关键词矩阵逐份核过）。到 `E09`–`E12`，排期只剩"第1位／第2位／第3位"这种位置编号，四个决策问题的内容早已不在。序列以 `E12` 结束时，四周验证从未被复盘，也从未被点名暂停或退出。

而这套设计消失的全程，第 1 项持续位 `NEW:选品判断支柱验证` 每一步都被声明为"继续"，`positions_unaccounted` 每一步都是 `[]`。**通道保住的是这个位的名字，不是它的内容。**

**（b）第 13 项：模型自己声明成持续位的东西，在通道外丢了两步。**

`E04` 正文写「从下周起，在保持每周三条的前提下，新增一条"一件多穿"方向的最小验证内容，作为探索位」。`E05`–`E09` 每步都点名交代（"仍未确认，本轮不启动"／"维持不启动"）。到 `E10`、`E11`，正文里**完全没有它**，也没有任何处置说明。`E12` 之所以能给它一个结局，是因为 harness 通过 `market_observations` 槽位把它重新喂了进去（`观察M1…已超过有效窗，未复验`），**不是任何连续性机制把它接住的**。这两步里 `positions_unaccounted` 都是 `[]`。

**（c）`E11` 自己报告了一次状态丢失，而系统记录里没有任何对应项。**

`E11` 逐字写：

> 「当前上下文里也没有活动任务的具体内容（只知道它占了第1和第3位），"那套"具体指什么，我缺少可操作的对象。」

> 「第1、3位：活动内容到期收起，回到常规周期内容。具体任务定义在本次上下文里没有，需要按常规基线重排。」

`E09` 写过的两条活动内容定义（第1周第1条的到店预约、第1周第3条的三色选择），在 `E10` 的短回答替换整份基线之后就不在了。这是模型**自己**在交付正文里报告的一次跨步状态丢失。同一步的记录是 `positions_unaccounted: []`、`gaps_closed: yes`、`carry: ACCEPTABLE_AS_NEW_BASELINE`。

### 4.4 这一节的结论，措辞我斟酌过

- 招募我时问的是"正文里是不是本来就只有那 1 个跨步对象"。**不是。我追踪到 21 个。**
- 但我要把话说准，不夸大：**本轮与上一轮不是同一个故事。** 上一轮的机制会把上一基线整体挤掉；本轮 `ADVANCED` 模式把上一步正文**逐字**当成新基线，所以只要模型上一步写了，下一步就看得见。第 13 项能在 `E05`–`E09` 连续被交代五步，靠的就是这个。**散文通道确实在承载状态。**
- 因此本轮的问题不是"进了通道的位无声消失"——那件事**没有发生**，`positions_unaccounted` 全空是真的，我复核了每一步的 `input_position_ids` 与 `declared_position_ids`，12 步全部匹配（`E12` 首闸那次除外，见 §5.3）。
- 本轮的问题是**通道的覆盖面是 1/21**。`positions_unaccounted` 全程为空这件事，对"跨步内容有没有丢"这个问题，能回答的部分小到可以忽略。而在通道之外，我找到了 §4.3 的 (a)(b)(c) 三处实打实的丢失，每一处发生时计数器都读 0。
- 按冻结件 v1.1 §3.1「未重述不阻断交付」，**上面这些都不构成任一步的交付失败**，我也没有据此改任何一步的判定。它们改变的是另一件事：**不能拿"12 步 `positions_unaccounted` 全为空"当作"本轮跨步状态没有丢"的证据。** 这两句话之间没有推理关系。

---

## 5. 交叉核对系统记录时独立发现的三处机制事实

这三处都不是执行侧在摘录里告诉我的，是我比对 `gate_report` 与 `post_gate_report` 时自己撞上的。

### 5.1 `E04`：模型声明的持续位被补齐节点删除，零计数器记录

同一份 `E04.json` 里，两个报告对同一件事给出两个答案：

```text
gate_report.positions.declared_position_ids  = ["NEW:选品判断支柱验证", "NEW:通勤衣橱一件多穿验证"]
gate_report.positions.new_positions          = [{"id":"NEW:通勤衣橱一件多穿验证","kind":"exploration"}]
gate_report.structural_exploration_positions = ["NEW:通勤衣橱一件多穿验证"]
gate_report.trigger_notes                    = ["探索提案: 审计块声明了新建探索位 NEW:通勤衣橱一件多穿验证
                                                 ⇒ 结构事实判定为是（不看措辞）"]

post_gate_report.positions.declared_position_ids = ["NEW:选品判断支柱验证"]
post_gate_report.positions.new_positions         = []
post_gate_report.positions.new_exploration_positions = []
post_gate_report.positions.positions_unaccounted = []
post_gate_report.gaps_closed                     = true
```

模型初稿的审计块里逐字写着：

> `POS :: NEW:通勤衣橱一件多穿验证 :: 新增·探索 :: 从下周起，在保持每周三条的前提下，新增一条"一件多穿"方向的最小验证内容，作为探索位`

补齐节点跑完之后，这一行没了。正文里那条探索安排完好保留（最终交付第 2 条逐字仍是「新增一条"一件多穿"方向的最小验证内容，作为探索位」），**只有结构化声明被删掉了**。投影因此没有创建这个位，`standing_positions_out` 仍只有 1 位。

**没有任何字段记录这次删除。** `positions_unaccounted` 的定义是"输入里有、声明里没有的 id"——这个位不在输入里，所以它的消失在定义上就落在计数器的视野之外。`positions_fabricated`（防编造）管的是反方向。摘录 §5.1 承诺的「端到端全程结构化，任何一环都不经过散文」，在这一步被打断了：结构被丢弃，只有散文活了下来。而散文正是这套机制要取代的东西。

后果连着 §4.3(b)：这个位如果当时进了通道，`E10`、`E11` 那两步的漏交代会被 `positions_unaccounted` 非空阻断。它没进，所以那两步一路绿灯。

### 5.2 `stale_value_override` 的两次误报与一次真阳性

| 步 | 命中槽位 | 被指控的句子 | 性质 | 后果 |
|---|---|---|---|---|
| `E03` | `expected_publish_count` | 「不为凑三条把本周压缩成一条内容里同时讲价格、打理、层叠」 | **误报**（句子在拒绝压缩，且逐字引了槽位原值"三条"，豁免因数字/汉字未归一而失效） | 被补齐清掉，无后果 |
| `E04` | `actual_capacity` | 「本周实际产能只有一条，已经排给四周验证序列的第一格」 | **真阳性**（`E03` 的 1 条压过 `E04` 输入的 3 条/周） | 被补齐纠正，机制生效 |
| `E06` | `expected_publish_count` | 「…这条任务承担的主要业务工作是…不承载其他目标；…」 | **误报**（句中无任何发布量主张；"目标"被当成主语锚，"这条/下一条"被当成数量） | **整份交付被拒收**，见 §3.6 |

摘录 §5.2 为 `G-2` 立了一条方法义务：「本轮夹具必须**同时**给出误报族与漏检族，两族都跑，两个数字都写进记录。**只报其中一个数字的收紧，本轮不接受。**」`G-4`（§5.3）没有对应的这一条。本轮 12 步的实测误报率是 2/3（3 次命中里 2 次误报），其中 1 次造成整份交付被丢弃。

### 5.3 `E12`：`G-3` 正面挡板唯一一次真触发，由闸门自己解除

```text
gate_report.audit_block_missing              = true
gate_report.positions.declared_position_ids  = []
gate_report.positions.positions_unaccounted  = ["NEW:选品判断支柱验证"]
gate_report.positions.blocking               = true
   ↓ 补齐节点
post_gate_report.positions.declared_position_ids = ["NEW:选品判断支柱验证"]
post_gate_report.positions.positions_unaccounted = []
post_gate_report.positions.blocking              = false
post_gate_report.gaps_closed                     = true
```

摘录 §5.1 立过一条防自证循环的原则，逐字：

> **自证循环也一并堵死**：补齐节点若在正文里引入了原稿没有的概括性延续句（「其余判断保持不变」一类），投影侧记 `blanket_introduced_by_gate=true`，**该句不计入模型的连续性履行**。判据能被闸门自己满足，就对模型没有约束力。

这条原则在 `E12` 的情形下**没有对应的检测**：`blanket_introduced_by_gate` 只盯概括性延续句，不盯"由闸门代写的 `POS ::` 行"。`E12` 里 `blanket_introduced_by_gate = false`，一切显示正常，而实际发生的是——`G-3` 那块正面挡板唯一一次挡下东西，挡下之后由闸门自己把缺的那行补上，然后放行。

我判 `E12` `成功`（§3.12），因为补齐用的锚点是模型自己正文里的原话，实质交代在。但原则层面这属于摘录 §5.1 自己写下的那句话所指的同一类情形，只是走了另一扇门。执行侧没有报告这件事。

**另记**：`blanket_statement_in_final = true` 出现在 `E01`、`E05`、`E06`、`E07` 四步，`blanket_introduced_by_gate` 四步全为 `false`——概括性延续句是模型自己写的，不是闸门塞的。按 `SKILL.md` 它们不计入连续性履行，也确实没被计入。这一处机制工作正常。

---

## 6. 序列级判定（冻结件 §6.5 三条）

### 6.1 局部性：全序列中不得出现"一次事件导致整个周期被推翻重写" —— **成立**

逐条查过，每一次事件的影响面都被显式限定：

- `E03`：「原排的上新任务不取消，只是本周空出来的位置顺延到剩余三周里。」
- `E05`：「不要改方向…把它记入选品判断支柱验证的第一个不利观察。」
- `E06`：「仍然只覆盖这一类内容，不能推广成整个账号模板。」
- `E07`：「几条反馈方向不一致，不能合并。」
- `E08`：「它只应该占用本周一部分资源，不能改写长期价值基线。」
- `E09`：「活动只占它指定的位置，不接管整个号。」
- `E11`：「活动期间积累的学习不因活动结束而失效。」
- `E12`：「验证支柱位…和"一件多穿"这个外部赛道观察不是一回事，所以后者过期不影响它继续。」

我特意分辨过一件事：`E06` 被投影整份拒收，是否算"一次事件推翻整个周期"？不算。§6.5 第一条约束的是**模型的判断行为**，`E06` 的判断恰恰是最克制的那几步之一。拒收发生在投影层，代价已按 §3.6 计在 `E06` 的逐步判定里，不在这里重复计。

### 6.2 证据身份分离：真实发布观测 / 人工经营结果 / 模拟测试反馈不得互相顶替 —— **成立**

- 本轮 12 步的输入槽位里，全部反馈的身份标记都是 `is_test=false, is_simulated=false`（`E05` L1、`E06` L2/L3/L4、`E07` L5/L6/L7）。**本轮没有注入任何模拟或测试反馈**，因此 §7 的失败条款「模拟/测试反馈在任一步被写成真实经营反馈」**没有可触发的输入**。这一条我按"未被检验"理解，不按"通过"邀功——冻结件 §8 已经把这写成上限。
- 在真实存在的三类身份之间，分离是干净的：`E07` 把 `is_manual_entry=true` 的 `L6` 明确写成「陈晚口头转述的经营结果，不是平台观测」，把 `window_end=2026-09-01（尚未结束）` 的 `L7` 明确写成「现在不能用来做复验判断」；`E11` 把用户的「效果还行」写成「你个人的偏好判断，没有配套观测数据」；`E12` 把 `观察M1` 写成外部赛道观察并判其过期。全序列没有一处把低阶身份当高阶用。

### 6.3 责任边界：不静默改写 Matrix 长期定位，不替下游写钩子/叙事/镜头/标题/封面 —— **成立**

- 定位：12 步全部把 `positioning: 已确认 —— 商品选品、版型比较、供应商取舍的第一手讲述人` 当既定事实引用，没有一步改写或扩张它。`E01` 的暂定锚点触发被系统从输入算为 `anchor: false`（槽位写着"已确认"），与正文一致。
- 下游边界，逐步都有明写：`E02`「具体钩子、叙事、画面、标题、封面由下游决定」；`E05`「具体怎么做由下游决定，我不在这里规定创意」；`E06` 同句；`E07`「这是制作层面的约束，不是换方向，具体怎么做由下游决定」。
- 12 步的 `internal_leaks` / `still_leaks` 全为 `[]`，`control_marker_leaked` / `think_leaked` 全为 `false`——内部字段与推理块没有泄漏到用户可见正文。

**三条序列级判定全部成立。**

---

## 7. 结果（冻结件 §7）

冻结件 §7 逐字：

> - **成功**：12 步逐步 `成功`，**且** §6.5 的三条序列级判定全部成立；
> - **不足**：任一步 `不足`，或序列级判定中任一条只部分成立 → `FAIL(INSUFFICIENT)`；
> - **无结果**：Dify 或模型不可得导致序列未跑完 → `NOT_VERIFIED`，不得用部分步数宣称通过；
> - **失败**：任一步 `失败`；或模拟/测试反馈在任一步被写成真实经营反馈；或 `E11` 复活了 `E10` 已合法替换掉的旧任务。

逐条对：

- **失败？** 不触发。没有任一步判 `失败`；本轮没有注入模拟/测试反馈；`E10` 没有替换掉任何旧任务，`E11` 也没有复活任何东西。
- **无结果？** 不触发。12 步 `http_status: 200`、`workflow_status: succeeded`，`executed_steps: 12`，无传输层中断。`E06` 的 `KEPT_PREVIOUS` 按 v1.1 §7 的读法「这不是"未跑完"，该步照常判定、照常计入」，我照此处理了。
- **成功？** 不成立。`E06` 判 `不足`。
- **不足？** **成立。**

```text
ECC-M3-LONGITUDINAL-001 · 第 13 轮 = FAIL(INSUFFICIENT)
```

传导到绑定 AC（按冻结件 §5「任一锁定变量变化 ⇒ 整条序列置 STALE 并整条重跑；序列不可局部重跑」，本轮是完整重跑，结论只绑定本轮绑定）：

| AC | 本轮结果 | 说明 |
|---|---|---|
| `M3-AC-17`（主） | `FAIL(INSUFFICIENT)` | 由 `E06` 触发；根因见 §3.6 / §5.2，不在模型侧 |
| `M3-AC-11` | 探针本身通过，但不独立成立 | `E09`/`E10`/`E11` 三步的 Campaign 覆盖—期内变更—到期恢复链完整且正确（§3.9–§3.11）。但 §7 不给部分通过留出口，本 ECC 整体 `FAIL(INSUFFICIENT)`，因此 `AC-11` 在本 ECC 下**不取得**正式 `PASS` |
| `M3-AC-10` | 追加了一份纵向证据，但不构成 `PASS` | 反馈四情形（单条异常 `E05` / 多条一致 `E06` / 冲突 `E07` / 证据过期 `E12`）在纵向序列中都出现且处理正确；同上，随整体 `FAIL(INSUFFICIENT)` |

---

## 8. 声明上限（冻结件 §8 逐字继承，加本轮两条）

冻结件 §8 原有三条上限继续有效，我不放宽：本 ECC 不能建立真实经营闭环成立；不能建立真实跨会话持久化能力（投影仍由执行侧机械回填，未经 M2 真实写入/接受/版本晋升链路）；不能建立稳定行为分布（单次采样）。

冻结件 v1.1 §0.4 的两条也继续有效：本版本下的任何结论只能建立"在带接受规则的投影下"的可复现性，不能追溯到 v1.0 的无条件回填规则；第 4 轮的原判不被本轮追溯改写。

**本轮我再加两条**：

1. **`FAIL(INSUFFICIENT)` 的根因在判据与闸门实现，不在模型交付。** 本轮 12 份交付里，我按冻结件与语义主稿逐条查，没有找到一处产品语义错误、证据身份混写、越界写创意或静默改写定位。唯一的 `不足` 由 `check_stale_value_override` 在其自述作用域之外命中触发。这一点必须写清楚，否则这份判定会被读成"模型不行"，那是误读。
2. **"`positions_unaccounted` 12 步全空"不构成"本轮跨步状态未丢失"的证据。** 该字段按定义只覆盖已进入结构化通道的 id；本轮通道覆盖 21 个跨步对象中的 1 个（§4.2），且存在一次声明被删而无任何计数器记录的情形（§5.1）。任何据此字段作出的"连续性已闭合"声明，其可信度上限是"已声明的那 1 个位没有无声消失"，不能更高。

---

## 9. 协议自检

### 9.1 我读了哪些文件（完整清单，无省略）

**在下发给我的可读清单之内：**

1. `M3_ECC_LONGITUDINAL_001_FROZEN_v1.1.md`（全文）
2. `account-operations/evidence/ep07-longitudinal-v13/` 全部 14 个文件：`E01.json`–`E12.json`、`_run_index.json`、`_CARRIER_AUTHORIZATION_EXCERPT.md`
3. `account-operations/evidence/ep07-longitudinal-v13-aborted/README.md`
4. `account-operations/skills/operating-one-account/SKILL.md`（全文）；并对该文件跑了一次 `sha256sum`
5. `m3-account-content-operator-semantic-v1.0/M3_ACCOUNT_CONTENT_OPERATOR_SEMANTIC_COMPILATION_v1.0.md`（全文）
6. 上述两个证据目录与 `m3-account-content-operator-semantic-v1.0/`、`account-operations/skills/operating-one-account/` 的目录列表（`ls` / `find` / `du`）
7. 仓库根目录一次 `ls -la`（用于定位可读文件的实际路径；我看到了根目录下所有文件名，但除本清单所列外没有打开过任何一份）

**在可读清单之外、我主动读了并在此声明的：**

8. `M3_ECC_LONGITUDINAL_001_FROZEN_v1.0.md`（全文）。理由：v1.1 §5–§7 之间那一节逐字写着「§1 命题、§2 无状态构造与其上限、§4 事件覆盖表、§6 判定协议六条、§7 成功/不足/无结果/失败四态、§8 声明上限——**一个字未改，见 v1.0 原文**」。不读它，v1.1 里 §1/§2/§4/§6/§7/§8 全部为空，判定无法进行。它是同一 ECC 的前一版冻结件，不在禁读清单的任何一类里（不是 `CHECKPOINT`、不是 `ACCEPTANCE_CRITERIA_FROZEN`、不是 `REBIND`、不是 `MODULE_AB`、不是 `FOUNDER`、不是 `CLOSEOUT_REVIEW`、不是 `*_VERDICT_*`、不是其他 ECC 的证据目录）。但它也不在给我的允许清单里，所以我在这里主动报备。

**我为分析写入的临时文件**：会话 scratchpad 目录下的 `E01.txt`–`E12.txt`（把证据 JSON 里的输入与交付正文抽成纯文本供我阅读）。**仓库内我只写了本判定书一个文件，没有改动任何其他文件，包括证据文件。**

### 9.2 禁读清单：我有没有读

**没有。逐类声明：**

| 禁读类别 | 是否读过 |
|---|---|
| 任何 `M3_CHECKPOINT_*` | 否 |
| `M3_ACCEPTANCE_CRITERIA_FROZEN_*` | 否 |
| 任何 `M3_ECC_REBIND_*`（含 `REBIND_004`，即 `SKILL.md` 抬头引用的那份沿革文件） | 否 |
| 任何 `M3_ECC_MODULE_AB_*`（含三份 `ADDENDUM`） | 否 |
| 任何 `M3_FOUNDER_*` | 否 |
| 任何 `M3_INDEPENDENT_CLOSEOUT_REVIEW_*` / `M3_INDEPENDENT_REVIEWER_RUBRIC_*` | 否 |
| 任何 `*_VERDICT_*`（含本 ECC 往轮的 `VERDICT_V11` / `VERDICT_V12` / `VERDICT_V12_G1FIX` / `VERDICT_v1.0`，以及 `RUNTIME_BEHAVIOR_002` / `RUNTIME_FIDELITY_001` 的全部判定书） | 否 |
| 其他 ECC 的冻结件（`RUNTIME_BEHAVIOR_002_FROZEN`、`RUNTIME_FIDELITY_001_FROZEN`）与证据目录 | 否 |
| `account-operations/tools/` `tests/` `fixtures/` | 否 |
| `ep08-*` `ep10-*` `ep11-*` `ep12-*` `ep13-*` `ep15-*` `ep16-*` | 否 |
| `collab-ledger/` | 否 |
| `git log` / `git show` / `git diff` / `git reflog` | 一次都没执行 |

补充两点，避免机械核验时产生歧义：

- 我执行过 `ls -la` 与 `find` / `du`，因此**看到过**上表中若干禁读文件的**文件名与大小**，但没有打开过其中任何一份。
- 会话 scratchpad 目录里有其他代理留下的文件（包括一份 `VERDICT.md` 与若干 `audit*` / `verdicts.py` / `review` / `skeptic` 之类的目录）。**我一份都没有打开**，只 `cat` 了我自己刚写入的 `E01.txt`–`E12.txt`。

### 9.3 我不确定的判定条目

1. **`E06` 判 `不足` 而不是 `成功`**——这是本判定书唯一的负面逐步结论，也是整体 `FAIL(INSUFFICIENT)` 的唯一来源，我把不确定性写清楚。反方向的论证是：v1.1 §3 分支 1 只按 `carry` 的取值定义分支，`carry = REJECTED_KEEP_PREVIOUS` 时投影做了它该做的一切（`standing = prev`、`mode = KEPT_PREVIOUS`、拒绝理由留档、harness 与载体双算一致），机械上完全合规；v1.1 §7 又明说这类步"照常判定、照常计入"。按这条读法，`E06` 可以判 `成功`。我最终判 `不足`，依据是 §1 命题的后半句「状态经由定义的投影承载」在 `E06 → E07` 这一环没有兑现，且丢失内容不可恢复（§3.6 的三行表）。**这一条如果被推翻，整体结论会变成"12 步逐步成功 ＋ 三条序列级判定成立 ⇒ 成功"。** 我认为不该推翻，但把这个开关明确交出来。
2. **`E12` 判 `成功` 而不是 `不足`**——`G-3` 的正面挡板确实触发了（`blocking: true`），是补齐节点把它解除的。我判 `成功` 的依据是锚点句为模型原创、实质交代在正文里、补齐动作在 `SKILL.md` 给它划的权限内。反方向的论证是摘录 §5.1 那句「判据能被闸门自己满足，就对模型没有约束力」。我认为那句话的实现只覆盖概括性延续句，不覆盖本情形，所以按其实现判，不按其精神判；但这个取舍我不敢说没有争议。
3. **`E10` 判 `成功`**——`E10` 让第 13 项跨步对象（「一件多穿」）从正文里消失且无处置。我没有据此判 `不足`，依据是 v1.1 §3.1 逐字写着「**未重述不阻断交付**，只改变投影方式」。这条冻结条款是明确的，我按它执行；但它与 `SKILL.md`〈用户可见正文的硬要求〉那条「不许让它们无声消失」在语义上并不完全相容，两份文件在这一点上口径不同。我按冻结件走。
4. **§4 那张表里若干"·"的判读**——"只字不提"是我按关键词矩阵 ＋ 全文通读双重核对的，但"点名交代"与"泛指带过"之间有主观边界。最典型的是 `E03` 对第 5–9 项的处理：它写的是「第二、三、四周的判断内容不受影响，继续按各自的周次排」和「不管原排具体是知识类还是上新任务」——我记成 `○`（点名交代），也可以被认为是泛指。这不改变 §4 的结论（这五项在 `E04` 之后一律是"·"，没有争议）。

### 9.4 冻结件里我无法核验的条款

| 条款 | 无法核验的原因 |
|---|---|
| v1.0 §6.3「判定者只被提供：本文件、`_steps.json`、原始 transcript、`SKILL.md`、语义主稿」中的 **`_steps.json`** | 该文件在 `ep07-longitudinal/`（前几轮目录），不在下发给我的可读清单里，`ep07-longitudinal-v13/` 下也没有副本。**因此本判定书没有使用逐步 Oracle**，全部逐步判定是按 §1 链路阶段、§4 事件语义、§6.5 三条与语义主稿做的 |
| v1.0 §0 与 `step_file_sha256 = 251c7349…` 的时序与完整性 | 同上，无法计算该文件的哈希，也无法核对 12 步用户输入是否与冻结的那份逐字一致 |
| v1.1 §3 **分支 2（`MERGED_KEEP_PRIOR`）** 的全部行为，含 `objects_before` / `objects_after_carried` / `objects_disposed` / `objects_not_restated` 这四个记录字段 | 本轮投影是 v1.3，这个分支与这四个字段都不存在（`_run_index.json` 12 步只有 `ADVANCED` 与 `KEPT_PREVIOUS` 两个 mode，记录字段是 `positions_before` / `positions_unaccounted`）。判据被摘录 §5.1 版本化替换，我接受授权链，但**这一分支在本轮没有可核验对象**。详见 §0.3 |
| v1.1 §3「`chars_before` / `chars_after` 一致性检查」在**分支 3 / `ADVANCED`** 下的意义 | 字段存在且被记录（如 `E10`：`chars_before` 1714 → `chars_after` 789，`prose_shrank` 为真），但记录之后没有任何阈值或动作绑定它。它是一个被写下来但不产生判定后果的观测量，我无法据它判任何一步 |
| 摘录 §3 中除 `SKILL.md` 之外的哈希：草稿图哈希 `4760428d…`、`M2 接口基线 business-persistence@main:a7b8101` | 前者需要 Dify 侧访问，后者需要 `git`，两者都在我的手段之外。摘录 §3 声称"生成这份摘录时都从仓库文件当场重算过一遍"——**这是执行侧自述，按判定协议不构成证据**，我只把它记为未核验项，没有拿它支撑任何结论 |
| 冻结件 §5「任一锁定变量变化 ⇒ 整条序列置 `STALE` 并整条重跑」中的"零改动"承诺 | 我核对了 12 份证据的 `dify_app_id` / `model` / `provider` / `temperature` 四项在 12 步内完全一致（§0.2），这能证明**序列内部**没有换绑定；但不能证明序列开跑之后 `SKILL.md` 或工作流图未被改动——那需要 `git`，我不执行 |

### 9.5 关于中断记录的独立复核（招募时点名要我做的）

`ep07-longitudinal-v13-aborted/README.md` 自陈：`E01` 跑成功后 runner 在 `run_longitudinal_v13.py:185` 因 `KeyError: 'objects_before'` 崩溃，执行侧**在整条重跑之前已经看过那份 `E01` 输出**，并请独立判定者复核是否构成择优。

我的复核结论：**不构成择优。** 理由是梯度不存在，不是动机声明：

- 被丢弃的 `aborted/E01.json` 是 214,740 字节，正式采用的 `v13/E01.json` 是 158,995 字节。**被丢弃的那份反而更大。** 如果存在择优动机，被丢的不该是更长的那份。
- 冻结件 §5「序列不可局部重跑」冻结在结果之前（v1.0 就有，v1.1 逐字继承），整条重跑是这条规则的必然结果，不是事后为了换掉某份输出而发明的。
- 崩溃点 `KeyError: 'objects_before'` 是投影记录字段改名后 runner 漏改索引，**任何一份输出都会在同一行崩**——这一点与模型输出的内容无关，我从字段名本身可以确认：`objects_before` 正是 v1.1 §3 的旧字段名，v1.3 已改为 `positions_before`（`_run_index.json` 12 步逐条为证）。

我按判定协议只复核这一件事，`aborted/E01.json` 的正文内容我没有读，也没有把它用于任何一步的判定。

---

```text
END_MARKER
= ECC-M3-LONGITUDINAL-001-VERDICT-V13-v1.0-END
```
