# SINGLE_ACCOUNT_CONTENT_PLANNER_PRD v0.1

> **本文件只登记一个已确认的能力缺口，不启动构建。**
>
> ```text
> Decision ID：PRD-DECISION-SINGLE-ACCOUNT-PLANNER-REGISTERED-V1
> 状态：REGISTERED_NOT_STARTED
> 登记日期：2026-08-21
> 启动条件：由 Founder 在需要时显式启动，不由执行侧自行开工
> ```
>
> 本文件是治理文件，不是模型输入。
> 它不进入 Skill、不进入 Dify System Prompt、不进入 User Prompt、不进入任何固定运行输入。
>
> 受 [`CONTENT_PRODUCTION_CHAIN_PRD_v0.1.md`](CONTENT_PRODUCTION_CHAIN_PRD_v0.1.md) 第 2 节
> 轻文档化与轻治理化裁决约束：本文件刻意只写「不写下来就会忘记或做错」的部分，
> 不预先设计字段、状态与流程。

## 1. 缺口一句话

当前系统有**矩阵设计能力**和**Campaign 协同能力**，
但没有**单账号持续经营与内容机会发现能力**。

现实中企业的大多数日常内容，不是每次启动一场多账号 Campaign，
而是某个账号基于当天真实发生的事情，独立生产一条有价值的内容。

以下问题今天在系统里**没有承接者**：

- 今天周宁账号应该发什么？
- 苏禾账号上传了一段真实试穿视频，能做成什么内容？
- 林序账号最近有哪些真实事实值得表达？
- 陈晚账号只基于最近门店问题，应该策划哪三条内容？

## 2. 缺口是硬编码的，不只是概念空白

以下为 2026-08-21 在 `main`（`727c744`）上核验到的事实。
**相关文件不在本分支上**，引用时只写文件名，不做链接。

| 位置 | 事实 |
|---|---|
| `V1_DEMO_INTEGRATION_CONTRACT_v0.1.md` 第 5.5 节 | 「Content Brief 只能消费 `USER_ACCEPTED` 的 Campaign」 |
| 同文件第 1 节 | 三份 Skill 使用**固定分支，映射写死** |
| `DIYU_DEMO_V1_MAIN_CHATFLOW_v0.1.yml` | 路由只有三条：`matrix` / `campaign` / `content_brief` |
| 同文件影子节点口径 | `requested_skill` 枚举只有 `MATRIX` / `CAMPAIGN` / `CONTENT_BRIEF` / `NONE` |

结论：「周宁这周发什么」进入系统后会被归为 `FOCUS`，
**没有任何可落的执行路由**。这不是遗漏，是当前架构不允许 Content Brief
有 Campaign 以外的上游。

## 3. 它不是 Campaign 的 N=1

两者的差别不在账号数量，而在**什么是给定的、什么是要搜索的**：

| | 给定 | 要搜索的 |
|---|---|---|
| Campaign | 经营任务 | 哪些账号、什么顺序、怎么接力 |
| Single-Account Planner | 账号 | 最近发生了什么值得讲、够不够独立成篇 |

Campaign 跑 N=1 会让它的整个输出面失效——
参战账号组合、接力关系、主承接人与替补、内容数量上限、核心内容顺序全部无意义——
**而「该讲什么」仍然没有被回答**，因为 Campaign 从来是继承任务，不搜索选题。

因此建议它作为**第四份决策类 Skill**，与 Campaign 形成**两条平行入口**，
不放在 Campaign 之下。

## 4. 真正缺的不只是 Skill，还有一份状态

现有三份 Skill **没有任何一环记得某个账号说过什么**。

如果只加 Skill 不加这份记忆，Planner 只能从责任卡与夹具重新推导，
会稳定地生成**看起来合理但互相重复**的选题——
而「与历史内容的差异」正是它必须回答的问题之一。

> **单账号持续经营能力的瓶颈是记忆，不是策划。**

### 4.1 一个能省掉大麻烦的区分

| | 是什么 | 是否被现行裁决延期 |
|---|---|---|
| **已发布内容清单** | 我们自己产出过哪些内容、归给哪个账号、讲过什么判断 | **未延期**——只是归档自有产物，与既有 RUN MANIFEST 同类做法 |
| 发布表现数据 | 播放、评论、涨粉、成交回流 | **已延期**，见 `PRD-DECISION-FEEDBACK-LEARNING-DEFERRED-V1` |

重复度判断只需要前者。
因此去掉「最近顾客评论与问题」一项后，本能力在当前边界内可以成立。

## 5. 它需要读取什么：账号经营视图

不能只读一张静态人设卡。以下为需要的输入，
并标出**今天是否已经存在**（据 2026-08-21 仓库状态）：

| 输入 | 今天是否已有 |
|---|---|
| 账号长期使命 | 有（账号责任卡 / 已接受 Matrix 产物） |
| 可信权威来源 | 有 |
| 可以表达与禁止表达的边界 | 有 |
| 账号关系姿态与语言习惯 | 有 |
| 可公开的商品、试穿、门店与经营事实 | 有（夹具） |
| 人员出镜与制作产能 | 有（资源夹具） |
| 当前平台与生产条件 | 有 |
| 最近真实经手的事件 | **无** |
| 最近上传的图片、视频与录音 | **无** |
| 已发布内容与重复度 | **无**（见第 4 节，可建） |
| 正在持续的内容系列 | **无**（可建） |
| 最近顾客评论与问题 | **无**（已延期，V1 不建） |

### 5.1 账号责任卡的挂载方式已有先例

`V1_DEMO_INTEGRATION_CONTRACT_v0.1.md` 第 6 节说明：
四张账号责任卡**不在** Campaign 与 Content Brief 的夹具包里，
因为该位置由**本轮已被用户接受的 Matrix 产物**占据。

这与「Matrix 应是低频初始化能力」一致：
Matrix 不必每条内容重跑，但它的**已接受产物是单账号策划的必需输入**。

## 6. 它应该输出什么

不是完整脚本，而是单账号内容决策：

```text
指定账号
为什么现在应该表达
主要顾客问题
本轮一手事实
账号新增判断
与历史内容的差异
内容来源模式
建议内容形式
制作资源建议
事实与授权边界
是否值得独立发布
是否需要升级为多账号 Campaign
```

接受后交给：`Content Brief → Creative Script → Production Director → Publishing & Packaging`。

最后一项「是否需要升级为多账号 Campaign」与 `C6_FOUNDER_CONFIRMED_v0.1.md`
第七节**返回最上游实际失效结论**的纪律方向一致：向上路由，不自行扩权。

## 7. 目标入口路由

| 用户情境 | 路由 |
|---|---|
| 「帮我设计总部账号矩阵」 | Matrix |
| 「这次上新四个账号怎么联动」 | Campaign |
| 「周宁这周发什么」 | Single-Account Planner |
| 「这段试衣视频能做什么」 | 素材理解 → Single-Account Planner |
| 「把刚才第三个方向做成一条视频」 | Content Brief |
| 「把这份 Brief 写成脚本」 | Creative Script |

Matrix 是**低频初始化与调整能力**，不是每生产一条内容都要重跑；
Campaign 只是**一种特定的内容组织方式**，不应成为所有内容生产的必经入口。

## 8. 启动时建议拆成三件事

不要打成一个包，否则会重演大设计。

| 拆分 | 是什么 | 依赖 |
|---|---|---|
| **A. 账号内容账本** | 记住每个账号讲过什么（最小形态：一份追加式清单） | 无，可最先做 |
| **B. Single-Account Planner** | 第四份决策 Skill，与 Campaign 平行 | 依赖 A 才有真正价值 |
| **C. 素材理解与账号匹配** | 多模态识别 ＋ 路由到合适账号 | 依赖 B |

试衣素材的完整处理（识别事件 → 匹配账号 → 查重 → 提炼独立判断 →
判断能否独立发布 → 生成单账号决策与纪实型 Brief → 进入制作）
跨了 C → B → Brief 三层。分开做，才不会为了 C 把 B 拖成大工程。

## 9. 启动前必须先裁决的四件事

这些是 Founder 决定，执行侧不得自行选择：

1. **排期**：插进当前 `P0`—`P9` 之前（推迟 `P1`—`P3`）、与其并行另开一条线、
   还是先只做 A；
2. **`CLAUDE.md` 第 3 节「当前唯一执行路径」与第 4 节「不得并行扩建外围系统」**
   是否随之调整——本能力属于该条明确禁止的范围，不调整就不能开工；
3. **`V1_DEMO_INTEGRATION_CONTRACT_v0.1.md` 第 5.5 节**是否新增
   「Content Brief 可消费已接受的 Single-Account 决策」这一平行分支，
   以及自然交互层是否新增第四条路由与 `requested_skill` 枚举值；
4. **本文件最终归属哪个分支**——它登记的是**决策链**缺口，
   却落在生产链分支 `feature/content-production-chain-v1` 上（当时唯一的隔离工作区）。

## 10. 本文件不做什么

- 不启动构建，不编写任何 `SKILL.md`，不创建任何 Dify 工作流；
- 不修改已落盘的三份决策 Skill、三份生产链 P0 文件、集成合同与自然交互层；
- 不定义字段、状态机、编号或接口——那些留到真正启动时按当时的真实需要决定；
- 不能证明该能力值得建设：本文件只记录缺口与代价，不代替 Founder 的启动裁决。

## 11. 登记声明

本文件在 `2026-08-21` 登记，状态 `REGISTERED_NOT_STARTED`。

在 Founder 显式启动之前：
不计入 `P0`—`P9` 任何阶段的验收，不作为任何阶段的门禁，不阻塞现有路径。
