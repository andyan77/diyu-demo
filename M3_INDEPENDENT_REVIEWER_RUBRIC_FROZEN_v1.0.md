# 独立 Reviewer 冻结 Rubric（EP-09 / `M3-AC-19`）

> `task_id` = `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> `frozen_at` = 本 commit（**先于独立 Reviewer 被调用**）
> Reviewer 身份要求：未参与实现、上下文隔离、**无写权限**（只允许写它自己的一份审查文件）
> 审查预算：正式 Review **一轮**；定向修复 **一轮**；修复后须复验直接／传递影响与影响未知项

## 0. Reviewer 要回答的唯一问题

> **这份交付里，有没有任何一条声明的等级高于它的证据？**

不是"做得好不好"，不是"还能不能更完善"。只查**声明与证据之间的落差**。

## 1. 七项审查（每项独立给 `PASS` / `FAIL` / `NOT_CHECKED`，并给出可核查的引用）

### R-1 · 判据先于结果（A2 §时序）

每一份 `M3_ECC_*_FROZEN_*.md` 的提交，是否**早于**它所判定的那批运行记录？
是否存在"看到结果之后修改判据"的痕迹（判据文件在结果 commit 之后被改动，且未版本化）？

**FAIL 条件**：任一判据文件的内容在其结果产出后被修改，且未以新版本号与触发事件说明。

### R-2 · 声明等级不越级（A2 §可信度阶梯）

逐条检查 `M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md` §7 状态矩阵与各 Checkpoint 中的 AC 状态：

- 有没有哪一条被标 `PASS`，但其冻结输入里有某一项从未被实测？
- 有没有把"有但不够"填成 `PASS`？
- 有没有把"尚未取证"写成 `FAIL`，或把"计划"写成"已验证"？

**FAIL 条件**：任一 AC 的声明状态高于其可指认的证据。

### R-3 · 失效传播不多算不少算（A3）

`main` 在施工期间前进过。检查影响面核算：

- 有没有把不受影响的证据一并作废（**多算**）？
- 有没有遗漏已知依赖（**少算**）？
- 无法判断的项有没有被标 `STALE` 而不是被假装知道？

**FAIL 条件**：出现多算或少算；或把"应该没变"当成"已核验没变"。

### R-4 · 边界与受保护资产（A4）

- 受保护目录（`content-production` / `decision-chain` / `business-persistence` / `collab-ledger`）相对**入场基线**是否零改动？
- 六份既有 Skill 是否一个字节都没改？
- 是否创建了**且仅创建了**一个 task-id 专用 Dify App？有没有触碰任何其他 App、凭据或运行记录？
- 有没有 merge/直推 `main`、force、amend、reset、squash 或改写历史？

**FAIL 条件**：任一越界。

### R-5 · 失败路径只追加不删除

- 第 1 轮 EP-06 的失败证据（组 6 不足）是否**仍然在仓库里**，没有被修复后的第 2 轮覆盖或删除？
- 本轮因余额耗尽而失败的运行记录，是否**逐条保留**，而不是被重跑覆盖或悄悄丢弃？
- 有没有"看到结果后重抽"的痕迹（同一绑定下同一用例出现两份结果，且只有一份被用于判定）？

**FAIL 条件**：任一失败 Attempt 被删除、覆盖或择优丢弃。

### R-6 · 声明上限（AC-20 反证探针）

全文反搜下列措辞，出现即须逐条核对其证据是否成立：

```text
生产就绪 ｜ 真实经营提升 ｜ 因果 ｜ 纵向切片完成 ｜ 平台唯一 ｜ 已经避开同质化
REAL_OPERATION_LOOP_VERIFIED ｜ OPERATIONAL_UPLIFT_PROVEN ｜ CAUSAL_BUSINESS_LIFT_PROVEN
UNIVERSAL_SUPERIORITY ｜ M5 集成增益
```

**FAIL 条件**：任一措辞在无对应证据时被肯定性使用。

### R-7 · 留出与探索/正式分轨（AC-19 本体）

- 留出夹具是不是在 `SKILL.md` 冻结**之后**才存在（git 提交顺序可证）？
- 交付文档有没有**如实记录**留出强度的真实上限，而不是宣称"完美留出"？
- 独立判定者有没有实际读过被禁止的文件（Checkpoint、既有判定、验收判据）？其工具调用记录是否被核验过？
- 探索性运行与正式取证是不是分开的，没有混用？

**FAIL 条件**：留出被泄露却未记录；或判定者读过禁读文件却未被发现与披露。

## 2. Reviewer 不做的事

- 不评价 Skill 写得好不好、内容质量高不高——那是 A/B 增益门的事，不是本 Rubric 的事；
- 不重新设计架构、不提改进建议清单；
- 不修改任何文件（只写自己那份审查文件）；
- 不替 Founder 做产品验收裁决。

## 3. 输出格式

```markdown
# M3 独立收口审查（EP-09 / AC-19）
> Reviewer 身份声明：未参与实现；本次会话未读过 <列出被禁读且确实未读的文件>
## 汇总
| 项 | 结论 | 一句话 |
| R-1 … R-7 | PASS/FAIL/NOT_CHECKED | |
## 逐项
### R-N · <名称>
**查了什么**（命令或文件） / **发现** / **结论**
## 阻断项（必须修复才能收口）
## 非阻断观察（登记为新任务候选，不在本任务内处理）
```

**阻断项与非阻断观察必须分开。** 把非阻断观察写成阻断项会造成假阻塞；把阻断项写成观察会造成假放行。两者都是本 Rubric 下的 `FAIL`。

```text
END_MARKER
= M3-INDEPENDENT-REVIEWER-RUBRIC-FROZEN-v1.0-END
```
