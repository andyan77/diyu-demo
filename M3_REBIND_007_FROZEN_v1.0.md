# M3 REBIND-007 · 判据冻结 v1.0（DD-5）

- `task_id`：`DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`（不建 NEW_TASK）
- 冻结时间：2026-08-27
- 直接前身：`M3_ECC_REBIND_006_FROZEN_v1.0.md`（v1.5）。**本文件不覆盖它**，历史判据原样保留。
- 候选：`v1.5.1`；改动只有一个文件、一处：`gate_v13/shared_checks.py::PERIOD_WORD`
- 对照组提交：`fc63add`（= v1.5，第 2 段正式取证真正跑的那份闸门）

---

## 1. 触发事实（判据冻结在结果之前）

第 2 段正式取证跑完 49 例行为后，出现 2 例拒收。逐例判读**先于**任何代码改动完成：

| 案例 | 拒收 | 判读 |
|---|---|---|
| `B15-DIR-02-topic-to-brief` | `stale_value_override` 阻断 | **误报**，本文件修 |
| `B09-5-no-market-data` | `HARD_FAIL_MIN_OUTPUT`，正文 0 字 | **真拒**，闸门行为正确；属另一条线（见 §5） |

---

## 2. DD-5 · 「本周期」不是「本周」

### 2.1 原句与病理

真实运行 `B15-DIR-02` 草稿原句：

> 这条选题的任务是：把一个具体选品决定拆开给观众看，证明「周宁」这个账号存在的理由
> ——这是长期价值目标下，**本周期最重要的一条**。

`PERIOD_WORD = (每|本|这|一|该)\s*(天|日|周)` 把「本周期」里的「本周」整个匹配掉，
于是「一条」被绑上周期，读成 `1 条/周`，去压输入里的 `expected_publish_count = 3 条/周`。

一个**选择性量词**（三条里最重要的那一条）被读成**速率主张**。这正是 REBIND-005 要修掉的
G-4 v1 病理换了个形状复发：第 9 轮语料里没有「…期」这种形状，所以第 9 轮预检的
「0 新增阻断」挡不住它。同族还有「本日期」。

### 2.2 修法

```python
PERIOD_WORD = re.compile(r"(每|本|这|一|该)\s*(天|日|周)(?!期)")
```

一个负向前瞻，不新增词表、不新增守卫、不动其他判据。

### 2.3 作用域声明

- `NOW_MARKER` 里的「本周」同样会匹配「本周期」，**本轮不改**：「本周期」本来就是现时性
  标记，这一处匹配在语义上是对的。改它会让判据在没有差别的地方多一次改动（A5）。
- 修法只影响**周期绑定**，不影响数值抽取、不影响槽位解析、不影响补齐节点。

---

## 3. 方法义务（与 REBIND-006 §3 同一套，不另立）

1. 对照组取**改动前那一次提交**的源码，不取 HEAD；启动时机械断言对照组确实是 v1.5。
2. 误报轴与漏检轴**两轴都量**；只量一轴会把专治漏检的守卫误判成无差别单元。
3. 漏检探针三族：族一二取真实运行原句一字未改、只机械伪造槽位权威值；
   族三由**独立解析器** `INDEP_RATE` 生成，不用被测代码的词表。
4. 「旧能抓到的新也要抓到」只有一个例外：**旧的那次抓取本身就是误报**。
   这个例外**必须机械判定**（`_lost_is_dd5`：复算旧判据开火点，看周期是否由后接「期」的
   周期词借来），不得由执行侧读一遍句子给结论。
5. 每个新守卫单独消融；两轴都不变 ⇒ 按 A5 删除，并把删除理由写进披露。

---

## 4. 预检结果（`ep33-rebind007-v151/VERIFY_REBIND007.json`）

语料两轴，全部是**已落盘的真实运行记录**，零模型调用：

- 主轴：本轮 v1.5 正式取证草稿 —— 行为 49 + 纵向 12 + 保真 8 = **69 次运行**
- 回归轴：第 9 轮 v14 语料 **64 次运行**
- 合计 133 次运行 × 2 份正文（草稿／终稿）= **266 份正文**

| 项 | 结果 |
|---|---|
| Q1 误报轴 | v1.5 命中 6 处 → v1.5.1 命中 4 处；清掉的 2 处都是 `B15-DIR-02`（草稿＋终稿），逐例判读为误报；**新增命中 0** |
| Q2 漏检轴 | v1.5 捕获 295 → v1.5.1 捕获 231；丢失 64 处，**64/64 机械判定为「旧判据的那次抓取本身就是 DD-5 误抓」**，真漏检 0 |
| Q3 具名族 | 负例 3/3 不响（「本周期最重要的一条」「本周期内最值得做的一条」「本日期最靠前的一条」）；正例 6/6 仍响（`本周实际产能只有一条`、`每周 3 条`、`一周 2 条`、`该周 1 条`、`4 条/周`、`本周期内每周 3 条`） |
| Q4 消融 | DD-5 关掉后误报轴 4→6、漏检轴 231→295，**两轴都变** ⇒ 该守卫成立 |
| Q5 真拒不变 | `E07`、`E08` 在 v1.5.1 下仍被拒；穷举不变性 4/4、8/8，无反例 |

零模型全量重放（`REPLAY_V151_SUMMARY.json`）：

| 轴 | 样本 | 改前拒收 | v1.5.1 拒收 | 清掉 | 新增拒收 |
|---|---|---|---|---|---|
| 主轴（本轮 v1.5 草稿） | 69 | 2 | **1**（只剩 `B09-5` 这个真拒） | `B15-DIR-02` | **0** |
| 回归轴（第 9 轮语料） | 64 | 13 | **2**（`E07`、`E08`） | 11 | **0** |

证据等级：主轴 59 例已观察（A 直发路，整条链零模型）、10 例推断（B 补齐路，中间隔着
补齐 LLM，只对持续位做集合代数反事实）。等级不因为数字好看而上调。

---

## 5. 本轮披露的残留（不修，写明）

- **R-1**（承 REBIND-006）：`／` 全角斜杠不被 `_qty_scan` 认作周期后缀。两轴消融无差别，按 A5 不修。
- **R-2**（承 REBIND-006）：槽位反序写法（「陈晚 2 条 + 导购 1 条，合计每周 3 条」）认不出权威值。
- **R-3**（承 REBIND-006）：族三 2 处既有漏检。
- **R-4**（承 REBIND-006）：DD-1 的 B 补齐路前提（补齐节点复现骨架 POS 行）。
  **本轮真运行已把它抬到已观察**，见 §6。
- **R-5（本轮新增）· 真周期词 + 选择性量词**：`本周价值最高也最不占产能的一条` 这种形状，
  周期词是真的，「一条」仍会被读成 `1 条/周`。语料里真实存在一例（`FX-M3-HOLD-02__B`）。
  曾写过一版 `SELECTIVE_PREFIX`（`最…的` 紧贴数字则不计速率），**在 133 次运行 × 2 份正文上
  改变零个阻断结论**——`B15-DIR-02` 已被 DD-5 清掉，`FX-M3-HOLD-02__B` 已被 DD-3b 清掉——
  按 A5 删除，留下这条披露。哪天有样本能把它量出来，再按同一套程序收口。

---

## 6. DD-1 真运行结论（`DD1_REAL_RUN.json`）

第 9 轮 DD-1 的修法（骨架同时保留输入侧持续位与模型本轮新声明的持续位）在**真跑**上成立：

- 69 次运行里 **36 例**声明了新持续位，共 **65 个**；
- 骨架对模型已声明位 **零删除**（`skeleton_missing_any_declared_position = []`）；
- 终稿侧 `positions_dropped_new`、`positions_introduced_by_gate` **全部为空**；
- 第 8 轮由 DD-1 造成的 10 例误拒，本轮真运行 **全部消失**。

等级：**已观察**（不是重放推断，是 v1.5 载体真跑出来的）。

---

## 7. 影响面（A3，不多算也不少算）

改的只有 `shared_checks.py` 一个文件。`SKILL.md` 字节未变（`245ee2ab…`）。
每组取证的命题必须拆成两层看，否则就会多算：

| 命题层 | 绑定 | 本轮状态 |
|---|---|---|
| 层一「模型在该输入上产出了什么」 | SKILL.md + 图的 LLM 节点 + 模型 | **CURRENT** —— 未改，69 份草稿仍然有效 |
| 层二「闸门对该产出怎么判」 | 闸门代码 | **STALE**，但**可零模型重新推导**，本轮已全量推导完（69/69、64/64） |

- `EP-08 A/A+/B′ 9 例`：不受影响（不依赖闸门）。但按新验收方法已 `NOT_APPLICABLE`。
- `EP-08 B 臂`、`盲评 36 份`：**未启动**，且按新验收方法 `NOT_APPLICABLE`。
- `AC-16 系统提示词绑定 + 画布实证`：部署 v1.5.1 会改图哈希 ⇒ **STALE**，需重做；
  重做是确定性读回，**零模型调用**。
- `M2→M3 投影契约`、`AC-12/AC-13 定向复验`、`下游 Brief 消费`：不受影响。

**v1.5.1 的正式重跑预算 = 0 个模型 token。** 闸门是后处理，草稿是模型输出，两者可分离。

---

`END_MARKER: M3-REBIND-007-FROZEN-v1.0-END`
