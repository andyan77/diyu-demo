# M3 · B09-5 零正文 —— 最高失效节点定位 v1.0

- `task_id`：`DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
- 定位方法：**零模型**。只读已落盘的运行记录与 Dify 已存的节点级执行明细，未发起任何模型调用。
- 结论一句话：**最高失效节点是主生成 LLM 节点本身**；闸门、补齐节点、装配、复检全部按设计正确动作，
  没有任何一个节点代写正文。

---

## 1. 现象

`B09-5-no-market-data`（绑定 `M3-AC-09`，「无外部市场资料」）本轮真运行输出：

- `gate_status = HARD_FAIL_MIN_OUTPUT`，`gate_path = hard_fail_no_repair`
- 正文 **0 字**，`cycle_state_carry = REJECTED_KEEP_PREVIOUS`
- 用户看到的只有一句系统说明：「本轮没有产出可用的运营判断……请补齐所缺的输入后重新发起。」

这正是 Founder 第八节硬性问题第 6 条点名的形态：**因无市场资料而输出零正文、让任务整体停摆**。

---

## 2. 逐节点重推（`/console/api/apps/…/workflow-runs/…/node-executions`，读的是已存记录）

| # | 节点 | 类型 | 状态 | 关键输出 | 判读 |
|---|---|---|---|---|---|
| 1 | 本轮投影输入 | start | succeeded | `account_context` 526 字、`loaded_references` 2935 字、清单标 fashion-and-market **LOADED** | 输入完整，**不是输入缺失** |
| 2 | **单账号持续运营决策** | **llm** | succeeded | `text` 276 字；`prompt_tokens=14770`、`completion_tokens=**133**`、`finish_reason=**stop**`、`reasoning_content` 长度 **0**、latency 4.22s | **失效点在这里** |
| 3 | 必填项闸门 | code | succeeded | `HARD_FAIL_MIN_OUTPUT`，`body` 长度 0，`needs_fix=no` | 正确检出 |
| 4 | 按闸门报告补齐缺失项 | llm | succeeded | `NO_CHANGE`（2109 tok） | 正确：硬失败时不放它写正文 |
| 5 | 确定性取稿 | code | succeeded | `final_text` 长度 **0**，`path=hard_fail_no_repair` | 正确：**没有代写** |
| 6 | 闸门闭合复检 | code | succeeded | `carry=REJECTED_KEEP_PREVIOUS` | 正确：不让空交付拿到基线 |
| 7 | 输出 | end | succeeded | 系统说明 54 字 | 正确 |

**排除项（都不是原因）**：

- 不是传输故障：HTTP 200、`workflow_status=succeeded`、`transport_attempts` 只有一次、无重试。
- 不是截断：`finish_reason = stop`，模型自己停的；`completion_params` 里没有 `max_tokens`。
- 不是下游代码吃掉了正文：LLM 节点的 `text` 原文就是 276 字，节点级记录与 `draft_raw` 逐字一致。
- 不是闸门误判：正文确实 0 字。
- 不是 SKILL.md 缺 AC-09 指令 —— 见 §4。

---

## 3. 失效签名与频次（跨全部历史记录，零模型）

三次退化生成的形状**完全一样**：推理通道里只有一个反引号，然后直接给出一个
（有时被代码围栏包住的）`<<AUDIT>>` 块，**没有正文**。

```text
v1.5  B09-5：  <think>\n<!--dify-deepseek-reasoning-->`\n</think><<AUDIT>>…<<END_AUDIT>>
v1.3  B09-5：  <think>\n<!--dify-deepseek-reasoning-->`\n</think>```\n<<AUDIT>>…<<END_AUDIT>>\n```
v1.2  E09  ：  <think>\n<!--dify-deepseek-reasoning-->`\n</think>`<<AUDIT>>…<<END_AUDIT>>
```

已落盘的全部 446 次有草稿的运行里，命中这个签名的共 **3 次**：

| 范围 | 命中/总数 | 比率 | 95% CI（Clopper–Pearson） |
|---|---|---|---|
| `B09-5` 这一格 | **2 / 6** | 33.3% | **[4.3%, 77.7%]** |
| `B09-5` 以外的全部运行 | 1 / 440 | 0.2% | [0.0%, 1.3%] |
| 全部记录运行 | 3 / 446 | 0.7% | [0.1%, 2.0%] |

`B09-5` 这一格的 6 次运行结果：v1.1 通过、v1.1-run2 通过、v1.2 通过、**v1.3 零正文**、
v1.4 通过、**v1.5 零正文**。SKILL.md 在 v1.3→v1.4→v1.5 之间改过两次，**这个失效穿过了改动**。

区间很宽，样本只有 6 次，**不能说"33% 是稳定失效率"**。能说的是两件事：
① 它不是一次性偶发——同一签名在两个不同输入、三个不同 SKILL.md 版本上都出现过；
② 这一格的失效率显著高于其余全部运行。

---

## 4. SKILL.md 是否需要修改

### 4.1 不是缺 AC-09 指令

SKILL.md `O-6` 末尾已经写死了这条，且写得很具体：

> **外部市场资料整格为空时，本轮照常给出完整的运营判断。**
> **两件不许做**：因为这一格空着就交零正文、或交一句"请补齐输入后重新发起"——
> 那是把一次本来能完成的工作退回给用户……
> ❌ 输入里 `market_observations: 无任何外部市场资料` ⇒ 输出零正文，只剩一句"缺输入"。

`〈最低实质产出〉` 也已经写明零正文 = 没有产出。
**所以这不是"没写"，是"写了没被执行"。** 补一句更狠的同义指令，属于 A5 意义上的无差别单元。

### 4.2 需要改的是**输出形状**，不是 AC-09 语义

三次失效的共同形状是「一个代码围栏包住的审计块，别的什么都没有」。
SKILL.md 里唯一被代码围栏包住的模板，就是 `〈审计块（正文之后，格式固定）〉` 那一段：

````text
```text
<<AUDIT>>
探索提案=是|否;…
```
````

当前 SKILL.md 只在**标题里**用「正文之后」表达了顺序，正文里**没有一条硬规则**说
「审计块不得是全部输出」或「审计块不得放进代码围栏」。

**建议的修改（只有两句，不动任何产品语义）**，写进 `〈用户可见正文的硬要求〉`：

1. 审计块**只能**出现在正文之后；正文不存在时**不许**单独输出审计块。
2. 审计块**不加代码围栏**，前后不加任何 ``` 标记。

### 4.3 因果等级：**推断，不是已观察**

- **已观察**：三次失效共享同一形状；SKILL.md 里唯一的围栏模板就是审计块；AC-09 指令存在且被违反。
- **推断**：「加这两句能降低失效率」。要把它抬到已观察，只能真跑，而真跑是模型调用——本轮禁止。

### 4.4 结论

**需要改 SKILL.md，但理由是输出形状鲁棒性，不是 AC-09 语义；且改动的效果目前只有推断级证据。**
按 Founder 第 4 段的授权范围，本轮**不改**，只提交这条判断和下面的影响面与预算，等新授权。

---

## 5. 决策相关的一句话

Founder 第五节要求 `B09-5` 必须进唯一测试输入组，第七节要求每个输入**只跑一次**、
输出不好**不许重跑**，第八节第 6 条把"因无市场资料输出零正文"列为**不得判 PASS** 的硬性问题。

三条合起来：**在当前候选上，这一格有实测过的、区间很宽但明显不低的概率一次就触发硬性问题，
从而让整组直接判 FAIL。** 这是把包交给 Founder 之前必须摆在桌面上的事实，不是风险提示话术。

---

`END_MARKER: M3-B09-5-HIGHEST-FAILING-NODE-v1.0-END`
