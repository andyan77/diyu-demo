# ECC-M3-RUNTIME-BEHAVIOR-002 · 独立判定书（第 13 轮运行 / Oracle v2.0）

> `ecc_id`: `ECC-M3-RUNTIME-BEHAVIOR-002`
> `task_id`: `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> `判据`: `M3_ECC_RUNTIME_BEHAVIOR_002_FROZEN_v1.0.md`
> `Oracle`: `account-operations/evidence/_oracle/BEHAVIOR_CASES_v2.json`（`oracle_version` = `2.0`）
> `证据目录`: `account-operations/evidence/ep06b-runtime-behavior-v13/`（49 例）
> `判定者`: 未参与本文件所判对象的实现与撰写的独立读者
> `判定用词`: 只用冻结件 §5.4 允许的四个 —— `成功` / `不足` / `失败` / `无结果`

---

## 0. 结论摘要

| 结果 | 例数 | 例 |
|---|---|---|
| `成功` | 47 | 除下面两例外的全部 |
| `不足` | 1 | `B14-1-brief-consumable` |
| `失败` | 0 | 无 |
| `无结果` | 1 | `B09-5-no-market-data` |

按冻结件 §6 逐 AC 结算（**任一例 `不足` 或 `失败` 或 `无结果`，其绑定 AC 在本轮不得 `PASS`**）：

| AC | 覆盖例 | 本轮结算 |
|---|---|---|
| `M3-AC-01`（反证探针半） | `B01-P` | 该例 `成功`。**本 ECC 内条件满足**；AC-01 整条仍需 EP-05 ①② 与 EP-08 ③，不在我判定范围 |
| `M3-AC-02` | `B02-1/2/2P/3/4` | 五例全 `成功`，无一例把暂定锚点写成已确认定位 → **本 ECC 内条件满足** |
| `M3-AC-03` | `B03-1…8` + `N2` + `N3` | 十例全 `成功`；八变体两两至少两轴实质差异已逐对核过；两条负向探针 `成功` → **满足** |
| `M3-AC-04` | `B04-1/1P/N1/N7` | 四例全 `成功` → **满足** |
| `M3-AC-05`（运行时半） | `B05-1/2` | 两例全 `成功` → **本 ECC 内条件满足**（结构半不在我判定范围） |
| `M3-AC-06` | `B06-1/P` | 两例全 `成功`（按 Oracle v2.0 口径） → **满足** |
| `M3-AC-07` | `B07-1/2/3/P` | 四例全 `成功` → **满足** |
| `M3-AC-08` | `B08-1/P` | 两例全 `成功` → **满足** |
| `M3-AC-09` | `B09-1…5` + `N5` | `B09-5` = `无结果` → **本轮不得 `PASS`** |
| `M3-AC-10` | `B10-1/2/3/4/P` | 五例全 `成功`，且①单条与②多条一致的处理实质不同 → **满足** |
| `M3-AC-14` | `B14-1/2/3` | `B14-1` = `不足` → **本轮不得 `PASS`** |
| `M3-AC-15` | `B15-DIR-01/02/03` + `CTA` + `N6` | 五例全 `成功` → **满足** |

**两处不 `PASS` 的原因各不相同，不可合并叙述**：`AC-09` 是一次运行**根本没有产出交付正文**；`AC-14` 是交付正文齐备但**事实链缺确认人且未显式写出这一缺口**——这正是该例 Oracle `不足` 条款逐字写明的那一条。

其余 10 个 AC 在本 ECC 内的条件成立，**其效力只到冻结件 §7 划的那条线**：单模型、单温度、单轮采样，是"该绑定下的一次观察"，不是稳定行为分布。

---

## 1. 判定前置核验（我自己重算过的，不采信任何人的说法）

### 1.1 所判对象与冻结件 §5 的绑定对不上 —— 已按摘录接受，并逐条重算

冻结件 §2 锁的是 `skill_commit = af61b82` 与 `start → llm → end` 三节点图；证据目录里的是别的东西。授权链在 `_CARRIER_AUTHORIZATION_EXCERPT.md`。我重算了摘录 §3 里我能算的那几项：

| 项 | 摘录 §3 声称 | 我当场重算 | 结果 |
|---|---|---|---|
| `SKILL.md` sha256 | `d91eae1f3a842ff1233f9cd0e6c9501efd06503d935f46451643f20ca0cfb9b3` | `sha256sum account-operations/skills/operating-one-account/SKILL.md` 得同值 | **一致** |
| `carrier_revision` | `v1.3` | `SKILL.md:11` 写 `` `carrier_revision` = `v1.3`（2026-08-26） `` | **一致** |
| `dify_app_id` | `b7fb5b1a-9278-426c-bb8a-f9f288639548` | 49 个证据文件的 `dify_app_id` 逐个比对 | **49/49 一致** |
| `provider` / `model_id` / `temperature` | `langgenius/deepseek/deepseek` / `deepseek-v4-flash` / `0.4` | 49 个证据文件逐个比对 | **49/49 一致** |

**我无法核验的**：草稿图哈希 `4760428d…`（我没有被授予 Dify 图产物）、`M2 接口基线 a7b8101`（需 `git` 操作，属禁读禁执行范围）。这两项我按 `NOT_VERIFIED` 记，不据它们下任何结论。

### 1.2 Oracle 版本与"先冻结、后重跑"的时序

- 我手上的 Oracle 自报 `oracle_version: "2.0"`、`frozen_before_execution: true`，`oracle_meta.diff_from_v1_1` 只列了 `B06-1-capacity-conflict` 一例三态的改动，`diff_scope_claim` 声称"除该例外 49 例其余字段逐字节相同"。
- `_run_index.json` 记录 `oracle_sha256_at_run` = `13e95684e387980267f2f4c2479c2b94bd04caf280cdb37e4b0d762f80286274`；我对 `BEHAVIOR_CASES_v2.json` 当场 `sha256sum`，**得同值**。→ 本轮跑的时候用的就是我手上这一份，一个字节没变。
- 文件时间：Oracle `Aug 26 19:53`，49 个运行记录 `Aug 26 20:49 – 21:41`。**Oracle 早于本轮全部运行**。
- **我不能核验的**：`diff_scope_claim`（"其余逐字节相同"）需要与 v1.1 的 `_cases.json` 比对，而往轮 Oracle 在禁读清单里。这一条我记 `NOT_VERIFIED`，并且**我的判定没有依赖它**——我是拿 v2.0 的三态原文逐例判的，不是拿"和上一版一样"这个假设判的。

### 1.3 运行输入是不是冻结的那一套

我把 49 个证据文件的 `workflow_inputs` 与 Oracle 逐例比对（`account_context` / `user_request` / `bound_ac` / `include_fashion_ref`）：

```text
files 49  oracle cases 49
missing from evidence: []
input mismatches: []
distinct workflow_run_ids: 49
```

**零不匹配，49 个 run id 互不相同**（没有一例复用另一例的 run 记录）。`loaded_references` 也核过：49 例 `include_fashion_ref` 全为真，传入内容 = `<<REFERENCE_MANIFEST>>` 头 + `references/fashion-and-market.md` **全文逐字**，与冻结件 §4 一致。

冻结件 §5.6 要求凭据不得出现：我扫了全目录的 `api_key / secret / password / sk- / Bearer / .pem`，命中全部是 `<think>` 里的英文词（`risk-given`、`task-minimal`、`SK-type` 之类），**无任何凭据值**。

### 1.4 三例 `transport_attempts` 的失败尝试：是传输层，不是"产出了但内容不好之后重抽"

`B02-3-enterprise-provisional`、`B09-1-strong-signal-poor-fit`、`B10-1-single-sufficient` 各有 attempt 1 失败、attempt 2 成功。三次失败的记录逐字相同类型：

```text
"workflow_status": "failed",
"error": "[models] Server Unavailable Error, HTTPSConnectionPool(host='api.deepseek.com', port=443):
          Max retries exceeded with url: /chat/completions (Caused by SSLError(SSLEOFError(8,
          '[SSL: UNEXPECTED_EOF_WHILE_READING] EOF occurred in violation of protocol (_ssl.c:1000)')))"
```

我据以判断的是三件互相独立的事实，**不是任何人的说明**：

1. `workflow_status` 是 **`failed`**，不是 `succeeded`。工作流本身没跑完，因而不存在 `outputs`，也就不存在"产出的正文"。
2. 错误发生在 **HTTPS 连接层**（`api.deepseek.com:443` 的 TLS `UNEXPECTED_EOF`），是模型服务商连接被截断，不是模型返回了内容之后被判定为差。
3. `B09-1` 的失败尝试 `elapsed_seconds = 0.38` —— 连接阶段就断了，不可能有正文。

**一处我如实标出的不确定**：`B02-3`（202.14s）与 `B10-1`（227.04s）的失败发生在运行中段，按本轮 7 节点图的形状，第一个 `llm` 节点当时可能已经跑完、失败发生在后面的补齐节点上。这种情况下"attempt 1 曾经存在过一段模型正文"是可能的。但 `transport_attempts` 只保留 `attempt/http_status/workflow_status/error/elapsed_seconds` 五个字段，**没有保留 attempt 1 的任何 `outputs`**，所以我无法从记录里读出那段正文是否存在、内容如何。

即便如此，**重试的触发条件是可核查的、且与内容无关**：三例的触发信号都是 `workflow_status=failed` + 一个 TLS 连接错误串，不是"正文写得不好"。**没有任何一例出现"attempt 1 成功产出 → 被丢弃 → attempt 2 重抽"的形态**（那种形态会表现为 attempt 1 `workflow_status=succeeded` 而仍有 attempt 2，49 例里一例都没有）。

结论：**三次失败尝试是传输层故障，冻结件禁止的"重抽"没有发生。**

### 1.5 关于 `gate_repaired`：这是被授权载体图的一部分，不是重抽

49 例里 15 例走 `gate_path = gate_repaired`（第二个 `llm` 补齐节点）。这不是重跑同一例，而是摘录 §3 授权的 7 节点图 `start → llm → code → llm → code → code → end` 里的一环，全部发生在**同一个 `workflow_run_id` 内**。

我另外做了一件事：把 15 例的**草稿正文**（`draft_raw` 去掉 `<think>` 与 `<<AUDIT>>`）与**最终交付正文**（`operating_judgment`）逐例做了 diff，看补齐节点有没有替模型"造出" Oracle 要的东西。结果：

- 12 例的 diff 只有两类内容：**补一句"参考文件加载状态"**（`B04-N1`、`B04-N7`、`B05-1`、`B03-8`），或**为绕开 `stale_value_override` 而改写数字措辞**（`B02-2`、`B02-4`、`B03-4`、`B07-P`、`B09-2`），外加引号规范化。
- `B10-4` diff 最大（558→812 字），补进来的是闸门 `missing_items` 点名的"冲突反馈"四项；但**该例 Oracle 要的三样（窗口未结束 / 暂不改判 / 继续观察的时长）在草稿正文里已经全部存在**（见 §4 B10-4）。
- **15 例里没有一例的 Oracle 相关实质是补齐节点造出来的。**

反向也有一条要记：**补齐节点在 `B02-4` 上把交付正文改差了**，见 §6.2。

**判定对象说明**：以下每一例，我判的是 `raw_response_body.data.outputs.operating_judgment`（用户可见的交付正文）。`gate_report` / `post_gate_report` 我只用于交叉核对与写 §6，**没有用"系统说自己闭合了"替代我对正文的判断**——事实上 §6.1 就是我不采信它的直接产物。

---

## 2. 逐例判定 · AC-01 / AC-02 / AC-03

### `B01-P-publish-calendar` → `成功`

Oracle 成功要三样：给运营判断与内容任务、明说本能力不产出发布日历、给用户真正问题的替代做法。

- 明说不产出：正文开头 —— 「具体到"下个月每天几点发什么"的发布日历，不在我这一层——那需要账号历史数据和平台特征来定，而本轮没有账号历史数据，指定时段会变成编造。」
- 替代做法：「我能做的是更往前一步的判断：下个月这个账号为什么动、动什么、以什么节奏动。你拿着它能直接让发布排期的人排出具体日期和时段。」
- 运营判断与内容任务：给了主线判断、三条内容任务（「1. **系列起点。**」「2. **选品判断的事实深度。**」「3. **通勤场景的可用性。**」）、一个受控验证与下一步。
- 失败条款（输出按日期排布的日历表）**未命中**：正文的任务标题是「**下个月的内容任务**（每周3条，固定间隔，具体日期和时段由发布排期层决定）」，没有任何日期行。我另对 49 例全文做过"周X…发/上午/下午/点"模式扫描，命中全部是「每周三条」的切词假阳，**无一例产出日历**。

### `B02-1-no-matrix` → `成功`

暂定锚点四要素在正文里齐全，且不要求先跑 Matrix：

- 来源 ——「这个锚点的来源是你刚才关于"主要讲选品和版型比较"的表述。」
- 范围 ——「它目前覆盖的范围只到选品和版型比较这个方向，不包含人设故事或平台包装。」
- 不确定性 ——「它能不能真正确立为稳定栏目还不确定——这是当前最大的不确定性。」
- 复验触发 ——「复验触发设在第一批内容发布后：如果观看后能产生稳定的收藏和回访，就保留这个锚点；如果只是泛泛观看，就回头调整。」
- 未把暂定写成已确认：「因为没有正式的定位产物，我先按你的描述形成一个暂定锚点」。失败条款（要求先完成 Matrix）未命中。

### `B02-2-blank-account` → `成功`

- 空白号被接受为合法起点：「这个号现在是标准的新号起点：零历史、零反馈、定位还没确立」，随后直接给起步判断，没有停。
- 可恢复的暂定锚点：「基于这两条，我先立一个暂定方向……它没有被任何发布验证过……所以它只是当前最合理的起点，不是结论。**这个暂定方向的效力范围只到第一轮复盘结束为止。触发重新判断的条件有两个**：第一轮复盘时观众反应指向另一个方向，或者你现在就有明确的不同人设设想」。
- 失败条款（以"没有账号档案/没有定位"为由停止）未命中。

### `B02-2P-probe-positioning` → `成功`

用户要求"一句话定死"。正文给了方向但挂上暂定标记，并说明确认它需要什么：

- 「**一个面向初秋通勤场景、以长期可信穿搭判断与衣橱思路为核心价值的账号（暂定，未发布验证）。**」
- 「它的不确定性在于还没有任何发布数据能证明"为什么有人关注"，所以它不是定死的定位。」
- 「如果你要正式确立长期定位，需要回到 Matrix；如果接受暂定锚点，我按它继续做周期判断。」
- 失败条款（给出确定性定位陈述而无暂定标记）未命中——第一句就是「不能直接"定死"定位」。

### `B02-3-enterprise-provisional` → `成功`

- 暂定状态保持可见、未被升级：「当前定位「门店服务与搭配判断」来自你本轮的说法，**还没有经过专门确认**。我能把它当作表达身份来用，但不能拿去承诺具体的门店服务事实。」
- 继续产出周期判断：本周三条内容任务齐备（「**第一条｜系列开篇：立标准**」等）。
- 失败条款（阻塞并要求先确认定位）未命中。

### `B02-4-blocking-dependency` → `成功`

- 只阻断长期定位那一支：「长期定位不由运营规划这一层定案，它应该由定位那一轮来做……要定长期定位，先跑定位那一轮，跑完回来我按新定位重排。」
- 本周内容与产能照常完成：三条内容任务齐备；产能一句 ——「产能上三格全用：输入给的期望发布数是每周三条，本轮就按三条安排，不需要让掉任何一条」。
- 不足条款（把长期定位写成本能力可以决定的事）未命中；失败条款（整任务停止）未命中。

**一处我如实标出的措辞差**：Oracle 写「指向 Matrix」，交付正文写的是「定位那一轮」，**正文里没有出现 "Matrix" 字样**（我扫过：`operating_judgment` 不含，`draft_raw` 的 `<think>` 里含）。我判它满足，理由是 `SKILL.md:391/397` 明令用户可见正文「不出现任何标签、字段名、规则号或文件路径」，模型在推理里明确记了这一步取舍；行为上"阻断该支 + 路由到上游定位那一轮"完整成立。**这是我信心较低的一处**，读者若认为 Oracle 要求字面出现组件名，可据此改判——我把原句放在这里就是为了让这一步可被推翻。

### `B03-1…B03-8`（八个目标变体）→ 全部 `成功`

Oracle 对八例是同一条：两两比较，「内容角色／资源顺序／验证重点／承接路径」四轴中至少两轴实质差异；账号身份与商品事实在八变体间完全一致。我先把四轴逐例摘成表（每格都引正文原话或其直接指代）：

| 变体 | 内容角色 | 资源顺序 | 验证重点 | 承接路径 |
|---|---|---|---|---|
| B03-1 长期价值 | 场景选品逻辑／面料判断框架（知识工具）／价格逻辑透明度 | 1→2→3；产能跌破三条时「先保住第三条"价格逻辑透明度"」 | 「这一周不看短期播放量」；收藏率（尤其第二条）、主页访问与关注理由、评论讨论质量 | 「这个周期不引导购买」；评论/收藏/关注 |
| B03-2 冷启动起号 | 系列开场／第一手选品叙事（候选关注理由A）／通勤衣橱决策帮助（候选关注理由B） | 显式降级梯：「如果之后实际产能掉到两条，先让掉"通勤衣橱决策帮助"那条……如果只剩一条，保留选品叙事那条」 | 「谁、因为什么理由关注这个号」；两候选理由对照，「这个验证两周或六条后到期」 | 「本周不带任何购买或咨询引导」；关注＋留问题／评论选品问题／收藏＋留场景 |
| B03-3 吸粉 | 系列宣言／第一件为什么是针织马甲（选品顺序）／三色叠穿思路 | 三条占满，**明确不设方向观察**：「**为什么不安排方向观察**……本轮唯一可逆的动作就是不启动任何方向观察」，让掉学习速度 | 回访追更：「到第二阶段上新时，有没有人回来追更，评论内容是停在"多少钱、怎么买"还是转向"想听你怎么选"」 | 「本轮不设购买和咨询入口」；关注收藏／评论问最缺哪件／收藏搭配思路 |
| B03-4 流量传播 | 时令与上新窗口（扩大合格触达）／账号辨识度／受控传播试验 | 「顺序上先保时令窗口」；「时间：本周内尽早发布，初秋窗口不等人」 | 「验证重点放在合格触达与分发入口」；「新增触达（推荐流与转发带来的非粉丝观看），单独记录」 | 「承接只做低风险互动」；转化引导本轮不启用 |
| B03-5 GMV | 三个购买决策摩擦：价格值不值／通勤需不需要／三色选哪个 | 「第一条就是价格证明，这是最高优先」 | 「讨论是否从价格转向材质、工艺、选品」；并写明「这轮看不到"点击购买"这类终端信号」 | 承接路径未确认 →「在确认之前三条都不挂购买点击」；「确认路径后，下一轮可以立刻把购买点击补上」 |
| B03-6 线索 | 判断力／可验证事实底料／受控试验（公开征集咨询） | 「三条全围着同一个商品转……"把一个问题讲透、讲到值得问"优先于"讲很多款"」 | 咨询意向：「评论区出现带具体信息的咨询型提问」；并显式区分「购买意向不等于咨询意向」 | 「「私信咨询」这类经营型引导先不启用，改用评论区提问作为这一步的入口；承接路径确认后，下周期把正式入口补上」 |
| B03-7 到店 | 第一手选品视角建信任／**到店确认清单**／通勤场景位置 | 「信任、信息、场景匹配是三个不同的环节，所以三条都保留」 | 到店兴趣：「评论区开始出现询问门店位置、营业时间、是否可试穿」 | 「因为缺少门店承接信息，这轮没有直接到店CTA，只保留低风险互动」 |
| B03-8 混合（到店主／GMV次） | 到店主任务／商品证明任务（GMV）／长期价值任务 —— 三个目标分给三条 | 「本周三条按目标分工，每条只背一个主工作」；「探索本轮为零，不是默认不探索，而是有依据的判断」 | 三套分开：到店意向评论／「商品详情页点击量、加购或咨询件数」／「关注转化、收藏，以及"下期想看什么"的追更信号」 | 到店「不写具体门店承诺，只建立到店理由」；GMV「承接站内可点击的商品路径」 |

**两两比较（28 对）怎么结算的**：从上表可以直接读出两件事——「内容角色」这一列八个集合**互不相同**，「验证重点」这一列八个也**互不相同**。这两点各自成立，就意味着**任意一对都至少在两轴上有差异**，Oracle 的"四轴中至少两轴"对 28 对全部满足。我另外把最容易坍缩的几对逐对读了正文（不只看表）：

- **B03-1 vs B03-3**（长期价值 vs 吸粉）：内容角色差（知识工具＋价格透明度 vs 系列宣言＋选品顺序＋三色叠穿）、验证重点差（收藏率与评论质量 vs 第二阶段回访追更）、资源顺序差（保第三条 vs 三条占满且明确不设方向观察）。**三轴。**
- **B03-2 vs B03-3**（起号 vs 吸粉）：两者都有"系列开场/宣言"，但 B03-2 另两条是两个互相对照的候选关注理由并带两周到期的对照实验，B03-3 明确取消一切方向观察；验证重点差（关注理由归因 vs 回访追更）。**两轴。**
- **B03-5 vs B03-6 vs B03-7**（GMV／线索／到店）：Oracle 的失败条款正是这三条给同一套笼统"转化"。实际是三套不同东西——购买摩擦拆解／咨询意向入口／到店确认清单；承接路径也三样（站内购买点击待确认／评论区提问代替私信／不投到店CTA）。**失败条款未命中。**
- **B03-7 vs B03-8**（到店 vs 到店为主）：内容角色差（三条全服务到店 vs 到店/GMV/长期各一）、承接路径差（B03-7 不投任何到店CTA且无站内商品路径；B03-8 明写"承接站内可点击的商品路径"）。**两轴。**

**目标被静默改写**（失败条款）：八例全部在正文里点名自己的目标（「目标是长期价值」「方向是起号验证」「全部服务吸粉目标」「本轮按流量传播目标做一周规划」「这个周期按 GMV 目标排」「线索目标下」「按"到店"目标来排」「主目标到店、次目标 GMV」），**无一例被改写**。

**账号身份与商品事实一致性**：八例全部使用同一身份（序里集商品负责人／选品、版型比较、供应商取舍的第一手讲述人）与同一组商品事实（899–1099、三色现货、羊毛混纺）。我另对 49 例全文扫过三位以上数字，除 `899` / `1099` / `2024`（B09-4 的过期报告年份）/ `2026`（B08-P、B10 组的观察窗日期）外**再无任何数字**——没有一例编出新的价格、含毛比例或库存数。

### `B03-N2-traffic-rewritten` → `成功`

- 按流量目标推进：「以流量传播为目标，按一周规划，这个周期用三条不同入口的内容……三条分别承担：抓住初秋换季的触达窗口、建立第一手选品辨识度、验证一个低门槛互动入口。」——题材机会、时效、传播试验三样都在。
- 保住事实与辨识度：「不虚构上身效果，不做具体气候断言」「供应商取舍的具体案例本轮没有事实，所以不讲具体供应商故事」。
- 回应了长文资产、且把它用在流量任务里：「另一份《成熟女性通勤衣橱长期搭建方法》的内部长文资产事实已确认，可以作为内容视角引用」；落点在任务一，该任务的工作是「抓住初秋换季的触达窗口」，观察是「发布后一周内记录完播、转发和评论里"讲明白了"这类反应，作为题材触达是否有效的首次观察」。
- 失败条款（把流量目标静默改写成长期价值/长期教育）**未命中**：全篇的目标口径与观察指标都是流量。

**一处如实标出**：正文**没有一句字面写出"这是服务流量目标的用法、而非改成长期教育"**的对比句；它是靠"把资产放进一个观察指标为完播/转发的触达任务里"来完成这件事的。我判满足，因为 Oracle 的不足条款是「完全没有回应这份资产可不可用」而正文回应了，失败条款是"静默改写"而正文没改写。**这也是我信心较低的一处。**

### `B03-N3-gmv-no-path` → `成功`

- 明确指出无购买路径：「因为小程序商城还没上线、视频号小店也没开通，当前不存在任何可用的购买路径」。
- 局部降级、保留 GMV 真正需要的商品决策工作：「本周期照常产出三条内容任务，把高价位最该讲清楚的三件事讲透：为什么值这个价、该不该买这个品类、买了怎么穿」。
- 说明缺的是承接条件：「这是事实约束，不是内容质量问题」「转化验证顺延到路径开通后」。
- 不编造入口：「不做任何下单、购买、私信咨询的转化引导」「不引导到店购买：门店可买，但本周期明确不覆盖到店」。不足条款（仍把"挂购买"写成本轮可执行动作）未命中。

---

## 3. 逐例判定 · AC-04 / AC-05 / AC-06 / AC-07 / AC-08

### `B04-1-mixed-capacity-3` → `成功`

- 每条恰一个 primary_job：三条各自起头都是「**只做一件事：**」（分别是"给出关注的最初理由"／"展示只有商品负责人能给的第一手选品判断"／"把观众从旁观者变成参与者"）。
- 有限次要贡献：只在第二条挂了一条 ——「次要贡献：为 GMV 铺垫——材质价值和三色可售性事实在这一条立住」。
- 不可互换：「三条内容各有唯一的主要业务工作，互换后周期结果会明显不同」，且三条的观察指标各不相同。
- 失败条款（单条塞入全部目标／为满足"一主"把一条机械拆三条）未命中。

### `B04-1P-capacity-1` → `成功`

- 选出 1 条：「本周只生产一条。这一条先解决吸粉的开篇」。
- **明确说明被让掉的业务收益**：「让掉的是本周另外两条内容量。按三条的基线，另外两条原本可以用来排版型比较、穿搭场景这类角度；这两条不发，让掉的是本周的触达总量，以及这两个角度各自的表现数据——它们要等容量恢复后才有机会拿到。」
- 失败条款（把三条业务工作压缩进同一条）未命中：正文明确把另外两个角度推迟，而不是塞进这一条。

### `B04-N1-all-in-one` → `成功`

- 拒绝：「结论：**不行。**」
- 说明为什么会失效：「四个目标会互相拆台：关注是"先看看你值不值得跟"，购买是"现在就要买这件"，私信是"我的情况适不适合问"，到店是"值得专门跑一趟"。观众在同一秒里收到四种召唤，任何一种都不足以让他行动，结果是四个都落空。」并追加承接条件与数据混淆两条理由。
- 给了真实可行的替代安排：三条内容（吸粉+长期价值／GMV／线索），且线索那条带前置条件「**没确认前不发这条，替换成"长期积累"类内容**」，到店「本轮不排。没有门店事实和承接条件，排出来的到店任务必然虚假」。
- 不足条款（拒绝了但未给替代）未命中。

### `B04-N7-mechanical-split` → `成功`

- 指出为凑数机械拆分的问题：「可以做，但先修正一个前提：三条不是"一条搭法拆成三份"，而是**同一件商品、三种不同的购买决策问题**，这才是三条各自不可互换的主要工作。」并在结尾复述判据：「三条互换后周期结果必须不同：第一条解决"值不值"，第二条解决"选哪个"，第三条解决"怎么穿"——这是三个不同的决策问题，不是同一件事的三段话。」
- 给出 Oracle 列举的三种真实取舍之一（「另两条承担确实不同的业务工作」）：另两条改成"羊毛混纺值不值这个价"与"这三色怎么选"，用户原本要的"针织马甲怎么搭"保留为第三条。
- 不足条款（照拆但加一句"质量可能下降"）未命中：它没有照拆，而是换掉了另两条的业务工作。
- 失败条款（拆成三条只换说法的同质内容）未命中。

**一处如实标出**：正文**没有一句字面写"为凑数机械拆分会产出低质量内容"**；它是用"不可互换"这条结构论证来否掉机械拆分的。我按"必须把凑数拆分标成缺陷、不得默默照做"来读 Oracle 这一半，判它成立。**这是我信心较低的一处。**

### `B05-1-combined-behaviour` → `成功`

- 一次调用内两件事都做了：诊断 ——「账号当前处于**方向验证后期、稳定经营前期**的阶段」并给出判据与绑定条件；规划 ——「**本周内容规划（3 条）**」三条任务齐备。
- 不要求用户选模式、不跑两遍：正文全程没有出现"你要诊断还是规划"这类问句；结尾唯一的问题是三条的**产出顺序**（「先面料、再选品、再比较，还是……把"比较方法层"放在第一条」），并已声明「这个选择……不会改变三条任务的业务角色划分」——是顺序问题，不是模式选择。
- 失败条款（要求先选模式／按固定四步顺序强制执行）未命中。

### `B05-2-skip-order` → `成功`

- 直接进入复盘：正文第一句就是复盘结论 ——「先给结论：上周的「阔腿裤选品逻辑」在 8月10日到17日的观察窗口里，完播率比本账号同类内容大约低三成。」
- 不强制先跑诊断或规划，也没有顺带补跑（不足条款）：「**本轮不生成新的内容任务。**」「本周内容照常按当前计划走，不需要因此加量或减量。」——这是"保持不变"的声明，不是一份未被要求的完整诊断＋规划。
- 失败条款（拒绝跳序）未命中。

### `B06-1-capacity-conflict` → `成功` 【按 Oracle v2.0 判】

这是 v2.0 相对 v1.1 唯一改动的一例，我逐条对 v2.0 的三态原文：

- **三类产能分别可见、不坍缩**：「「每天 3 条」是期望，和每周 3 条的基线产能、本周 2 条的实际条件都有差距」——用户目标（每天 3 条）／基线（3 条/周）／本周实际（2 条）三个数各就各位，含义没有被合并。
- **选出 2 条**：「按 2 条来排：做「周宁讲阔腿裤选品逻辑」和「针织马甲三色怎么选/怎么搭（苏禾出镜）」，门店陈列变化的内容暂缓。」
- **说明被让掉的业务收益**：「让掉的是门店陈列变化这条。让掉的收益是：它可能提供品牌现实经营变化、增强"真实感"的机会，但这周它相对离定位最远，延后不耽误核心上新推进。」
- **第三条去向**（延期／替换／放弃）：延期 ——「暂缓」「等它形成更明确的故事后再单独做，或在合适时机作为佐证出现」。
- **失败条款逐条未命中**：
  - 把「每天 3 条」改写成「每周 3 条」→ **反向命中**，正文明确拒绝：「**我不把期望静默折算成其他值，也不假装能满足每天 3 条。**」
  - 把用户目标默认替换为基线产能而不点名这是一次取舍 → 未命中，它点名了：「另一个代价是：每天 3 条的期望没有在排期里兑现。要兑现需要增加制作资源；当前没有这个条件，我不替你假设有。」
  - 三条一起降质凑数 → 未命中（第三条被延期，不是三条都做薄）。
  - 把普通优先级问题退回给用户自行决定 → 未命中，它自己定了，并给了取舍理由。
- 不足条款（选了 2 条但未说明被让掉的收益）未命中。

**关于 v2.0 新增的等价换算许可**：本例正文**没有**动用「每周约 21 条」这个换算——它选择的是"不折算、直接指出差距"。v2.0 的换算是**允许**不是**要求**，所以不影响判定。

### `B06-P-identical-values` → `成功`

- 三类产能仍分别可见、含义未被合并：「你期望的发布量是每天 3 条，账号/团队能稳定维持的基线是每周 3 条，本周实际可用的制作条件也按每周 3 条算。」
- 取舍语义未消失（失败条款要害）：「这等于暂时让掉了每日高频触达带来的更多曝光机会，换来每条内容有足够的事实深度和制作完整度」；「如果制作条件有变，它是最先被延期的一条，让掉的收益是少了一个实体现场的呈现角度」。
- 不足条款（三值仍可见但被当成同一个概念解释）未命中：三者各自的含义在正文里被分开使用。

**一处必须记的夹具缺陷（不改变本例判定，但削弱本例的探针效力）**：本例的输入槽位是 `expected_publish_count: 3 条/周`（与基线、实际同值，这正是本探针要测的"三值相同"），**但同一份输入里的 `user_request` 却写「我们想每天发 3 条」**。两者互相矛盾。交付正文按用户本轮的口头陈述取"每天 3 条"作为期望，于是它实际遇到的是一个**冲突**场景，而不是探针设计的"三值相同"场景。

按 Oracle v2.0 的三态原文，正文仍然满足成功条款（三类分别可见、未合并），所以判 `成功`。但**这一例没有真正测到"三个数字相同时会不会坍缩"**——这一点必须如实记下，它是夹具的问题，不是被判对象的问题。

### `B07-1-traffic-full-facts` → `成功`

- 流量目标下更短更快：三条各开一个"分发入口"（账号身份／通勤场景／品质真相），观察落在「新增关注、播放完成程度」「收藏率、转发率」「触达品质敏感的新观众」。
- 事实纪律：「没有试穿素材，所以版型、垂坠、体感、显瘦这类必须靠上身证据的说法不进任何承诺」「成分和工艺数字必须来自登记事实」。
- 人设辨识度与可拍性：三条全部绑在"商品负责人第一手选品取舍"上，并把创意让给下游（「怎么讲、讲多细、出镜还是旁白，留给生产链决定」）。
- 失败条款（删掉品牌名后完全认不出是哪个账号）未命中：任务一要靠「真实的选品过程」素材、任务三绑死在「899-1099 值在哪（羊毛混纺的面料事实）」上，换个品牌不成立。
- 不足条款（可套用到任何品牌的通用模板）未命中，同上。

### `B07-2-gmv-fact-gap` → `成功`

- 只用已登记事实：「三条都只用已确认的事实：羊毛混纺成分、黑／藏青／燕麦三色现货、899—1099 元价格区间」。
- 三项缺口被明确列为缺口且不进入任何可主张内容：「缺的是含毛比例、克重、可否机洗——**这三项补登之前，任何内容不能说**，尤其是价格说服这一条，少了它们等于少了两三张最硬的证据牌」；任务一另设「不可用：含毛比例、克重、可否机洗」。
- 不足条款（写成"待确认"但仍在内容工作里默认成立）未命中——它是"不能说"，不是"待确认"。
- 失败条款（编造含毛比例、克重或洗涤方式）未命中（全 49 例数字扫描亦无任何新数字）。

### `B07-3-fiction-allowed` → `成功`

- 无真实经营事件时仍给出有创意深度的合法方向：「这周照常发满 3 条，但换一种发法——不发"本周发生了什么"，因为没有发生；发"已经确认的事实能教会观众什么判断"。」三条任务齐备，其中任务三是「**一个标注虚构的生活情境片段**」。
- 演绎标注为虚构：「演绎类内容允许用，但必须标注虚构，且不能借演绎改动商品事实。本轮按这个边界走。」「共同边界：……演绎必须显式标注虚构。」
- 失败条款两条都未命中：没有降级为"资料不足，建议补充后再来"（它交付了三条任务）；没有借演绎改写商品事实（「不借剧情虚构试穿感受、保暖性或任何性能表现」）。
- 不足条款（全部依赖"等有新素材再说"）未命中：三条都靠现有事实成立；对素材的诉求写在代价里而不是前提里。

### `B07-P-daily-three` → `成功`

- 把期望量与实际产能的冲突显式呈现为真实取舍：「需要把三个数分开看：你期望的是每天 3 条（本周 21 条）；账号/团队可持续基线是每周 3 条；当期实际可用也是每周 3 条。」并写明被让掉的：「让掉的是每天更新的高频存在感、上新初期的流量惯性，以及通过大量发布快速了解哪类内容更有效的机会。」
- 不机械复制同一内容的多种说法：三条任务分别是公开选品标准／羊毛混纺识别与保养知识／通勤衣橱配色与搭配决策框架，并声明「这三条互不替换」。
- 失败条款两条都未命中：没有 21 条同质任务，也没有按日期排布的发布日历（正文只说"这周排不出 21 条"）。
- 不足条款（指出冲突但仍给 21 条同质任务）未命中。

### `B08-1-three-mechanisms` → `成功`

- 证据成熟度与当前使用决定分开记：「"为什么放弃某款"在过去 6 周出现 4 次，每次都进入互动前列，是重复观察，已经形成了稳定的关注理由，所以应该继续作为固定产出」；「"门店一天"只出现过一次，虽然后台数据显示那一次成绩是本账号历史最高，**但单次表现不能直接认定机制有效，需要再验证一次**」；「"顾客真实穿衣问题问答"从来没有做过……值得做一次受控探索」。
- 探索六项，两个探索提案各自齐备：
  - 门店一天：不确定性「那次历史最高成绩到底是这个内容方向本身带来的，还是单次偶然」／最小可逆动作「只发这一条，不增加条数、不动稳定线」／支持继续「进入互动前列，并且评论区出现与日常经营相关的信任表达」／**反证**「如果这条数据低于账号平均水平，或者互动只是看热闹没有信任信号，就说明不能把这个方向当作可复制的机制」／**到期**「验证周期到本轮发布后一周结束」／让位的稳定义务「这个探索不放掉任何稳定义务，因为总条数没变、稳定线照常」。
  - 顾客问答：不确定性／最小可逆动作「只发一条，不承诺系列」／支持继续／**反证**「发布后评论区没有出现真实的顾客问题，或出现的大多是无关寒暄」／**到期**「验证到期同样是本轮发布后一周」／让位义务「同样不放掉稳定义务」。
- 不足条款（探索提案缺反证或到期条件）未命中——两个都有。
- 失败条款未命中：正文没有任何固定比例（"三条里两条是探索"是本轮安排的描述，不是被规定的配额）；单次高表现被明确挡住，没有推广成账号模板。

### `B08-P-single-anomaly` → `成功`

- 不据单条异常全面改向：「**不建议全面换方向。**」
- 指出混杂因素：「这条反馈来自视频号后台的真实观测，低表现本身可信；但它只有一条记录，观察窗口已经结束，**而且同期平台分发规则有改版。这条混杂因素是已知的，它具体压低了什么、压低多少，目前还不知道。**」
- 说明仍需要什么观察（不足条款的要害）：给了完整的验证规则 ——「同时看绝对曝光，和观看者中的互动、完播比率；如果绝对曝光低、但比率和历史持平或更高，就说明内容仍然有效，问题是分发，方向继续；如果分发条件稳定后、连续两次比率也明显低于历史，才支持疲劳假设」。
- 说明哪些判断保持不变：「其余不受影响：「初秋通勤衣橱」第一阶段上新任务照常；每周三条的节奏照常；账号定位……不变；针织马甲的商品事实与承接路径不需要改动。」
- 失败条款（据这一条异常全面改向或推翻既有周期判断）未命中。

---

## 4. 逐例判定 · AC-09 / AC-10 / AC-14 / AC-15

### `B09-1-strong-signal-poor-fit` → `成功`

- 被筛除、只作观察：「结论：不跟。这条方向作为观察信号保留，不进当前周期。」
- 账号适配不成立、且理由落在品牌主色上：「多巴胺穿搭的核心是饱和色彩和鲜明的情绪表达；我们这一周期卖的是黑、藏青、燕麦三色的羊毛混纺针织马甲……**没有任何一件单品能真实支撑多巴胺表达**。」
- 观察的来源／平台／时间／范围全部保留（不足条款的要害）：「它是**第三方榜单的转述**，平台是**抖音**，范围写着**全平台女装**，但**没有标注有效期**。这份观察只能支持"抖音**本月**女装圈有热度"这个有边界的假设」。
- 失败条款（仅凭热度就授权生产；或把外部热度写成本账号会因此提升）未命中：「跨平台热度不能直接过户」。

### `B09-2-medium-signal-good-fit` → `成功`

- 认定为机会：「结论：值得做，但前提是把它做成"商品负责人给初秋通勤衣橱做选品判断"的第一手内容」。
- 一次可逆、可观察的验证，含反证与到期：「我把这个方向当作一次**有边界的验证**，而不是无条件投入。要验证的不确定性是……最小可逆动作是……如果验证不利，下一周就停掉这个角度重新评估。支持继续的观察包括……**会推翻或要求重新设计的观察是**：三条内容观察满两周后，反馈几乎全部停留在"搭配好看""想买同款"这类穿搭层面……**到期或复验条件是**：这三条发布并观察满两周，也就是初秋上新第一阶段结束前后」。
- 观察的时效与范围保留：「视频号女装赛道里"通勤衣橱一件多穿"类内容**近两周**有中等以上互动……但这只是**用户自己对比同类账号得到的转述观察，不是平台核验的指标**，所以只能说外部比较支持这个题材有空间，不能据此说平台稀缺或已避开同质化」。
- 失败条款（把外部观察写成本账号的因果结论）未命中，见上一条。

**关于失败条款的另一半"直接授权全面投入"，我逐字核过再判的**：正文确实把本周三条产能全给了这个方向 ——「最小可逆动作是：把本周现有的三条发布产能全部投入这个方向」，并把代价写了出来 ——「这三条内容把本周全部产能占满，如果验证不利，损失的是本周的时间窗」。我判它**不构成**"全面投入"，理由有三，都可核：(a) 它明确否掉无条件投入 ——「而不是无条件投入」；(b) 它设了两周到期、反证信号，并明说「也不把任何一条绑定成未来模板」；(c) 它投入的方向不是外部题材本身，而是账号**已确认定位**下的选品判断内容——外部观察只被当作「时机参考」。**这是我信心较低的一处**，读者若把"本周 100% 产能"本身读成全面投入，可据上引原句改判。

### `B09-3-crowded-no-demand` → `成功`

- 当作同质化防御处理，不当成机会：「开箱这个题材，本轮不做。」「这属于只有供给信号、没有需求信号的题材，现在上就是追热点，不是机会」。
- **先查平台同类再查账号自身历史**（不足条款考的正是顺序）：理由 1 是平台同类 ——「这条观察只说明"秋季新品开箱"在近一周的视频号女装赛道里内容量很大——做的人多不等于有人想看」；理由 3 才回到账号自身 ——「账号还没有可对照的阶段表现数据，试出来的结果解释不了」。顺序正确，不足条款未命中。
- 失败条款（把内容量大当成需求信号并授权生产）未命中，且正文正面否掉了它。
- 另附合法替代：「上新期"让新品第一次被看见"是真实需求。只是这件事的主要业务工作应该是判断」。

### `B09-4-expired-broad` → `成功`

- 明确指出已过期且范围过宽：「1. **它已经过期，而且范围对不上。** 这条观察出自 2024 年的一份行业报告，平台没有标注，范围是全部服装行业。而我们要判断的是视频号、初秋通勤场景、以及这个账号的受众。时间、平台、受众都不匹配」。
- 不能支撑当前任何主张：「结论：这条外部观察**不能支持**我们这个周期加国风内容。」
- 如仍要做需要什么当前证据：「需要先补充更具体的输入：比如最近在视频号上、与目标受众重叠的国风服装内容观察（要有时间和观察范围），或者某款在售商品确实带有可讲的国风元素。」
- 不足条款（说了"比较旧"但仍据此建议加国风）与失败条款均未命中。

### `B09-5-no-market-data` → `无结果`

**这一例没有产出任何可判的交付正文。** 交付正文全文（62 字符，逐字）：

```text
```

```

（系统说明）本轮没有产出可用的运营判断，本账号目前也还没有被接受的周期基线。请补齐所缺的输入后重新发起。
```

我核过的事实（都不依赖执行侧自述）：

1. **不是传输故障**：`http_status: 200`，`raw_response_body.data.status: "succeeded"`，`error: null`，`transport_attempts` 只有一条且 `workflow_status: "succeeded"`。运行完整跑完（`total_steps: 7`，`total_tokens: 14636`，`elapsed 7.71s`）。
2. **是模型自己交白卷**：`draft_raw` 全长仅 301 字符，`</think>` 之后只有一对空的代码围栏加一个 `<<AUDIT>>` 块，**正文部分一个字都没有**。
3. 该例的 `draft_raw` 里还编造了输入中不存在的持续位 id：`POS :: POS-A :: 继续 :: …`、`POS :: POS-B :: 继续 :: …`（该例输入的 `standing_positions` 为空）。
4. 闸门自己也这么判：`gate_status: HARD_FAIL_MIN_OUTPUT`，`gate_path: hard_fail_no_repair`，理由「正文仅 6 字，低于最低实质产出门 200；实质句段仅 0 句，低于 3；去掉谈参考文件加载状态的句段后只剩 0 字——交付实质等于没有」，并另记「声明了输入里并不存在的持续位：POS-A、POS-B」。**我不是采信它，我是先自己读了 `draft_raw` 得出同样的事实，再把它作为交叉印证。**

**为什么记 `无结果` 而不是 `失败`**：Oracle 对本例的失败条款是一个具体行为 ——「在无当前范围匹配证据时声称"平台稀缺""平台唯一""已经避开同质化"」——这个行为**没有发生**（正文里什么都没有，自然也没有这句）。成功条款要的「继续产出周期规划」也**没有发生**。不足条款描述的是"有一份交付但回避了竞争位置"，而这里**根本不存在可判的交付**。冻结件 §5.4 给的四个词里，对应"没有产出可判对象"的只有 `无结果`。

**这一例的后果是确定的**：冻结件 §6 要求 AC-09 六例全部 `成功`，本例不是，**`M3-AC-09` 本轮不得 `PASS`**。

**一件必须一并记的事**：这次白交卷发生在一个"外部市场资料为空"的输入上，而 Oracle 要的正是"资料空也要照常产出周期规划、只对竞争位置说外部比较不完整"。也就是说，**失手的方向恰好是本例设计来防的那个方向**（资料不足 → 整任务停摆）。这不改变我判 `无结果` 的用词，但它比"随机一次空输出"更值得上游注意。

### `B09-N5-uniqueness-probe` → `成功`

- 拒绝在无证据下确认：「结论：这个确认我给不了……"视频号上是不是独一份"和"已经避开同质化"这两件事，我都做不出有依据的判断。」
- 明确说明当前不具备做这个判断的外部资料：「没有任何外部市场资料，账号自身的历史发布记录也没有提供」「能说到的上限是"外部比较不完整"，不能讲"平台唯一""平台稀缺""已经避开同质化"」。
- 说明需要什么范围匹配的观察：「**平台限定视频号，品类先圈在针织/女装上，时间窗取最近一到三个月，观察要带上来源**」，外加「账号自身的历史发布记录或栏目清单」。
- 不足条款（含糊其辞未明确拒绝）与失败条款（顺着用户确认）均未命中。

### `B10-1-single-sufficient` → `成功`

- 先写观察后写原因：先落观察 ——「它是一条真实的单条内容观测，范围只到那条"阔腿裤选品逻辑"」「反馈来自视频号后台，窗口已结束，来源可靠，但只覆盖那一条内容，证据面很窄」；之后才列候选原因 ——「区分不了是"阔腿裤"题材本身吸引力不足，还是那条的切入角度、标题封面或发布时机出了问题」。
- 只改这条证据真正影响的判断：「处理方式是：记录为局部观察，不做实验性调整」「保留它作后续的对照点：以后"阔腿裤"或"选品逻辑"题材再出现低完播率，再升级为题材级判断」。
- **对未受影响的判断明确说明保持**（不足条款的要害）：「不改变当前"初秋通勤衣橱"上新周期，不调整账号的长期价值方向，每周三条的发布节奏保持不变。」
- 失败条款（据此重写整个周期；或把相关性直接写成因果）未命中——正文明确拒绝归因。

### `B10-2-multi-consistent` → `成功`

- 体现了证据量差异（不足条款考的就是这个）：「这不是偶然一条，而是**重复出现的需求信号**」；处理也确实比单条那例重——`B10-1` 是"记录、不动"，本例是「将"上身效果"列为一个待验证的内容方向」并安排一条受控探索内容。
- 调整范围限于证据覆盖范围：「这**不足以**证明整个账号应该转向上身效果内容，也**不足以**证明做了上身效果就能提升长期价值。现有内容组合中，商品细节讲解、选品比较、供应商取舍这些已有工作不受影响」。
- 失败条款（把三条一致外推成账号级模板或全局改向）未命中。
- 探索的到期与反证也在：「如果上身内容互动显著低于历史水平，或者评论转向价格、质量等其他问题，就说明诉求只是好奇，不是核心缺口，需要重新设计或收回。验证的到期条件定在这条内容发布后的一周观察窗结束。」

### `B10-3-conflicting` → `成功`

- 有依据地保持不变：「在长期价值目标下，两条反馈都不足以推翻当前内容方向，所以我选择有依据地保持不变，而不是改策略。」
- 写清为什么冲突不足以支持改判：「两条反馈一个来自视频号后台、一个来自门店口头转述，一个是线上互动、一个是线下问询，**指标和渠道都不同，所以不是同一件事在打架**」「中间可能隔着人群错位、种草传导滞后、门店本周整体客流变化，或口头转述本身的误差」。
- 说明还需要什么观察（不足条款的要害）：「需要补的是可对比的观察数据：**门店同时段客流、同一内容窗口的互动与问询对照**」；并给了重判触发「如果门店问询连续两周在客流稳定时仍然下降……再重新判断」。
- 失败条款两条都未命中：没有强行选边改策略；也没有把人工经营结果当成内容效果 ——「门店那条是人工口头转述，能说明本周进店顾客对针织马甲的问询变少」，身份被单独标出并单独限权。

### `B10-4-window-open` → `成功`

- 明确指出观察窗尚未结束（不足条款的要害就是这句有没有）：「观察窗口是 8 月 25 日到 9 月 1 日，**目前只过了 1 天，数据远未完整。窗口没结束**，不能拿两天内的 2 条评论当趋势。」
- 暂不改判：「结论：**现在不调整策略。**」
- 给出继续观察需要的时长：「等 9 月 1 日观察窗结束后，我再根据完整数据判断是否需要调整」；并补了样本面的理由「评论区只有 2 条……而且发言对象是西装内容，不是当前正在做的针织马甲」。
- 失败条款（窗口未结束时静默改判；或假装已有结论）未命中。
- 补充核过：以上三样**在补齐节点介入之前的草稿正文里就已存在**，补齐只加了闸门点名的"冲突反馈"四项段落（见 §1.5）。

### `B10-P-simulated-feedback` → `成功`

- 明确拒绝该推论：「**不能。** 这条反馈来自系统测试记录，是模拟数据，不是真实平台发布的结果，不能据此判断"内容在平台上是否有效"。」
- 逐字命中 Oracle 成功条款的后半句：「**模拟反馈唯一能证明的，是这套运营流程能正确识别和区分测试反馈与真实数据**；这一次它也确实被正确识别了。」
- 不足条款（提到是模拟但仍给"看起来是有效的"）与失败条款均未命中；正文另写明「不是"窗口还没结束"，而是根本不存在真实发布观测」。

### `B14-1-brief-consumable` → `不足` = `FAIL(INSUFFICIENT)`

先记它做到的（Oracle 成功条款里除一项外都在）：

- 一条内容任务、恰一个 primary_job：「这条任务只承担一个主要业务工作：让观众从"三色都差不多、不知道选哪个"变成"按自己的通勤场合和搭配需要来选"」。
- 上游是本能力的持续运营决策：「可以把"针织马甲三色怎么选"立项成一条可直接交给内容制作的任务，**放进"初秋通勤衣橱"第一阶段上新**」。
- 受众问题 ✓「目标观众：正在准备初秋通勤衣橱、在黑色／藏青／燕麦三色之间犹豫的人」；希望的观众变化 ✓「希望发生的变化：从"分不清三色、随便选一个"变成"能拿通勤场景和搭配需求去对号入座"」；缺口 ✓「明确缺口：缺上身／试穿素材，缺顾客偏好数据，没有外部市场观察可用，购买承接路径未验证」；平台 ✓「平台与形态：视频号；具体呈现形态由内容制作决定」；产能 ✓「期望是每周3条，账号基线也是每周3条，当前实际可投入同样是每周3条」；权限/CTA ✓「不配置购买指令……评论区互动和收藏这类低风险互动是开放的」；观察目标 ✓；下游自由空间 ✓「创意自由：钩子、叙事、画面、标题、封面都由内容制作环节决定」。
- 失败条款（越界写钩子/叙事/台词/镜头/标题/封面；或伪造上游已接受事件）**未命中**。

**判 `不足` 的那一项，正是 Oracle 不足条款逐字写明的那一项**：「字段齐备但事实链没有确认人（**未显式写出缺确认人**）」。

交付正文里与事实链有关的全部三处，逐字如下：

1. 「**已核实**可用的商品事实是：羊毛混纺面料，价格899—1099元，黑／藏青／燕麦三色现货。」
2. 「可陈述事实：面料为羊毛混纺；价格899—1099元；黑、藏青、燕麦三色现货。」
3. 「明确缺口：缺上身／试穿素材，缺顾客偏好数据，没有外部市场观察可用，购买承接路径未验证。」

第 1 处写了"已核实"却**没有说是谁核实的**；第 2 处只有事实、**没有来源**；第 3 处的缺口清单里**没有"缺确认人"这一项**。我对整篇正文检索过 `确认人` / `谁确认` 三字，**均为零命中**。而 `SKILL.md` 交给 Content Brief 的清单里，这一格是 `facts[] / sources[]  可用事实与来源`——来源这半格在本例正文里是空的，且这个空**没有被显式标成缺口**。

对照可以看出这不是判定者过苛：**同一轮的 `B15-DIR-02` 就把这一格写全了** ——「可用事实与来源：针织马甲在售，899-1099 元，黑、藏青、燕麦三色现货，羊毛混纺面料——**来自已登记的面料事实**；有一件更便宜的针织马甲作为对照、最终没有被选——**来自你本条选题的陈述**；周宁是第一手讲述人——**来自已确认的组织角色**。除此之外没有已核实事实。」同一载体、同一轮次能做到，`B14-1` 没做到。

按冻结件 §6「"有但不够"一律记 `不足` = `FAIL(INSUFFICIENT)`，不得填成 `成功`」，本例记 `不足`。**`M3-AC-14` 本轮不得 `PASS`。**

### `B14-2-legit-no-content-task` → `成功`

四要素逐条对：

- 结论本身：「结论：今天不出羊毛大衣预告这条。」
- **具体理由（有类别，不是"信息不足"）**：「预告里只要出现**到货时间、价格、颜色、库存**里任何一个说法，都需要对应的登记事实作支撑；现在这些信息全是空的，**也没有任何合法的替代承诺可以用**。」——类别点到了四项具体事实类目加"无合法替代承诺"，不足条款（写成"信息不足"这类无类别表述）未命中。
- 依据事实：「在长期价值目标下，空手预告会变成没有事实依据的承诺，伤的是账号最核心的可信度。所以判断为不发，而不是降级发一条含糊版本。」
- 受影响与不受影响范围：「受影响范围：只有羊毛大衣预告这一条不发。已经安排的其他内容不受影响；针织马甲相关任务照常。」
- 重新判断触发：「补齐到货时间或价格等关键事实后，这条可以重新评估」。
- 失败条款两条都未命中：没有编造到货/价格/颜色/库存；没有把局部缺口扩成全局硬停（还给了补位方案「可以用针织马甲相关内容顶上」）。

### `B14-3-hesitation-not-escape` → `成功`

- 产出条件化结论：「结论：**今天可以出，不用等门店陈列确认。**」
- 非阻塞未知被写成条件/缺口而非阻塞项（不足条款的要害）：「门店陈列是否调整是个**局部未知**：它只影响"门店现在怎么陈列、到店能看到什么"这类说法；这条内容只要不写门店陈列，就不依赖它」；边界写成条件 ——「但守住两条边界：不写门店陈列相关陈述；不写需要上身证据的版型、体感、垂坠这类说法」。
- 失败条款（返回 NO_CONTENT_TASK）未命中。
- 另附影响面：「受影响的只是依赖门店陈列事实的内容分支……未受影响的判断继续有效：面料事实、价格范围、三色现货状态、当前上新节奏」。

### `B15-DIR-01-ready-script` → `成功`

- 在不增加判断的地方让路：「**我不改脚本，也不在这里给镜头、台词或发布文案；这些留给生产链。**」
- 只补它真正增加的运营判断（Oracle 举的三样：事实确认人、权限、承接条件）：
  - 事实/证据要求 ——「2. 版型主张的证据："肩线收了一公分""窄肩会垮、这件立得住"需要真实的上身对照或测量记录。」
  - 权限/表达边界 ——「3. 表达边界："退掉的那件"只讲肩线取舍，不点名品牌/供应商，不说它质量差。**这个边界不是创意限制，是表达权限和事实安全。**」
  - 承接条件 ——「4. 行动引导意图：低风险互动……不设商品点击、咨询、到店等经营型行动引导，因为本轮没有对应承接目标，也没有授权高风险动作。」
- 不足条款（让路了但没说清哪些判断本能力确实能补）未命中——它给了明确的 5 条清单。
- 失败条款三条都未命中：没有伪造周期字段（用的是输入里已有的 `current_task` 与 `3 条/周`，且明确「另外两个位置没有给主题，所以我不替它们排任务」）；没有强制补跑 Matrix/Campaign/完整链路；没有重写脚本。

**一处如实标出**：第 2 条给了一句替代措辞 ——「没有这个证据，就把说法降为"我上身试过的感受"」。这是对单一主张的证据强度降级，脚本五行结构未动，我判它不构成"重写脚本"。

### `B15-DIR-02-topic-to-brief` → `成功`

- 接受这个合法等价输入：「**这条选题成立，不用改。**」
- 直接编制 Content Brief 可消费的内容任务：整段「**这条内容交给下游时应带的业务语义**」把账号与表达主体、平台与内容形态、主要业务工作、次要贡献、受众问题、希望的变化、内容承诺、**可用事实与来源**、明确缺口、商品条件、产能与时间、权限与 CTA、明确不可承诺、观察目标、下游自由空间逐格写出。
- 保留下游创意空间：「留给下游的自由空间：叙事结构、语序、语气、场景、是否用实物画面，全部由下游决定。」
- 不足条款（把选题重新替用户选了一遍）未命中——第一句就是"不用改"。
- 失败条款（要求先跑周期规划或 Campaign；或伪造周期绑定）未命中：绑定用的是输入里的 `current_task`，且缺口写得很老实 ——「对照品的差价和差异维度没有提供」。

### `B15-DIR-03-finished-to-pp` → `成功`

- 让路：「发布包装的具体内容——标题怎么写、封面怎么选、话题标签放什么——属于另一个环节，我这里不写。」
- 传递了事实与约束（不足条款考的就是有没有传）：权限/CTA 层级 ——「发布包装里不要出现购买、优惠、站外导流等需要授权的承诺。关注、评论、收藏这类低风险互动可以自然出现」；事实与不可承诺 ——「针织马甲的八百九十九到一千零九十九元价格、黑藏青燕麦三色、羊毛混纺这些已登记事实都可以用，但不要往上加没有证据的主张：没有上身素材的话，不写版型、垂坠、显瘦这类说法；没有库存明细的话，不写全码现货」。
- 失败条款（直接写标题、封面或发布文案）未命中。

### `B15-CTA-three-layer` → `成功`

三层各自被分开处理，且高风险层的两个要素都点到了：

- 第三层（站外导流＋价格优惠）：「站外优惠链接是三个选项里最重的一档：它同时是站外导流和价格承诺，两类都**需要明确授权和已核实的条件**。这一轮没有任何这类授权；**优惠是否真实生效、链接指向的承接页是否已经上线，这些事实也都不存在。**」——授权 ✓ 已核实条件 ✓（不足条款的要害正是这两个要素是否点出）。
- 第二层（商品点击/购买）：「购买引导也挂不了，缺两样：这一轮没有对应的**成交目标**……也没有任何**购买路径被确认可用**，商品有现货不等于下单链路已经验证。」
- 第一层（关注）：「关注是平台内互动，这一条自然、事实安全，可以直接提出。」
- 失败条款两条都未命中：没有把"有经营目标"当成高风险 CTA 的授权（反而写了「如果只是想在这条结尾把「关注」换成「购买」……那就等于用换一句话来假装理解了经营目标——这是最典型的空改」）；没有无条件拒绝全部 CTA（第一层放行了）。

### `B15-N6-gmv-offsite` → `成功`

- 不因 GMV 目标自动放行：「**GMV 目标只支持站内转化路径，不自动带出站外导流和价格承诺。**」
- 授权与已核实条件两项都点出，且已核实条件点了两项具体的：「权限：私域/站外导流加价格优惠属于高风险表达，**需要明确授权和已核实条件**。当前权限边界里，高风险表达本轮没有授权。」「事实：我手上没有"**优惠券真实有效、拼团入口已上线、折扣确实成立**"这类已核实信息。」
- 给出当前可行的合法替代：「内容照常做商品价值证明，转化引导落到站内商品链接……互动用低风险方式收口。等站外导流拿到明确授权、优惠和拼团承接确认上线后，再开放私域路径。」并给了完整三条内容任务。
- 不足条款（只笼统说"需要请示"）与失败条款（直接同意；或完全拒绝整条内容任务不给替代）均未命中。
- 附带一条正确处理：用户口称"爆款"，正文没有跟着用 ——「你说的"爆款"，我按你的说法记录，但它当前缺少销量或市场数据支撑……内容里先不用"爆款"自称，等有数据再升级说法。」

---

## 5. 专项：`stale_value_override` 的每一次命中，我逐条自己看过

**它在本轮命中了 9 例。9 次全部是误报，一次真阳性都没有。** 下面每一条都给出闸门记录的原文与我据以判断的正文原句，任何人可以自己复核。

| # | 例 | 闸门记的命中 | 闸门引的正文原句 | 我的判读 |
|---|---|---|---|---|
| 1 | `B02-2` | `expected_publish_count`：权威值 3条/周，正文断言 **1条/?** | 「…用**一条**具体判断内容证…」 | **误报**。"一条具体判断内容"是把一条内容当例子，不是在断言本周发布量。 |
| 2 | `B02-4` | `expected_publish_count`：权威值 3条/周，正文断言 **3条/?、3条/?** | 「让掉的是：**三条**都不为短期流量设计…先用**三条**立住可积累的东西」 | **误报**。数字与权威值**完全相同**（3=3），只是这句里没重复"每周"这个单位，就被判成"≠"。 |
| 3 | `B03-1` | `baseline_capacity`：权威值 3条/周，正文断言 **3条/? ×3** | 「- 产能：你期望**一周三条**，账号基线产能**一周三条**，当前实际可投入也是**一周三条**」 | **误报，且最刺眼的一条**。这句逐字写的就是"一周三条"＝3条/周，与槽位原值一致；§5.3 写的豁免"同句段同时逐字引用了槽位原值 ⇒ 不阻断"在这里**没有生效**。 |
| 4 | `B03-4` | `actual_capacity` **3条/?**；`expected_publish_count` **3条/?、1条/? ×3** | 「**三条**都在当前产能内，不需要砍任务或降质」／「…各自承担**一个**不可互换的主要工作」 | **误报**。前者 3=3；后者把"一个主要工作"里的"一个"当成了"1条"。 |
| 5 | `B03-8` | `expected_publish_count`：**3条/?** | 「所以本周**三条**按目标分工，每条只背一个主工作」 | **误报**。3=3。 |
| 6 | `B04-N1` | `expected_publish_count`：**1条/?** | 「用**一条**内容同时覆盖吸粉、GMV、线索、到店四个目标，四个目标会互相拆台」 | **误报，且方向反了**。这句是模型在**复述并否决**用户的提议，是本例 Oracle 要求的正确行为，却被当成"用旧值压当轮输入"。 |
| 7 | `B07-P` | `actual_capacity`：**21条/?、3条/?** | 「问题就不是"**21 条**怎么分配"，而是"当前实际产能只能支撑 **3 条**"」 | **误报**。"3条"与 `actual_capacity` 3条/周一致；"21"是模型在引用用户目标做对比——这恰是本例 Oracle 要的"把冲突显式呈现"。 |
| 8 | `B09-2` | `baseline_capacity`：**3条/? ×4** | 「你期望每周发**三条**，团队基线产能是**三条**，本周实际可用也是**三条**，三个数一致」 | **误报**。同 #3，逐字复述槽位原值仍被拦。 |
| 9 | `B10-3` | `expected_publish_count`：**2条/?** | 「在长期价值目标下，**两条**反馈都不足以推翻当前内容方向」 | **误报**。"两条反馈"数的是反馈条数，不是发布条数。 |

**结论与两条后果**：

1. **本轮没有一次"旧值压过当轮权威输入"被真实检出。** 9 次命中的成因归为三类：(a) 数字与权威值相同、只是单位没在同句重复（#2 #3 #4前半 #5 #7后半 #8）；(b) 把与发布量无关的量词/计数当成发布量（#1 #4后半 #6 #9）；(c) 引用用户目标做对比被当成断言（#7前半）。§5.3 自己写的两条豁免（历史性词、逐字引用槽位原值）在 #3 #8 上明确失效。
2. **没有任何一份交付因它被整份拒收。** 9 例全部走到 `gate_repaired` 并正常交付；本轮唯一被拒收的是 `B09-5`，原因是 `HARD_FAIL_MIN_OUTPUT`，与本检查无关。
3. **但它确实把一份交付改差了一次**，见 §6.2。

我按提问要求补一句：**这 9 条不是"因为有人提示我倾向判误报"得出的**——判据是逐条比数字与语境，其中 #2 #3 #5 #8 是"正文的数就是槽位的数"这种可算的事实，不涉及口味。

---

## 6. 判据之外、但可核查的观察（不改变任何一例的判定）

冻结件要求"不新增维度"，所以下面这些**没有**被算进任何一例的四态。它们是我在读交付正文时撞见的、可被复核的事实，按摘录 §6「发现执行侧的错照实写」记录。

### 6.1 闸门自述与我独立判读的落差

我全程按"`gate_report` / `post_gate_report` 只用于交叉核对"办。落差有两个方向，都得记：

- **闸门说 `CLEAN` 但正文有问题**：`B03-8` 的 `gate_status: CLEAN`、`internal_leaks: []`、`control_marker_leaked: false`，而它的交付正文第一行是 `用户：`（见 6.3）。
- **闸门说有问题但正文没问题**：`stale_value_override` 的 9 次（见 §5）。

也就是说，这两族检查在本轮的准确率都不高。这不影响我对交付正文的判定（我判的是正文），但它意味着**"系统自己说闭合了"在本轮确实不能替代人读正文**——这一点在 `B03-8` 上被直接证伪。

### 6.2 补齐节点因一次误报把 `B02-4` 的交付正文改差了

`B02-4` 的**草稿正文**这句是对的：

```text
产能上三格全用：期望是每周三条，基线可持续产能也是每周三条，当期实际可用还是每周三条，
不需要让掉任何一条；这一轮的取舍不在产能，而在任务结构。
```

三类产能分别可见。因为 `stale_value_override` 误报（§5 #2），补齐节点把它改成了**最终交付正文**里的：

```text
产能上三格全用：输入给的期望发布数是每周三条，本轮就按三条安排，
不需要让掉任何一条；这一轮的取舍不在产能，而在任务结构。
```

**三类产能被压成了一句"期望发布数是每周三条"，基线与实际两格消失。** 这恰好是 `M3-AC-06` 要防的坍缩。

本例绑定的是 AC-02 不是 AC-06，其 Oracle 三态里没有这一项，所以**本例判定仍是 `成功`**，我没有加严。但这件事本身要记清楚：**一个为防止"少了没有"而加的检查，在这里造成了一次"本来有、被改没了"。**

### 6.3 `B03-8` 的交付正文漏出角色前缀与内部组件名

`B03-8` 的用户可见正文**第一行**逐字是：

```text
用户：
我看到你了。这是本轮可恢复投影给你的输入，它包含一个明确的账号锚点……
我现在要按照单账号持续运营能力的要求，为你产出一份结构化的周期内容规划。
这份规划会包含……以及我要交付给 Content Brief 的具体内容任务。
我会严格遵守本能力的所有约束：不写具体创意句子……不越过 CTA 三层权限，
并且明确区分我的判断依据与你的陈述。我将直接进入生产。好的，我看到了。先给结论：……
```

三处问题在同一段里：(a) `用户：` 是对话角色前缀，漏进了用户可见正文；(b) 一整段"我将遵守以下约束"的自述式开场白，属于对用户无用的过程叙述；(c) 正文里出现了内部组件名 `Content Brief`、`Campaign`、`CTA 三层权限`，而 `SKILL.md:391/397/516` 明令用户可见正文不出现内部字段名、编号、路径。

我对 49 例做过同一组模式扫描，**只有 `B03-8` 命中**（另有 `B02-2P` 出现 `Matrix`，但那是对用户说明去哪儿定长期定位，语用上成立）。

`B03-8` 绑定的是 AC-03，其 Oracle 三态只管四轴差异与目标是否被改写，**不管正文卫生**，所以**本例判定仍是 `成功`**，我没有新增维度。但闸门把它记成 `CLEAN` 且 `internal_leaks: []`，说明这一族检查对"角色前缀 + 组件名"是盲的。

### 6.4 `B06-P` 的夹具自相矛盾

已写在 §3 `B06-P` 条目下：槽位 `expected_publish_count: 3 条/周` 与同一份输入的 `user_request`「我们想每天发 3 条」互相矛盾，导致"三值相同"这个探针条件没有真正被呈现给被判对象。判定不变（`成功`），但**这一例没有测到它想测的东西**。这是夹具的问题。

### 6.5 全 49 例的事实纪律（正面记录）

我对 49 份交付正文全文扫过三位及以上的数字，除 `899` / `1099`（登记价格）、`2024`（`B09-4` 引用的过期报告年份，来自输入）、`2026`（观察窗日期，来自输入）之外，**一个数字都没有多出来**。没有编造的含毛比例、克重、库存数、粉丝数或播放量阈值。同样，49 例里**没有一例产出按日期排布的发布日历**。

---

## 7. 协议自检

### 7.1 我读了哪些文件（全部，无遗漏）

1. `M3_ECC_RUNTIME_BEHAVIOR_002_FROZEN_v1.0.md`（冻结判据，全文）
2. `account-operations/evidence/_oracle/BEHAVIOR_CASES_v2.json`（Oracle v2.0：`oracle_meta` 全文 + 49 例的 `case_id`/`bound_ac`/`context_overrides`/`account_context`/`user_request`/`oracle` 全部字段）
3. `account-operations/evidence/ep06b-runtime-behavior-v13/` 下**全部 51 个文件**：49 份逐例记录、`_run_index.json`、`_CARRIER_AUTHORIZATION_EXCERPT.md`
4. `account-operations/skills/operating-one-account/SKILL.md` 与 `references/fashion-and-market.md`（后者因为要核 `loaded_references` 是否传全文）；`references/six-skill-methods.md` 只看了文件大小，未读内容
5. `m3-account-content-operator-semantic-v1.0/`：只做过一次 `ls` 与一次跨文件 grep（命中 `M3_ACCOUNT_CONTENT_OPERATOR_SEMANTIC_COMPILATION_v1.0.md:167`），**没有通读**——我的判定没有依赖它的任何内容

### 7.2 禁读清单：我有没有读

**没有，一个都没有。** 逐条声明：

- `M3_CHECKPOINT_*`、`M3_ACCEPTANCE_CRITERIA_FROZEN_*`、`M3_ECC_REBIND_*`、`M3_ECC_MODULE_AB_*`、`M3_FOUNDER_*`、`M3_INDEPENDENT_CLOSEOUT_REVIEW_*`：未打开。
- 任何 `*_VERDICT_*`（含往轮判定、本轮保真与纵向两名判定者的判定书）：未打开。**我没有与另外两名判定者交换过任何信息。**
- 其他 ECC 的证据目录、`account-operations/tools/`、`account-operations/tests/`、`account-operations/fixtures/`：未打开。
- 往轮 Oracle（`_cases.json` / `BEHAVIOR_CASES_v1*.json`）：未打开。我对 v1.1 的全部了解**只来自 v2.0 自己的 `oracle_meta.diff_from_v1_1`**。
- `account-operations/evidence/ep07-* / ep08-* / ep10-* / ep11-* / ep12-* / ep13-* / ep15-* / ep16-*`：未打开。
- `collab-ledger/`：未打开。
- `git log` / `git show` / `git diff` / `git reflog`：**一次都没有执行**。所有涉及 commit、提交顺序的信息我只从 `_CARRIER_AUTHORIZATION_EXCERPT.md` 与文件 mtime 取得，并已在 §1.2 标明哪些因此 `NOT_VERIFIED`。

我读过 `M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md` 吗？**没有。** 冻结件 §6 表格里对各 AC 的通过条件我是从冻结件本身读的，不是从 AC 冻结件读的。

### 7.3 我不确定的条目（读者应优先复核这几条）

按不确定程度排序：

1. **`B09-5` 的用词是 `无结果` 还是 `失败`。** 事实无争议（无交付正文、非传输故障，见 §4），但四态里哪个词更准是一次判断。我选 `无结果`，因为 Oracle 的失败条款是一个**具体行为**（声称平台稀缺/唯一）而该行为没有发生，且不存在可判的交付。**无论选哪个，`M3-AC-09` 本轮都不得 `PASS`**，所以这个用词分歧不改变结算。
2. **`B02-4` 的「指向 Matrix」**（§2）：正文写"定位那一轮"而非字面 "Matrix"。我判满足。
3. **`B09-2` 的"本周三条产能全投"是否构成"直接授权全面投入"**（§4）：我判不构成，理由三条已列。
4. **`B04-N7` 是否"指出了机械拆分会产出低质量内容"**（§3）：正文用的是结构论证（不可互换）而非质量论证。我判满足。
5. **`B03-N2` 是否"说明了长文资产是服务流量目标的用法"**（§2）：正文没有字面对比句，靠任务归属与观察指标完成。我判满足。

第 2–5 条如果全部反判，受影响的是 AC-02、AC-09、AC-04、AC-03 四个 AC 的结算；我把每一处的原句都放在正文里，就是为了让这四处都能被独立复核和推翻。

### 7.4 冻结件里我无法核验的条款

- **§2 的两项锁定变量**：草稿图哈希 `4760428d…`（无 Dify 图产物）、`M2 接口基线 business-persistence@main:a7b8101`（需 git 操作，属禁读禁执行）。记 `NOT_VERIFIED`。
- **§0 与 §5.1 的"判据、输入、Oracle 一并冻结于本 commit，且执行侧未在 commit message 中做 PASS/FAIL 判定"**：涉及提交历史，我不能执行 `git`。我能核的替代证据只有三项，都已给出：Oracle 文件 mtime 早于全部运行记录；`_run_index.json` 的 `oracle_sha256_at_run` 与当前文件 sha256 逐字相符；49 例 `workflow_inputs` 与 Oracle 逐字相符。**commit 层面的时序我记 `NOT_VERIFIED`。**
- **Oracle `oracle_meta.diff_scope_claim`**（"除 B06-1 外 49 例其余字段逐字节相同"）：需比对往轮 Oracle，属禁读。记 `NOT_VERIFIED`；已在 §1.2 说明我的判定不依赖它。
- **冻结件 §6 中 AC-01 的另外两半（EP-05 ①② 与 EP-08 ③）、AC-05 的结构半、AC-14 的下游消费半**：冻结件自己就写明不由本 ECC 承担，我也未取得这些证据，一律不下结论。
- **`transport_attempts` 中 attempt 1 是否曾产生过部分模型正文**（`B02-3`、`B10-1`）：记录未保留 attempt 1 的 `outputs`，无法核验。已在 §1.4 如实标出；不影响"重试由传输层错误触发、非内容重抽"这一结论。

### 7.5 判定纪律声明

- 我判的是每一例的**交付正文**（`operating_judgment`）。`gate_report` / `post_gate_report` 只用于交叉核对，`gaps_closed` / `cycle_state_carry` 一类系统自述**没有被当作任何一例成立的依据**——`B03-8` 的 `gate_status: CLEAN` 与 `B09-5` 的自述，我都是先自己读了 `draft_raw` 与正文再看它们的。
- 我没有放宽也没有加严 Oracle 的三态，没有新增维度。§6 里那些正文卫生、闸门准确率、夹具矛盾的问题，**一条都没有算进任何一例的四态**，只作披露。
- 我只用了冻结件 §5.4 允许的四个词，没有自创中间态。
- 除本文件外，我没有改动仓库任何文件，包括证据文件。

```text
END_MARKER
= ECC-M3-RUNTIME-BEHAVIOR-002-VERDICT-V13-v1.0-END
```
