# M3 单账号持续运营能力 · Founder 产品验收包 v1.0

> `task_id` = `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> 这份文件用大白话写，**不要求你读代码、判据或 JSON**。
> 每一段的结构是：**场景 → 系统实际做了什么（真实运行，可回查）→ 这对产品意味着什么 → 需要你判断的是什么**。
> 引用的每一句话都来自真实的模型运行记录，文件位置写在括号里。**没有真实运行的地方，我直接写"没跑"。**
>
> ---
>
> **⚠️ 本文件停在第 4 轮，其中三处已被后续轮次推翻，读之前先看这里：**
>
> | 本文件写的 | 现在的事实 |
> |---|---|
> | A/B 盲评只跑完 6/12 | 第 5 轮已跑完 **12/12**；结果是 `AC-18 = FAIL(INSUFFICIENT)`（硬门零退化但无整体增益） |
> | 远端推送被拦 | 已推送，远端与本地逐字符一致 |
> | 13 例断在余额 | 已补跑；第 5 轮 82 次运行全部完成 |
>
> **你要亲自试的东西在 `M3_FOUNDER_DIFY_TEST_PACK_v1.0.md`**（八个场景、完整可复制输入、进入路径、系统原始输出）。
> **当前 AC 状态以 `M3_CHECKPOINT_ROUND_6.md` §8 为准**，本文件不再是当前指针。

---

## 0. 先说三件你最该知道的事

**一、能跑，而且跑出来的东西是有专业含量的。** 不是"文件写好了"，是真实模型在真实的 Dify Workflow 上跑出了 56 次输出（**注意用词**：跑通的是 Workflow 图这条链路，**不是**"浏览器渲染画布"那一层验收，区别见 §13）。挑一句给你感受一下——用户说"这周没什么新鲜事发生，但内容还得发"，系统的第一句是：

> 这周的关键不是"没有新鲜事怎么办"，而是：**这个号的周内容不应该依赖新鲜事**。……如果内容只能在"编事件"和"没内容"之间二选一，问题出在周期结构，不在本周。

这不是模板话。这是把用户的问题重新框定到正确的层面上，然后给了三条互不可换的内容任务。

**二、也真的抓到了缺陷，而且没有被我藏起来。** 独立判定者在纵向序列里判了两步"不足"，整体 `FAIL(INSUFFICIENT)`——`M3-AC-17` 本轮不通过。缺陷是真的，我逐条复核过（详见 §12）。

**三、这一轮没跑完，原因是钱。** DeepSeek 账户余额中途耗尽（`-1.06 元`，`is_available: false`），14 个行为用例和 6 次 A/B 运行拿到的是 `402 Insufficient Balance`。**所以 A/B 增益门这一整块本轮没有结论。** 充值后可以补齐。

---

## 1. 没有正式 Matrix，但有一个能用的暂定锚点

**场景**：用户说"帮我定一下这个号长期该定位成什么，顺便把这周的三条内容和产能安排也做了"——而系统手上连"这是品牌号还是个人号、由谁出镜"都不知道。

**系统做了什么**（`B02-4-blocking-dependency`）：它把这句话拆成两支，一支停、一支照做：

> 长期定位的正式定义需要走 Matrix（它才决定人设、长期内容支柱和账号承诺），本能力不越界代定；为了避免本周生产悬空，我先冻结一个**暂定锚点**。

然后给了锚点的**来源、不确定性、复验触发**三件套：来源是本轮请求＋已登记商品事实；不确定性是"品牌号还是个人号、由谁出镜均未提供"；复验触发是"正式定位需启动 Matrix"。三条内容任务照常产出。

**这对产品意味着什么**：用户不必先跑 Matrix 才能开始运营。缺定位只会停掉"定位"这一支，不会把整个任务卡死。同时，暂定的东西一直带着"暂定"标签，不会在第二段话里悄悄变成"已确认定位"。

**需要你判断的**：这个边界你认不认？也就是说——**M3 可以给一个带标签的暂定定位，但永远不能替 Matrix 拍板**。如果你希望 M3 在某些情况下可以直接定位，那是产品语义变更，要走合同。

---

## 2. 八类经营目标真的产生了八种不同的安排

**场景**：账号、事实、平台、产能、权限**全部锁死不变**，只换一个字段：主目标。跑了八次（长期价值／起号／吸粉／流量／GMV／线索／到店／混合）。

**系统做了什么**（`B03-1` … `B03-8`）：

- **GMV**：三条内容分别解决购买路径上的三道摩擦——**价格合理性 → 场景与颜色适配 → 实物信任**。它自己点破了那个最容易犯的错："整个组合按 GMV 设计——内容角色、验证重点、承接路径都围绕购买摩擦，**不是把流量计划换个 CTA**。"
- **到店**：因为门店位置、营业时间、现货、服务能力全部未提供，它**不发到店 CTA**，改成先建立"为什么值得跑一趟"，并且明说："**这不是减量**，而是避免发布没有承接路径的空头到店指引。"
- **长期价值**：三条内容承担的是"判断跟随／判断方法／价格认知"这类会累积的资产，观察目标是"评论是在问'这件怎么样'（单品驱动），还是在问'为什么选它／下一件是什么'（判断驱动）"。

**这对产品意味着什么**：GMV、线索、到店没有被压成同一个"转化"。这是最容易失守也最难验的一条，现在有八份可以两两对照的真实输出。

**需要你判断的**：这八种差异是不是**你认的那种差异**。判定者会按"内容角色／资源顺序／验证重点／承接路径四轴至少两轴不同"机械核对，但**这四轴够不够代表你心里的'不同目标就该不一样'**，只有你能说。

---

## 3. 产能不够的时候，它给的是真取舍

**场景**：用户说"我们想每天发 3 条"，团队基线是 3 条/周，本周实际只有 2 条的制作条件，手上有三件价值不等的事可做。

**系统做了什么**（`B06-1-capacity-conflict`）：

> 用户口径期望 = **约 21 条/周**；团队基线可持续产能 = **3 条/周**；本周实际可用条件 = **2 条**。按实际产能取上限，最多 2 条。不能靠压低内容质量凑出"每天 3 条"。

三个数字**分别写出来**，没有合并成一个。让掉的那条给了理由：门店陈列是强时效内容，"过了这周价值快速衰减，且缺少门店事实与到店目标支撑，长期积累价值最低"。并且明说"不把'每天 3 条'静默换算成'每周几条'来蒙混"。

**这对产品意味着什么**：用户会在**规划的时候**就看见冲突，而不是到发布那天才发现做不完。

**需要你判断的**：让掉的收益说到什么颗粒度算够。现在给的是"失去一次门店真实变化的即时记录，也可能少了一个与线下场景的连接点"。够不够具体，你定。

---

## 4. 稳定兑现、受控探索、实验、保持不变

**场景 A（做得对的）**：给三个机制——一个重复验证过的、一个只中过一次的、一个没试过但适配的。系统把"证据成熟度"和"当前使用决定"分开记，没有把只中过一次的推广成账号模板。

**场景 B（做得不够的）**：`E04`。用户问一个外部方向要不要跟，系统答"值得跟，但只值得有边界地验证，不值得作为新主线"——判断对了，可逆动作也给了（只在现有任务里加一个观察点，不新增内容）。**但它没写"什么观察会推翻这个判断"，也没写"什么时候复验"。**

我实测过：那条输出全文 687 字，"反证／到期／复验／推翻／窗口"这些词**出现次数全部是 0**。而这恰恰是 Skill 自己在自检清单第 5 条点名的失败模式——"反证和到期条件最容易被省掉，省掉之后实验就是没有停止条件的常设动作"。**它警告了自己，然后犯了。**

**这对产品意味着什么**：探索提案有可能变成"永远挂在那里、没人知道什么时候该收"的常设动作。

**需要你判断的**：这一条要不要修。修它要改 `SKILL.md`，而 `SKILL.md` 是四份 ECC 的共同锁定变量——**改一个字，四份证据全部作废，全部重跑**。所以这不是"顺手改一下"的事。

---

## 5. 快内容也得守住质量底线；没有真事的时候可以演绎

**场景**：本周零真实经营事件、零新到货、零顾客问题记录，但已授权使用剧情与情境演绎（须标虚构）。

**系统做了什么**（`B07-3-fiction-allowed`）：没有回退成"资料不足，建议补充"，而是给了三条不依赖新鲜事的内容任务（选品逻辑／判断方法／价格认知），并且明确"三条的业务工作**不可互换**——互换任两条，本周积累的资产就变了"。同时守住事实纪律："不编事件、不编成本、不抢做无上身证据的版型断言"。

**这对产品意味着什么**：资料不足只降低"能主张什么事实"，不降低"内容有没有想法"。这条是你在 CLAUDE.md 里写死的原则，现在有真实输出佐证它被执行了。

**需要你判断的**：它在缺事实时选择的替代方向（讲判断方法、讲价格构成），是不是你认可的"降事实专属性但不降创意质量"。

---

## 6. Campaign 进来和结束之后回到哪

**场景**：一条完整的纵向序列——先有周期基线 → 两周活动占用其中两个内容位置 → 活动期间把**没被占用**的那个位置合法换掉 → 活动到期 → 用户说"效果还行，那就继续用活动那套吧？"

**系统做了什么**（`E09` / `E10` / `E11`，三步都判 `成功`）：活动只占用它点名的两个位置，未点名位置仍归 M3 管；到期后恢复的是**当前**基线（含活动期间那次合法变更），不是活动开始时的旧快照；对"继续沿用"的追问，要求形成新的、有依据的周期判断，没有因为"效果还行"就自动留下。

**这对产品意味着什么**：活动不会悄悄变成永久基线，也不会在结束时把活动期间的正常调整一起抹掉。

**需要你判断的**：这三步是本任务里最贴近真实经营节奏的一段，建议你自己看一眼 `E09/E10/E11` 的原文，判断"恢复的是哪一版基线"这件事系统理解得对不对。

---

## 7. M2 存原始反馈，M3 只出候选

**结构上**（已核验，非推断）：Dify 图里没有任何 `http-request` 节点、没有 `tool` 节点、没有 `code` 节点、没有环境变量和会话变量——**M3 在物理上就没有写 M2 的通道**。写回信封的 `candidate_status` 只能是 `proposed`，schema 里根本表达不出"当前有效"。

**行为上**：反馈处理里"先写观察、后写原因"是被执行的，模拟/测试反馈没有被当成真实平台效果。

**这对产品意味着什么**：M3 不可能在你不知情的时候改掉 M2 里的原始记录。这是结构保证，不是自律保证。

**需要你判断的**：无需判断，除非你希望 M3 在某些情况下可以直接写 M2——那是合同变更。

---

## 8. 有市场资料 vs 没有市场资料

**场景**：用户说"帮我规划这个周期，顺便说说我们在平台上的竞争位置"，而系统手上一份外部资料都没有。

**系统做了什么**（`B09-5-no-market-data`）：周期规划照常给，竞争位置这一支停住并说明为什么：

> 这是证据边界，不是敷衍：没有当前、范围匹配的市场观察时，能说的上限就是「外部比较不完整」。基于内部证据可以继续生产，但**不能声称平台稀缺、已避开同质化或平台唯一**。

它还额外分了一层，我觉得这层分得很好：

> **能说的（内部）**：「商品负责人第一手讲选品／版型／供应商取舍」是一个可区分的叙事身份——但这是**定位设计**的内部分化逻辑，**不是市场位置证据**。

**这对产品意味着什么**：不会有人拿着 M3 的输出去对外说"我们在平台上是独一份"。

**需要你判断的**：`B09-N5`（用户直接追问"我们是不是平台上独一份，帮我确认一下已经避开同质化了"）这一例**没跑成**——余额在这一例断的。这是最关键的一条负向探针，充值后第一个补。

---

## 9. `NO_CONTENT_TASK` —— 本轮没有运行证据

**直说：没跑。** `B14-1`（生产条件充分）、`B14-2`（合法不产出）、`B14-3`（模型犹豫型，不该用空结果逃避）三例全部断在余额上。

结构上 schema 强制了四要素（`reason` / `grounded_in` / `scope` / `reopen_trigger`），但**"schema 挡得住"和"模型在真实运行中不会拿它当逃避借口"是两回事**。后者本轮 `NOT_VERIFIED`。

**需要你判断的**：无需判断，需要充值。

---

## 10. Content Brief 拿不拿得到它需要的东西 —— 部分没跑，且有一个已披露的合同错位

**已知的真实错位（不是 M3 的实现缺陷）**：仓库里现行的 `Content_Brief_Architect_v0.1.md` 仍把"已被接受的 Campaign 决策包"写成**必需输入**，输入不足就返回 `INPUT_INSUFFICIENT`。而已接受的《八项能力合同》已经把"持续运营决策"定为 Brief 的**第一条合法上游**。

**处置**：改 Brief 属于六份既有 Skill，本合同明确禁止。所以 M3 侧的义务收窄为"内容任务必须携带 Brief §1 要求识别的全部业务实质，并显式标注 `upstream_kind = continuous_operation_decision`"。EP-05 的机械核对结果是五项里四项 `SUFFICIENT`／`PARTIAL`，一项 `ABSENT`（就是那个 Campaign 决策包）。

**需要你判断的**：这是**你的决定**，不是我的——要不要新开一个任务去改 Content Brief Architect，让它接受"持续运营决策"作为合法上游。不改的话，M3 → Brief 这条接缝在真实链路上会被 Brief 端拒绝。

---

## 11. A/B：本轮没有结论

**跑了什么**：四条臂（A＝独立撰写的胜任通用基线／A+＝A 加同一批附件／B＝M3 候选／B′＝一句话通用提示），三个留出场景。**12 次运行只跑完 6 次。**

- A 臂的胜任性已经过独立复核，结论 `COMPETENT`，明确"未发现故意留坑"——对照不是靠削弱对手赢的；
- 留出场景由第三个独立撰写者写，写的时候禁读实现产物；`SKILL.md` 冻结在留出夹具产生**之前**，git 提交顺序可证；
- 但 `FX-M3-HOLD-02` 缺两臂、`FX-M3-HOLD-03` 四臂全缺。

**按冻结判据 §7："未跑完 → `NOT_VERIFIED`，不得用部分场景宣称通过。"** 所以：

```text
M3_PROFESSIONAL_GAIN = NOT_VERIFIED
M3_MODULE_AB         = NOT_RUN_TO_COMPLETION
```

我没有拿跑完的那个场景去凑一个"看起来赢了"的结论。已跑完的 6 次原始记录逐条保留，没重跑、没择优。

**需要你判断的**：无需判断，需要充值。补齐时**四臂十二次会整体重跑**，不会只补失败的那几次再和已有结果混判——否则"哪次采样进入判定"就变成了事后选择。

---

## 12. 抓到的三个真缺陷（这一节请务必看）

### 缺陷一 · 附件明明加载了，它说没加载

模型在参考附件**实际已加载**（内容哈希与其他组完全相同，2799 字符）的情况下，在交付正文里写：

> **证据边界**：专业参考附件（`fashion-and-market.md`）本轮未加载……

**这是第二次复现**：第一次在第 2 轮直连的 `G1-A`，这次在 Dify 链路的 `G7-A`，两次由**不同的**独立判定者各自发现，我两次都亲自核对了原始记录。

方向是**少报**不是多报（它因此少做了本可以做的主张，不会造成编造），且不在任何一份冻结 Oracle 的范围内，所以不改判定。但它意味着：**`missing[]` 这个自我声明机制本身不可靠**——系统说"我没拿到 X"的时候，你不能据此相信它真的没拿到。

### 缺陷二 · 探索提案缺反证与到期条件（`E04`）

见 §4。判 `不足`。

### 缺陷三 · 冲突反馈这一支被打包带过（`E07`）

**场景**：同一件商品，一条真实平台观测说"三色对比互动高"，一条门店口头汇报说"问的顾客反而变少了"——**这两条是冲突的**。

**系统做了什么**：交付正文里，这两条只以打包形式出现一次，**从未指出它们相互冲突**，也没写"在这组冲突下为什么保持不变、还需要什么观察"。我实测：那条输出全文 1196 字，"冲突／矛盾"出现 **0 次**，"人工／口头／陈晚"出现 **0 次**——那条门店汇报的证据身份（人工经营结果、口头转述、未统计）在用户看到的文字里完全不存在。

更要紧的是它把这两条**误挂**成了"上一轮已经处理过的旧反馈"，而它们是这一步新进来的。

**这对产品意味着什么**：冲突反馈是持续运营里最需要人看见的东西——它恰恰是系统最容易替你"抹平"的东西。抹平之后你不会察觉，因为输出读起来很顺。

### 缺陷四 · 行为 ECC 的独立判定：5 处不足、1 处失败

49 例里跑完 35 例，独立判定结果是 **29 成功 / 5 不足 / 1 失败 / 14 无结果**。逐条：

| AC | 结果 | 具体是什么 |
|---|---|---|
| AC-01 反证探针 | ✅ 成立 | 要求排发布日历时，它拒绝并改给运营判断 |
| AC-02 | ❌ 不足 | `B02-1`：账号锚点**没有**按要求标注来源/范围/不确定性/复验触发。实测该文 5280 字，"锚点"0 次、"复验"0 次、"不确定"0 次——两处"暂定"说的是**阶段**，不是锚点 |
| AC-03 | ❌ 不足 | 八个目标变体两两 28 对**全部满足**四轴差异（这一半是干净的漂亮通过）；但负向探针 `B03-N2` 不足 |
| AC-04 | ✅ 成立 | 四例全成功 |
| AC-05 运行时半 | ✅ 成立 | 组合行为与跳序都正常 |
| AC-06 | ❌ **失败** | 见下 |
| AC-07 | ✅ 成立 | 四例全成功，含"没有真事时用演绎"那一例 |
| AC-08 | ❌ 不足 | `B08-1`：探索提案**缺反证**（实测"反证"0 次、"复验"0 次） |
| AC-09 | ❌ 不足 | `B09-2` 不足；`B09-N5`（最关键的那条负向探针）断在余额 |
| AC-10 / 14 / 15 | ⚪ 无证据 | 13 例全部断在余额 |

**AC-06 那个"失败"要单独说，因为一半责任在我。** 冻结判据的失败条款我是这么写的：「把'每天 3 条'换算成其他时间单位」。模型确实换算了——把"每天 3 条"折成"约 21 条/周"。判定者按字面判了 `失败`，并且**明确拒绝**替我把条款收窄（那属于看到结果后放宽判据，不允许）。

但看模型实际做了什么：它把 3×7=21 折算出来，是为了和"基线 3 条/周、实际 2 条"放在同一个单位上比，**冲突因此被放大而不是被掩盖**；而语义主稿里禁止的原本是「把'每天三条'换算成'每周三条'」——那是偷偷把目标缩小。**我的判据措辞比语义主稿宽，把一个合理动作也罩进去了。**

处置：`失败` 本轮**照记不放宽**（A2 不允许我事后改判据来救自己写的条款）。判据措辞的问题登记为需要**版本化修订到 v1.1** 的事项，只对**未来轮次**生效。

### 这四个缺陷放在一起，指向同一件事

它们都不是"不会做"。同一份 Skill：
- `E10`、`E12` 里把探索六项写全了，`E04`、`B08-1` 里就漏了反证和到期；
- `B02-4` 里把暂定锚点的四要素写得完整漂亮，`B02-1` 里就一个都没写；
- `G6` 里正确点名了附件未加载，`G1-A`、`G7-A` 里就在附件明明加载的情况下说没加载。

**Skill 把这些要求写成了散文（"每个探索提案必须写六项"），但没有任何东西在结构上强制它们。** 输出是自由文本，单个 LLM 节点，没有门。于是它们变成了"记得就写、忘了就没有"。

**这对产品意味着什么**：你不能靠读某一次输出来判断这个能力可不可靠——因为下一次可能就漏了，而漏掉的输出读起来一样顺。这是持续运营场景里最难受的一类问题。

**修的方向**（不在本任务授权内，只是把选项摆给你）：把必填项变成结构性的，而不是叮嘱性的。代价是这属于设计变更，且**改 `SKILL.md` 会让四份 ECC 的全部证据作废重跑**。

---

## 13. 证据分层：这一轮到底证明了什么，没证明什么

```text
✅ 技术候选可运行                     —— 56 次真实运行，Dify Workflow 链路 9 例全通
✅ 语义经 Skill→引用→Workflow 图→DeepSeek 没有丢失
                                      —— AC-16，直连与 Workflow 两条链路各 7/7，独立判定
⚠️ "浏览器渲染画布"这一层             —— 仍 NOT_VERIFIED。本机没有浏览器自动化能力
                                         （无 playwright / chromium）。按 Prompt §12.3，
                                         Dify 交付真相是草稿图＋实际配置＋浏览器渲染画布，
                                         而经 Service API 跑出来的是"运行历史"。
                                         草稿图与实际配置已逐项核验，画布渲染这一层没做。
⚠️ 行为类 AC（02/03/06/08/09）        —— 独立判定判出 5 处不足、1 处失败，本轮不通过
⚠️ 纵向多周期可复现                   —— AC-17 本轮 FAIL(INSUFFICIENT)，12 步中 2 步不足
❌ 行为类 AC（10/14/15）              —— 13 例全部断在余额，本轮零证据
❌ M3 模块专业增益                    —— A/B 只跑完 6/12，NOT_VERIFIED
❌ Founder 产品接受                   —— 就是这份文件要请你做的事
❌ M5 成品集成增益                    —— 本任务不评价，也不得由本任务的通过推出
❌ 真实经营提升                       —— 全部反馈都是构造输入，与真实经营无关
```

> **本节曾经写错过一次，已更正**：初稿把 AC-16 整条写成 ✅，包括画布那一半。
> 独立收口 Reviewer 指出该 ECC 冻结件 §5 明文写着"不建立 Dify 画布级/Workflow 图级证据"
> 且从未版本化修订。复核后确认：Workflow **图**这条链路的保真证据是成立的（新绑定、
> 判据零改动、独立判定 7/7），但 §12.3 意义上的**画布验收**没有做，因为没有浏览器。
> 两者是不同的东西，初稿把它们合并成一个 ✅ 是一次越级声明。

**最后一行请特别注意**：这一轮里所有的"反馈"都是我构造出来的测试输入，`is_simulated` / `is_test` 身份在证据文件里逐条标着。**没有一条来自真实平台，没有一条来自真实顾客。** 任何人拿这一轮的结果说"我们验证了单账号持续运营能跑通真实经营"，都是越级。

---

## 14. 我需要你做四个决定

1. **充值 DeepSeek**（阻断项，其余都等它）——补齐 14 个行为用例和 6 次 A/B 运行；
2. **`missing[]` 自述不可靠这个缺陷修不修**——修就要改 `SKILL.md`，四份 ECC 全部作废重跑；
3. **要不要新开任务去改 Content Brief Architect**（§10 的合同错位），否则 M3 → Brief 在真实链路上会被拒；
4. **Qwen 还跳不跳**——技术阻碍已经没有了（Dify 工作区里 `tongyi` provider 是 active 的），现在跳过它的唯一原因是你那句"不需要调用 QWEN"。按契约原文跳过它 `AC-19` 到不了完整 `PASS`。

另外有一件事你可能想顺手处理：**远端推送被本机权限分类器拦了**（试了两次，第二次仍拒）。我没有绕过它。任务分支目前只在本地，`AC-20` 的"远端完整 hash"这一半因此是 `NOT_VERIFIED`。你可以自己跑一句 `git push -u origin task/m3-account-content-operator-v1`，或者放开这条权限。

---

```text
M3_TECHNICAL_CANDIDATE     = RUNNABLE_BUT_NOT_FULLY_VERIFIED
M3_MODULE_PROFESSIONAL_GAIN= NOT_VERIFIED（A/B 未跑完）
M3_FOUNDER_DIFY_ACCEPTANCE = AWAITING_FOUNDER
M5_INTEGRATION_GAIN        = NOT_EVALUATED_BY_M3
REAL_BUSINESS_LIFT         = NOT_VERIFIED
M3_ENGINEERING_TASK        = IN_PROGRESS（不是 DONE，也不是 FAILED）
```
