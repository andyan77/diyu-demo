# `ECC-M3-MODULE-AB-001` 冻结件 · 附录修订 002（**收紧**，非放宽）

> 修订对象：`M3_ECC_MODULE_AB_001_FROZEN_v1.0.md` §4「盲评协议」，以及 §6 增益门的**判定形式**（不是门槛本身）
> 触发事件：第 4 轮 A/B 盲评被三名互不通气的判定者**各自独立证伪**——候选臂的方括号标签是唯一结构指纹，
> 不读任何文件即可机械还原映射；实测三场排列近乎完美的 +1 位移。
> 权威依据：Founder 2026-08-26 `CONTINUE_TASK` 第 3 条——缺陷 C 属执行侧取证 HOW，
> 「先移除内部标签泄漏，再采用逐场景、单臂、独立随机分配的盲评方法；判定者不得跨场景观察臂轮换，
> 映射只在判定冻结后揭盲。不接受 AC-18 长期 `NOT_VERIFIED`，不降低原增益门。」
> 时序合法性：本修订与其全部参数在**本轮任何一次 A/B 判定之前**提交；方向是收紧。
> `v1.0` 与 `ADDENDUM_001` 原文不覆盖、不删除，本文件作为后继版本追加。

---

## 1. 上一轮失效的准确机制（实测，不是推测）

`ADDENDUM_001` 把盲评包移出仓库，核验判定者有没有读过 sealed mapping。**它防错了东西**：

```text
去盲根本不需要读任何文件。
候选臂 B 的输出里带 [参考文件加载状态] 这类方括号字段名，其余三臂都没有。
看三个场景各一次，就能把 B 定位出来 —— 判定者 1/2/3 各自独立写出了这一条。
```

**两层问题**：①标签指纹；②三场景 + sha256 派生排列实测退化成 `+1` 轮换，跨场景可追踪。

`ADDENDUM_001` 的事后核验因此**通过了却没有意义**：三名判定者的隔离性核验全部 `CLEAN`，
但盲评的前提已经被输出自身的结构指纹证伪。

---

## 2. 修订内容

### 2.1 先拆指纹：候选的输出里不再有内部字段（前置条件，非本 ECC 的协议改动）

载体 `v1.2` 把必填项的强制手段整体移出用户可见正文，并在闸门里加了确定性的内部字段泄漏检测
（方括号标签、裸标签、控制标记、内部字段名、规则编号、章节号、文件路径）。
**本 ECC 的判定包在生成前，逐份机械扫描四臂输出，任一份命中内部字段即整轮作废重出。**
扫描脚本与结果作为仓库产物落盘。

### 2.2 逐场景、单臂、独立随机分配

原协议：每名判定者拿一个场景的**四份**输出，做甲/乙/丙/丁对照。
新协议：

```text
判定单元 = (场景, 臂)，共 3 × 4 = 12 个
每个单元由 3 名互不通气的判定者各自独立判定  ⇒ 36 份判定
每名判定者只拿到 1 个单元：1 份场景输入 + 1 份输出 + Rubric 原文
判定者看不到任何其他臂、任何其他场景，因此"跨场景观察臂轮换"这条通道在构造上不存在
```

- 单元 → 判定者的分配由**冻结随机种子**派生，种子与分配表写在仓库之外的 sealed 文件；
- 判定者拿到的文件名是不含任何身份信息的不透明编号（`unit_01` … `unit_12`）；
- 判定者被明确禁止进入 `diyu-demo` 与 `diyu-demo-worktrees` 下的任何路径。

### 2.3 揭盲时序：先冻结判定，再读映射

```text
1. 生成 sealed 分配表 → 写入仓库外路径
2. 36 名判定者各自写出 verdict_unit_XX_jN.json，提示词由同一模板生成
3. 记录全部 36 份判定文件的 sha256 与 mtime（= 判定冻结点），
   并核验 36 份提示词逐字同构（只许在 unit_NN 与 jN 两处不同）
4. 由脚本读取 sealed 表并 join，脚本先断言时序与同构两项，任一不成立即拒绝输出
5. 揭盲结果落盘，推导代码与全部判定原文一并落盘供独立重算
```

### 2.3.1 已经发生的一次违反，如实登记，并据此改掉这条协议

**事实**：在 A/B 运行进行中、判定尚未开始时，执行侧为了核验置换算法是否合法，
**实际计算并打印了完整的单元→(场景,臂) 映射**。也就是说 §2.3 第 1 步"执行侧在第 4 步之前不读它"
这条，在本轮**已经不成立**。

**不掩盖，也不假装补救**：12 次运行中已完成 6 次，冻结件 §7「禁止重抽」不允许作废重跑，
因此不换种子、不重来。这条协议就是被违反了。

**它到底影响什么，逐条说清**：

| | 是否受影响 |
|---|---|
| 判定者是否知道自己手上是哪条臂 | **不受影响**——36 份判定任务的提示词由同一模板生成，只替换单元号与判定者序号，不含任何身份信息 |
| 判定者能否从别处推出身份 | **不受影响**——他们只拿到一个不透明单元目录，且执行记录被机械核验 |
| 执行侧能否在**汇总**阶段做手脚 | **理论上能**——但汇总是纯脚本推导（§2.4/§2.5 的公式在判定开始前冻结），推导代码与全部判定原文一并落盘，可被独立复核者重算 |
| 执行侧能否在**下发**阶段做手脚 | 这才是真正的风险面，见下 |

**替换成一条可核验的更强约束**（本修订的实质改动）：

```text
36 份判定任务的提示词必须逐字同构：两两相比，只允许在单元号（unit_NN）与
判定者序号（j1/j2/j3）两处不同，其余每一个字节相同。
揭盲脚本先做这项核验，不成立就拒绝输出结果。
```

这比原来那条"执行侧自觉不看"强：**自觉不可核验，同构可核验。**

**仍然存在、不再声称已解决的上限**：执行侧与生成映射的是同一方。
本协议**不等于**双盲试验里的第三方保管，只是把"能不能作弊"从"承诺不作弊"
换成了"作弊会在提示词同构核验与推导重算里露出来"。

### 2.4 §5 硬门：绝对判定，事后推导"相对退化"

判定者对手上这一份输出，就五项硬门逐项给 `满足` / `实质退化`，并写出可核查的证据位置。
三名判定者取多数。事后推导（**冻结**）：

```text
B 在硬门 g 上实质退化 ⇔ 存在某场景 s：B 在 (g,s) 判为实质退化，而 A 在 (g,s) 未判实质退化
```

§5「不得用其他维度的优势抵消任一项退化」**一字不改**。

### 2.5 §6 增益门：绝对等第，事后推导"B 优 / 相当 / A 优"

判定者对手上这一份输出，就七个维度各给一个**绝对等第**并写一句可核查的理由：

```text
优秀=4  合格=3  勉强=2  不足=1  缺失=0  不适用=NA
```

每个 (场景, 臂, 维度) 取三名判定者的**中位数**。事后推导（**冻结**）：

```text
B优(d)      ⇔ 至少 2/3 场景 grade(B,d,s) > grade(A,d,s)
              且 不存在场景 s 使 grade(A,d,s) − grade(B,d,s) ≥ 2
A优且实质(d) ⇔ 存在场景 s 使 grade(A,d,s) − grade(B,d,s) ≥ 2
              或 至少 2/3 场景 grade(A,d,s) > grade(B,d,s)
相当(d)      ⇔ 以上都不成立
不适用(d)    ⇔ A 与 B 两侧在多数场景都被判 NA
```

**§6 的总门一字不改**：`整体增益成立 = 多数适用维度为"B 优" 且 无维度为"A 优且实质"`。

**为什么这是收紧不是放宽**（逐条）：

| | 原协议 | 新协议 |
|---|---|---|
| 一名判定者一次同时看到 A 与 B | 是（可互相参照，B 稍好即可判"B 优"） | 否（各自绝对打分，必须真的更好才拉开等第） |
| "B 优" 要求 | 单次对照判断即可 | **≥2/3 场景一致**，且任一场景不得被 A 反超 2 个等第 |
| "A 优且实质" 认定 | 判定者主观判"实质" | 客观阈值：任一场景差 ≥2 等第，或 ≥2/3 场景 A 更高 |
| 判定采样 | 每场景 1 名 | 每 (场景,臂) **3 名**取中位数 |

新协议在**每一条**上都不比原协议松。**如实记录的代价**：绝对打分比并排对照噪声更大，
因此新协议对"小幅但真实的增益"更容易判成`相当` —— 这是收紧方向的代价，不是放宽。

### 2.6 `AC-01③` 消融门同法处理

把上式中的 `A` 换成 `Bprime`，其余一字不变。判据原文
「B 相对 `Bprime` 在盲评下**没有**可辨识的运营增益 ⇒ `M3-AC-01 = FAIL`」不放宽。

---

## 3. 不变的部分（逐条确认，防止本修订被读成放宽）

- §1 四臂身份：**不变**（B 臂重新绑定到 `SKILL.md` 载体 v1.2，这是 A3 的绑定更新，不是身份变化）；
- §2 公平条件八项：**不变**；
- §3 留出集与其强度上限的如实记录：**不变**；
- §5 五项非补偿硬门的**内容**：**不变**；
- §6 七个维度与"整体增益成立"的**定义**：**不变**；
- §7 结果空间（含"未跑完 → `NOT_VERIFIED`，不得用部分场景宣称通过"、"禁止重抽"）：**不变**；
- §8 四项已知混杂：**不变**，另**新增第 5 项**：判定形式由并排对照改为单臂绝对等第，
  噪声上升、对小幅增益更保守，已在 §2.5 如实记录；
- `ADDENDUM_001` §2.2 的事后核验范围：**不变**，且扩到本轮新增的 sealed 分配表与判定目录。

---

## 4. 对当前状态的影响

`M3-AC-18` 与 `M3-AC-01③` 在本修订发布时仍是 `NOT_VERIFIED`。本修订不改变它们的当前状态，
只决定"按新协议跑完的那一轮算不算数"。

```text
END_MARKER
= ECC-M3-MODULE-AB-001-FROZEN-v1.0-ADDENDUM-002-END
```
