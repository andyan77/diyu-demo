# 统一 Founder Canvas · 技术债登记 v1.2

`task_id: DIYU-V1-UNIFIED-DIFY-APPLICATION-001`

**本表取代 v1.1，成为唯一主表。** v1.0 / v1.1 原文保留不改。

登记依据（按时间）：
- Founder 2026-08-29 指令一「停止继续调用模型测试，将当前问题登记为技术债务，推进下一步」
- Founder 2026-08-29 指令二「登记为技术债，继续收口，完成项目 DIFY 集成全部模块的单一应用交付」
- **Founder 2026-08-29 裁定 `FOUNDER_ADJUDICATION_UAPP_INTENT_ROUTING_001` = `RETURN`**
  （裁定登记见 `UAPP_FOUNDER_ADJUDICATION_001_INTENT_ROUTING.md`）

当前部署身份：`app_id = 2448e4f9-818f-4b88-9311-d18546e97da9`，**69 节点 / 81 边**。

| 口径 | 值 |
|---|---|
| 构建脚本（Manifest / 文档用） | `40e4585825bb7d211f357d89136cb2294936ca54287333fef49b6f74e5b64f2b` |
| 数据库原文（运行器 / 裁定器用） | 见 `evidence/UAPP_ADJUDICATION.json` 的 `graph_sha256_psql` |

Manifest：`UAPP_CANDIDATE_RUN_MANIFEST_v1.8.yaml` sha256 `5041c515…`
判据：`UAPP_FROZEN_SCENARIOS_v1.0.json`（`c45c4668…`）与
`UAPP_FROZEN_SCENARIOS_v2.0.json`（`518eac03…`），**两份都先于各自运行冻结，全程一字未改**。

---

## 一、验收项当前真实状态

| 验收项 | 当前状态 | 依据 |
|---|---|---|
| UAPP-AC-01 新应用独立发布、旧资产零漂移 | `PASS / CURRENT` | D-01、D-08，当前图上重跑 |
| UAPP-AC-02 只开一个应用、只说自然语言 | **`FAIL`** | Founder 裁定下调；解禁条件未成立，见下 |
| UAPP-AC-03 完整主故事 | `FAIL` | TD-UAPP-01 |
| UAPP-AC-04 六能力可达 | `NOT_VERIFIED (STALE)` | 六例 PASS 绑在图 `c95ffbe4…`，本轮改图后按 A3 再次置 `STALE` |
| UAPP-AC-05 短入口、无暗跑、无固定全链 | **`FAIL`** | Founder 裁定下调；解禁条件未成立 |
| UAPP-AC-06 缺口精确停 | **`FAIL`** | TD-UAPP-03 未处置；本轮 TD-UAPP-16 也未成立 |
| UAPP-AC-07 撤回与副作用真实性 | `FAIL`（最后实测）／修复已部署未复验 | TD-UAPP-04 |
| UAPP-AC-08 等价表达 | `NOT_VERIFIED (ABSENT)` | 四例从未运行 |
| UAPP-AC-09 多轮与幂等 | `NOT_VERIFIED (ABSENT)` | RECOVERY-01 从未运行，且依赖一次成功的 FULL-01 |
| UAPP-AC-10 零内部泄漏（机器面） | `PASS / CURRENT` | D-12 正负控制；本轮两条用例正文扫描零命中 |
| UAPP-AC-11 Manifest 先于结果 | `PASS / CURRENT` | 两份判据与八个 Manifest 版本均先于各自运行冻结，Attempt 只追加 |
| UAPP-AC-12 Founder 实测 | **`RETURN`** | Founder 已实测并退回；解禁条件未成立 |

### 解禁条件为什么不成立

Founder 裁定原文：「`AC-02`、`AC-05`、`AC-06` 和 `AC-12` 保持 `FAIL`/`RETURN`，
**直到上述路径复验成立**」，而「上述路径」是裁定点名的**三条**用例。

| 冻结用例 | 判定 | 说明 |
|---|---|---|
| `UAPP-INTENT-01 / T1`「有个账号一直没流量，怎么办」 | **`PASS`** | 判 `MATRIX`，来源 `canvas_triage`，`uapp_seam` 真实执行 |
| `UAPP-INTENT-01 / T2`「用对应的专业能力来分析」 | **`PASS`** | 沿用上文，能力再次真实执行，未退回对话 |
| `UAPP-INTENT-02N` 能力歧义负例 | **`FAIL`** | 系统直判 `CREATIVE_SCRIPT` 就跑，没走「只问一个」 |

**三条里过了两条。** 按裁定原文，条件未成立，四项验收项**照旧保持 `FAIL`/`RETURN`**。
执行侧不因为「主路已经通了」就把它们提上去。

### 本轮真正验证成立的是什么

**只有一件事**：自然语言意图能被系统自己识别并真实调用专业能力，且同会话追问能沿用上文。
证据是两张不同的图上各跑一次、结论一致的 `UAPP-INTENT-01`，判在 Dify 节点执行记录上。

**这不等于 `AC-02`/`AC-05` 成立**，它们的覆盖面比这一条宽。

---

## 二、技术债条目

### TD-UAPP-01｜完整主故事的第一轮交付不稳定　**未偿还**

**事实**：同一条冻结输入 `UAPP-FULL-01 / T1`，一次产出了完整内容制作依据并把产物与版本
写进 M2（`043f8079…`，库中可查）；另一次接缝判定输入不足，返回精确缺口「这条内容对观众的
承诺是什么」，未交付、无产物。两次输入逐字节相同。

**归因**：能力侧的输入充分性判断 + 模型波动，**不是画布的接线缺陷**。
画布在两种情况下的行为都正确（交付则登记，不交付则不登记）。

**后果**：T1 不交付时，T2 没有可发布的版本、T3 没有可挂载的对象，
`UAPP-FULL-01` 整条链无法在一次会话内演示完，故判 `FAIL`。

**处置须由 Founder 裁定**，执行侧不自行选择：① 冻结判据里的 T1 输入本就不够充分
（缺显式的「内容承诺」一项），属判据设计问题，需另出判据版本重跑；② 接缝的充分性阈值偏严。
**不得靠反复采样刷成通过。**

### TD-UAPP-02｜写回链的三处接线缺陷　**已修复，端到端未复验**

**事实**：跨轮 id 取错来源、反馈 `kind` 枚举写错、失败写入被当成「本轮没走」而隐藏。
三处均已定位到最高失效节点并修复，各自补了确定性判据（D-21/D-22/D-23/D-24），
确定性面 24/24 PASS。

**未偿还的部分**：修复后**没有做过一次成功的端到端模型复验**（Founder 已叫停模型测试）。
确定性判据能证明请求体构造正确，**不能证明 M2 真的收下了**。

### TD-UAPP-03｜缺口轮由对话节点接管，一次问了三件事　**未偿还，待 Founder 裁定**

**事实**：`UAPP-GAP-01 / G1`「这周想发点东西，你看着办吧」→ 系统一次问了平台、
图文还是视频、主题方向三件事。冻结判据写的是「只问真正阻塞的那一项」。

**归因**：查 M1 给对话节点的 `dialogue_directive` 原文，它只说「如实说明当前状态」，
**没有要求问那三件事**——多出来的问题是对话节点自行添加的。且平台并非前置条件：
`FULL-01 / T1` 在没有平台的情况下照样产出了制作依据。

**权威域**：该行为来自**逐字复用的 M1 对话节点**。改它即改 M1 已接受的对话语义，
属产品语义、归 Founder 裁定。**执行侧不自行处置，也不回头放宽已冻结判据。**

**并列如实披露**：`GAP-01 / G2` 的冻结输入本身没有点名任何能力，
而系统纪律是「不替用户选能力」，因此「补齐后应当继续产出」这一预期**可能是判据当初写得不准**。
判据已冻结，看到结果之后不得修改，本轮仍按写下的判据判 `FAIL`，此项单独登记、不追溯。

### TD-UAPP-04｜撤回轮自相矛盾的交付　**已修复，端到端未复验**

**事实**：素材登记成功（`56e73168…`），撤回在 M2 **真的成功了**
（`withdrawn_at = 2026-08-29T12:22:53`，`already_withdrawn = false`），副作用台账也正确。
但回给用户的正文开头是 M3 的判断「这份撤回要求现在执行不了……我不会声称已经撤回」，
**与真实发生的写入正好相反**。

**归因**：撤回轮仍调用了 M3，而 M3 手上没有素材事实，它诚实地报「查不到」；
画布随后按会话变量照常执行了撤回。两句都不假，拼在一起构成自相矛盾的交付——
这正是 AC-07「副作用真实性」最不能出的问题。

**修复**：撤回轮不再进 M3；写回轮无 M3 正文时以「实际发生了什么」作为交付正文。

**未偿还的部分**：修复后未做端到端模型复验。归档证据 `UAPP-WITHDRAW-01.json` 现被
机器标为 `STALE`（绑在图 `e509b550…`）。**这不改变那次失败发生过的事实**，
AC-07 仍按最后一次实测记 `FAIL`。

### TD-UAPP-05｜裁定器曾把一次真实失败判成通过　**已修复，留作教训**

**事实**：`UAPP-WITHDRAW-01` 最初被裁定为 `PASS`。该 `PASS` 是**错误的**——
裁定器只检查了副作用台账与数据库行，**没有检查真正回给用户的正文**，
因而漏掉了 TD-UAPP-04 的矛盾。人工阅读正文后才发现。

**修复**：`WD-01` 改为判在真实答复正文上；新增 `WD-03`「真的写入了就不得在正文里否认它」。
用归档证据回放，`WD-03` 命中，**结论由 `PASS` 更正为 `FAIL`，原记录保留不删**。

**留作教训**：确定性判据只能证明它检查过的那一面。
`UAPP-GAP-01` 同样被机器判成 `PASS`，也是人工读正文才发现不成立——
**裁定器至今没有 GAP 专属检查，那是「没测到」，不是「通过」。**

### TD-UAPP-06｜等价表达与恢复用例从未运行　**未偿还**

`UAPP-EQUIV-01a/b/c/n` 与 `UAPP-RECOVERY-01` 的冻结输入均已就位，
但**一次也没有运行过**。状态 `NOT_VERIFIED (ABSENT)`，不是通过、也不是失败。

**追加事实（v1.1 新记）**：`UAPP-RECOVERY-01` 的冻结模式是
`continues_full_01_conversation`——它必须接在一次**成功的** `FULL-01` 会话后面才有意义
（要有一条已写入的反馈，才谈得上第二次提交是否幂等）。因此 AC-09 的偿还
**依赖 TD-UAPP-01 先有结论**，不是一次独立运行就能补上。

### TD-UAPP-07｜已通过结果与当前图不同步　**偿还过一次，因改图再次失效**

**原事实**：`UAPP-CAP-01..06` 的六个 `PASS` 绑定在图 `e8819f5b…`（psql 口径 `f36e788d…`）上；
此后为修复 TD-UAPP-02/04 图被改动四次。按 A3 一律置 `STALE`。

**偿还方式**：在当前图上做**定向复验**（A3 的失效后复验，不是被叫停的那种重复采样——
同一输入在同一张图上仍然只跑一次）。六例全部重跑，全部 `PASS`，
证据落在 `evidence/formal/UAPP-CAP-0N_attemptc95ffbe4.json`，旧证据一字未动。

**同时补上一个真实缺陷**：见 TD-UAPP-09。

**再次失效（2026-08-29 意图路由轮）**：为修 TD-UAPP-11 图从 `c95ffbe4…` 改到
`40e45858…`（中间还有两版），那六个 `PASS` 按同一条 A3 规则**重新置 `STALE`**。
本条因此**不是已结项**：它记录的是一个会反复发生的结构性问题——
每改一次图，所有绑定旧图的模型面结论都要重算。
现在有 TD-UAPP-09 的图绑定检查兜底，机器会自己拦，不再靠人工记得。

**AC-04 当前为 `NOT_VERIFIED (STALE)`，不是 `PASS`。** 不得因为「上一版复验过」就搬过来。

### TD-UAPP-08｜M5 已接受的八项技术债未偿还　**未偿还**

`TD-M5-01..08` 在本任务内**未被偿还**，仍然有效。本任务从未以任何形式声称偿还它们。

### TD-UAPP-09｜裁定器缺少图绑定新鲜度检查　**已修复，正负控制通过**

**事实**：裁定器只比对**判据哈希**，从不比对证据里记的 `graph_sha256_at_run` 与当前已发布图。
判据不变而图改动时（本任务改过四次），它会把绑在旧图上的证据报成 `freshness: CURRENT`。
TD-UAPP-07 那六个 `STALE` 是我**人工**认定的，机器当时并不会拦。

**这是检查器缺陷，不是被测对象缺陷**：真实结论没有被它改错过（人工先一步认定了 STALE），
但防线本身是空的——下一次没人手工盯着就会漏过去。

**修复**：新增图绑定检查，不一致即整份置 `NOT_VERIFIED / STALE` 并写明跑在哪张图上。

**控制**：负向控制——八份旧证据在修复后全部由「可读成 CURRENT」变为 `STALE`，
并各自指出所绑旧图（`f36e788d…` / `e509b550…` / 空）；正向控制——当前图上的六份新证据读成
`CURRENT` 且判 `PASS`。检查器能区分正例负例。

### TD-UAPP-10｜同一张图两个哈希口径，文档间会被读成漂移　**已修复**

**事实**：Manifest 与本表记的是构建脚本口径 `c95ffbe4…`，运行器与裁定器记的是数据库原文口径
`2349143f…`。两者是**同一张图**的不同序列化算法（已复算确认，65 节点 / 77 边一致），
但两份文档并排放会被读成图漂移了。

**修复**：裁定器输出同时给出 `graph_sha256_psql` 与 `graph_sha256_manifest`；本表首节列出对照表。

---

### TD-UAPP-11｜意图—能力路由桥接缺失　**已修，本轮复验**

**事实**：Founder 在 Dify UI 实测「有个账号一直没流量，怎么办」，系统只跑到对话节点，
**没有任何能力被调用**，并反问「你希望现在就调用相应的专业能力来做判断吗」。
执行侧复现一致（`evidence/UAPP_RUN_diag_intent01.json`）。

**归因（有证据，非推测）**：
- M1 shadow 输出 `route_intent: "DISCUSS"`、`requested_capabilities_text: ""`；
- M1 compiler 输出 `needed_capabilities: []`，但同一份 `per_capability` 里
  **六项全是 `reachable_if_requested: true`**；
- Canvas 路由节点源码写着「不做意图识别、不替用户选能力」，只读 `needed_capabilities`。

**最高失效节点＝Canvas 桥接层。** M1 做的正是它被接受时的设计——
`needed_capabilities` 是「用户点名了哪个」的登记表，不是意图分类器。M1 一个字未改。

**修复**：分类器扩成分诊台（增 `intent`）；路由改为「用户点名 > 已发生的事 > 分诊台意图 > 闲聊」；
新增 `uapp_ask_one`（确有歧义时只问一个，绕开 M1 对话节点）与 `uapp_chat_guard`（空头支票守卫）。
新增确定性判据 D-25…D-29。

### TD-UAPP-12｜输出类型未被判据覆盖，D-19 放行过一次真缺陷　**已修**

**事实**：新增的两个代码节点把 `promise_hits` / `question_count` 返回成 int，
而 Dify code 节点输出声明是 string。首次正式运行三轮全部 HTTP 400：
`Output promise_hits must be a string, got int instead`。

**这不是被测对象独有的问题，是判据的洞**：D-19 只比对输出**键名**，不比对**类型**，
所以它报 PASS 放行了。

**修复**：节点改为返回字符串；新增 **D-30** 比对声明类型与实际返回类型。
**控制**：负向——在仍是旧代码的线上图上 D-30 报 FAIL 而 D-19 报 PASS，
证明它抓的正是 D-19 漏掉的那一面；正向——重建后 D-30 转 PASS。

### TD-UAPP-13｜分诊台输出预算不足导致静默不分类　**已修**

**事实**：`uapp_action` 的 `max_tokens = 800`。扩成分诊台后实测
`finish_reason = length`、`completion_tokens = 800/800`，推理把预算烧光，
**JSON 一个字没输出**，`structured_output` 全 null，路由静默落到 `DIALOGUE`。
证据：`evidence/formal/UAPP-INTENT-02N_attempt00d9dcdd.json`。

**最坏的地方不是失败本身，是它在证据里长得和「判了闲聊」一模一样**——
两者都表现为「没有 intent」，而前者是分类失败、后者是正确分类。

**修复**：`max_tokens` 800 → 4000；路由新增 `triage_failed` 输出把两者分开。
新增 **D-31**（预算下限）与 **D-32**（两种情况可区分）。
**控制**：两条判据在修复前的线上图上均报 FAIL，修复后转 PASS。

### TD-UAPP-14｜空头支票守卫的正则漏掉实测句式　**已修**

**事实**：守卫原正则把修饰词写成单个可选组
`(调用|启动|进入)\s*(相应|对应|专业)?\s*(能力|模块|专家)`，
遇到「调用**相应的专业**能力」这种三个修饰词叠加的写法就漏。
实测正文「我也可以帮你看看要不要请对应的专业能力来给建议」**未被拦下**。

**修复**：修饰词改为可叠加 `(?:相应|对应|专业|的)*`，并补「要不要请/找/叫…专业能力」句式。
**控制**：八条正负样本双向通过——四条空头支票全部命中，
四条干净正文（问候、真实分析、含「能力」二字的正常句、真的跑完了的陈述）**零误删**。
误删干净正文比漏一句更糟，所以负向控制不能少。

### TD-UAPP-15｜裁定器把 `_scrub` 的返回元组当字符串比　**已修**

**事实**：`_scrub` 返回 `(正文, 命中正则列表)`。裁定器新加的 IR-07 与 AM-03
写成 `scrub(ans) != ans`——元组和字符串恒不相等，**两条判据永远 FAIL**。
`UAPP-INTENT-01` 因此被误判为 FAIL。

**这是判据缺陷，不是被测对象缺陷**：真实正文里并没有泄漏。
修复后用**同一份归档证据回放**（不重跑），结论由 FAIL 更正为 PASS，原记录保留。

**同一类错误第二次出现**：TD-UAPP-05 是裁定器漏检导致假 PASS，
本条是裁定器写错导致假 FAIL。两个方向都出过，说明裁定器本身缺少自检。
**登记为未偿还项**：裁定器没有针对自身的正负控制夹具。

### TD-UAPP-16｜歧义负例未成立：系统直接判了一个能力就跑　**未偿还，须 Founder 裁决**

**事实**：冻结负例 `UAPP-INTENT-02N`，输入「这条我想再打磨一下」。
最终图上分诊台判 `CREATIVE_SCRIPT` 并直接进入能力，
**没有走「只问一个决定性问题」那条路**。判据 `AM-02`「确有歧义时不硬猜一个能力就跑」不成立，
本例判 `FAIL`。

分诊台给出的理由是：「用户说『这条我想再打磨一下』，是对某条内容本身的文案/稿件进行润色修改，
属于 CREATIVE_SCRIPT。」——**这个理由本身讲得通**，不是胡判。

**归因分类：`ORACLE_OR_CRITERION` 与 `SYSTEM_UNDER_TEST` 二者未能区分，证据不足以定案。**

两种可能，执行侧无权自己选：

1. **判据侧**：「这条我想再打磨一下」根本不构成 Founder 所说的「确实存在能力歧义」。
   把它选为负例是执行侧的产品语义判断，冻结判据里已显式披露过这一点。
   若如此，需另出判据版本换一条真正有歧义的输入——**不在本版原地改**。
2. **被测对象侧**：分诊台提示词里写了「`AMBIGUOUS` 是例外，不是保险选项」，
   是执行侧按裁定第 3 点「用户意图明确时直接执行」有意压低反问倾向。
   可能压得过头了。

**为什么不换个输入重跑**：判据已冻结且已看到结果，此时换输入求 PASS 就是刷分。
`blind_resampling_allowed = false`。本例照记 `FAIL`，处置交 Founder。

**一并披露一处判据不够严的地方**：本例 `AM-01`「只问一个问题」判成了 PASS——
正文里确实只有一个问号。但那一个问题是**能力自己返回的精确缺口**，
不是「只问一个」那条路给出的。判据没有把这两个来源分开，
所以这个 `PASS` 不能被读成「只问一个的路径验证过了」。
`uapp_ask_one` 至今**没有一次真实运行记录**，它只有确定性判据 D-27 的桌面验证。

---

## 三、本任务从未验证、也不承诺的事

- **真实内容平台发布**：从未连接、从未发布。全部发布记录显式 `is_test=true / is_simulated=true`。
- **真实经营提升**（涨粉、成交、到店）：从未验证。
- **非测试数据**：从未写入。本轮复核：近 6 小时新增的非测试发布行 **0** 条；
  本任务全部写入落在 33 个会话级测试工作区（`ws-uapp-*`）内。
- **`uapp_ask_one` 的真实运行**：该节点至今**没有一次真实运行记录**，
  只有确定性判据 D-27 的桌面验证。见 TD-UAPP-16。
- **AC-12**：Founder 已实测并判 `RETURN`。本表不把它算作已验证。
