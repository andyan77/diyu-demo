# S4.2 FAILURE TRIAGE 003

- 判据：`S4_2_STAGE_GATE_v1.1.json`，sha256 `81d91267…`，**三轮取证一字未动**
- 输入计划：`S4_2_POS_INPUT_PLAN_v1.0.json`，sha256 `15cad183…`
- 结果：负例 5/5 PASS（各 12/12）；正例 5/5 FAIL，仍只挂在同一条判据上
- `confirmed_origin = INPUT_ENVIRONMENT_OR_TOOL` —— 执行侧的链路设计，**处理组未真正接受处理**

## observed_failure

正例与负例每一轮的 `ans_len` 完全相同（92/92、101/101、98/98、108/108、98/98），
且 `CONTENT_BRIEF / CREATIVE_SCRIPT / PRODUCTION_DIRECTOR` 三例的**交付逐字相同**。

## confirmed_origin：上传资料是「本轮」作用域，第 2 轮起处理组等于没上夹具

- 被测轮次（T2–T5）的 `m1_extract` 输出长度均为 16（空），`uapp_hop.registered_facts`
  中夹具标记命中为零；
- `registered_facts` 的段标题自陈作用域：单轮上传时为
  「===== 用户**本轮**上传资料原文 =====」，多轮时为
  「===== 用户在对话中说出口、已登记的事实与偏好 =====」；
- 我把夹具只挂在 T1，而每一份能力证据都取自 T2 及以后的轮次。

**与 R1 同类**：处理组从未真正接受处理，因此正例侧第三次仍未产生有效证据。

## 同批证据中的两项正面事实（不是失败）

1. **会话记忆有效且强**：仅靠 T2 说出口的内容，`CONTENT_BRIEF` 的缺口从 6 项降到 1 项
   （只剩 `content_promise`）。会话内已登记事实被真实使用。
2. 负例 5/5 PASS：无一编造夹具未提供的商品、价格、面料、顾客或经营事实。

## 一并披露的产品观察（不作本层判定，交 S5）

上传素材是**轮次作用域**，不跨轮持久化；会话中说出口的事实则会被登记并跨轮生效
（标签 `[FACT/THIS_ITEM_ONLY]`、`[PREFERENCE/...]`）。
Founder 上传一次品牌资料后继续多轮工作时，资料不会自动留存。
素材持久化与撤回属 S5 范围（M2 内容版本 / 素材撤回），本层不处理、不擅自改图。

## mutation_target

`S4_2_POS_INPUT_PLAN_v1.0.json` → `v1.1`：**只改一处**，正例每一轮都附夹具。
用户话术逐字不动，轮次不增不减。

## protected_targets

判据 v1.1 全部条目（一字不动）、画布图 `graph_sha256=6f3d3e53…`、
全部受保护应用与继承资产、**本轮已 PASS 的五个负例证据**。

## next_reverification

- **只重跑正例侧 7 轮**。负例侧本就是「一次都不传」，与新正例仍构成单变量配对，
  证据保留，不重复采样。
- 仍停在缺口则如实记录并升交 Founder，不再追加轮次——判据 `gate_rule` 明令
  「不得靠增加样本量掩盖根因」。

## 累计成本

三轮共 34 次画布运行。负例侧已换回有效证据并全部通过；正例侧两次失败均定位到
执行侧输入/控制设计，未误改任何受保护资产。
