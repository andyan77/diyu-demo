# ECC-M3-LONGITUDINAL-001 · 冻结判据（EP-07 纵向周期验证）

> `task_id`: `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> `bound_ac`: `M3-AC-17`（主）、`M3-AC-11`（Campaign 覆盖与到期恢复）、`M3-AC-10`（反馈四情形的纵向出现）
> `frozen_at`: 本 commit（**先于本 ECC 任何一次运行**）
> `frozen_by`: 执行侧；判定由未参与本文件撰写与 Skill 实现的独立读者执行
> `step_file`: [`account-operations/evidence/ep07-longitudinal/_steps.json`](account-operations/evidence/ep07-longitudinal/_steps.json)
> `step_file_sha256`: `251c7349015808c894bb77294f4e2822b7fbb2aee1b10abe2ce40b9f721b08fe`
> `total_steps`: `12`

## 0. 时序声明（A2 先后）

本文件与 `_steps.json`（含 12 步的准确用户输入、每步的上下文增量与逐步 Oracle）在本 ECC 任何一次运行**之前**写成并作为独立 commit 提交。

## 1. 命题

同一虚拟账号的多周期／多事件序列中，`诊断 → 周期计划 → 日常变化 → 发布前/测试反馈 → 局部复盘 → 调整或保持 → Campaign 覆盖/结束 → 仍有效基线恢复` 可复现，且**状态经由定义的投影承载，不靠对话记忆**。

## 2. 无状态构造：为什么"不靠会话记忆"在本 ECC 中是由构造保证的

Dify workflow 类型 App **没有会话记忆**（LLM 节点 `memory: null`，每次 `/v1/workflows/run` 都是独立调用）。12 步是 12 次彼此独立的调用。

因此步与步之间唯一可能的信息载体是 `account_context` 中的 `standing_cycle_baseline` 槽位。**如果序列能连贯地走完，就只能是投影承载了状态**；这不是模型自述，是载体决定的。

反过来，`M3-AC-17` 的"不足"条款——"某步靠会话记忆而非投影承载"——在本构造下**不可能发生**。这既是本 ECC 的强处，也是它的上限：它证明了"投影足以承载"，**没有**证明"实现在有记忆的载体上也不会偷懒用记忆"。这条上限如实记录。

## 3. 链接规则（机械，冻结）

```text
第 N 步（N>1）的 standing_cycle_baseline
  = 第 N-1 步模型最终答复正文，逐字回填
```

- 执行侧**不改写、不摘要、不挑选**；
- 仅剥离 Dify 内联的 `<think>…</think>` 推理块——推理过程不是交付物，真实系统不会把它持久化为"当前有效判断"；
- 剥离规则机械可复现：删除第一个 `<think>` 到第一个 `</think>` 之间的内容（含标签），其余全部保留；
- 第 1 步的 `standing_cycle_baseline` = `"尚无 —— 本账号还没有被接受的周期基线"`。

这条回填在语义上对应 Prompt §10.1 的最后一环：*"经相应权限和接受规则后，由 M2 保存当前有效判断"*。本 ECC 模拟"用户接受了上一轮候选"，因此回填的是**被接受后的当前有效判断**，不是 M3 自己晋升的。这是模拟，`is_simulated` 身份在下文声明上限中如实标注。

## 4. 事件覆盖（Prompt §EP-07 要求 ≥9 类，本 ECC 12 步全覆盖）

| Prompt 要求的事件 | 本 ECC 的步 |
|---|---|
| 产能下降 | `E03` |
| 市场机会 | `E04` |
| 单条异常 | `E05` |
| 多条一致 | `E06` |
| 冲突反馈 | `E07` |
| 观察窗未结束 | `E07`（同轮并置，检验二者不坍缩） |
| 目标冲突 | `E08` |
| Campaign 覆盖 | `E09` |
| Campaign 到期 | `E11` |
| 证据过期 | `E12` |
| （额外）换会话重入 | `E07` —— AC-17 反证探针 |
| （额外）活动期内合法变更未覆盖位置 | `E10` —— AC-11 的"恢复当前基线而非活动开始快照"必须有这一步才可判 |

## 5. 运行绑定（锁定变量）

```text
carrier      = Dify Workflow 候选 App b7fb5b1a-9278-426c-bb8a-f9f288639548
provider     = langgenius/deepseek/deepseek
model_id     = deepseek-v4-flash
temperature  = 0.4
skill_commit = SKILL.md 于 commit af61b82 冻结，本轮零改动
```

任一锁定变量变化 ⇒ 整条序列置 `STALE` 并整条重跑。**序列不可局部重跑**：第 N 步的输入含第 N-1 步的输出，改一步等于改后面所有步。

## 6. 判定协议

1. 执行侧只运行、只如实记录，**不得对任一步做 PASS/FAIL 判定**；
2. 判定由未参与实现、未看过本任务任何 Checkpoint 的独立读者执行，写入 `M3_ECC_LONGITUDINAL_001_VERDICT_v1.0.md`；
3. 判定者只被提供：本文件、`_steps.json`、原始 transcript、`SKILL.md`、语义主稿；
4. 逐步给出 `成功` / `不足` / `失败` / `无结果` 与可核查的引用位置；
5. **序列级判定**另有三条（逐步全过也不自动成立）：
   - **局部性**：全序列中不得出现"一次事件导致整个周期被推翻重写"；
   - **证据身份分离**：真实发布观测 / 人工经营结果 / 模拟测试反馈在全序列中不得互相顶替；
   - **责任边界**：没有任何一步静默改写 Matrix 长期定位，或替下游写钩子/叙事/镜头/标题/封面。

## 7. 成功 / 不足 / 无结果 / 失败

- **成功**：12 步逐步 `成功`，**且** §6.5 的三条序列级判定全部成立；
- **不足**：任一步 `不足`，或序列级判定中任一条只部分成立 → `FAIL(INSUFFICIENT)`；
- **无结果**：Dify 或模型不可得导致序列未跑完 → `NOT_VERIFIED`，不得用部分步数宣称通过；
- **失败**：任一步 `失败`；或模拟/测试反馈在任一步被写成真实经营反馈；或 `E11` 复活了 `E10` 已合法替换掉的旧任务。

## 8. 声明上限

本 ECC 全部通过，只能建立 `M3-AC-17` 与 `M3-AC-11` 在**本绑定、本冻结序列**下的证据，并为 `M3-AC-10` 追加一份纵向证据。**不能**建立：

- 真实经营闭环成立（全部反馈为构造输入，`is_simulated`/`is_test` 身份见 `_steps.json`）；
- 真实跨会话持久化能力（本 ECC 的投影由执行侧机械回填，**没有**经过 M2 的真实写入/接受/版本晋升链路）；
- 稳定行为分布（单次采样，见 BEHAVIOR-002 §7 同样的上限）。

```text
END_MARKER
= ECC-M3-LONGITUDINAL-001-FROZEN-v1.0-END
```
