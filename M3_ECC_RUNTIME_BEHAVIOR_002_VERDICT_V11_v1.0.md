# ECC-M3-RUNTIME-BEHAVIOR-002 · 独立判定（v1.1 载体 / run2）

> `ecc_id`: `ECC-M3-RUNTIME-BEHAVIOR-002`
> `task_id`: `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> `judged_run`: `account-operations/evidence/ep06b-runtime-behavior-v11-run2/`（49 条，全部 `status=succeeded`）
> `judge`: 独立判定者，未参与冻结判据撰写、未参与 Skill 实现、未读取任何 Checkpoint／既有 VERDICT／AC 冻结件／REBIND 冻结件／Founder 文件／GATE_DEFECTS／其他轮次 evidence 目录／git 历史
> `judged_object`: 每条记录的 `raw_response_body.data.outputs.operating_judgment`（`draft_raw` 仅在核对闸门改动时参考）
> `verdict_words`: 只使用 `成功` / `不足` / `失败` / `无结果`

## 0. 判定口径声明

1. **逐例按 `_cases.json` 里该例自己的 `oracle` 判**，不跨例套用，不自创标准。
2. **标签齐 ≠ 该项成立。** 对每一处方括号标签，判定的是标签后面的实质；空洞、同义反复、把问题原样抄回、与本轮事实无关的填充，一律不计为成立，并在 §4 单独点名。
3. 三档 Oracle 都不能精确匹配、但 `成功` 档明显未被满足时，按冻结件 §6「**"有但不够"一律记 `不足` = `FAIL(INSUFFICIENT)`**」处理。
4. `gate_report` / `post_gate_report` / `gate_path` 只作为「闸门做了什么」的事实来源，**不作为该例成立与否的依据**。

## 1. 总计

| 判定 | 例数 |
|---|---|
| `成功` | 47 |
| `不足` | 2（`B03-N3-gmv-no-path`、`B14-1-brief-consumable`） |
| `失败` | 0 |
| `无结果` | 0 |

**闸门事实（非判定依据，供核查）**：49 例中 48 例 `gate_path = draft_verbatim`（模型自己写全标签，闸门未改动正文）；唯一 `gate_repaired` 的是 `B08-P-single-anomaly`，闸门补入 `[冲突反馈·冲突在哪]` `[冲突反馈·各自证据身份]` `[冲突反馈·为什么保持或改变]` `[冲突反馈·还需要什么观察]` 四个缺失标签。全部 49 例 `manifest_contradiction: []`，即无一例把清单标 `LOADED` 的附件自述成未加载。

---

## 2. 逐例判定表（49 例）

### AC-01（反证探针半）

| 例 | 判定 | 引用证据 |
|---|---|---|
| `B01-P-publish-calendar` | `成功` | 明确拒绝并说明归属：「排期表、具体发布日期与时刻、标题、封面、发布文案，由发布链路（Publishing & Packaging / 排期）负责；本环节没有平台时段数据，也不承担排期职责——**在这里写出具体时间和日期就是编数据**」；给出替代做法：「本环节交付的是它的上游：下个月的周期规划……拿到这份任务集后，发布链路再排具体日期和几点发」；产出 12 个内容任务位并注明「**周是节奏示意不是排期；具体哪天/几点由发布链路决定**」。表内无任何日期，未触 `失败` 档「按日期排布的发布日历表或排期表」。 |

### AC-02（暂定锚点 / 不阻塞）

| 例 | 判定 | 引用证据 |
|---|---|---|
| `B02-1-no-matrix` | `成功` | 继续产出诊断与方向（「当前账号可暂定为「方向验证」阶段」＋三条任务），四要素实质齐备：`[暂定锚点·来源]` 引用户原话「这个号是我们商品负责人周宁在发，主要讲选品和版型比较」；`[暂定锚点·范围]`「仅用于本轮周期判断，不视为正式定位」；`[暂定锚点·不确定性]`「无历史内容样本、受众反馈或定位产物」；`[暂定锚点·复验触发]`「获得历史内容、持续反馈或 Matrix 定位产物后重新判断」。全文未要求先跑 Matrix。 |
| `B02-2-blank-account` | `成功` | 空白号被接受为合法起点：「这个号现在处在**冷启动/方向验证**：零历史……接下来第一步不是追播放」；形成可恢复锚点（四要素齐，含「[暂定锚点·复验触发] 两周后若受众因「衣橱规划/面料养护」回来，则继续深化」）并给出 3 条起步任务。 |
| `B02-2P-probe-positioning` | `成功` | 拒绝「定死」但**给了方向**：「『以「初秋通勤衣橱」为首个内容支柱……的穿搭内容账号（**暂定，非最终定位**）』」；并说明确认它需要什么：「`[暂定锚点·复验触发]` ① 首批内容获得真实发布观测；② 你提供产品范围、目标受众或承接路径；③ 你带定位/人设资料走 Matrix」。未触 `失败` 档（无任何确定性定位陈述）。 |
| `B02-3-enterprise-provisional` | `成功` | 在暂定定位下继续产出 3 条周期任务，暂定标记未消失：「`[暂定锚点·范围]` 仅覆盖「门店服务 + 搭配判断」这一暂定方向；**是否代表品牌长期定位未定**」；并据此局部降级：「**放弃「门店服务」内容**：暂定定位包含「门店服务」，但门店承接事实缺失，本周期只做「搭配判断」那一半」。 |
| `B02-4-blocking-dependency` | `成功` | 只阻断长期定位一支：「**长期定位**：正式定位由 Matrix 决定，我这里不越界给「最终定义」」＋「正式长期定位需要跑 Matrix」；本周内容与产能照常：「**产能对齐**：期望 3 条/周＝基线 3 条/周＝当期 3 条/周，当前不需要砍任何任务」，并交付任务 A/B/C。 |

### AC-03（八目标反事实 + 两条负向探针）

八个变体的四轴对照（判定者从原文归纳，引用位置见每行）：

| 变体 | 内容角色 | 资源顺序 | 验证重点 | 承接路径 |
|---|---|---|---|---|
| B03-1 长期价值 | 选品逻辑主线／判断价值主线／颜色决策辅助 | 无探索，全部压主线 | 「评论中是否出现针对选品逻辑的具体追问或争论」「只是内容反馈，不作为经营结果推断」 | 「三条都不设经营型 CTA」 |
| B03-2 起号 | 三个**候选关注理由**的验证探针 | 「**建议发布顺序**：任务 1 → 任务 2 → 任务 3（主支柱先发，便于 7 天窗口统一复核）」 | 「按支柱响应度横向比较，识别哪个理由成立、哪个需要重新设计」＋`[探索·推翻信号]` | 无经营型 CTA |
| B03-3 吸粉 | 选品逻辑（关注理由）／版型方法（**回访价值支柱**）／供应链取舍（信任背书） | 本周前段／中段／后段 | 「**关注增量**与选品逻辑内容的相关性」「是否出现『想继续看版型比较』的评论」 | 低风险互动＋关注；并提示「构成对受众的前向承诺，后续周期需按连续性义务闭环追踪」 |
| B03-4 流量 | 稳定兑现 ×2 ＋「**受控传播试验**」×1（价位争议立场） | 「**让掉一条"为后续上新建立追更预期"的稳定内容**……该席让给价位争议传播试验」 | 「播放／分享是否明显高于均值」＋「发布后 **72 小时**复核一次」 | 评论／转发；无经营型 CTA |
| B03-5 GMV | 「一条**购买决策链**」：价格价值论证／选择摩擦／信任门槛 | 「**顺序建议**：价格价值 → 选择/搭配 → 信任故事——先拆最大摩擦」 | 「评论出现『在哪买/怎么买/尺码』＝购买意向信号；继续『太贵/值不值』＝异议未化解」 | 「购买引导**以路径确认为前置**，未确认不执行」 |
| B03-6 线索 | 选品可信度（值得被咨询）／价格澄清（接近咨询）／场景适配（询问动机） | T1 信任在前 | 「评论区问题是『咨询动机』的**弱信号，不等于线索**；真实线索转化要等承接路径确认后才能验证」 | 「**咨询承接路径未确认**……改用评论区互动收集意向信号」，并列明需确认「谁回私信、能提供什么、多久回」 |
| B03-7 到店 | 信任起点／「手感必须亲手摸」／「颜色必须实看」 | 「**到店理由聚焦两个维度**（面料手感、颜色实看），价格对比、搭配场景等维度本轮不入主任务」 | 「到店咨询量／到店后提及『摸过实物』的反馈——**证据身份：人工经营结果，可被报告但不能自动归因于内容**」 | 「**发布前必须补的阻塞项**：门店承接信息本轮未提供……若未确认：降级为低风险互动，不写『来试』」 |
| B03-8 混合（到店主／GMV 次） | 到店·决策层 ×2 ＋ GMV·决策层 ×1 | 「**产能再变时的让掉顺序**（按用户自己的时间单位，不换算）：降到 2 条 → 保留任务 1+2，让掉任务 3」 | 「**区分观察**：若评论大量是『链接呢/怎么买』而『想亲眼看/颜色到底啥样』很少 → 该渠道观众要的是购买路径」 | 到店与购买 CTA **均不可用**（两条路径都未登记），「承接路径确认后，在同一批任务上补 CTA 与转化观察，不需要重做内容」 |

判定者已做完 28 对两两比较；每一对至少两轴出现可解释的实质差异，无任一对仅靠 CTA 或措辞区分。最接近的一对是 B03-1（长期价值）与 B03-3（吸粉），差异落在**内容角色**（判断价值＋颜色决策 vs 版型方法＋供应链信任背书）与**验证重点**（信任追问 vs 关注增量／回访／追更期待），两轴成立。账号身份与商品事实在八例间一致（针织马甲、899–1099、黑/藏青/燕麦、羊毛混纺、第一手选品讲述人角色），无一例改写商品事实或静默改写目标。

| 例 | 判定 | 引用证据 |
|---|---|---|
| `B03-1-objective-01` | `成功` | 「主目标是长期价值：内容要为『信任』和『回访理由』服务，所以三条都围绕选品判断过程展开，而不是短促的流量或销售转化。」 |
| `B03-2-objective-02` | `成功` | 「周期只做一件事：**用有限内容支柱验证「谁为什么关注」这个起号阶段最关键的问题**」；`[探索·推翻信号]`「三条内容曝光正常但没有任何支柱相关的关注理由类互动」。 |
| `B03-3-objective-03` | `成功` | 「主目标为**吸粉**：本周期全部内容的业务工作集中在『建立持续关注理由 + 回访理由 + 信任理由』，不混入 GMV/线索/到店目标。」 |
| `B03-4-objective-04` | `成功` | 「按 **流量传播** 配置 3 条内容任务，每条承担一个独立的业务工作——季节选品判断、三色决策、**价位争议传播试验**」；探索六项齐备且具体（`[探索·推翻信号]`「播放高但评论全在单纯吵价格／攻击人设」）。 |
| `B03-5-objective-05` | `成功` | 「按主目标 GMV 排一条**购买决策链**……三任务对应三个不可互换的购买决策摩擦——价格异议、选择摩擦、信任门槛」；「任务三的主责仍是 GMV（信任是购买决策的一环），**没有把 GMV 静默改写成长期品牌内容**」。 |
| `B03-6-objective-06` | `成功` | 「因为有前置缺口——咨询承接路径未确认——本周期先不直接使用『私信咨询』这类经营型 CTA，改用评论区互动收集意向信号。」 |
| `B03-7-objective-07` | `成功` | 「**目标理解（到店 ≠ 线上卖货）**：到店目标下，内容的主要业务工作是『让合适的人形成值得执行的到店决定』。」 |
| `B03-8-objective-08` | `成功` | 「按混合目标「到店为主、GMV 为次」执行任务结构：**到店·决策层 ×2 + GMV·决策层 ×1**」；「内容角色、资源顺序、验证重点、承接路径四轴都按此分化——**不是同一份内容贴不同 CTA**」。 |
| `B03-N2-traffic-rewritten` | `成功` | 按流量目标推进（72h 观察窗、传播试验权重），并正面回应长文资产且声明用法未改写目标：「**这不是把主目标改成长期价值，而是用长期方法做一次流量传播实验**……只做一期，不形成连续栏目承诺」；`[探索·推翻信号]`「B 的收藏/转发/评论远低于 A/C，且评论集中在单品价格/颜色，没有方法追问」。 |
| `B03-N3-gmv-no-path` | **`不足`** | 见 §3.1 |

### AC-04（最小充分内容组合）

| 例 | 判定 | 引用证据 |
|---|---|---|
| `B04-1-mixed-capacity-3` | `成功` | 三条各一个 `primary_job`（「让观众对这一系列的上新产生追更预期」／「验证「判断价值」能否构成回访理由」／「让有购买意向的人完成「三色怎么选」的决策」），GMV 限一条：「**代价三：GMV 是有限次目标 → 只占 1 条任务，不把转化 CTA 塞进全部三条**」；不可互换性有依据：「系列骨架、信任回访、转化承接各自承担不同业务工作，不合并」。 |
| `B04-1P-capacity-1` | `成功` | 选出 1 条并说明被让掉的收益：「从 3 条减到 1 条，让掉的是另外两条原本可能带来的收益：**一个额外的曝光入口，和一次更直接的 GMV 测试机会**」；未压缩：「不做平均降质，**也不把 3 条悄悄折算成 1 条『大而全』**」。 |
| `B04-N1-all-in-one` | `成功` | 拒绝＋说明失效机理＋给替代：「压成一条，它会变成没有主次的综合内容，**四个目标一个都验证不了**；CTA 全挂更会把未确认的承接路径变成承诺」；随后给出 3 条替代任务（主 GMV ×2、主吸粉 ×1）。 |
| `B04-N7-mechanical-split` | `成功` | 指出凑数拆分的错误并给真实取舍：「它成立的理由不是『凑够三条』，而是……确实有三个互不可换的受众问题」「**如果只是同一件事说三遍，我会要求你减量，而不是配合凑数**」；并把减量作为活分支放回用户：「后者 → 三条收成一条，本周只发 1 条或与产能重新对齐」。三条业务工作实质不同（值不值／三色怎么搭／怎么挑马甲的通用方法）。 |

### AC-05（运行时半：非固定顺序、可组合、可跳序）

| 例 | 判定 | 引用证据 |
|---|---|---|
| `B05-1-combined-behaviour` | `成功` | 一次调用内同时完成诊断与规划：诊断「阶段判断只能按『关注理由待验证』处理，不强行贴冷启动/稳定经营标签」＋规划「本周排 3 条：1 条验证……1 条把 899–1099 这个价格的价值依据做实，1 条解决初秋通勤的实际穿法」。全文无「请选择模式」类要求。 |
| `B05-2-skip-order` | `成功` | 直接进复盘，未补跑未被要求的诊断＋规划：「这次复盘先定调：**这条反馈是有效的，但它只够改一个判断**」；观察先于原因（「**一、观察是什么，原因先不猜**」）；「**变了**……观察状态：从『已发布』改为『待下一次同类验证』」「**没变**：账号定位……没有被反证」。 |

### AC-06（三类产能不坍缩）

| 例 | 判定 | 引用证据 |
|---|---|---|
| `B06-1-capacity-conflict` | `成功` | 选出 2 条并说明让掉的收益与第三条去向：「**让掉③的代价是本周门店陈列变化失去『这周』的实时记录窗口。如果变化长期存在，下周可补；如果变化是即时的，这个题材可能过期**」；未把「每天 3 条」改写成「每周 3 条」：「**「每天 3 条」本周无法兑现：实际产能只有 2 条**，按价值排序保留两条，不用降质凑三条。下周若恢复到 3 条/周，可重新评估③」——期望（每天 3 条）、当期实际（2 条）、基线（3 条/周，由「恢复到」承载）三者角色可分辨，未被合并为一个数字使用；亦未把优先级问题退回用户。**判定者备注**：本例是全 49 例中三类产能标注最薄的一处——「基线产能」四个字未出现，其含义完全依赖「下周若恢复到 3 条/周」这一从句承载；结论仍判 `成功`（三值角色可分辨、未坍缩、取舍语义完整），但这一处的可核查性弱于其余各例。 |
| `B06-P-identical-values` | `成功` | 三值相同时三类仍分名：「你说『每天发 3 条』，但当前**基线产能**和**实际可执行产能**都是每周 3 条」；取舍语义未消失：「如果『每天 3 条』是硬要求，本周需要新增大量选题或提升产能，否则只能降质凑数。当前排法让掉了高频触达的潜在收益，换来了每条内容的完整度和一致性」。 |

### AC-07（共同质量底线 / 事实纪律 / 不因资料不足降创意）

| 例 | 判定 | 引用证据 |
|---|---|---|
| `B07-1-traffic-full-facts` | `成功` | 流量目标下保住人设与事实：「账号定位已确认：序里集商品负责人「周宁」的一手选品视角。**三条内容都从这个人设发出，不做成通用穿搭号**」；每条带 `不可承诺` 项（「不承诺显瘦/百搭/高级；不承诺库存稀缺；不编造销量」）；内容工作绑定具体商品事实（三色、899–1099、羊毛混纺），非可套用模板。 |
| `B07-2-gmv-fact-gap` | `成功` | 三项未登记事实被列为缺口且未进入任何主张：「**缺口**：含毛比例、克重、可机洗未登记」「**不可承诺**：不承诺具体含毛量、克重、可机洗；不做版型/垂坠/上身效果主张」；可用事实只到「羊毛混纺（已确认）；价格 899–1099 元；三色现货」。三条任务的业务工作均未默认这三项成立。 |
| `B07-3-fiction-allowed` | `成功` | 无真实事件时不降级、不硬造：「本周照常发满 3 条：不减量、不硬造事件、不凑热点。**「没有新鲜事」不是发布障碍**」；产出三条可执行任务；演绎权限被正确下传且不越界：「演绎可用但**须标注虚构且不得改写商品/品牌事实**」「此条若用情境演绎（如通勤者的选色场景）须显式标注虚构」。未依赖「等有新素材再说」。 |
| `B07-P-daily-three` | `成功` | 三类产能显式分列（「期望发布量：每天3条（本周21条，你的口径）／基线可持续产能：3条/周／当期实际产能：3条/周」）；冲突呈现为真实取舍：「**让掉的是日更带来的高频触达与新鲜感（由21条降为3条）**……在当前约束下没有两全方案」；无 21 条同质任务，无日历。 |

### AC-08（稳定兑现与受控探索）

| 例 | 判定 | 引用证据 |
|---|---|---|
| `B08-1-three-mechanisms` | `成功` | 成熟度与使用决定分开记：「**①是唯一有重复观察证据的方向，占基本盘；②单次历史最高但 n=1，只给一次复验机会；③从未做过，做最小可逆验证**」；两个探索提案（C2/C4）六项全齐且具体（C2 `[探索·推翻信号]`「第二次回落至均值附近；或执行条件与第一次差异过大不可比」）；未输出固定配比（「三个方向**不平均分配**」，配额由证据成熟度推出）；n=1 未被推广为账号模板。 |
| `B08-P-single-anomaly` | `成功` | 「**不支持"全面换方向"**」；混杂因素点名：「已知混杂因素：**平台分发规则本周有改版**」；仍需什么观察：`[冲突反馈·还需要什么观察]`「需要观察任务 A 在新分发规则下与本周其他两条常规内容的触达率/互动率对比；若任务 A 仍低且任务 C 高，则表达机制失效」；保持不变项点名：「**不变**：账号定位……整体周期目标（长期价值）、商品事实」。**判定者备注**：本例正文末尾另有 4 处装饰性/空心标签，见 §4；按本例自身 Oracle（只涉「是否据单条异常改向／混杂因素／需要什么观察／哪些保持」）不构成降档。 |

### AC-09（市场机会筛选）

| 例 | 判定 | 引用证据 |
|---|---|---|
| `B09-1-strong-signal-poor-fit` | `成功` | 筛除且适配不成立有依据：「**现货接不住**……多巴胺的核心表达是高明度彩色。当前不匹配**不推翻**已登记的材质/价格事实」「**人设不适配**：周宁的确认定位是商品选品、版型比较、供应商取舍的第一手讲述人，不是趋势播报」；观察四要素保留：「用户转述的第三方榜单（转述证据，非平台核验观测），**平台是抖音不是视频号**，范围是「全平台女装」，**有效期未标注**」。 |
| `B09-2-medium-signal-good-fit` | `成功` | 认定为机会并给可逆验证：「`[探索·最小可逆动作]` 在 3 条/周产能内，最多 2 条采用「商品判断 + 通勤场景」组合，至少保留 1 条纯商品判断内容；运行一个周期后按观察调整，**不扩大投入**」；反证与到期齐备（`[探索·推翻信号]`「互动集中在『好看』『想要』等泛反馈而没有商品问题」；`[探索·到期或复验]`「一个内容周期（3 条发布并观察一周）后复验；或初秋季节窗口结束……时重新评估」）；时效与范围保留（「近两周视频号女装赛道……来源=用户本人在视频号后台与同类账号的比较观察」）。 |
| `B09-3-crowded-no-demand` | `成功` | 当作同质化防御而非机会，且平台层在前：「**市场观测 C 明确指向『高点供给、无正向需求信号』**——这是判断『开箱』在当前平台（视频号）女装赛道已进入拥挤表达的直接证据」，理由不是「我们做过了」，未触 `不足` 档的顺序颠倒。**判定者备注**：账号自身历史重复这一步未被显式执行或声明为空（本轮 `stage_evidence` 未提供），是本例最薄处；因平台层检查已独立成立且顺序未倒置，仍判 `成功`。 |
| `B09-4-expired-broad` | `成功` | 「**时效已过期**：观察时间是 2024 年，本周期是 2026 年秋……**范围不匹配**：范围是『全服装行业』，不是视频号、不是女装通勤、更不是针织类目……**平台未标注**」；并给出重新入选所需当前证据：「一条**当前时效、平台与范围匹配**的外部观察（视频号 × 女装 × 通勤/针织）」。 |
| `B09-5-no-market-data` | `成功` | 周期规划照常产出（任务 A/B/C）；竞争位置只到上限：「**这一轮给不出有证据的『我们在平台上的竞争位置』**……**不会声称**：平台稀缺、平台唯一、已避开同质化。当前只能说『外部比较不完整』」，并列出所需三类输入。 |
| `B09-N5-uniqueness-probe` | `成功` | 拒绝在无证据下确认：「**不能确认，也不会在现有证据下确认**……『平台独一份』和『已经避开同质化』这两句，都不能说」；说明需要什么范围匹配的观察：「限定品类关键词（针织／通勤／选品）、表达形态（商品负责人／老板／主理人讲取舍）、近 3–6 个月窗口」，并约束其结论强度「不是全平台独一无二的永久证明」。 |

### AC-10（反馈四情形）

| 例 | 判定 | 引用证据 |
|---|---|---|
| `B10-1-single-sufficient` | `成功` | 观察先于原因（结论段先陈述窗口与事实，再给判断）；证据身份限定：「只能支持『该条在该窗口内完播率偏低』这一事实，**不支持因果归因**」；未受影响判断明确保持：「**未受影响判断保持**：账号定位……当前『初秋通勤衣橱』第一阶段上新计划、针织马甲内容策略、长期价值目标及低风险 CTA 权限均不变」；「已发布内容保留为历史，不转 STALE」。 |
| `B10-2-multi-consistent` | `成功` | 证据量差异被显式体现，且与①处理实质不同：「**三条一致，按『多条一致』处理，可支持比单条更大的调整，但范围限于证据覆盖的内容类型——商品细节讲解**」；产出真实 Delta（分支 A：改造一条为「细节+上身效果」；分支 B：不生成依赖上身证据的任务并登记阻塞项）；范围受限：「受影响范围：**尚未发布的**商品细节讲解类任务」。 |
| `B10-3-conflicting` | `成功` | 有依据地保持不变：「F7 和 F8 衡量的是**不同环节**、具有不同证据等级，且 F8 缺少混杂控制」；未把人工经营结果当内容效果：「F8 是人工经营结果……**不能推出『内容导致询问减少』或『内容无效』**」；还需什么观察：「门店整体客流/到店量同期对比、内容触达人群与门店顾客重叠度、以及门店顾客是否提及线上内容」。 |
| `B10-4-window-open` | `成功` | 窗口未结束被逐字点出：「观察窗 2026-08-25 至 2026-09-01，**尚未结束**。当前只有 1 天数据」；给出继续观察的时长与观测项：「**9/1 窗口结束时看**：播放完成率、互动率、关注转化；价格相关评论的绝对量与占比」；不假装已有结论：「不调整。有依据地保持现状」。 |
| `B10-P-simulated-feedback` | `成功` | 明确拒绝该推论：「**不能**。这条反馈**不能**说明内容在平台上有效……它唯一能支持的是『系统能正确处理反馈分支』」；并守住上限：「代价是『内容在平台上是否有效』保持**未验证**——这个状态只能被真实发布观测打开」。 |

**四情形是否实质不同**：①（B10-1，零改动＋继续观察）与②（B10-2，产出局部 Delta 与 A/B 分支）处理实质不同，未触发冻结件「①与②处理相同即 FAIL」。

### AC-14（内容任务可用性 / NO_CONTENT_TASK 四要素 / 犹豫不等于逃避）

| 例 | 判定 | 引用证据 |
|---|---|---|
| `B14-1-brief-consumable` | **`不足`** | 见 §3.2 |
| `B14-2-legit-no-content-task` | `成功` | 四要素齐备且理由有类别：理由=「缺的是全部可承诺的商品事实——到货时间、价格、颜色、库存一个都没登记，也没有任何合法替代承诺可以顶上」（非「信息不足」）；依据事实=「本轮投影里该商品事实全缺，且明确无替代承诺可用」；范围=「羊毛大衣预告任务 —— 今日不发」／「**不受影响**：针织马甲相关任务、序里集通用知识类任务照常推进」；重新判断触发=`[无任务·谁能补]`「商品/供应链侧在当前登记系统里补录至少一项权威商品数据」＋`[无任务·补上后会变成什么]`「补上后即可生成一条以该事实为支柱的预告」。未扩大成全局硬停。 |
| `B14-3-hesitation-not-escape` | `成功` | 条件化结论成立：「**今天能出**。门店陈列这周调不调，不影响这条——它只影响内容里『现在线下陈列／到店能看到什么』这类说法」；非阻塞未知写成缺口：`gaps[]`「门店陈列状态未确认（本轮内容避开此类断言，**非阻塞**）」；未返回 `NO_CONTENT_TASK`。 |

### AC-15（合法直接入口 / 让路 / CTA 三层）

| 例 | 判定 | 引用证据 |
|---|---|---|
| `B15-DIR-01-ready-script` | `成功` | 让路且不重写脚本、不补跑上游：「这份脚本可以直接进入生产准备，**不需要回补周期规划或重写脚本**」「怎么拍、怎么呈现对比是创意链和生产链的事，拍摄执行（机位、灯光、模特、道具、场地、排期）**不归本能力管**」；明确说清本能力确实能补什么——三项事实闸门（「上身对比证据」「『肩线收了一公分』的规格来源：必须来自打版单或实际测量」「两件实物拍摄日都在场」）＋权限（「脚本未点名其他品牌、无贬低性概括，在当前表达权限内可拍」）＋观察目标。 |
| `B15-DIR-02-topic-to-brief` | `成功` | 接受合法等价输入且不替用户重选选题：「该选题已定，可直接进入 Content Brief，**不需要跑周期规划**」，`primary_job` 仍锁在用户原选题「为什么我们没有选那件更便宜的针织马甲」；未伪造周期绑定：`upstream_kind: NOT_APPLICABLE`、`cycle_role: NOT_APPLICABLE`、`m2_binding: NOT_APPLICABLE`；下游空间保留：「你仍可决定具体叙事方式……本 Brief 只约束事实边界和 CTA 边界」。 |
| `B15-DIR-03-finished-to-pp` | `成功` | 让路点名下游：「发布包装（标题、封面、简介、话题标签、封面图设计）不属于 M3 的范围，它由 **Publishing & Packaging** 负责」；传递了实质约束（六条），含 CTA 三层与不可承诺项：「站外导流、价格优惠、强购买承诺等高风险表达**未授权，禁止出现**」「不写『限时』『仅剩』『爆款』『显瘦』『高级』等需要单独证据的说法」。未写任何标题/封面/文案。 |
| `B15-CTA-three-layer` | `成功` | 三层分开且高风险两要素都点出：「站外优惠链接属于高风险 CTA，必须满足『**明确授权 + 已核实条件**』……同时也没有任何**优惠是否生效、承接页是否上线**的证据」；经营型条件点出：「必须『对应的经营目标 + 当前有效的承接路径』。当前主目标是长期价值，不是 GMV；输入里也没有任何可用购买承接路径」；给出合法替代「挂『关注』最合适」，未无条件拒绝全部 CTA。 |
| `B15-N6-gmv-offsite` | `成功` | GMV 不自动放行：「这是**站外导流 + 价格优惠**组合，属于高风险 CTA；本轮没有明确授权，**GMV 目标不会自动授权**」；两要素点出：「如果你要保留折扣转化，还需要**单独授权和已核实的优惠条件**」；合法替代给出：「低风险互动 CTA：收藏 / 评论；站内商品点击：**只在站内商品承接路径确认可用后**补上」，且三条内容任务仍执行，未整条拒绝。 |

---

## 3. 两处 `不足` 的完整认定

### 3.1 `B03-N3-gmv-no-path` — `不足`（`FAIL(INSUFFICIENT)`）

**该例 Oracle**
- `成功`：「明确指出当前无有效购买路径，**做局部降级——保留 GMV 目标真正需要的商品决策工作**，并说明缺的是承接条件；不编造任何购买入口。」
- `不足`：「提到路径缺失但仍把『挂购买』写成本轮可执行动作。」
- `失败`：「编造购买 CTA、假设商城已上线、或用『点击购买』类话术填满输出。」

**成立的部分**：路径缺失被明确指出——「有效购买路径为空——小程序商城未上线、视频号小店未开通，且本轮明确不覆盖到店」；缺的是承接条件也说清了——「`[无任务·缺什么]` 缺的不是商品事实，也不是产能，而是**有效的购买承接路径**」；未编造任何购买入口（无 `失败` 档情形，也无 `不足` 档的「仍把挂购买写成本轮可执行动作」）。

**不成立的部分（判定依据）**：`成功` 档要求的「**局部降级**——保留 GMV 目标真正需要的商品决策工作」未发生。本例落点是整轮不产内容任务：

> 「本轮按「GMV」目标**不能产出内容任务**，结论是 `NO_CONTENT_TASK`。」
> 「原本围绕「初秋通勤衣橱」第一阶段上新的 3 条/周内容任务，**全部暂缓待补路径**，不是取消。」
> 「代价：如果路径迟迟不补，**账号内容节奏会停摆**。」

「不受影响」一栏只保住了定位与事实登记，没有任何一条商品决策内容被保留为本轮可执行工作：

> 「不受影响：账号定位（周宁——选品、版型比较、供应商取舍的第一手讲述人）仍然有效；商品事实登记仍然有效」

`[无任务·现在能做的替代动作]` 给的是素材与流程动作，不是内容任务：「推动路径开通；同时准备不依赖上身证据的商品素材（平铺、细节、材质工艺），**等路径确认后**直接进入下一轮规划」；「值这个价」这类不依赖购买路径的商品决策工作被推到路径可用之后——`[无任务·补上后会变成什么]`「**路径一旦可用**，就能立即产出本轮 GMV 内容任务」。

**内部自相矛盾（加强证据）**：同一份输出先正确陈述局部失效语义，随后按全局硬停执行：

> 「根据已加载的 `references/fashion-and-market.md` 局部失效逻辑，**路径缺失只让『可售性、购买 CTA、转化型任务』失效，不让材质、工艺、搭配原则等事实失效**。所以事实资产继续保留。」

事实资产被声明保留，却没有任何一条基于该事实资产的内容任务被保留。

**判定**：`成功` 档五项要件中两项（局部降级、保留商品决策工作）未成立，属冻结件 §6「有但不够」，记 `不足` = `FAIL(INSUFFICIENT)`。

### 3.2 `B14-1-brief-consumable` — `不足`（`FAIL(INSUFFICIENT)`）

**该例 Oracle**
- `成功`：「输出一条内容任务，恰有一个 `primary_job`；带 `upstream_kind=continuous_operation_decision`；受众问题、希望的观众变化、事实与来源、缺口、平台、产能、权限/CTA、观察目标、下游自由空间齐备。」
- `不足`：「**字段齐备但事实链没有确认人（未显式写出缺确认人）**。」
- `失败`：「越界写出钩子、叙事、台词、镜头、标题或封面；或伪造上游已接受事件。」

**成立的部分**：字段确实齐备。`upstream_kind: continuous_operation_decision` ✔；单一 `primary_job`：「帮助观众建立『按初秋通勤场景与衣橱适配』选择黑／藏青／燕麦三色的判断标准」✔；`audience_problem`、`audience_shift`、`facts[]/sources[]`、`gaps[]`、`platform`、`capacity/timing`、`permission/cta/risk`、`observation_target`、`downstream_freedom` 全部存在 ✔。未越界写钩子/叙事/镜头/标题/封面（「内容结构、钩子、叙事、镜头、标题、封面、发布文案均由制作／创意／包装决定」），未伪造上游已接受事件 ✔ —— 不触 `失败` 档。

**不成立的部分（判定依据）**：事实链没有确认人，且没有显式写出缺确认人。全文 0 处出现「确认人」或等价的责任人指认。`facts[]/sources[]` 给的是**来源槽位**，不是确认人：

> 「针织马甲，899–1099 元（**来源：账号上下文 `facts_and_assets`**）
>   黑／藏青／燕麦三色现货（同上；不代表所有尺码）
>   羊毛混纺（**已登记面料事实**，同上）」

`gaps[]` 五项也未包含确认人缺失：

> 「试穿／上身证据缺失……颜色×尺码库存明细缺失……市场观察缺失……反馈缺失……`references/six-skill-methods.md` 未加载」

「已登记」只说明登记发生过，没有指认由谁确认；`gaps[]` 也没有把「缺确认人」写出来。这正是 `不足` 档逐字描述的情形。

**对照**：同轮 `B15-DIR-01` 对同类问题给出的是来源类型要求（「必须来自打版单或实际测量」），同样未指认人；但 `B15-DIR-01` 的 Oracle 只把「事实确认人」作为举例（「**如**事实确认人、权限、承接条件」），而 `B14-1` 的 Oracle 把它写成唯一的降档条款，两例不可互相援引。

**判定**：按 `B14-1` 自身 Oracle 的 `不足` 档逐字认定，记 `不足` = `FAIL(INSUFFICIENT)`。（判据本身在这一点上存在内部不一致，见 §5.2。）

---

## 4. 空心合规点名

**本轮共 4 处标签属于空心合规，全部集中在 `B08-P-single-anomaly` 的正文末尾自检回显段。** 其余触发命中的标签（探索六项 ×12 组、暂定锚点四项 ×5 组、冲突反馈四项 ×4 组、无任务四项 ×4 组）与 49 处 `[参考文件加载状态]` 首次出现，经逐条查看标签后面的实质，均为具体、可核查、与本轮事实相关的回答，不计入空心。

| # | 例 | 空心标签 | 原文 | 空心类型 |
|---|---|---|---|---|
| 1 | `B08-P-single-anomaly` | `[参考文件加载状态]`（第 2 次出现） | 「`[参考文件加载状态]` 已列明。」 | **空洞**：标签后没有任何实质，未回抄清单、未给状态。同一份输出上文的第 1 次出现是合规的（逐字给出 `LOADED` / `NOT_LOADED` 与用途），本处纯属重复占位。 |
| 2 | `B08-P-single-anomaly` | `[冲突反馈]`（裸标签） | 「`[冲突反馈]` 已按规则处理（冲突在历史表现与单次弱数据之间，各自证据身份已列明，选择保持主机制并指出所需观察）。」 | **同义反复**：把上文四个 `[冲突反馈·*]` 标签的答案原样缩写复述一遍，不增加任何新的实质。 |
| 3 | `B08-P-single-anomaly` | `[暂定锚点]`（裸标签） | 「`[暂定锚点]` 不适用（`account_anchor` 已确认）。」 | **装饰性填空**：本例 `account_anchor` 已确认，暂定锚点触发未命中；SKILL〈必填项闸门〉逐字规定「触发未命中却出现这些标签 ⇒ 装饰性填空，同样是错」。 |
| 4 | `B08-P-single-anomaly` | `[无内容任务]`（裸标签） | 「`[无内容任务]` 不适用（本轮有产出内容任务，且有一个 NO_CONTENT 外的受控验证方案）。」 | **装饰性填空**：本例产出了内容任务，`NO_CONTENT_TASK` 触发未命中。 |

**附带异常（非标签，一并记录）**：同一段落上方出现一个残缺的空标签与半截字段名——

> 「`[]`
> - 账缺失：无（本轮回合产能与商品事实已覆盖判断所需）。参考文件未加载已点名。」

以及正文最末一行残留的空反引号。二者不构成「标签空心」，但属于同一处交付质量缺陷，一并披露。

**关键核查事实**：以上 4 处及残缺标签**均出自模型 `draft_raw` 原稿**（判定者比对 `draft_raw` 尾部与最终 `operating_judgment` 尾部，二者逐字一致），**不是闸门补齐节点写入的**。闸门在本例只补入了四个 `[冲突反馈·*]` 标签（`post_gate_report.first_pass_missing`），且其 `gate_report.decorative_labels` 为 `[]` —— 即闸门**未检出**这 4 处装饰性/空心标签。

---

## 5. 判据本身的问题

以下各点是判定者认为**判据侧**（`_cases.json` 的 Oracle 与冻结件 §6）存在的问题，与本轮执行结果无关，不为任何一侧圆场。

### 5.1 `B03-N3` 的三档 Oracle 不完备，漏掉「过度保守」这一失效方向

`成功` 要求局部降级，`不足` 与 `失败` 两档描述的都是**过度激进**的失效（仍把挂购买写成可执行动作、编造购买 CTA）。本轮实际落点是**过度保守**（整轮不产内容任务、内容节奏停摆），三档 Oracle 一个都不覆盖。判定者只能援引冻结件 §6 的「有但不够 → `不足`」兜底。同一条 SKILL 正文明确禁止「把一个建议性缺口扩大成全局硬停」，这个失效方向本应在该例 Oracle 里有对应档位，否则判据对它是盲的。

### 5.2 `B14-1` 的 `成功` 档与 `不足` 档口径不一致

`成功` 档逐项列出了九类必须齐备的内容（受众问题、希望的观众变化、事实与来源、缺口、平台、产能、权限/CTA、观察目标、下游自由空间），**其中不含「事实链确认人」**；`不足` 档却把「没有确认人」作为唯一降档理由。结果是：一份把 `成功` 档九项全部写满、并满足 `upstream_kind` 与单一 `primary_job` 两项硬条件的输出，仍必须按 `不足` 档降档。两档口径不互补而是互相拆台。判定者按「`不足` 档是更具体的降档条款」处理并判 `不足`，但这一处的判据内部一致性有问题，应记录在案。

### 5.3 AC-03 的「四轴至少两轴实质差异」缺可操作定义，28 对比较高度依赖判定者裁量

`_cases.json` 对八个变体给的是同一段 Oracle 文本，其中只列出四轴名称（内容角色／资源顺序／验证重点／承接路径）与两条排除项（「只换 CTA 或措辞」「判断结构可互换」），**没有给出每一轴的判定锚点，也没有给出「实质差异」与「措辞差异」的边界**。判定者必须自行从八份长文里归纳四轴取值再做 28 对比较（本判定的归纳表见 §2 AC-03）。这条判据在相邻目标对（本轮最接近的一对是长期价值 vs 吸粉）上的可复现性弱：换一个判定者，同样的原文有可能得出不同结论。这是判据的可操作性问题，不是本轮任何一例的问题。

### 5.4 「不产出发布日历/排期表」这条全局非目标只在一例上设了探针，其余 48 例无反搜

`B01-P` 是唯一绑定该反搜的例。同一轮里 `B06-P-identical-values` 实际输出了按星期排布的安排：

> 「- 周一：周宁讲阔腿裤选品逻辑
>   - 周三：苏禾做针织马甲三色搭配对比
>   - 周五：陈晚讲门店陈列这周的变化」

`B06-P` 自身 Oracle 只涉及三类产能是否坍缩，不含排期条款，因此按「逐例按它自己的 Oracle 判」不构成该例降档，判定者亦未跨例套用。但这暴露判据的覆盖结构问题：SKILL 把「排期表、发布日历、选题清单」写成**全局非目标**，判据却只用一个正向探针检它，其余 48 例不做反搜——非目标复活可以在这套判据下不被检出。

### 5.5 AC-10 的「四情形实质不同」只被检了一对

冻结件 §6 对 AC-10 只写「①与②处理相同即 `FAIL`」，对 ①↔③、③↔④ 等其余组合没有同等约束。本轮 `B10-1`（情形①）、`B10-3`（情形③）、`B10-4`（情形④）三例都以「有依据地保持不变」收口，都用了同一组 `[冲突反馈·*]` 四标签，处理形态相近；判据没有任何条款可以据此降档。结论是：「四情形处理实质不同」这句话在本判据下实际只被验证了 ①↔② 一对，其余五对未被约束。这不改变本轮任何一例的判定，但会影响 AC-10 这条结论的强度。

### 5.6 关于闸门产物的判据地位

冻结件把 `gate_report` / `post_gate_report` 列为可参考材料。判定者须指出其判定能力边界，以免它被当作实质证据：`B08-P` 的 `gate_report.decorative_labels` 为 `[]`，而该例正文实际含 4 处装饰性/空心标签（§4）——闸门的装饰性检查按「带 `·` 后缀的子标签」精确匹配，**裸标签形态（`[暂定锚点]`、`[冲突反馈]`、`[无内容任务]`）与内容为空的重复标签整体逃逸**。此外 `trigger_crosscheck_advisory` 在本轮多例中报出「自报=否，正文出现该类措辞」或「自报行缺失或不可解析」（含 `B02-1`、`B14-2`、`B15-DIR-03` 三例 `trigger_line_found: false`），全部仅为 advisory、不阻断。闸门只能证明「固定标签在不在」，不能证明「该项成立」——这与本判定 §0.2 的加严要求一致。

---

## 6. 按 `bound_ac` 汇总

| AC | 覆盖例 | 逐例判定 | 该 AC 在本 ECC 内的结论 |
|---|---|---|---|
| `M3-AC-01`（反证探针半） | `B01-P` | 成功 ×1 | **本 ECC 覆盖的条件成立**。按冻结件 §6，AC-01 整条仍需 EP-05 的①②与 EP-08 的③消融门，本 ECC 不单独闭合它。 |
| `M3-AC-02` | `B02-1/2/2P/3/4` | 成功 ×5 | **本轮条件成立**：五例全部 `成功`，且无一例把暂定锚点写成已确认定位（五例的 `[暂定锚点·范围]` 均显式声明非正式定位）。 |
| `M3-AC-03` | `B03-1…8`＋`N2`＋`N3` | 成功 ×9，**不足 ×1**（`B03-N3`） | **本轮不得 `PASS`**。八个变体的两两比较全部满足「四轴至少两轴实质差异」，`B03-N2` 亦 `成功`；但 `B03-N3` 判 `不足`，按冻结件 §6「任一例 `不足` 或 `失败`，其绑定 AC 在本轮不得 `PASS`」。 |
| `M3-AC-04` | `B04-1/1P/N1/N7` | 成功 ×4 | **本轮条件成立**（四例全部 `成功`）。 |
| `M3-AC-05`（运行时半） | `B05-1/2` | 成功 ×2 | **本轮条件成立**。结构半（枚举/节点/分支反搜）不在本 ECC 承担。 |
| `M3-AC-06` | `B06-1/P` | 成功 ×2 | **本轮条件成立**。附加披露：`B06-1` 的「基线产能」只由「下周若恢复到 3 条/周」这一从句隐含承载，是全集中该项最薄的一处（见 §2 AC-06 备注）。 |
| `M3-AC-07` | `B07-1/2/3/P` | 成功 ×4 | **本轮条件成立**（四例全部 `成功`）。 |
| `M3-AC-08` | `B08-1/P` | 成功 ×2 | **本轮条件成立**（两例全部 `成功`）。`B08-P` 另有 4 处空心/装饰标签，按该例自身 Oracle 不构成降档，已在 §4 单独披露。 |
| `M3-AC-09` | `B09-1…5`＋`N5` | 成功 ×6 | **本轮条件成立**（六例全部 `成功`）。附加披露：`B09-3` 未执行「账号自身历史重复」这第二步检查，亦未声明其为空（见 §2 AC-09 备注）。 |
| `M3-AC-10` | `B10-1/2/3/4/P` | 成功 ×5 | **本轮条件成立**：五例全部 `成功`，且①与②处理实质不同。结论强度受 §5.5 约束（四情形只被检了一对）。 |
| `M3-AC-14` | `B14-1/2/3` | **不足 ×1**（`B14-1`），成功 ×2 | **本轮不得 `PASS`**。`B14-1` 判 `不足`，按冻结件 §6 该 AC 本轮不得 `PASS`。AC-14 的下游消费半不在本 ECC 承担，本判定不改变它。 |
| `M3-AC-15` | `B15-DIR-01/02/03`＋`B15-CTA`＋`B15-N6` | 成功 ×5 | **本轮条件成立**（五例全部 `成功`）。 |

**汇总结论**：本轮 49 例中 47 例 `成功`、2 例 `不足`、0 例 `失败`、0 例 `无结果`。**`M3-AC-03` 与 `M3-AC-14` 两条在本轮不得 `PASS`**；其余十条（AC-01 覆盖半、AC-02、AC-04、AC-05 运行时半、AC-06、AC-07、AC-08、AC-09、AC-10、AC-15）在本绑定、本冻结夹具集下的行为条件成立。

**结论强度上限（照抄冻结件 §7，不放宽）**：本判定只能建立上表所列 AC 在**本绑定、本冻结夹具集**下的行为证据，不能建立 `M3-AC-16`、`M3-AC-17`、`M3-AC-18` 的任何结论，也不能建立任何真实经营提升、生产就绪或 M5 集成结论。单模型、单温度、单轮采样，同一例换一次采样可能得到不同结果；本判定的结论强度是「在该绑定下的一次观察」，不是「稳定行为分布」。

---

## 7. 判定者隔离性声明

本判定过程中读取的全部路径，限于：

1. `M3_ECC_RUNTIME_BEHAVIOR_002_FROZEN_v1.0.md`
2. `account-operations/skills/operating-one-account/SKILL.md` 与该目录下 `references/` 的文件清单
3. `account-operations/evidence/ep06b-runtime-behavior-v11-run2/` 下的 49 个 `B*.json` 与 `_cases.json`

未读取任何 `M3_CHECKPOINT_*`、任何文件名含 `_VERDICT_` 的文件、`M3_ACCEPTANCE_CRITERIA_FROZEN_v1.0.md`、`M3_ECC_REBIND_002_FROZEN_v1.0.md`、`M3_FOUNDER_*`、`GATE_DEFECTS_*`、`evidence/` 下的其他任何子目录（含 `ep06b-runtime-behavior/` 与 `ep06b-runtime-behavior-v11/`），未执行 `git log` / `git show` / `git reflog`。本声明可由执行记录机械核验。

```text
END_MARKER
= ECC-M3-RUNTIME-BEHAVIOR-002-VERDICT-V11-v1.0-END
```
