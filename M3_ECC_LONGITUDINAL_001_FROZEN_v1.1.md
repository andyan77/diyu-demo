# `ECC-M3-LONGITUDINAL-001` · 冻结判据 **v1.1**（后继版本，不覆盖 v1.0）

> `task_id`: `DIYU-V1-M3-ACCOUNT-CONTENT-OPERATOR-001`
> `supersedes`: `M3_ECC_LONGITUDINAL_001_FROZEN_v1.0.md`（原文保留，不删不改）
> `bound_ac`: `M3-AC-17`（主）、`M3-AC-11`、`M3-AC-10` —— **与 v1.0 相同，一个字未改**
> `frozen_at`: 本 commit（**先于本版本任何一次运行**）
> 唯一改动：**§3 链接规则**。其余各节（§1 命题、§2 无状态构造、§4 事件覆盖、
> §6 判定协议、§7 结果空间、§8 声明上限）逐字继承 v1.0。

---

## 0. 为什么要有 v1.1，以及它的 A2 状态（先说，不藏）

### 0.1 触发事件发生在看到结果之后

第 4 轮 `E07` 实测：模型一份 523 字符的零实质输出被 v1.0 §3 的**无条件逐字回填**
当作"当前有效判断"写进投影，把上一基线里的 `P1`/`P2`/`P3` 与四周节奏整体挤掉，
`E08` 起再未出现，**没有任何机制察觉**。

**这个改动是看到 E07 之后才做的。** 按 A2，看到结果后改判据，本轮该项只算探索、不产生正式 `PASS`
——除非有权威事件。下面两条是它的合法化依据，缺一不可。

### 0.2 权威事件

Founder 2026-08-26 `CONTINUE_TASK` 第 4 条（逐字）：

> `E07` 导致链式投影静默丢内容，先作为当前 M3 任务的 `AC-17` 阻断修复：
> 零产出不得覆盖上一有效周期状态；投影前后必须做关键对象/版本/数量一致性检查；
> 内容丢失必须显式失败，不得静默继续；增加 `E07→E08` 连续性回归。

这是 A1 意义上的有权者决定，构成 A4 的 `REBASE`：同一 `task_id` 下建立新合同哈希、继承旧证据。

### 0.3 v1.0 §3 自己已经写明它模拟的是什么

v1.0 §3 在写死"逐字回填"之后，紧接着解释：

> 这条回填在语义上对应 Prompt §10.1 的最后一环：*"经相应权限和**接受规则**后，由 M2 保存当前有效判断"*。
> 本 ECC 模拟"用户接受了上一轮候选"。

**执行侧不主张这句话本身构成改法授权。** 它解释的是模拟前提，不是许可条款。
它能说明的只有一件事：v1.0 的实现（无条件回填）与它自己声明的语义（经接受规则后回填）之间
本来就有落差，`E07` 把这个落差变成了实测事故。**授权来自 §0.2，不来自这句话。**

### 0.4 不追溯改判

- 第 4 轮 `M3-AC-17 = FAIL(INSUFFICIENT)`（`E04` `不足`、`E07` `失败`）**保持原判，不被本版本追溯改写**；
- 第 4 轮 12 步的原始记录逐条保留在 `evidence/ep07-longitudinal-v11-run2/`，不删不改；
- 本版本下取得的任何结论，只能建立"**在带接受规则的投影下**纵向序列可复现"，
  **不能**追溯建立"v1.0 那条无条件回填规则下也可复现"。两者是不同绑定，不得混用。

---

## 3. 链接规则（v1.1，机械，冻结）

第 1 步的 `standing_cycle_baseline` = `"尚无 —— 本账号还没有被接受的周期基线"`。

第 N 步（N>1）的 `standing_cycle_baseline` 由**三条互斥分支**确定，全部机械可复算、全部显式记录：

```text
输入：prev            = 第 N-1 步之前的有效基线
      out             = 第 N-1 步模型最终答复正文（仅剥离第一个 <think>…</think>）
      carry           = 第 N-1 步载体输出的 cycle_state_carry
      objects(prev)   = 从 prev 抽出的关键对象（任务位号 P\d+、节奏 N 条/周）

分支 1  carry = REJECTED_KEEP_PREVIOUS
        ⇒ standing = prev（**不覆盖**）
        ⇒ 记 mode = KEPT_PREVIOUS，并记下拒绝理由
        —— 这是 Founder 第 4 条第 1 项"零产出不得覆盖上一有效周期状态"

分支 2  carry = ACCEPTABLE 且 objects(prev) 中存在本轮未重述、也未被点名处置的对象
        ⇒ standing = out + 机器写的一段：prev 里提到这些对象的原句，**逐字保留**
        ⇒ 记 mode = MERGED_KEEP_PRIOR，并逐个列出被保留的对象与原句
        —— 这是第 3 项"内容丢失必须显式失败，不得静默继续"：不静默丢，也不静默补，全部列出

分支 3  其余
        ⇒ standing = out（整体替换）
        ⇒ 记 mode = REPLACED
```

每一步都记录 `objects_before` / `objects_after_carried` / `objects_disposed` /
`objects_not_restated` / `chars_before` / `chars_after`
—— 这是第 2 项"投影前后必须做关键对象/版本/数量一致性检查"。

**三条约束不变**：执行侧不改写、不摘要、不挑选 `out` 的任何一个字；
被保留的 `prev` 原句逐字复制，不重述、不概括；
`<think>` 剥离规则与 v1.0 逐字相同。

**执行侧的承载决定不单信载体**：harness 独立复算一遍 `carry` 与关键对象集，
与载体返回值不一致时两者都记录，不静默采信任何一边。

### 3.1 为什么"未重述"不判交付失败

`O-11` 要求限定影响面，`O-9` 允许"有依据地保持不变"。
一份聚焦回答（例如 `E04` 只回答"这个市场机会跟不跟"）本来就不该把整份基线抄一遍。
把"本轮没重述"判成交付失败，会把闸门变成"每轮必须全量重写"，与产品语义相反。

因此**未重述不阻断交付**，只改变投影方式（分支 2）。
交付侧唯一的阻断来自最低实质产出硬门——那才是"零产出"。

---

## 5. 运行绑定（锁定变量，v1.1 重绑定）

```text
carrier      = Dify Workflow 候选 App b7fb5b1a-9278-426c-bb8a-f9f288639548（载体 v1.2 图）
provider     = langgenius/deepseek/deepseek
model_id     = deepseek-v4-flash
temperature  = 0.4
skill        = SKILL.md carrier_revision v1.2
projection   = 本文件 §3
```

**任一锁定变量变化 ⇒ 整条序列置 `STALE` 并整条重跑；序列不可局部重跑**（与 v1.0 相同）。
本版本同时更换了 carrier 与链接规则，因此 **12 步全部重跑**，不复用第 4 轮任何一步。

---

## 逐字继承 v1.0 的部分

§1 命题、§2 无状态构造与其上限、§4 事件覆盖表（12 步对 ≥9 类事件）、
§6 判定协议六条（含三条序列级判定：局部性 / 证据身份分离 / 责任边界）、
§7 成功/不足/无结果/失败四态、§8 声明上限四条——**一个字未改**，见 v1.0 原文。

### §7 的一处必须说清的读法（不是改动）

v1.0 §7「无结果：Dify 或模型不可得导致序列未跑完 → `NOT_VERIFIED`」。
v1.1 的分支 1 会让一步"没有可承载的新判断"而序列继续走下去——
这**不是**"未跑完"，该步照常判定、照常计入。序列只在**传输层**失败时中断。

```text
END_MARKER
= ECC-M3-LONGITUDINAL-001-FROZEN-v1.1-END
```
